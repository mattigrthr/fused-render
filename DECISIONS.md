# fused-render — Decision Log & Project Context

Purpose: this file + `SPEC.md` + `ARCHITECTURE.md` carry the *entire* design conversation.
A fresh session should be able to continue the project from these three files alone.

---

## What we are building (one paragraph)

A fully local file explorer: a Python server on `127.0.0.1` + a browser UI to browse the **entire computer's** filesystem and preview files. The core primitive is **renderable HTML**: any `.html` file previews live and gets an injected JS runtime with (a) `fused.runPython(pyPath, params)` — executes the `main()` function of a local Python file and returns its JSON result — and (b) `fused.params` — string key/values synced into the browser URL, so any view's state is bookmarkable/refreshable. **Preview templates** (parquet table, image, text …) are just renderable HTML files shipped inside the app that receive the target file as a reserved `_file` param — the same two primitives are the entire rendering power of the system. Local only, forever; never cloud — reframed, not overturned, by mounts (D102): the app itself still only ever reads local absolute paths; remote storage enters exclusively as an rclone-managed *mount* presenting as a local folder, so nothing downstream of the mount layer speaks a cloud protocol.

## Project timeline / conversation state

- 2026-07-03: initial spec drafted (SPEC.md), broad v1 with security layer, decorators, worker pools.
- 2026-07-03/04: extended design discussion trimmed it down hard (all decisions below).
- 2026-07-04: M1 scope locked; blueprint written (ARCHITECTURE.md); build delegated to a subagent with the main session architect-reviewing.
- 2026-07-04 (later): M1 shipped + verified; M2 sidebar/bookmarks shipped; dark theme, sortable listing, `_file` URL cleanup, no-cache, shell.js ES-module split (D23–D28). An authoring skill for agents lives in `skills/fused-render-authoring/` with an eval workspace beside it.
- Pre-existing partial code: `fused_render/_child.py`, `fused_render/executor.py`, `fused_render/__init__.py`, `pyproject.toml` (written before discussion finished; kept — they match the final design).
- 2026-07-07: shell migrated to React (D52) on the owner's direction; templates/runtime/server contracts unchanged.
- 2026-07-27: appearance follows the OS by default with a System/Light/Dark pin (§30, D134); the Linux tray glyph follows the desktop panel's colour scheme (D135).
- 2026-07-10: M16 pinned view specced + shipped (§25, D97): menu-bar popover with a live WKWebView of one pinned file's `/embed` view, detachable into a floating always-on-top window. Same day, D98: dropped the status-item menu entirely — the popover (with a native header row of all actions) is the app's single surface.

## Decisions (all explicitly made by the project owner)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D1 | Python entry point | Function named `main()` — plain convention, no decorator, no import | Rejected `@render` decorator: convention simpler, file works standalone |
| D2 | Filesystem scope | **Whole computer. No root-scoping concept at all.** `--start-dir` is only the initial UI location | Rejected serve-root model entirely |
| D3 | Security | **None in v1.** Only freebie kept: bind 127.0.0.1. Token auth, Origin/Host checks, sandboxed iframe = documented follow-ups (SPEC §9 keeps the threat note for later) | Owner: "forget about security, make it as simple as possible; all things are follow-ups" |
| D4 | HTML isolation | Originally chose sandboxed iframe + postMessage bridge; **superseded by D3** → plain same-origin iframe, runtime fetches server API directly | Bridge protocol deleted from scope |
| D5 | Python execution | Fresh subprocess per call, 30 s timeout | Rejected warm pool (later upgrade, API-compatible) and in-process (crash risk) |
| D6 | Return types | JSON-native only; DataFrame/bytes → clear error telling user to convert | Arrow/DataFrame encoding = follow-up for data-heavy templates |
| D7 | Param values | Strings only. `set()` rejects non-strings. User JSON-encodes himself | Rejected auto-typing (footguns) and JSON escape hatch (unneeded now) |
| D8 | Param↔URL sync | URL wins on load; first param write on a pristine history entry pushes one entry (flagged `fusedParamEntry` on `history.state`), every later write replaces — Back restores the as-loaded params, churn costs at most one entry per visit. **AMENDED BY D268:** "first write" means the first write attributable to a USER GESTURE; a write made before this document has seen any interaction (a page seeding a computed default at load) replaces instead and leaves the entry pristine, so the push is still there for the user's first real change. **EXTENDED BY D271:** a caller may ask for that same replace path explicitly (`set(k, v, {history: "replace"})`), because the gate is read when the write LANDS and a boot path continuing behind an `await` is already post-gesture | Was "`replaceState` always, no entries". First-change-push chosen over settle-debounce (timers/heuristics) and per-param opt-in (config burden). Flag on `history.state` travels with the entry, so after Back to the pristine entry the next *user-caused* write correctly pushes again. Read the last clause with D268: unqualified, it says a re-seed after Back re-pushes — which is exactly the trap D268 closes |
| D9 | Py path resolution | Relative → against the HTML file's dir; absolute → anywhere on disk (consistent with D2) | |
| D10 | Re-run on param change | Manual: author calls `runPython` inside `params.onChange` | Declarative binding = possible later layer |
| D11 | Template registry | **Server-side** (single source of truth). `stat` response carries `template` field; shell obeys | Rejected shell-side JS registry (migration pain once user overrides exist) |
| D12 | Shell dispatch | Exactly three-way: **template > html > fallback**. Shell has zero file-type special cases; image & text are ordinary templates | Owner explicitly moved image/text out of the shell into templates |
| D13 | M1 template set | parquet (paged table via pyarrow) + image + text. All other formats follow up | |
| D14 | Python env | The env the server was launched from. Good enough for v1 | Per-project venv detection = maybe later |
| D15 | Streaming results | Not required. Plain request/response | |
| D16 | Frontend stack | No framework, no build step; plain JS/CSS | Descoped Vite/React from original spec |
| D17 | Error DX | runPython failure the page doesn't catch → red traceback overlay auto-shown in iframe; python `print()` echoed to browser console | Architect default, unobjected |
| D18 | pyarrow | Normal dependency (not extras) | Simplest install UX; revisit if weight bothers |

### M2 decisions (2026-07-04, post-M1)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D19 | Next feature | Left sidebar: Home entry + bookmarks of the current right-side view | Owner-defined scope |
| D20 | Bookmark semantics | Save the **exact URL verbatim** at capture time (incl. all params); click = plain redirect; renamable + deletable | No structured bookmark model — URL is the whole state by design (PR-1/PR-7) |
| D21 | Bookmark storage | Browser localStorage (`fused.bookmarks`) | Owner chose simplest over server-side JSON file; export path trivial if migrated later |
| D22 | Bookmark naming | Default = basename of viewed path; rename covers the rest | Rejected params-in-name (noise) and prompt-at-create (friction) |

### Post-M2 refinements (2026-07-04, all owner-directed unless noted)

| # | Decision | Choice |
|---|---|---|
| D23 | Look | Dark theme everywhere; single palette via CSS vars; "Raw" header action removed (fallback card keeps a Download link) |
| D24 | Bookmark UX | "+ Bookmark" text button (not a star glyph); hover card shows target path + saved params; active bookmark + starred button highlighted (architect) |
| D25 | Listing sort | Sortable columns; sort state in URL params, dirs group first (URL-is-state philosophy) |
| D26 | `_file` plumbing | `_file` rides on the template iframe's own URL, not the shell URL (no path duplication); runtime falls back to shell URL for manual/legacy links |
| D27 | Caching | `Cache-Control: no-cache` on every response — stale JS caused half-old-UI bugs |
| D28 | Frontend structure | shell.js split into ES modules (see ARCHITECTURE §1/§6): one-way deps, pure store/format modules, no build step kept (architect, owner asked) |

### M3 decisions — DMG distribution (2026-07-04)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D29 | Packaging | Every desktop build ships its own CPython 3.12 and runs `python3 -m fused_render.app` — but from a **different source per platform**: macOS copies Homebrew's **framework** python into the .app, Linux and Windows unpack **python-build-standalone** (`cpython-3.12*`, uv-managed) | Rejected PyInstaller freeze: breaks subprocess executor (sys.executable) + seals env. **Corrected Jul 2026** (flagged stale in D177): this row used to say the .app bundles python-build-standalone, which was never true — py2app builds a standalone .app by copying a `Python.framework` tree, and a standalone build has no framework layout to copy, so `build_dmg.sh` hard-FATALs on a non-framework interpreter (`PYTHONFRAMEWORK` empty). The inaccuracy was not free: it is part of why D175's `PYTHONHOME` problem was a surprise, since the framework copy is exactly what lets the bundled interpreter self-locate its own `sys.prefix` — the property `app_interpreter()`'s probe now verifies instead of assuming (D172) |
| D30 | User py env in app | Bundled interpreter ONLY, no override; pyarrow+pandas preinstalled | Owner chose hermetic-simplest over override-setting; supersedes D14 for packaged app (dev installs keep D14) |
| D31 | Lifecycle | Menu bar icon via rumps: Open / Copy URL / Quit; LSUIElement, no Dock | Rejected fully-headless (invisible process) |
| D32 | Signing | Ad-hoc unsigned for now; signing/notarization hook left in build script | Developer ID ($99/yr) deferred until public distribution. **(Realized by D73: Developer ID signing + notarization now implemented in `build_dmg.sh`, credential-gated with ad-hoc fallback.)** |

### M3.5 decisions — packaging rework (2026-07-04, after owner-requested research)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D33 | Bundle builder | **py2app** replaces the hand-rolled shim+tarball assembly | Canonical rumps packager; modern py2app ships a real python interpreter in-bundle so `sys.executable` subprocess executor works; its stub executable gives proper LaunchServices/AppKit process identity — hand-rolled bash-shim launches were the likely cause of flaky NSStatusItem/event behavior under Finder |
| D34 | Dock presence | **Regular app (no LSUIElement)** — Dock icon + menu bar ✦ both | Owner expects Dock presence; Dock right-click → Quit permanently solves lifecycle confusion. Supersedes D31's LSUIElement detail (menu bar item stays) |
| D35 | DMG creation | `dmgbuild` (config-driven) replaces raw hdiutil; **Briefcase external-app mode** = designated future path for sign+notarize+DMG when Developer ID lands | Full Briefcase rejected: its app template breaks sys.executable. **(Superseded by D73 for signing/notarization: done in-place with `codesign`/`notarytool` in `build_dmg.sh`, not Briefcase. `dmgbuild` for the DMG itself stands.)** |

### Editing decisions (2026-07-05)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D36 | Cross-origin POST guard | The two mutating/executing endpoints (`POST /api/run`, `POST /api/fs/write`) require a custom `X-Fused: 1` request header; missing/wrong → 403 | Read endpoints are already safe cross-origin (the browser blocks a foreign page from reading our response), but a POST can be fired blind via a no-cors fetch by any website open in the same browser. A custom header forces a CORS preflight, which fails cross-origin since we send no CORS headers, so only our own same-origin pages get through. **NOT authentication** (D3 stands — no tokens, no user accounts): it only blocks blind cross-origin POSTs; anything that can already run our JS is unaffected. Cheap and removable if the descoped security layer lands. |
| D37 | File editing | `POST /api/fs/write` (atomic temp-file + `os.replace`, preserves mode) with optimistic locking on `st_mtime`; `code_template.html` is now an always-editable CodeMirror buffer (Save / Cmd+S, conflict banner offering Reload/Overwrite) | Supersedes SPEC §1 non-goal "no editing in v1". Optimistic lock (409 on stale/deleted mtime) chosen over no-lock (silent clobber) and over server-side locking (needs state, D-against SV-1). HTML "Source" view reuses the same editable template, so HTML editing came free |
| D38 | Bookmark updating | Opening a bookmark **via sidebar click** arms it (sessionStorage `fused.armedBookmark`); when view params diverge from the saved url on the same page, an "Update bookmark" button appears left of "+ Bookmark" and overwrites the bookmark's url with the current one. Changing pathname disarms **permanently** — only a new sidebar click re-arms (owner-chosen over re-arm-on-return). Shell observes param changes by wrapping `history.replaceState` (the iframe runtime writes params through it; no native event exists) | Keeps D20 verbatim-URL model — update = overwrite url, no structured diffing. sessionStorage so tracking survives refresh but not new tabs |

### Embed mode (2026-07-06)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D39 | Embed mode | `/embed/<path>?params` serves the same shell chrome-free: sidebar/breadcrumb/preview-header hidden via `body.embed` CSS, sidebar never initialized. Router prefix is dynamic (`/embed/` vs `/view/`, fixed at page load), so refresh, in-listing navigation, and param sync stay inside embed. View modules stay embed-unaware — CSS does all hiding | One server route + prefix switch reuses the whole shell; rejected separate embed page (duplication) and query-flag (`?embed=1` would collide with the user param namespace, D7/D8) |
| D40 | HTML view mode param | Rendered/Source toggle driven by reserved shell param `_mode=render\|source` (absent = render, and switching back to render deletes the key). Clicks write it via `history.replaceState` (D8); `?_mode=source` on view/embed/bookmark URLs opens straight into source | URL-is-state (D8/D20/D25): mode was the last piece of view state living only in JS. `_`-prefix already hidden from `fused.params` by the runtime, so no collision with user params |

### M4 — Live editing: autosave + auto-reload (2026-07-06, full spec = SPEC §13)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D41 | Editor autosave | code_template autosaves 250 ms after last edit (debounced), always-on, no toggle. Same optimistic lock as manual save; 409 → conflict banner shows and autosave suspends until resolved — never auto-overwrites. Manual Save/Cmd+S stay and cancel the pending timer | Always-on chosen over a setting: live-preview loop is the point; half-typed states reaching disk are normal (D17 overlay explains broken intermediates). Auto-overwrite rejected — would reduce the lock to decoration |
| D42 | File-change feed + auto-reload | New `GET /api/fs/events?path=A&path=B` SSE endpoint (async 200 ms stat loop, no watchdog dep, no X-Fused — read-only). All reload logic lives in the injected runtime: each rendered page watches its own file + `_file` + every `runPython` file (learned from new `resolved_py` field on `/api/run`, recorded on failures too) and reloads itself on change (300 ms debounce). `fused.autoReload(false)` opts out — the editor uses it; watching starts at DOMContentLoaded so the opt-out is race-free. Directory listings watch their dir and re-render | Full iframe reload over fine-grained re-run: runtime can't replay what a page did with a python result, and URL-is-state (D8/D20/D25) makes reload lossless. Runtime-side dependency tracking over a server-side dep map (server state/staleness for knowledge the page already has) and over static html analysis (misses dynamic paths). SSE over client polling (chatty) and over WebSocket (two-way machinery for one-way events) |

### Bookmark reordering (2026-07-06)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D43 | Bookmark drag-reorder | Native HTML5 drag-and-drop on sidebar rows; localStorage array order **is** display order (no `order` field, no schema change) — `moveBookmark(id, postRemovalIndex)` splices. Whole row draggable (link gets `draggable="false"` so the browser doesn't drag the URL); insert indicator via box-shadow (no layout shift); drag disabled during rename; tooltip suppressed while dragging. `draggedId` reset in `drop` as well as `dragend` — Chrome skips `dragend` when the re-render detaches the source row | Native DnD over vendoring Sortable.js (~40 KB for one short vertical list) and over pointer-event reimplementation (more code, same result). No drag handle — whole row is a bigger target and rows have no competing drag affordance |

### Bookmark folders (2026-07-06)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D44 | One-level bookmark folders | Drop a bookmark on the middle of a top-level bookmark → folder created around both (rename starts immediately). Same localStorage array, folders are `{type:"folder", name, collapsed, children:[bookmarks]}` — old flat data loads unchanged, no migration. Drop zones: 25/50/25 (above/into/below) where combine is possible, half-split otherwise; folders reorder at top level only, never nest; child rows ignore folder drags. Empty folders auto-pruned after every mutation (move, delete, create). Collapse persisted. Folder UI: inline-SVG folder icon in the star-glyph slot (icon + name toggle fold, no chevron), semibold name, child-count pill pinned far right that swaps for the hover actions, children indented behind a 1 px rail; collapsed folder holding the active bookmark gets the active highlight. **(One-level limit superseded by D121 — folders now nest to arbitrary depth; the UI language here otherwise stands.)** | One level deep over arbitrary nesting: schema already recursive so depth is pure UI work later, but nested drop-target semantics (cycle guards, indent-sensitive zones, collapsed-edge ambiguity) ~2.5× the cost for a rarely-needed depth. `children` array over flat `parentId` (rendering + reorder messier). Chevron removed after review — icon alignment with star glyphs reads better in a narrow sidebar; count pill carries the collapsed signal |

### M5 — Layout mode: split panes (2026-07-06, full spec = SPEC §14)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D45 | Layout mode | `/view/_panel?_layout=<tree>&<merged params>` (also `/embed/_panel` — layout mode works under both prefixes, so a layout can itself be embedded) — sentinel pathname intercepted by the shell `route()` before stat (zero server changes). Pane tree in reserved `_layout` param: `,` = row, `;` = column, `()` nesting; each segment = pane fs path + optional pane-local query, delimiter chars percent-encoded. Panes are `/embed/<path>` iframes (D39) with a pane bar: crumbs, split right/down (duplicates pane), maximize (transient, not in URL), close (a close that leaves one pane — or closing the last pane — exits to `<prefix>/<pane path>` in the active prefix; single-pane layouts exist only via hand-typed URLs). Entry = split right/down icon buttons in crumb-actions carrying current path+params (row `,` / col `;`). Layout view observes pane URLs (iframe load + pane `fused:urlchange`) and re-encodes `_layout` via replaceState — so bookmarks (D20) and the update-bookmark flow (D38) capture layout+params verbatim with zero bookmark-layer changes. New module `views/panel.js` (imports router only) | Reusing embed pages makes every pane a full navigable mini-explorer for free; rejected panes-as-bare-`/render`-iframes (loses listing/navigation/templates dispatch). Sentinel path over a new top-level route (`/layout`): stays inside the `/view`+`/embed` prefixes the server and router already handle. Sentinel pathname `_panel`, tree param `_layout` (sentinel renamed same-day from `_layout`, no backward compat kept). Reserved `_layout` param over `?layout=`: `_` prefix is already hidden from `fused.params` (PR-6), no user-param collision |
| D46 | Runtime param target | `fused.params` reads/writes the **topmost same-origin ancestor** window's URL (was: direct parent) — in normal view/embed, parent = top, so nothing changes; in layout mode all panes share the layout URL, so param merging and same-key cross-pane sync are structural. Notification: shell wraps pushState too (was: replaceState only) to dispatch `fused:urlchange`; runtime listens on the target window and notifies `onChange` only when the non-reserved param snapshot actually changed (diff guard kills loops/dupes; `set()`'s direct notify removed in favor of the event path) | Chosen over a manual sync layer in the layout module (watch each pane URL, merge up, propagate down, loop-guard — more code, more failure modes). Trade-off accepted: params are global session state by definition; pages wanting pane-private state namespace their own keys (documented, not enforced) |

### Post-M6 audit cleanup (2026-07-06)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D49 | Layout-family naming + codec ownership | Vocabulary line drawn: **"layout" = the family** (the `_layout` param, `views/layout-codec.js`), **modes name themselves** (`renderPanel`/`stopPanel`/`panelUrl`/`.panel-*`/"Panel" label; tabs already consistent). Codec owns everything both modes share: the `_layout` codec, embed URL helpers, and the `fused:urlchange` hook (`attachEmbedUrlChange`/`detachEmbedUrlChange`, one expando `_fusedUrlHooked`); it imports `router.js` only (for the single `EMBED_PREFIX` — its duplicate + dead export deleted). Breadcrumb imports the segment encoder from the codec directly (panel.js re-export removed). Panel pane DOM now built via `createElement` like tabs (drops the escapeHtml/innerHTML template) | Audit findings (architect + reviewer, both flagged the re-export and the duplicated hook machinery). Rejected from the same audit: pane registry Map over DOM `data-id` lookups (vanilla idiom, indirection for nothing); centralizing the `_fusedParamBoundary` flag outside tabs.js (would leak tab knowledge into main.js; contract already documented in runtime/tabs/ARCHITECTURE) |

### M6 — Tab mode + folders-as-tabs (2026-07-06, full spec = SPEC §15)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D47 | Tab mode | `/view/_tab` + `/embed/_tab` sentinel (same interception as `_panel`, zero server changes); tab list = flat top-level `,` row of the same `_layout` codec (nested structure defensively flattened to leaves on parse). Tabs are `/embed/<path>` iframes, **lazy-mounted on first activation, kept alive hidden** — state survives switching, hidden tabs stay param-synced (runtime listens on top window, D46). Params are **tab-independent — no merged pool** (inverts D45's pooling for this mode): the tab shell sets `window._fusedParamBoundary = true` and the runtime climb (D46) stops below a boundary-marked ancestor, so each tab's pages target their own embed URL; the full pane query (user params included) is captured segment-local by the ordinary `_layout` sync. A segment path may itself be `_panel`/`_tab` — nested layouts flow through the ordinary embed pipeline; a nested panel keeps merged-pool semantics among its own panes (climb stops at the panel shell) while isolated from other tabs. Tab bar: label = basename of live path, per-tab ×, trailing `+` (new tab at start dir); **active tab NOT in URL** (refresh/bookmark restores first tab). Last tab close exits to plain view. Codec extracted to shared `views/layout-codec.js` (panel.js, new tabs.js, breadcrumb.js import it) | Keep-alive over recreate-on-switch: preserves scroll/editor state; hidden tabs stay live for free. Tab-independent params (owner call, reversing the initial merged design): a folder of bookmarks must reproduce each bookmark exactly — pooling would let the last child clobber same-key siblings; the boundary flag keeps the isolation structural (one runtime `if`, no sync layer). Active tab left out of URL: encoding it would flash "Update bookmark" on every switch (churn > value). Reusing the `_layout` param + codec over a new `_tabs` param: one codec, one escaping story, and panel↔tab URLs stay hand-convertible |
| D48 | Folder opens as tabs | Clicking a bookmark folder's **name/row** composes and opens `/view/_tab?_layout=<children>`: each child's **entire saved query stays segment-local** (D47 — every bookmark keeps exactly its own params, no hoisting, no collisions). Opening also expands a collapsed folder (sidebar mirrors the tabs); the **folder glyph** keeps the plain collapse/expand toggle (was: name and glyph both toggled). Folder click arms nothing (a folder isn't a bookmark); ★ Bookmark saves the composed URL as a normal bookmark, which then gets the full D38 update flow | Folder-as-dashboard was the original ask behind tab mode; a tab must reproduce its bookmark verbatim. Rejected: breadcrumb "Tabs" entry button (folder-only entry, owner call); encoding folder identity in the URL (a tab layout is a value, not a live link to the folder — later folder edits don't retro-sync, by design) |

### M7 — Custom template overrides (2026-07-06, full spec = SPEC §16)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D50 | User template registry | Self-contained template folders `~/.fused-render/<name>/template.html` (+ sibling `.py` readers/assets; name = arbitrary label) bound to extensions by `~/.fused-render/registry.json`: dotted keys → folder name or `null` (`{".parquet": "geo", ".tar.gz": "archive", ".png": null}`). `null` disables the built-in (shell raw/metadata fallback). Matching: longest-suffix, case-insensitive (dotted keys give compound-extension support the built-in `splitext` table can't express); precedence registry > built-in; `.html`/`.htm` exempt (renderable HTML stays the core semantic). Registry read per resolution (no restart); missing dir/file = no-op; invalid entry (bad JSON, unsafe name, missing template.html) → built-in fallback + `template_error` on stat. Unregistered folders inert (drafts). Server-only change — shell already obeys stat's `template` path, `/render` renders any path, M4 auto-reload gives the live template dev loop for free. Companion repo skill `fused-render-custom-templates` documents layout+registration and delegates authoring to `fused-render-authoring` | Hybrid chosen over pure convention dir (folder-name-as-extension dead-ends on many-ext-to-one and edit-in-place) and over manifest of absolute paths (loses the self-contained folder unit). Folder names as binding (`mp4.mov.webm/`) rejected: renames to change mappings, conflict rules. Convention fallback (unbound folder named like an ext auto-binds) rejected: two sources of truth, deleting a registry line wouldn't unregister. Name validation is correctness, not auth (D3 stands) |

### Layout URL grammar — parenthesized `_layout` (2026-07-06, spec = SPEC §14.1 LM-2)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D51 | `_layout` scope grammar | The entire `_layout` value is **parenthesized and emitted last**: `?global=1&_layout=(/a.html?x=1&y=2,/b.html?z=3)`. Parens delimit param scope visually (inside = iframe-local, outside = global) and structurally: **`&` is literal inside them** — segment queries read exactly as written, no `%26` soup. Wrap is codec-transparent (`parseLayout` already reads `(A,B)` as `A,B`); only the URL layer changed. All shell-query reads go through `layout-codec.js` `splitShellSearch` (balanced-paren scan; safe — literal parens inside segments are codec-escaped, so span parens are structural and balanced): panel.js, tabs.js, breadcrumb's bookmark-equality `sameSearch`. The runtime (standalone, no imports) duplicates the scan as `splitSearch`: get/getAll parse only the non-layout remainder; `set()` reinserts the raw span untouched and last — also fixes the old wart where `URLSearchParams.toString()` percent-mangled a readable `_layout` on the first `fused.params.set()`. **Strict read** (owner call): unwrapped `_layout` = not this grammar, reads as absent, key dropped on next sync — old-format bookmarks fall back to a single start-dir pane/tab. **Paste breakage accepted** (owner call): auto-linkers may eat the trailing `)`; unbalanced span = invalid layout (span still excluded from params), missing-layout fallback | Chosen over "enforce `_layout` last as position-only convention" (look without guarantee; `&` stays escaped) and over "raw tail after `_layout=`" (position-dependent; trailing hand-added `&debug=1` silently swallowed into last segment — parens make position a convention and the boundary explicit, params after `)` stay global). Lenient read of old grammar rejected (owner call): one grammar, no two-format parse paths. Auto-closing missing trailing parens rejected with it: same leniency, masks truncation |

### Shell → React migration (2026-07-07, owner-directed)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D52 | Shell framework | The host shell (routing, listing, preview chrome, sidebar/bookmarks, breadcrumb, panel/tab modes) is a **React 18 + Vite app** in `frontend/` (plain JS + JSX, no TypeScript), built into `fused_render/static/shell-dist/` — **committed output**, like the vendored template libs, so `pip install` needs no node. Supersedes D16 **for the shell only**: templates, `/render`, and the injected `runtime.js` stay plain HTML/JS — the rendering primitive is unchanged; only the logic behind the product scaffolding moved to React. Server change is one line (serve `shell-dist/index.html`). Same DOM structure/class names → `shell.css` reused verbatim (moved to `frontend/src/`). Route model preserved: a navigation epoch remounts the active view (the React equivalent of the vanilla `route()` DOM rebuild); `fused:urlchange` re-renders chrome only. The history replaceState/pushState wrapping stays in the bootstrap — pane runtimes depend on it (D46). Layout-mode iframe discipline: src frozen at mount (React never rewrites it), tabs render as a flat keyed list that only appends/removes (never re-parents), `_fusedParamBoundary` set at render / cleared on unmount | Owner asked for the migration ("templates and main rendering stay HTML, the logic shifts to React"). Vite+JSX chosen over no-build React (htm/vendored ESM): htm-templated code is a worse authoring surface and the repo already has a node build precedent (`scripts/vendor-codemirror`). Committed dist over build-on-install: keeps pipx/pip UX and the local-first, no-CDN stance. Plain JS over TS: matches the existing style constraints; TS is a separate decision if wanted |

| D53 | Shell language | The `frontend/` shell is **TypeScript** (strict; `tsc --noEmit` gated in `npm run build`, Vite/esbuild does the actual transpile). `.ts` for logic (`src/lib/`), `.tsx` for components. API/store/codec shapes are typed at the source (`Config`/`StatResult`/`FsEntry` in `lib/api.ts`, `Bookmark`/`BookmarkFolder` in `lib/bookmarks.ts`, `LayoutLeaf`/`LayoutSplit`/`LayoutNode` in `lib/layout-codec.ts`); window expandos shared with the runtime (`_fusedParamBoundary`, `_fusedUrlHooked`) declared in `src/global.d.ts`. Templates and `runtime.js` stay plain JS (unchanged trust: they are the no-build primitive). Supersedes D52's "plain JS" detail; everything else in D52 stands | Owner asked ("can we convert this to typescript"). Strict over loose: the URL/codec layer is exactly where typos hide, and the codebase is small enough to keep strict cheap. @types/react pinned to ^18 to match react 18.3 |

| D54 | Shell build output | `fused_render/static/shell-dist/` is **not committed** (gitignored). Dev machines are expected to have node: run `cd frontend && npm run build` (or `npm run watch`); the server fails at startup with that exact instruction when the dist is missing. Shipped artifacts get the shell at package-build time: a hatchling build hook (`scripts/hatch_build.py`, wired in pyproject) runs `npm install && npm run build` when building a wheel, and `artifacts` overrides the VCS-ignore so the output ships; the DMG pipeline pip-installs this repo into its build venv, so it inherits the same hook. Editable installs skip the hook (dev owns the build loop). Supersedes D52's committed-output detail; owner call | Owner: "don't commit build files; node on dev machines is fine; shipping puts everything in the dmg or the wheel". Committed dist had the usual costs: binary-ish diffs on every shell change, drift risk between source and checked-in bundle |

### openfused engine migration (2026-07-07) — REVERTED

> **Reverted 2026-07-07 (D67):** D55–D58 rolled back — the openfused backend, `@fused.udf` contract, PEP 723 venvs, and wheelhouse are removed; `executor.py`/`_child.py` with string-coerced params and the `{ok, result, error:{type,message,traceback}}` wire shape are restored. Rows kept for history.

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D55 | Execution engine | **openfused** (dist `fused`) `LocalPythonComputeBackend` replaces `executor.py`/`_child.py` — new module `fused_render/engine.py`. Fresh subprocess per call retained (D5's isolation mechanism intact), 30 s timeout kept, result caching disabled (`cache_storage=None`). `requires-python` floor moves to **3.11** (tomllib for PEP 723 parsing; openfused's own floor). The dependency is a **direct-URL reference to the public nightly wheel** (`fused @ https://fused-magic.s3.amazonaws.com/fused-py-nightly/fused-0.0.0.post0-py3-none-any.whl` — owner call 2026-07-07; the wheel's filename version is deliberately unconstrained, the URL's content is what installs). Dev installs may override with the local sandbox checkout editable (`pip install -e ~/Documents/fused/sandbox/openfused`) | One engine shared with the fused platform instead of a bespoke runner; the backend interface gives a path to remote execution (Lambda/hosted) with no fused-render changes. Rejected keeping the hand-rolled executor alongside (two contracts to document and test) |
| D56 | Script contract | **`@fused.udf def main(...)`** replaces the bare `main()` convention (supersedes D1). Params arrive as **raw JSON types — no annotation coercion**; the calling JS owns types and passes numbers as numbers (inverts the old string-coercion model; see SPEC PY-4). Third-party deps are declared per-script via **PEP 723** `# /// script` inline headers, resolved by the backend into **cached per-requirements venvs** (uv, pip fallback, keyed interpreter+reqs under `~/.openfused/venvs`); no header = bare stdlib venv — the server's own env is no longer visible to scripts (supersedes D14/D30's "env that launched the server" model for user code). Plain `result = ...` scripts work for parameterless cases. New /api/run wire shape: see ARCHITECTURE §3 | udf registration is what lets the backend own param delivery and future remote transport; PEP 723 is the standard way to make a single .py carry its deps. Rejected keeping annotation coercion server-side: the engine no longer inspects signatures, and typed-JSON-from-JS is one rule instead of a coercion table |
| D57 | No backward compatibility | Pre-launch product: /api/run consumers, built-in templates, and examples migrated in place; the old `{ok, result, error:{type,message,traceback}}` shape, string-coerced params, and bare-`main()` support are **deleted**, not deprecated | Owner call 2026-07-07. One contract, no dual-shape parse paths (same posture as D51's strict read) |
| D58 | Packaged-app offline installs (wheelhouse) | The .app ships **`Contents/Resources/wheels/`** — wheels for the `[bundled]` extra list + pyarrow (cp312/arm64, transitive deps included), built by `build_dmg.sh` via `pip download`. `fused_render/app.py` sets **`PIP_FIND_LINKS`/`UV_FIND_LINKS`** at startup so openfused's PEP 723 venv builds resolve from the local wheelhouse first, falling back to PyPI when online (FIND_LINKS deliberately, not NO_INDEX — offline-first, not offline-only). Evolves DM-2/D30: the bundled interpreter still ships and runs the server, but user code no longer imports from its site-packages — the wheelhouse is what preserves offline first-use for the popular data stack | Without it, a fresh offline .app install could not run any script with a PEP 723 header (venv build needs an index). Known redundancy: the baked `[bundled]` site-packages are now dead weight for user code — flagged as a future slimming, not done in this change |

### M8 — Template modes (2026-07-07, full spec = SPEC §7/§16 PT-6..PT-12, CT-10..CT-11)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D59 | Folder-per-template + one namespace | Built-in templates refactor from flat files (`parquet_template.html` + `parquet_reader.py`) to **self-contained folders identical in shape to user templates** (D50): `fused_render/templates/<name>/` with `template.html` (required), sibling helpers (`reader.py`), optional `icon.svg`. Built-ins renamed to public names: `table` (was parquet), `csv`, `xlsx`, `tree` (was json), `markdown`, `image`, `media`, `pdf`, `code`, `text` — name = folder = registry ref = `_mode` value = tooltip label, **public stable API** once shipped. **One resolution rule everywhere:** name → `~/.fused-render/<name>/template.html` if present, else `fused_render/templates/<name>/template.html`, else unusable; user folder shadows a built-in by name (deliberate override channel). `vendor/` unchanged — no `template.html`, never resolves | One grammar for built-ins and user templates makes the mode-list registry (D60) expressible with plain names. Rejected: a mode name separate from the template name (name + label indirection) — one namespace, the template name is the mode; keeping flat built-in filenames as referenceable names (leaks `.html` suffixes into URLs/registry and blocks per-template icons/assets) |
| D60 | Ordered mode lists + registry splice | Extension mapping goes **1:n**: `TEMPLATES` becomes ext → ordered list of names, **first = default**; `code` appears as a secondary mode only where raw text is meaningful. Registry values (D50) grow to `list \| string \| null`: list = full ordered mode list, replace semantics; string = single-mode list (unchanged); `null` = disable (unchanged). **`"..."` splice token** expanded in place to the built-in list (**removed by D94** — `"..."` is now a dangling name, not a splice) — `.` is forbidden in names (CT-6) so it can't collide. Per-entry validation: unresolvable entries dropped + `template_error`; user value resolving to nothing → built-in list fallback. Stat replaces `template` with the **`templates` array** `[{mode, path, icon}]` — clean break, no compat alias (shell is same repo) | Code mode on binary formats (e.g. `.parquet`) rejected — garbage rendering. Merge/patch registry keys (`order`/`hide`/`default` objects) rejected for the flat list + splice: one value shape, replace semantics are predictable, and `"..."` covers "add without knowing built-in names" and future-built-in pickup without a merge algebra |
| D61 | `_mode` param + icon-only switcher | Non-default mode selected via reserved **`_mode=<name>` on the shell URL** (bookmarkable; same pattern as D40's render\|source toggle). Absent = default = `templates[0]`; selecting the default deletes the param; unknown/stale value → default, no error; switch swaps iframe src with a fresh document (`key={mode}`). Shell shows a switcher (right side of preview header) only when >1 mode: **icon-only buttons, name via `title` tooltip**, active = accent. Icons = **monochrome SVG masks** (`mask-image` + `currentColor`), loaded via existing `/api/fs/raw`; every built-in folder ships one; `icon: null` → shell-rendered first-letter placeholder in a rounded box. The `.html` Rendered\|Source pair migrates to the same switcher component (two fixed shell-baked entries; `.html` stays registry-exempt, CT-4). Accepted quirk: template params persist on the shell URL across switches — same-name params collide across modes, documented not prevented | URL-is-state (D8/D20/D40): mode is view state, so it rides the shell URL. Icon+label switcher buttons rejected by owner for icon-only + tooltip (header space; names are tooltips, not chrome). Mask-tinting over `<img>`/inline-SVG recoloring: active/inactive coloring free from `currentColor`, no per-state assets, no new endpoints |
| D62 | HTML modes via `_render` sentinel | `.html`/`.htm` get a real server-side mode list — `TEMPLATES[".html"] = ["_render", "code"]` — **hardcoded**, registry-exempt (CT-4 stands): users cannot rebind the extension or remove `_render`. A `_`-prefixed mode name is a **shell sentinel** (SPEC PT-12): no template folder behind it, resolver emits `{mode, path: null, icon: null}` without touching the filesystem; `_`-names in a registry list are invalid (dropped + `template_error` — sentinel namespace shell-owned). Shell: `HtmlPreview` deleted; `TemplatePreview` handles `_render` (iframe = `/render?path=<file itself>`, no `_file`; shell-baked eye icon) and filters unknown sentinels defensively; preview dispatch collapses to **two-way** (`templates` non-empty → TemplatePreview, else fallback — no extension checks in shell). `/api/config` drops `source_template` (shell `Config` type too) — code view arrives via stat like everything else. Old `_mode=source` bookmarks fall to default silently — accepted break, mode is now `code` | Framework flows through html like any extension (future html modes = add to server list); `_` prefix matches reserved-param convention (`_mode`, `_file`). Rejected: keeping the shell-hardcoded Rendered\|Source pair — no icon/framework flow, permanent special case in dispatch; making html registry-editable — would let users break the core render guarantee (§4). *(Revised by D73: the registry exemption is dropped — the `["_render", "code"]` default now ships in the built-in registry and users may rebind `.html`/`.htm`; the sentinel mechanics stand.)* |

### M9 — Python `api` mode (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D63 | `api` template for `.py` | New built-in template folder `templates/api/` giving `.py` files a **swagger-style run form**: `TEMPLATES[".py"] = ["code", "api"]` — **code stays default** (owner call), `api` one switcher click away. `inspector.py` (stdlib-only, no PEP 723 header → bare venv) statically parses the target via `ast` — **never imports/executes it** — returning module docstring, PEP 723 deps, and the **`@fused.udf` function's** name/docstring/params (last decorated function wins, matching the engine's `_registered_udfs[-1]` pick) (annotation source via `ast.unparse`; defaults via `ast.literal_eval`, non-literal defaults shown as source, never evaluated). Template renders annotation-driven widgets (`int`/`float` → number, `bool` → checkbox, `str` → text, other/unannotated → JSON textarea with string fallback) and coerces JS-side per PY-4 (typed JSON, no server coercion); empty optional inputs are **omitted** so python defaults apply. Execute posts **directly to `/api/run`** (not `fused.runPython`) because the response panel needs the full wire shape — stdout/stderr/duration — that the runtime helper flattens; response card shows pretty-printed JSON `return_value`, collapsible stdout/stderr, red traceback on error, duration badge. Inputs persist to URL params on Execute (URL-is-state → shareable pre-filled runs). Re-inspect on file change is free: the runtime already watches `_file` (LR-1) and reloads the frame. No `@fused.udf` function → docstring + "nothing to execute" notice; syntax error → parse error shown, code mode still works | Owner calls 2026-07-07: name `api` (over swagger/run/udf), code-first default, **only the `@fused.udf`-decorated function shown** (no read-only listing of other top-level functions — one file = one entry point, SPEC §5), JSON-only result rendering v1 (rich renders for images/tables = follow-up), re-inspect on change. AST inspection over importing the module: imports execute top-level code and need deps installed — a doc view must be side-effect-free. Direct `/api/run` fetch accepted over extending runtime.js with a rich-result API: template-local need, no new runtime surface |

### Scientific-file templates — GeoTIFF / NetCDF (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D64 | GeoTIFF / NetCDF preview templates + vendored sci decoders | Two new template folders (`geotiff/`, `netcdf/`, each `template.html` + `icon.svg`) adapted from Fused's battle-tested standalone sci viewers, restyled to the shell palette; input is the reserved `_file` param (drag-drop/URL/help chrome dropped). Decode is fully in-browser — no `reader.py` — via decoders vendored as single self-contained ESM bundles under `templates/vendor/` by `scripts/vendor-sci/build.sh` (Node 22 + esbuild, `--format=esm --minify`, **no `--splitting`** so one file per lib) — no CDN at runtime (D3). `.tif/.tiff → ["geotiff"]`, `.nc/.nc4/.cdf → ["netcdf"]` join `TEMPLATES` (binary formats: no `code` mode, same reasoning as `.parquet`, D60). GeoTIFF full-fetches small files and range-requests (`geotiff.fromUrl`) over the raw endpoint for >32 MiB (Starlette FileResponse honors Range). The sciViz core the two templates share (colormaps, stretch/stats/histogram, canvas draw, UI kit) is first-party ESM at `templates/shared/sciviz.mjs`, served from a dedicated `/template-shared/*` mount so `vendor/` stays strictly third-party; `shared/` has no `template.html`, so it can never resolve as a template name (PT-6). Zarr (a *directory* store — needs directory-template routing, a server/shell semantics change) is split into its own follow-up PR per review on the original combined PR. | Adapt the standalone viewers verbatim (7 rounds of browser QA) over rewriting decode/render. Only byte-identical helpers were extracted to sciviz.mjs; divergent helpers stay in-template. Dedicated `/template-shared` mount over nesting under `templates/vendor/`: vendor/ stays third-party-only |

### Zarr template + directory-template routing (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D65 | Zarr preview template + directory-template routing | Zarr stores are *directories*, so preview needs the shell to render a template for a directory. A new **`DIR_TEMPLATES = {".zarr": ["zarr"]}`** table mirrors `TEMPLATES`'s ext→ordered-name-list shape (D60): when `_templates_for` is called with `is_dir`, it resolves the extension on the directory's basename (`os.path.splitext(os.path.basename(os.path.normpath(path)))`) through the **same `_resolve_mode_list`** as files, so `stat.templates` entries come out identically shaped `[{mode, path, icon}]`. Directory templates are **package-only**: the M7 user registry (D50) is a per-file *suffix* match with no coherent meaning for a directory, so it deliberately does not apply (documented in `_templates_for`). Shell dispatch (App.tsx) changes from `is_dir → Listing` to: a target with non-empty `templates` previews (even a directory) **unless** the shell-owned `?listing=1` forces the plain listing; a directory with no templates still lists. Directory previews render a **"Browse contents"** header action that navigates to `?listing=1`; embed mode hides the whole header, so the same action also rides as a `.preview-browse-chip` corner chip pinned over the iframe, CSS-revealed only in embed. `listing` never leaks into template params: it only takes effect on a directory, and when set it selects the listing view (no template iframe is mounted). The `zarr/` folder ships `template.html` (adapted verbatim from Fused's existing Zarr viewer, imports the shared sciViz core) + a chunked-grid `icon.svg`; zarrita 0.7.3 is vendored (`templates/vendor/zarrita.bundle.mjs`, ~1.4 MB — numcodecs inlines its WASM as base64) via `scripts/vendor-sci`. | Separate `DIR_TEMPLATES` over overloading `TEMPLATES` with a dir flag: the file case (the common one) stays a plain dict lookup and the directory case is explicit; reusing `_resolve_mode_list` keeps one resolution rule and one stat shape. Dispatch on `templates` not `is_dir` so no per-format logic leaks into the shell (D12 holds). `?listing=1` override kept minimal — one dispatch check + a `navigateUrl` link, zero new server routes. Registry-for-directories rejected as out of scope (no file-suffix rule maps cleanly onto a directory). Split from D63's PR per review on the original combined PR. *(Revised by D73: `DIR_TEMPLATES` is replaced by trailing-`/` registry keys — `".zarr/"` — and the user registry can bind directories after all.)* |

## Descoped / follow-up list (recorded, not built)

Security layer (token, Origin/Host validation, sandboxed bridge — see threat note SPEC §9; X-Fused preflight guard shipped, D36) · warm worker pool · DataFrame/Arrow returns · WebSocket/SSE push · exec console + structured logging · caching · search/sort/tree/keyboard-nav/hidden-file toggle · editing beyond code_template (text/markdown buffers, new-file/rename/delete APIs) · pushState opt-in · declarative param binding. *(Built since this list was written: M2 template set, file editing — D37; directory templates — D64.)*

### Engine rollback (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D67 | Roll back the openfused engine | Reverts #8 (D55–D58), #18 (default data-stack venv deps), and #19 (D66 run-error logging, which existed to surface openfused backend 500s). `executor.py`/`_child.py` return: bare `main()` with annotation-driven string coercion (D1), server-env imports (D14/D30), old `{ok, result, error:{type,message,traceback}}` wire shape, `requires-python >=3.10`. Kept features that landed on top were retargeted, not reverted: template folders/mode lists (D59–D62) keep their new paths; built-in readers lose `@fused.udf` + PEP 723 headers; the `api` template (D63) inspects a bare `main()` and consumes the old wire shape | Owner call 2026-07-07: roll back all engine changes, keep everything else |

### Application log (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D68 | Application log file for "Internal Server Error" / right-click-open diagnostics | Re-introduces the diagnostics half of D66 (rolled back with the engine in D67, though the *reason* — a Finder-launched .app has no visible stderr, so a bare "Internal Server Error" or a failed right-click "Open with FusedRender" leaves nothing to report — never went away), retargeted to the current `executor.py`/`_child.py` model. Three parts. **(1) `fused_render/logs.py`:** `setup_logging()` attaches a `RotatingFileHandler` (2 MB + 1 backup) to the **root** logger — uvicorn/library output propagates there too — writing a **per-process** `fused-render-<pid>.log` to the **system temp dir** by default (`FUSED_RENDER_LOG_DIR` overrides). Temp because the log is disposable diagnostic output with no retention policy: rotation bounds a single file's size, but nothing prunes the directory, so a permanent home (a dotdir, app support) would silently accumulate stale logs forever; temp storage is OS-reclaimed and clears on reboot, giving a fresh log per session. Per-pid (not a fixed name) so concurrent instances can't collide: a shared name is fine for *sequential* launches (the handler opens append, never truncates) but two live writers would interleave their lines, and size rotation is not multi-process safe (one process's rename + reopen leaves the other writing to the renamed fd, clobbered on the next rollover). The writer knows its own path via `getpid()`, so `Open logs` / the CLI print still resolve it. Idempotent (won't stack handlers); boot line records fused-render version / python / platform / `sys.prefix`. **(2) Server 500s self-report + browser request log:** a catch-all FastAPI `exception_handler(Exception)` puts the full traceback in both the JSON 500 body (fine under D3 — local single-user tool, the only reader owns the machine, so the existing D17 overlay renders it screenshot-ably) and the log, tagged with the request method + path. The existing `no_cache` middleware also grew a **per-request access line** (`<method> <path> -> <status> (<ms>)`, SPEC SV-3) so the log reconstructs the call sequence a page made — the context for *which* request 500'd; a request that raises is logged before the re-raise so the trail stays complete. Static-asset mounts (`/static`, `/template-assets`, `/template-shared`) are skipped — high-volume and never the failure, they'd just churn the rotating file. `uvicorn.access` couldn't carry this: it's `propagate=False` (never reaches our root handler) and the menu-bar app runs uvicorn at `log_level="warning"`, which suppresses it — a middleware works identically for both entry points. **Startup** is explicit too: the boot line timestamps every launch, the CLI logs its bind + start dir, and the app logs `app starting` / `starting server` / `server ready`. **(3) Right-click-open trail:** `app.py` logs the Finder open-files event (raw filenames), dock-reopen, server bootstrap start/ready, and — instead of the old `print` that went nowhere — the readiness failure; the "server did not become ready" path and the new **"Open logs"** menu item (reveal in Finder) round it out. `executor.run_python` logs every failed run (with the worker traceback) and now returns the normal wire shape on an `OSError` spawn failure rather than 500-ing unlabeled. Both entry points (`cli.py`, `app.py`) call `setup_logging()` first; the CLI also prints the log path | Owner-directed 2026-07-07: an application log to diagnose Internal Server Error / right-click-open failures. Root logger over a package logger: capture what stderr would have said, not just our own lines. Traceback in the response body over a bare 500 (D66 precedent): every silent 500 becomes a screenshot. System temp dir over a persistent location for the default: rotation bounds a single file but nothing prunes the folder, so ephemeral storage avoids piling up stale logs; a persistent dir is one env var (`FUSED_RENDER_LOG_DIR`) away. Structured logging / exec console stays on the backlog; this is tracebacks + boot/open context only |

### Fused engine, optional (2026-07-07 — supersedes D67's posture, full spec = SPEC §5.6 PY-12..PY-14)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D69 | Re-introduce the fused engine as **optional with fallback** | New `engine.py` (the D55 engine resurrected, adapted): when the `fused` package is **importable**, `/api/run` executes through `LocalPythonComputeBackend` — fresh subprocess per call, PEP 723 requirements + a default data-stack set resolved into cached venvs, params via `_params.json`. Absent, `executor.py`/`_child.py` run **unchanged** — no hard dependency, `requires-python` floor stays `>=3.10` (a `python_version >= "3.11"` marker on the optional `[fused]` extra; the D55 nightly-wheel URL). `FUSED_RENDER_ENGINE` = `auto`\|`fused`\|`builtin`; the choice is logged and surfaces in `/api/config.engine`. **Contract when fused runs it**: `@fused.udf`-decorated function (any name, last wins, raw-JSON params) or `result =` script — plus a **bare-`main()` compat bridge** (annotation coercion + script-dir cwd, matching the built-in executor) so pages and built-in templates behave identically under either engine. **One wire shape** on both paths: the D1 `{ok, result, error:{type,message,traceback}, stdout}` (fused adds `stderr`/`duration_ms`) — the fused engine adapts, consumers never fork. User source is compiled as its own unit under its real filename (leading `from __future__` imports stay valid; tracebacks carry exact file/line, plumbing frames stripped). `api` template inspector recognizes both entrypoint forms | Owner call 2026-07-07: use fused to run code **if it's available**. Differs from D55/D57 exactly where D67 hurt: no hard wheel dependency, no floor bump, no contract break for existing pages (the bridge), one wire shape (no consumer migration). Rejected re-imposing D57's fused-only contract: built-in templates and existing pages must run under both engines since availability varies per install |
| D70 | `FUSED_RENDER_ENGINE` default flips from `auto` to `builtin` | `_select_engine()`'s unset-env default changes from `auto` (opportunistic: use fused iff importable) to `builtin` (never touch `fused`, even if it's importable). `auto` and `fused` are unchanged and still opt in to the D69 engine explicitly. Only the silent, install-dependent default behavior changes — a `pip install "fused-render[fused]"` next to fused-render no longer silently switches which engine runs a page's code | Owner call 2026-07-08: an install-order-dependent default (whether some *other* package happens to be importable) was surprising — same fused-render install could run pages under two different contracts depending on what else shares the environment. Explicit opt-in (`auto`/`fused`) over `auto`-by-default: engine choice should be a decision, not incidental to what's on `sys.path` |
| D71 | Export moves from a CLI subcommand to `POST /api/export` | `fused-render export <page.html> --out <dir>` (the standalone, no-server-required CLI form added for M10) is removed; `cli.py` goes back to `serve`-only. The identical `export_page()` logic (`export.py`, unchanged) is now called from a new endpoint on the already-running server — `POST /api/export {"page", "out"}` (both absolute paths, same convention as every other endpoint), guarded by the same `_require_fused` X-Fused header check as `/api/run`/`/api/fs/write`. Success returns `{"out", "entrypoints", "assets"}`; an `ExportError` returns a `400 {"error"}`, matching `/api/run`'s error shape rather than the CLI's stderr+exit-code convention | Owner call 2026-07-08: export is a one-off local build step used from tooling that already talks to the running server (or will, e.g. a future in-shell "Deploy" UI) — a second process-invocation surface (parse `sys.argv`, print to stdout/stderr, `SystemExit`) duplicated what an HTTP endpoint already gives for free, and diverged from every other export-like action (`/api/run`, `/api/fs/write`) in error shape and auth. No behavior change to export.py itself — only how it's invoked |
### Built-in helpers run in-process (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D72 | First-party helpers execute in-process | `run_python` splits by target: a `.py` on an **explicit allowlist** (`executor.INPROCESS_HELPERS` — the `table`/`csv`/`xlsx` `reader.py` and the `api` `inspector.py`) runs **in the server process** (`executor._run_inprocess`); everything else — the `api` template's Run button (the user's own `.py`), user-authored template readers under `~/.fused-render/`, **and every other shipped `templates/` helper** — keeps the **fresh-subprocess-per-call** isolation (D5/PY-6). Boundary is exact realpath membership in the allowlist, **not** a "path under `templates/`" check: that was the original design but it silently swept in later-added shipped helpers (the `claude/` chat agent, the geo/h3/las/vector/zarr browsers + tile servers) that are slow or long-running — running those in-process skips the 30 s timeout and can pin a FastAPI threadpool worker indefinitely (Bugbot, PR #26). An allowlist makes the safe subprocess path the default; a new reader is added only after confirming it is fast, bounded, and free of user-code execution (symlink-safe; correctness, not auth — D3 stands). Param binding was extracted to `fused_render/_binding.py` so both paths coerce identically; `_child.py` imports it top-level (`import _binding`) because it runs as a standalone script and its own dir is `sys.path[0]` (works dev-from-source). In-process runs catch `BaseException` → error dict (a helper fault can't kill the server thread), mutate no process-global state — the helper module is built with `module_from_spec` + `exec_module`, never inserted into `sys.modules`, and `sys.path` is left untouched — so they stay safe under the `asyncio.to_thread` threadpool, do **not** capture stdout (helpers don't print; a `sys.stdout` redirect is process-global and would race), and have **no timeout** (allowlisted helpers are bounded local-file reads / `ast` parses). Safe because the allowlisted helpers are trusted and **never import or execute user code** — the readers open a data file, the inspector `ast`-parses without importing (D63). | **The macOS TCC bug this fixes:** the server runs *in* the menu-bar app process (`app.py`), so its own file reads reuse the one Downloads/Desktop/Documents grant the user gave the app. But data-file previews read through `fused.runPython` → a **fresh `Contents/MacOS/python` subprocess** — a *different*, only **ad-hoc-signed** Mach-O (`build_dmg.sh`; no Developer ID yet, D35) that macOS TCC does not attribute to the app, so every preview / pagination / slider tick on a file under a protected folder re-prompted. Moving the trusted readers in-process makes that access the app's again → prompt once, not per click. Secondary win: no per-call pandas/pyarrow re-import (≈1 s) — they stay warm in the server. Rejected: warm worker pool (the sanctioned PY-6 v2 upgrade) — still a separate process identity, so it wouldn't eliminate the prompt, and it would break "no stale state" for *user* code; left as the general path for user scripts. Full elimination for user code needs Developer ID signing + shared TCC identity (D35 → now D73). Accepted trade: a native crash in a first-party reader (e.g. a corrupt file) now takes the server thread instead of an isolated child — low risk on read paths, and the same libraries read the bytes either way |

### Code signing & notarization (2026-07-07)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D73 | Developer ID signing + notarization in `build_dmg.sh` | Replaces D35's deferred "Briefcase external-app" plan with **in-place `codesign`/`notarytool` in the existing script** (Briefcase's app template breaks `sys.executable`, so it was never a fit for the actual signing step — only its `codesign` invocation was ever going to be used). **Credential-driven, keychain-based:** `FUSED_RENDER_CODESIGN_IDENTITY` (a `Developer ID Application: …` string or cert SHA-1) is used verbatim; unset → auto-detect a single `Developer ID Application` cert via `security find-identity -v -p codesigning` (several → hard stop asking to pick; none → **ad-hoc fallback**, so a plain `bash scripts/build_dmg.sh` still works for local testing). `FUSED_RENDER_CODESIGN_KEYCHAIN` optionally targets a dedicated keychain (CI). Developer-ID mode signs **inside-out** (every nested Mach-O by magic-byte enumeration, then the `.app`) with **`--options runtime --timestamp`** and a generated **entitlements** plist — `disable-library-validation` (bundled CPython loads third-party native libs not signed by our Team), `allow-jit` + `allow-unsigned-executable-memory`, `allow-dyld-environment-variables` (`app.py` sets `PYTHONHOME`). `--deep` deliberately dropped (Apple advises against it for distribution; skips nested executables → notarization rejects). Notarization is opt-in via `FUSED_RENDER_NOTARY_PROFILE` (a stored `notarytool store-credentials` profile): submit the DMG `--wait`, then `stapler staple` + `validate`; ad-hoc + notary profile = hard error. Info.plist gains the `NS*FolderUsageDescription` strings (Desktop/Documents/Downloads/Removable/Network) so the TCC prompt explains itself. Docs: `docs/signing.md` (get an identity, store notary creds, CI keychain recipe, troubleshooting) + README build section. | **This is the general form of the D72 fix.** One stable Team ID signing the app stub *and* the nested `python` the executor spawns makes macOS attribute the worker's protected-folder access to the app → the Downloads/Desktop/Documents prompt appears **once**, covering user scripts (the `api` Run button, user template readers) that D72's in-process split can't touch without breaking isolation. Notarization is the other half of shippability (Gatekeeper, no right-click-Open dance). Keychain-based identity + notary profile keeps secrets out of the repo/CLI. Ad-hoc fallback preserved so the dev/local build is unchanged. Usage strings don't change *when* the prompt fires (D72 + signing do) but a reasonless prompt reads as malware. Rejected: full Briefcase (breaks `sys.executable`, D35); committing a fixed identity/team (secrets, per-developer identities differ) — everything is resolved from the keychain at build time |

### Layout params go pane-local (2026-07-08 — supersedes D45/D46's merged pool, LM-3/LM-7/LM-9 rewritten)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D72 | No automatic param promotion in layout modes — params are pane-local, globals only by hand | Panel mode adopts tab mode's TM-3 contract: the panel shell sets `window._fusedParamBoundary = true` (render-time set, cleared on unmount), so every pane's runtime targets its own `/embed` URL and the full pane query — user params included — is captured segment-local inside `_layout` by the ordinary sync. The breadcrumb Split entry no longer partitions params (`_`/`sort`/`order`/`listing` → pane, rest → pool): the current view's whole query goes into each segment; `panelUrl` is called with no top-level pool. Global params survive as a hand-typed convention: the runtime's get/getAll now merge the same-origin ancestor chain *above* the boundary (nearest wins, pane-local wins over all) and listen for `fused:urlchange` on those ancestors too, so `?city=sf&_layout=(…)` typed on the shell URL is readable from every pane — but `set()` never writes above the boundary (a pane setting a globally-present key shadows it locally), and the shells only pass top-level params through untouched | Owner call 2026-07-08: the promote-some/keep-some split (D45's LM-3 partition + D46's climb-to-shell) was complex and surprising — which params ended up where depended on a hardcoded key list. One rule now: everything rides the segment; global is opt-in by typing it. Rejected keeping automatic promotion for non-underscore keys (the removed behavior), and rejected dropping global support entirely (read-only ancestor fallback is cheap and keeps deliberate cross-pane linking possible). D46's structural cross-pane sync consequence (LM-9 auto-linking) is intentionally gone |
| D73 | Template bindings unified into one registry format: built-in table → `templates/registry.json`, trailing-`/` directory keys, `*` wildcard segments, `.html` exemption dropped | The `TEMPLATES` and `DIR_TEMPLATES` dicts leave `server.py`: built-in bindings ship as **`fused_render/templates/registry.json`**, exactly the user-registry format (SPEC §16), read per resolution by the same loader as the user file (CT-5 symmetry; editable installs get live edits free). One matcher serves both: keys are **dot-anchored suffix patterns** — compound (`.xyz.json`), `*` wildcard = exactly one whole non-empty segment (partial wildcards invalid), and a **trailing `/` marks a directory key** (`.zarr/`), replacing `DIR_TEMPLATES`. Specificity: more segments > fewer; tie broken rightmost-first, literal beats `*`; a match needs a non-empty stem (a file literally named `.json` doesn't match `.json`); precedence stays any-user-match > built-in. **Revises D65:** the trailing-`/` convention gives a directory binding the coherent suffix-match meaning D65 said was missing, so directory templates are user-bindable too. **Revises D62/CT-4:** `.html`/`.htm` become ordinary keys — their `["_render", "code"]` default simply ships in the built-in registry; users may rebind (owner call: "user can shoot themselves"). Sentinels: server keeps `KNOWN_SENTINELS = {"_render"}`; those names are referenceable from **any** registry list (so a rebind can keep/restore `_render` by listing it explicitly (the `"..."` splice that once did this was removed, D94)), all other `_` names stay invalid per CT-6 | Owner call 2026-07-08. Motivation: bindings were split across two code dicts and one JSON file with two different match rules (`splitext` vs longest-suffix) — now one format, one matcher, one precedence story covering files, compound extensions, wildcards, and directories. Per-call read of the built-in registry chosen over load-once ("whichever is simpler" — one loader, no second code path); a parse failure of package data surfaces as `template_error`, and a test pins the shipped file (parses, every name resolves). Rejected partial wildcards (`.geo*.json`): precedence between overlapping partials has no obvious answer, whole-segment `*` keeps specificity a two-rule sort. Rejected making the built-in registry user-editable in place — it ships inside the package; the override channel remains `~/.fused-render` |

| D74 | File-change feed moves from SSE to WebSocket | `/api/fs/events` becomes a **WebSocket** endpoint (same path, same repeated-`path` query params, same 200 ms stat loop). Messages are JSON text: `{"path", "mtime"}` on change, `{"keepalive": true}` every 15 s. Clients (runtime.js LR-4, Listing.tsx LS-1) swap `EventSource` for `WebSocket` and add their own 1 s reconnect-on-close (EventSource auto-reconnected; WS doesn't). `websockets` becomes a hard dependency — plain uvicorn ships no WS protocol implementation | **Revises D42's "SSE over WebSocket" call.** Bug 2026-07-08: SSE rides ordinary HTTP/1.1, and Chrome caps HTTP/1.1 at **6 connections per origin** — a 6-pane panel of runPython views held all 6 sockets open with change feeds, so every subsequent fetch (`/api/run` on a param change!) queued browser-side forever; closing one pane released a socket and everything drained. WebSockets live in a separate, much larger browser connection pool, so N open panes no longer starve request/response traffic. Rejected multiplexing one SSE stream through the shell via postMessage (complex, and still breaks at 6 open tabs since the cap is per-origin browser-wide); rejected HTTP/2 (uvicorn doesn't speak it) |

### Bookmarks move server-side (2026-07-08 — supersedes D21/SB-5's localStorage store)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D75 | Bookmarks persist to `~/.fused-render/bookmarks.json` behind the server; shell-state backends live in a new `fused_render/shell/` package | The store moves off localStorage to a file, via **`GET /api/bookmarks` → `{exists, bookmarks}`** and **`PUT /api/bookmarks`** (whole-tree, atomic temp-file+`os.replace`, **last-write-wins**, no locking per D3). Server code lands in **`fused_render/shell/`** — `storage.py` (the `~/.fused-render` home dir, resolved via `home_dir()` with a `FUSED_RENDER_HOME` test override, + atomic `read_json`/`write_json`) and `bookmarks.py` (an `APIRouter` `create_app` includes) — kept out of `server.py`'s fs/render internals; the next shell-state resource is a new module here reusing `storage`. The PUT carries the same `X-Fused` D3 guard as the other writes (duplicated locally, **not** imported from `server`, to keep shell↛server acyclic). Frontend (`lib/bookmarks.ts`): reads stay **synchronous off an in-memory cache** (React renders can't await), hydrated once at boot by `hydrateBookmarks()`; mutations become **async** — clone → `await` PUT → advance cache (**no optimistic update, no rollback**: a localhost PUT of this tiny tree is sub-ms, and advancing the cache only after a successful write means the UI never shows unpersisted state). The event bus (`notifyBookmarksChanged`/`useBookmarksVersion`) is unchanged — still the house re-render signal. **One-time import:** `exists` is false only until the file is first written; on boot, `!exists` + non-empty legacy `localStorage["fused.bookmarks"]` → import it once (PUT flips `exists` true forever, so deleting every bookmark later never re-imports). Legacy localStorage is left dormant as a read fallback, never written again. `fused.armedBookmark` stays in sessionStorage (D38, ephemeral). | Owner call 2026-07-08. D21/SB-5 chose localStorage but flagged the server-file move as a "trivial export" for later — this is that move (bookmarks are now shareable/portable per-machine, not per-browser-profile). Rejected **TanStack Query** (owner asked, then agreed): 3 tiny single-writer last-write-wins resources don't need request caching/dedup/pagination, and its refetch-on-focus/reconnect defaults actively fight last-write-wins — the hand-rolled cache mirrors the existing `App.tsx:useStat` house pattern with no dependency; revisit only if a resource needs polling / cross-window live sync / cross-resource invalidation. Rejected **optimistic updates** (owner asked): nothing to hide at localhost latency, and await-then-reflect is simpler *and* more correct (surfaces a failed write instead of a fake success). Rejected **auto-import + clear localStorage** (kept dormant fallback) and **fresh start** (would drop existing bookmarks). The `exists` flag over a bare emptiness check is load-bearing — an emptiness check would re-import after the user deletes everything. Rejected importing `server._require_fused` into `shell/` (import cycle — `server` imports the router). |

| D76 | User templates + registry move under `~/.fused-render/templates/` (revises D50/D59/D73's path) | The user override channel nests one level down: `USER_TEMPLATES_DIR = home_dir()/templates` and `USER_REGISTRY = .../templates/registry.json` (server.py imports `home_dir` from `shell/storage.py` — server→shell, still acyclic). The home dir is now a clean container of siblings: **`~/.fused-render/{bookmarks.json, templates/{<name>/, registry.json}}`**. Resolution rule, key grammar, precedence, per-entry validation — all unchanged (PT-6/CT-3/D73); only the two path constants + the paths quoted in docs/error strings move. **No auto-migration** — an existing user moves their own folders (owner call). | Owner call 2026-07-08. Once `bookmarks.json` (D75) landed in `~/.fused-render` and 1–2 more shell-state files are coming, user template folders + `registry.json` sitting loose at the home root mixed two unrelated concerns in one flat dir; a `templates/` subdir separates the template override channel from shell state so each is a clean namespace. Rejected auto-migrating on-disk template folders at startup (owner declined — moving a user's real files silently is surprising and risky, unlike the bookmarks localStorage import which only reads a browser key); the home dir is created lazily on first write regardless, so a fresh install needs nothing. |

| D77 | Bookmarks poll the server every 30 s for cross-tab convergence (revises D75's "no live sync") | `main.tsx` runs a 30 s `setInterval` calling `refreshBookmarks()` — a plain `GET /api/bookmarks` routed through the same serial queue as mutations/hydration, so it never overwrites an in-flight local write. It swaps the in-memory cache and fires `notifyBookmarksChanged()` **only when the tree actually differs** (JSON compare), so an idle tab doesn't re-render every tick. No import logic (hydrate owns the one-time import); a failed poll is logged and skipped. Convergence is **eventual (≤30 s)**; simultaneous writes stay **last-write-wins** (unchanged). | Owner call 2026-07-08. D75 rejected cross-tab sync, but the per-tab in-memory cache (vs the old shared localStorage, which every read re-parsed) made another tab's edits invisible until a full reload — a regression Bugbot flagged — and let a stale tab clobber a fresher file. A 30 s poll is the cheap fix: readers converge and the clobber window shrinks to ≤30 s, with none of the WebSocket live-sync machinery the file-change feed needs (D74). Rejected instant WS/broadcast sync (overkill for a tiny, rarely-edited resource — the whole reason TanStack was rejected in D75) and per-mutation read-modify-write (refreshes only the write path, not the displayed list). |
| D78 | Annotation logic is fully contained in the annotate template; views expose only stable ids | The annotate view template owns ALL annotation code, including off-page anchor handling: an `ID_RESOLVERS` table inside `templates/annotate/template.html` recognizes the stable element ids paged views render — `__fr_r<row>_c<col>` (table), `__fr_s<sheet>_r<row>_c<col>` (xlsx), `__fr_page_<n>` (pdf) — labels an unmounted id as off-page ("row 5" / "Alpha · row 3" / "page 3" chip, not "detached"), and navigates to it on reveal by writing the ordinary `offset`/`sheet` shell-URL params the paged view already watches (xlsx sheet names are read from the framed view's `#sheet` select options). View templates carry NO annotation code, hooks, or references — the ids are inert, deep-linkable markup useful on their own. Supersedes the interim `window.__fusedAnnotateAnchorResolver` per-view hook, removed the same day it landed. | Owner call 2026-07-08: "all the code for different view types lives in the annotation template itself — we are not injecting any code into other views." The per-view resolver hook was an extensibility seam (a custom view could teach annotate its paging), but it scattered annotation-aware code across templates — exactly what the template rebuild (SPEC §17) exists to prevent. Accepted trade-offs: annotate hardcodes the stock templates' id shapes and page size (100), and can no longer ask a view whether a row is truly gone from the data — a comment past the data's end keeps its "row N" chip instead of turning "detached". |

### Deploy — hosted publish through the fused CLI (2026-07-08, M11)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D78 | Deploy button/modal orchestrates the `fused share` CLI; fused-render still mints and hosts nothing | A **Deploy** action in the preview header (pages carrying the `_render` mode, SPEC §19) opens a modal that: resolves the **fused CLI** (`FUSED_RENDER_FUSED_BIN` verbatim/split → the server venv's own bin/ → PATH) and, when missing, offers a one-click `POST /api/deploy/install` that pip-installs **the wheel pinned by the `deploy.PINNED_FUSED_REQUIREMENT` code constant** (sync-tested against pyproject's `[fused]` extra; Python ≥ 3.11 per the wheel's marker and a pip-capable interpreter, else the manual hint — an earlier metadata-read approach was dropped: dist-info is absent on source runs/stripped app bundles and **stale on editable installs that predate the extra**, which made the button report "metadata unavailable" exactly when it mattered); reads **hosted envs** (`fused`/`aws`, never `local`) directly from the CLI's `~/.openfused/envs.json` (`OPENFUSED_ENVS_FILE` honored), defaulting to the managed **`fused`-backend** env; **re-exports the page to a fresh temp bundle** per deploy (§18 machinery, deleted afterwards) and runs `fused share create <bundle> --public` with `OPENFUSED_ENV=<chosen env>` on the child; keeps a **thin per-page pointer** at `~/.fused-render/deployments.json` (env/backend/token/url/status) so the header shows a deployed dot with zero CLI cost, the URL is re-showable (`create` returns it exactly once; AWS returns none — url stays null there), and a redeploy hits the **same token** (`repoint`; revoked tombstone → `recreate --same-token` + repoint with best-effort re-revoke on failure; absent token → fresh create). `share list --all` is reconciled on modal open and backs the modal's per-env **share list**, joined to local pages via the pointer store. Deploys are **public capability links only** in v1. Server code is `fused_render/deploy.py` (an APIRouter beside shell/bookmarks; X-Fused on POSTs); tests drive a stub CLI through the `FUSED_RENDER_FUSED_BIN` seam. | The flow app already proved this exact shape (its spec/app/deploy-project.md): the CLI is the one mutation boundary, output is already JSON, `share list` is truth, and the app keeps only a pointer — reusing the pattern means zero fused-repo changes and no credential ever transits fused-render. Rejected **importing fused as a library** in-process: heavy import, version-couples the server to the wheel, and bypasses the supported share surface; the CLI-child pattern also lets the package live in a *different* interpreter than the server. Rejected **hardcoding the wheel URL** for install (pyproject metadata is already the pin). Rejected **auth-gated deploys** for v1: per the fused repo's spec/serve/fused-render.md, `html-run`/`html-asset` are `inherit`, so a browser `<img>`/asset GET can't attach a bearer on an authed mount — public is the only posture that works fully today; the modal states the link is public. Rejected a per-row deployed badge in directory listings (a pointer-store join per listing row for a niche signal; the preview-header dot + the modal's share list cover discovery). Deferred: rotate-link (one `share recreate` call away), gate pickers when authed asset delivery lands upstream. |

### Deploy in the packaged .app — one autodetected CLI, bundled (2026-07-08)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D79 | fused CLI resolution is explicit-or-in-interpreter only; the .app bundles the CLI | `deploy.fused_cli` has **exactly two sources**: (1) the explicit `FUSED_RENDER_FUSED_BIN` override (verbatim/split; spawned with `PYTHONHOME`/`PYTHONPATH` **scrubbed** — inside the .app those are bundle-scoped and would break any external Python, the las template's external-spawn precedent), and (2) the **one autodetection**: the `fused` package importable in the server's own interpreter, run as `[sys.executable, fused_render/_fused_cli.py]` — a shim that sets `argv[0]="fused"` and calls `fused._cli.main()` (the executor's proven nested-interpreter spawn pattern; env kept). D78's venv-bin scan and PATH lookup are **removed** (owner call): a venv with the console script always has the package importable, so bin-scanning was redundant, and PATH/well-known-dir probing could silently pick up an unrelated `fused` the user never chose for this server. The **DMG build ships the CLI**: `build_dmg.sh` installs `[bundled,app,fused]`, `setup_py2app.py` force-copies `fused` + data-bearing deps (botocore models, anthropic tokenizer data, keyring/pluggy entry-point packages, cryptography + `_cffi_backend`), a post-build smoke test runs real verbs through the shim before signing, and a 3-line `Contents/Resources/bin/fused` wrapper (bundled python + shim, `PYTHONHOME` set like the build's own smoke tests) exposes the same CLI for terminal setup (`cloud setup`/`cloud login`/`env create`) — `/api/deploy/config.setup_cli` names it when frozen so the modal's guidance points somewhere real. The wrapper sits under `Resources`, not `MacOS`: a first build attempt put it in `Contents/MacOS/` and Developer-ID signing refused the bundle seal ("code object is not signed at all / In subcomponent: …fused") — everything in a bundle's `MacOS/` is nested code that must carry a code signature, which a shell script can't; under `Resources` it is sealed by the resource rules, the bundle format's intended home for helper scripts. `ty` deliberately not shipped (Rust binary console script; fused's verify scanner skips with a warning when absent). | Owner call 2026-07-08 ("only a single one autodetects, otherwise the user should have explicitly overridden it"), reconsidering deploy for the .app: the bundle has **no pip and no console scripts, and its notarized seal cannot be pip-installed into at runtime** — so on-demand install is structurally impossible there, and the previous "install with a Python on your PATH" message was a dead end (Finder launches don't even get the user's shell PATH). Shipping the pinned wheel in the bundle follows the `[bundled]` extra's exact philosophy (packaged users can't pip install → the interpreter ships the stack) and makes deploy zero-setup. Rejected: PATH/well-known-dir autodetection (silent, version-skewed CLI pickup the user never configured); vendoring `uv` to provision a managed env outside the bundle (heavier, second package manager to sign and update, unnecessary once the wheel ships in-bundle — revisit only if CLI updates must decouple from app releases); in-app OAuth login UI (deferred — the wrapper covers interactive setup). |

### Preferences page (2026-07-08, M12)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D80 | A `/view/_prefs` sentinel page, entered bottom-left; engine becomes a per-request preference | A muted gear row pinned to the **sidebar's bottom edge** (margin-top:auto — unobtrusive per the owner's ask) navigates to **`/view/_prefs`**, a shell-owned sentinel like `_panel`/`_tab` (`/view` only). Four sections, each a thin client over an existing backend: **logs** (path from `logs.log_path` via `GET /api/prefs`, revealed with the existing `/api/fs/reveal`); **execution engine** — a persisted pref in `~/.fused-render/prefs.json` (`shell/prefs.py`, the D75 shell-state pattern), `builtin` default, **read per /api/run request** so switching applies to the next run with no restart (CT-5's no-restart discipline), degrading to builtin with a visible note while the fused backend isn't importable; `FUSED_RENDER_ENGINE` stays the process override that locks the switch (`server._select_engine` became `_forced_engine`: unset now returns None and defers to the pref instead of hardcoding builtin); **deployments** — the per-env `share list` view with **revoke by env+token** (`deploy.revoke_mount`), which also covers mounts with no local pointer and flips any matching pointer so per-page state stays consistent; **template registry** — `GET /api/templates/registry`, the merged read-only bindings table (splice-expanded lists, `disabled` nulls, builtin/user/user-override source per row). | Owner ask 2026-07-08 (logs location, Fused/Local execution switch, see-and-revoke shares, later + registry view). Engine-as-preference chosen over restart-required env var editing: the engine seam (D69) already dispatches per call, so per-request pref reads make the switch free; the env var is kept as the stronger process pin rather than removed (scripts/CI rely on forcing). Revoke-by-token added instead of forcing every revoke through a page pointer — the prefs page lists mounts that have no pointer (CLI/another machine), and the fused CLI's owner-binding already guards misuse. Registry view is deliberately read-only: bindings live in the registry files (D73's design); an editing UI would duplicate that grammar's validation for little gain. Rejected a modal for prefs (the page is bookmarkable, holds four sections, and the sentinel-route pattern already exists) and a top-level nav item (the ask was unobtrusive). |

### Claude sidecar key rename, bookmark history, session restore (2026-07-09)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D82 | Claude sidecar on-disk key renamed `sessions` → `claudeSessions`; no migration | The `<file>.json` sidecar written by `fused_render/templates/claude/agent.py` stores its transcript list under `"claudeSessions"` (was `"sessions"`). Only the **on-disk key** changes — the `runPython` call contract stays exactly as-is: `main(action="sessions", ...)` and the `{"sessions": [...]}` response shape are unchanged (`_sessions()` reads the renamed key from disk but still returns it under `"sessions"`). Sidecars written by older code keep their old `"sessions"` key and are read as empty (their entries never appear again) — no migration | Owner call 2026-07-09 (memory `url-shapes-are-literal`: only the sidecar key was in scope, not the action name or response shape, which are the external contract with `template.html`). Old sessions being silently dropped is accepted — the sidecar is a convenience cache, not a durable transcript store |
| D83 | Bookmark create/update mirrored into the target file's `.html.json` sidecar as `bookmarkHistory`; delete is a no-op | New `POST /api/bookmarks/history` (`fused_render/shell/bookmarks.py`, same `X-Fused` guard as the PUT) upserts-by-`id` into a `bookmarkHistory` array in `<file>.json` (or `<dir>.json`) — the same sidecar the claude template owns via `claudeSessions`. Create-vs-update can't be told apart from the whole-tree bookmark PUT (D75), so the frontend (`addBookmark`/`updateBookmarkUrl` in `lib/bookmarks.ts`) fires this call itself, fire-and-forget, after its own mutation commits. A bookmark whose url resolves to a sentinel (`_panel`/`_tab`) or a path that doesn't exist on disk gets `{"recorded": false}` — no file touched. **Delete never removes a history entry** — the sidecar is a permanent record of every bookmark a file ever had, by design. Both `agent.py`'s `_load_sidecar` and the server's writer were hardened to preserve unknown keys on read-modify-write, so `claudeSessions` and `bookmarkHistory` never clobber each other (only a true simultaneous write loses a delta — accepted, D3) | Owner call 2026-07-09. Rejected detecting create/update server-side (the whole-tree PUT carries no diff); rejected wiring `renameBookmark`/`setBookmarkIcon` (out of scope — a stored entry's `name`/`icon` can go stale after a later rename, noted as a follow-up) |
| D84 | **REMOVED (D329) — the per-file session sidecar and both halves of its restore/track machinery are deleted; this row stands only as the history of what the feature was.** *Original form:* Per-file `lastSession` sidecar: opening a file with an empty query replays its last URL params | Each viewed **file** (not directories, not embed-mode panes) gets a `lastSession: {search, updated_at}` key in its `.html.json` sidecar — `search` is the shell query string verbatim, no leading `?` (same "store the URL literally" posture as bookmarks). `GET`/`PUT /api/session` (`fused_render/server.py`) read/write it; `PUT` carries the `X-Fused` guard (D36), `GET` doesn't (read-only). On opening a file with an **empty** shell query (listing click, DMG/Finder open, root redirect) the frontend (`lib/session.ts`, wired into `App.tsx`'s `StatView`) restores the stored `search` via `history.replaceState` before the preview mounts. Opening with a **non-empty** query (bookmark, hand-typed, refresh) lets those params win — no restore — and, if they include any key other than `_mode`, upserts them as the new `lastSession`. While viewing, any qualifying param change is upserted (400 ms debounced, fire-and-forget). Dropping params back down to empty/`_mode`-only leaves the stored `lastSession` untouched (a later bare open re-applies the old one) — an accepted quirk, not a bug | Owner call 2026-07-09. Only a shell-query-empty vs non-empty check is needed to discriminate "finder/redirect open" from "bookmark/hand-typed open" — every code path that opens a file with pre-set params already treats them as authoritative. Rejected clearing `lastSession` when params drop to empty (simpler to leave it, and matches "only track once a qualifying param exists"); rejected doing this server-side (param changes are client-side `replaceState`/`pushState` and never reach the server). Companion fix: `agent.py`'s `_load_sidecar` hardened (shared with D83) so a sidecar holding only `lastSession` (claude never used on that file) survives a later claude turn intact |
| D96 | History view: a `history` template renders any `<ext>.json` sidecar; `.html`/`.parquet` targets only | New `fused_render/templates/history/` — `template.html` + `icon.svg`, no `.py` (JSON is browser-parseable, same posture as `tree`). Registry: wildcard `".*.json": ["history", "tree", "code"]` (matches any compound `<ext>.json`, beats bare `.json` by specificity; no `annotate` — annotating the sidecar log itself doesn't make sense) and `history` appended to both `.html` and `.parquet`. The template resolves sidecar vs target from `_file` (HV-3: any `.json` whose stem still carries its own extension is the sidecar itself), reads via `fused.readFile`, validates **per top-level key** against an inline schema (hand-rolled subset validator) so one corrupt key warns only its own section (HV-4); unknown keys render as a collapsed raw section, not corruption (HV-5). Claude sessions navigate to `_mode=claude&session_id=` and bookmark/lastSession entries replay their stored `search` via `window.top.location` (HV-7); comments are display-only (HV-8). Never writes the sidecar | Owner call 2026-07-09. Template-not-shell posture follows annotate-v2 (D78): all logic stays in the template. Read-only comments chosen over URL-seeding a logged comment into annotate (owner rejected synthesizing URLs; annotate's live store is the URL `comments` param, the sidecar log is write-only). Validation inline in the template (owner) rather than a vendored JSON-Schema lib — four known keys need only a small subset. Registry key generalized from `.html.json` to the `.*.json` wildcard, and target scope widened to `.html` + `.parquet`, after the initial `.html`-only ship (owner edit 2026-07-09, Bugbot flagged the narrower SPEC/tests as stale, not the code); broaden further later by adding registry keys |
| D97 | Pinned view (M16): menu-bar NSPopover + WKWebView showing one pinned file's `/embed` view; detachable into a floating always-on-top window | With a pin set, left-click on the status item toggles an `NSPopover` (transient, 420×560) anchored to it; right/ctrl-click opens the existing rumps menu; no pin → menu on every click, current behavior untouched (PV-2). The popover's `WKWebView` loads `http://127.0.0.1:<port>/embed/<path>` — the same chrome-free page shell panes iframe, so ANY registry-renderable path is pinnable (html, parquet, images, directories) with the full runtime for free (PV-1). Menu gains "Pin File…" (NSOpenPanel) / "Show Pinned View" / "Unpin" (PV-3). Webview created lazily, kept alive across open/close so view state survives (PV-4). Drag-off detaches (`popoverShouldDetach:` YES) and the detached window is raised to `NSFloatingWindowLevel` — the literal "always on top" ask (PV-5). Pin persisted at `APP_SUPPORT_DIR/pin.json`. New dep `pyobjc-framework-WebKit` in the `[app]` extra + py2app packages, lazily imported like rumps (PV-6). AppKit code in `fused_render/menubar_pin.py`, pure-python store in `fused_render/pin_store.py` (PV-7) | Owner call 2026-07-10, chose popover over (b) standalone floating NSPanel (popover gives both quick-glance and, via detach, always-on-top; panel-only loses the menu-bar-anchored glance) and (c) WKWebView inside an NSMenu item (NSMenu tracking mode fights webview events — known flaky). `/embed` over `/render?path=` because the latter is html-only and chrome policy already solved for panes. Single pin in v1 — pin list is a follow-up |
| D98 | Status-item menu removed: the popover is the app's single surface; all actions move into a native header row inside it | Any click (left/right/ctrl) toggles the popover — no NSMenu on the status item. Header: Open in Browser / Copy URL / Pin… (→ Change… when pinned) / Unpin (hidden when unpinned) / Logs / Quit, native NSButtons above the webview; popover opens even before server-ready (placeholder body) so Quit is always reachable (PV-2/PV-3/PV-8). The rumps menu is still built but only ATTACHED if the popover controller fails to construct — never-unquittable guarantee (PV-8) | Owner call 2026-07-10, same day as D97: "not intuitive for the user to right click on an item in the menu bar" — the left-click-popover / right-click-menu split shipped hours earlier was undiscoverable. Header is native, not web chrome, so Quit works when the server is dead. Rejected keeping a trimmed menu on right-click (two surfaces for six actions) |
| D99 | *(AMENDED BY D270: what is queued during the 400 ms window is a key→value DELTA plus the pathname it was aimed at, not the formed URL this row's first implementation replayed — the flush merges, and a traversal or a pathname change discards. Everything below stands.)* Coalesced history writes in the runtime: params apply instantly via a pending-search overlay; the `replaceState` lands at most every 400 ms (trailing flush; flush on pagehide) | WebKit (Safari / the popover's WKWebView) hard-limits history writes to 100 per 30 s and THROWS SecurityError past it — scrubbing a slider in the pinned popover killed the chart's handler mid-scrub (Chrome has no limit, so the browser shell never hit it). `fused.params` readers (`get`/`getAll`/`set` rebuilds) go through `targetSearch()` = pending overlay else `location.search`, so values are live during the debounce window; the once-per-visit first-change-push (D51 semantics) stays immediate so Back gets its entry; all writes are try/caught so a throttle hit can never propagate into a caller again | Found 2026-07-10 testing M16. Overlay chosen over plain debounce (stale URL reads would freeze the chart during scrub) and over throttling in templates (every template would have to know; the runtime owns URL sync). 400 ms ⇒ ~75 writes/30 s, safe margin under WebKit's 100 |
| D100 | History comment rows deep-link into annotate via an id-only `comment` param; annotate imports the logged comment on a boot-time sidecar lookup | The history view's comment rows (D96) become **navigable**: a row with a string `id` opens the target with `_mode=annotate&comment=<id>` (same `navigateShell`/`wireClick` affordance as claude-session rows), an **id-only** deep link mirroring the claude `session_id` resume contract — the id is the whole contract and is never cleared after use. Annotate's `template.html` grows a boot-time focus resolver (runs once per page-load, after the initial render/frame wiring): it reads `comment`, focuses the id if it's in the live URL `comments` store, else does a **one-shot sidecar lookup** — a single id-keyed read of `<file>.json`'s `comments` log, strips the server stamps (`recorded_at`/`updated_at`), upserts the entry into the live set (re-recording it, a harmless upsert no-op), then focuses. Focus mirrors the existing cross-view card click: `switchView(c.view)` first when the comment's view differs (the reload re-runs the resolver on the right view), else `setActive`+`reveal`. Unreadable/unparseable sidecar or missing id fails silently. All logic stays inside the two templates (containment, HV-1/§17). Supersedes the read-only-comments sub-call in D96 | Owner reversed the 2026-07-09 read-only call on 2026-07-10. The earlier rejection was of **synthesizing** comment URLs from the write-only sidecar; the id-only param avoids that — it seeds nothing but an id (the `session_id` precedent already blessed for claude), and annotate stays the single source that resolves it. The live URL param remains the sole live store; the sidecar read is one boot-time id lookup for a comment absent from the live set, not a live-store sync. Rejected clearing `comment` after use (kept the resume-contract symmetry with `session_id`) and copying full comment fields into the history→annotate URL (id-only keeps the link short and lets annotate own the shape) |
| D101 | Explicit-delete tombstones (`deleted_at`) + full-state hydration on the comment deep link; absence never deletes | Annotate's delete button now sends the id as `deleted_ids` on the SAME `record` call that drops it from the URL array: `_record(file, comments, deleted_ids)` stamps `deleted_at` (server `time.time()` SECONDS, matching recorded_at/updated_at) and bumps `updated_at` on each named sidecar `comments` log entry in the same atomic read-merge-write as the upserts (unknown ids ignored; nothing recorded AND nothing tombstoned never touches disk). The tombstone is PERMANENT: `_record` strips any incoming `deleted_at` and never clears a stored one, so a stale bookmarked URL that still carries the deleted comment — or the hydration merge's live-wins rule — cannot silently resurrect it in the log. The history→annotate deep-link resolver (D100) is upgraded from a single-entry import to **full-state hydration**: for an id absent from the live URL store it imports EVERY non-tombstoned log entry (stripping `recorded_at`/`updated_at`/`deleted_at`), merges into the live set (live wins by id), saves once, then focuses; a tombstoned wanted id gets no import and no focus — deleted stays deleted. History rows render tombstoned comments dimmed + struck (`.ev.deleted .title`, line-through) with a " · deleted" tooltip and are inert (no deep link). All logic stays in the two templates + `annotate.py`. Supersedes D100's single-entry import | Owner call 2026-07-10. Absence-based deletion is **impossible** by construction: each shared URL carries only its own review subset (D100), so a comment missing from a `record` array means "not in this review", not "deleted" — the only way to express intent-to-delete is an explicit tombstone. Tombstone-on-the-same-record-call chosen over a separate `delete` action (the first iteration): two fire-and-forget read-modify-writes can interleave so the record's whole-log write lands after the delete's and silently drops the fresh `deleted_at`; one call, one write, no ordering to get wrong. Permanent over clear-on-re-record (the first iteration): any old URL still holding the comment would re-record it on the next unrelated save and silently undo the delete; with delete undo rejected by the owner there is no legitimate clearer, so the stamp is one-way (hand-edit the sidecar to revive). Full-state hydration (over D100's single-entry import) lands a deep link in the comment's *whole* live context, not one orphaned pin. An earlier iteration let clicking a tombstoned history row resurrect the comment; owner rejected it same day (resurrection re-recorded the id, silently clearing the tombstone and bringing the comment back everywhere) — deleted rows are now inert. Kept `deleted_at` in SECONDS (recorded_at/updated_at precedent), not the comment's own ms `createdAt` — server stamps are seconds by design (D83/D84, HV-6). The history SCHEMA needs no change: its subset validator ignores unknown entry fields (HV-6), so `deleted_at` passes untouched |
| D102 | Mounts — remote storage via rclone-managed mounts as local paths (`shell/mounts.py`, `/view/_mounts`) | A mount = a named remote-storage mount: an rclone remote spec (`s3remote:bucket/prefix`) + a mountpoint under `home_dir()/mounts/<name>`. Mount lifecycle goes through a detached `rclone rcd` daemon over its local HTTP API (`mount/mount`, `mount/unmount`, `mount/listmounts`), spawn-or-reuse via `home_dir()/rcd.json` — the tile-server daemon pattern. Mounts deliberately **survive server restarts** (rcd is `start_new_session`); a fresh server adopts them via listmounts, and per-mount opt-in `automount` remounts flagged ones at startup in a background thread. Unmount-while-busy (measured EBUSY when a tile daemon holds a file open) asks both daemon state files (`~/.cache/fused-render-geotiff-v2/daemon.json`, `~/.cache/fused-render-gridv2/daemon.json` — parallel implementations, both always handled) to `/quit`, retries once, then fails loudly; never force-unmounts. `/api/fs/walk` clamps to `WALK_MAX_ENTRIES_REMOTE` (2 000) under the mounts dir — every dir listed in a mount is a remote LIST call. Credentials live exclusively in rclone's config: S3-compatible remotes are created non-interactively from the page; OAuth backends (Drive…) are set up with `rclone config` in a terminal (in-app OAuth deferred). vfs flags validated by measurement on a 204MB COG + 362MB parquet: `CacheMode full` (warm reads ~0.01s vs 15–130s cold), `CacheMaxAge 24h` (the 1h default's eviction is what made revisits slow), 8M/64M chunked reads; `--vfs-read-ahead` measured a net LOSS (slows the parquet footer read) and left out. Cold first-reads are bounded by the remote file's *layout* (row-group size, overview levels), not the viewer — remote-optimized formats are the fix, documented not coded. macOS mounts via `nfsmount` (built-in NFS client, no macFUSE kext); Linux via FUSE `mount`; Windows via rclone's default WinFsp `mount` (D132 — WinFsp user-installed, not bundled) | Owner-directed 2026-07-10 after a validated prototype (S3 sentinel-cogs COG tiles + GDrive rendered through unmodified readers/tile servers). **D2/D3 reframed, not overturned**: the app still only ever sees local absolute paths; remoteness lives in the mount layer. rcd over per-mount child processes: a pkill'd server orphaned its mount child (measured), and rcd fixes orphaning/supervision/OS-specific unmount in one move. Mountpoints under the dotdir over OS-standard locations: `/Volumes`+`/mnt` need root, and user-visible dirs (`~/Documents`) invite iCloud/Spotlight to crawl a network mount. A desktop-client tier (Google Drive Desktop's `~/Library/CloudStorage` as mount-free mounts) was built, verified, then dropped — detection and install guidance are macOS-only (no official Drive/OneDrive Linux clients), and one OS-agnostic mechanism keeps the concept simple; users can still browse/bookmark those paths directly |
| D103 | rclone bundled into the packaged macOS app (`build_dmg.sh`, `shell/mounts.py`) — D102's mounts (rclone) drops from a user prerequisite to zero-setup | `build_dmg.sh` downloads a pinned rclone release (`v1.74.4-osx-arm64`, matching this script's Apple Silicon-only py2app target — see the FRAMEWORK_PYTHON note) against its published sha256, stages it under `build/rclone-bin/<version>/` (cached — reruns skip the download unless the pin changes), then after the py2app build copies it to `Contents/Resources/bin/rclone` — same spot as the existing bundled `fused` CLI wrapper (D-fused/§19 DP-3) — with a `rclone version` smoke test before signing. It's a real Mach-O binary (a statically-linked Go build, only system frameworks in its `otool -L`), so the existing signing sweep (§5, walks the whole bundle by magic-byte detection) signs it with no dedicated rule. `shell/mounts.py`'s `rclone_bin()` — previously the single `shutil.which("rclone")` choke point every mount call went through — now checks `sys.frozen == "macosx_app"` first (the same check as `deploy.py`'s `_setup_cli_hint`) and resolves the bundled path off `sys.executable`, falling back to `shutil.which` when unpackaged (dev checkout, Linux) so the source workflow is untouched | Owner-directed 2026-07-14: rclone was the app's one remaining external prerequisite (README) for a feature (D102) otherwise fully self-contained; bundling removes it. arm64-only and hardcoded-version-plus-checksum were explicit owner calls over a universal2 lipo'd binary and an always-fetch-latest build (matches the build script's existing single-arch Apple Silicon target and its already-immutable-per-run venv/icon caching pattern) |
### Directory views — the built-in listing becomes a mode (2026-07-09, M13)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D81 | Directory views: the built-in listing becomes the `_listing` sentinel, a universal `/` directory key, `?listing=1` removed (revises D65/D73) | Adds a **third way to render a directory beyond zarr**: user-defined directory-view templates, ordered beside the built-in listing in one mode list. **Server:** `KNOWN_SENTINELS` grows to **`{"_render", "_listing"}`**; `_listing` is the shell's built-in directory listing, emitted `{mode:"_listing", path:null, icon:null}` without touching the FS, referenceable from any registry list exactly like `_render`. The matcher (`_key_segments`/`_match_registry`) gains a **universal directory key** — the bare `/`, a **zero-segment** key that matches *any* directory at the **lowest** specificity (below every dot-anchored dir key, so `.zarr/` > `/`), stem = the whole basename. Built-in `templates/registry.json` ships **`"/": ["_listing"]`** and changes **`".zarr/"` from `["zarr"]` to `["zarr", "_listing"]`** — so every directory now carries a non-empty `templates` list (≥ `["_listing"]`) and dispatch is uniform (a directory previews its default mode like a file; `is_dir` no longer forks the shell except as a safety net). **Shell:** `App.tsx` dispatch drops the `?listing=1`/`forceListing` branch — a directory routes to `Preview` (it always has modes) with `dir + empty templates → Listing` kept only as the safety net for a `null`-disabled dir. `Preview.tsx`'s `TemplatePreview` renders `<Listing fsPath>` in the body when the active mode is `_listing` (the same special-case shape as `_render`'s no-`_file` iframe), including a plain folder's single `_listing` mode — so the preview header is uniform across files and dirs; `ModeSwitcher`/`modeTitle` gain a shell-baked `_listing` list icon + "Listing" label. Annotate is a template mode (D78) bound only to file keys, so it never lands in a directory's list and never applies to `_listing`. **`?listing=1` is removed** (revises D65/PT-13): the listing is reachable only via `_mode=_listing`. The normal-view **"Browse contents" button is gone** — the switcher's `_listing` icon is now that affordance (and a peer mode you can switch back from, unlike the old one-way trip); the embed-only `.preview-browse-chip` is repointed to **toggle `_mode=_listing`** so embedded directories keep their members within reach. Per-directory-**name** binding (literal, non-dot-anchored keys) is **deferred**. | Owner call 2026-07-09. Custom directory views were impossible because the listing lived *outside* the mode list — a shell fallback, not a mode — so nothing could sit beside it or default over it, and no registry grammar could bind a view to a plain extension-less folder. Promoting the listing to the `_listing` sentinel makes it an orderable peer (`["_listing", "gallery"]`, `["gallery", "_listing"]`), and the universal `/` key is the missing grammar for "every folder"; users compose by listing modes explicitly (`"/": ["_listing", "gallery"]`; the `"..."` splice was later removed, D94). **Registry-driven** default (`"/": ["_listing"]`) chosen over leaving listing a pure shell fallback (owner) — the model would otherwise be half-applied (couldn't order or switch the listing); shipping it as data keeps dispatch uniform and modes composable. **`?listing=1` removed** (owner) collapses two ways-to-the-listing into one mechanism (`_mode`) and upgrades zarr's one-way "Browse contents" into a real switchable mode. The plain-folder bare-render special case was dropped (owner) so the header is uniform. Rejected: keeping listing a fallback-only view (half-model); forcing `_listing` to always be present (owner keeps the D73 "user can shoot themselves" posture — dropping it forgoes the listing for those dirs, an explicit `_listing` entry restores it); per-name binding now (needs a non-dot-anchored literal-key grammar with its own precedence story — deferred until a real need). Accepted break: old `?listing=1` bookmarks ignore the dropped param — a plain folder still lists (its default), a `.zarr` bookmark opens the map, not members (same class as D62's `_mode=source` break). Milestone **M13**; PT-12/PT-13, CT-2/CT-3 updated. |
### M15 — Template management view (2026-07-09, full spec = SPEC §23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D86 | Sources abstraction for template provenance | Generalizes the core/user pair (§7/§16) into an ordered `Source {id, label, editable, precedence}` list (SPEC TV-1); today exactly two ship (`core` precedence 0, `user` precedence 100, unchanged directories/precedence from D50/D73/D76). Effective binding resolution (highest-precedence match wins) is unchanged — sources are a provenance/presentation layer over PT-6/CT-3, not a new resolution rule (TV-2). Modeled, not built: a third (org/project) source can be appended later with zero UI rework | Chosen so the inventory/bindings UI (§23) never hardcodes "two sources" — every list, filter, and picker groups by the sources array instead of an `is_core` boolean. Rejected a boolean/enum core-vs-user flag: cheaper today, but the frozen contract explicitly anticipates a third source, and retrofitting a list later would touch every UI surface that currently branches on the boolean. Rejected building a third source now — no concrete need yet, the model is the deliverable |
| D87 | Registry PUT/reset operate per-key, not whole-file | `PUT /api/templates/registry` (TV-5) takes `{key, value}` and does a read-modify-write of **that key only** against the user registry file (atomic via the existing `read_json`/`write_json` helpers); `POST /api/templates/registry/reset` (TV-6) deletes one key the same way. Neither endpoint accepts or writes the whole registry object | Bookmarks (D75) and prefs use whole-tree last-write-wins PUTs — fine there because each is edited from one UI surface with one shape. The registry is edited one row at a time from a table with potentially many independent rows open across a session (a row editor saves and closes, another row opens); a whole-file PUT built from a client's possibly-stale full copy would silently drop a concurrent edit to a different key. Per-key RMW removes that lost-update window without adding server state or locking (SV-1 stands — one open+modify+atomic-replace, not a held lock) |
| D88 | Editing template internals stays in the file explorer, not a second editor | The Templates view (§23) never edits `template.html`/`reader.py`/css/icon file contents; its header states so explicitly (TV-18). Users use the existing file explorer + `code` template (`templates/code/`) + `/api/fs/write`, which already has autosave, optimistic locking, and conflict handling (D37/D41) | The alternative — embedding a CodeMirror buffer in the Row editor or an inventory row for "quick edits" — would duplicate a feature that already exists and already handles the hard parts (save conflicts, autosave). "Open in explorer" (TV-10/TV-16) gets a user to the real file in one click; a second, lesser editor inside Templates would be scope creep with no capability the file explorer lacks |
| D89 | Export zips contain template folders only, never registry.json | `GET /api/templates/export` (TV-7) zips the selected templates' folder contents at top level; no registry bindings are included. **Revised 2026-07-09 (owner):** export is not user-only — **core templates are exportable too** (user-shadow-wins resolution), since a template folder is portable regardless of source; names travel as repeated `names=` params so a comma in a folder name round-trips. Import (TV-8/TV-9) never writes or merges registry entries — an imported template lands inert (CT-7) until a user explicitly binds it via the Row editor | A template folder is portable; a registry binding is a local, potentially conflicting choice — the destination machine may already use that extension differently, or not want it re-pointed at all. Shipping bindings in the zip would mean import silently rewrites someone else's registry as a side effect of moving files. Rejected auto-merging matching registry keys on import: reintroduces full conflict semantics (what if the destination already binds `.parquet` to something else?) at the registry-key level on top of the folder-level conflicts import already resolves — two conflict systems instead of one |
| D90 | Two-step staged import: stage + manifest, then commit with per-item resolution | `POST /api/templates/import` (TV-8) unpacks the uploaded zip to `home_dir()/.import-staging/<importId>/` and returns a manifest (valid/invalid/`conflictsExisting` per top-level folder) without touching `USER_TEMPLATES_DIR`; `POST /api/templates/import/{importId}/commit` (TV-9) applies a per-item `overwrite|skip|keep-both` resolution and moves from staging. Hardening at stage time: uncompressed size > 50 MB, entry count > 2000, or a single entry > 25 MB rejects the whole upload (zip-bomb guard); any entry that's absolute, contains `..`, normalizes outside the staging root, or is a symlink rejects the whole upload (zip-slip guard); `importId` is a `secrets.token_hex` token, staging dirs expire and are swept opportunistically per call | Conflicts can only be known after inspecting the zip's contents against what's already installed, and the ask was **per-conflict** resolution, not one global overwrite/skip flag — that needs a durable server-side step between "uploaded" and "committed" so the UI can show a table and let the user decide per item. Rejected one-shot import with a global flag: no per-item control, and an all-or-nothing reject on any conflict punishes an import bringing 9 new templates and 1 name collision. Zip-slip/zip-bomb guards are non-negotiable server-side regardless of any client-side zip preview, since the upload is untrusted input reaching the filesystem |
| D91 | UI exposes the full key grammar, including null/disable, not a simplified subset | The Row editor's key pattern builder (create mode) covers all four CT-3 key shapes — simple, compound, wildcard, directory — with live preview and client-side validation (server re-validates, authoritative); "Disable for this type" (writes `null`) is a first-class action next to Save/Reset (TV-15) | The registry grammar already supports compound (`.tar.gz`), wildcard (`.*.json`), directory (`.zarr/`), and null-disable keys server-side (§16) — some already used by the shipped built-in registry. A UI that only handled simple `.ext` keys would make those existing, valid registry states unreachable or un-undoable except by hand-editing `registry.json`, defeating the point of shipping a management UI at all. Rejected a simple-keys-only UI with "edit the JSON yourself" for the rest: makes the UI a subset tool rather than a full replacement for manual registry editing |
| D92 | Templates management ships as its own route, not folded into Preferences | New sentinel route `/view/_templates` + `Templates.tsx` (TV-11), reachable from a Sidebar button beside the Preferences gear (TV-12) — not a new section on `/view/_prefs`. **Revised 2026-07-09 (owner):** the read-only registry glance was *removed* from Preferences (PF-7, §20.5) once this view shipped — one home for bindings, not a glance in Preferences plus an editor here. The `GET /api/templates/registry` endpoint stays (unchanged contract, TV-4); it is now consumed by the Templates view instead of Preferences | The feature's surface — a bindings table, a multi-field Row editor modal, an inventory panel, and a multi-step import wizard — is materially larger than any existing Preferences section (§20's four are each a thin client over one existing backend) and has its own mutation-triggered refresh lifecycle. Folding it in would turn Preferences into a mixed bag of one-off toggles and a full CRUD surface. Rejected replacing PF-7 in place: breaks the "quick read-only glance" Preferences currently serves, and the frozen contract explicitly keeps the old endpoint working for it. Rejected a modal launched from Preferences instead of a route: loses URL-addressability/bookmarkability of a specific filter or row-editor state, inconsistent with every other shell surface (D8/D20/D40) being reachable via URL |
| D93 | User templates are deletable with an export-first prompt; core never | `POST /api/templates/delete` (TV-19) removes one folder under `USER_TEMPLATES_DIR` only — **core is read-only**, a core-only name 404s and its folder is untouched; unsafe names → 400, symlinks rejected. The Library Delete action (user rows only) opens a confirm modal offering "Export & delete" (downloads a recovery zip first, deletes only if that resolves), "Delete without export", or Cancel. Registry bindings are **not** rewritten — a binding to the deleted name resolves broken until rebound (folder-only, like export/import). **(Bindings stance and modal buttons superseded by D109: two default-on checkboxes — export-first + a `cleanRegistry` user-registry sweep — with Delete/Cancel only.)** | Owner ask 2026-07-09 (delete was previously deferred to the file explorer). Export-first because a delete is destructive and a template folder is the only copy; letting the user grab a zip in the same gesture makes it recoverable without a trash/undo system (D3 single-user, no server-side trash). Rejected cascading the delete into registry edits: two mutations in one destructive click, and a broken binding is visible + fixable in the same view; rejected a soft-delete/trash (no infra for it, file explorer already reveals the folder) |
| D94 | The `/view/_templates` tab lives in the URL (`?tab=`), switched via navigateUrl | The active tab (bindings default / `?tab=library`) is derived from `location.search`; switching calls `navigateUrl` (pushState + `fused:navigate`) so the nav epoch bumps, App remounts the view, and it re-derives the tab — and browser back/forward moves between tabs via popstate | Owner ask 2026-07-09 (back/forward should work across tabs). Chosen navigateUrl over raw `history.pushState`: the nav epoch (`useNavEpoch`) listens to `popstate`/`fused:navigate`, not the `fused:urlchange` that a bare pushState fires, so a raw pushState changed the URL but never re-rendered the view. Deriving from the URL instead of local state keeps "URL is state" (D40) consistent and needs no popstate listener — the epoch remount already re-reads it |
| D95 | Splice token `"..."` removed; empty list `[]` disables like `null` | The list-splice grammar (D60/CT-11) is **gone**: a `"..."` registry entry is no longer expanded to the built-in list. It resolves to no folder, so it is treated as an ordinary **dangling name** — dropped from the rendered mode list with a `template_error`, surfaced as a broken (`exists:false`) ref in the Templates view. `PUT /api/templates/registry` **accepts** it — a dangling name is saved, not rejected, so a binding with one bad entry is never un-savable — and the row editor shows a warning naming it (nothing auto-removed, the user decides). PUT rejects only structurally invalid entries (non-string/empty). Separately, an **empty list `[]` now disables** previews for a type, exactly like `null` (no built-in fallback) — `_names_from_value` maps both to disabled. Built-in `registry.json` never used a splice, so nothing shipped breaks; the directory-view compose example (D81) becomes an explicit list | Owner call 2026-07-09. The splice was a hidden indirection — a binding's real mode list wasn't visible in the JSON, and a dangling/typo'd entry masqueraded as intentional; making every name literal keeps the registry WYSIWYG and lets the management UI flag dead pointers. Empty-list-disables removes a footgun where `[]` silently fell through to the built-in list instead of meaning "nothing", and gives the UI a natural disable path that isn't `null`-only |

### Explorer search — streamed breadth-first walk (2026-07-09, M14)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D85 | The explorer search walks breadth-first, streams NDJSON, and scores client-side; the entry cap is a safety valve, not a budget | `/api/fs/walk` rewritten from `os.walk` (depth-first) to a FIFO `_walk_bfs` generator: **every depth-N entry emits before any depth-N+1**, so an early stop always leaves complete shallow coverage. `?stream=1` streams `{"entries":[…]}` NDJSON batches (500/line) + one terminal `{done, truncated, total}` line; client disconnect closes the generator and cancels the walk. Non-stream shape unchanged for old clients. Pruning: `.git`/`.venv` join `WALK_IGNORE_DIRS` (never emitted nor descended, hidden mode or not); macOS packages (`.app`/`.framework`/`.bundle`/`.photoslibrary`) emit as one leaf entry, never descended (Finder semantics). Cap raised 20k → **200k** and reframed as a memory/latency valve — BFS means it only ever cuts the deepest levels of pathological trees, and the UI reports it (live `N matches · M scanned…` counter, `+`-suffixed final count, zero-match message naming the covered count). The shell fetches **one hidden-inclusive dataset** (`hidden=1` always) and gates dot-entries at display time by query intent (dot-leading segment shows them) — `.py` is an extension search, intent flips never refetch. Scoring stays client-side, incremental: flushes throttled to 200 ms, each scoring only newly arrived entries merged into the prior ranked list; full re-scan only on query/intent change. Results page in 250-row chunks via a sentinel row + IntersectionObserver; the query mirrors to the URL debounced 200 ms (Safari throttles `replaceState` and then throws). | Owner report 2026-07-09: search unusable — depth-first + 20k cap let `~/Desktop` eat 15,926 slots so Movies/Music/Pictures had **zero** searchable children; `/Applications` burned 7,557 entries inside one `.app`; first paint blocked ~1 s on the full walk; a later report found per-chunk full re-scoring saturated the main thread (stuck stale-dim, dead clicks) — hence the throttle + incremental merge. Client-side scoring kept over a server-side paged search (owner probed this): ranked fuzzy search must scan the full corpus per query wherever it lives, the "server" is the same machine (moving ~30 MB between processes saves nothing), per-keystroke ranking then costs an HTTP round trip + Python-speed matching, and the local-tool precedent (fzf, VS Code Quick Open, Everything) is corpus-in-matcher-memory. Revisit only as a persistent Spotlight-style index if the cap ever genuinely bites. Rejected: walk-more-on-scroll (can't rank what you haven't walked — scroll pages the *rendered* rows instead); a second hidden-only walk per dot-query (double fetch, wrong-dataset races — the old `forHidden` state machine dissolved once one dataset carried both). |

### Bookmark files groundwork (2026-07-10)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D97 | Bookmark names are globally unique, case-insensitive; duplicates auto-suffix, existing files migrate on GET | The namespace is **all bookmarks** — top-level and folder children together (folder names are a separate namespace, untouched). The uniqueness key is the **sanitized filename stem, lowercased** (`sanitizeBookmarkStem`: `/ \ :` and control chars → `-`): case-insensitive (`notes` collides with `Notes`) and sanitize-collision-proof (`a/b` collides with `a:b` — both would export as `a-b.bookmark`). **Going forward** the frontend enforces it inside `addBookmark`/`renameBookmark` (`lib/bookmarks.ts` `uniqueNameIn`, exported preview `uniqueBookmarkName`): a colliding create/rename silently becomes `<name>-1`, `<name>-2`, ... — the first suffix free against every current name — never a rejection. **Existing files** migrate once in `GET /api/bookmarks` (`shell/bookmarks.py` `_dedupe_names`): oldest by `created_at` keeps its name, each newer duplicate gets the first free suffix (a pre-existing literal `x-1` is dodged, the duplicate jumps to `x-2`); the migrated tree is written back only when something changed, so the routine GET stays read-only and the migration is idempotent | Groundwork for bookmark files: a bookmark's name becomes its `<name>.bookmark` filename, and filesystems are one-name-one-file (case-insensitive on the default macOS volume — hence case-insensitive here). Auto-suffix chosen over rejecting the mutation: SB-3's one-click no-prompt capture has no UI moment to report a name conflict, and Finder's own "copy 2" precedent matches. Migration on GET (not PUT, not startup) because GET is the single load path every shell boot already hits; PUT-side validation was rejected — the server would have to either reject (breaking last-write-wins whole-tree PUTs from older tabs) or silently rewrite what the client just committed (cache/server divergence, D77 poll notwithstanding) |
| D98 | "Save to disk" writes a portable `<name>.bookmark` (format v1) into the deepest common ancestor dir of the bookmark's targets; the frontend computes the file, the server only validates and writes | File format v1: `{version: 1, name, icon?, kind: single\|panel\|tab, path?, search}` — `path` (single only) is the target relative to the `.bookmark` file's own dir; `search` is the bookmark url's search string verbatim, no leading `?`, except panel/tab `_layout` leaf paths are rewritten relative to the save dir (grammar, nesting, per-leaf queries and global params untouched). Save dir = the single target's containing dir, or the deepest common ancestor directory of all layout leaves, compared path-segment-wise (`/a/bc` vs `/a/b` share `/a`, never a string prefix) over each leaf's containing dir — every leaf sits strictly under it, so relative paths never contain `..`. Filename = sanitized bookmark name + `.bookmark` (`/ \ :` and control chars → `-`; names are already globally unique, D97). Split: `lib/bookmark-file.ts` (`bookmarkSaveTarget`) computes `{dir, filename, content}` reusing the router codec and the `_layout` codec; `POST /api/bookmarks/export` (X-Fused-guarded) validates — absolute existing dir, bare `*.bookmark` filename, content parses as JSON with an int `version` — and writes verbatim, overwrite allowed. Not savable (button greyed, title says why; the enabled title shows the exact destination): any leaf without an absolute fs path, zero leaves, or no common root (different Windows drive letters) | Relative paths keep the file portable — a folder moved or synced with its `.bookmark` inside still resolves; the common-ancestor rule is the deepest dir that can express every leaf without `..`. Frontend-computes/server-writes because the `_layout` grammar's only parser lives in the shell codec (`splitShellSearch` — plain query parsing cannot read it); duplicating it in Python would be a second parser to drift, so the server stays a dumb validated writer. Overwrite-not-reject: the name is globally unique (D97), so an existing file is a stale snapshot of the same bookmark and a re-save is a refresh |
| D99 | Double-clicking a `.bookmark` in Finder opens the described view: the app registers the doc type (Owner) and routes to a `/view/_bookmark` sentinel; the frontend resolves relative paths, the server only reads. macOS only | Doc-type registration (`scripts/setup_py2app.py`): a new `CFBundleDocumentTypes` entry at `LSHandlerRank: Owner` — unlike the existing extensions, `.bookmark` is our own format — backed by a `UTExportedTypeDeclarations` for `io.fused.render.bookmark` (conforms to `public.json`; identifier deliberately NOT branch-suffixed — every build describes the same format). Open path: `app.py view_url_path` (module-level, AppKit-free, testable) maps any Finder-opened `*.bookmark` (case-insensitive) to `/view/_bookmark?file=<url-encoded abs path>` instead of `/view/<path>`; the existing pending-open queue and single-instance handoff are untouched. The `_bookmark` sentinel (App.tsx + `views/BookmarkOpen.tsx`) calls `GET /api/bookmark-file?path=` (`shell/bookmarks.py`, unguarded read-only GET like `GET /api/bookmarks`) which validates — absolute path, exists, `*.bookmark`, JSON dict with `version == 1` (anything else 400s with a clear message, forward-compat) — and returns `{dir, bookmark}`; `lib/bookmark-file.ts` `bookmarkOpenUrl` (the exact inverse of D98's relativize, same file) absolutizes `path` / every `_layout` leaf against `dir` (already-absolute paths pass through, defensive) and the view `location.replace()`s to the result — single, `_panel` or `_tab` by `kind`. Browsing to a `.bookmark` in the explorer renders the same redirector instead of StatView. Errors render in place, never redirect (no loops) | Frontend-resolves/server-reads mirrors D98's split for the same reason: the `_layout` parser lives only in the shell codec, and Python must not grow a second one. Sentinel page over a server-side 302 because the resolution needs that codec, which only runs in the browser. macOS only: the doc-type plumbing is py2app/LaunchServices; other platforms still open the file via the explorer path. `replace` over `assign` so Back never re-enters the redirect page |

### Explorer search — gitignore-driven pruning (2026-07-10)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D100 | The walk prunes what each repo's own gitignore ignores, via a streaming `check-ignore` co-process; the hardcoded dir set shrinks to a universal floor; search excludes rather than dims (revises SR-2, D85's pruning + D-#75's walk dimming) | Inside a git repository, `_walk_bfs` drops every entry the repo's gitignore rules ignore — not emitted, not descended. Verdicts come from `_IgnoreOracle`: one long-lived `git check-ignore --stdin -z -v -n` subprocess per repo answering path queries incrementally (measured ~14 µs/query; `-v -n` echoes all four NUL fields for every query, making the stream pairable, and exposes the matched pattern's sign so negations like `!keep.log` are honored — a match on a `!` pattern is NOT ignored). Queries are chunked (200/write) so git's stdout can't fill the pipe mid-write (co-process deadlock). Repo tracking rides the BFS queue: each dir inherits its parent's repo root, a `.git` child (dir or worktree/submodule gitfile) starts a nested repo with its own rules, and a walk rooted below a repo top resolves it via one `git rev-parse --show-toplevel`. A `.gitignore` with NO repo in scope (un-inited project, Obsidian vault — the owner's content-engine/.env report) starts a standalone ignore root: the oracle grafts the directory onto a shared lazily-created empty `GIT_DIR` as its `GIT_WORK_TREE`, so the same check-ignore co-process honors standalone .gitignore files, cascading and negations included. Oracles are LRU-capped (`WALK_MAX_ORACLES` = 8) and all closed when the walk generator ends; any git failure marks that oracle broken → no pruning for that repo (optimization, never a dependency). `WALK_IGNORE_DIRS` shrinks in role to the universal floor (`node_modules`, `__pycache__`, `venv`, `.venv`, `.git`, `site-packages` — new) covering non-repo junk and `.git` itself. Consequence: walk entries no longer carry the `ignored` dimming flag (#75's per-batch `_annotate_ignored` removed — nothing ignored survives to dim); `/api/fs/list` keeps dimming. Measured: fused-render repo walk 292 entries in 43 ms (gitignored `shell-dist`, lockfile churn etc. gone). | Owner call 2026-07-10 ("check if the folder has .gitignore and ignore those files — makes much more sense than hardcoding"): the hardcoded set could never enumerate every ecosystem's junk (`dist`, `build`, `.next`, `target`, `.turbo`, …), while each repo's gitignore is the project's own authoritative noise list. Search-excludes-vs-listing-dims mirrors VS Code (explorer shows gitignored, Quick Open hides). Rejected: parsing .gitignore in Python (reimplementing git's semantics — nested files, excludesfile, negation precedence — when `check-ignore` is the authority, same posture as #75's `_git_ignored`); one subprocess per directory (thousands of ~10 ms spawns per walk); one giant upfront batch per repo (the walk discovers paths incrementally — a co-process answers as it goes); pruning by the `ignored` flag client-side (entries would still be walked, streamed, and scored — the win is not descending at all). No user toggle yet — add "search ignored files too" only if someone actually needs to find a build artifact. |

### Bookmark localStorage migration removed (2026-07-14)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D104 | The one-time legacy `localStorage["fused.bookmarks"]` import (D75) is deleted outright, not just left dormant | `lib/bookmarks.ts` drops `LS_KEY`, `MIGRATED_KEY`, `legacyMigrated`/`markLegacyMigrated`, `readLegacy`, and `mergeLegacy`; `hydrateBookmarks()` is a plain GET-into-cache with no import branch. The server's `exists` flag on `GET /api/bookmarks` (`shell/bookmarks.py`) is unchanged — it still distinguishes absent/corrupt from a valid (possibly empty) file — but its docstring/comments no longer describe it as gating a localStorage import. D21/D75's historical entries are left as-is (they record what was true when written) | Owner call 2026-07-14: every pre-D75 install has long since had its bookmarks migrated to `~/.fused-render/bookmarks.json` on first boot after that release; no interim version is being supported going forward, so the dormant fallback code is dead weight. Rejected keeping it "just in case" — the whole point of the `MIGRATED_KEY` marker (which already decoupled the import from `exists`) was to run the merge at most once per install; those installs have all run it by now |
| D105 | "New template" scaffolds from a starter kit shipped outside `templates/`, with a self-contained authoring skill; extensions chosen at create time; "Open in Claude" spawns Terminal (macOS-only) | New backend endpoints (TV-20/TV-21). **Starter kit lives at `fused_render/template_starter/`** — inside the package (a `git add` makes hatchling ship it, `.claude/` dot-dir and all; a wheel-build test confirms it) but **deliberately outside `fused_render/templates/`** so the inventory scanner (which walks `TEMPLATES_DIR`/`USER_TEMPLATES_DIR`) never sees it and it never resolves as a template. It holds `template.html` (required), `reader.py` (optional read-only data-prep stub), `CLAUDE.md` (authoring contract), and **`.claude/skills/template-authoring/SKILL.md`** — a **self-contained** authoring skill copied into every new template, so each scaffolded folder carries its own guidance (no dependency on a repo-level skill). **`condition.py` is documented but NOT scaffolded by default** (the common case is unconditional; the author adds one only when gating). `POST /api/templates/new {name, extensions}` copies the kit into `USER_TEMPLATES_DIR/<name>` and, for each extension, **appends** `name` to whatever list that key currently resolves to (its user override, or the core default) via the **same per-key RMW as D87** (`_apply_binding`, shared with PUT) — never a whole-list replace, so an existing multi-mode binding keeps its other templates. Extensions are asked at create time, then editable via the ordinary Row editor (TV-15). `POST /api/templates/open-in-claude {name}` runs `claude` in the folder via `osascript`/Terminal.app; `_claude_bin()` is replicated from `templates/claude/agent.py` (not imported — a template folder is not an import root) | Owner ask (M15 follow-up): make authoring a real flow, not "hand-create a folder and hand-edit registry.json". Starter-outside-`templates/` keeps the scaffold from polluting the inventory or shadowing a name while staying wheel-shipped (one source dir, copied per template). Per-template self-contained skill chosen over pointing at a repo skill so an exported/moved template stays authorable anywhere. `condition.py` left unscaffolded to keep the default template minimal — a gate is opt-in, and an empty/always-true `condition.py` would be a footgun (fail-closed on a broken one). Extensions-at-create reuse D87's per-key RMW rather than a whole-file write so a create never clobbers concurrent binding edits. macOS-only Terminal spawn because `osascript`/Terminal.app is the only wired path today; other platforms return a clear error rather than a broken half-launch (Linux/Windows terminal spawn deferred until needed) |
| D106 | Starter-kit authoring skills are **build-time copies of the canonical repo skills** — single source, no committed duplicate (refines D105) | Replaces D105's bespoke `.claude/skills/template-authoring/SKILL.md` (deleted). The scaffolded template now carries the **two canonical skills** — `fused-render-authoring` (writing `template.html` + `reader.py`, the `fused` runtime bridge) and `fused-render-custom-templates` (registration, registry keys, `condition.py`, `icon.svg`) — copied verbatim from the repo-level `skills/<name>/`, which stay the sole source of truth. Two delivery paths, both keying off `skills/`: (1) **wheel build** — the hatchling hook (`scripts/hatch_build.py`) copies the two skills into `fused_render/template_starter/.claude/skills/` at build time, gitignored there and shipped via pyproject `artifacts` (same not-committed-but-packaged pattern as the Vite shell, D54); `POST /api/templates/new`'s copytree then carries them into the new folder. (2) **editable/dev install** — the gitignored starter skills dir is absent, so `_ensure_starter_skills(dest)` resolves the repo `skills/` dir (`../skills` relative to the package) and copies the two folders directly after scaffolding. If neither source exists, creation proceeds **without** skills (never fails). `CLAUDE.md` in the starter is a lean redirect (what the folder is + a pointer to the two skills), no contract content duplicated | Owner ask: skill content should have a single source. The bespoke per-template skill (D105) duplicated the canonical skills' contract, so edits drifted between two copies. Build-time copy keeps the wheel self-contained (an exported/moved template still carries its guidance) while committing zero duplicates — the same trade-off D54 already accepts for the shell bundle. The dev fallback exists because editable installs skip the build hook (D54), so tests and dev scaffolds resolve `skills/` at create time instead |

### Template binding recommendations in export/import (2026-07-15)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D107 | Export zips carry a root `recommendation.json` — a **template → keys** map of each exported template's bindings, built by reverse lookup over the MERGED registry; recommendations only, never registry rows (refines D89) | `GET /api/templates/export` (TV-7) always writes a root-level `recommendation.json`: `{"version": 1, "recommendations": {"<template>": ["<registry key>", …]}}` (TV-22). Built per export by reverse lookup over the **effective merged** registry (user shadows core per key, TV-2): a template maps to every key whose effective list contains its name. Templates with no bindings are omitted; the file is written even when the map is empty; names and key lists sorted (deterministic zips). Import staging (TV-8/TV-23) parses it and excludes it from stray-file warnings; malformed JSON/shape → non-fatal warning, recommendations dropped (the folders still stage); `version != 1` silently ignored (forward-compat); CT-3-invalid keys filtered at staging with a per-key warning. Each valid staged item gains `recommendedKeys: [{key, status}]`, status ∈ `new` \| `already-bound` \| `disabled` (key has a user `null`/`[]` override) | Owner ask 2026-07-15: an exported template should carry *what it was for* — the receiving machine otherwise gets an inert folder and a guessing game. **Template → keys chosen over registry-key → list slices**: a slice would encode the author's mode *ordering*, and applying it on import would clobber the importer's own list order/composition for that key; a template→keys map only says "these keys suggest me", leaving ordering entirely to the importer's registry (append-only, D108). Merged-registry reverse lookup over builtin-or-user-file reads: what the author actually *sees* bound is the effective merged value — exporting a core template whose key the user re-ordered should recommend the user's reality, not a stale core row. Refines (not overturns) D89: still no `registry.json` in the zip, still nothing auto-merged — the sidecar is an offer the wizard surfaces, applied only per explicit user tick |
| D108 | Import commit applies accepted recommendations **append-only** after the folder moves, against **final** names; rename-follows-bindings; disabled keys re-enable only on explicit opt-in | `POST …/import/{id}/commit` (TV-9) gains optional `bindings: {originalStagedName: [keys]}` (TV-24). The whole map is CT-3-validated **before any move** — a 400 leaves the stage intact; a corrupt user registry is refused up front. After the moves, bindings resolve through the landed-name map: skipped/invalid templates' bindings are ignored; a keep-both rename binds the **new** folder name (the binding follows what actually landed). Application mirrors TV-20/D105's additive posture per key: already-bound → no-op; key only in core → the user entry is created as the **full core list + appended name** (never a shorter shadow); user-disabled key → re-enabled as core list + name, but **only when a binding for that key was explicitly requested**; appends always land at the END, never reordering existing user bindings. Response gains `bindingsApplied: [{key, template}]`. Wizard (TV-25): master toggle + per-template chips — `new` default ON, `disabled` default OFF (amber "disabled by you" badge, warn line on re-enable), `already-bound` inert (green badge, never sent), "+ add" custom-key chip; skip greys the strip, keep-both shows "will bind as `<renamed>`", commit button counts bindings, step 3 lists what applied. A zip without the sidecar leaves the wizard unchanged | Append-only over replace/insert-at-author's-position: the importer's existing mode order is their deliberate state (D87's per-key posture); an import must extend, never rearrange. Bind-after-moves-by-final-name because binding a name that never landed (skip) or landed renamed (keep-both) would create dangling or wrong refs — the landed map is ground truth. Full-core-list-plus-name for core-only keys reuses D105's rule: a user entry that shadows core must start as a faithful copy, or the import silently *removes* modes. Disabled-keys-opt-in (default-OFF chip + warning) because `null`/`[]` is an explicit user decision (D95) — an import silently undoing it would be the registry-rewrite side effect D89 rejected; surfacing it as an amber opt-in keeps the user the author of the re-enable. Validate-before-move so a bad bindings map can never half-apply an import (the stage survives a 400, retryable) |
| D109 | Delete modal becomes two default-checked checkboxes (export zip first, clean registry bindings) + exactly **Delete**/**Cancel**; the opt-out bindings sweep **supersedes D93's "registry bindings are not rewritten"** | The confirm modal's three buttons ("Export & delete" / "Delete without export" / Cancel) are replaced: two checkboxes, both CHECKED by default — "Export zip before deleting" (keeps D92/TV-16's export-first guarantee: the delete proceeds only after the recovery-zip download resolves) and "Remove registry bindings for this template" — plus a danger **Delete** and **Cancel**. `POST /api/templates/delete` (TV-19) gains optional `cleanRegistry: boolean` (default false; the modal always sends it explicitly): when true, after the folder delete the **user** registry is swept — every user key whose value references the deleted name drops it, and a key whose value the sweep EMPTIES is **removed entirely** (revert to core), never left as `[]`, which D95 defines as *disabled*. Name matching is exact (names are folder identities, not lowercased like keys); the core registry is never touched; a corrupt user registry is refused (400) BEFORE the rmtree, so a refusal leaves the folder intact. Response gains `registryKeysCleaned: [keys]` when the flag was set | Owner ask 2026-07-15, superseding D93's never-rewrite stance: D93 rejected the cascade as "two mutations in one destructive click", but with the sweep surfaced as a visible default-on checkbox the user can untick, the user authors both mutations in the same gesture — the objection was to *hidden* coupling, not to cleanup itself. Checkboxes over three buttons: the old buttons fused the export question into the primary action's identity and never asked the bindings question at all; two orthogonal toggles + one danger button state each choice explicitly. Emptied-override-removes-key over leaving `[]`: a sweep that left `[]` would convert "remove this template's bindings" into "disable previews for the type" (D95) — a stronger action the user never picked; removing the key reverts to core, the least-surprise state |
| D110 | Read-only truthfulness for archive previews and mounts (SPEC RO-7/RO-8): a **preview** copy from the zip/tar readers lands **0444** (written tmp + `os.replace`, so re-previews and concurrent previews are race-free), and a mount record carries a persisted `read_only` flag — re-detected non-mutatingly on every attach (rc `operations/fsinfo` write features + `config/get` anonymous-S3 check, shared predicate with `_public_object_url`; inconclusive probes persist nothing), overridable via a strict-boolean `read_only` in the create body that detection never overwrites — that `server._writable` folds in (mtime-cached, no store parse per stat), flipping `stat.writable` and the `/api/fs/write` 403 together | Two holes let rw templates edit files whose real backing store never takes the write: (a) an archive member previews as an ordinary writable temp copy, so "saved" edits land in the throwaway extract and silently never reach the archive; (b) under a mount `os.access(W_OK)` always says writable — with `CacheMode=full` a kernel write "succeeds" into the local VFS cache and only fails at the async upload (anonymous `aws-open` being the built-in case). The chmod route for previews reuses the ENTIRE existing RO contract (RO-1..RO-6) with zero template/server changes — the permission bit *is* the marking; deliberate extract/extract_all keeps writable output AND its fail-loud EACCES on write-protected targets (the preview mechanism must not smuggle silent replacement into user exports). Mount detection is deliberately non-mutating: a real write probe would drop junk objects into user buckets, and the VFS cache makes a kernel-mount probe a liar, so structurally write-less backends (http) and credential-less S3 are flagged and anything else — including IAM-restricted creds, which only a real write could expose — stays rw with the existing 403/conflict path as backstop. Re-detect-per-attach (rather than detect-once) so credential changes converge without recreating the mount; tri-state probes so a transient rcd hiccup can't freeze a wrong verdict; mounts.json writers serialized (`_store_lock`) now that the automount thread persists flags. Rejected mounting RO remotes with the VFS `ReadOnly` option for now — it is the deeper fix (kernel-truthful `W_OK`, covers RO-3 writer gates too) but per-mount vfs options split the mount/serve VFS instance unless the serve's option set forks identically, which needs its own validation; named follow-up in RO-8. `mount_view` exposes the flag; the Mounts card shows a "— read-only" hint |

### Windows Explorer "Open with" ships in the wheel (2026-07-15)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D105 | `winopen.py` and its assets are committed and packaged, reversing D-19e63cd's "keep off upstream" | `fused_render/winopen.py`, `fused_render/assets/fused-render.ico`, `fused_render/assets/file_icons/file.ico` (the generic fallback — the per-category icons aren't rendering right yet, so only this one ships for now; `_icon_for_ext` degrades any missing variant to `fused-render.ico`), and the `scripts/windows/` helpers are tracked; `[project.gui-scripts] fused-render-open = "fused_render.winopen:main"` is back in `pyproject.toml`. Hatchling's wheel builder only excludes files matched by `.gitignore` — a file kept out of the wheel via the *untracked/`.git/info/exclude`* route (as this one was) is invisible to that exclusion logic and gets packaged anyway whenever the person building the wheel happens to have it on disk, but is silently absent from any wheel built off a clean checkout (e.g. CI). That mismatch meant `--register` worked for some customers and failed with `No module named fused_render.winopen` for others, purely depending on who built their wheel | A customer's install failed at the file-opener registration step because her wheel came from a clean-checkout build (the new CI release pipeline) that never had this untracked file. Another customer's identical-looking instructions had worked, because she'd been handed a wheel built by hand on a machine where the file physically existed. Keeping a "supported" feature real only on one person's machine is a footgun that scales with every customer install; shipping it for real is the fix, not telling people which wheel to ask for |

### GitHub deep links — fused-render:// clone into the workspace (2026-07-15)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D110 | `fused-render://open?git=<github tree URL>` deep links: OS protocol handler → server confirm page → sparse clone into `~/Documents/Fused` → open `index.html`/folder; trust-on-confirm (SPEC §26) | The link carries the original GitHub URL verbatim as the `git` query param (author copies + prefixes; owner's shape call — action in host position, payloads as params, so future kinds add params, not grammar; the `git` value reads to end-of-string so unencoded URLs survive). macOS `CFBundleURLTypes` + `application:openURLs:` in app.py; Windows an HKCU `fused-render` URL-protocol class in the same `--register` as the Open-With keys; both entry points only ferry the raw URL to `GET /clone?src=…` — parsing, validation and cloning live server-side in `deeplink.py`. Nothing touches disk before the confirm page's button: the page (self-contained `static/clone.html`) previews via read-only `GET /api/clone/info` and its button fires the X-Fused-guarded `POST /api/clone`, which runs the user's own git (`clone --filter=blob:none --sparse` + `sparse-checkout set`) so private repos ride existing credentials. Destination = `<Fused dir>/<subpath basename>` holding the repo root (`.git` kept); re-click = `pull --ff-only`, dirty/diverged trees surface git's error, a foreign dir at the destination is refused, never clobbered. First `/tree/` segment = ref (single-segment refs only — the URL grammar can't delimit slashed branch names). The confirm page states the trust boundary in plain words: once opened, repo content renders same-origin and can run Python via `/api/run` — confirming the clone IS the trust grant (owner call: trust-on-confirm over sandboxing cloned content) | Owner asks 2026-07-15. Protocol-only (no hosted `https://…/open` redirect page) accepted knowing GitHub READMEs strip custom schemes — links work in Slack/docs/terminal, the redirect page can come later without changing the scheme. Sparse clone over GitHub tarballs: update-on-reclick needs `.git`, and tarballs would special-case private repos. Nesting accepted (`<dest>/<subpath>` skeleton dirs visible) over a hidden `.repos` + symlink scheme — less magic, git semantics intact, symlinks are second-class on Windows. Localhost `http://127.0.0.1:<port>/clone` links rejected as the carrier: branch-dependent port, server must already run, and a bare cross-origin GET link must never mutate disk anyway |

### Fused account — in-app login & setup (2026-07-15)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D111 | In-app Fused account surface (SPEC §27, M18): `/view/_account` + `/api/account/*` do `fused cloud login`, `cloud setup`, `env default/delete`, and `cloud logout` from the app; scope = the **managed `fused` backend only**, AWS provisioning stays terminal; the §1 "no authentication/user accounts" non-goal is **clarified, not reversed** | Login: spawn `fused cloud login --no-browser`, capture the authorize URL from child output (PYTHONUNBUFFERED=1), client opens it, `OPENFUSED_LOGIN_RETURN_URL` (loopback-validated) 302s the browser back into the app; completion polled via the DP-2b presence signal, deeper truth via `fused cloud orgs` (`?probe=1`). Setup: `fused cloud setup` as the one tracked background job (202 + job_id, polled `{state, detail}` with the CLI's own lines, 409 when signed out or busy, env-name defaults `fused`/`fused-<env>`). Logout kills-and-waits-out any in-flight login before the CLI deletes credentials. Deploy modal's DP-2b warning and no-envs guidance become working buttons (sign in in place / route to the setup panel); DP-16's bundled terminal wrapper demoted to a power-user escape hatch. Full contract: SPEC §27 (AC-1…AC-11) | Owner ask 2026-07-15 ("login and setup directly in fused-render, no copying CLI commands out of the app"). Mechanics ported from the flow app's proven connect-fused surface rather than reinvented — same CLI, same failure modes already shaken out (stdout buffering, return-URL hook, job polling, logout/login race). Managed-backend-only scope mirrors flow's line: AWS `env create`/`infra serve` provisioning is a long, credential-heavy, terminal-native flow with poor modal ergonomics — not worth blocking the 90% path for. Non-goal clarified because the surface manages the *fused CLI's* credentials for deploy targets; fused-render itself still has no accounts/tokens/users (D3 stands, X-Fused guard unchanged) |
| D112 | The account surface **shells the fused CLI** (subprocess through the DP-3 seam, extracted to `fusedcli.py`) — never the fused Python API in-process, and fused-render never reads or writes a credential itself | All auth/env state lives where the CLI puts it (JWT file, OS-keyring secrets store, envs.json); the app reads presence/status only. The seam module is shared by deploy.py and account.py, both routers mutually acyclic and neither importing server.py | In-process `fused.agent_core` calls were rejected: they'd bind fused-render to fused's internal APIs and version (the app must also drive an EXTERNAL `FUSED_RENDER_FUSED_BIN` CLI it can't import), couple the server's process to the CLI's credential/keyring behavior, and put token bytes in this process's memory for no gain. Subprocess keeps the CLI the single authority (DP-2b's posture), matches deploy.py and flow, and costs only stdout parsing + status polling — both proven. Cost accepted: `login`/`setup` have no `--json`, so the login URL is regex-captured from output and completion is polled |
| D113 | **Stale-request cancellation for `runPython` (SPEC RH-9).** A third `opts` arg: `opts.key` (a string) names a **latest-wins channel** — a newer call on a key aborts the prior in-flight call on that same key; `opts.signal` (a standard `AbortSignal`) composes with it. A superseded/aborted call rejects with a plain `AbortError`, which the runtime's `unhandledrejection` handler treats as benign (no traceback overlay, no console noise). Implemented identically in the local runtime (`static/runtime.js`, fetch to `/api/run`) and the hosted/exported runtime (`fused` repo `widgets/static/fused_render_runtime.js`); the sine example passes `{ key: "sine" }` to demonstrate | The slider loop (`onChange` → `runPython`) fires one request per intermediate value while scrubbing; without cancellation every stale value is still computed server-side and its response can land out of order over the value the slider stopped on. Aborting the fetch frees the browser connection and lets the server drop the now-irrelevant subprocess when it notices the closed socket. AbortError is swallowed (not surfaced) so the existing fire-and-forget pattern (`onChange(draw)`, no `.catch`) keeps working — the losing re-render simply stops at its `await` without an overlay | Owner ask 2026-07-16 ("moving a slider through values should cancel the requests that are no longer relevant"). **Opt-in by key, not automatic by pyPath:** RH-4 allows concurrent calls, and shipped templates fire concurrent same-file calls that must all complete — `claude/agent.py` poll loops, `tar`/`sqlite` readers keyed by action, the netcdf/zarr tile browsers — so auto-cancelling by path would break them. Keying is a deliberate author choice; the default (no key) leaves RH-4 untouched. `AbortSignal` is the web-standard escape hatch for authors who want raw control; the `key` convenience is the one-liner that covers the slider case. Rejected "never-resolve superseded promise" (would strand suspended async frames and hide the reason) in favour of a real AbortError that authors *can* inspect but need not catch |
| D114 | **Make `runPython` stale-request cancellation the DEFAULT (SPEC RH-9, supersedes D113's opt-in).** The channel key now **defaults to the `.py` path** instead of being absent: a new call for a file aborts the prior in-flight call for that same file with no author effort, so an ordinary `runPython("./x.py", params)` slider loop cancels its own stale scrubs. `opts.key` regroups the channel; **`opts.key: null` opts out** (fully concurrent, D113's old default); `opts.signal` composes unchanged. A **superseded** call now **never settles** (instead of rejecting with `AbortError`); an abort from the caller's own `signal` still rejects with `AbortError`. Shipped templates that fire concurrent same-file calls were audited and given `opts.key: null` at the call sites that must all complete (writes, poll loops, detached-worker kickoffs). Local + hosted runtimes stay in lockstep; the sine example now demonstrates the zero-config default | Owner ask 2026-07-16 ("create new PRs for it being default"). The dominant `runPython` use is "recompute this view from current params", where the last value is the only one that matters — making that the default means every slider/scrub page gets smooth behaviour for free instead of each author remembering a `key`. The `.py` path is the natural channel: a page almost always calls one file per logical view. **Never-settle (reversing D113's "rejected never-resolve"):** with cancellation opt-in, only authors who keyed in saw the rejection and could filter `AbortError`; as a *default* it must be invisible to pages that already wrap `runPython` in `try/catch` (the canonical authoring pattern) — a rejected `AbortError` would flash a stale error through their own catch while the latest value is still computing. Never-settling makes the stale continuation simply stop; the "stranded async frame" worry from D113 is bounded (an unreferenced suspended awaiter + its pending promise form a GC-collectible cycle). The safety cost — auto-cancel would abort in-flight writes/polls — is paid once via the `opts.key: null` template audit, and is documented so page authors hitting the same pattern know the escape hatch. `opts.signal` keeps the standard `AbortError` reject because there the caller explicitly asked to abort and expects to observe it |

### Canvas layout viewer — the first conditional template (2026-07-13, SPEC §28)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D114 | `canvas.toml` gets a gated `canvas` mode offered only for genuine canvas files: the gate content-sniffs (basename `canvas.toml` + `type == "canvas"`), and `.toml` lists `canvas` first | Two coupled calls. (1) **Condition gate (CT-12):** `templates/canvas/condition.py` `method(target_path)` returns True only when the basename is literally `canvas.toml` (cheap pre-check, no I/O) AND a `tomllib` parse yields top-level `type == "canvas"` — with a 2 MB size guard before the open and a catch-all that fails closed to False (an ordinary `.toml` is not a `template_error`). The basename check runs first so a plain `.toml` never pays the open/parse cost, and under deferred CT-12 (post-merge semantics) the gate is not run at stat time at all: stat marks the entry `conditional` and `/api/fs/conditions` evaluates it in the background. (2) **Mode ordering:** `registry.json` binds `".toml": ["canvas", "code", "annotate"]` — `canvas` FIRST among the gated offers; the immediate default stays the first unconditional entry (`code`, PT-8), with `canvas` joining the switcher when its verdict allows and vanishing when it doesn't (plain/malformed/mis-named toml), exactly as before this template existed. The reader (`reader.py`) is a bare `main()` + `fused.udf(main)` shim (the las/usd/pdf house pattern), not a directly-decorated `main` | First consumer of the CT-12 conditional mechanism, so the gate's shape sets precedent. Content-sniff over extension-only because `.toml` is a shared config extension (pyproject, ruff, poetry, …) — a `.canvas.toml` compound key was rejected (Fused writes the file as literally `canvas.toml`, no compound extension exists to bind) and a bare `.toml → canvas` default was rejected (it would hijack every config file's preview). Basename-before-parse keeps the per-stat cost off the hot path for the common non-canvas `.toml`. Canvas-first ordering chosen over appending `canvas` last because when the file IS a canvas the layout viewer is the point of opening it; the gate guarantees this only bites real canvases, so `code`-first for everything else is preserved. Bare-main+shim over a directly-decorated `@fused.udf main`: a decorated `main`'s `__call__` sandboxes filesystem access (a canvas read fails), and the runner already invokes the registered udf via its raw `_fn`, so the shim runs identically under the engine while keeping `main` a plain callable for the built-in executor and tests — the same reason las/usd/pdf use it |
| D115 | Sidebar **Recents** — last 3 opened files, urls updating live (SPEC §29) | New shell state backend `shell/recents.py` + `~/.fused-render/recents.json` (`{collapsed, entries: [{url, openedAt}]}`): urls stored verbatim incl. query (D20 posture), newest first, deduped by target fs path, capped at 20 (buffer so 3 valid rows survive missing-file filtering); `GET /api/recents` hides entries whose file no longer exists WITHOUT deleting them; `POST /api/recents/open {url}` accepts only an existing file's `/view/` url (directories, `_`-sentinel routes, embed urls no-op with `recorded: false`); `PUT /api/recents/collapsed` persists the fold with the data (D44 posture). Recording rides the StatView seam: `useRecentsTracking` (lib/recents.ts) is mounted beside the session hooks — records once when the stat confirms a file, then re-records the current url on every param write (`fused:urlchange`, 500 ms debounce), so an entry always carries the file's LATEST params; the currently-open file stays listed. Sidebar: top 3 as basename rows (D22), click = query-preserving `navigateUrl`, arms nothing; heading toggles the fold, count pill as the collapsed signal (no chevron — D44 visual language); section hidden while empty. **Display order is stable slots, not raw MRU (RC-11, owner revision after testing):** the data stays strict MRU server-side, but the visible 3 use session-scoped slots — a displayed file keeps its row (re-open/param update changes it in place), only a not-displayed file entering at the top moves anything, a vanished file's slot fills from the bottom without reshuffling survivors, and boot seeds slots from MRU. Rows keyed by fs path (url mutates every param write — a url key would remount/flash the row); the store notifies only when the visible slice (slot order, slot urls, collapse) changed — url updates DO notify so hrefs stay fresh (RC-3), re-rendering in place with zero movement, and no-op refreshes render nothing | Owner decisions 2026-07-16: live url updates over open-time snapshots (a recent should reopen where you left the file — session restore's posture, surfaced cross-file); files only; missing files hidden silently, never pruned (a mount reconnect or undelete brings them back); the open file included. Server-side dedupe/cap/filter keeps the client dumb — the hook just POSTs the current url and refreshes. The StatView tracking seam was chosen over hooking router.navigate: it already owns the confirmed-file gate (isDir from stat) and the urlchange listener wiring, so live updates and file-vs-dir classification cost zero new plumbing; the server rejects non-file urls as the backstop |
| D116 | **Recent errors for a deployed page** — surface a deployed mount's captured runtime failures (traceback, output tails, params) inside fused-render, read through the fused CLI's `fused share errors`. New read-only endpoints `GET /api/deploy/errors?env&token[&limit&since&until&kind&entrypoint]` (newest-first summaries) and `GET /api/deploy/error?env&token&err_id` (one full record); a shared `DeploymentErrors` component renders an expandable list, mounted below the current-deployment card in the Deploy modal (auto-loads for the one deployed page) and lazily under a per-row "Recent errors" toggle on the account Deployments list (no CLI call per row until opened) | `deploy.py` shells `fused share errors …` through the same `_run_share` seam as `share list`/`create` (D112 — subprocess, never the fused Python API; fused-render mints no URLs and reads no records itself). Deployed pages answer a failing request with an opaque `500 {"error","id"}` (the fused repo's `spec/serve/error-reporting.md`); the id is the join key, and only the mount's owner — through their management credentials / the CLI — can read the record back, so the debugging loop that exists locally (the runtime overlay) finally closes for a hosted page. Read-only, so no `X-Fused` guard (matches `/status`, `/shares`); filters pass through verbatim (the CLI parses `2h`/`3d` sugar). A CLI predating `share errors` fails with click's "No such command", translated to an upgrade hint. Lazy per-row mount keeps listing a busy env from fanning out one subprocess per deployed mount. This is the fused-render consumer of the `share errors` API added in fusedio/fused#337, served by the managed Fused service | Owner ask 2026-07-17 ("prospectively implement, for a deployed file, seeing the most recent errors, using the fused API just implemented"). Prospective: the pinned fused wheel predates `share errors`, so the verb lands with a wheel bump — the endpoints already degrade to the upgrade hint until then. Two surfaces because "a deployed file" maps to the per-page Deploy modal (the direct answer) while the account Deployments list already enumerates every mount, so the same component serves both for near-zero extra cost |

### DMG size optimizations (2026-07-17)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D116 | **Bundle prune pass in build_dmg.sh (step 4a)** — after py2app, before the smoke tests and codesign: delete package `tests`/`test` directories under Resources/lib, all `__pycache__` trees, pip/setuptools/wheel/pkg_resources/PyObjCTest, and the copied Python.framework's developer-only corners (stdlib `test`, idlelib, ensurepip, lib2to3, tkinter, include/Headers/share). Plus a `FUSED_RENDER_SKIP_CODESIGN` escape hatch that skips the whole signing step for size-measurement/dev builds (default behavior unchanged) | py2app's `packages` option whole-copies every forced package (a deliberate D33 choice — the runtime-import blindness documented in setup_py2app.py), which drags ~100 MB of package test suites and ~160 MB of stale `__pycache__` into the bundle. None of it is reachable: nothing in fused_render or any supported user path imports from a `tests` package, the GUI is rumps/pyobjc (tkinter unused), and the bundle has no pip by design (SPEC §19 DP-3). Prune placed BEFORE the existing smoke tests so they validate the pruned bundle, and before codesign so the seal stays valid. Rejected: py2app `excludes` (doesn't reach into whole-copied packages) and zip-freezing (breaks the D33 executor model) |
| D117 | **DMG compression UDZO → ULFO (LZFSE)** | One-line dmgbuild settings change | LZFSE compresses this bundle notably tighter than zlib and decompresses faster on mount. ULFO needs macOS 10.11+ to open — well under the app's LSMinimumSystemVersion 11.0, so no compatibility cost. ULMO (lzma) rejected: marginally smaller but much slower to build and mount |
| D118 | **Strip debug + local symbols (`strip -S -x`) from all bundled .so/.dylib** in the 4a prune pass, before codesign | `find`-driven sweep, per-file `\|\| true` so a stray non-Mach-O file can't fail the build; smoke tests (duckdb via _child.py, fused CLI verbs) run after and gate regressions | Wheels ship unstripped native libs (pxr's libusd_ms.dylib alone: 67 MB, ~96k symbols). Debug/local symbols are dead weight in a distributed app — global symbols kept, so dlopen and inter-lib linking are untouched. Must precede signing: stripping rewrites the file and would break an existing signature |
| D120 | **Declarative bundle manifest (embedded `<script type="application/fused-bundle">`) with `include` globs + computed-`rawUrl` runtime resolution (SPEC EX-7, EX-4a)** | A page declares its own bundle set in-repo via one embedded `<script type="application/fused-bundle">` JSON block; `export.plan_export` extracts it, expands `include` globs (`data/*.json`, `tiles/**/*.png`) against the page dir through the existing safety gauntlet (`_reject_unsafe_rel`/`_within_page_dir`), and folds them **beneath** the caller's EX-6 `include`. The block is **stripped before the `fused.*` dependency scan** (so a JSON value like `"fused.writeFile( decoy"` can't false-positive) and is **unversioned + forward-lenient** (the `type` attr is the discriminator; unknown keys ignored so future directives are additive). Zero-match glob → warning; missing literal → error. Two supporting changes: (1) `_LITERAL_CALL` gains a `(?=\s*[,)])` lookahead so `fused.rawUrl("data/" + name)` is counted as a *computed* path, not mis-collected as a bogus literal `data/` asset (a latent scanner bug); (2) hosted runtime `assetKeyFor` (fused repo `fused_render_runtime.js`) resolves a computed path against the bundled key set (exact `CFG.assets[path]` fast path, else `normalizeAssetKey` → membership in `Object.values(CFG.assets)`), so `_asset` serves it by key. Local `fused.rawUrl`/`readFile` now resolve page-relative for real: the runtime sends the page's own path as `base` to `/api/fs/raw`, which joins a relative `path` against `dirname(base)` (mirrors `/api/run`'s `html` resolve) — so one `fused.rawUrl("data/" + name)` works local AND hosted (RH-1). | Owner ask 2026-07-17: kill the two ugliest export artifacts — a hand-maintained `RAW_URLS` literal table and a fake `_bundle*()` function whose only job was to be seen by the literal scan — with a reviewable, reproducible, in-repo declaration; no fake code. **Embedded `<script>` over a sidecar `fused.bundle.json`** (owner call): fused-render pages are single self-contained HTML files, so the manifest should travel inside the file; the exporter already reads the page HTML, and a single well-delimited block parsed by `json.loads` is more robust than the call-site regex. **No `version` field, forward-lenient** (owner call): the block is additive as the design evolves — an older exporter must tolerate a newer page. **`include` only, `exclude` warned-not-applied** (from the leak analysis): an `include`-only block leaks nothing a hosted page doesn't already expose (the served asset map enumerates every file the globs resolve to), but `exclude` would publish the *withheld* file names in the public page source — so exclusion stays on the EX-6 `/api/export`/Deploy-modal path, kept on the deployment record (D78), off the artifact. **Scanner lookahead** fixes a real misclassification (`rawUrl("p"+x)` previously tried to bundle a file literally named `p`), correct independent of the manifest. **`base` server-side resolution** over client-side page-dir derivation: reuses the exact contract runPython already uses (`html`), avoids duplicating the mode-specific page-path logic in the runtime, and adds no new filesystem exposure (a relative path resolves *more* narrowly than the prior cwd-relative). Verified end-to-end: buildings_to_hexagons exports all 13 `data/*.json` via `["data/*.json"]` with `loadData` collapsed to one `fused.rawUrl("data/" + name)`; the hosted runtime (run under node with a simulated seed) resolves that computed path to the right `_asset?name=` URL. |
| D119 | **usd-core is fetched on demand, not bundled** — removed from the `[bundled]` extra (and from reader.py's PEP 723 header); `convert_worker.py:_ensure_pxr` downloads the pinned usd-core wheel from PyPI on the first USD-file conversion, unpacks it (a .whl is a zip — no pip, the packaged app ships none by design, SPEC §19 DP-3) into `~/.fused-render/usd-site/<version>-<cpXY>/` and prepends it to sys.path | Same large-binary-on-first-use posture as pandoc/typst (docs/install_worker.py). Progress rides the worker's existing progress.json (stage `install-usd`), so the template's convert poll loop shows the download with ZERO page changes — no new reader action, no new HTML. Wheel selected from the PyPI JSON API by pinned version + `cp{maj}{min}` + platform (macosx universal2 / manylinux x86_64 / win_amd64); a failed install surfaces as the mesh layer's best-effort `meshError` | Investigation finding: the packaged app NEVER actually contained pxr — it isn't in setup_py2app's `packages` and modulegraph can't trace the worker's runtime import, so packaged USD mesh extraction was silently broken (`meshError: No module named 'pxr'`). This change therefore FIXES packaged .usd/.usdz mesh preview (verified end-to-end against the built DMG's own python: cold install → mesh.glb, warm re-run) and drops 224 MB from pip-installed `[bundled]` dev environments; DMG size is unchanged. Subtlety: the site dir goes onto sys.path before it exists, and importlib caches the "nothing there" finder — `importlib.invalidate_caches()` after the unpack is load-bearing. Tradeoff accepted: the FIRST preview of a .usd/.usdz file needs network (one ~25 MB wheel download); splat-only NuRec captures and .ply/.obj/.stl/.glb paths never need pxr at all. A dedicated install-worker + reader action (the typst pattern verbatim) was rejected as strictly more moving parts for the same UX — the convert worker already owns a polled progress file |
| D120 | **Hosted bundles mirror the page folder; imported modules auto-bundle** (SPEC EX-1/EX-2/EX-7) | Two coordinated changes so a deployed page's Python "just works" with the same relative layout it authored in. (A) The hosting layer (`fused` repo `build_html_artifact`) keys every bundled file by its **real page-relative path** instead of relocating assets under an `assets/` prefix — at serve time files land under the project root, which is already the runtime's cwd **and** `sys.path[0]`, so a bare `open("data.csv")` and `import helpers` resolve with no rewriting (`openfused.asset_path` is no longer required — and, since it anchors under `<root>/assets/`, no longer applies to fused-render pages). (B) `export.py` now discovers first-party modules a bundled entrypoint imports (static `ast` scan, transitive) and ships them as a new manifest **`resources`** category — bundled so `import` resolves, but NOT on the `_asset` allow-list, so a module's source is never web-served (a page fetches assets, never resources). | Local/hosted parity: fused-render already runs a page's code from its real folder (PT-3), so the hosted path should too — the previous flatten-to-`code/` + relocate-to-`assets/` scheme broke `open()` (needed `asset_path`) and `import` (module not bundled at all). Root-keying is the minimal fix because the Lambda handler already roots cwd + `sys.path[0]` at the extraction dir. Resources kept separate from assets (rather than shipped as web-served assets) so auto-discovery can't silently expose helper source over HTTP. A single-segment key colliding with a handler-internal root file (`fused.py`, `_params.json`, `_runner.py`, …) is rejected at build time — the one real cost of a flat root vs. the old `assets/` sandbox. | Only absolute imports resolving to a `<name>.py` beside the page are bundled; stdlib/third-party and subpackages are left alone, and a relative `from . import x` is skipped (a hosted entrypoint runs flattened, no package context) — documented as a known limit. Fuller fidelity (running each entrypoint as its real file via `runpy` so `__file__`/relative-package imports also work) was deferred as Layer C — it needs the `@fused.udf`/`result=` dispatch reworked and adds risk for a rarer case. Cross-repo: manifest `resources[]` threaded through `load_html_bundle`/`build_html_artifact`, the Fused-backend inline upload (`cli._read_html_bundle_source`), specs (`fused` `spec/serve/fused-render.md`, this repo's `docs/EXPORT.md`). |

### Nested bookmark folders (2026-07-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D121 | **Nested bookmark folders to arbitrary depth (revises D44)** | `BookmarkFolder.children` becomes `BookmarkItem[]` — folders nest recursively, unlimited depth. Frontend (`lib/bookmarks.ts`): recursive `findById`/`removeById`/`containerOf`/`isDescendant` helpers replace the repeated "top level, else one level of children" scans; `allBookmarks()` flattens all depths (armed-bookmark lookup keeps working); `prune()` cascades bottom-up so an emptied nested folder can empty its parent; `deleteFolder`/`toggleFolder`/rename/icon/url mutators find nested folders. `moveItem` drops the folders-top-level-only guard; new **cycle guard**: a folder never moves into itself or any of its own descendants (`isDescendant`, checked before removal; a destination inside the moved subtree aborts with no write). Sidebar: recursive `renderItems` — nested `.folder-children` divs compound the existing rail/indent CSS per level (no CSS change); collapse hides the whole subtree; collapsed-folder active highlight recurses over all descendants. DnD: "into" zone now on any folder row at any depth for dragged bookmarks AND folders; drag-over a dragged folder's own descendants is a dead target; bookmark-onto-bookmark combine (`createFolderWith`) works at any depth — the new folder replaces the target in its own parent's children (owner revision after first hands-on test; initially shipped top-level-only for scope control); search recurses all depths (ancestor folder-name match pulls in its subtree); folder-as-tabs uses bookmark children only (nested folders skipped, not flattened). Server (`shell/bookmarks.py`): `_dedupe_names` collects bookmarks at every depth (name namespace stays global per D97, folder names untouched); `_strip_invalid_children` (the interim crash guard) replaced by recursive `_sanitize_tree` — keep an entry iff it's a dict with a string `url` or a `type:"folder"` with list `children`, same rule at top level, migrate-on-GET write-only-when-changed posture kept | Owner call 2026-07-20: a hand-edited `bookmarks.json` with a nested folder blanked the whole shell (every folder child rendered as a bookmark row; `bookmarkSaveTarget` threw on the missing `.url`). D44 chose one level because nested drop-target semantics looked ~2.5× the cost for a rarely-needed depth — but the schema was already recursive by design, and the depth is now actually wanted. Rejected keeping the strip-on-GET sanitizer as the fix (treats wanted data as corruption); rejected a fixed depth cap (the data model doesn't need one, and the compounding indent rail keeps deep trees legible enough — revisit if real trees get absurd); combine into nested folders was initially rejected to contain drop-zone complexity (the exact cost D44 flagged) but reinstated the same day after the owner's first hands-on test — creating a subfolder by dropping bookmark-on-bookmark inside a folder is the natural gesture, and generalizing `createFolderWith` to "replace target in its own parent array" cost little. No migration needed — old one-level files are already valid nested trees |

### Tile-daemon CORS + access token (2026-07-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D122 | **Open CORS (`Access-Control-Allow-Origin: *`) on the standalone tile daemons, gated by a per-daemon access token — distinct posture from D36's main-server guard** | The four persistent localhost tile daemons — `geotiff/tile_server.py`, `netcdf/grid_tile_server.py`, `map/vector_tile_server.py`, `zarr_aoi/tile_server.py` — each bind their own random port on `127.0.0.1` (so `runPython`'s ~700ms per-call subprocess cost doesn't cap MapLibre pan/zoom responsiveness) and answer every endpoint with `Access-Control-Allow-Origin: *`. Endpoints are read-only GETs (`/meta`, `/tile/{z}/{x}/{y}.png` or `.mvt`, `/hist`, `/value`/`/probe`, `/stats`; `map/`'s `/open` is the one POST, an async warm-up); templates run in the shell's iframe (a different port = different origin), so their `<img>`/`fetch`/MapLibre requests need the response readable cross-origin, which is why the D36 no-CORS trick can't be reused here. **Access control is a per-daemon token, not CORS.** Each daemon mints `secrets.token_urlsafe(32)` at startup, writes it into its state file (`~/.cache/fused-render-*/daemon.json`) next to the port, and returns it from `main(action="ensure")` alongside the port. Every endpoint except `/ping` (a version-only liveness probe) requires `?t=<token>`; a missing/wrong token is a 403 before any dispatch. The templates thread the token into every request: the three sci-viz templates append it in their one `dURL()` chokepoint, `pyramid/` appends it to its `/ltile` URL, and `map/`'s `map_render.py` folds it into the `tile_url`/`status_url`/`meta_url` query strings it hands the client. Token must ride the query string (not a header) because `<img>`/MapLibre tile sources can't set custom headers. `/quit` is token-gated too (the respawn path in `main()` passes the stale daemon's own token); `/ping` stays open so liveness needs no secret. | Contrast with D36 (main `server.py`): D36 withholds CORS headers to make a custom-header preflight fail, blocking blind cross-origin POSTs — that works only because the main server's same-origin shell JS never needs to read a foreign response. The tile daemons have the opposite requirement (their only client *is* a cross-port iframe reading tile bytes), so open CORS is deliberate, not an oversight. But `127.0.0.1` binding is **not** an access boundary against the real threat here — a malicious page in the *same browser* can fetch `http://127.0.0.1:<port>/…` cross-origin, and open CORS lets it read the reply; loopback only stops a different *machine*. Originally (first cut of this decision) the only obstacle was the random ephemeral port — obscurity, not access control. The token closes that: a foreign page can't produce a valid request even if it guesses the port, so the read-only tile/metadata bytes stop being cross-origin-readable. The token lives in the state file, so it's only as private as the local filesystem — consistent with D3 (local code execution is already out of scope; this guards the *browser* boundary, which the port-only scheme left open). Rejected a header-based token (breaks `<img>`/MapLibre tile loads); rejected reflecting the main origin instead of `*` (needs the daemon to know the main port, and the token already gates access so CORS breadth no longer matters). |

### Bundled learn content as a read-only archive mount (2026-07-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D123 | **The repo's `learn/` content ships inside the app as `Contents/Resources/learn.zip` and appears at startup as a builtin read-only mount** | `build_dmg.sh` step 4e (after rclone bundling 4d, before signing — Resources content must exist before the bundle seal) zips `learn/` fresh every run (`zip -qr -X`, `.DS_Store` excluded) into `Contents/Resources/learn.zip` and smoke-tests it with the just-bundled rclone (`lsf ":archive:<zip>"`) — the exact binary shipped must read the exact zip shipped, so an rclone bump that drops the archive backend fails the BUILD. At startup, `run_automount` first calls `ensure_learn_mount()` (before the list snapshot and its empty early-return, so a fresh install with zero user mounts still gets it): under `_store_lock` it upserts a mount record `{name: "learn", remote: ":archive:<bundle>/learn.zip", read_only: true, read_only_user: true, builtin: "learn"}` — rclone v1.74's archive backend (verified nfsmount + read-only on macOS) mounts the zip like any remote. `read_only` is pinned user-level (`read_only_user`) so attach-time detection never reconsiders it and all three read-only layers engage via the existing plumbing (VFS ReadOnly, serve read_only, kernel rdonly). The `builtin: "learn"` marker keeps the record distinct from a user mount that happens to be named "learn" (that mount is warned about and never clobbered); the remote is refreshed each startup so a moved/upgraded bundle never leaves a stale path; the record is removed when the zip is absent (dev checkouts — `FUSED_RENDER_LEARN_ZIP` env override is the dev path). `mount_view` exposes `builtin` so the UI can later hide delete. `ensure_learn_mount` never raises — a storage failure must not break automount | Copying `learn/` loose into Resources was rejected: no mount semantics — the point is to exercise and present the bundled content through the same mounts surface as remote data. A persistent unmarked mount record was rejected: the bundle path goes stale after every upgrade/relocation, and a user deleting the record would be permanent, the wrong semantics for default content (remove-when-absent + refresh-remote + builtin marker give upgrade-proof, default-content behavior instead) |

### HTML files presented as launchable apps (2026-07-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D124 | **Every `.html`/`.htm` file row is styled as a launchable app: app-tile icon, accent-leaning tint, "App" chip, "Open App" verb** | `FileIcons.tsx`: the html glyph changes from a globe to a rounded app tile with a play triangle (same house SVG style); new exported `isAppEntry(name, isDir)` (true for `.html`/`.htm` files, driven by the same `EXT_VARIANT` map) is the single shared definition. `shell.css`: `.file-icon--html` tint moves from `#cd9d75` to `#c9d95e` — html is the **only** file category allowed to lean toward the accent yellow (`--accent #E5FF44`); the "never the accent yellow" icon-tint rule is amended with this one carve-out. New `.app-chip`: a small (10px/14px) muted-accent bordered pill rendered after the filename on html rows in both the main listing and search results; sized well under the 16px icon height so row height never changes (no-jumpy-lists rule). `Listing.tsx`: the row context menu's first entry reads "Open App" for html rows, "Open" otherwise — labels only, behavior/routing untouched. No manifest, no entrypoint marker, no per-folder configuration. | Owner goal 2026-07-21: educate brand-new users that html files are apps you launch, not documents you view. Session constraints — folders move between users (kills explorer-side metadata) and multiple apps can live in one dir (kills an `index.html` convention as sole mechanism) — reframed the problem: rather than distinguishing *the* entrypoint from other html, treat **every** html file as launchable and make it look that way. Strongest education-per-complexity: zero authoring concepts (no manifest/marker/pin to learn), the visual + verb carry the whole lesson. Rejected for now: sidecar/in-file entrypoint manifests (only needed if helper-html clutter appears; if so, invert — mark *helpers* for demotion, since apps are the common case); heuristic content sniffing (false positives, surprising UI); folder-as-app-bundle (changes the explorer's identity, decide separately). Accepted blur: an html *document* (exported report, saved page) is styled as an app — harmless, it opens and shows content. Variant key stays `"html"` internally (rename to `"app"` = CSS + comment churn, zero user-visible gain). `Preview.tsx` untouched — its menu has no "Open" action (the file is already open). |

### Fused account folded into Preferences as a tab (2026-07-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D125 | **The sidebar footer drops its dedicated "Fused account" tile; the account surface becomes a "Fused account" tab on the Preferences page, offered only when Deploy is enabled; the signed-in dot moves onto the Preferences tile itself** | `Sidebar.tsx`: the footer's four-button 2×2 grid (Templates/Mounts/Fused account/Preferences, D111-era addition) goes back to a single row of three (`.sidebar-footer` `display: flex` again, not `grid`) — Templates, Mounts, Preferences. The green signed-in dot (`account-signedin-dot`, same affordance as the deploy dot) rides the Preferences icon instead, gated on **both** `useDeployEnabled()` (new shared hook, `lib/prefs.ts`, factored out of `Preview.tsx`'s private one) and `useAccountLoggedIn()` — a dot for a feature the user hasn't opted into (Deploy) would be a mystery indicator, and clicking the dot does nothing special: it's not its own click target (far too small to hit reliably), it rides the Preferences button's existing one. `views/Account.tsx`'s default-exported page component becomes a named `AccountPanel()` with no page wrapper (the outer `.prefs-page` div moves up to `Preferences.tsx`, which now owns it). `views/Preferences.tsx` gains a two-tab layout (**Render preferences** / **Fused account**, `?tab=account` in the URL — same bookmarkable-tab pattern as Templates' bindings/library split); the account tab button only renders while `prefs.deploy.enabled` is true, and requesting it via URL while disabled silently falls back to Render preferences. The old `/view/_account` sentinel isn't deleted — `App.tsx` redirects it (render-time `history.replaceState`, the same technique the `/` → start-dir redirect already uses) to `/view/_prefs?tab=account`, so existing bookmarks and the Deploy modal's "Set up hosted environment" button (updated to link straight to the new URL) keep working. Preferences' own Deploy-toggle section (renamed from "Deployments" to **"Deploy to Fused account"** — distinct now from the account tab's own "Deployments" share list) drops its `navigateUrl("/view/_account")` link in favor of an in-page `setTab("account")` call, and only shows that link once Deploy is enabled (matching the tab's own gating). | Owner ask 2026-07-21: the account tile read as a second, separate "account" surface competing with Templates/Mounts/Preferences, when in fact fused-render has no account concept of its own — the only reason the corner needs an account affordance at all is Deploy, which is itself opt-in. Folding the dot into Preferences and the panel into a conditional tab makes the sidebar footer reflect that dependency instead of presenting Account as a peer feature. A popover/dropdown on the Preferences tile was considered and rejected: every other sentinel route (`_prefs`, `_templates`, `_mounts`, the old `_account`) is a bookmarkable navigation, not a modal (D80's rationale for Preferences applies here too), so a tab keeps the same pattern rather than introducing a new one. Deleting `/view/_account` outright was rejected: it's linked from the Deploy modal and may be bookmarked, and a redirect costs nothing. |

### Persistent server-down banner (2026-07-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D126 | **A persistent bottom-right banner health-checks the backend and, when it's gone, tells the user to reopen the fused-render app** | New self-contained `components/ServerStatusBanner.tsx`, mounted once in `App.tsx`'s `#app` root (sibling to Sidebar/`#main`) so it survives the epoch-keyed view remounts. It polls `GET /api/config` (already the cheapest no-param endpoint — no new backend surface) every 5s with a plain `fetch(..., { cache: "no-store" })` and a 4s `AbortController` timeout so a hung request counts as a failure and probes never pile up (an in-flight guard also prevents overlap). The banner appears only after **2 consecutive failures** (one blip ≠ server gone); any success resets the counter and, if the banner was up, flips it to a green "Reconnected — fused-render is back." state that auto-dismisses after 5s. Polling pauses while `document.visibilityState === "hidden"`; `visibilitychange`→visible, window `online`, and window `focus` each trigger an immediate probe, and the down state offers a "Check again" button doing the same. Styling: `.server-status*` in `shell.css` next to `.listing-toast*` — `position: fixed; bottom/right: 16px; z-index: 1100` (above the toast's 1000), `--error`-tinted border/title for down, existing `--success` for reconnected, `--bg-alt`/`--border`/`--fg`/`--fg-muted` otherwise, max-width 360px, `role="status"`/`aria-live="polite"`. Copy (owner-approved, exact): title "fused-render isn't running", body "The app that powers this page has stopped or was closed. Reopen the fused-render app, and this page will reconnect on its own." | The backend is a native app the user launches, not a CLI service — when it dies, the only remedy is reopening the app, so the copy says exactly that instead of suggesting commands. Bottom-right (owner-chosen; initially bottom-left) keeps it clear of the listing toast (bottom-center) and content. Polling continues at the same 5s cadence while down so recovery is noticed promptly and the page "reconnects on its own" as the copy promises. Piggybacking on existing fetches (show banner when any API call fails) was rejected: an idle page makes no calls, which is precisely when the user needs to be told the server died. Reusing `Toast.tsx`/`ErrorBanner.tsx` rejected: both are view-owned/transient; this is app-shell-owned and persistent. |

### Bookmarks flag missing-file targets; Recents stays hidden-when-missing (2026-07-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D127 | **`GET /api/bookmarks` gains a `missing` id side-channel (target confirmed gone from disk); the sidebar flags those rows instead of silently rendering them as valid. Recents (D115) is explicitly left unchanged** | Bookmarks previously had NO existence check at all — a stale bookmark rendered identically to a live one. `fused_render/shell/pathops.py` gains a generic (file-OR-directory) mount-safe existence trio — `mount_exists`/`local_exists`/`exists` — mirroring the existing files-only `is_file` trio (`mount_exists` uses `mounts.rc_stat_for`, fails open on "indeterminate"; unlike Recents' files-only D22 contract, a bookmark may target a directory listing). `bookmarks.py` splits the existing `_fs_path_from_url` into a decode-only `_decode_fs_path` (shared) and the exists-gated wrapper (unchanged behavior, still only used by the history sidecar), flattens the tree to its bookmark leaves (`_flatten_bookmarks`, arbitrary folder depth per D121), and fans the existence checks out concurrently on a dedicated `ThreadPoolExecutor` under one 1.5s wall-clock budget (`_MISSING_CHECK_BUDGET_S`/`_MISSING_CHECK_POOL`) — mirroring `recents.py`'s `CHECK_BUDGET_S`/`_CHECK_POOL`/`_keep_entry` shape exactly, including walking `candidates`/`tasks` by index rather than iterating the `done` set (a `set` of `Task`s has no defined order, which would make the returned id list nondeterministic). `GET /api/bookmarks` becomes `async def` and returns `{exists, bookmarks, missing}` — `missing` is computed fresh every GET and NEVER written into `bookmarks.json` or merged into a bookmark's own fields, so it can never round-trip through the whole-tree `PUT` (D75's contract is untouched). Frontend `bookmarks.ts` tracks the ids in a separate `missingIds` set (not part of `cache`/`BookmarkItem`), exposed via `isBookmarkMissing(id)`; both `hydrateBookmarks` and the existing 30s `refreshBookmarks` poll (D77) refresh it, and the poll's change-detection now also fires on a missing-set change alone (a target can vanish/reappear with the tree itself untouched). `main.tsx` additionally refreshes on window `focus` (immediate, not waiting out the rest of the 30s tick — the common "fixed the file, switched back" case), in-flight guarded the same way `ServerStatusBanner`'s health probe is (D126) so a burst of focus events can't stack redundant GETs on the shared bookmark mutation queue and delay real edits behind them. `Sidebar.tsx`: a flagged `BookmarkRow` gets a `.missing` class (a styling hook only — the name keeps its normal `--fg` color, unchanged from a live bookmark, on owner request) plus a small always-visible `⚠` badge (`--error`-colored, own `position: relative; z-index: 1` stacking context so the row's stretched-link overlay doesn't swallow its hover/title) between the name and the hover-only action buttons; the hover card (`TooltipContent`) adds a "File not found" line. Nothing is auto-removed — same posture as Recents' own missing handling, just visible instead of hidden. **Recents is deliberately left as-is**: D115 already made an explicit owner call to hide (not flag) a missing recent, and that call stands — this feature is Bookmarks-only. | User ask: flag bookmarks/recents whose target no longer exists. Investigation showed Recents already solves this (by hiding, D115) while Bookmarks had zero handling — confirmed with the user to leave Recents' existing behavior untouched and scope this to Bookmarks. Flag-not-hide (vs. mirroring Recents' hide) was the deliberate choice for Bookmarks: unlike Recents (an MRU jump list where a stale entry is disposable), a bookmark is a named, user-curated, permanent entry — silently hiding it would look like data loss, and Templates' existing `exists:false`/broken-ref pattern (D95, `RowEditorModal`'s "surface, don't auto-remove" convention) was the established precedent to follow instead. A per-bookmark persisted `exists` field (baked into the tree) was rejected: it would round-trip through the whole-tree `PUT` and either go stale between polls or require every mutation path to recompute it — a side-channel keeps the persisted format and the D75 whole-tree-PUT contract completely untouched. Reusing `_fs_path_from_url`'s raw `os.path.exists` as-is (instead of the new mount-safe `pathops.exists`) was rejected for the new bulk GET-time check: that call already predates this change and is only exercised on rare, user-initiated writes, but a check running on every 30s poll must not risk the documented NFS-mount GETATTR wedge (`pathops.py`'s load-bearing invariant) — hence the new `pathops.exists`/`mount_exists`/`local_exists` trio rather than calling the existing helper directly. |

### Server-down banner gains a "Start fused-render" launch deep link (2026-07-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D128 | **New action-only deep link `fused-render://launch`: both native handlers make sure the app/server is running and open NO browser tab; the D126 down-banner links to it as "Start fused-render"** | Recognition lives in `deeplink.py` as `is_launch_url(src)`, shared by both platforms: accepted forms are `fused-render://launch`, a trailing-slash variant, and the slashless `fused-render:launch` (some carriers strip the empty authority), all case-insensitive. The action carries **no payload by definition** — any query string or extra path makes the link NOT a launch link (same D110 grammar: action in host position, payloads as query params; a future launch payload would be a new query param, and silently ignoring one today would freeze "ignored" into the contract). `github_url_from`'s unsupported-link error now names both actions. macOS (`app.py` `application_openURLs_`): a launch URL sets `state["docs"]` like any deep link (suppressing the bootstrap's auto-opened home tab) and is then dropped — never turned into a target, never queued on `state["pending"]`, no tab in either the ready or still-booting state; the OS having launched/foregrounded the app IS the action, the server boot is already in flight. Windows (`winopen.py` `_open()`): `is_launch_url` → `_ensure_server(requested_port)` (which already reuses a live server or spawns one detached) then return without `webbrowser.open` — the import of `deeplink` stays lazy inside the existing `fused-render:` branch so the common file double-click path never pays for fastapi. Banner (`ServerStatusBanner.tsx` down state): a plain `<a className="server-status-launch" href="fused-render://launch">Start fused-render</a>` between the body and the Check-again button; `.server-status-launch` shares the `.server-status-retry` rule block (same padding/border/radius/hover) plus `display: inline-block; color: inherit; text-decoration: none; margin-right: 8px`. Owner-approved D126 copy is untouched. | The D126 banner tells the user to "reopen the fused-render app" but makes them go find it — a click target that does the reopening removes that scavenger hunt. No tab on purpose: the banner page already polls `/api/config` every 5s and flips to "Reconnected" by itself (D126), so a tab opened by the handler would always be a duplicate of the page the user is already looking at. Opening the home tab "just in case" was rejected for exactly that reason; so was making `launch` accept-and-ignore payloads (grammar strictness is cheap now, impossible to add back later). A `navigator.registerProtocolHandler`/JS-side launch was never on the table — only the OS protocol registration can start a dead native app. |

### Linux open-with self-integration, standard-MIME tiering, deep links (2026-07-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D129 | **Linux "Open with" uses standard freedesktop MIME types where one exists, custom glob types only for orphan extensions (macOS "Alternate rank" parity)** | The shared association table (`scripts/file_associations.json`, derived from `fused_render.winopen`) gains a nullable `standard_mime` per extension, populated from a curated `winopen._STANDARD_MIME_FOR_TOKEN` map (single source of truth, consumed by both desktop packagers AND the Linux runtime). An extension with a reliable shared-mime-info type (`.py`→`text/x-python`, `.png`→`image/png`, `.csv`→`text/csv`, `.pdf`, `.json`, office OOXML, `application/vnd.sqlite3`, `application/geo+json`, …) registers under that type; only orphans with no standard type (parquet, pmtiles, fgb, laz, gpkg, the 3D-model family, ambiguous `.ts`, …) get a custom `application/x-fused-render-<token>` glob type. The `.desktop` `MimeType=` list emits the effective type per extension (deduped) + `x-scheme-handler/fused-render`; the MIME-package XML defines glob types ONLY for orphans. No `xdg-mime default` is ever set for a file type. | Was: one custom glob type per extension for ALL extensions — which redefines the global type identity of files the shared database already names (a `.py` file becoming `application/x-fused-render-py` system-wide is a hijack), and, combined with an install-time default, would steal the user's chosen default app. The macOS build already solved this with LaunchServices "Alternate rank" (`setup_py2app.py`): custom UTI only for owned/orphan extensions, Alternate (never-steal) for the rest. Porting that posture to Linux means: reuse standard types, mint new identity only for orphans, and never claim a file-type default. Conservatism is deliberate — a WRONG standard type is worse than the custom fallback, because the DE resolves a file to whatever type its globs assign, so naming a type the DB won't assign drops the association entirely; hence uncertain types are left null. A test asserts the runtime-generated XML/MimeType (`fused_render._mime_package`) is byte-identical to the packaging-generated ones, so the two can't drift. |
| D130 | **The Linux AppImage self-integrates its `.desktop`/MIME/icon at first supervisor start; no third-party AppImage integrator required (revises gate (d))** | New `supervisor/_linux/integration.py`, kicked off on a daemon thread from `core.run` via an optional `_backend.integrate` hook (Linux-only; Windows registers via its installer, macOS via the bundle, so both omit it). From a real AppImage (`$APPIMAGE` points at an existing file) it writes, idempotently, `$XDG_DATA_HOME/applications/fused-render.desktop` (Exec=`<quoted $APPIMAGE> %u`, absolute icon path, generated `MimeType=`), `$XDG_DATA_HOME/mime/packages/fused-render.xml` (orphan glob types, generated at runtime from `fused_render._mime_package` — no `scripts/` import), and the icon under `hicolor/256x256`, then best-effort runs `update-mime-database`, `update-desktop-database`, and `xdg-mime default fused-render.desktop x-scheme-handler/fused-render`. A stamp (app version + AppImage path + content hash) under the state dir skips unchanged runs and re-integrates on move/upgrade. `%u` (not `%f`) is required so the DE also delivers `fused-render://` deep links to `Exec` (D110 on Linux). | Was: gate (d) "open-with works **after user-level AppImage integration**" — i.e. depended on the user having appimaged / Gear Lever / a manual desktop-file drop. An AppImage has no install step, so nothing registers it with the DE out of the box; leaning on a third-party integrator is not parity with the macOS/Windows builds, where associations work immediately post-install. Self-integration at first run closes that gap. Discipline mirrors `startup.py`: acts only when packaged (dev `python -m …` is a silent no-op), every step is log-and-continue (a missing `update-*` tool, an unwritable dir — none may stop the app starting), and it runs off the startup path so `update-*` shell-outs never delay the server/tray. Only the deep-link **scheme** gets an `xdg-mime default` (a scheme needs one to route at all); file types deliberately get none — the never-steal-defaults rule from D129. |

### Desktop state standardizes on the flat ~/.fused-render dotdir across Linux and Windows (2026-07-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D131 | **The desktop supervisor's durable state IS the flat `~/.fused-render` dotdir on Linux AND Windows — shared with the dev/CLI by design, no `desktop/` namespace anywhere (state or cache); cache stays OS-native, runtime stays on the tmpfs; no migration (pre-release)** | `supervisor/paths.py`: Linux `discover_linux()` and Windows `discover()` both set `root = Path.home()/".fused-render"` and **`state = root` itself**, with `logs/` and `temp/` as subdirs of it. The supervisor sets `FUSED_RENDER_HOME=paths.state` and `FUSED_RENDER_BRANCH=""`, so `shell/storage.home_dir()` resolves to `~/.fused-render` and mounts land at `~/.fused-render/mounts/<name>` — byte-for-byte what the released macOS app and the dev/CLI already do (macOS never sets `FUSED_RENDER_HOME`, so `home_dir()` falls through to the same dir). Cache stays OS-native and disposable, also without a desktop segment — Linux `$XDG_CACHE_HOME/fused-render` (fallback `~/.cache/fused-render`), Windows `%LOCALAPPDATA%\FusedRender\cache` (fallback `<root>/cache`; a missing `%LOCALAPPDATA%` only steers cache, never fatal). Linux runtime stays on `linux_runtime_dir()` (`$XDG_RUNTIME_DIR/fused-render`, tmpfs, `0700`; fallback `$XDG_CACHE_HOME/fused-render/runtime`). The Linux integration stamp (`_linux/integration.py`) is a single namespaced file (`desktop-integration.json`) under `paths.state`, so it coexists safely with CLI files; the `.desktop`/MIME/icon files it writes still go to `$XDG_DATA_HOME` (D130, unchanged). Nothing in the supervisor purges or wholesale-resets `paths.state` — a hard invariant now that state is the shared root. | One-known-place intent, taken all the way: the desktop app and the dev/CLI intentionally SHARE state — same `mounts.json`, prefs, templates — because "all user config in one known place" is the product, and macOS already ships exactly this. Supersedes the earlier D131 `desktop/`-subtree layout: the user rejected the namespace; the isolation it bought protected against dev-vs-desktop last-write-wins on shared files, but sharing is the intent, and the desktop supervisor only ever runs one server against the dir at a time. Cache is deliberately kept OS-native: it holds disposable GBs (uv/rclone/duckdb) that must stay out of backup scope. Runtime is deliberately kept on `$XDG_RUNTIME_DIR`: sockets/locks belong on the per-user tmpfs at `0700`. No migration because Linux/Windows are pre-release; macOS needs no change (already flat). Rejected: a full XDG layout (scatters state across three roots and buries the user-facing mounts under `~/.local/share`); the `desktop/` namespaced subtree (splits the user's config into two places and diverges from macOS). |
| D132 | **Windows remote mounts enabled through the existing rcd path; WinFsp is user-installed (rclone's own stance), not bundled — pending the licensing call (extends D102/D103)** *(2026-07-24: the not-bundled stance is superseded by D133 — the installer now bundles and chain-installs the WinFsp MSI; everything else here stands, including the runtime detector + friendly message as the declined-UAC fallback)* | The mount stack already routes everything through `rclone rcd`'s HTTP API and `mounts.py`'s `mountType` selection already sends non-darwin to rclone's default `mount` (which is WinFsp on Windows), so only two POSIX assumptions needed fixing in `shell/mounts.py`: (1) **the mountpoint leaf must NOT pre-exist** — WinFsp creates it and refuses to mount over an existing dir, so `attach_mount` skips `os.makedirs(leaf)` on win32, `os.rmdir`s a stale EMPTY leaf, and errors on a non-empty one (POSIX still pre-creates the leaf); (2) **`_force_unmount` never shells out to `umount`/`diskutil` on win32** — a WinFsp mount vanishes with its serving process, so force-unmount = poll `os.path.ismount` within the 15 s budget and report (it does NOT kill rcd: that would tear down every mount, and the shared `_kill_current_rcd` is POSIX-only — it escalates through `signal.SIGKILL`, absent on Windows). A `_winfsp_available()` detector (system DLL under `%ProgramFiles(x86)%\WinFsp\bin\winfsp-x64.dll`, `ctypes.util.find_library` fallback; vacuously True off-Windows) gates only the mount-CREATION path — checked AFTER `attach_mount`'s already-mounted adopt branch returns, so a live mount that survived a restart (re-attached by `run_automount`) is never rejected on a detector false-negative — and returns a friendly "install WinFsp from https://winfsp.dev/rel/" message when the driver is missing for a genuinely new mount. Production rcd teardown already works via the supervisor's Job Object (grandchild auto-enrollment). **Deferred:** bundling the WinFsp MSI in `installer.iss`, dev-mode rcd persistence on Windows, and Windows CI. **Unverified live** (needs a real Windows box + WinFsp kernel driver): the leaf-must-not-exist rule and `os.path.ismount` on WinFsp reparse points — both documented rclone behavior, tracked as the post-merge validation step. | WinFsp is GPLv3-with-exception against this repo's intentionally-unset license (`pyproject.toml`), so — mirroring rclone's own distribution stance — the user installs it; MSI chaining is revisited after the license call. Directory mounts (not drive letters) preserve the D2/D3 invariant that the app only ever sees local absolute paths under `mounts_dir()`. Force-unmount as process-teardown-only: no `umount` equivalent exists on Windows and the macOS escalation ladder is an NFS-flap workaround with no Windows analog. |
| D133 | **WinFsp is bundled in the Windows installer and auto-installed when missing (reverses D132's user-installs stance)** | `build_windows_installer.ps1` downloads the pinned WinFsp MSI (`v2.1` / `2.1.25156`) against its published SHA256 (same discipline as the rclone/zig pins) and stages it as `winfsp.msi` in the bundle root. `installer.iss` carries it as a `dontcopy` [Files] entry and, at `ssPostInstall` (after the payload swap), `InstallWinFsp()` skips entirely when the driver is already present (`{commonpf32}\WinFsp\bin\winfsp-{x64,a64}.dll` — mirroring `_winfsp_available()`), else `ExtractTemporaryFile`s it to `{tmp}` (auto-deleted, never persisted in the payload) and runs `msiexec /i … /qn /norestart` through `ShellExec('runas', …)` — the app itself stays `PrivilegesRequired=lowest`, so that verb raises the ONE UAC prompt of the whole setup. A declined prompt or a failing MSI is only `Log`ged, never a setup failure: mounts then degrade to `shell/mounts.py`'s runtime "install WinFsp" message (that detector + message stay in place as the fallback), so the install succeeds either way | Owner-directed 2026-07-24: end users shouldn't have to install WinFsp by hand from winfsp.dev for mounts to work — the licensing hesitation behind D132's deferral is resolved in favor of shipping it. Chained-MSI over a merged payload: WinFsp is a kernel driver needing an elevated per-machine MSI, which must stay separate from the per-user app install; `runas`+`/qn` over ShellExecuting the MSI with full UI keeps setup one-click (a single UAC consent, no second wizard) |

### Download page adds Windows/Linux; latest.json publish centralized into one job (2026-07-24)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D133 | **render.fused.io download page gains Windows/Linux sections; `latest.json` is now written once, after all three OS builds, merged against the live manifest instead of each build job racing to write it** | `scripts/download_page/index.html` gets a Windows section (`#windows-exe`) and a Linux section (`#linux-appimage`), same before/after-manifest pattern as the existing macOS section — falls back to the GitHub releases page, upgraded to the direct CDN link once `/latest.json` loads. `write_manifest.py` now takes only `VERSION` as required; `DMG_URL`/`DMG_SHA256`/`WHL_URL`/`WINDOWS_URL`/`LINUX_URL` are each optional and, when unset, fall back to whatever is already in the manifest at `EXISTING_MANIFEST_PATH` (if given). `.github/workflows/release.yml`'s three OS jobs (`build-sign-notarize-release`, `build-windows-release`, `build-linux-release`) now expose `version` + artifact-URL job outputs — written only right after that platform's own artifact upload succeeds, not after later unrelated steps (per-platform update manifest, CloudFront invalidation, `gh release upload`) whose failure shouldn't withhold an otherwise-good link — instead of publishing the root manifest themselves; a new `publish-download-page` job (`needs` all three, `if: '!cancelled() && ...'` — deliberately not `always()`, which would also fire on a manually cancelled run) downloads the live `latest.json` (a non-404 fetch failure hard-exits the step rather than silently treating it as empty and blanking every platform this run didn't touch), merges in whatever this run produced, and re-uploads `latest.json`/`index.html`/`og-image.png` + invalidates the CloudFront root paths — the three OS jobs keep invalidating only their own artifact prefixes. The CI role's IAM policy (`terraform/main/iam_github.tf` in the application repo) gains a scoped `s3:GetObject` on `arn:aws:s3:::fused-render/latest.json` (it previously had `PutObject` only), plus a `s3:ListBucket` on the bucket scoped via an `s3:prefix` condition to that same key — `GetObject` alone makes S3 answer a missing object with 403 rather than 404, which would make the first-publish-is-safe check above refuse to ever create the object. | The page's `<meta name="description">` already promised "macOS, Windows, and Linux" (both native installers have existed since D130-D132) but the body only ever had a macOS section — this closes that gap. A single writer for the root manifest was chosen over each OS job doing its own read-modify-write: those three jobs run in parallel on a normal tag push, and a race between three writers each reading a stale copy would silently drop whichever platform lost the race — worse than the small added latency of waiting for all three to finish (the page is already explicitly cosmetic/best-effort, `continue-on-error` throughout). Merging against the live manifest (rather than assuming every run has every platform's facts) specifically protects a `workflow_dispatch` scoped to one platform: rebuilding only Windows must not blank out the still-good macOS/Linux links. |


### Appearance follows the OS by default, with a Light/Dark pin; Linux tray icon follows the desktop panel (2026-07-27)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D134 | **fused-render resolves its appearance from the OS light/dark preference instead of being dark by constant; a shell menu offers System (default) / Light / Dark, persisted per browser profile. Shell chrome and twelve tier-1 view templates gain real light palettes; every other template is explicitly classified and left alone** | Full contract in SPEC §30 (AP-1..AP-15). Shape: `frontend/src/lib/theme.ts` owns the single `localStorage` key `fused-render:theme` (best-effort read/write, the `viewstate.ts`/`sidebarstate.ts` posture), resolves it against `prefers-color-scheme`, and writes the resolved value to `data-theme` on `<html>` — an attribute write, never a re-render, which is what keeps it clear of the standing "a React re-render must never touch a live iframe" rule (D45). The FIRST application is an **inline `<head>` script in `frontend/index.html`**, ahead of every stylesheet, duplicating the key + resolution rule (nothing importable exists that early; a test pins the spellings). `App` mounts `useThemeSync()` for later changes: another window's override via the `storage` event, and — while the setting is System — the OS flipping mid-session, including macOS's automatic sunset switch. `shell.css` is fully tokenized: all 61 hex + 58 `rgba()` literals move into `:root` **unchanged** (verified by expanding both palettes back out and diffing — dark is value-identical), and `:root[data-theme="light"]` redefines the whole set; translucent washes become `rgba(var(--tint), a)` with `--tint` flipping white→black, and light mode maps `--accent` from the brand lime to a same-hue deep olive-lime that clears 4.5:1 both ways so one token still serves text, border, fill and wash. The switch is an **Appearance section on the Preferences page** (`views/Preferences.tsx`, §20) — the ordinary `prefs-section` + `prefs-radio` shape, and the one section on that page not backed by `/api/prefs`, since the preference is per-browser-profile `localStorage` with no server store. View documents are reached by `static/runtime.js`, which resolves the same key in the view's own document and writes `data-theme` there — **only when `<html>` carries `data-fused-theme`**. Tier-1 templates (AP-9) each get a `:root` dark palette + `:root[data-theme="light"]` twin with every stylesheet colour tokenized; `code` additionally drops `CM.oneDark` in light mode and reconfigures the RUNNING CodeMirror view (`StateEffect.reconfigure` off a `MutationObserver` on `data-theme`) rather than rebuilding it. | Users on a light desktop got a dark app with no way out, and users wanting dark pinned had no guarantee either. **Reading in the view rather than pushing from the shell** was the key call: it means the shell never reaches into an iframe, so no theme change can remount/reload a pane or lose scroll, selection or unsaved edits — the `storage` event already fans a change out to every same-origin document for free. **Opt-in via an `<html>` attribute** (rejected: a `<meta>`, and rejected: theming every rendered document): `runtime.js` is injected at the top of `<head>` and is parser-blocking, so a `<meta>` further down isn't parsed yet — and user-authored `.html` views must keep owning their CSS entirely and receive no signal at all. **Inline bootstrap** was rejected-alternative-free: applying the theme from React would flash dark for a light-mode user on every load, which is the one thing the feature can't do. **`--tint` as an rgb triple** over duplicating ~40 wash rules per theme. **One darkened `--accent`** over adding a second accent-for-text token: the lime is unreadable on white as text and near-invisible as a hairline, and 56 call sites use it for all four roles. **Tokenizing every literal** (rather than only the obvious ones) is what the light palette needs to be complete, and a test enforces it so the next colour added can't silently be dark-only. The tier lists are exhaustive over `templates/` with a test asserting it, so a new template can't skip classification. Explicitly rejected: any server-side settings store/endpoint/config file (this is per-browser-profile state — a browser and the desktop window may legitimately differ); migrating or overriding `excel`/`log_studio`/`tableau`'s own toggles and private keys; light palettes for the media/geospatial/studio groups (deferred — accepted consequence: they render dark inside a light shell); restyling dark. `vector` keeps its carto dark-matter basemap and its maplibre paint expressions dark: those are JS strings where `var()` can't resolve, swapping the style at runtime would tear down the added data layers, and light chrome over a dark map is the normal look for a mapping view (`map` is itself light-by-design/exempt). |
| D135 | **The Linux tray glyph is tinted from the desktop's XDG-portal colour-scheme preference instead of unconditionally white, and re-tints live; white stays the fallback for every failure mode** | `_tint_white()` mapped every glyph pixel to white and `_icon_pixmap` was `lru_cache(maxsize=1)`d on the path alone, so the pixmap was computed once per process and could never change — on a light panel that is a white glyph on a light background, effectively invisible (confirmed on Linux). `_linux/tray.py` gains a generic `_tint(image, rgb)` (with `_tint_white` kept as the dark-panel wrapper) and reads `org.freedesktop.portal.Settings.Read("org.freedesktop.appearance", "color-scheme")` on the bus the tray already holds — `0` no preference, `1` prefer dark, `2` prefer light. Only an explicit `2` gets the dark glyph (`_TINT_DARK`, a near-black rather than pure black so it reads as a glyph on off-white panels); `0`, `1`, an unknown value, an absent portal, a D-Bus error, a timeout and a malformed reply all keep white. The read happens BEFORE the interfaces are exported, so the first `IconPixmap` served is already right. `SettingChanged` re-tints and emits SNI `NewIcon` — `IconPixmap` therefore reads through a one-element ref the handler swaps, and `NewIcon` fires only when the tint actually changed. `_icon_pixmap` stays cached (bring-up retries forever on watcher-less sessions) but keys on the tint too, `maxsize=2` so the two variants coexist. `_pack_argb32` and `_ICON_SIZE` untouched; dark-panel output byte-identical (asserted). | Icon tinting is cosmetic, so the entire path is best-effort: nothing about it may prevent the tray registering, hence white-on-any-failure rather than a hard dependency on the portal (many desktops expose none, and the tray already retries bring-up forever on watcher-less sessions — those users must see no regression). The portal setting was chosen over reading a DE-specific gsettings key or shelling out: it is the cross-desktop standard, and `dbus-fast` is already a Linux dependency on a connection that's already open, so this adds none. Live re-tint via `NewIcon` rather than re-registering the status item, so the icon never blinks out of the panel — mirroring the shell's live-repaint choice (D134). Deliberately NOT wired to the in-app System/Light/Dark override: the tray follows the *desktop*, the app follows its own setting, and conflating them would make the tray unreadable whenever the two disagreed. macOS and Windows are untouched and already correct — `menubar-template.png` is a real template image recoloured by the OS, and the Windows tray uses the full-colour `.ico`. |

### App call log: one record per API call a page makes, plus fifteen rounds of adversarial review (2026-07-25)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D136 | **The app call log: one bounded structured record per API call a page makes, written at the middleware, read by a `calls` view template + a `fused-render calls` CLI** | New `fused_render/calls.py` + `templates/calls/` (SPEC §31, CL-1..CL-17; design and rejected alternatives in `docs/CALL_LOG_DESIGN.md`). The record is the serve plane's error record (the `fused` repo's `spec/serve/error-reporting.md`) **widened to successes** — same field names, units and caps — so a page's local numbers and its deployed ones are comparable and render in one viewer. **Write path:** the ASGI middleware creates the record on the way in (`request.state.fused_call`), route handlers enrich it in place (`/api/run` adds the resolved `.py`, params, engine, output tails, traceback; `/api/fs/write` adds bytes + conflict/readonly), and the middleware writes it on the way out — one write point, so a new endpoint is logged by default and fail-open has exactly one place to be correct. A 500 records from the `except` branch because `@app.exception_handler(Exception)` runs in `ServerErrorMiddleware`, outside user middleware. **Attribution:** `runtime.js` sends `X-Fused-Page`/`-Target`/`-Call`; that header is the whole test for "is this an app call", so the shell's own requests are excluded by construction rather than by an endpoint blocklist that would drift. **Store:** append-only `~/.fused-render/calls/<date>-<pid>.calls.jsonl` behind a bounded queue + background writer thread; three independent bounds (per-record caps, a per-page token bucket, retention by age AND directory size). **Two consequences of how this codebase already works:** superseded calls (D114 latest-wins cancellation) are counted but excluded from every latency percentile, and a new `page-error` record (via a `window.onerror` hook the runtime lacked) covers the case where NO call happened. The executor gained `duration_ms` + a `stderr` tail so both engines' records match; `calls/reader.py` joined `INPROCESS_HELPERS` (D72); reading the log never appends to it (shape-matched, since the reader runs from the package, the staged core copy, or a user fork). Preferences carries capture/redaction/retention | Owner-directed 2026-07-24: "a log of the API calls my app makes — what the call was, its stdout/stderr/error/result size, and graphs of response time, call count, response size". **Reusing the serve record shape** over inventing a local one: it is already shipped, already capped, and it makes phase 3 (deployed apps) a widening rather than a second format. **JSONL over the `<file>.json` sidecar** (D82–D84, which `history`/§24 already renders and which would have been the tempting per-page home): every sidecar writer does a whole-file read-merge-write, which at call volume is O(n²) plus a lost-update race — the sidecar is right for low-frequency history, wrong for a firehose. **Over the app log** (`logs.py`): that file is disposable by design (D68) and unparseable prose, and a call log must survive a reboot to graph last week. **Over SQLite:** it would query beautifully, but it adds a concurrency story (two servers, WAL files) to a repo whose whole persistence posture is atomic whole-file writes + last-write-wins; JSONL keeps that posture and stays greppable by an agent with no tooling — and `.jsonl`'s existing `duckdb` binding queries it with no new code. **Capture ON by default** (owner posture): a diagnostic you must enable before the thing you wanted to diagnose is worthless — the interesting call already happened. **Params recorded by default**, same named trade-off the serve spec makes (they are the inputs the author's code already received, usually the whole repro, and already visible in the URL), with a `keys`/`off` pref for a page that passes a secret. **Excluding superseded calls from percentiles** is not a nicety: with D114 on by default, a 40-tick slider scrub would otherwise report "40 calls, p95 3.2s" for one user-perceived request — the chart would lie in the most common interaction the product has. **A template, not shell code** (HV-1/D78 containment) and a **CT-12 conditional peer**, so a page nobody ran grows no dead mode and a user can fork the whole viewer. Rejected for phase 1: client-side reconciliation of the exact superseded/`client_ms` values (the server's `disconnected` inference covers most of it; revisit once the charts show which distinction is actually missed), sampling/coalescing `/api/fs/raw` (log it plainly first, coalesce if the volume proves it), a header chip and a history-view section (shell UI, phase 2), and deployed-app records (needs serve-plane work in the `fused` repo, and per-call records for anonymous traffic must aggregate rather than enumerate — phase 3) |
| D137 | **Four corrections to the call log found by adversarial review of D136: the page must REPORT supersession (the server cannot infer it), "this file" must match a call's entrypoint, reads must be bounded by the window, and the size trim must never delete a live file** | (1) **CL-5a — supersession is client-reported.** D136 relied on `asyncio.CancelledError` out of `call_next` to infer an abandoned call; **that exception never fires** — aborting a fetch does not raise into the handler, so the run completed and was recorded as an ordinary success. Verified with a real browser scrub: 10 ticks produced 10 `ok` records, `stale=0`, all counted in p50/p95. `runtime.js` now stamps a stable `X-Fused-Call` id per call and POSTs abandoned ids to `/api/calls/event` (`kind: "superseded"`, batched per macrotask, flushed on `pagehide`, `keepalive` **not** `sendBeacon` — a beacon cannot set the `X-Fused` header the endpoint requires); `finish()` stamps the outcome from a TTL'd, hard-bounded mark set, consumed once. (2) **The Calls view on a `.py` showed nothing** — the mode the registry offers for a data file, whose `condition.py` gate correctly confirmed it had history, rendered an empty state, because the `page` filter matched only `page`/`target_file` and a `.py` is never a record's `page` (the `.html` is). It now also matches `entrypoint`. (3) **Reads were O(store), not O(window)** — `since` was applied after parsing every record, so a one-hour question parsed the whole retention window (measured 395 ms/op on a 20 MB store, ×4 ops per render, polled every 2 s while following). Files are now read backwards from the tail in blocks, a file whose mtime predates the window is skipped whole, and within a file the first out-of-window record ends it (measured 0.5 ms — ~790×). Files are skipped but never stopped at: same-day files from different pids interleave in time. (4) **The size trim deleted live files** — trimming is whole-file, and when today's file was the only one (exactly the single-day-burst case the cap exists for) the sweep unlinked the file the writer had open, silently discarding the day. Today-dated files are no longer candidates, a new `MAX_FILE_BYTES` per-file roll (`<date>-<pid>-<part>`) bounds within-day growth instead, and a store still over cap logs a warning rather than pretending the cap held. Retention also now sweeps on a day roll, not only at startup and on a 24 h timer | The D136 tests passed while (1) was completely broken, which is the lesson worth recording: `test_superseded_calls_are_counted_but_never_in_the_percentiles` wrote records with `outcome="superseded"` directly and asserted the rollup excluded them — it tested the **consumer** and nothing tested that anything ever **produces** the value. Both halves were individually correct and the seam between them was empty; `TestClient` cannot model a socket abort, so nothing pushed against it. The new tests drive the real path, and one asserts the `runtime.js` wiring textually since no test in the suite executes it. The §6.2 deferral of client reporting to phase 2 was simply wrong — it traded away the feature's central guarantee for ~30 lines, on the assumption that a server-side substitute existed. It turned out cheap anyway because of a timing accident: the client knows at abort time, ~1 s before the still-running handler finishes, so the report lands before the record is written and no append-only mutation is needed. **Still not fixed, deliberately:** a closed tab or reload is reported by nobody and records as `ok` (needs `request.is_disconnected()`), and the abandoned subprocess still runs to completion — `runtime.js`'s long-standing comment claiming the server "drop[s] the now-irrelevant subprocess when it sees the closed socket" is false, and always was. Both are separate changes; the second is an efficiency bug this log merely made visible |
| D138 | **Call-log follow-ups: null keys omitted on write (a healthy record no longer reads as ERROR), the hot path stops re-reading prefs.json, Preferences navigates in-app, `err_id` becomes a real join key, and client-disconnect detection is abandoned as infeasible** | (1) **Null-valued keys are dropped at write time.** A narrow record (a `stat`, a raw read) was mostly `null`s, and the field NAME `"error": null` made every *successful* call render as **ERROR** in `log_studio` — which the registry offers for `.calls.jsonl` and which infers a level by sniffing the raw line for level words (owner report, from the raw store view). Now only a record that really failed contains the word, so the inference is right rather than wrong; records also shrink ~20%. (2) **The hot path no longer reads `prefs.json` per call.** `enabled()` plus the redaction mode opened and parsed it on every call, and `is_first_party` resolved three realpaths — measured 2.76 ms/run (7.0%) of pure logging overhead, now 0.99 ms (2.4%). A 1 s snapshot cache, invalidated by the prefs endpoint on write so CT-5's no-restart rule still holds. (3) **Preferences' call-log action navigates the explorer to the store** (`navigate(dir, {isDir:true})`) instead of revealing it in Finder/Explorer, with reveal kept as a second button: the explorer is *how you reach the view* — a `.calls.jsonl` opens in the `calls` template — whereas revealing in the OS file manager only gets you raw text (owner call). (4) **`err_id` is wired.** It was documented as the join key to a failure and never set. The middleware now mints it in its `except` branch (it runs before the exception handler) and stashes it on `request.state`; the handler echoes the same id into the 500 body, so a screenshot of the error and the record in the log name each other. (5) **`_buckets` is bounded** — idle-and-refilled entries first, then least-recently-seen past the cap; eviction can only ever grant a page more budget, never less. (6) **Client-disconnect detection is NOT implemented** — see the rationale | (6) is the substantive decision. The goal was to mark an abandoned run `disconnected` instead of `ok` (CL-5a's named gap). Both approaches are dead ends under this app's shape, each verified: from a **route**, `BaseHTTPMiddleware` wraps the downstream `receive`, so `request.is_disconnected()` never sees `http.disconnect` and a watcher waits forever — while the same watcher fires immediately with no middleware in front, which is what makes it easy to "verify" wrongly. From the **middleware** it does fire — but `is_disconnected()` peeks by CONSUMING a message off the receive channel, so polling it steals the `http.request` body the downstream route is waiting for and **every request with a body hangs**. The first spike used a body-less route and looked like a clean success; the hang only surfaced when a test made a real POST with a body and waited patiently. That test is now kept as a **regression guard** (`test_a_run_with_a_body_completes_under_the_middleware`) rather than deleted, because the trap is re-approachable. Doing it properly means converting `no_cache_and_log` to pure ASGI so it can tee the receive channel — a change to the hottest path in the server, which belongs in its own commit and not riding along with an observability feature. Meanwhile the common case (a slider supersession) IS reported by the page (D137), so the gap is a closed tab or reload only. Second time this session that a green spike hid the failure case; the pattern is recorded in the design doc's §9.5 |
| D139 | **Two more call-log corrections from a fourth review pass: a day's store files are MERGED on append time (name order does not rank pids in time), and the oversized-record shrink re-runs the same prune every other write does** | (1) **Same-day files are merged, not concatenated.** `_iter_records` walked files in reverse name order and drained each before the next. But a name orders records only by its **date** segment: the pid segment is arbitrary AND compared lexically, so `2026-07-25-8000-000` sorts after `2026-07-25-12345-000`. With two live servers — the case per-pid files exist for (CL-7) — `query` returned the stale process's tail as the newest page, and `--follow` waited out its entire timeout while the live server logged in front of it (reproduced: cursor frozen on the stale file across a fresh write). Now each day's files are `heapq.merge`d on `recorded_at`, descending. The merge is **per day** because whole days cannot interleave (a file only takes appends while the UTC date still matches its name), which bounds open handles to one day's files and keeps the walk lazy — a `limit` satisfied by today never opens last week's — and the merged streams are closed deterministically when the walk is abandoned mid-page. (2) **The shrink path prunes.** A record still over `RECORD_CAP` after the per-field caps was serialized straight from `_shrink`, which marks dropped fields by setting them to `None` — so oversized records kept explicit nulls and carried no `level` or `recorded_at`, resurrecting D138(1)'s `"error": null` ERROR misread on exactly the records most likely to matter. `_prune` is idempotent, so the fix is to run it on the shrunk dict. | Inclusion semantics are deliberately **unchanged**: each file keeps its own exact `since` stop (D137), and the merge decides only the order records come out in. That split is what makes the fix safe to reason about — a store with out-of-order stamps comes back in a slightly odd order rather than losing records, and the proof behind the per-file stop (`occurred_at <= recorded_at`, so a record appended before the window cannot have started inside it) is untouched. Both defects are the same shape and are recorded in the design doc's §9.5: `_iter_records` already carried a comment saying same-day files interleave in time — the fact was understood, written down, and then applied to the file-skip bound only, one line short of the ordering that depends on it equally; and (2) is a fix that missed its own second exit. A comment explaining why a bound is shaped a certain way is evidence to go re-audit every other place that fact bears on |
| D140 | **The call log's read cursor is scoped to the caller's FILTERS, not to the whole store** | `query` took its returned cursor from the first record in the store walk, before `_matches` — so with any filter the cursor was the newest id in the store rather than the newest the caller was shown. `--follow --page X` consequently woke on another page's traffic and then printed no calls for X (reproduced: baseline `other-1` while the only matching record was `mine-1`). The cursor is now taken on the first MATCHING record. Two supporting rules: when nothing newer matched, the caller's own cursor comes back unchanged — returning `None` would read as "start over" and be answered with an unbounded newest page — and with no matches at all it is `None`, with the CLI omitting the cursor line rather than printing one that cannot be passed back. The walk still stops at the cursor by IDENTITY, checked before the filters, so an older cursor that no longer matches them ends the walk instead of reading as purged. | Third distinct cause of the same symptom, which is the reason this is recorded rather than just fixed: D138/D139 and this one were all *the follower reporting no activity when there was activity* — first append-vs-start time, then file-order-vs-merge-order, now store-scope-vs-filter-scope. A false negative in a verification tool is one symptom with many causes, and fixing two of them was no evidence the third was absent. The `calls` view template is unaffected: its Follow re-polls the reader ops rather than comparing cursors |
| D141 | **`--follow` stops waiting out records that are already on disk, and a cursor the scan never reached is no longer reported as purged** | (1) **Follow waits only when the caller's cursor is at the tip.** It compared the store tip against a pre-wait baseline even when an explicit `--since-cursor` already had unseen records behind it — so the normal agent race (ask a human to open a page, the calls land, THEN run follow) timed out holding exactly the records it was waiting for and answered "no new calls" (reproduced: two records on disk, a 6 s timeout fully consumed). A cursor is the caller stating what it has already seen, so when the tip is past it the answer is available immediately. The wait is unchanged when the cursor IS the tip, and when no cursor is given. (2) **`scan_truncated` is surfaced.** The seeking walk gives up after a bounded scan, so `cursor_missing` alone could not distinguish "purged by retention" from "never reached" — a valid deep cursor read as a dead one, and the skipped gap went unmentioned. The flag is now in the CLI's JSON and switches the text note from a confident "purged, or wrong" to a statement that the store is deeper than this read. | (1) is the FOURTH distinct cause of one symptom — the follower reporting no activity when there was activity (D138 wrong stop key, D139 file order, D140 cursor scope, now the wait predicate). It landed immediately after I wrote that such a symptom deserves a sweep rather than a point fix, and then swept only the store and read paths — never questioning the predicate that decides whether to wait at all. Knowing the method is not applying it widely enough; recorded in the design doc's §9.5 because that is the transferable part. (2) is a smaller instance of the same discipline: do not report absence that was never verified. Both negative cases are tested explicitly, since a careless version of (1) would answer instantly for every follow and quietly delete the feature |
| D142 | **The Calls gate (and the size trim) order store files by mtime, not by name — the third and last home of the name-order-is-time-order belief** | `condition.py`'s bounded probe took `reversed(names)[:MAX_FILES]` as "the newest files". Since a store name ranks records only by its date segment (CL-7), a few same-day files are enough for that window to be entirely stale: reproduced with three stale pids sorting lexically after the live one, where the gate returned False while the page's records sat in the live file — so the Calls mode silently never appeared on it. Now ordered by mtime, which for an append-only file IS its newest record (the same fact the reader's file-skip already relies on). The bounded-probe guarantee is unchanged and tested: still exactly MAX_FILES tails read, just the right ones. `sweep()`'s size trim carried the same belief as a COMMENT ("oldest first (store_files is name-sorted)") — harmless, because same-day files are the same age and today's are excluded, but it is now mtime too rather than left standing as justification. | Third round running where the finding is *the fix was not propagated widely enough*: D140 across the layer, D141 across the symptom, D142 across the codebase. D139 fixed this exact belief in `_iter_records` and I never asked where else it lived — Bugbot's report cites CL-7/D139 back at me. The cheap habit that would have caught all three: after fixing a wrong belief, grep for the BELIEF, not for the bug. Doing that here is what surfaced the trim comment, which no reviewer had flagged |
| D143 | **`--follow`'s early-answer test is a bounded read, not an id comparison — fixing a regression D141 introduced for cursors that came from a different read** | D141 skipped the wait when `cursor != baseline`. That asks "is the caller's cursor the current tip", which is a different question: a cursor from a BROADER read is never the tip of a narrower one. So the ordinary agent pattern — take a global cursor from `calls --json`, then `--follow --page X` — skipped the wait, matched nothing and printed "no calls recorded" after 1 s of a 5 s timeout (reproduced). Now the test is the actual question, answered by one bounded read (~1 ms): are any MATCHING records newer than this cursor? A cursor that cannot be found is deliberately NOT counted as "already new" — absence proves nothing about what arrived, so it falls through to the wait, which is what `--follow` was asked to do. | The lesson is about the SHAPE of negative-case testing, and it is uncomfortable because D141's own note said "a careless version of this fix answers instantly for every follow and quietly deletes the feature" and then tested exactly two cursor shapes against that: at-the-tip, and absent. The third — a cursor from elsewhere — is the one that broke. Enumerating the negative cases you thought of while writing the fix is the same blind spot as testing the consumer you just wrote; enumerate the INPUT SPACE instead ("where did this cursor come from" had three answers, and I covered two). Recorded in the design doc's §9.5 |
| D144 | **A lost `--since-cursor` makes `--follow` resume from the baseline, and the prefs snapshot honours an invalidation that lands mid-read** | (1) **An unfindable cursor is replaced by the pre-wait baseline.** D143 correctly made such a cursor fall through to the wait, but the post-wait read still used the ghost id — which `query` answers with "the newest page", and the cursor-bounded digest then summarised that history as what arrived. Note the shape: the TIMEOUT path was already empty and correct, so the failure mode was waiting *successfully*. The baseline is exactly "everything up to the moment the wait began", so resuming there makes the digest what a follower actually asked for; `cursor_missing` still reaches the caller (JSON key plus a text note) so a lost cursor is never silent. (2) **`_prefs_snapshot` guards its store with a generation counter.** The `prefs.json` read is deliberately outside the lock — holding it across file I/O would serialize every logged call behind one disk read — so an `invalidate_prefs_cache()` from the prefs endpoint can land mid-read, and storing the result unconditionally repopulated the cache with the PRE-write snapshot for the whole 1 s TTL. A capture on/off toggle then looked ignored, defeating CL-14a's invalidate-on-write guarantee. The read may still return the value it fetched (it was true when fetched); it just no longer outlives the request. | (1) is the third consequence in a row of the same primitive's edge cases, and worth noting for its shape rather than its size: the fix that introduced it was itself a fix for the prior round, and each time the *newly reachable* state was the one left unconsidered — here, "we waited, and the wait succeeded, but the anchor was never valid". When a change makes a previously-unreachable path reachable, that path is the one to test. (2) is the first genuine concurrency finding in the feature; the 1 s TTL made it look benign, but it is exactly the window a user toggling a preference is watching. Both negative cases are tested — an uncontended read still caches, and a timeout after a lost cursor still reports nothing |
| D145 | **A lost cursor is reported on the timeout path too, and the Calls view remembers a reload that arrives mid-read instead of dropping it** | (1) **`--follow`'s timeout branch reports `cursor_missing`.** It returns before both output sites, so the `cursor_lost` flag D144 plumbed through reached the caller only when records happened to arrive — the branch hardcoded `"cursor_missing": False` and printed a bare "no new calls within Ns". That withholds the flag in precisely the case it explains: an agent holding a ghost id usually has nothing arriving either, and "nothing ran" versus "I could not tell you what is new" are different answers. The proof/guess split (`scan_truncated`) is carried from the **probe**, not from the post-wait read — once the cursor is replaced by the baseline that read is not looking for the caller's id at all, so a valid-but-deep cursor was being called "purged by retention, or wrong" on every follow. (2) **The Calls view's `load()` keeps a pending flag.** `if (inflight) return;` dropped the newer request outright: the in-flight read is already committed to its filters, so changing scope/window/query/Failed mid-read rendered the PREVIOUS filters while the controls and the URL showed the new ones, and nothing reloaded until the user pressed Refresh (or Follow's poll happened to fire). Now coalesced to exactly one re-run in `finally`, so the single-flight property holds — a burst of clicks costs two reads, not N — and a transient reader failure cannot strand the view on its error card. | (1) is D144's own claim ("a lost cursor is never silent") failing on the branch its test did not assert against: the test called the timeout path and checked only `timed_out is True`. Touching a value's *producer* without checking every *exit* that consumes it is the same omission as D139→D142, one layer down — and a flag that is only reported when it does not matter is worse than absent, because its silence reads as "fine". (2) is the first UI-concurrency finding in the feature, and it needed a harness the repo did not have: the template's JS had only source-grep assertions, so the fix is tested by extracting the real `load()` from `template.html` and driving it under node with stubbed ops. Extracted rather than copied on purpose — a copy keeps passing after the shipping function regresses, which is the one thing a test of a single-flight guard must not do |
| D146 | **The Calls gate resolves the store path by the writer's rules, and the duplication is pinned by a test instead of by inspection** | `condition.py` must work as a standalone copy in the user template dir, so it duplicates the branch-aware store path rather than importing it — and the duplicate had drifted to "join the raw `FUSED_RENDER_BRANCH` value", which is wrong three separate ways (reproduced, all three): a **default-branch** ref is a baseline opt-out, so `FUSED_RENDER_BRANCH=main` writes to `~/.fused-render/calls` while the gate probed `branches/main/`; refs are **sanitised** (`Feature_X` → `feature-x`); and refs are **truncated to 12 chars**, so this very branch writes to `branches/claude-fused/` while the raw join nested `branches/claude/fused-api-call-logging-d97w88/` — an extra level deep, on the `/`. Each one puts the probe in a directory that never receives records, so the gate fails closed and the Calls mode silently never appears on a page with plenty of history. Now mirrors `_branch.sanitize` + `branch_dir` + the ref priority (env var wins even when empty, then the build-time baked ref, read through one guarded import), and the fix is anchored by an **equality test across a table of refs** (`condition._store_dir() == calls.store_dir()`), plus the end-to-end default-branch case. | The uncomfortable part is that the correct version of this code already existed one template folder over: `zarr_aoi/tile_server.py` inlines the same resolution with a docstring saying "keep the sanitize rule in lockstep with `_branch.sanitize`" — a precedent from an earlier review round that I did not go looking for when writing the second standalone copy. Two lessons, and the second is the durable one. (a) When you deliberately duplicate a rule, the *first* question is who else already duplicated it. (b) A duplicate justified by a comment is a duplicate that will drift; the fix is not a better comment but a test that asserts the two implementations agree — which is why the table test, not the three individual rules, is the real deliverable here. Note also which case the raw join happened to get right: baseline and an already-canonical ref, i.e. exactly the two a developer would try by hand |
| D147 | **The call log stores one path form — the shell's canonical, forward-slashed form — and it is enforced at the single write point** | Path filters compare with `==`, but on Windows the store held two incompatible forms of the same path. `page`/`target_file` arrive from headers and the read routes' `path` param already canonical (router.ts: "the shell's canonical form keeps forward slashes"), while `/api/run`'s relative-`py` branch builds its target with `os.path.normpath` and gets backslashes back, and the CLI ran `--page` through `os.path.abspath` and got backslashes too. Reproduced with `ntpath` standing in for Windows' `os.path`: `calls --page` matched **nothing at all** — not even the page the caller was standing on — the Calls view on a `.py` (which matches via `entrypoint`) rendered empty on a file the gate had just confirmed has history, and the gate's own substring probe missed that history too. Fixed by canonicalizing every path field in `record()` (the single writer) and both path filters in the CLI, through one named rule — `_view_url_codec.canonical_fs_path`, which `view_url_path` had been computing inline and now calls. Drive-letter paths only: on POSIX a backslash is a legal filename character, so an unconditional `.replace` would silently rewrite a page's identity. | Three things worth keeping. (a) **The fix belongs at the boundary, not at the three call sites.** The report named three broken readers; there was one broken *writer* plus one broken filter, and normalizing at the writer fixed the third reader — the gate — with no code in it. Patching each reader would have left the store holding two forms and the next reader broken by default. (b) **This is D146's class of bug one layer down, and I had just written the lesson for it.** D146 was two spellings of a directory; this is two spellings of a file path. The habit that catches both is to ask what canonical form a value has and where it is established — and after D146 I checked that the gate agreed with the writer about the *directory* without ever asking whether they agreed about the *paths inside it*. (c) **The platform I do not run on is the one the invariant breaks on.** Both forms are identical on POSIX, so every test and every manual check I did was blind to it by construction. The regression tests therefore simulate Windows with `ntpath` and drive-shaped literals rather than sitting behind a `sys.platform` guard, which would have meant the coverage only ever ran where the bug cannot happen. |
| D148 | **The prefs payload reports whether the call store exists; the read never creates it** | `Browse call logs` navigated to `calls.dir`, which the writer creates on its first append — so before any page had made a logged call the path did not exist and the explorer landed on a stat error (reproduced: `dir_exists False` on a fresh home, `True` after one `_append`). The tempting fix is to `makedirs` it, and that is the wrong place twice over: `GET /api/prefs` is a read, and a read that provisions storage is a side effect nobody asked for; and the lazy create is what keeps an empty store from appearing for a user who never records a call. So the payload gains `dir_exists`, the button waits on it, and the muted line says *why* — "no calls recorded yet" is the same answer as "why has no page got a Calls mode?". | The instructive part is the asymmetry the fix explains rather than hides: `log.dir` sits two lines above `calls.dir` in the same payload and needs no flag, because logging does `makedirs` at startup while the call store is created by its writer. Two directories in one payload with two different lifetimes, and the UI had been treating them as one kind of thing. Also worth noting what the D147 belief-sweep found here: `calls.dir` has exactly one consumer, so there was no second site to fix — the sweep is cheap and its negative result is worth as much as a hit. The prefs comment claiming `dir` exists "to reveal the store through /api/fs/reveal, exactly as log.path does" describes an affordance nothing actually calls; left alone, but it would have had the identical bug. |
| D149 | **The call-log prefs report their effective state and what forced it, from the writer's own resolvers** | `FUSED_RENDER_CALLS` and `FUSED_RENDER_CALLS_RETENTION_DAYS` beat the stored prefs inside `calls.enabled()`/`calls.retention_days()`, but the payload reported only the stored values — so the Preferences page showed **capture on, keep 90 days** while the process had **capture off, keep 1 day** (reproduced exactly that way). The engine block two lines above exists to prevent precisely this and says so in its own docstring — *"the SAME resolver the server's /api/run dispatch uses, so the page never reports a different running engine"*. Now mirrored: `effective_enabled`/`effective_retention_days` come from the resolvers the writer itself calls, `enabled_forced_by`/`retention_forced_by` carry the raw env value, the controls stay bound to the stored prefs (a PUT must round-trip and the choice applies once the var is removed) and are disabled while forced, and a muted line names the variable. | Three things. (a) **The fix is a call, not a copy.** The tempting shape is to re-derive precedence in the prefs layer; a test pins the effective pair to `calls.enabled()` across the spellings the real resolver accepts (`off`, `no`, `false`, `0` — it treats any non-false-ish value as on), which a reimplementation checking `== "0"` would fail. That is D146/D147's lesson applied *before* the drift instead of after. (b) **The sweep found a site the report didn't name**: the param and retention selects were gated on the stored `enabled`, so a forced-off process left them live and a forced-on one greyed them out while calls landed. Gated on `effective_enabled` now. (c) **Absence is a decision worth asserting.** The param mode has no env override, so it deliberately gets no `*_forced_by` pair — with a test that the keys stay absent, because an always-null pair would imply an override exists. That test passes on both sides by design, like D147's POSIX guards. |
| D150 | **`forced_by` means the variable is in force, not merely set** | D149's `*_forced_by` flags were `os.environ.get(...)` — non-null whenever the variable existed. But `calls.retention_days()` honours `FUSED_RENDER_CALLS_RETENTION_DAYS` only as an integer, so `=abc`, `=` or `=3.5` left the **writer on the stored pref** while the page disabled the retention select and captioned it *locked by FUSED_RENDER_CALLS_RETENTION_DAYS=abc* (reproduced: writer 30 days, UI locked and blaming the variable). The user could then change retention neither from the page nor by fixing a variable that was never in force. `calls.py` now owns the question as `retention_days_override()`/`enabled_override()` — returning what the variable imposes, or None when it imposes nothing — `retention_days()`/`enabled()` are expressed in terms of them, and prefs' one `_forced_by(env_name, override)` helper reports the raw value only when the override is real. | D149 fixed the `effective_*` half by asking the writer and left the `forced_by` half a presence check, which is the **same second copy of the precedence rule in the same function** — the tell being that presence and force coincide for `FUSED_RENDER_CALLS` (every set value decides, empty included) and diverge for retention, so one control looked right and the other was wrong. `0` is the case that keeps the resolver honest in both directions: a real override (age pruning off) that a truthiness check on the *resolved* value would drop, sitting one character from the empty string that must be ignored — so the tests pin `0`, `-5`, `""`, `abc` and `3.5` explicitly. Absence of an override is now stated by the writer rather than inferred by the reader. |
| D151 | **The call-log store is partitioned per app: `logs/<slug>-<hash>/`, keyed by the page's containing directory, resolved by ONE named resolver** | The flat store (D136) had three coupled defects: a quiet app's reads paid for a chatty app's volume, the size trim let one app evict another's history, and the gate's bounded newest-files probe filled with the busy app's files so a quiet page's Calls mode silently never appeared — a fourth cause of the D140-class false negative. Partition = the page's folder (an app is an `.html` + sibling `.py`s; a `.py` is never a record's `page`, so any per-page scheme strands the data-file lookup that D139/D147 fought for). `partition_name()` hashes `canonical_fs_path(normcase(realpath(dir)))` — symlinks and case folded BEFORE the bug this time, D147's lesson — behind a small LRU; the gate carries a pinned standalone duplicate like `_store_dir`. `query()` deliberately does NOT narrow to a partition — the existing filter test failed the narrowed build and was right: "this file" is a three-role match (D139/CL-16) and a template's record about your file lives under the TEMPLATE's partition, so only the gate narrows (partition-first, old global scan as fallback — its probe was always a bounded heuristic, so this strictly improves it); the size cap trims the largest partition first; the sweep reaps emptied dirs; `index.json` is advisory, written only on partition creation. Renames truncate (owner-accepted): a moved app is a new partition and the old history ages out. | The decisive rejection: a composite `<time>:<id>` cursor so partitions could stop independently. Unnecessary — the walk stays MERGED (`store_files()` spans partitions, the CL-12 day-merge is unchanged), so the identity stop, the shown-id guarantee, `cursor_missing` and the scan budget — five rounds of reviewed cursor semantics (D140–D146) — carry over verbatim instead of being re-litigated. Partitioning changes which FILES a filtered walk selects, never how the walk works. Per-partition quotas rejected (waste the cap when one app is active — the common case); a rename chain in the index rejected as bookkeeping that can drift for a rare event. No migration: a flat day file interleaves apps so files cannot be moved, and the log has never shipped — the reader ignores stray root files. |
| D152 | **The subprocess worker bootstraps the package onto its own `sys.path`, and the in-process allowlist covers both copies of every helper** | Reported symptom: the Calls view showed *"Could not read the call log: No module named 'fused_render'"* — and log_studio was fine. Cause was neither the call log nor the store: `_child.py` is spawned as a standalone SCRIPT, so `sys.path[0]` is the PACKAGE directory rather than its parent, and `import fused_render` resolves in the child only when the package is pip-installed into that interpreter. Reproduced by stripping exactly the path an editable install contributes, leaving site-packages: the child could not reach the real package. `_child.py` now appends the package's parent itself (a real install and the user's own module dir both keep precedence). Separately, `INPROCESS_HELPERS` listed only the STAGED core-templates copy, so a run served from the bundled original fell to the subprocess path silently; both copies are now listed, while a user fork under `~/.fused-render/templates/` deliberately stays user code on the subprocess path. | The diagnostic lesson: **the helper that broke was the only allowlisted one whose CORRECTNESS depended on the allowlist.** Every other in-process helper imports pandas/duckdb, so falling to the subprocess path costs them ~700 ms and nothing else — the same fall made this one fail outright. That asymmetry is exactly why the bug read as "the call log is broken" rather than "the child cannot import the package", and why log_studio working was the clue rather than a contrast: it is stdlib-only, so it never needed what was missing. Fixing only the allowlist would have restored the built-in path and left a user fork of the reader broken; fixing only the child would have left a silent per-poll spawn. Both halves, and a test for each — the child test drives the REAL child with the parent stripped, so it asserts the bootstrap rather than however the checkout happens to be installed (verified: it fails on the unfixed child). |
| D153 | **log_studio's expanded row renders a structured line as FIELDS, and the level facets list only the levels the file contains** | Two owner-reported gaps in the `.log` viewer. (a) The expanded row showed the whole line as one escaped `<pre>` string — worst of both worlds for a JSON log (too wide to read, and the field you want is buried mid-line), and the call log's own records are JSON, so the viewer the docs point at for them was the least readable way to read them. Now: `parseStructured` accepts leading text + a trailing JSON OBJECT (arrays and scalars are not field sets and are refused), renders nested objects indented under their key, keeps a multi-line or >160-char value as a block so a traceback's newlines survive, and makes each scalar a `[data-query]` chip that filters the log by that value — reusing the contract the patterns view already had, which is also why a click there does not collapse the row. A **Raw JSON** toggle returns the exact text on disk, per row, and survives Follow polls now that rows persist (D150-era reconcile). A plain-text line keeps the raw `<pre>` it always had. (b) The facet list offered all seven levels on every file, because the reader's `_counts()` seeds them at zero — correct for histogram buckets, which need a zero to stack, wrong as a filter menu: six controls that answer "no matching lines" on an INFO-only log. Filtering the LIST alone was wrong and was reported immediately: unchecking a level made the absent ones reappear, because the click handler recomputed "which levels exist" from the unfiltered `overviewLevels` while the list used the filtered set — so unchecking WARN wrote all six others into the URL, and the list kept them since "selected" beats the count filter. It also broke the all-selected reset (`next.length === available.length` could never hold against seven). One `facetLevels(data, selected)` resolver now answers for both, and `overviewLevels` has exactly one caller. | Two things worth keeping. **Nothing in the line may be dropped to make it pretty**: the text before the JSON is shown as its own `(line prefix)` row rather than discarded, because a view that silently hides part of a log line is worse than one that does not parse it. **Filtering a facet list by count has two traps.** A SELECTED level at zero is reachable (a narrowed window, a rotated file), and hiding its chip removes the only control that undoes an active constraint — a filter the user can neither see nor clear; so selection beats the count. And the list is not the only place that decides which levels exist — the control that WRITES the filter decides too, and filtering only the display left the two disagreeing, which is D147/D149's one-rule-two-copies shape in a fourth place. The tell was available at the time: the handler's `available` had to mean the same thing as the list's, and a shared resolver was the way to say so once. Tests drive the full click round-trip (click -> URL param -> re-parse -> re-render), which is what catches a mismatch between the two sides; verified failing on the display-only version. Values are HTML-escaped in both the cell and the `data-query` attribute (log content is untrusted text), and recursion is depth-bounded. Tests extract the shipping functions and run them under node — a copy would keep passing after the real code regressed — and the facet tests were verified to fail against the unfiltered list. |
| D154 | **log_studio drops its own theme button and becomes a tier-1 view: one resolver in the codebase, and no built-in view both opts in and keeps a switch** | Third attempt at the same question, and the first one that removes rather than adds. The viewer hardcoded `data-theme="light"`, so it opened WHITE inside a dark app; D153's round gave it a private resolver reading the same `fused-render:theme` key — which fixed the default and **silently broke its toggle**, because the toggle writes `?theme=` through `fused.params` and that lands on the SHELL's URL (D46), not the iframe's, so the view's `pinned()` never saw it and the resolver won every time. The button is gone, the private resolver with it, and the view now carries the ordinary AP-8 `data-fused-theme` opt-in: `static/runtime.js` resolves, applies pre-paint and follows (`storage`, `matchMedia`). Fallout that mattered: **six level accents had no light value** (`--danger/--fatal/--info/--warn/--debug/--trace`) and inherited hues picked against `#101318` — latent while light was toggle-only, the common case once light became the default for a light-mode user; **13 colour literals** outside the palettes (submit button, tooltips, notice bar) are now tokenized, which the tier-1 literal test requires; the charts read the palette through `getComputedStyle` at draw time, so a `MutationObserver` on `data-theme` re-draws them; the `data-theme="light"` literal on `<html>` is dropped (dead — the runtime overwrites it before first paint — and a difference from the other twelve); and the collapsed rail, which kept a 44px strip only so the toggle stayed reachable, now hides outright. AP-9 gains `log_studio`, AP-11 loses it and gains the general rule, AP-13 is narrowed to "no signal *by default*" (the opt-in is a property of the document, not of its author), and the storage key is back to exactly three readers. | The rule worth keeping is the one AP-11 now states: **a view either opts in or owns its appearance — never both.** An in-view switch layered on top of a pushed theme is not a switch, it is a value that gets overwritten by the next `storage` or OS event, and the failure is silent because the button still animates. That is what made this three rounds instead of one: round two treated "opens white in a dark app" as a missing default and bolted a resolver alongside a control it had not checked still worked, which is the same one-rule-two-copies shape as D147/D149/D153 — here the two copies were *the shell's resolver and the view's*, and the view's lost. Removing the control was cheaper and better than repairing pinning: the app already has a three-way Appearance setting, and a per-view override of it is a feature nobody asked for. Second lesson, on latency: **making a code path the default converts its latent defects into live ones.** The six missing light tokens had been wrong since D134 and were invisible because reaching light mode required clicking a button most users never clicked; the tier-1 palette test catches exactly this and is why the promotion surfaced them in one run rather than in a bug report. `excel` and `tableau` keep their buttons and their private keys — they never opted in, so nothing overwrites them, and AP-11 is now about that consistency rather than about a list. |
| D155 | **Self-review round: the log viewer's Raw/Fields toggle is fixed at the rule rather than the instance, and the worker's PYTHONPATH stops outranking the caller's** | Five fixes, two owner-reported and three found by reading the diff. (1) **The Raw JSON / Fields toggle did nothing**, on exactly the records worth reading. Two defects stacked: the raw text was addressed by POSITION (`querySelector("pre")`) while `fieldRows` renders any multi-line or >160-char value as its own `<pre class="field-block">` INSIDE the table — so a record with a traceback has two and the nested one is first, and clicking Raw un-hid a node inside the table it had just hidden. It now has a name, `.detail-raw`. (2) Then `.hidden` **did not hide**: the UA's `[hidden] { display: none }` loses to any class rule setting `display`, and this template sets one on nearly every box, so with (1) fixed the raw text appeared UNDERNEATH the fields. Measured in Chromium — `el.hidden = true` on `#pager` left `display: flex`, height 47. `.field-table` (grid), `#pager` (flex) and `.context-lines` (grid, a 2px sliver) were all affected; `.hist-skel[hidden]` already carried a one-off `display: none` for exactly this. Replaced by one global `[hidden] { display: none !important }` and the one-off removed. (3) `enrich_write` mapped **any** 403 to `readonly`, including the X-Fused guard's refusal — a `unauthorized` flag now separates them, derived by re-asking `_require_fused` rather than re-spelling the rule. (4) `calls_dir` reached the runtime as raw `abspath`, i.e. backslashed on Windows, where every path the runtime holds is forward-slashed — canonicalized through the codec this change itself introduced. (5) `_child_env` **prepended** the package's parent to PYTHONPATH; it now appends, matching `_child.py`'s own `sys.path.append`. | The through-line is the one D144/D147/D149/D151/D153 keep finding: **a bug report names an instance, and the fix belongs at the rule.** (2) is the clearest case in the set — the trap had already been hit once and patched at `.hist-skel`, which is precisely why it was still live everywhere else; one global rule also covers the element nobody has written yet. (5) is a reversal worth stating, because the original reasoning was *locally* true and still wrong: prepending was justified with "the user's own module directory still wins, since `run()` puts it at `sys.path[0]`" — true, but the wrong axis. What it displaced was the caller's PYTHONPATH, and on an installed layout the prepended value IS site-packages, so it reversed the one override PYTHONPATH exists to provide, for every module rather than for this package. Appending satisfies the actual requirement (the entry has to be REACHABLE, not first) and cannot displace anything. Verifying it also produced a useful near-miss: the first repro, `import tests` against the repo root, did NOT shadow — `tests/` has no `__init__.py`, and a namespace package has lowest precedence by design, so a regular module later on the path wins regardless of order. The effect is real only for REGULAR packages, which is what site-packages holds; the demonstration was redone with two of those. A repro that fails to reproduce is not the same as a bug that is not there, and the difference was the mechanism, not the severity. Every behavioural fix here was pinned by a test verified failing against the unfixed code first, and the two UI fixes were confirmed in a real browser rather than at source level — the standing lesson from §9.5's green-spike failures. |
| D156 | **The supersession mark rides the superseding request (`X-Fused-Supersedes`) instead of a deferred POST, and the worker's bootstrap diagnostic fires only for the package itself** | Two Bugbot findings, both upheld. (1) **The supersession report lost a race with fast calls.** `reportSuperseded` deferred `POST /api/calls/event` by `setTimeout(0)` to batch, while `finish()` stamps `superseded` only if the mark is ALREADY present — the store is append-only, so a late mark cannot reclassify a written line. Measured in Chromium against a local server, that POST landed **~19 ms after the abort**; every abandoned call whose handler finished inside that window was written `ok` and averaged into the latency percentiles, which is exactly what CL-5 exists to prevent, and in-process helpers (D72) finish that fast routinely — so a template re-querying per keystroke hit it repeatedly. The mark now travels as `X-Fused-Supersedes` on the request that CAUSED the supersession: one only ever happens because the page is issuing a new call on the same channel, and that request leaves in the same synchronous task as the abort, so `begin()` takes the ids at the earliest moment the server sees anything. The header TAKES the queued ids rather than copying them (a duplicate arriving after `finish()` consumed the mark would sit in the map until its TTL); the POST survives as the `pagehide` backstop, where no request is left to carry them. Verified end to end in a browser: the superseding `/api/run` carries the abandoned id, **no `/api/calls/event` request is made at all**, and that id is recorded `superseded`/DEBUG with the rollup showing `count=8 superseded=4`. (2) **The worker's ImportError diagnostic was too broad** — it matched on the first dotted segment, so a missing SUBMODULE under an importable package got the bootstrap blame (executable, PYTHONPATH, sys.path). Now an exact `e.name == "fused_render"`. | (1) is the round's real lesson and it is about **evidence, not design**: the deferred POST was defended in a comment that reasoned "the superseded run keeps executing for as long as it would have, so the report reliably lands before the record is written". That is true of the case the author pictured — a slow run superseded by a slider tick — and false of the case that matters here, because the calls that lose the race are precisely the FAST ones. The comment was plausible, was never measured, and stood through eleven review rounds; the fix took one `PerformanceObserver` reading. Anywhere a correctness argument rests on "X reliably happens before Y" across a network boundary, the number is cheap and the intuition is not. Worth noting what the fix did NOT do: it does not make the ordering a guarantee — request #2 could still, in principle, reach the server after the abandoned call's record is written — it removes a macrotask and a whole round trip, which is the most that can be done without buffering records server-side, and buffering would trade a rare mislabel for a delay on every record. (2) is a smaller instance of the same discipline as D152: `.split(".")[0]` looked equivalent to an exact match because the author only had the package-missing case in mind, and Python's own semantics settle it — a missing package reports `name` as the top-level name even for a submodule import, while a missing submodule reports the full dotted path, so the exact match is not merely narrower but precisely right. |
| D157 | **`claude`, `history`, `annotate` and `zip` become tier-1 views; `claude` leaves the light-by-design list it never belonged on, and `zip`'s breadcrumb/Extract bar moves to the bottom of the pane** | Four AP-9 conversions of the ordinary shape: `data-fused-theme="shell"` on `<html>`, a `:root` dark palette that IS each view's previous appearance value-for-value, and a `:root[data-theme="light"]` twin over the same token set. `zip` was already fully tokenized and needed only the twin. `history` had a partial palette plus 19 stray literals (the dot grid, the continue banner's gradient and both its borders, the warn/error cards, the timeline node ring, the accent glow, the skeleton shine); `--spine-x`, a geometry constant, moved out of `:root` onto `body` rather than being duplicated into the light block to satisfy the equal-token-sets rule. `claude` had 6 tokens and 18 literals, and its brand orange is the one value that could not carry over — `#d97757` is ~2.6:1 on white and serves as body text (the working verb, bullets, the beta badge) as well as a fill, so light gets the same hue at `#b8542f`. `annotate` had **no palette at all** — one `color-scheme: dark` and 57 literals — and is the only one of the four with colour outside CSS: the Custom Highlight API paints inside the FRAMED document, so its two tints are resolved off our own `<html>` with `getComputedStyle` and rewritten from a single `MutationObserver` on `data-theme` (one observer for the page's life — the frame reloads on every view switch, and re-observing per load would stack them). Separately, `zip`'s header became a footer: the listing is what the eye lands on, and the Extract button now holds a fixed position instead of being pushed around by a growing breadcrumb. `#notice` follows it down. | The interesting one is `claude`. It sat in AP-10 **light by design** while rendering `#131417` — a dark view labelled light — and nothing caught it, because AP-10's whole contract is "carries none of the tier-1 structure", which a dark view satisfies just as well as a light one. The exhaustiveness test (AP-14) proves every template is *classified*, not that its classification is *true*; the four buckets are only distinguishable by what a view does NOT have. Worth remembering the next time a list is made exhaustive and that is mistaken for making it correct. `annotate` is the other lesson: a view with zero tokens looks like the cheapest possible retheme and is the most expensive, because every literal has to be given a NAME and a role before it can be given a light value — and the ones that turn out not to be CSS at all (the Highlight API strings) are exactly the ones no palette test can see. The deferred list keeps the views where the colour lives in a canvas, a map style or a third-party viewer; these four left it because they are plain DOM chrome. `annotate`'s stage is still whatever the framed document chose to be, which is correct — that document opts in on its own terms. |
| D158 | **The `csv` template is deleted; `.csv`/`.tsv` fall through to `duckdb` as the default and `excel`/`code` as the alternates** | `csv/` was a 221-line pandas-backed paged table — the same job `duckdb` already did as the FIRST mode on both keys, so nothing reached it unless a user picked it out of the switcher. Removed: the folder, its entry in `executor.INPROCESS_HELPERS` (the in-process allowlist is now duckdb/structure/xlsx/sqlite readers + the api inspector), its `NICE` label in `learn/list_templates.py`, and its place in the AP-9 tier-1 list. `registry.json` drops the mode from `.csv` (`duckdb, excel, code, reader, annotate`) and `.tsv` (`duckdb, code, reader, annotate`). Tests that used `"csv"` as a stand-in for *a real builtin template* switch to `excel`, which is still bound to the same key; `test_core_templates.py`'s tier-1 delivery check parametrizes on `text` instead. A user registry that still names `csv` is not an error — it resolves to no folder and surfaces as a dangling `exists: false` ref, the same as any other unknown name (D73). | The one thing lost is the **pandas** path: `duckdb` needs duckdb installed, and on an install without it `.csv` now falls to `excel` (pandas/openpyxl) and then `code`. That is a real narrowing of the fallback chain, accepted because `duckdb` ships in the bundled extra and `excel` covers `.csv` natively. Worth noting for the general case: a template being *reachable* is not the same as it being *used* — a mode that is never first in any registry list is only ever seen by someone who went looking for it in the switcher, which is the signal that it has stopped earning its maintenance. Historical decisions D59 and D72 still name `csv` and are left alone: they record what was true when they were made. |
| D159 | **`fused.ai` runs the `claude` (Claude Code) CLI directly instead of relaying to a local OpenAI-compatible proxy** | `/api/ai` invokes the `claude` binary as a one-shot completion subprocess (`-p --output-format json --tools= --setting-sources= --no-session-persistence --max-turns 1` — the empty flags in single-token equals form, since cmd.exe's %* expansion behind a Windows .cmd shim drops separate empty argv elements — the prompt over **stdin** — argv has an OS size cap that a data-heavy prompt can blow — the system prompt via `--system-prompt-file` (a temp file, deleted after the call: no user-controlled string may enter argv, since cmd.exe re-parses it behind a Windows .cmd shim and cannot be escaped reliably; model ids are charset-validated `[A-Za-z0-9._-]+` for the same reason), replacing the shipped agent prompt, `CLAUDE_CODE_MAX_OUTPUT_TOKENS` carrying the effort→tokens mapping). Binary from PATH, `FUSED_RENDER_CLAUDE_BIN` overrides; the `ai_base_url` pref and `FUSED_RENDER_AI_BASE_URL` are removed. The wire contract is unchanged (`{ok, result:{text, model, usage}}`, same validation and 400s); error types gain `"timeout"`, and `"ai_unavailable"` now means "binary not found/runnable". The model echo prefers the full id from the CLI's `modelUsage` key, so an alias request (`sonnet`) reports the model that actually ran. | The proxy asked the user to install, run and keep alive a separate service before the first `fused.ai` call worked — for an audience that already has Claude Code installed and logged in, which makes the CLI both the credential and the runtime with zero setup. Local-only stance (RH-11) is unchanged: the CLI lives on the author's machine, so the exporter still rejects hosted pages. Rejected: keeping both backends behind the pref (two code paths to test for an MVP with one real user population); calling the Anthropic API directly (requires key management the shell deliberately avoids). Subprocess-per-call over a persistent session matches the executor's own fresh-subprocess discipline (D70). |

### Markdown v2 — read/write notes, wikilinks and the link graph (2026-07-28)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D153 | **Markdown v2: one template that reads, writes, resolves `[[wikilinks]]`, shows backlinks and graphs them — with read/write behaviour copied from Obsidian rather than invented** | Full contract in SPEC §32 (MD-1..MD-22); design and rejected alternatives in `docs/markdown-graph.html`. Shape: `templates/markdown/graph.py` is the one data module — parse, resolve, index, assemble — and all three surfaces read it (the note view, the local graph panel inside it, and the new folder-level `graph` directory mode, which calls it as `../markdown/graph.py` because `/api/run` resolves a relative `py` against the page's directory). `.md` keeps its mode list; the universal `/` key gains `graph` as a CT-12 conditional peer. The editor is the CM6 bundle we already ship, rebuilt with `@codemirror/lang-markdown` on a GFM base. The rendered surface tokenizes wikilinks and tags through `marked`'s own inline-extension hooks rather than pre-processing the source. | Obsidian parity is a *reachable* target rather than an aspiration precisely because Obsidian's editor IS CodeMirror 6 and its Live Preview is a decoration set over the source document — the mechanism we already vendor for annotate mode. **One Python module over one-per-surface**: the failure mode of three surfaces is that they disagree about what a link is, and a shared module makes that impossible rather than unlikely. **marked extensions over a source pre-pass**: a pre-pass would need a second copy of `graph.py`'s code-masking rule in JS, and the two copies would drift on the first edge case (a wikilink inside a fence). marked already knows what is inside a fence, so the duplication is simply not created. Rejected: teaching the existing `code` template about markdown (it is the plain-text escape hatch and should stay one), and a separate `notes` template (the `.md` default IS the note view). |
| D154 | **The index stores RAW link targets and resolves at assembly time; an ambiguous target is a ghost, never a guess** | `parse_note` records the target exactly as authored; `resolve_link` turns it into a path against the candidate set that exists *now*, tried shortest-first (sibling folder → vault root → path suffix → bare basename, case-insensitively, extension optional). Two notes sharing a basename with no path to choose between them resolve to **nothing** — a dim ghost node and a dashed link. Backlinks are computed the same way, by resolving every other note's links, never from a stored reverse index. The `[[` popup's inserted form is chosen by running each candidate form through `resolve_link` itself and taking the shortest that comes back to that note, with a property test over a vault of colliding basenames. | Caching a resolved edge is the one mistake that makes incremental updates *wrong* rather than merely slow: renaming `Foo.md` silently changes what every other note's `[[Foo]]` points at, and a stored reverse index would keep asserting the old answer. So the invalidation story stays honest — a row is invalid only when its own file changed. **Ambiguity as a ghost** over Obsidian's nearest-file tie-break: picking one of two `Design.md`s makes the graph assert an edge the author never wrote, and the dashed link is a prompt to disambiguate rather than a silent wrong answer. **The popup's link form is derived from the resolver, not from a parallel "shortest path" rule** — that is the D146/D147 lesson applied before the drift instead of after: two implementations of one rule need a test that asserts they agree, and the cheapest such test is to make one of them call the other. |
| D155 | **Obsidian's save model is adopted wholesale — no save button, no dirty indicator, no prompt — with exactly one deviation (conflicts) and no stat ticker to detect external changes** | 2 s idle autosave, plus a flush on blur, tab switch, `pagehide` and before any navigation this view initiates (via `__fusedFlushEdits`, so a mode switch cannot drop edits); `⌘S` only forces the flush early. Saves are single-flight and re-compare the buffer after the write, so edits landing mid-write stay dirty. `fused.autoReload(false)`. **The deviation:** a clean buffer silently reloads when the file moves under it, exactly as Obsidian; a dirty buffer whose `mtime` moved gets a reload-or-keep banner, autosave suspends while it is up, and "keep my version" is the one write without `expectedMtime`. External changes are noticed on focus / visibility change and at the next save through the optimistic lock — **not** by polling `stat`. Read-only comes off `stat.writable` (which consults `mounts.mount_read_only`), so the editor opens locked. | The absence of save ceremony IS the behaviour being copied, so the earlier dirty-dot/save-button sketch was dropped deliberately rather than forgotten — a dirty indicator would be a visible admission that saving is something the user has to think about. **The conflict deviation is not a preference**: Obsidian assumes it is the only writer in the vault, and fused-render sits on shared and mount-backed paths where that is false, so last-write-wins would eat somebody's edit. **No stat ticker** is the interesting call: a poll is exactly the traffic that killed a mount in the `fs/events` stat-storm incident, and branching on mount-ness in the template would break the standing rule that templates stay mount-agnostic. Focus/visibility plus the `expectedMtime` lock covers every case a poll would, a moment later — accepted. **`stat.writable` over `os.access`**: on an rclone mount with `CacheMode=full` a doomed write succeeds locally and 403s later on async upload, so `os.access` is a lie there and the honest answer already exists in the shell's persisted `read_only` flag. |
| D156 | **The graph does not support remote mounts at all — refused in two independent places, never bounded** | The folder mode's `condition.py` returns `False` for any mount-backed path (so the mode is never offered), and `graph.py` refuses a mount-backed root **before it walks anything or even creates the index directory**, returning a clear `mount_unsupported` result. Both call the app's own `shell.mounts.is_mount_backed` rather than carrying a copy of the rule, and both treat a failed import of it as *refuse* rather than *guess*. Single-file `.md` read and write on a mount stay fully supported. The gate additionally never enumerates a directory (CT-12): mount check, one `isdir`, then two `os.path.isfile` probes for the vault marker `index.md` (`Index.md` too, for a case-sensitive filesystem) — with the tests making `listdir`/`scandir`/`walk`/`glob` **fatal** inside a gate call. The marker set was originally a wider guess at an "entry-point note" (`README.md`, `readme.md`, `Home.md`, `notes.md`, `.markdown` variants); it is now `index.md` only, because `README.md` is in essentially every code repository and the mode was therefore offered on every checkout on the disk, where a link graph says nothing. Narrowing it costs discoverability, not capability: `_mode=graph` still reaches the folder mode by hand, and the note view's own graph panel is untouched. | Owner instruction, and the right shape anyway: a recursive walk over a remote mount is precisely the pattern that wedges it (kernel NFS listing on a flat million-key prefix — the MUR SST incident), so bounding the risk is strictly worse than not taking it. **Two halves rather than one**: the gate is the UX (nothing offered, nothing to click) and the module is the guarantee (a hand-written `_mode=graph` URL or a `root` param cannot reach the walk). Either alone would be a rule that holds only until someone types a URL. **A refusal, never a partial walk** — a truncated graph over a mount would look like a working feature while silently omitting most of the vault, and the user has no way to tell those apart. **Enumeration made fatal in tests** rather than documented: `zarr_aoi/condition.py` explains the rule in prose and this gate is the second place it binds, so the cheap way to keep it true is a test that fails when it is broken, not a third copy of the paragraph. **The marker, not a likely-note list**: what the gate is actually asking is "is this a notes vault?", and the wide list answered a different, easier question ("does this folder contain any conventional markdown entry point?") whose answer is yes for most of a developer's disk. `index.md` is the convention a vault's own entry note follows and a repository's does not, so it separates the two cases with the same two stats — and the tests now assert BOTH directions, since a gate that says yes to everything passes any test that only checks the yes. |
| D157 | **The graph canvas was extracted to `templates/shared/` the moment the second surface needed it; Live Preview decorations are deliberately NOT built, and `view=live` resolves to Source rather than lying** | `templates/shared/graph-canvas.js` (served from the existing `/template-shared/` mount, like `ro-badge.js`) holds the sim and every interaction rule; the markdown template's local panel and the folder-level `graph` mode both drive it. A hand-rolled O(n²) spring layout on Canvas 2D — no force library vendored. Colours are read from CSS custom properties at draw time, because `var()` cannot resolve inside a `fillStyle`. **Not built, listed in MD-18a rather than left implied:** Live Preview decorations (the vendored primitives are in place and the mode is accepted as an alias of Source), a frontmatter properties table, inline note embeds, image paste/drop, `.fused-graph.json` tuning, gitignore-awareness of the walk, and a watch-registry subscription. | Extracting at the second use (not the first, not the third) is what kept this from becoming the fourth inline copy of a thing — the shell-navigation helper in this same template is the counter-example, a rule already duplicated inline in `history`, `canvas` and `preview`, which this change does **not** fix and which is called out as a follow-up rather than quietly extended. **`view=live` resolving to Source** over dropping the value: a URL saved against a later version then degrades instead of breaking, and only the two modes that exist are offered as buttons — a third button that behaved like Source would be a mode that lies. **Naming the omissions in the SPEC** is the point of this entry: the design doc describes Live Preview as the largest single piece and explicitly sequenced it last so the valuable parts land without it, and an unbuilt item that is written down as unbuilt is a decision, whereas one that is merely absent reads as an oversight the next reader has to rediscover. *(The Live Preview clause of this entry is superseded by D158 — it is now built, and the mode alias it describes no longer exists.)* |
| D158 | **The markdown template is one Live Preview surface: no reading/source modes, no toolbar, `code` left untouched — and the decoration layer is covered by executing it, not by asserting about its source** | `marked` and the whole rendered-HTML pipeline are gone (`renderReading`, `rewriteRelativeImages`, `rewriteRelativeLinks`, `enableTaskBoxes`, the mode buttons, the `view` param, `⌘E`). What replaces them is a single `Decoration` set over the CM6 document, revealing raw source per **line** the selection touches. The toolbar is gone with them: the shell's breadcrumb names the file, Obsidian shows no save state, so the read-only badge and save *failures* float, and the only persistent chrome is a 26px toggle opening a right sidebar that holds backlinks **and** the graph (Obsidian's layout). `code` stays exactly as it was and stays registered for `.md`. New: `scripts/vendor-codemirror/live-preview-probe.mjs` + `tests/test_markdown_live_preview.py` run the real builder against the real grammar headlessly. | **One surface over Obsidian's two** because the split does not transfer: in Obsidian, Reading and Editing are leaves of one `MarkdownView`, so the editor instance — and with it the caret and the undo history — survives `⌘E`. Here a mode is a *template swap*, so the same toggle would cost more and buy less; and a read-only file already gives us the reading view for free, as decorations over a locked buffer, which unifies that path instead of branching it. **Two editors for `.md` with two save models is accepted, not reconciled** (MD-16 here, AS-1 in `code`): the owner asked for `code` untouched, `annotate` already writes `.md` on its own terms, and a user who picks the generic text editor asked for generic behaviour. Stating the divergence in SPEC beats a refactor nobody wanted. **The probe is the load-bearing part of this entry.** Everything else here is visible in a diff; buildDecorations is not, because its correctness depends entirely on what the vendored grammar calls each range — and the grammar was wrong twice in ways no source assertion could catch: `---\ntitle: x\n---` parses as `HorizontalRule` + `SetextHeading2` (so YAML rendered as a rule plus a heading), and the inner brackets of `[[Wiki]]` parse as a `Link` node, which an over-broad code-guard silently treated as un-linkable — no wikilinks rendered at all. Both were found by *running* it, within minutes, after the source looked correct. CM also throws on overlapping replacements, so building the set is itself the collision check; the tests skip where the gitignored vendor `node_modules` is absent rather than pretending to pass. |
| D159 | **Markdown follow-ups: decorations move to a StateField, the scan root climbs to a vault marker, "unknown" stops masquerading as "missing", read-only becomes a mode rather than a second view, and the palette is Obsidian's neutrals with fused's accent** | Seven changes to `templates/markdown` and the shared canvas. (1) The Live Preview decoration set is a **`StateField`**, not a `ViewPlugin`, and `buildDecorations` takes a *state* and walks the whole document. (2) The note view's default scan root **climbs** from the note's folder to the nearest ancestor holding `.obsidian/`, `.fused-graph.json` or `.git`, bounded to 8 levels, never entering a mount, `isdir`/`isfile` probes only; `main` also coerces a string `depth`. (3) `resolved === null` now means **unknown**, rendering wikilinks inert (no ghost, no `data-create`), with the sidebar saying why in graph.py's words. (4) A **read-only/editing mode** in the `edit` param, implemented as the same two CM facets the unwritable-file path already used. (5) The side panel is **resizable** (localStorage, not params) and the graph layout is roughly twice as open and **fitted to the canvas**. (6) A ghost is only offered where a target could *be* a note, and creating one cannot escape the scan root. (7) Both palettes are Obsidian's `--color-base-*` neutrals, with the editing surface forced to the page's own background. | **The StateField is a bug fix, not tidying.** CM throws "Decorations that replace line breaks may not be specified via plugins", and the table widget replaces a multi-line `Table` node — so scrolling a note that contained a table threw mid-update and abandoned the render, which is what the user saw as parts of the page going white. A state field is *allowed* to span line breaks, so the constraint disappears instead of being worked around; the cost is a whole-document walk (fine at MD-10's 256 KB cap) and one subtlety worth knowing: the language extension parses in the background, so the field must also rebuild when the syntax **tree** advances or everything past the parse frontier stays undecorated. The probe could not have caught this — it calls the builder directly and never builds an `EditorView` — so the coverage is a *pair*: a source test that the decorations come from a StateField, and a probe test that a replacement really does cross a newline, neither meaningful alone. **The root climb** is the owner's actual complaint: `dirname(file)` meant every link leaving the note's folder was a ghost and every inbound link from outside it invisible, so a real note sat in a halo of grey `../…` ghosts. Marker-based rather than "walk up until it looks like a vault", because a *probe* is constant-time and a *listing* is not — the CT-12 discipline, here doubly binding since the ascent decides the scope of a walk. It refuses to cross into a mount so that a local note under a mounted folder is scanned where it is rather than answered `mount_unsupported`. **Unknown vs missing** is the same class of error twice: a ghost asserts "this note does not exist" and an empty backlinks panel asserts "nothing links here", and on the no-scan path *nobody looked*. Both now say so. The page still resolves nothing itself (MD-6) — its only two answers are graph.py's map and "we do not know". **Read-only as facets, not a view:** the reading/writing split was deliberately removed in D158 and does not come back; a mode that changes only writability costs two facets and a rebuild whose caret and scroll `buildEditor` already preserves. Editing stays the default. The reveal guard in `selectedLines` turned out to be load-bearing rather than defensive: verified in a real browser, a text selection inside a non-editable view *does* reach the state's selection (`anchor:115, head:182` after swiping one line), which without the guard would un-render whatever was swiped over. **Graph spacing was measured, not eyeballed** — the sim run headlessly over a panel-sized neighbourhood and folder webs of 60/200/600, counting label collisions at the resulting zoom. That killed the first attempt (scaling spacing by node count) as *worse*: once the layout is fitted to the canvas, scaling it uniformly changes nothing on screen, so the knob that should vary per surface is the zoom. It also exposed a pre-existing failure — all nodes seeded on one 70px ring, with velocity accumulating across steps, threw a 600-note graph 1263x2016px in a 1200x800 canvas *before* any change here — fixed with disc seeding and a speed ceiling. **The palette borrows Obsidian's neutrals but not its accent.** `oneDark` was the diagnosis behind "the dark theme is ugly": it ships its own `#282c34` background, a blue cursor, red headings and green markdown markers, so the editor was a second, lighter, bluer slab inside the page with an acid-yellow accent fighting it — and the owner's specific gripe, the red title, was `oneDark`'s `tags.heading`. It is kept only for fenced-code colours; every surface colour is overridden to a token, and prose highlight spans are reset to `inherit` outside fences, so the Live Preview's own classes are the only thing colouring the document. Obsidian's own violet accent was tried and rejected by the owner as off against the greys — which is the point of a neutral scale: it carries any accent, so fused's yellow stays. Light `--warn` is darkened (2.8:1 on white is not readable for the one message in this view that must be read), and light `--accent` likewise. The costs accepted: fence markers keep `oneDark`'s green (they are inside a code block); a `HighlightStyle` of our own would need the vendored bundle rebuilt, whose blast radius covers `code` and `annotate`, so it was not done. |

### Claude chat template on Windows (2026-07-28)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D160 | **The claude template finds `claude.exe` in Claude Code's install locations rather than requiring it on PATH — and its detach, liveness and cancel paths each take the win32 route instead of the POSIX one** | `_claude_bin` resolution order is now `FUSED_RENDER_CLAUDE_BIN` → `shutil.which` → per-platform install locations, with the Windows list ordered `%USERPROFILE%\.local\bin\claude.exe` (PowerShell installer), winget's `Links` shim dir, npm's `%APPDATA%\npm` prefix, legacy `%USERPROFILE%\.claude\local` — `.exe` ahead of any `.cmd`. Three POSIX-only mechanisms are replaced on Windows: `start_new_session=True` (accepted and ignored there) becomes `DETACHED_PROCESS \| CREATE_NEW_PROCESS_GROUP`; `os.kill(pid, 0)` becomes `../shared/procutil.pid_alive`; `os.killpg(pid, SIGTERM)` becomes `taskkill /PID <pid> /T /F`. Two adjacent fixes rode along: `PROJECTS` follows `CLAUDE_CONFIG_DIR` (the supervisor sets it in every packaged build), and `_bad_id` rejects `\` and a drive prefix in `run_id`/`session_id`, not just `/`. `tests/test_claude_agent_windows.py` simulates win32 from any host (patching `os.name` plus the two win32-only creationflags constants) and is also wired into the real `windows-desktop` CI job. | The PATH assumption was the *reported* bug and the least interesting one: the Windows installer appends `%USERPROFILE%\.local\bin` to the user PATH in the registry, so a process that inherited its PATH at login — which is every GUI-launched app, and the supervisor's children — cannot see a `claude` that works perfectly in a fresh terminal. **The `os.kill(pid, 0)` bug is the one worth remembering.** It is the canonical POSIX liveness probe and it silently means something else on Windows: signal 0 *is* `CTRL_C_EVENT`, so Python routes it to `GenerateConsoleCtrlEvent` — a Ctrl+C send that either lands or errors, and the error made `_alive` condemn a healthy run. The page polls 400 ms after `start`, so the first poll ended every turn as *"claude exited unexpectedly"* — our own string, with an empty `err.log`, which reads as a Claude Code crash and is not one. This repo already knew: `templates/shared/procutil.py` exists precisely for this and documents the aliasing in a comment; the claude template simply never used it. The flashing terminal window came from the same class of mistake one level down — `executor.py` already passes `CREATE_NO_WINDOW` so a windowless server doesn't flash a console per run, but the worker then spawned a console-subsystem `claude.exe` with no flags of its own, and a console-less parent forces Windows to allocate a new console. So the lesson is not "port the POSIX call" but **a POSIX-only kwarg that the platform accepts and ignores leaves no trace at the call site** — `start_new_session` raised nothing, warned nothing, and simply did not detach. Rejected: reading the user PATH back out of `HKCU\Environment` to pick up a post-login install (the install locations answer the same question without a registry dependency, and a non-standard install dir is what the env override is for); refusing a `.cmd` shim outright (it works, it is just second choice); and testing only on the real Windows runner (that job is path-gated and would leave the Linux matrix blind to a win32 regression, so the branches are simulated there too). |

### Claude template: tool approvals reach the browser (2026-07-28)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D161 | **The chat window becomes the permission prompt, via `--permission-prompt-tool` and a shipped one-tool MCP server; `--permission-mode acceptEdits` is dropped** | New `templates/claude/permission_server.py` — a stdlib-only stdio MCP server whose single `approve` tool writes each request to `perm/<id>.req.json` under the run dir and blocks until `agent.py` drops `<id>.res.json` next to it. `_start` generates the run's `mcp.json` (`command: sys.executable`, perm dir in argv, per-server `timeout` above the server's own wait) and spawns claude with `--mcp-config` + `--permission-prompt-tool mcp__fused_approvals__approve`. `poll` returns the run's whole request list (not just the unanswered ones, so a re-attaching frame rebuilds cards it never saw) and reports `phase: "awaiting"` while any is parked; a new `decide` action writes the verdict; `cancel` releases every parked request before killing the group. `template.html` renders each request as a card — tool name, a per-tool summary (Bash command, Edit `-`/`+`, Write content), and Allow / "Allow all X in this reply" / Deny — all through `textContent`. Three properties are load-bearing: the request id is **minted by us**, never the CLI's `tool_use_id` (it is joined into a path); the decision file is a **one-way latch** (`O_EXCL`, first writer wins) so a double-click or a cancel racing an allow cannot overwrite a verdict the tool already acted on; and anything that is not the exact string `allow` **fails closed**. A reader that *loses* the create waits the write out (`DECISION_WRITE_WINDOW`) instead of substituting its own verdict — `O_EXCL` publishes the file's existence before its content, and reading that gap as "nobody answered" is a real misreport (Bugbot, PR #308): the loser of a double-click showed *its* verdict on the card, and the server's timeout claim could hand claude a deny for a tool the user had just allowed. `poll` is the one reader that never waits (it re-reads every 400 ms), the UI re-renders a card whose polled verdict differs from the optimistic one, and a writer that dies after the create unlinks the file it claimed — an empty one holds the latch forever while never parsing. `--disallowed-tools AskUserQuestion,ExitPlanMode` because naming a prompt tool also un-gates those two in headless mode. Wire contract, flags and the spawn line are pinned by `tests/test_claude_permission_bridge.py`, which drives the server over its own stdio JSON-RPC and never invokes `claude`. | The reported symptom was "approval prompts not coming through", and the honest description of the old behaviour is worse than that: they were not *late*, they were **never asked**. Headless claude denied anything the session's rules did not already allow with `tool requires user interaction; no prompt available in headless mode` — a refusal with no prompt, no error and no trace in the UI, so Claude simply appeared to give up. `acceptEdits` was never the fix for that; it only widened the silent-yes to file edits (anywhere on disk) while leaving every Bash/WebFetch on the silent-no path. Dropping it is therefore not a separate hardening — the ask *becomes* answerable in the same change that makes it visible. **MCP over the SDK's `canUseTool`**: `canUseTool` rides the `--input-format stream-json` control protocol, which needs a live stdin pipe for the process's whole life, and this template's executor is fresh-process-per-call (PY-6) with a detached run — there is no writer to keep that pipe open. A file-mediated MCP server fits the architecture that already exists (detach, redirect, poll) with no server changes, which is the same reasoning that chose poll-a-file over a push channel. **"This reply", not "this session":** an allow-all returns `updatedPermissions … destination: "session"`, which was *verified* to die with the process — each turn is a fresh `claude -p --resume`, so turn 2 asks again. The button is labelled for what it does rather than for what the CLI calls it, and cross-turn persistence is left unbuilt on purpose: it needs a grant store of ours replayed as `--allowedTools`, i.e. a durable, invisible, un-revokable grant, which is a larger call than this bug. **The rule is the bare tool name.** The `--permission-prompt-tool` wire hands over `{tool_name, input, tool_use_id}` and no permission *suggestions* — the narrowed `Bash(npm test:*)` patterns the interactive prompt offers are computed on the CLI side and never reach us. Deriving our own would be a hand-rolled matcher deciding what counts as "the same command", in the one place in this repo where getting it wrong grants something nobody approved; handing the CLI a bare tool name keeps all matching on its side. **Which is exactly why the second button is not offered for every tool** (owner feedback on the first build, looking at "Allow all Bash in this reply" sitting under a single read-only `gh pr diff`): a bare-tool-name rule is the *only* rule we can write, and for Bash that one rule is "stop asking about commands", which is not a proportionate second option next to one command. `WHOLE_TOOL_GRANTABLE` limits it to the repeat-heavy file tools (`Edit`, `Write`, `Read`, `Glob`, `Grep`, `NotebookEdit`) where the grant buys one click instead of eight against the same target; Bash, the web tools and every unrecognised or MCP tool fall through to Allow/Deny, and the default for an unknown tool is no grant. The general lesson is that an affordance inherits the granularity of the mechanism behind it — the coarse rule was defensible, offering it as a *button on every card* was not. **The allowlist is enforced in `agent.py` too, and that reverses a call made in the first build** (owner, round two): the UI was left as the only place the restriction lived, on the reasoning that the browser *is* the user and a second copy would drift. The first half is true and the second half is answered by this repo's own D146 — a duplicated rule needs a **test that asserts the two agree**, not a comment — and it does not survive the real objection, which is that a view is the wrong place for the only copy of a security-relevant rule: any other caller of `decide` inherits the gap. A session scope asked for on an ungrantable tool now narrows to allow-once and reports the scope it actually got. Worth stating plainly because it is a live question every time: the grant is `addRules` for one `toolName`, **not** the CLI's separate `setMode` update, so it never turns on `auto` — verified end to end (after allow-all on an `Edit`, a second `Edit` passed and a `Write` in the same turn still parked its own card). **The run tree is created `0700`, its files `0600`** (owner). The whole conversation lives there — `out.jsonl` is the transcript, `meta.json` the user's message, `perm/*.req.json` every tool payload — under a temp root that is world-readable on a typical Linux box, and everything was landing at the umask default: verified `drwxr-xr-x` on the run dir and `-rw-r--r--` on a 42 KB `out.jsonl` before the fix. Modes now come from the `mkdir`/`open` itself rather than a `chmod` afterwards, so nothing is briefly readable, and `_private_dir` walks the chain because `os.makedirs`' `mode` has reached only the leaf since 3.7. The deliberate non-action is that **existing** directories are not tightened: the chain begins at a directory we do not own, and a stray `chmod` on the temp root would be a far worse bug than the disclosure being fixed — the run dir, always freshly created here, is the level that holds the data. Only `.res.json` had been 0600, because that one was written through an `os.open` with an explicit mode for unrelated reasons (the O_EXCL latch) — which is the tell worth remembering: the one file that got it right got it right *by accident*, as a side effect of a different requirement, so the other seven were never noticed. macOS' per-user temp root hid all of it there. **Two follow-ups the privacy fix itself caused, and they pull in opposite directions** (owner P2 + Bugbot, same round): a `0700` root is unusable *shared*, because the first account to open a chat owns the namespace and every other local user can no longer create a run at all — while anything writable by them is either a world-writable directory we made or the very disclosure the `0700` prevents. There is no mode that satisfies both, so the sharing goes instead: the root is now `fused_render_claude-<uid>` (POSIX only; Windows' temp dir is already per-user). Separately the create was a TOCTOU — two runs starting at once both find the root missing, both `mkdir`, and the loser propagated `FileExistsError` out of `_start`, so one chat silently never sent its message (reproduced at 2 of 8 concurrent first runs). Parents now accept an existing directory; the **leaf** stays exclusive, because that one is the private boundary and finding it already there is a collision rather than a race to shrug off. And then the per-uid path, being *predictable*, became the next problem (Bugbot, High): another account can pre-create `fused_render_claude-<victim-uid>/runs` under a world-writable temp root, and owning the **parent** is enough — the sticky bit that stops one account renaming another's entry protects our entries in `/tmp` but is **not inherited** by a directory they made, so they rename the 0700 run dir aside the instant after `mkdir` returns and leave a world-readable one for the transcript to land in. `_require_private` now vouches for any existing directory in our tree (real directory by `lstat` so a symlink cannot pass, owned by our euid, no group/other write) and raises otherwise: a planted directory can deny the chat, but a refusal is not a disclosure. Same round, the leftover-key mechanism leaked through its own bookkeeping (Bugbot): `covered` was hand-listed, so naming both sides of an `a || b` marked the *loser* covered as well and the leftover dump skipped it — invisible on the card, authorised by `updatedInput`. It is now derived from what was actually rendered. And "rendered" had to be sharpened twice more, both times because the derivation was still describing an intention rather than an outcome. **Non-empty** is the second (Bugbot, High): `buildPermCard` emits a `<pre>` only for a truthy body, so a key marked covered while rendering as nothing was hidden from *both* surfaces at once — reproduced as `Write {"file_path": "/notes.md", "content": ""}`, whose card is a bare path with no body, identical to an ordinary path-only approve, while the empty content **truncates the file**. `show()` now records a key only when there is text to see, so empty values fall through to the leftover dump and print as the `""` they are. The general invariant is now one test — every input key is either rendered in the summary or named in the dump — and it carries a `bool(text)` guard, because `"" in shown` is always true in Python and *that* is precisely why the earlier alternation test could not catch this: the assertion designed to prove nothing was hidden read an empty value as present in every card it looked at. And the dump lost a key a *third* way (owner P2, next round): it was built by assigning each leftover into a `{}`, and `__proto__` is the one name that cannot be assigned — the input arrives via `res.json()`, which like `JSON.parse` *defines* an own `__proto__` key, so `Object.keys` lists it and the dump owes the user a rendering of it, but assignment reaches `Object.prototype`'s legacy setter, creates no own property, and `JSON.stringify` prints `{}` over a field `updatedInput` authorises in full (for a primitive value the setter does not even change a prototype — it simply discards it). `Object.fromEntries` defines rather than assigns, so the key survives; the guard is a node probe that parses the input *inside* node, because a JS object literal in the test would go through assignment and hide the case under test. The old test asserted on the source strings of this block, which is precisely why it did not notice — so it now runs `leftoverInput` instead, the same treatment `summarizePermission` already had. The pattern across this whole round is worth naming: **each hardening step created the next finding** — 0600 on the files exposed the shared-parent question, the shared-parent fix exposed the race, and the race fix had to be reconciled with multi-user usability. **Releasing a claim is only safe while nobody has been told an answer** (Bugbot, third round). The unlink-on-failed-write added above was copied into *both* writers, and it is right in only one of them: `agent.py` frees the claim because the write is all that happened and `_decide` returns an error, so a retry should be able to win it; `permission_server`'s timeout write has already handed claude a deny by the time it fails, so freeing the latch lets a later Allow land and the card reads "✓ Allowed" for a tool that was refused. The server now keeps its claim, which costs a card reading "could not record that decision" — the truth. Worth recording because the symmetry is exactly the trap: the same three lines, the same failure, opposite correct answers, and the thing that distinguishes them is not visible at the call site. Alongside it, a **low-severity crash in the same commit**: `int(float("inf"))` raises `OverflowError`, which is *not* a `ValueError`, so `FUSED_RENDER_PERMISSION_TIMEOUT=inf` (or `1e400`) took down `agent.py` at **import** — every action in the template, not just the setting — while the docstring promised a fallback. Now caught, and clamped to the int32-millisecond ceiling the CLI applies to a per-server MCP timeout, since past that a bigger number is not a longer wait. **The card shows the input whole — no truncation, no dropped keys** (owner P1, plus a Bugbot finding of the same shape). `summarizePermission` clipped Bash commands, Edit strings, Write bodies and unknown-tool JSON at 1200 chars while `permission_server` returned `updatedInput` **unchanged**, so an Allow authorised bytes the card had elided. That is not a display nit: the input is model-authored, so a prompt-injected model that knows where the cut falls puts something benign in front of it and the real payload behind it, and the user clicks Allow on the part they can read. Bugbot's separate report was the same hole by omission — `Grep`/`Glob` resolved `file_path || path || pattern`, so naming a path hid the query being authorised. Fixed together: nothing truncates (the `<pre>` was already `max-height` + `overflow: auto`, so length is a scrolling problem rather than a disclosure one), the pattern is the body for the search tools, and each case now declares which `input` keys it renders so `buildPermCard` can print whatever is left over — a key the curated view has no case for is shown verbatim instead of trusted to be unimportant. The lesson worth keeping is that **a summary in a security prompt is a different object from a summary anywhere else**: everywhere else eliding detail is a kindness, here it is the attack. Pinned by a node probe that runs the card's real summariser over a table of tool inputs (a 5 KB command whose last line is the destructive one; a `Grep` with both a pattern and a path); eight of those tests fail against the pre-fix template. **A card can also switch the mode, not just answer the request** (owner ask): "Allow, and let Claude decide from here" sends `setMode` on the same `updatedPermissions` channel the allow-all uses for `addRules`, which re-points the **running** session — measured, a turn that carded Edit/Write/Write in the strictest mode carded only the Edit once the first card switched. Three guards, because this is the broadest button on a permission card: it appears only while a stricter mode is in force, only alongside an allow, and only for `SWITCHABLE_MODES` (`acceptEdits`/`auto`) — `bypassPermissions` is unreachable from a card by any route and is re-validated in `permission_server`, since that is the side that actually hands the CLI its payload. The click also writes the `permission` URL param: a `setMode` dies with the process exactly like a session rule, so without that the next turn silently goes back to asking. **And that param is emphatically not the same fact as the mode the run is in** (Bugbot, next round) — reading it as one deleted the button at the only moment it was needed. The picker applies to the *next* spawn, so switching it to "Claude decides" mid-turn leaves the live session strict and still carding, while every card built after the switch concluded it was already in `auto` and dropped the escalation — the user asks for fewer prompts, keeps getting them, and the one control that could grant the request has disappeared. `poll` now reports the run's real `mode`, **derived rather than stored** (`_live_mode`): the mode recorded in `meta.json` at spawn, re-pointed by each allow whose `setMode` reached disk, ordered by `created_at` rather than by id because ids lead with `HH%M%S` and misorder a run spanning midnight. Deriving it means it cannot drift from what claude was actually told — a deny carrying a mode, or a mode outside `SWITCHABLE_MODES`, moves nothing. An absent `mode` shows the button, because the two directions are not symmetric: a needless button is a no-op click, a missing one is the bug. The gate moved into `permChoices` for the same reason `leftoverInput` did — it was previously pinned by a substring assertion (`WHOLE_TOOL_GRANTABLE.has(p.tool)` appears in the file), which is a test of the source rather than of the behaviour, and the extraction broke it while the code stayed correct. Which is the one thing this button has over the allow-all — the mode was *already* a persisted param, so the escalation the user asked for can outlive the turn without inventing the grant store that cross-turn allow-all would need. **How many cards you get became a picker** (owner ask, same round): `permission` rides the URL beside model/effort and maps onto the CLI's own `--permission-mode` — *ask every time* (default, no flag), *auto-accept edits* (`acceptEdits`), *Claude decides* (`auto`). Measured, not assumed: under `acceptEdits` an Edit applied with no card while `WebFetch` still parked one; under `auto` the classifier vouched for an edit+write. The bridge stays wired in all three, because the mode only decides how much is auto-approved *before* the prompt tool is consulted and whatever is left must stay answerable — a mode that skipped the bridge would reintroduce the exact silent refusal this entry exists to remove. `bypassPermissions` is deliberately absent (the owner's goal was "have Claude evaluate the request", not "no cards"), and an unknown value falls back to the strictest rather than erroring: extra auto-approval must be opted into, never handed over by a mangled param. Two more from the same round: **the expiry is latched, not labelled** — `poll` marking a leftover request `expired` writes it, and `decide` records an expiry once the run is not alive, because both orderings previously let a click land on disk after the run died and left the card reading "✓ Allowed" for a tool claude never ran (Bugbot); and **`FUSED_RENDER_PERMISSION_TIMEOUT` is now read on this side as well**, since `agent.py` stamps its own value into the generated `mcp.json` as both the server's env and the CLI's per-call ceiling — a constant here silently overwrote the user's setting, so the variable documented as configurable was not one (owner verified 5 → 3600). **Two independent ceilings for an abandoned card**, because there is no third party to notice: the server denies itself after 1 h *and writes that verdict down* (a returned-but-unrecorded deny left the card reading "waiting for you" forever on a page that re-attached), and the generated `mcp.json` sets the per-server call timeout above it so our sentence wins over the CLI's MCP-timeout error. What no test here can catch is the CLI changing its half of the contract — the result schema, the flag names, or which tools a prompt tool un-gates (that last one was found by reading the binary, not the docs, and is exactly the kind of thing that silently comes back). |
| D162 | **The markdown graph drops its per-note size cap, and a note is named by its file — never by a frontmatter `title:` or a leading `# H1`** | Two changes to `templates/markdown/graph.py`, both narrowing what the layer claims to know. (1) `MAX_NOTE_BYTES` (256 KB) and the whole `skipped_large` channel are gone — from `_walk`, `_assemble`, both scan signatures, both payloads, and the notices in `templates/markdown/template.html` and `templates/graph/template.html`. `_walk` still stats every note, but only for the index's `(mtime_ns, size)` cache key. (2) `parse_note` no longer emits `title`, and `_display_title(rel)` takes only a path: every `title` in every payload is the filename with directory and extension stripped. `PARSER_VERSION` 1 → 2, which invalidates every cached row wholesale (MD-8), so no migration. `_parse_frontmatter` keeps its tiny YAML subset for `tags` alone. SPEC gains **MD-3a** (naming) and rewrites **MD-10** (bounded by count, not size). The `_note_payload` fallback for a note absent from the scan stays — it is still reachable via a skipped directory or a cap, just no longer by being large. | **The size cap failed in a way the notice hid.** A skipped `.md` landed in NEITHER `notes` nor `assets` — `_walk` routes only non-notes to `assets` — so it was absent from both candidate indexes, and every `[[…]]` aimed at it resolved to `None` and drew a **ghost**: the graph asserted the note did not exist while it sat on disk, which is exactly the "unknown masquerading as missing" error D159 fixed elsewhere and this reintroduced by another route. Found on this repo: `DECISIONS.md` (323 KB) was the one skipped file. Both halves of the cap's original rationale were wrong — a long-lived decision log is precisely a note people want backlinks into, and the read cost it avoided is already paid once and cached by MD-8, so a big note is read on the open after it changes and stat-only forever after. Raising the number was rejected as the fix: any ceiling reproduces the ghost for whatever sits above it, and the walk's real budget is `MAX_ENTRIES` (a file count) which is untouched. Also corrected a comment in `template.html` that justified the Live Preview's whole-document decoration walk by citing this cap — the editor's bound has always been its own `MAX_BYTES` (2 MB), so the justification was already false and a 500 KB note already took that walk. **Naming by file** is the owner's call, and Obsidian's rule: a frontmatter `title:` or an `# H1` renaming a note meant the label on a graph node was not the string you would search for, rename, or type inside `[[…]]`, while an H1 that merely repeated the filename made the two agree often enough to hide the cases where they did not. Observed directly in a folder graph whose nodes read `— module overview`, `— specs`, `— exposed surface` — H1 fragments, useless as identifiers. Headings are still parsed in full; they are just headings. A side benefit that fell out: the name needs only a path, so a note the scan never parsed still has one. |
| D163 | **The link graph is laid out in bands by folder — one block per folder, wrapped to the surface — and only the horizontal axis is simulated** | `templates/shared/graph-canvas.js`, shared by both graph surfaces, stops being a free 2D force layout. `graph.py` now sends each note node a `dir` (its folder relative to the scan root, `null` for a ghost or a tag) so the canvas never has to recover one by splitting an id. The canvas groups nodes into one band per distinct folder ordered by depth then name, folderless nodes into trailing `unresolved`/`tags` bands, and owns `y` outright — eased toward the band, never simulated, except for a node the user dragged (`userPinned`, distinct from the `pinned` that setData sets on the focus). A band WRAPS: its nodes are dealt round-robin across `ceil(count * spread / usable)` lanes and the band grows to hold them, so the same folder is one airy line in a window and a compact block in a 320px panel. In-row pitch is `max(REST, widest measured label + 20)` via `measureText`. Three force rules follow: repulsion is horizontal and per-LANE; an edge holds `spread` only within a lane; an edge across BANDS pulls toward `ex = 0`; an edge across lanes of one band applies no horizontal force. Edge alpha washes out with edges-per-node. Band names draw in screen space at the left edge, inside a gutter `frameAll` fits around; separators sit in the `BAND_GAP` between adjacent bands only. Covered by 8 new behavioural tests in `test_graph_canvas.py`, whose harness gained a pumpable rAF queue and a ctx that records `arc`/`fillText` — positions are read out of the draw calls, so a layout that computes right and draws wrong still fails. `test_markdown_template.py`'s disc-seeding source assertion is replaced by one of them. SPEC gains **MD-19a**. | Owner feedback, verbatim: the graph "is not helpful", and what was wanted was which layer/folder the linking files are in. A free layout spends two axes on nothing nameable, so spending one on the tree is the cheapest way to make position mean something. **Every subsequent constant here was measured, and four of them are fixes for a failure the previous attempt caused** — the sim was run headlessly against real payloads and screenshotted in the app after each. (1) One row per folder ran labels together — observed as `READMEauthoring`, `catalogcomments`. (2) Nine nodes in a row is ~1080px, which in a 320px panel fits only at a zoom below the `> 0.7` threshold that hides labels: measured 0/9 labels drawn, i.e. strictly worse than the layout it replaced, which at least packed a square blob. Wrapping fixed both at once. (3) Band-wide horizontal repulsion then silently UNDID the wrap over the following frames — the block re-spread into one row and labels vanished again; confirmed by screenshot showing named bands and anonymous dots. Repulsion had to become per-lane. (4) Cross-lane edge alignment then collapsed a near-complete folder onto a single x, squeezing same-lane neighbours to a fraction of their spacing — labels overlapping from the opposite direction. Lanes are a wrapping artifact, so that force was removed entirely. (5) Reserving the full half-label-width as fit padding demanded 130px of margin against a 200px block, zooming out far enough to hide EVERY label to avoid clipping ONE; capped at 28px, accepting that an outermost long name can overhang. (6) `LANE_GAP` 30 → 38 because a label reaches up into the lane above it. Rejected: a fixed band pitch (a 14-folder graph, this repo's own, made the stack several times the canvas height); colouring by folder instead of positioning by it (the owner asked for layer/depth, and colour needs a legend to decode anyway); routing or bundling the edges (the honest content of a near-complete graph is "these all link to each other", which no arrangement of 30 curves states better than a faint texture). The hairball and the label overlap were both reported by the owner directly; neither was visible in any source diff, which is why the harness now reads positions out of `arc()` rather than asserting on the file. |
| D164 | **Graph positions are assigned, not simulated: barycenter-ordered bands with measured label slots, curved edges, and a per-frame label declutter; backlinks group by linking note** | The spring sim in `templates/shared/graph-canvas.js` (REST/REPULSION/MAX_SPEED and the three per-lane force rules of D163) is replaced by a deterministic `layout()`: within each band, nodes are ORDERED by the mean x of their neighbours (three alternating Sugiyama-style sweeps; an exact tie between a linked and an unlinked node resolves toward the linked one, because a stable sort otherwise strands it every sweep), then SPACED by a slot grid — each label owns `max(SLOT_MIN 60, measured width + SLOT_PAD 18)`, columns are as wide as the widest slot they hold, lanes wrap when a band's slots outgrow the surface. The whole layout shifts so the focus sits on the canvas mid-line; the stack centres vertically (minus the trailing BAND_GAP, which off-centred everything by half a gap). `step()` is now a glide — 0.2 ease with a 0.5px snap — so re-sends stay calm: carried-over nodes glide to new assignments, NEW nodes are born at theirs. Edges draw as vertical S-curves (`bezierCurveTo` through the mid-y; a same-band edge bows downward instead), the density wash floor drops to 0.18 with the focus note's own edges held at `wash * 2.2`, and which labels PRINT is decided per frame: candidates are ranked focus > hovered neighbourhood > degree and any label whose box would intersect one already kept is dropped for the frame (hover summons it back). Alternate bands carry a 4% foreground fill instead of separator hairlines. The backlinks list renders one row per LINKING NOTE with a muted `×N` for multiplicity, instead of one row per link. `test_graph_canvas.py` re-pins the geometry (settled-on-first-draw, column alignment, slot spacing, drag-collision label drop); `test_markdown_template.py` swaps the sim-constant pins for `REPULSION not in source` plus the slot constants. | Owner feedback, a screenshot after D163 shipped: labels printed over each other (`configuratiobservability`) and the edges fused into one central rope. The cause is structural, not a constant: the sim ran two forces that want opposite things — in-lane springs holding labels apart, cross-band pulls aligning linked notes into columns — and in the hub-shaped graph this panel actually shows (everything links to README) the column pulls all point at one x, outnumber the springs, and win. No tuning fixes a fight between forces; ordering + slots answer both wants with nothing left to solve, and overlap becomes impossible by construction rather than discouraged by tuning. The declutter pass is the owner's own suggestion ("hide the label if text/lines overlap it? only show it on hover or appropriate zoom") — the slots cover what the layout placed, the declutter covers what it did not: a node dragged onto a neighbour, two labels passing mid-glide. **Rejected: one node per row, label beside the dot** — built, shipped to a screenshot, and rejected on sight ("it looks bad"): solving overlap by construction is right but a vertical list reads as a file tree, not a graph, so the fix had to keep nodes spread in both axes. Rejected: keeping the sim and only hiding colliding labels — the rope of edges stays, and a graph whose labels MUST hide is worse than one whose labels cannot collide. Rejected: routing/bundling the curved edges (same grounds as D163: a near-complete folder's honest content is a faint texture). Verified against the live panel by screenshot at depth 1, 2 and `all` (205 notes) before the tests were rewritten, since D163's failure was exactly a layout that passed its tests and failed on sight. |
| D165 | **The tag concept is removed from the markdown template — for now** | Tags come out of every layer at once, so no half of the feature survives to rot: `templates/markdown/graph.py` loses `_TAG`, the tag block in `parse_note` and the `"tags"` key from its row (PARSER_VERSION 2 → 3, so a cached row still carrying tags is invalidated rather than replayed), `"tags"` from the `note` and `candidates` payloads, and the tag-node loop from `_graph_nodes_and_edges`; `templates/markdown/template.html` loses the `.tag` rule, the `#tags` sidebar element and the code that filled it, `tagWidget`, `TAG_RE` and its decoration pass, `URL_NODES` (it existed only to keep a URL fragment from becoming a tag), and the `#tag` autocomplete branch; `templates/shared/graph-canvas.js` loses `TAG_BAND`, its band label and rank, and the accent fill for `kind === "tag"` (accent is now the focus node alone). `_parse_frontmatter` goes with it: `tags` was the only frontmatter key anything read, so the tiny YAML subset had no remaining caller — `_frontmatter_span` stays, because the block must still be masked out of the body (MD-3). SPEC MD-3/MD-14/MD-18a/MD-19a/MD-20 rewritten; `test_markdown_graph.py`, `test_markdown_live_preview.py`, `test_markdown_template.py` and `test_graph_canvas.py` keep the ghost/unresolved halves of the tests that covered both and gain behaviour tests that a `#word` produces no node and no widget. | Owner: "remove the tag concept for now." The graph this template is actually read in is a code-adjacent vault, and there the hashtag rule fired on prose and code fragments rather than on deliberate tags — `#backer`, `#dsamed-environments` and their kind arrived as accent-coloured nodes in their own band, indistinguishable in weight from real notes. The grouping a tag buys is also the one thing the layout already gives for free (D163: a band per folder), so the noise was paying for a second, worse grouping axis. Removing rather than filtering: every filter considered (require frontmatter, require a minimum count, blacklist code-ish shapes) is a heuristic guessing at intent, and a link layer whose rules need guessing is exactly what MD-3 exists to avoid. **"For now" is literal — this is one coherent, revertable commit**, and the concept may come back behind better parsing (an explicit frontmatter-only `tags:`, or a tag pass that knows it is inside a code-adjacent document); the tiny YAML parser comes back with whatever frontmatter key is read next. Verified by screenshot against the live dev server on a real vault: no tag nodes, no `tags` band, focus still accent, unresolved band and ghost styling untouched, no tag chips in the sidebar, no console errors. **Two unrelated removals ride along, both "this was never used":** (1) `VAULT_MARKERS` drops `.fused-graph.json`, leaving `(".obsidian", ".git")` — the file was specified in MD-12 as a marker whose *contents* were unbuilt, which made it a marker only a fused user who had read the SPEC could ever have placed, and `.obsidian/` plus `.git` cover every vault anyone actually keeps; MD-12's "not built" clause now names per-vault tuning generally rather than a filename nothing reads, and the marker-climb test loses that parametrize case. (2) A confirmed layout bug in `place()` in `templates/shared/graph-canvas.js`: lanes were `ceil(total slot width / usable)` with **no cap at the band's node count**, and a band's height is `lanes * LANE_GAP` — so a band of two very long labels asked for five lanes, filled two, and still grew to five, pushing every band below it down and making `frameAll` zoom the whole graph out to fit space nothing was drawn in. Now `min(row.length, …)` with a floor of 1. The canvas harness gains an opt-in `LABEL_W` hook so one test can have *measured* labels (every other geometry test is pinned against the unmeasured `SLOT_MIN` slot, which is why the hook is opt-in rather than a default `measureText`), and the new test fails at 288 against 168 on the uncapped code. *(Historical rows D157/D159 and `docs/markdown-graph.html` still describe `.fused-graph.json`; both are records of the conversation at the time and are deliberately not rewritten.)* |

### Templates decoupled from the package: env plumbing, not imports (2026-07-29)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D166 | **A template learns about its environment from EXPORTED ENV VARS, never by importing `fused_render` — and the worker's `PYTHONPATH` injection that made those imports appear to work is removed** | Contract in SPEC PY-15 (PY-6a marked retired). The server exports `FUSED_RENDER_HOME_DIR` (already branch-resolved), `FUSED_RENDER_MOUNTS_DIR` and `FUSED_RENDER_ORIGIN` once before it starts serving (`server.export_app_env` + `set_server_origin_env`, called from both startup seams — `app.py` and `cli.py`), and `shell/mounts.export_ro_mounts_env` publishes `FUSED_RENDER_RO_MOUNTS` from `_write`, so it tracks every store mutation. `templates/shared/appenv.py` is the one reader: stdlib-only, values resolved **per call**. Migrated: `markdown/graph.py` (mount refusal + index dir), `annotate/annotate.py` and `claude/agent.py` (`mount_read_only`), `zarr_aoi/tile_server.py` (its inlined `_home_dir`, plus the `_branch.branch_port` origin fallback DELETED outright), `graph/condition.py` and `reader/condition.py`'s home-dir fallback. `executor._child_env()` and `_child.py`'s matching `sys.path.append` both go. One allowlisted exception: `reader/condition.py`'s `shell.prefs` read, an in-server-only pref, not a mount fact. | The imports were all `try: from fused_render... except: <fallback>` and worked only because `_child_env()` put the package on the child's `PYTHONPATH`. The **fused** local backend deliberately strips `PYTHONPATH`/`PYTHONHOME`/`VIRTUAL_ENV` from children for venv hermeticity (`python_compute.py`) while inheriting every other var untouched — so under that engine every guarded import took its fallback branch on every run, SILENTLY: a read-only mount looked writable (`os.access` lies under `CacheMode=full`), a vault got refused for the wrong reason, a tile daemon read from the baseline port. Env vars cross that boundary intact, which is why the fix needs no change in the `openfused` repo. **Env over per-request param injection**: the consumers sit deep inside the templates (`annotate._sidecar_writable` → `_save_sidecar`, `claude._mount_read_only`), so threading a value down would mean changing `main()` signatures in both — and there is no race to worry about, because this is global state any two concurrent runs would want identically. **Derived answers over the store**: `appenv` never reads `mounts.json`; it gets the mountpoint LIST, so the schema (`read_only_user`, legacy records, future flags) and all mount POLICY stay in `shell/mounts.py` and the mount-agnostic rule holds — a template may ASK a fact, never branch on how mounts work. **Values exported already-resolved**: the branch nesting and `_branch.sanitize` had TWO copies (one of them inlined in `zarr_aoi/tile_server.py` with a comment begging for lockstep maintenance); exporting the output of `storage.home_dir()` deletes the copy instead of pinning it with a test. **Deleting the `branch_port()` origin fallback is a fix, not a regression**: it yields the baseline `1777`, wrong under any `--port` override (the desktop launcher auto-picks a free port), so it pointed every read at a dead port and surfaced as an opaque zarr *"No group found in store"*; absent `FUSED_RENDER_ORIGIN` now raises a message that says what is actually wrong. **The fail-closed/degrade branches were kept and are now tested directly** — they stop being safety nets and become the primary path when no server exported the vars, so "no detector => refuse the walk" and "no RO list => the genuine `os.access` rule" each have their own test. Rejected: leaving the `PYTHONPATH` injection in as a belt-and-braces (it would preserve exactly the engine-dependent divergence this ends, and a half-removal — one half gone, one kept — is the worst state of all, so a test pins the absence of both); a `fused_render`-lite shim package importable from templates (a second import surface to keep in sync, and still unreachable in a uv-venv daemon); passing the facts as run params (see above). *(Bookkeeping: `DECISIONS.md` carries TWO rows numbered **D72** — in-process helpers, and layout params. Flagged, not renumbered: both are referenced by ID from code comments and SPEC, so a rename would silently invalidate those citations. This row takes the next number after the true maximum, D165.)* |

### fused.ai latency: warm CLI process and streaming (2026-07-29)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D168 | **`fused.ai` prewarms the claude CLI (prewarm-use-once-respawn) and streams NDJSON to the page** (amends D159, the fused.ai decision) | Measured on the D159 shape: the CLI is a Node program whose startup alone costs **~1.5–2.5 s per call** — at haiku/low-effort sizes it dominated the whole call. The process now runs with `--input-format stream-json --output-format stream-json --include-partial-messages --verbose` (mandatory with stream-json output; the CLI exits 1 without it), so it can be spawned BEFORE the prompt exists and fed the user message over stdin later. A one-slot warm pool holds one pre-spawned process keyed by its spawn config `(binary, model, system prompt, max_tokens)` — a warm process is only usable on an exact config match, since model and system prompt are argv/env at spawn. The slot converges on the **last-used config** (seeded with the defaults at server startup, fire-and-forget so readiness never waits and a missing binary skips it), a take is lock-guarded so concurrent requests never share a process (the loser spawns fresh, same code path), a replacement prewarm fires on every request, and a warm process that fails before producing anything gets one fresh-spawn retry. `{"stream": true}` on `/api/ai` switches the response to `text/x-ndjson` — `{"type":"chunk","text"}` per text_delta, closed by a `{"type":"done"}` frame carrying the same result schema (or the error, when it struck after HTTP 200 already left; validation errors before the first byte are still plain 400/502 JSON) — and `fused.ai` grows `opts.onChunk`. Non-streaming responses are byte-identical to D159's. `_run_claude_cli` is replaced by `_spawn_claude_stream` as the mockable seam. | **Why D159's subprocess-per-call needed amending but not abandoning**: the isolation property it bought (each call sees a prompt-fresh process, D70 discipline) is kept in full — a probe on 2.1.220 showed **context accumulates across user messages inside one process** (message 2 recalled message 1 verbatim, no per-message reset), which is exactly why multi-turn reuse was rejected: a reused process would leak one page's prompt into another's completion. Prewarm-use-once-respawn changes only the SPAWN TIMING: the ~2 s startup moves off the critical path into the idle gap between calls, and every process still serves exactly one prompt before being reaped. Streaming attacks the other half of perceived latency — time-to-first-token instead of time-to-last — and always spawning with `--include-partial-messages` means one warm process serves both modes (the extra `stream_event` lines are skipped when not streaming). Rejected: a multi-process pool (one slot already covers the one-page-hammering-one-config case that actually occurs); per-message context reset (no CLI flag for it); keeping the old `--output-format json` path alongside (two subprocess shapes to test for zero behavioural difference). |
| D169 | **The warm pool becomes ONE persistent claude process, reset per request over its stdin protocol** (amends D168) | The prewarm-use-once-respawn pool is replaced by a single long-lived instance (`_AiSession`) that every `/api/ai` call flows through. Per request, over the CLI's stream-json stdin: **`/clear`** as a plain user message (~0.7 s — emits a `conversation_reset` event, a fresh session_id, and a zero-cost local result; fully wipes the conversation, which is what carries D159's isolation guarantee now), then a **`set_model` control_request carrying BOTH `model` and the undocumented-but-real `system_prompt` field** (~0 ms, applies from the next turn; sent unconditionally on every request — the instance carries no config state between requests, only the process handle), then the **thinking budget**: absent or `low` effort sends **`set_max_thinking_tokens` 0** — the universal no-thinking switch, effective on EVERY model — while `medium`/`high`/`xhigh` sends **`set_max_thinking_tokens` null** (resets to the session default; mandatory each such request, because the clamp PERSISTS across `/clear`, unlike `effortLevel` which `/clear` resets) followed by an **`apply_flag_settings` control_request with `effortLevel`** (~10 ms, the same code path as the interactive `/effort`; `xhigh` joins the accepted values). The clamp is what `low` means because `effortLevel` only works on effort-capable models (sonnet/opus class) — haiku, the default, silently ignores it AND thinks by default in stream-json mode (a one-word answer measured 159 output tokens / ~5 s until the clamp restored ~tens of tokens), and the budget clamp overrides `effortLevel` in either send order, so the two are never combined on the low path. Omitted effort therefore defaults to `low` — which IS behavioral parity with the pre-rewrite code, whose env var clamped thinking to zero on every call unconditionally (the old `medium` default only mapped to a now-gone output cap). Each control_response is awaited (10 s control timeout; a wedged reconfiguration kills and respawns). All mechanisms live-probed on claude 2.1.220. Fallout: `--max-turns 1` is dropped from argv (the instance is multi-turn by design); the `CLAUDE_CODE_MAX_OUTPUT_TOKENS` and `MAX_THINKING_TOKENS` env overrides are deleted (Claude Code owns effort/thinking semantics — with `/effort low` a one-line answer measured 2.8 s wall, 7 output tokens); `max_tokens`/`opts.maxTokens` is **removed entirely** (no per-call mechanism in the CLI; effort is the only knob), and `_AI_EFFORT_TOKENS` with it. Concurrent requests **serialize** through the one instance behind a lock — the fresh-spawn-for-the-loser path is deleted. Health: a dead instance respawns on the next request; a mid-call failure before any delta was delivered gets one fresh-spawn retry (as before); reaps are `asyncio.shield`ed so cancellation (client disconnect, shutdown) cannot orphan the process or its temp system-prompt file. | Why single-instance beats the use-once pool it replaces: the pool only ever had ONE slot keyed by exact config, so the first call per config — and every call in an alternating-config page — still paid the full ~2 s spawn; the reset protocol makes **every** call warm at ~0.7 s reconfig, holds memory flat (~350 MB, one Node process) instead of churning a spawn per call, and needs no config-tuple matching at all. Accepted tradeoffs, named: **serialized concurrency** (local single-user app, calls are seconds — a queue beats a per-call spawn for the loser); **undocumented-field risk** — `set_model.system_prompt` is probed behavior, pinned to ≥2.1.220, and the failure mode is bounded by construction: a control error discards the instance and retries ONCE on a fresh spawn whose argv carries the requested config; a transient rejection heals there, while a CLI that rejects the field on every instance surfaces as a clean `ai_error` naming the rejection — one retry, no crashloop, no hang (both pinned by test). Rejected: keeping the pool alongside as a fallback (two lifecycles to test; the respawn path IS the fallback); skip-set_model-when-unchanged config tracking (a ~0 ms request is not worth carrying mutable state whose staleness would be invisible); resetting via `--resume`/session juggling (persists to disk, `--no-session-persistence` is the point); trusting fire-and-forget control writes without awaiting responses (observed safe, but the error branch is where the graceful degradation lives). |

*(Bookkeeping: D168/D169 amend D159 and were authored in parallel with D166's own env-plumbing work on `origin/main` — both branches independently claimed D166 the same day. D166 stays with the already-merged env-plumbing decision; the fused.ai warm-process work renumbers to D168/D169, with every code/SPEC citation updated to match.)*

### Engine parity, ahead of the built-in executor's removal (2026-07-29)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D167 | **Param binding has one implementation, embedded rather than re-stated: `engine.build_code` ships `_binding.py`'s own source into the generated wrapper** | The fused engine's child cannot `import fused_render` (the local backend strips PYTHONPATH), so the binding logic has to travel inside the generated code. It used to travel as a hand-written copy (`_fused_coerce`/`_fused_bind`) and had silently drifted in two ways: the copy resolved *string* annotations (`inspect.signature(..., eval_str=True)`) while `_binding` did not, so a template with `from __future__ import annotations` returned `"7"` under the built-in engine and `7` under the fused one; and the copy raised bare `TypeError` where `_binding` raises `ParamError`, so one bad param surfaced two different `error.type` values on the wire (PY-14). Fixed on both ends: `_binding.bind_params` gained the `eval_str` resolution (with the NameError/TypeError fallback to "no coercion"), and `build_code` now reads `_binding.py` via `importlib.resources` (zip/frozen-safe) and `exec`s it into a private namespace whose `__name__` is `"__main__"` — private because the user's module shares the wrapper's globals and `coerce`/`bind_params`/`ParamError` are plausible template names; `"__main__"` because traceback omits that module prefix, so the final line stays `ParamError: …` and `_split_error` recovers the same `error.type` the built-in worker reports from `type(e).__name__`. Rejected: keeping the copy and hand-syncing it (the drift above is the evidence that fails), and installing the package into the script venv (it is a user-code venv, not ours). `tests/test_engine_parity.py` runs one table of 14 coercion cases through the built-in engine, the generated wrapper, and — when `fused` is importable — the real backend, comparing bound values *and* `error.type`; `test_engine.py` adds the structural half (patch the source the wrapper reads, the patch must reach the child). |
| D170 | **SUPERSEDED by D172 (the interpreter switch), which removed `DEFAULT_REQUIREMENTS` entirely.** **`DEFAULT_REQUIREMENTS` is not a mirror of `[bundled]` and stops claiming to be; per-template PEP 723 headers cover the difference, and a test — not a comment — states the relationship** | The comment "Mirrors the `bundled` extra… keep the two lists in sync" was false for ten distributions and could not fail. Replaced by `tests/test_engine_requirements.py`: it read `[bundled]` and the core `dependencies` out of pyproject.toml and pinned the exact deltas (`BUNDLED_NOT_DEFAULT`, `DEFAULT_NOT_BUNDLED`), then walked every template file's imports with `ast` (function-level included) and asserted each `[bundled]` distribution it imports was in DEFAULT_REQUIREMENTS, in the file's own header, or in the header of the file that spawns it. Four real gaps fell out and were fixed here: `log_studio/reader.py` declared `drain3`; `map/map_render.py` gained a `rasterio` header (it had first been put on `map/vector_tile_server.py`, a file `run_python` is never handed, where the declaration was inert while looking correct — which is why the test derives the entrypoint relationship from the source via `_module_refs`/`_venv_roots` instead of a hand-kept table); and `usd/reader.py`'s header had a prose line **inside** the TOML block, so `script_requirements` raised and the whole usd template failed under this engine — notes go below the block. **What D172 keeps and what it discards:** the two derivations are the durable part and survive verbatim; the *baseline set* they were pinning does not exist any more, so the `drain3` and `rasterio` headers this row added were deleted again (both distributions are in `[bundled]`, which the app's interpreter now serves directly). The usd TOML fix stands on its own. The lesson that outlived the mechanism: a header on a non-entrypoint file is never read, and only a derivation from the source can say which files are entrypoints. |
| D171 | **CI gets one dedicated `fused-engine` job that installs the `[fused]` extra on 3.11 — and proves the engine tests ran rather than skipped** | The engine slated to become the default had zero CI coverage: no job installed the extra, so `engine.available()` was False everywhere and every engine test skipped while the matrix went green (D167 and D170 are both things that job would have caught). One job, not a change to the 3.10–3.13 matrix: the matrix must keep exercising the built-in engine, and the extra's direct-URL wheel is marked `python_version >= "3.11"`, so on 3.10 it installs nothing and would prove nothing. Two independent non-skip guards, because "installs the extra and skips everything" is worse than no job at all: `FUSED_RENDER_REQUIRE_FUSED_ENGINE=1` (set by the job) turns `engine.available()` into an assertion in `test_engine.py`, and the job fails if pytest reports **any** skip in test_engine.py/test_engine_parity.py. `cache: pip` keyed on pyproject.toml is for the extra's 27 transitive PyPI deps (boto3/pandas/pyarrow/cryptography), not the 3 MB wheel itself, which S3 serves without `Cache-Control`; `setup-uv` because the backend builds each script venv with `uv venv`/`uv pip` when uv is on PATH. One bug fell out of running it for real: with a **cold** `~/.openfused/venvs` (i.e. every CI run) the real-backend tests race under `-n auto` — `fused`'s venv creation is guarded only by an in-process lock, so losing workers die on `<venv>/bin/python` mid-build — hence the `warm_fused_backend_venv` fixture in `tests/conftest.py`, which serializes *creation* across workers with an O_EXCL lock file in xdist's shared basetemp. |
| D172 | **A script with no PEP 723 header runs on the app's own interpreter with no venv; a header is the script's COMPLETE dependency list, with no baseline unioned in** *(SUPERSEDED IN PART by D230: the declaration moved from the file's header to the folder's `pyproject.toml`. The no-baseline half stands verbatim — a folder's manifest is its complete list.)* | `DEFAULT_REQUIREMENTS` (numpy, pandas, pyarrow, duckdb, polars, matplotlib, scipy, pillow, openpyxl, shapely, geopandas) was installed into every script's venv, so the packaged app downloaded from PyPI — slowly, and needing network it otherwise doesn't — to obtain what `[bundled]` + the core `dependencies` already ship. Now: no header ⇒ `interpreter=<the app's python>` and no venv at all; header ⇒ the venv path unchanged, containing exactly what the header declares. `DEFAULT_REQUIREMENTS` is **deleted**, not kept for the header path, so a header means what PEP 723 says it means — the script's whole dependency list, not a delta against an invisible baseline. It also shrinks the venvs that survive: geotiff's used to install geopandas/matplotlib/polars/scipy/duckdb it never imports, which was pure download time in D173's loader. **The risky part is resolving the interpreter**, because `_execute_sync` spawns it verbatim as argv[0] and silently ignores `requirements` on that branch — no fallback, no error naming the cause. So the candidate is PROVEN rather than assumed: one subprocess per server process runs it and must report this app's own `sys.prefix` (same install ⇒ the app's site-packages), under the child's env rather than ours, since `python_compute` strips PYTHONHOME and a packaged interpreter may need it to find its stdlib. Anything that fails is handled by D175 (a PYTHONHOME wrapper, then a loud error) — never a spawn of something unverified, and never a silent degradation. Two guards on being wrong about `sys.executable`: an autodetected candidate whose basename isn't python-shaped is rejected WITHOUT spawning (if it were ever a py2app launcher stub, spawning it could start a second copy of the app, and the name check means we never have to learn what that stub does with `-c`), and the probe budget is 5s, not 60 — it is paid on the request path in exactly the case it exists to catch, and 60s of nothing reads as a hang. Platform by platform: macOS py2app points `sys.executable` at a real `Contents/MacOS/python` (D33, smoke-tested in build_dmg.sh) so it passes; the Linux AppImage's `usr/python/bin/python3` is an ordinary relocatable python; Windows runs under `pythonw.exe`, and `python.exe` beside it — same install, usable std streams — wins when present. `execute()` cannot carry `interpreter` (it derives one only from a uv workflow project, which a standalone .py has none of), so that path calls the documented `_execute_sync` subclass contract off the event loop; with `cache_storage=None` that is what `execute()` would have done anyway. Consequences accepted: nine core templates lost headers that named only things the app already ships (including the `rasterio` one D170 had just added — rasterio IS in `[bundled]`), and `pano/pano.py` had to declare the numpy and pillow it always imported and had been getting from the baseline. Three more headers went for a different reason — see D174. Four remain: `latex/engine.py`, `docs/docs.py`, `pano/pano.py`, `geotiff/tiff_reader.py`. `[bundled]` is unchanged, deliberately. `tests/test_engine_requirements.py` now enforces both halves — a header must be NECESSARY (declares something outside `[bundled]` + core deps) and COMPLETE — so the decision is self-policing rather than a comment. Rejected: keeping `DEFAULT_REQUIREMENTS` for the header path (it is what made an incomplete header like pano's invisible); adding `--system-site-packages` to the venv path (that is `fused`'s code, and the interpreter switch makes it moot). |
| D173 | **A script whose header needs a download gets a visible, cancellable install with verbatim errors — not a 30-second timeout** *(SUPERSEDED IN PART by D230: the flow, the detached worker, the progress record and the cancel contract are unchanged; the KEY is now the project folder, and the per-folder sharing this row deferred is exactly what D230 builds.)* | `fused.runPython` has roughly a 30s budget and the venv build ran inside it, so the first run of a header-carrying template either timed out or returned `EngineError: fused-render internal error … while running <path>` with uv's actual complaint buried in a traceback nobody reads. That is the worst possible answer to "you need a package you don't have", and it is the whole reason this flow is visible. Now `/api/run` pre-flights (header present + venv absent ⇒ `needs_install`, carrying the venv key and requirement list, plus a populated `error` object so a client that knows nothing about the loader still shows a real message), `POST /api/env/install` spawns a detached worker, `GET /api/env/progress?key=` polls it, `POST /api/env/cancel` stops it, and runtime.js drives the loader and retries the run **once** — in the shell, so every template gets it with no code of its own. The shape is `templates/docs/install_worker.py`'s (a detached worker writing `{stage,pct,detail,done,error,pid,ts}` to `progress.json`), reused rather than reinvented: one pattern in this repo, not two. Three things that fail silently and so are pinned by tests: the venv key is composed from `fused`'s own `requirements_venv_id`/`venv_key` and never re-derived (a local "sha256 of the sorted requirements" would agree until upstream changed the recipe, then fill a venv no run ever reads — a permanent double download with no error anywhere); requirements are re-derived from the .py on disk, never read out of the request body, so loader and run address one venv; and cancellation signals the process GROUP (or the uv download the worker waits on survives) but ONLY when the recorded pid leads its own group, which a `start_new_session` worker always does — without that guard a stale or recycled pid inside the server's group takes the server down, which it did to a pytest session during development. Errors are VERBATIM: `venvs._run_step` raises with uv's stderr inside, and that text reaches `progress.json` and the page unedited ("No solution found when resolving dependencies: … was not found in the package registry"). Progress is coarse and says so — `_run_step` uses `capture_output=True`, so pip's per-package output cannot be streamed without changing `fused`; four named stages, and a test pins the list so a future "62%" implying per-package resolution has to argue with it. Scope is **per-file**: the venv belongs to the requirement set one .py declares, and sharing one venv across several Python files in a folder is deliberately out of scope. Rejected: streaming pip output (needs a change to `fused`); a generic "install failed" message (the resolver's own words are the answer); installing on the request path with a longer timeout (a cold multi-wheel resolve has no bound worth guessing). |
| D174 | **A header belongs only on a file `run_python` is actually handed, and never on a daemon launcher whose real environment is its own venv** *(SUPERSEDED IN PART by D230: headers are gone, so the first half is now "no file may carry one at all". The second half is UNCHANGED and load-bearing — the geotiff and zarr_aoi daemons still own their `DAEMON_VENV`, because the built-in executor honors no declaration of ours.)* | Two distinct wastes, both invisible, both found by reading the templates after D172 rather than by a failure. **(1) The double download.** `geotiff/tile_server.py` and `zarr_aoi/tile_server.py` each declared the full heavy set (numpy/pyproj/imagecodecs/rasterio; numpy/zarr/s3fs/gcsfs/crc32c) *and* built a self-managed uv venv from an identical `DAEMON_DEPS`. But the only thing `run_python` executes in these files is `main("ensure")`, which is stdlib-only — it starts or reuses the daemon and returns a port; every heavy import is in the `--serve` half, which runs under `_daemon_python()`. So a first run built a fused script venv that then ran nothing but stdlib, and D173's loader made that wait *visible* — a progress bar for an environment nothing would ever import from. Headers deleted; `DAEMON_DEPS` is the real declaration. It has to be: the built-in executor (still the default engine) ignores PEP 723 entirely, so the header could never have served it, which is exactly why these templates grew their own venvs. **(2) The inert header.** `geotiff/_tiff_core.py` carried a complete, accurate, never-read dependency list: nothing hands it to `run_python` — `tiff_reader.py` imports it (and its header covers it) and `tile_server.py`'s daemon imports it (and DAEMON_VENV covers it). D170 shipped this same mistake on `map/vector_tile_server.py`, so it is now a third enforced invariant rather than a lesson: a header is only allowed on a file some NON-.py file in its own template folder names — which is what a `runPython('./x.py')` call site is — with sibling .py imports deliberately not counting, since one module importing another is precisely the relationship that does not make the importee an entry point. Verified by reintroducing the header and watching the test fail. **The narrow regression, accepted:** `_daemon_python()` falls back to `sys.executable` when uv is absent, and uv ships in the AppImage and the Windows installer but NOT the macOS DMG — so on a DMG install without user-installed uv, the geotiff daemon loses imagecodecs where the header had incidentally supplied it via the script venv. That fallback already lacked imagecodecs under the default engine (`_daemon_python`'s own docstring says so), and the header path is itself unreliable there — with no uv, `fused` builds venvs via `python -m venv`, the path `venvs.py` warns "cannot reliably self-locate its stdlib" for a packaged interpreter. Paying a guaranteed double venv everywhere uv exists to patch one unreliable case where it doesn't is the wrong trade. **Amendment (D182): the accepted regression is no longer accepted, because its premise is gone.** That reasoning rested on "the DMG ships no uv"; D176 then made `build_dmg.sh` bundle uv at `Contents/Resources/bin/uv` and hard-fail the build without it. The DMG's uv works — it was merely unreachable from `shutil.which`, since nothing put that directory on PATH. It also undercounted the blast radius: zarr_aoi's `s3fs`/`gcsfs`/`crc32c` are in neither the core deps nor `[bundled]`, so every remote store failed to open, not just LZW/JPEG GeoTIFFs. Fixed by putting the bundled uv on PATH once (D182), which is why the header deletion above stands unchanged. Net effect: opening a supported GeoTIFF or Zarr AOI now installs nothing through the loader at all — the daemon builds one venv, once. `geotiff/tiff_reader.py` keeps its header and its install, correctly: it is the fallback decode path for TIFFs the daemon can't read, it is a real runPython target, and it does the decoding in-process. |
| D175 | **A header-less script gets a generated PYTHONHOME wrapper when the app's own interpreter can't be used directly, and a loud error when nothing can be — never a silent empty venv, and never the network** | Two holes in D172. **(1) The fallback was not viable.** On probe failure both `interpreter` and `requirements` were empty, so the backend built a bare stdlib venv — and with `DEFAULT_REQUIREMENTS` deleted that venv has no data stack at all. The outcome was not "slower" but `ModuleNotFoundError: numpy` on a template that works today, reported as the script's own error, on a code path with no user-visible trace. Now: if no interpreter verifies, `/api/run` returns `InterpreterUnavailable` naming `FUSED_RENDER_APP_PYTHON` and runs **nothing**. That loud failure is what made hole (2) diagnosable at all, which is the argument for keeping it. **(2) The probe fails on the macOS DMG by default.** `scripts/build_dmg.sh` launches the bundled interpreter as `env -u PYTHONPATH PYTHONHOME="$RES" …/MacOS/python` and states the reason, while `python_compute` strips PYTHONHOME from the child — so the direct candidate reports the **build machine's** Homebrew framework as its prefix, a path absent on any user's machine. **Measured on a real DMG rather than reasoned**, which matters because two successive designs were wrong before this one: a `venv`-based rescue is *impossible* in that bundle — it ships no `venv` module (not in `Resources/lib/python3.12`, not in `lib/python312.zip`) and the embedded `Python.framework` holds only the dylib with no second interpreter binary, so `-m venv` fails regardless of environment and no `--system-site-packages` or `.pth` work could have helped. The venv rung was therefore deleted, not kept: it served no packaging (the AppImage and Windows both pass rung 1), and its only plausible trigger — a server whose `sys.executable` belongs to a different installation — is a misconfiguration better reported than papered over. **The answer is a wrapper script**, legal because `interpreter=` is merely an executable path: it exports the PYTHONHOME this process was launched with, unsets PYTHONPATH (mirroring build_dmg.sh) and execs the real python. Verified inside the mounted bundle under the fully stripped child env: `sys.prefix` is `Contents/Resources`, pandas / geopandas / rasterio / duckdb all import, and a header-less script runs through `engine.run_python` building **no venv**. Two details are load-bearing, not stylistic. **`exec -a <wrapper>`** (hence bash) makes the child's `sys.executable` the wrapper, so a daemon re-spawned as `[sys.executable, …]` re-enters it: `geotiff/tile_server.py` and `zarr_aoi/tile_server.py` scrub PYTHONHOME from the env they spawn into (deliberately — it would poison a uv venv), and measured on the DMG that spawn dies with `ModuleNotFoundError: No module named 'pandas'` when `sys.executable` is the raw python and succeeds when it is the wrapper, because the wrapper re-establishes PYTHONHOME itself. **`PYTHONEXECUTABLE` was rejected** though it achieves the same `sys.executable` with less machinery: it is inherited by every descendant and applies to *any* python they run, and a measurement showed an unrelated venv interpreter reporting our wrapper as its own `sys.executable` — which is exactly geotiff's uv-venv daemon re-spawning into the wrong interpreter. `exec -a` affects one process. Generation is gated to POSIX **and** to a process that actually has a usable PYTHONHOME, so Windows and the AppImage never get one; the wrapper is 0700 derived state under the app's own cache (never `fused`'s venvs path, where it could collide with a requirements key), regenerated only when its content would differ, and **probed exactly like any other candidate** — one acceptance rule for both rungs, `sys.prefix` equality, since PYTHONHOME makes the wrapper's prefix the app's own. CI cannot mount a DMG, so a stand-in reproduces the property that matters (stdlib and site-packages flattened into one PYTHONHOME-only directory) with a test asserting the stand-in genuinely fails without PYTHONHOME — otherwise every test below it would pass while testing nothing. Scope note: templates that DO declare a header are still expected to download; that is what D173's loader is for. |
| D176 | **`[bundled]` is not the macOS bundle. The bundle now ships ALL of it — the one held-out distribution, `drain3`, was REVERTED (see the amendment at the end of this row) — and the packaging list is derived rather than hand-written** | The DMG shipped with the interpreter switch working (a `.pptx` opened instantly, no venv) and then failed with *"the slides template needs python-pptx: pip install python-pptx"* — advice a DMG user **cannot follow**. Cause: `pyproject.toml`'s `[bundled]` is the dev-install list and, on Linux and Windows, the real shipping list (`uv pip install "<wheel>[bundled,…]"` in build_linux_appimage.sh / build_windows_installer.ps1). macOS is the outlier — py2app **copies** only what `setup_py2app.py` names — and eight `[bundled]` distributions were absent from that hand list. So the omission had taken effect on exactly one platform, silently: slides worked on Windows and not on a Mac. D172 then deleted the PEP 723 headers that had been accidentally covering it, because it read `[bundled]` as "what the app has". **The size-based split first proposed here is SUPERSEDED and must not be reinstated from its old reasoning**: the owner chose completeness over size, accepting ~85 MB of growth (from ~272 MB) to bundle `pymupdf` (60.3), `pikepdf` (13.8), `zarr` (3.1), `fpdf2` (2.8), `python-pptx` (2.5), `google-auth` (1.8) and `msgpack` (0.3). **`drain3` (0.1 MB) was excluded for a different reason entirely — to keep the install loader (D173) exercised on a shipped build rather than being code no packaged app runs — and that exclusion is now REVERTED; see the amendment at the end of this row.** The reasoning at the time: a script venv cannot see the app's site-packages, so an excluded dependency forces its template to declare its COMPLETE third-party closure, and `log_studio`'s closure is exactly `['drain3']`, the only zero-duplication candidate (slides would drag in Pillow and fpdf, pdf_studio openpyxl, zarr_aoi numpy), while still doing real resolution work (`jsonpickle==1.5.1`, `cachetools==4.2.1`, both hard-pinned, and drain3 is sdist-only so uv builds a wheel). `log_studio/reader.py` therefore carried a `sys_platform == 'darwin'` header; the marker (new — `engine._marker_applies`) made it evaporate on Linux and Windows, which already have drain3, instead of making them re-download it. **Two things had to change for macOS to work at all, both measured on an installed DMG:** the bundle contains no `venv`, `ensurepip` or `pip` module, so the loader died with "No module named venv" — uv is now bundled at `Contents/Resources/bin/uv` (as the AppImage and Windows installer already do) and put on the worker's PATH, and it works precisely because uv constructs venvs itself instead of importing `venv`. Verified end to end against the installed app with uv absent from PATH: venv built, drain3 installed, imports. Note the D175 wrapper must NOT be the venv base — measured, uv cannot install into a venv based on it — so it serves the no-venv interpreter path only; and `uv_bin()` deliberately does not gate on `sys.frozen`, which only py2app's boot script sets. `rasterio` was in the bundle only because `fused/workbench/core/_serialization.py` happens to `import rasterio` where modulegraph can see it — incidental, one upstream edit from vanishing, now pinned by the derivation. **`setup_py2app.py` now DERIVES its force-list** from the installed distributions' own metadata (`packages_distributions()`, which handles python-pptx→pptx and pymupdf→fitz/pymupdf), so transitive deps (lxml, xlsxwriter, numcodecs, pyasn1…) come along automatically and the hand-tracing of transitives is retired; the earned exceptions are preserved as explicit sets (`_duckdb`/`_cffi_backend` in `includes`, namespace packages never forced). **`google-auth` cannot go through py2app at all**, learned by failing a real build rather than by reading code: as `"google"` it is a PEP 420 namespace package py2app's bootstrap cannot resolve, and as `"google.auth"`/`"google.oauth2"` the build dies with `ImportError: No module named 'google'` — because `collect_packagedirs()` (build_app.py:1210) maps `get_bootstrap()` over EVERY `packages` entry and `modulegraph.util.imp_find_module` splits the name and calls `imp.find_module("google")`, all of which happens before py2app's own dotted-aware `included_subpkg` path is reached. Citing that dotted-aware path as evidence the entry would work was wrong: it is a real code path but not the first consumer. So `google/` is copied into the bundle after py2app (`STAGED_PACKAGES`, read from one declaration by both build_dmg.sh and the tests), exactly the explicit staging rclone and uv get, with a build-time smoke test that it IMPORTS through the bundled interpreter rather than merely being present. **uv is now a build-host requirement**, like the Linux and Windows scripts already have (`require uv` / `Resolve-Tool "uv"`): build_dmg.sh copies the host's binary in, so it fails loudly when uv is absent — which is what CI's macOS runners are, hence `astral-sh/setup-uv` in both the test and release DMG jobs (the release would have broken identically). **A second CI-only failure came out of the same build**: py2app died right after its own codesign line with no traceback, and only the `==>` heading that had not printed said where. Its missing-modules report `__import__`s every name modulegraph could not resolve and catches only `Exception`, so a name whose import raises SystemExit terminates the build silently; the set of missing names depends on what is installed, which is why local builds passed. `no_report_missing_conditional_import` turns that half off (we never read it), and build_dmg.sh grew an ERR trap that reports line, command, exit status and disk — the next silent `set -e` death names itself. The only hand-written part is `BUNDLED_EXCLUDED`, which `tests/test_bundle_contents.py` requires to name a real `[bundled]` entry and state a measured cost. **AMENDED (same PR): `BUNDLED_EXCLUDED` is now EMPTY — the `drain3` exclusion above is gone, and with it `log_studio/reader.py`'s PEP 723 header.** The justification did not survive counting: `[bundled]` has 20 entries, and `pyproj`, `imagecodecs`, `py360convert` and `pypandoc-binary` are **not** among them, so the `geotiff`, `pano`, `docs` and `latex` headers each already force a real install on a DMG — the loader was exercised four times over without the 0.1 MB holdout. "Keep a dependency out so its code path stays warm" is only worth anything when nothing else covers the path, and here four things did. It also cost something concrete: that header was the only marker-scoped PEP 723 block in the tree, and its `sys_platform == 'darwin'` marker made the file read as header-LESS on Linux, so both header invariants in `tests/test_engine_requirements.py` skipped it silently on CI — a blind spot fixed earlier in this PR by reading headers with `apply_markers=False`. `test_a_header_only_macos_needs_is_scoped_to_macos` lost its subject and was deleted; `apply_markers=False` stays as a forward guard. **The mechanism stays with an empty dict on purpose**: it is what makes "ships, or excluded with a stated cost — never just absent" enforceable, and `test_every_exclusion_is_declared_and_reasoned` is the guard that bites the day a genuinely large dependency does need holding back. |
| D177 | **A hand-maintained list that mirrors another list will diverge. Derive it, or make divergence a test failure — a comment is not a mechanism** | Stated as a standing rule because it happened **three times in one piece of work**, each time silently, each time found from a symptom far from the cause: (1) `engine.DEFAULT_REQUIREMENTS` claimed in a comment to "mirror the `[bundled]` extra" and was wrong for ten distributions (D170); (2) per-template PEP 723 headers maintained on files `run_python` is never handed, so they were complete, accurate and **never read** — twice, on `map/vector_tile_server.py` and `geotiff/_tiff_core.py` (D174); (3) `setup_py2app.py`'s `packages` list was missing eight of the distributions `[bundled]` promises, shipping a DMG whose slides template told the user to run a command a DMG cannot run (D176). The shape is identical: two lists that must agree, agreement maintained by human attention and asserted by a comment. Comments cannot fail. The rule, in preference order: **derive** the second list from the first at build or import time (what `setup_py2app.py` now does, which also retired the transitive-dependency bookkeeping); else **generate and commit** it with a test that regenerates and diffs; else, if it must stay hand-written, **reduce it to the deltas** and make a test demand each delta be justified in the source (`BUNDLED_EXCLUDED` must state a cost; `test_engine_requirements.py` derives entrypoint-ness and invocation from the AST rather than a table). A fourth instance, found by CI on the one interpreter that exercised it: `engine.script_requirements` deferred `import tomllib` behind a comment explaining that this kept Python 3.10 working — and it did, but only for scripts with NO PEP 723 block. Any script that HAD one raised `ModuleNotFoundError` on 3.10, with `requires-python = ">=3.10"` and no `tomli` dependency anywhere. The comment described a real mechanism and asserted a guarantee one size too large, which is the hardest version of this failure to see: nothing was out of sync, the reasoning was just incomplete, and it was invisible until headers became the main path. Fixed with `tomli; python_version < "3.11"` and an import of whichever exists; the test marker that gated those tests on the VERSION rather than on availability had been keeping them skipped on precisely the interpreter where the bug lived. Corollary, which is what made all three expensive: **a deliberate omission that is not written down is indistinguishable from a bug**, and the next reader will either "fix" it or delete whatever was quietly compensating for it — exactly how D172 removed the headers covering (3). A fifth instance was found the same way and **is** fixed here: **D29 claimed the .app bundles python-build-standalone**, when `build_dmg.sh` FATALs on anything but a *framework* python. Nothing was out of sync in code — the decision record itself had drifted from the build it described, which is the version of this failure no test can catch, since there is no second list to diff against. Corrected in place rather than superseded, because the decision (ship our own interpreter, don't freeze) never changed; only the description of how was wrong. The lesson that generalises: **a decision row describing a mechanism should name the check that enforces it**, so a reader can tell drift from intent — D29 now points at the FATAL that makes its claim true. |

*(Bookkeeping: THIRD same-day collision on this range. `main` had already renumbered the fused.ai rows to D168/D169 above — itself a fix for colliding with D166 — while this branch independently took D167/D168/D169. D167 is untouched on `main`, so the binder row keeps it and its `_binding.py` citation stays valid; the other two renumber to **D170/D171**, with the CI workflow comment updated to match. The root cause is structural and unfixed: two branches cut from one base always pick the same "next" number, and nothing here makes that detectable before a merge.)*

### server.py split into a real package (2026-07-29)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D178 | **`server.py` (4900 lines, every concern + a 1180-line `create_app()` with every route inline) is split into `fused_render/server/` — one module per concern, `routers/` for route-wiring-only modules, routes as real `APIRouter`s instead of closures** | `server.py` had grown to mix git-ignore-aware walking, directory listing, a stat cache, an fs watch registry, remote-mount probing, the fused.ai Claude session, template/condition resolution, raw-proxy hardening, fs mutation endpoints, and route wiring in one file. Split into `server/common.py`, `templates.py` (+ condition gating), `gitignore.py`, `walk.py`, `mount.py` (+ stat cache), `watch.py`, `proxy.py`, `fs_mutate.py`, `session.py`, `ai.py`, plus `server/routers/{shell,config,fs_read,render,run,export}.py` for the route groups that carry no reusable logic of their own — `server/app.py` is left with just `create_app()`: `FastAPI()` construction, `app.state` wiring, and `include_router()` calls. `server/__init__.py` re-exports `create_app`/`set_server_origin_env`/`export_app_env` so every existing `from fused_render.server import create_app` call site (cli.py, app.py, ~30 tests) needed no change. First cut used a flat `fused_render/_server_*.py` naming scheme (no subpackage) — correctly called out as not how this should be organized (a stuttering `_server_` prefix on 16 files sitting loose next to unrelated modules is a naming convention standing in for a package structure) and redone as the `server/`+`routers/` layout above. Every moved line is byte-identical to the original (a script diffed the extraction against `git show HEAD:fused_render/server.py`, not eyeballed); the only actual code changes are the closure→`Depends()`/`app.state` conversions `APIRouter` requires (documented per-route) and one real bug the split surfaced: `_STAT_CACHE_GEN` was a plain module-level int incremented via `global` in `mount.py` — imported by value into whichever module read it, that copy would freeze at 0 forever (a rebind via `global` only updates the defining module's own `__dict__`), silently breaking the `/api/fs/stat` TOCTOU race guard (caught by `test_midflight_invalidation_is_not_refilled`, not by inspection). Fixed by reading it live through `mount._STAT_CACHE_GEN` (module-qualified) at the two call sites in `fs_read.py` instead of an imported bare name. Rejected: re-export shims in `server/__init__.py` to keep old `server.X`/`monkeypatch.setattr(server, "X", ...)` call sites working unchanged — tests were retargeted to wherever their code actually lives instead, since a shim would have quietly reintroduced the exact `_STAT_CACHE_GEN`-style bug for every moved mutable global. Full suite matches the pre-split baseline exactly: 2691 passed, 86 skipped, 1 pre-existing unrelated failure (`test_an_abandoned_merge_closes_the_day_s_files` needs `/proc/self/fd`, Linux-only). |
| D179 | **The `@fused.udf` shim in 22 template files is inert locally and load-bearing once deployed — and 31 sibling files that also define `main()` do not have it.** Recorded, not fixed: this is a deploy-path inconsistency, and an entrypoint change does not belong in a packaging diff. The facts, all verified rather than reasoned: the real `fused` wheel has no `_registered_udfs` and `fused.udf(fn)` creates none, so `engine.py`'s generated epilogue always takes its bare-`main()` bridge and the shims do nothing under this engine. The hosted path is the opposite — `export.py` bundles each `runPython` target and the `fused` wheel's hosting layer serves it through a runner that resolves `_registered_udfs` or `result` **with no bare-main fallback**, so there the shim is the only thing that makes the file callable and a template without one returns null. Both halves were previously written down backwards: the shims were described as dead code that could be deleted, which would have broken every deployed template that has one. What makes this a D177 instance rather than a simple bug is that nothing enforces either side — the shim is a hand-applied convention across 53 files with `main()`, 22 carrying it and 31 not, and no test, lint, or export check can tell a deliberate omission (a `condition.py` gate, a daemon, a worker — never a `runPython` target) from a template that will silently return null when someone deploys it. The follow-up owes two things: a derivation of "is this file a `runPython` target" (`export.py` already computes exactly that for bundling, so it exists), and then either the shim on every such file or a bare-main fallback in the hosted runner — the latter being the real fix, since it retires the shim entirely and makes local and hosted agree on the contract `engine.py` documents. |

*(Bookkeeping: this row landed on `main` as D172, colliding with the interpreter-switch D172 above, which this branch had taken independently on the same day. Renumbered to D178 here — the interpreter row keeps D172 because D170's supersession note and a dozen `engine.py`/test citations point at it, while this row's only citation was `ARCHITECTURE.md`'s tree comment, updated to match.)*

### install-loader spawn window (2026-07-30)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D180 | **A claim and a progress record are two facts about one state, so ONE function answers "is this install in flight": `envinstall.progress()` now reports a claim with no record as a starting install, and `start()`'s join branch returns `progress(key)` instead of a synthetic record of its own** | Reported symptom: opening a `.docx` in the packaged app showed the install screen and then hard-failed with *"Cannot open sample.docx: the installer left no progress record"* while the install itself ran to completion — a manual refresh recovered. Cause: `_claim` is written **before** `_spawn`, and the parent's first `_write` lands only **after** `Popen` returns (a fork/exec of a Python interpreter), so for that whole window "claim present, no record" is what a healthy install looks like. `progress()` only knew about records and answered `None` — "never started" — and `runtime.js` treats a null record as fatal by design (correctly: a silent loader is the failure this flow exists to remove). The join branch's author had already seen the gap and closed it with a synthetic record, but **a fallback that protects one response body does not protect the separate request that follows it**: the client's next act is `GET /api/env/progress`, which calls `progress()` fresh and got null, so the fix was in the wrong place — it covered the POST's own payload and nothing else, not a reloaded page that polls without ever having POSTed. Which is why this is fixed in `progress()` and the synthetic record is **deleted** rather than merely persisted to disk: two record shapes for one state, one of them existing only in a response body, is the same divergence D177 is about, and the ONE place that can be right for every poller is the function every poller calls. `_claim_is_stale()` had already worked out the missing reasoning ("claim present, no record" = mid-spawn, not dead, bounded by `_CLAIM_GRACE_S`) and `progress()` had simply never been given it; the age rule is now `_claim_within_grace()`, read by both, so they agree by construction rather than by comment. Past the grace window a claim with no record resolves as **done-with-an-error** ("the installer never started"), not as a perpetual "starting": a server killed between claiming and writing must self-heal, and the poller needs an answer it can show and retry — the retry's `_claim` then takes the stale claim over, which is the pre-existing `_claim_is_stale` semantics unchanged. **The trap, and the reason the old `progress()` body survives as `_recorded_progress()`:** `_claim_is_stale` itself calls into progress, so if it saw the claim-derived record the claim would be its own alibi, nothing would ever be stealable, and one crash would wedge a key forever — the exact failure that function was written to prevent. So takeover reads the **record-only** view; the claim may be evidence to a poller, never to the decision about the claim. `cancel()` reads the record-only view for a different reason: a claim has no pid to signal, and a "cancelled" record written inside the spawn window would be overwritten moments later by the spawner's own `_write`, so the page would show cancelled and then watch the install continue — cancelling mid-spawn is a no-op reporting `False` and the poll keeps showing "starting". Why the race was routine rather than rare: `templates/docs/template.html` fires `warmup` unawaited and immediately awaits `import` for the same file, so one of the two always loses the claim and the user-visible one is the file open. The template was left alone — the concurrency is legitimate and the server must tolerate it — and a sweep found one more instance of the same shape, `templates/latex/template.html`'s `Promise.all([py(outline), py(bib)])` against a PEP 723 `engine.py`, which the server fix covers identically. The `map`/`netcdf`/`zarr_aoi` fire-and-forget warmups are safe only incidentally: their scripts carry no PEP 723 header, so they never reach the loader. Tests: a fresh claim reads as in flight, a stale one resolves, takeover still turns on claim age in both directions (the circularity guard), the join yields a pollable record, and `GET /api/env/progress` is non-null with a claim held — the last named to be distinguishable from `test_the_loader_polls_the_key_the_installer_actually_returned`, which reaches the same user-visible message via the wrong key. |
| D181 | **A guarantee, not an assertion: the install loader's orderings are now enforced by the OS, and a reaped pid must be one we spawned** | A review pass over the D173/D178 work found four things that were true only by luck, all in the same shape — something silently not happening. **(1) The pre-flight escaped its own error guard.** `run_python`'s `if requirements: envinstall.is_installed(...)` sat ABOVE the `try:` whose comment says it catches "every other failure". `is_installed` -> `venv_key_for` imports `fused.agent_core...venvs` unguarded and `_backend_attr` raises `RuntimeError` BY DESIGN when an upstream private attribute disappears — `routers/env.py` catches exactly that `(ImportError, RuntimeError)` pair for its own calls. Uncaught, /api/run became a 500 whose body is `{"error": "<string>"}`, and runtime.js reads `data.error.message` off that: the diagnostic `_backend_attr` wrote to be READ reached the user as the literal word `undefined`. Moved inside the guard. **(2) The spawn record could lose the worker's.** `_spawn` returns the moment `Popen` does, with the worker already running, so a resolver failing on its first import can write `done` before the parent writes `spawn`. The parent's `_write` was unconditional and the comment ASSERTED the parent wins — the same reasoning that produced the D180 bug. It is now an `O_CREAT|O_EXCL` create (`_write_if_absent`), so the worker's `os.replace` is the unconditional winner and the parent only fills an actual absence; because that write can no longer overwrite, winning the claim first unlinks the PREVIOUS attempt's record, or a retry would open showing the last failure's error while the new worker downloaded fine behind it. **(3) `_pid_alive` could reap another part of the server.** The pid comes out of `progress.json`, a not-`done` record survives a crash mid-install, and that pid can since have been recycled onto a child of the CURRENT server (an rclone rcd, a tile daemon, a pyramid build worker). `waitpid` reaped it and the owner's later `poll()`/`wait()` failed with ECHILD, which subprocess reports as **exit status 0** — a live or crashed child read as "finished successfully", which every owner branches on. Catching `ChildProcessError` cannot help: it only says the pid was not ours after the reap. The reap is now gated on `_SPAWNED`, the in-process set of worker pids we started and have not reaped. Deliberately in-process, not recorded next to the pid: a persisted owner pid would still need a boot identity to survive server-pid reuse, whereas "is this our child" stops being true the instant this process exits (the kernel reparents orphans to init, which reaps them), so the set is exact rather than an approximation. **(4) A cancel the server could not honour was silent.** `cancel()` reports `False` inside the spawn window — no pid is recorded yet — and the client ignored that: it had painted "cancelling…", the next `paint()` overwrote it with the installer's detail, the install ran on, `poll()` RESOLVED, and the script the user had just cancelled executed. Left server-side (a cancel marker the worker polls would add a cross-process protocol for a sub-`Popen` window) and fixed in runtime.js, in two halves because either alone still leaves a lie on screen: the dropped cancel is stated in a `notice` the painter defers to, with the button left live so a second press reaches the pid once the record carries one; and the resolve path honours `cancelled` — the venv stays built, but the run is abandoned, because a successful run is the one outcome pressing Cancel must never produce. Tests: the interleavings directly (a `_spawn` that writes the worker's record first, a retry after a failure, a child this module did not spawn left unreaped with its real exit status asserted), and the cancel through the loader's own JS under node. |
| D182 | **The bundled `uv` goes on the server's PATH, because a template may ask a fact about its environment but never branch on how the app was installed** | Five templates set their daemon's venv up with uv and resolve it as `shutil.which("uv") or ~/.local/bin/uv` — geotiff, zarr_aoi and netcdf's tile servers, plus las and pyramid. They have to resolve it that way: SPEC §26/MD-11 and D166 forbid a template from branching on its host, so "if this is a bundle, look in `Contents/Resources/bin`" cannot live in a template file. The macOS bundle ships uv at `Contents/Resources/bin/uv`, which is neither beside the interpreter nor on anyone's PATH — `envinstall._worker_env()` was the only thing that prepended it, and only for the install worker. So on a DMG with no user-installed uv, all five silently degraded: geotiff and zarr_aoi's `_daemon_python()` fell back to `sys.executable`, losing `imagecodecs`/`pyproj` (LZW and JPEG stop decoding) and `s3fs`/`gcsfs`/`crc32c` (every remote zarr store fails to open) — none of which is in the core deps or `[bundled]` — while las and pyramid raised advice a DMG user cannot act on. Dropping the PEP 723 headers (D174) removed the script venv that had incidentally supplied those deps, which is why it surfaced now. Fixed ONCE, in `export_app_env()`: the /api/run child and the daemon it spawns both inherit this process's environment, so prepending uv's directory makes every existing `shutil.which("uv")` start working with no template edit and no fifth copy of the resolution. Two reasons that is the right layer rather than a per-template helper — it is the SAME mechanism the Linux and Windows desktop supervisors already use (`supervisor/paths.py` prepends their payload's tools dir to the server's PATH), so this closes a platform gap instead of adding a second pattern; and the resolution is `envinstall.uv_bin()`'s, shared rather than restated, since it already knows all three packaged layouts. No uv anywhere stays a non-error: a dev checkout without uv is normal, and the templates already say so when they need it. Test asserts through `shutil.which("uv")` — what the templates actually call — with a fake `.app` layout as the only uv on PATH. |
| D183 | **A step that exists only to report a failure has to survive the failure — and nothing may re-resolve the payload after the payload is resolved** | Two bugs in `scripts/build_dmg.sh`, both invisible by construction: the build still failed, just without the answer. **(1)** `VAR="$(cmd 2>&1)"` under `set -euo pipefail` aborts on the ASSIGNMENT, so when the command fails — the only case the step exists for — the ERR trap fires and the following `grep -q` plus `echo "$VAR" >&2` never run. The traceback `2>&1` had just captured is discarded and the operator is told only "failed at line N". Four sites (google-auth import, the duckdb `_child.py` smoke, uv, rclone) now use `|| true` inside the substitution, the idiom `UV_SRC` already used; the grep IS the check and the exit status was never read. uv's and rclone's stderr still goes to the terminal on purpose — rclone's check is line-anchored, so folding stderr in could shift line 1. **(2)** `pip install pytest` went into `$BUILD_VENV`, which IS the shipped payload: py2app runs under that venv and copies modules out of its site-packages, and its purelib is `cp -R`'d into the .app. pytest's constraints could therefore repin `pluggy` (in setup_py2app.py's explicit force list) or `packaging` (in the derivation closure) with nothing but a pip warning. Now `--no-deps` plus a named `iniconfig`, and a loud import check so a future missing pytest dependency cannot read as a broken reconciliation step. The reconciliation itself is untouched — it is the only place `[bundled]` is genuinely installed, so it is the only place test_bundle_contents.py's per-distribution skips become assertions. Both are pinned by structural tests (`tests/test_build_dmg_diagnostics.py`), because no CI job on any other platform can run this script. Same review also made the `fused-engine` job's zero-skip gate premise TRUE rather than narrowing the gate: `tests/test_server_env_install.py` skips four tests without `node`, the job never installed one, and it passed only while `ubuntu-latest` happened to ship it — `setup-node@v4` now declares it, and `tests/test_ci_engine_gate.py` derives that requirement from the policed files so the next node-dependent test cannot re-break it. |
| D184 | **Apps live two levels under the workspace, `<fused_dir>/<tag>/<name>/`, and a tag is just a folder — there is no registry to update when a new one shows up** | The Home apps grid (`api_apps()`) previously treated every non-hidden top-level dir in `fused_dir()` as an app, flat. That made it impossible to tell a just-created app apart from a seeded example, from a git-cloned repo (D110's deep-link destination is the same `fused_dir()` root), from any other folder the owner happens to keep in the workspace — all three read as the same kind of thing. `api_apps()` now scans two levels: every non-hidden top-level dir is a "tag", every non-hidden dir directly inside it is an app, listed with a new `tag` field. Deliberately no whitelist/constant enumerating valid tags — the owner's call, made explicit when asked: "any subfolder is a tag", so introducing a new category later is one `os.makedirs` away, never a code change. `POST /api/apps/new` writes to `<fused_dir>/local/<name>/` (was `<fused_dir>/<name>/`); the packaged example seed (`shell/seed.py`) now copies into `<fused_dir>/examples/<name>/` instead of loose at the root. The seed's crash-safety moved with it: instead of one hidden `.<name>.partial` per example rsync'd and renamed individually, the whole `examples/` tree is staged under a single hidden `.examples.partial` at the workspace root and published with one `os.rename` — a crash mid-copy still leaves only a hidden entry at the root (never a half-written non-hidden `examples/` dir that would make the root look non-empty and wedge the empty-dir seeding gate off forever), at the cost of redoing the whole copy on retry rather than just the interrupted example, an acceptable trade given how small and fast the seed set is. Frontend: `AppInfo.tag` renders as a quiet mono pill on each `AppCard`; the "Apps" doorway and empty-state copy on Home.tsx were reworded to mention the tag-folder shape. Explicitly out of scope, the owner's call when asked directly: no migration of pre-existing flat workspaces. An install that already has flat top-level app folders from before this change (this repo's own dev workspace among them) keeps them exactly where they are — they simply stop appearing in the Home "recent" grid (they're not two levels deep) until moved by hand into a tag folder; they remain fully browsable via the file explorer regardless. Tests: `test_apps_api.py` and `test_shell_seed.py` updated throughout for the extra nesting level, plus new cases pinning the no-registry behavior directly (`test_any_top_level_folder_is_a_tag_no_registry`, `test_sorted_by_tag_then_name`) and the seed's new single-staging-dir crash recovery (`test_partial_seed_leftover_is_cleaned_and_reseeded` rewritten against `.examples.partial`). |
| D185 | **Scaffolded folders (apps AND templates) carry no `.claude/` of their own — the canonical skills are synced to Claude Code's user-level skills dir instead (`user_skills.py`), the only place skills are copied to; refreshed at server startup and at scaffold time** (superseded in part by D216: the sessions fused-render spawns now get the skills as a `--plugin-dir` plugin, and this sync is convenience for sessions it did not spawn) | The owner's ask: a freshly scaffolded app should be pure content, not boilerplate — the per-app `.claude/skills/` copy (a D106-style build-time duplicate) was the offender. Considered and rejected: a `CLAUDE.md` that tells Claude to fetch the skill from a GitHub URL as a fallback — it breaks offline (against the product's no-network-at-runtime stance), skews versions (the URL tracks `main`, the runtime serving the app is whatever's installed — and the skill describes bridge APIs that must match that runtime), and "follow instructions fetched from a remote URL" is a trust pattern not worth normalising. The accepted design leans on the fact that fused-render controls the environment its app sessions run in: `sync_user_skills()` copies **all three** canonical skills (`fused-render-authoring`, `fused-render-custom-templates`, `fused-render-usage`) into `<CLAUDE_CONFIG_DIR or ~/.claude>/skills/` — the same env resolution the claude template backend uses (`templates/claude/agent.py`), so on packaged desktop builds (where the supervisor points `CLAUDE_CONFIG_DIR` at the app's own state dir, supervisor/paths.py) the skills land exactly where the sessions we launch will look. Source resolution mirrors D106: repo `skills/<name>/` wins (editable installs, always-current), else a new packaged copy at `fused_render/skills/` (build-time copy in `scripts/hatch_build.py`, gitignored, shipped via `artifacts`). Ownership by marker file: only a dir carrying `.managed-by-fused-render` is ever overwritten, so a user-authored skill that happens to share a name is never clobbered; marked dirs are refreshed on every sync, which also fixes the staleness the per-app copies had (a copy scaffolded by an old version never updated). Sync points: a `create_app` **startup event** (deliberately not the `create_app` body — tests build the app without running lifespan, so they never write outside the conftest-redirected `CLAUDE_CONFIG_DIR`) plus `POST /api/apps/new` and `POST /api/templates/new` (repairs a deletion right before the session/scaffold that needs the skill). Best-effort throughout — a failed copy logs a warning and must never break startup or creation. Both scaffolds' copytrees now ignore `.claude` (a dev checkout can hold a stale pre-D185 build copy in either starter kit), the build hook deletes those legacy dirs, `templates_api._ensure_starter_skills` is deleted (its D106 per-folder copy job no longer exists — D106's single-source rule lives on in the sync's source resolution), and both starter `CLAUDE.md`s reference the skills by name with "restart fused-render" as the repair path. The owner's explicit call: skills are copied to the user location and **nowhere else** — the trade accepted with it is that an exported/moved template or app folder no longer carries its own guidance onto machines fused-render doesn't manage. Tests: `test_user_skills.py` (marker ownership both directions, refresh, missing-source no-raise, packaged fallback, and that the `SKILLS` tuple matches the real repo dirs), plus `test_apps_api.py::test_new_app_has_no_dot_claude_and_syncs_user_skills` and the reworked `test_templates_api.py::test_new_template_scaffolds_and_binds`; `tests/conftest.py` now redirects `CLAUDE_CONFIG_DIR` for the whole run so no test touches the real `~/.claude`. |

### macOS quit teardown (2026-07-30)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D184 | **Quit is a teardown with an ORDER, run off the AppKit main thread under a DERIVED deadline, entered through ONE function by every surface — including the ones AppKit owns — and the process that mounts something is the process that must unmount it** | Quitting from the tray icon hung for seconds, raised macOS "server connection interrupted / disks not ejected properly" alerts for every mounted remote, then died in a "quit unexpectedly" SIGABRT dialog. Three independent defects, all funnelling through one four-statement `_do_quit`, and each one is a different instance of the same mistake — treating "ask for it" as "it happened". **(A) The mounts were orphaned.** On darwin `attach_mount` uses `"mountType": "nfsmount"`, so the KERNEL holds a real NFS mount whose server IS the rcd process; quit killed rcd with those mounts still attached, the loopback NFS client timed out, and macOS complained. The comment banked on "rcd unmounts cleanly on SIGTERM" and was wrong twice: rclone's handler issues a PLAIN `umount`, which a busy macOS nfsmount rejects — the exact state `reconnect_mount` documents and the whole reason `_force_unmount` exists — and `_kill_current_rcd` escalates to SIGKILL after 5s, skipping rclone's unmount altogether. This was the ONLY mount teardown in the tree that did not go through the rc-unmount → force-unmount ladder. Now `lifecycle.unmount_all_for_quit` runs the existing ladder per mount, on its own daemon thread — the `mount_state` pattern — so a wedged `umount -f`, which blocks in the kernel and cannot be cancelled, costs only its share of a bounded budget instead of the quit. The rung is `detach_mount(m, force=True)`, exactly what `restart_rcd` already does before it kills a daemon (quit is the same "the daemon is about to go away" situation, so it gets the same call rather than a second copy of the ladder — a first cut open-coded rc-unmount + `_force_unmount` and thereby skipped detach_mount's EBUSY handling, which quiesces the tile daemons that hold files open under the mount and retries). `force=True` is load-bearing: the plain call refuses to force ("failing loudly beats corrupted reads"), right for a user action on a live mount and wrong here. One rung is added on top, gated on the KERNEL's view (`_is_mounted`) rather than on rcd's answer, because detach_mount can report done with a mount still attached — its own force gate is `_ismount`, which is False for a wedged mount, and its success path trusts an rcd OK that the INCIDENT 2026-07-16 split-brain shows can sit over a surviving kernel entry. The persist/ownership gate that guarded only the reap is factored into `_rcd_is_ours_to_reap` and now gates BOTH rungs, because unmounting a daemon's mounts is as much a teardown of it as signalling it: a persisted dev daemon (`FUSED_RENDER_RCLONE_PERSIST`) or a still-live foreign spawner (a CLI server sharing the per-home rcd) keeps its mounts. **And unmounting is not enough while something else is still MOUNTING** (review's catch): `health.startup()` runs `run_automount` on a daemon thread calling `attach_mount` per mount, seconds each against S3, so quitting during startup — the readiness-failure abort above all, whose own premise is that automount has had time to run — could land an attach after that mount's unmount thread had finished, and the reap then killed rcd under a live nfsmount: defect (A) restored by concurrency rather than by omission. Interlocked in two layers, since neither covers the other's window: `_QUIT_TEARDOWN_LATCH` (a `threading.Event`, never rebound — a bool set through `global` would freeze at False for every importer, the D178 `_STAT_CACHE_GEN` bug) is armed before the mount snapshot and `attach_mount` DECLINES while it is set (declining, not unwinding: the mount would either outlive the reap or need tearing down twice, and every caller already treats an error string as "not mounted, logged, move on"); and for the attach already inside `mount/mount` when we armed it, **the attacher itself is responsible for leaving nothing attached**. That second layer started as a one-shot sweep of the kernel mount table after the fan-out, and review was right that a sweep cannot be the answer: `mount/mount` carries a 60s timeout, so no choice of "when to look again" is correct when the writer may take a minute — it can land after the sweep, or after `stop_local_rcd`. So `attach_mount` re-reads the latch when `mount/mount` returns and immediately detaches what it just created. That works even after rcd is reaped (the force rung is a kernel `umount -f`, which needs no daemon), and it is the only party that both knows a mount was created and is positioned to act at the right moment — the general principle being to make the last writer responsible rather than have the teardown guess. The sweep stays as a narrowing, together with a bounded wait on an in-flight-attach count (a dict mutated under a Condition, spanning the whole region that can leave a mount behind, self-detach included); both spend only the leftover join budget, so neither can extend what `app.py` was promised. The latch is armed only AFTER the ownership gate passes: a daemon we are deliberately leaving running keeps its mounts, so an attach racing that quit is harmless. **(B) The beachball was structural, not slow code.** The whole reap ran inside a menu-item action with the AppKit run loop blocked: an rc `core/pid` probe (3s) plus SIGTERM and SIGKILL polls (5s each) — ~13s of guaranteed hang. `start_quit` moves it to a worker and returns at once; a watchdog thread calls terminate when teardown finishes OR after `QUIT_HARD_DEADLINE_S`, whichever comes first. **That deadline is DERIVED, and review caught it being a lie:** it was a hand-picked 15.0 while the steps it waits on summed to 21 (drain 2 + unmount 6 + the reap's 3s rc probe + 3s `ps` + two 5s exit polls), so on a slow-but-healthy teardown it fired DURING the SIGTERM wait, skipping the SIGKILL escalation — and on macOS production rcd is a normal child that survives the parent's `exit()` by reparenting to launchd, so an rclone ignoring SIGTERM was left alive under mounts whose teardown had not finished: defect (A) reintroduced by arithmetic. `rcd.py` now names its inline timeouts and exports `RCD_REAP_WORST_CASE_S`, and `app.py` computes the deadline as drain + unmount budget + that + a stated margin, so tightening any inner budget moves it and nothing restates the sum. **A second review pass then found the derivation itself still incomplete, twice — the lesson being that "derived" is only as honest as the term list.** (i) Every iteration of both kill polls evaluates `_live_rcd_port()`, which makes its OWN `core/pid` probe with a 3s timeout, and the loops test the clock BEFORE an iteration, so one entered just under the deadline blocks a full probe past it: each phase is bounded by 5+3, not 5. Counted per phase now (the true ceiling — the probe's dead-answer cache outlives a phase, so a phase cannot pay for two), and the poll's conjunction is reordered to `not _pid_alive(pid) and _live_rcd_port() is None` so the free syscall short-circuits the expensive probe: a live process now costs no probes at all, where a wedged daemon used to be re-probed for the whole wait. (ii) The tile-daemon quiesce ran before the join deadline was even computed, i.e. outside the number `app.py` is handed — 3s per wedged daemon, sequential. It now has its own bound (`_QUIT_QUIESCE_BUDGET_S`, joined as a whole so adding a daemon to `DAEMON_STATE_FILES` cannot stretch the deadline) and `_QUIT_UNMOUNT_BUDGET_S` is the sum of quiesce + joins; the halves stay separate pools because a slow quiesce must not eat the time the unmounts need. 34s worst case is deliberate: it is a backstop for a genuinely stuck step, not a target — the teardown is off-thread so the UI stays responsive, and the normal path finishes in well under a second. Tests assert the inequality strictly, that the reap bound equals its full term list, and that the poll makes no rc probe while the pid is alive. Both halves of the watchdog are load-bearing: teardown must get a real, bounded chance to complete (a fire-and-forget quit would reintroduce (A) whenever the process won the race), and a wedged step must never leave an app that refuses to quit. Exactly one terminate call, because only the watchdog ever makes it; it hops back through `AppHelper.callAfter` since `NSApplication.terminate:` is main-thread-only. **(C) The SIGABRT was the GIL, not DuckDB.** From the crash report's main-thread stack (`FusedRender-2026-07-29-135823.ips`): `rumps.quit_application()` → `NSApplication.terminate:` → C `exit()` — NOT Python finalization — and pyobjc releases the GIL for the duration of that ObjC call, so the C++ static destructors `exit()` runs execute with NO GIL held. One destroys the `DuckDBPyConnection` that `templates/duckdb/reader.py` stashes on the *duckdb module* to keep DuckDB's HTTP range cache warm across reader runs; its destructor touches the Python C-API (`PyEval_SaveThread`) → `Py_FatalError` → `abort()`. It lives in the app process because the duckdb reader is an in-process helper (`executor.INPROCESS_HELPERS`) and on macOS the server runs in-process inside the rumps app — so previewing one mounted parquet file made every later quit crash. Linux/Windows exit through normal finalization, with the GIL held, and were never affected. Fixed with `reader.close_http_connection()`, placed next to the stash it owns (plus a shared `_HTTP_CON_KEY` so writer and closer cannot drift), called from the quit path while Python is healthy; it drops the attribute BEFORE closing, since a closed-but-reachable connection is still an object for `exit()` to trip over. **A close is an event; the invariant needed is "no stash from here on"** — review's catch: the drain ahead of the close is a bounded join that can time out, so uvicorn may still be serving while unmount and reap run for seconds, and one parquet read in that window called `_http_connection` and restashed a fresh connection. So the close sets a one-way latch (on the duckdb module, since the reader module object is transient) and `_http_connection` RAISES past it instead of building another — **and a latch is only a barrier if it is read at the moment of the write** (the next review's catch): the check sat at the top of `_http_connection` while the `setattr` that installs the stash is ~30 lines and one `duckdb.connect` + `LOAD httpfs` + four PRAGMAs later, so a read that passed the check could install its connection after the close had latched and cleared, and nothing would ever close THAT one. The install decision now happens under a lock that `close_http_connection` also holds while it latches and takes the stash, which turns the property into one that can be stated: once `close_http_connection` returns, nothing can put a connection back on the duckdb module. Building stays outside the lock (reads must not serialize behind each other's setup) and a builder that loses — to the latch, or to another builder — closes what it built, because handing an unclosed `DuckDBPyConnection` to the collector is the same crash on a longer fuse. The lock itself lives on the duckdb module too, installed via `dict.setdefault` on its `__dict__` — the one way to create a shared lock when you do not yet have one to guard the creation, and necessary because a module-level `Lock()` in a file the executor re-execs per request is a different object every call, i.e. no lock at all — the right shape because the remote fast path's call site already treats any failure there as "fall back to the plain file path", which is what a process on its way out should do. The order is deliberately unchanged rather than moving the close to just-before-terminate: the steps in between are the ones that can hang, and a deadline firing there would terminate having never closed anything — precisely the crash. A sweep of the other in-process helpers found no second module-stashed native handle — `templates/excel/reader.py`, `templates/h3/h3_reader.py`, `duckdb/writer.py` and both sqlite helpers all use function-local connections — so the parquet reader's stash is the only one. **(D) A fixed quit path is worth nothing on the surfaces that don't use it.** The most important finding of the review, and the one nothing in (A)–(C) would ever have surfaced: `setup_py2app.py` sets no `LSUIElement` on purpose (D34 — regular app, Dock icon AND menu bar item), and there was no `applicationShouldTerminate_` anywhere, so **Dock icon → Quit, ⌘Q and logout/restart bypassed the entire teardown** — straight to `-[NSApplication terminate:]` → `exit()`, with `state["quitting"]` never consulted. Every defect above was still fully live on the surface a user reaching for the Dock icon actually hits. Fixed with the canonical Cocoa pattern (return `NSTerminateLater`, tear down off-thread, then `replyToApplicationShouldTerminate:`) — the ONLY answer that keeps the teardown off the main thread, since `applicationWillTerminate:`, the other hook rumps does define, runs synchronously on the way to `exit()` and would just reinstate the beachball. The hook is added to rumps' delegate class the same way the openFiles/openURLs/reopen handlers already are, which is what makes it upgrade-safe (it adds a method rumps has no opinion about rather than wrapping one): verified against the installed rumps 0.4.0, which defines no `applicationShouldTerminate_`, and pyobjc resolves the dynamically added selector to AppKit's real signature — `I@:@`, an unsigned-int return, so `NSTerminateLater` arrives as 2 and not as a bridged object that AppKit would read as "terminate now". A failed install is logged, never raised (PV-8: an app that won't launch is worse than one whose Dock quit skips the teardown), and the reply wait carries its own backstop because an app AppKit is awaiting a reply from cannot be quit at all. **The ordering is now a tested property, not statement order in a closure, and there is exactly ONE teardown rather than one per surface.** `_do_quit`'s body became module-level `quit_teardown` (drain the server → close the duckdb stash → unmount → reap rcd, each step independently guarded so one failure cannot skip the rest), `start_quit`, and `begin_quit` — the single entry every surface funnels through: it starts the teardown unless one is running, and sets a shared `quit_ready` event BEFORE the surface's own end action, which matters because that action is what re-enters `terminate:` (a delegate hook answering `NSTerminateLater` to our own end-of-teardown terminate would hang the quit forever; it answers `NSTerminateNow`). `make_quit_action` is what the tray item and the popover's `quitApp_` get, via the actions dict. Idempotence spans surfaces, not just clicks: a Dock Quit during a tray-initiated teardown joins it instead of racing a second unmount + reap — and that claim needed a LOCK, not a premise: `begin_quit`'s check-then-set was documented as safe because "both entry points are AppKit callbacks on the main thread", which routing the readiness-failure abort through the same action falsified in the same round (the bootstrap thread now calls it). Held open, the unlocked window really did produce two fan-outs, two reaps, and — worse — two separate lazily-created `quit_ready` events, so the delegate hook would wait on a signal the other surface never set: an app AppKit never gets a reply from. Review also found the last direct bypass — `_bootstrap_server`'s readiness-failure abort called `quit_application()` straight, and by then the server may have been up 15s, long enough for `run_automount` to have attached mounts — so it goes through the quit action too, and a structural test asserts `quit_application` is reachable from nowhere but the terminate hop, because a new bypass is invisible in behavior. One efficiency fix from the same pass: the tile daemons are quiesced ONCE before the per-mount unmounts fan out, rather than per busy mount by each `detach_mount` (each of which quits all daemons) — and hoisting it beats reacting to EBUSY anyway, since a released file cannot cause a busy unmount in the first place. The server drain is a bounded join on the server's thread, now returned by `_start_server_thread`: `should_exit` alone is fire-and-forget, uvicorn never resets `started`, so the thread ending is the only observable "it stopped serving" — and it comes first because an in-flight `/api/fs/raw` read holds files open under a mount, a measured cause of busy-mount unmount failure. Tests (`tests/test_app_quit.py`, the first tests app.py has ever had) fake at the rc/subprocess boundary like `test_shell_mounts.py`, so they need no macOS, no rclone and no real mount: the ordering trace (duckdb before any unmount, every unmount before any `os.kill`), a prompt return against a time budget while teardown sleeps past it, terminate firing on a teardown that never finishes, a wedged AND a hanging mount not stopping the others or the reap, both gates leaving everything attached, the stash close in its no-duckdb / no-stash / raising-close variants and the latch refusing to restash, the delegate hook's three cases (immediate `NSTerminateNow`, joining a tray teardown, starting one) with an injected reply callback so no AppKit is needed, the deadline inequality, and the two structural assertions above. Three test-quality lessons from the review, all now written into the tests themselves: a one-way latch tripped on purpose LEAKS unless the fixture clears it unconditionally (`monkeypatch.delattr(..., raising=False)` records an undo only when the attribute already exists, so `tests/test_duckdb_reader.py` would have kept passing while silently asserting on the local-path fallback — hence an autouse guard there that fails loudly on a latch from any source); a process-global interlock needs the same treatment or every attach test in `test_shell_mounts.py` starts refusing; and a race test that starts N threads and hopes the GIL switches in the right microsecond PASSED against the unlocked code — only holding every caller inside the window with a barrier fails deterministically. |
| D185 | **Annotate picks the line-number anchor strategy on a lineNumbers gutter that is actually LAID OUT, not on "is this CodeMirror" and not on the gutter merely existing** | Annotation was dead on the markdown notes view — clicks did nothing, drag-selections did nothing, stored comments never painted, and nothing was logged. `isCmDoc` was `!!doc.querySelector(".cm-editor")`, and the notes view IS a CM editor (MD-18a), so every gesture took the code path whose only source of a line number is `.cm-lineNumbers .cm-gutterElement`: `cmVisibleLines()` returned `[]`, and both handlers bail on an unresolvable anchor AFTER `preventDefault()`/`stopPropagation()` — swallowing the gesture rather than failing it. The first fix was wrong in a way only the running app showed: querying for `.cm-lineNumbers` still matched, because the notes view ships `CM.basicSetup` (so the gutter element exists) and merely hides `.cm-gutters` in CSS — the "no line numbers" of MD-18a is a display rule, not a missing extension. The condition that matters is the one `cmVisibleLines` itself applies, `height > 0`, so the probe now measures the gutter's box. `.md` binds `annotate` as a trailing mode and the view picker defaults to the first non-annotate mode, so a `.md` file landed on the broken view by default, which is why it read as "annotation doesn't work on markdown". The probe now requires the gutter that strategy reads from, and a gutterless editor falls through to the ordinary element-path/quote anchors — the right model for prose anyway. Quote containers are hoisted to `.cm-content`, because the `.cm-line` div a selection sits in is remounted on scroll and its `nth-of-type` path drifts onto another line; the quote plus its occurrence index locates the span inside the stable content element, and off-screen text has no pin until it scrolls back in (off-screen, not detached — the same property code anchors have). Rejected **teaching the markdown view to keep a hidden lineNumbers gutter** for annotate's benefit: it puts annotation-aware code inside a view template, which D78 removed on purpose, and `cmVisibleLines` filters on `height > 0` so `display:none` would not even work. Rejected **deriving the line from `posAtCoords`** for now: it fixes this and any future gutterless editor, but needs the framed view's `EditorView` instance, which annotate deliberately cannot reach. Kept as the follow-up if path-anchor drift in long notes turns out to bite. |
| D186 | **A note OPENS read-only; editing is one click away and lives in the URL** | Reverses MD-1a's original "editing is the default, as in Obsidian" (owner call). Obsidian's default is right for a notes app you open to write in; fused-render is a FILE EXPLORER, where opening a file is a read — and on a surface that is always a live editor, a stray keystroke on a note you opened to look at silently rewrites it (MD-16 autosaves after 2s idle, with no save button to not press). So an absent `edit` param is read-only and only an explicit `"1"` grants editing; the preference still rides in `fused.params`, so an editing session survives a refresh and travels in a shared URL. `aria-pressed` on the corner toggle flips to track EDITING, since the accent must mark the state worth noticing and that is now "this surface is writable"; the glyph still names the current mode (padlock/pencil), and an unwritable file (MD-15) is unchanged — always locked, toggle disabled. Side benefit, not the reason: the notes view makes a better annotate stage (§17), taking no edits and revealing no markers under a reviewer's clicks. |
| D187 | **The graph's band names, and the gutter they reserve, appear only where there is more than one band** | Reverses the explicit earlier choice to draw the name even for a single band ("these are all in `x/`" is the answer the layout exists to give). With one band the vertical axis distinguishes nothing, so the legend is one folder name repeated down the left edge of a panel it labels in whole — which the shell's breadcrumb already says — and it costs up to 92px of a 320px sidebar. Names and fills now share one condition (`> 1`), where the fill already had it, and `gutterFor` returning 0 hands that width back to the lane layout: two long-labelled notes in one folder fit one lane of the full width where they used to wrap onto two for a legend that is no longer drawn. |
| D188 | **Path completion is free: the `candidates` payload already carried the file list, so `](…)` completes with no new Python action — and the target is percent-encoded on insert** | MD-14 completed `[[`/`![[`/`[[#`/`[[note#` only, which left the most-typed target of all — the `](path)` of an ordinary markdown link or image — with nothing, even though `_candidates_payload` had been returning `notes[]` **and** `assets` (every non-note file under the root, root-relative, capped at MAX_ASSETS) since the embed index needed them. `assets` was simply unused by the page. So the whole feature is client-side string work over a payload that already exists: no new action, no second walk, no extra run per popup, and it can never offer a file the graph does not know about — the same free-once-the-index-exists property `[[` has. Candidates are written **relative to the open note's own folder** because that is `resolve_link`'s FIRST tier (MD-4), so the inserted form resolves back to that exact file with no basename ambiguity to lose to, and it doubles as a real relative path the shell can navigate (MD-4a) where no scan ran at all; `../` is emitted only where the target is genuinely above the note, since climbing to the root and back down is noise nobody would type. **Encoding on insert is load-bearing, not cosmetic:** `[x](my file.png)` is not a link to the GFM parser — the space ends the target — so an unencoded suggestion would insert markup that Live Preview cannot render and the delegated click handler never sees, i.e. the popup's own output would be the bug. The escapes therefore go in the document (per segment, so `/` and `..` survive, and parentheses too since one closes the target early, which `encodeURIComponent` leaves alone) while `displayLabel` shows the human-readable path; MD-4a already decodes on the way back. Rejected wrapping the target in `<>` instead: it is one form more for the parser and the widget to agree about, where percent-escapes were already a supported path. Assets in `![[…]]` are offered root-relative and **unshortened**, because `_link_form`'s guarantee (every offered form run through `resolve_link` itself) is a Python one with no asset equivalent in the payload, and reimplementing the shortening in JS would be the parallel-rule divergence MD-3/MD-14 exist to prevent. Finally, a failed scan is now a **row that says why** rather than a `null` return: on a mount-backed root the action refuses by design (MD-11) and every source bailed silently, which the popup drew as "no matches" — the same unknown-vs-missing lie MD-11a forbids everywhere else. The reason is cached with the failure, so that root costs one run per 5 s TTL instead of one per keystroke. |
| D189 | **A markdown toggle is only "free" where the GRAMMAR already has a rule for it — so `~~`/backtick were added and `==` was refused — and ⌘K may only trust the tree about a `Link` after re-reading the source, because the grammar calls a wikilink's brackets a `Link` too** | Four editing gaps closed at once (MD-18), and two of them are the same lesson about what the vendored grammar does and does not know. **(1) The marker set stops where the grammar stops.** `toggleWrap` was always marker-agnostic, so strikethrough (`~~`) and inline code (backtick) cost one keymap entry each and nothing else: `Strikethrough` and `InlineCode` are real nodes, so Live Preview already renders both (MD-18a) and both already hide their markers off the caret line. `==` highlight looks like the same one-line change and is not: no rule, no node, no decoration — the markers would sit bare on the rendered page, which is worse than not having the toggle, and the alternative is a regex pass in the decoration builder plus a place for it to disagree with the grammar (MD-3). Refused deliberately rather than left as an oversight. `⌘⇧X`/`⌘⇧E` were picked because Obsidian assigns no default hotkey to either command, so there was no default to match — only collisions to avoid (`markdownKeymap`, `basicSetup`'s default/search/close-brackets keymaps, and the `Prec.highest` completion keymap `autocompletion()` installs). `⌘E` was available and was NOT taken: the read-only↔editing toggle has no keyboard binding by owner call, and leaving ⌘E unbound keeps that decision visible instead of quietly spending the key on inline code. **(2) The `Link` node is not evidence that a link is there.** ⌘K on a caret inside `[a](b)` used to nest a second link, and the fix has to ask the tree which node encloses the caret — but the trap that bit the link layer once already (MD-3, MD-18a) is that the vendored grammar wraps a `[[wikilink]]`'s inner brackets in `Link`/`Image` nodes, so "an enclosing `Link`" is also true inside every wikilink and every embed. Treating those as inline links would have made ⌘K select a target that does not exist and mangle the wikilink. So `enclosingLinkTarget` applies the SAME second test the decoration builder applies before it draws a link widget — the range must also match the plain-inline-link regex — rather than inventing a second rule about what a link is; a wikilink, an embed, a reference link and a titled link all fail it and fall through to today's insert. Verified by executing the real grammar (the discipline MD-18a's probe exists for), not by reading the tree's documentation. **(3) `⌘B` with no selection now wraps the WORD under the caret**, as Obsidian does, which also makes the unwrap reachable from a bare caret; the word comes from `state.wordAt`, so there is no second definition of a word in this template, and its `null` (whitespace, empty line) is what keeps the old empty-marker-with-the-caret-inside behaviour where that is still right. **(4) `Tab` accepts an open completion** before indenting — CM's `completionKeymap` binds only `⏎` to `acceptCompletion`, so every MD-14 popup was unreachable by the key a Tab-completing habit uses — which needed `acceptCompletion` added to `entry.js` and the bundle rebuilt (MD-13: what is not re-exported is tree-shaken). **One thing every new binding forced:** the read-only facets do not filter a hand-built dispatch, and each of these commands builds its own. `editable.of(false)` keeps the keys away from the view, but that is a property of the VIEW, so a `whenWritable` wrapper checks `state.readOnly` and swallows the key — the invariant MD-1a/MD-15 state belongs in the commands, not in an argument about focus. |
| D190 | **The headings outline is the TOP SECTION of the one right sidebar, and it reads the LIVE CodeMirror document rather than the saved payload** | MD-19b. The note view had the open note's headings in three places (the `[[#` popup, the `](#` popup, `?heading=` scrolling) and no place that simply *showed* them, which is the one navigation aid a long note needs and the sidebar did not have. **Where:** a section above backlinks inside the existing `#side` panel, not a panel of its own — MD-19a allows exactly one right sidebar behind exactly one 26px toggle and MD-2a forbids the toolbar row a second control would want, so a second toggle was never on the table. Above rather than below because the ordering is by subject, not by size: the outline is about the note in front of you, backlinks and the graph are about the rest of the vault, so the thing describing the open document sits nearest it — which is also where Obsidian puts it. The only structural cost is that the first section is now the one that has to clear the floating corner cluster, so the 72px padding moved from `#links-head` to `#outline-head`. **What it reads:** `view.state.doc`, on the `docChanged` the editor already reports. The payload's `notes.headings` was the obvious source and is the wrong one: it re-parses only on save (MD-9), so an outline fed by it would lag every heading typed by up to one autosave interval — a stale outline is worse than no outline, because it is a navigation aid that would send you to the wrong place. Reading the document is also not a new idea here: `?heading=` scrolling already treats the document as the heading index (MD-4b). **No timer** — the existing `docChanged` listener is the only trigger, keeping the template poll-free (MD-17's `fs/events` stat-storm is the standing reason). Two consequences, both deliberate: the heading scan mirrors graph.py's `_mask_code` (frontmatter and fenced code masked, ATX only, trailing `#`s stripped) so a `# not a heading` inside a fenced block is absent here exactly as it is absent from every other heading surface; and `syntaxTree(state)` was **rejected** as the source despite being what the decoration builder uses, because it is parsed only as far as CM has got — on a long note the tail headings would silently be missing, and `ensureSyntaxTree` over the whole document on every keystroke is the cost this avoids. Rows scroll by **line number**, not by heading text: the row was built from that line, and re-finding it by text (as `scrollToHeading` must, since a URL param carries only text) hands the second `## Notes` to the first one — so MD-4b's caret rule moved into one `scrollToLine` both paths call. Indent is by **nesting depth computed from the levels present**, not by hash count, so a note whose headings all start at `##` is not a uniformly indented note and `#` → `###` is one step in. **No state, so no param:** the outline has no open/closed and no collapse of its own (MD-20 carries panel and depth because those are states a shared URL must reproduce; inventing an `outline=` param for a section that is always drawn when the panel is would be state for its own sake). A redraw is skipped when the heading list is unchanged — load-bearing rather than an optimisation, since rewriting the list's innerHTML on every keystroke resets its scroll position and drops the row under the pointer. It is fully functional read-only (MD-1a): rows are buttons outside the editor going through the one delegated click handler, and scrolling is not an edit. |
| D191 | **An asset ships a resolver-VALIDATED wikilink form like a note does — validated against the asset index, from the note the popup is running in — and `null` where no form resolves back, rather than a bare path that resolves elsewhere** | Closes the gap D188 documented and accepted: `![[…]]` completion inserted the asset's bare root-relative path, because `_link_form`'s run-every-candidate-through-`resolve_link` guarantee existed for notes only. Two concrete failures, both real rather than theoretical. **(a) Tier 1 runs first.** Resolution tries the linking note's own folder before the root (MD-4), so `![[img/a.png]]` written in `docs/` binds to `docs/img/a.png` the moment that file exists — which means a form validated *from the root* is not validated at all for a note in a subfolder. `_candidates_payload` therefore takes the open note (`file` is now sent with the `candidates` action) and validates every form from it. That also fixes the same latent hole for notes, and makes the sibling reachable as a bare basename where the root-validated form had to carry a folder. Where the note's own folder shadows every suffix of the target, a **`../` escape** is the last form tried — tier 1 lands back on the root path and resolves — which is a form MD-14's `](` completion already writes, so nothing new has to understand it. **(b) A collision has no form at all.** `_candidate_index` files a note under its stem, so an asset `img/a.png` and a note `img/a.png.md` share the key `img/a.png` and `_only` refuses to pick (MD-4); no form of either resolves back, and the ladder used to *return the full path anyway* — a target the resolver reads as a ghost. So `_link_form` returns **None**, the payload ships `null`, and the popup **drops the row** instead of inserting a lie. **Two fields, not one:** `link` validated against the note index and `embed` against the notes-plus-assets index, because `_resolved_links` sends a non-note embed target through the second and everything else through the first — one field could only be right for one of the two popups. Asset rows carry `embed` only (an asset outside an embed is not a wikilink target), which turns `assets` from a list of strings into a list of `{rel, embed}`; `](` path completion reads `.rel` and still computes its own relative form, as it must (MD-14). **Cost, measured rather than guessed:** this is per-candidate resolver work over up to 5000 notes **and** 5000 assets, and resolution's third tier was an O(index) `endswith` scan — on a synthetic deep tree at both caps that cost ~1.9s for the note and asset passes alone (and the notes half of it, ~0.8s, was already being paid unmeasured). So `_suffix_index` memoises that tier the way `_candidate_index` memoises the first two: the whole payload now computes in ~40ms, a ~50x improvement over what the naive version of this change would have shipped. It is a memoisation and must stay one — a key's suffixes at every `/` boundary are exactly the keys the scan would match — and an equivalence test pins both paths to the same answers, ambiguous ones included, rather than trusting the argument. Rejected **eager per-asset work without the memo** (1.9s in front of a popup is not free-once-the-index-exists), and rejected **computing the shortening in JS** for the same reason D188 did: it is the parallel-rule divergence MD-3 exists to prevent. |
| D192 | **The completion popup is themed through the EDITOR's own theme extension, not page CSS — and it has no icon column at all** | MD-14's popup shipped as stock CM6 with one page rule over it, and that rule was the whole problem: `li[aria-selected]` filled the selected row with `var(--accent)`, a full-bleed acid-yellow bar with near-black text, the loudest thing in a view whose accent is otherwise spent on links only. Two choices worth recording. **(1) A `theme` extension rather than page rules.** The appearance flip does not rebuild the editor — it dispatches `StateEffect.reconfigure` over the extension array (MD-14/MD-15's reason: a rebuild throws away unsaved edits, the caret and the scroll position) — so a theme extension in that array is reinstalled with everything else, whereas a page rule has to keep out-specifying whatever oneDark reinstates, at the one place in this template where CM's stylesheet wins on source order. It also means one theme covers both palettes: every value is a `var()` token resolved off `<html>` at paint time, so there is no dark copy of these rules to drift out of sync with the light one. The generic `.cm-editor .cm-tooltip` shell stays a page rule (the search panel is a tooltip too), which is exactly why the popup's own selectors carry `.cm-tooltip.cm-tooltip-autocomplete` — equal specificity would lose to it on order. **(2) `icons: false`.** CM renders the `type` field as a glyph and its glyph for `text` is the literal string `abc`, which appeared on every path row naming nothing. The alternative was four distinguishable marks (note / heading / image / other file) — rejected on two counts: the sources set two `type` values across four contexts, so meaningful marks would mean changing what the sources emit for a purely visual gain, and the row already says its kind without a glyph (an extension, `.md` or not; a heading by its indent and `H2` marker; an asset by its `embed` detail). `icons: false` removes the element rather than hiding it, so no empty column holds width in a popup that has a vault's worth of paths to fit. What replaces the accent bar is the **sidebar's** language, because that panel is a foot away from this popup: rows at `.bl`'s density and radius in the body font, a selected row at `--bg` on the `--bg-alt` surface with an accent left edge and 600 weight — two non-colour signals, so the selection is not colour alone — a dimmed right-hand `detail` column, and `.cm-completionMatchedText` in the accent, which is the one thing on a vault-length list that says why a row is still in it. **That last one needed code, not CSS**, and driving the live page is what found it: CM matches against `label` and then refuses to guess where those characters landed in a `displayLabel`, handing such an option an *empty* match — so every path row and every heading row in this template (all of which display a form of their own, by MD-14's encoded-insert rule) rendered with no emphasis whatsoever. The mapping is supplied through the result's own `getMatch` hook: the matched characters, found again as a subsequence of what the row shows, with a character the display deliberately drops (a heading's `#`) skipped rather than fudged — so the emphasis is on real matched text or absent, never a range slid sideways over the wrong letters. |

### preview template merged into the built-in listing (2026-07-30)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D185 | **The `preview` directory template is DELETED and its one unique feature — the split preview pane — becomes part of the built-in `_listing` view. A hand-ported copy of a shell view is a fork, and a fork drifts** | `templates/preview/template.html` was 990 lines of vanilla JS re-implementing `frontend/src/views/Listing.tsx`: the same folder listing, the same columns, the same in-folder search, the same per-filetype icons and tints — plus one thing the shell did not have, a **split pane** showing the selected entry's preview beside the list (Finder list view). Everything else about it was already worse, and by inspection rather than by prediction: no live directory watch (LS-1), no file operations (rename/delete/new — §24), no multi-select, no streaming recursive search (SR-7..SR-11 — its search filtered the one loaded page), dark-only by `color-scheme: dark` and a local token block (which is exactly why it sat on AP-12's deferred list), and its own local re-implementation of "is this row a launchable app" against D124's exported `isAppEntry`. That gap set only widens: every listing feature since the port landed in one file and not the other, and nothing made the divergence visible — the two views share no code, no test, and no type. So the direction is the one that ends the duplication rather than paying to close it: fold the **pane** into `Listing.tsx` (FS-9..FS-15), where it inherits watch, file ops, multi-select, streaming search, theming and app rows for free and cannot drift from them again, then delete the template folder and drop `preview` from the built-in `"/"` binding — now `["_listing", "graph", "zarr_aoi"]`. **Rejected: keep `preview` as a peer directory mode** — it is the status quo, and it is a second listing that a user reaches by a switcher click and then finds is missing the features the first one has; two answers to "show me this folder" is a bug report, not a choice. **Rejected: port the shell's listing features INTO the template** — that is re-writing watch, file ops, selection, the streamed search and the theme system in vanilla JS against `fused.runPython`, to arrive at a duplicate of a React view we maintain anyway; the pane is ~200 lines in `Listing.tsx` and the rest of the template is the part we are trying to stop having. Pane default is **OFF**, so a plain folder listing is byte-for-byte what it was; visibility and width persist per-folder through `lib/viewstate.ts` (localStorage) and deliberately **not** the URL — a bookmark captures a view (SB-2), and pane geometry is a workspace habit, not view state worth pinning into a shared link. *(Revised same day, owner: visibility now ALSO rides the URL as a sticky `?preview=true` — authoritative over viewstate on mount, carried across directory navigation, withheld from file targets so it cannot shadow a template's own param via D72's ancestor-climb — because "is the pane open" turned out to be exactly the thing a shared link should reproduce. **Width** stays viewstate-only for the reason stated above, which was always the stronger half of the argument: a pixel drag on one window is not a fact about the view. See FS-13.)* *(Review follow-up, same day: automated review flagged the 50 % default as no longer proportional once measured. Owner: **one-time default is intended** — the pane must not resize itself under the user. The half of the finding that WAS a bug is fixed: the measured width could be persisted to `panew` by a later toggle or a bare divider click, indistinguishable from a drag, which would freeze one window's arithmetic into the folder's saved state. A `paneSized` provenance ref now gates both writes. The same review's second finding was also real and fixed: the pane's file case picked the first unconditional template and showed the metadata card for an **all-conditional** list, while full-screen `Preview` falls back to `templates[0]` once verdicts land — so the pane claimed "no preview" for a file that opens fine full-screen. It now resolves the gates and shows the first allowed entry, failing closed to the card only on an empty list or a broken gate. See FS-11, FS-12.)* *(Second review pass, same day, two more real bugs fixed. **The 220 px minimum was on the wrong element:** FS-12 specifies it as the LIST's floor, but `PANE_MIN_W` and `.listing-pane-slot` applied it to the pane, and the 65 % fraction does not imply it — on a 600 px container 65 % leaves the list 210 px, and the deleted template's list `min-width` had not been carried over. The pane is now capped at `min(65 %, container − 220 px)` through one shared `clampPaneWidth` helper, with a CSS `min-width` on the list as the backstop for a window resize under an already-sized pane. **And a toggle-off threw away the dragged width:** `savePaneState` deleted `panew` whenever the pane went off, so closing the pane and then leaving the folder or refreshing lost a width the user had chosen. The two keys are now written independently. See FS-12, FS-13.)* *(Third review pass, same day, one low-severity bug fixed: the PT-12 sentinel filter kept a `_listing` entry (reachable only via a user registry binding it onto a file type) in the pane's candidate list, but the pane's `show()` only special-cases `_render` — an unhandled `_listing` fell into the ordinary-entry branch and built a `/render` URL from its `null` path, where full-screen `Preview` instead mounts `<Listing>` in place of the iframe. The pane has no slot for a nested `Listing`, so `_listing` is now excluded from the pane's candidates outright — it falls through to the file's next template, or the metadata card if `_listing` was the only entry. See PT-12.)* **Two accepted breaks, and they are different mechanisms** (do not conflate them): a **user registry** that still lists `"preview"` gets the ordinary dangling-name treatment of D95/CT-6 — the name resolves to no folder, is dropped from the rendered mode list, and `template_error` on the stat payload names it, with the entry showing broken (`exists:false`) in the Templates view (§23); an old **`?_mode=preview` URL or bookmark** hits PT-9's unknown-value path instead and falls back to the default mode (`_listing`) **silently, with no error at all** — `activeTemplate()` in `Preview.tsx` is a name lookup over `stat.templates` with a default fallback, and `template_error` is a registry-validation field that has nothing to say about a stale param. The user-visible outcome of the second break is the right one anyway: the URL asked for a folder preview with a pane and gets the listing that now has the pane. Owner approved 2026-07-30. *(Later revision, owner: the "one-time default is intended" call above is **overturned for the geometry, not for the provenance**. The pane's width is no longer a pixel number at all — it is a FRACTION of the split container, rendered as a percentage `flex-basis`, so it rescales with the window instead of freezing one window's arithmetic; the measuring layout effect and `PANE_FALLBACK_W` are deleted, since a percentage needs no measurement. The provenance half stands and is unchanged in spirit: only a **dragged** fraction is written to `panew`, now tracked as `sized` in the pane state rather than a ref. Old pixel values in `panew` are **ignored, not translated** — they were measured against a container this window may not have. See FS-12/FS-13.)* *(Later revision, owner, two passes, and between them **every persisted thing in this row is now gone**. **Pass one — visibility stops being a choice:** the toggle, the sticky `?preview=true` and the `pane` viewstate key are all deleted, and `shouldShowPane(containerW)` — a measurement of the split container above 700 px — decides the split by itself. One bit with three writers that had to agree, standing in for a question the layout can answer: there IS no room for two panes under 700 px, and there always is above it. The drag-to-close gesture goes with the toggle, because closing needs a way back and a width-decided split offers no reopen affordance. **Pass two — width stops being per folder:** `panew` is deleted too. Per-folder width was the "two sibling folders keep independent splits" reasoning of the original row, and it does not survive contact with use — the divider **jumped on ordinary navigation**, out of a folder that had a saved width into one that did not, snapping between the user's drag and the breakpoint default on every hop; the file preview's `_side` sidebar had used one global width all along and never had the problem. The dragged fraction is now a module-level variable in `listing/pane-store.ts` — one width for the whole DOCUMENT, no storage at all — which holds across every client-side navigation (the shell only ever `pushState`s) and is cleared by a **refresh**, deliberately: once dragged, a pane stops adapting to the window forever, and the way back must be something a user can find without being taught a gesture. `sessionStorage` would survive the refresh and defeat exactly that, so it is rejected; so is a double-click-to-reset. Undragged, the fraction now STEPS with the container (30 / 50 / 70 % at 700 / 1000 / 1440 px) instead of sitting at a flat half, so "more room, more preview" holds without anyone touching the divider. Existing `panew` and `pane` values are **purged once** at `listing/pane.ts` module init rather than translated — a width chosen for one folder is not a statement about the session — while the folder's **sort stays per folder**, which is the one thing here that really is a fact about the folder. The `parsePaneFrac` legacy-pixel rule directly above is retired with the key it read. See FS-9/FS-10/FS-12/FS-13.)* **(Revised again by #456 — the pane is not un-closable: a `_side` param on the folder URL records WHICH of its three modes is up (Preview / Claude / Git) or `_side=off`, that the user shut it, and the close chevron in the pane's header plus the reopening button in the listing's search row are exactly the two-place, chosen-by-state affordance the pass above deleted — restored as a MODE control, which is a question no measurement answers, and with the reopening half rendered while the pane is down, which is the "closing needs a way back" condition the old toggle failed. What stays deleted is the on/off toggle, `?preview=true` and the drag-to-close gesture. The width half of both passes is untouched. SPEC FS-10/FS-13.)** |

### git view — repository history scoped to the open path (2026-07-31)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D193 | **The BINDING half of this decision is OVERTURNED — `git` is FOLDER-ONLY (SPEC GT-2/GT-3): the universal `/` key and no file key, its gate refusing anything that is not a directory. Staging, stashing, committing and pushing are repository-level acts, and the working tree a file sits in is its folder's; the per-file question is answered INSIDE this view, not by a second mode — the Commits section is a log SCOPED to the open target, and selecting a commit renders the open file as of that commit through `GET /api/git/show`, resolved on read (the `history` mode that used to own the per-file question was deleted 2026-08-13; see D243). The file keys came back briefly between D235 and now, purely to make the mode reachable while a folder had no mode surface of its own — the preview pane peeks folder rows now, so that is bought properly instead. The rest of this row stands: path scoping, and `git rev-parse` as the authority.** *Original form:* **`git` is a built-in mode for BOTH directories and text-ish files, scoped to the open path, and its repo detection is `git rev-parse` in one bounded subprocess — the CLI is the authority, no marker probe, no walk** | A file explorer that will not tell you what changed in the folder you are standing in is asking you to keep a terminal open beside it. So `git` (SPEC §33) joins the universal `/` directory key and the hand-authored source/config/prose/log file keys as a `condition.py`-gated companion mode, and everything it shows is **scoped to the open path** rather than repo-wide: a repo-wide log is what `git log` is for, and the question a preview pane can answer better than a terminal is the narrow one — *what happened to THIS folder, THIS file.* Four decisions worth recording. **(1) Detection is the CLI, and a `.git` stat fast path was considered and dropped.** The owner's constraint was explicit — detect a repo without backtracking all files — and the tempting cheap answer is wrong twice over: `.git` exists only at the repository ROOT, so a probe has to ASCEND to answer a nested path (`repo/pkg/` has no `.git` of its own, and a nested path is the common case in a file explorer), and the two shapes it would then need to know about are exactly what a hand-rolled probe gets wrong — a `.git` **directory** in a clone, a `.git` **file** in a linked worktree or a submodule. `git rev-parse --is-inside-work-tree` answers all of them from any depth in one fork, and git's own ascent is O(depth) stats, never a descent; the gate itself never calls `listdir`/`scandir`/`glob` and the tests make enumeration **fatal**, so a listing added later fails rather than ships (the CT-12 rule `zarr_aoi/condition.py` documents, doubly binding here because this gate runs on every directory AND every text-ish file opened). Adding the stat probe *in front* of the CLI was rejected on the only ground that matters for a gate: it can turn a correct answer into a guess and cannot turn a wrong one into a right one, and one exec is already cheap. **(2) Mount-backed is refused BEFORE the fork, and refused again in the reader.** Same rule `graph/condition.py` makes (MD-11), through the same `shared/appenv.is_mount_backed` mechanism rather than a second copy, for a worse reason: the reader runs `git status`/`git log` on the path, and git over an rclone-NFS mount stats and lists its way through the work tree. The ordering is part of the requirement — a refusal that still forked git at the mount would have paid the cost the refusal avoids — and the check is repeated in `log.py` because a hand-written `?_mode=git` URL bypasses the switcher entirely. The gate is the UX; the module is the guarantee. **(3) One unscoped `git status`, filtered in Python — not one scoped call plus an unscoped one.** The header's clean/dirty light means the REPOSITORY (that is what the word means) while the list below means the SCOPE, and both fall out of one walk of the index this way. The filter has to reproduce git's pathspec semantics including the case a prefix test gets wrong: `--untracked-files=normal` collapses a wholly-untracked directory to `dir/`, so an entry can be an *ancestor* of the scope rather than a descendant and the scope is dirty because of it. **(4) The user's git config is deliberately left alone.** The reflex hardening is `GIT_CONFIG_GLOBAL=/dev/null` + `GIT_CONFIG_NOSYSTEM=1`, and it would break real repositories: `safe.directory` lives in the global config, and a repo the user has explicitly marked safe must keep working inside the app. So only the knobs that could corrupt parsing are overridden per command with `-c` (`core.quotepath`, `color.ui`, `log.showSignature`, `diff.noprefix`, `diff.mnemonicPrefix`), the non-interactivity is done with environment instead (`GIT_TERMINAL_PROMPT=0`, `GIT_OPTIONAL_LOCKS=0`, no pager, no askpass, no LFS smudge), and the external-diff kill is the `--no-ext-diff` **flag** rather than `-c diff.external=` — clearing the value tells git to run the empty program, a hard exit 128, which the tests caught. Everything a URL can carry is treated as hostile data: a `sha` is validated as a hex object name **before any argv is built** (so an option-shaped value cannot cause even one fork), every path rides after `--` wrapped in `:(literal)` so a filename holding `*`/`?`/`[`/a leading `:` matches itself, and argv is always a list. Nothing is unbounded: the log paginates with `limit + 1` as its own `has_more` probe, diffs **stream** through a byte cap and a line cap with a watchdog kill (captured output would buffer a hundred-megabyte diff into memory before it could be trimmed, and a manual read loop has no `timeout=`), truncation is reported in the UI, and the change list is capped too. Refusal is a **payload**, not an exception, so a non-repo / missing git / mount / empty repo renders a calm empty state and never the red traceback overlay — that overlay is a debugging affordance for a view's own bug, and "this folder has no git" is not one. File-key placement is a judgement call stated for the record: `git` goes on code, config, prose and logs, and NOT on record streams, tabular data, geo, media, 3D, archives or generated tool files — a scoped commit log over a data blob says nothing a diff can render, and `.json` in / `.jsonl` out is that same line drawn twice (`.json` is the dominant hand-authored config format; `.jsonl` is an append-only stream). The mode is **read-only forever**: nothing stages, commits, checks out or fetches, and `GIT_OPTIONAL_LOCKS=0` says as much to git. Not reusing the name `history` — `templates/history/` is the app's own sidecar/view-history inspector (§24, D96) and has nothing to do with git. *(Owner ruling, same day: **a template has no header at all** — "a template is not the same as app, it does not require any headers." A template renders inside the shell's chrome, so the shell owns identity and the template ships content; the header band is deleted and the only facts that survive are the ones that are DATA rather than chrome, folded onto the section labels — the branch (and a detached/unborn HEAD) on `UNCOMMITTED`, with the dirty light dropped because `UNCOMMITTED n` already says it and a count beats a lamp. An intermediate "one compact status line, not a display title" step was the same argument stopped halfway and is superseded. See GT-10.)* *(Review pass, same day, three real defects found by Cursor Bugbot and confirmed by the reviewer, each a case of two facts sharing one signal. **(1)** The page grows its log WINDOW rather than paging, so the server's clamp on that window made `has_more` permanently true and "load more" refetched the same rows forever; the clamp is now its own `capped` field, and it is deliberately independent of the malformed-record guard, whose own fix (count the records git emitted, not the ones that survived parsing) had the OPPOSITE failure — a premature "end of history". Two defects on one expression, opposite in direction; neither signal may be derived from the other. **(2)** Only the NEW path of a rename was scope-tested, so a file moved OUT of the open folder vanished from Uncommitted — the exact change a scoped view exists to show. Both sides are now tested and the direction is reported, which is also the concrete justification for filtering an unscoped status in Python rather than delegating to a pathspec: `git status --porcelain -- pkg` was VERIFIED to omit that rename, so git's own scoping has the same defect. **(3)** An untracked DIRECTORY (git collapses one to a single `dir/` row) has no patch, so it fell through to the vs-HEAD branch and opened a blank pane wearing the commit-oriented sentence — wrong twice, since it is not a commit and the path IS in scope. It now answers with its contents via `ls-files --others --exclude-standard` (so .gitignore is honoured rather than reimplemented), streamed under the same byte cap and entry-capped, each file clicking through to its own diff. See GT-7, GT-8, GT-9.)* |

### Revert from Claude Code's own file-history store, not from git (2026-07-31, SPEC §34)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D194 | **The undo for an agent's edits reads Claude Code's `file-history` checkpoint store — deliberately NOT git — and "revert the last change" means the newest version whose content DIFFERS from disk, not the highest `@vN`** | Claude Code already writes a full copy of every file it is about to change, at `<claude-config-dir>/file-history/<sessionId>/<sha256(abspath)[:16]>@v<N>`, and the filename key being a pure function of the absolute path is the fact the whole design rests on: a file's entire timeline is a **glob**, so enumeration needs no transcript parsing and is cheap enough to run on a view boot. **Rejected: git as the authority.** Git answers "what did the last commit say", which is a different question from "what did this file look like before the agent touched it" — and it has no answer at all in a directory that is not a repository, which is where a lot of this app's files live. The two are complementary; a git-backed template is separate work and this does not touch it. **Rejected: parse the session transcript to build the timeline.** Those `.jsonl` files reach **5 MB+** and would be read on every render — a performance trap for a panel that is often collapsed. They are consulted only for the one fact the filesystem cannot carry (FH-5), opt-in, line-prefiltered before any `json.loads`, byte-capped, and wrapped so corruption degrades to "no extra rows". **The selection rule is the load-bearing decision, it was measured rather than assumed, and it took TWO tries.** First: on a real session 6 of 13 files matched their highest `@vN` and 7 did not, because disk moved on after the last checkpoint — so "restore the highest N" is wrong about half the time and would frequently be a silent no-op that reads as a broken button. That conclusion still holds. Its replacement, *"the newest version whose content differs from disk"*, is the more instructive failure: it is right whether a checkpoint is a pre- or a post-image, it sidesteps that ambiguity cleanly, it survived TWO independent reviews that audited it for bugs rather than questioning the premise — and **it oscillates on the second press.** With `disk == v3`, v3 does not differ so the target is v2; once `disk == v2`, v3 differs and is the newest such entry, so the target is v3 again. A two-state ping-pong, for ever, with v1 unreachable at any point: "Revert last change" undid itself on the second click and the start of the chain could not be reached at all. The user found it by pressing the button twice. The fault is in what the rule ANSWERS — "which checkpoint is most recent and isn't what I have" — which is not what undo means. **Undo is POSITIONAL:** locate where disk sits in the chain, then step backwards. Over the newest-first list: position = index of the *newest* entry whose content equals disk (newest, not oldest — with duplicate content on both sides of a real checkpoint, walking from the oldest match steps straight over it); target = the first *older* entry that still differs (the differs-check stays inside the walk rather than taking `position + 1`, because identical adjacent versions are common and restoring one writes the same bytes back); `position == -1` means disk is in no checkpoint at all, so the first step back is "discard to the most recent checkpoint" — the same index that yields `unique_current`, derived once so the two cannot disagree; and no older differing entry is `at_earliest`, a terminal state that DISABLES the button rather than falling back to something newer, since falling back is precisely what made the old rule a toggle. Now `v3 → v2 → v1 → delete → disabled`: monotonic, whole chain reachable. **Rejected: add a Redo button.** None is needed — the timeline already lets the user click a newer row explicitly, and that asymmetry is correct for a control labelled "Revert last change". *(One more instance of the same class, found by pressing the button four times in the running app: `at_earliest` may only be claimed by an ENRICHED scan. The boot timeline skips the 5 MB transcripts, so it cannot see the creation boundary and reported the terminal state one step early — and the view believed it and disabled the button on a file whose remaining step back was a delete `revert_plan` would have offered. The unenriched answer is now explicitly provisional (`enriched: false`) and the click asks the plan, which always enriches and is the authority.)* Two more empirical semantics get their own handling rather than a guess: **chains are per-session and N restarts**, so two sessions both holding a `@v2` for one path is ordinary and the merged timeline orders on **mtime**, never on N (a version is the pair, surfaced as one opaque id); and a **null `backupFileName` means the file did not exist**, so reverting across that boundary is a **delete**, never a restore of empty content that would leave a zero-byte file the agent never created. **That third semantic also produced the worst bug in the review, and the fix is an identity test rather than a tighter heuristic.** The boundary rows exist only in the transcript, and they were attributed by `trackingPath` being a path-boundary SUFFIX of the target — with this module having, by its own admission, no idea what the repo root is. `src/main.py`, `README.md` and `index.ts` recur across every checkout on a disk, so an unrelated project that CREATED its own `src/main.py` injected a boundary row into this file's timeline; because those rows sort by the transcript's own timestamp it was typically the NEWEST entry, so it became the revert target and turned "Revert last change" into a **DELETE of a file the agent had never touched**, behind a confirm sheet asserting "Claude created it" about it. Reproduced against the module. The record already carries `realParentDir`, an ABSOLUTE directory, on exactly these null-backup records — so the rule is now `join(realParentDir, basename(trackingPath)) == abspath(target)`: no suffix match, no cwd guess, no cross-project collision possible. **Rejected: narrow the suffix match, or add a cwd-prefix guard** — both are heuristics over a fact that is already available exactly. A record with no usable `realParentDir` is **refused rather than guessed**: the cost of refusing is one missing row, the cost of guessing is deleting the wrong file. This also let the slug cross-product go, replaced by a glob on `projects/*/<session>.jsonl` — the slug is the cwd with separators replaced, which is lossy and was never evidence of anything. |
| D195 | **The store is touched read-only, forever; the single write is to the target, behind a mandatory confirm step, an atomic replace, and a sidecar stash — because the bytes being overwritten are frequently the only copy** | Read-only-ness is asserted as a **whole-tree byte snapshot** across every action rather than per call, so a write added under the config dir later trips a test instead of shipping: that directory is the user's live edit history and this feature is a guest in it. One accepted corollary, chosen deliberately: the user's own annotate saves are not checkpointed by anybody, so they are **not revertible** — this reverts the agent's edits and only those. The sharp hazard is FH-3's other half: since disk often matches no checkpoint, a naive restore vaporizes work with no undo at all. Three independent defences, in order. **(1)** The plan payload computes `unique_current` — "no version holds what is on disk" — and the view gates an explicit warning on it, next to byte counts, line counts and the line delta, before any write; gating rather than always showing it is what keeps it a signal instead of noise people click past. The delta is stated as *what the restore does* (lines it introduces / takes away), because that is the number a confirm step has to show; the reverse framing reads identically on symmetric edits and lies on every asymmetric one, and above a byte cap or for non-text content it degrades to net counts and **says it is inexact** rather than implying a diff nobody computed (difflib is quadratic and a timeline renders every version). **(2)** The pre-restore content is stashed into the target's own `<file>.json` sidecar under `revertStash`, through the read-merge-write `annotate.py` already owns so every unowned key round-trips — never into the Claude dir. Bounded and skipped above a byte cap or for binary, because that sidecar is a small JSON file three writers rewrite constantly and `file_history` already is a version store; a revert the user confirmed is never blocked by a stash that could not be written. Two review findings landed here, both about the stash being *trustworthy* rather than merely present. It was read in TEXT mode, so universal-newline translation rewrote CRLF and lone-CR files: `b"line one\r\nline two\r\n"` stashed as `'line one\nline two\n'` while the sibling `size` recorded 20 against 18 characters — the sidecar contradicted itself and a hand-recovery would restore bytes that were never there. Binary read + explicit decode, `size` = byte count; this package ships a `windows/` dir, so CRLF is a live case. And the SKIP DECISION was computed inside the revert, after the click, surfacing only post-hoc — so the sheet carried a permanent hedge ("a copy is kept ... unless too large or not text") under which the one genuinely unrecoverable combination, `unique_current` AND no stash, read exactly like the safe case; `os.replace` then destroyed the only copy and the user found out in the past tense, beside "Reverted to v3". The predicate (a stat, a decode, an access) now runs in the PLAN, so the sheet states the truth and escalates that combination with stronger wording plus a button that names the act ("Overwrite permanently") rather than merely "Revert". *(It briefly also required a tick-to-confirm, gated on the irreversible case only; the owner removed that as friction after using the panel, so the escalated warning is now the ONLY thing between the click and unrecoverable loss and carries real visual weight in that case. The bridge's `confirm_unique` token is a separate mechanism, derived from the plan and never from widget state — removing the tick did not touch the programmatic guard.)* Each refusal is also reported truthfully: folding an EACCES into "not UTF-8 text", or a `getsize` failure into "nothing on disk to stash" (which reads as "the file is absent"), describes a fixable machine problem as a fact about the content. **(3)** The write itself: `file_writable` reuses `_sidecar_writable`'s gate — read-only mount FIRST via `shared/appenv`, because `os.access(W_OK)` **lies** under CacheMode=full (the write lands in the VFS cache and only 403s at the async upload) — plus W_OK on the directory, which the sidecar's gate does not need and an `os.replace` does, then mkstemp + `os.replace` in the target's own directory carrying the existing mode. **Path confinement is a matching problem, not a sanitizing one:** the client's selector is an opaque id matched against the enumerated timeline and never joined into a path, so `..`, separators and absolute prefixes have nothing to traverse, and a restore may only touch a path the store already has a version for — the only target guard a module that cannot see the view can offer. **Placement:** `templates/shared/file_history.py`, stdlib-only, reached by `sys.path` — **not** `fused_render/file_history.py`, which would be unreachable from the only place that needs it, since a template child under the fused engine has no PYTHONPATH (the same reason `appenv` exists). **Every failure crosses as data**, because anything raised out of a template's `main` becomes the red traceback overlay and "no store on this machine" is not an error — though "as data" is not the same as "as one undifferentiated message": an unreadable store or session dir is reported with its path and errno rather than as "no versions for this file", since `chmod` is actionable and the collapsed form sends the user looking at the wrong thing, and an ImportError (the folder copied without `shared/`) is distinguished from any other import-time failure, which used to be reported as "helper is not available" and read as "you copied the folder wrong". **Four more findings, each on the destructive path.** *The confirm gate lived only in the page:* `revert` chose its own target from `version_id=None`, with no plan echo and no confirmation token, and the sole guard was a source grep asserting one `action: "revert"` call site — which pins today's template, not the bridge, so any future second caller inherited an unguarded file-destroying entry point. The bridge now requires the plan's `id` back (also a freshness check: a plan built against one disk state and applied against another is how a user confirms one diff and gets another) plus `confirm_unique` when the plan set `unique_current`. *`enrich` changed what the button DID, not just what it showed:* it was a parameter of `revert_plan`/`apply_revert` and the view passed its History-panel state, so the same button performed a RESTORE before the panel was expanded and a DELETE after — one click, two destructive outcomes, decided by a disclosure widget. The parameter is gone; the plan and the write always enrich, and only the boot timeline stays cheap. *A symlinked target reported success and restored nothing:* `os.replace` swaps the LINK for a regular file rather than writing through it, so the real file kept its pre-revert content while the stash captured a file that was never overwritten — refused now, deliberately rather than `realpath`-ing first, because the store's key is the sha256 of the path the VIEW opened and following the link would revert a different chain. *A read-only-mount probe that RAISED fell through to `os.access`:* the blanket `except Exception` wrapped the call as well as the import, re-opening exactly the incident the probe exists for — a malformed `FUSED_RENDER_RO_MOUNTS` would let a doomed revert report `ok: true` and surface the 403 later at an async upload this UI never sees. Narrowed to `except ImportError` for the import (a missing `appenv` still degrades to `os.access`, since there is no flag to consult) with the call itself failing CLOSED. *(The identical shape in `_sidecar_writable` is pre-existing and deliberately left alone.)* *(Three bugs found by driving the running app rather than by reading it, all fixed: the collapsed row count read the unenriched list and said "(2)" over 3 rows — it is now the row loop's own counter, absent while collapsed, with enrichment landing before the open state paints so it cannot jump; an unparseable transcript stamp made a row claim 1970, and both label and tooltip now fall silent instead; and `renderHistory()` rewrites the note element, so setting an error message and THEN re-rendering wiped it — every **failed** revert was a silent no-op, the worst possible error path for a destructive action.)* *(Review follow-up: that write-order bug was fixed as an INSTANCE, not as a class, and a reviewer found its sibling one path over — the post-revert timeline reload can fail and write its own error into the same element, which the success message then erased, so a revert that landed but left the panel stale reported unqualified success. `loadHistory` now returns its error to the caller, which composes one honest sentence. Also fixed in the same pass: a dropped version was a silent `continue`, which under the positional rule moves both where the walk starts and where it lands — every drop is now recorded with its path, reason and errno, named in the payload's note, and a drop at or newer than the chosen target makes `revert_plan` REFUSE rather than walk to a different point in history and present it as "the last change"; older drops stay harmless, because one unreadable ancient checkpoint must not cost the user their undo, and an explicitly clicked row is never blocked since the user named it. And the confirm sheet no longer renders an unparseable timestamp as "1/1/1970", mirroring the guard the row list already had.)* *(Three owner-reported UI findings from using the panel, all in the template. The dot column had **three** glyphs over three colours carrying two unrelated meanings — collapsed to two states (`●` here, `○` elsewhere) with the revert target moved onto ROW treatment, an accent stripe plus wash, since `◉` and `●` are barely distinguishable at 11px and "where are you" and "what does the button do" belong in different visual channels; `at_earliest` consequently needs no fourth marker, because no striped row already reads as "nothing to go back to". The tick-to-confirm was **removed** as friction (see D194). And the post-revert reload collapsed the panel and threw away the outcome — which I had earlier written off as pre-existing shell behaviour rather than fixing; it now persists through `sessionStorage` keyed by the target path rather than a URL param, because the URL here is explicitly the SHARING channel for a review and a disclosure state is a workspace habit, per the D185 argument for pane geometry, with the outcome read-and-clear so it cannot replay on later reloads.)* *(Fourth review round, automated — four findings, all real, all live. **(1)** `writable` is a field on a SUCCESSFUL plan rather than an error, so a read-only target still opened a destructive confirm sheet from a timeline row while the main button had correctly gone dead; closed at BOTH layers, the plan-level check being the one that closes the class, and `writable_reason` now travels with the verdict because a read-only mount, a chmod'd file and an unwritable directory are three different things to do about it. **(2)** The History toggle flipped `histOpen` and painted the open label BEFORE awaiting the enrich, so a failed fetch left an expanded label over an empty list — the third instance of the optimistic-then-await trap in this one panel, fixed by having `loadHistory` always repaint from the last good timeline so its error path resyncs every label. **(3)** `_unsafe_skips`' `-inf` floor with no target was reported as contradicting the "older skips are harmless" docstring, with the suggested remedy being to treat them as harmless. **Not implemented, and I argued against it**: with no target, a version we failed to read is exactly a candidate for the older differing entry we did not find, so every skip genuinely matters and `-inf` is the correct floor — treating them as harmless would claim terminality from a scan with a hole in it, the same class of error as the oscillating rule. What WAS broken was the message (it blamed an unidentifiable last change, which describes the other case) and that a stable refusal was only discoverable by clicking; it is now an `unconfirmed` STATE, distinct from and mutually exclusive with `at_earliest`, that disables the button and shows its reason, with one `_selection` helper feeding both the timeline and the plan so they cannot disagree. **(4)** The stored outcome was cleared only on boot, so a reload that never arrived left it to reappear as fresh news on the next open, and a failing refresh on a dying page could write it back after a newer boot had consumed it — restructured into a one-writer/two-clearing-readers carry slot rather than given a fourth guard, since this mechanism had by then produced three separate defects.)* *(Fifth round, two findings, and both were the SAME root cause as B1 — a guard living below the layer that decides whether to OFFER the action — which is why this one is a seam and not a third point fix. **N1:** with blocking skips but a live target, `timeline` kept publishing that target, the panel struck the row as "this is what Revert does" and left the button enabled, and every press hit `revert_plan`'s refusal; a reload left the same live control, so it repeated. This is the half of B3 that the earlier scoping missed — only the no-target branch had been converted to a visible state. **N2:** a symlink was refused in `apply_revert` alone, so the sheet opened on a target that could not succeed and `_stash` ran first, reading THROUGH the link — a failed revert that had already put the wrong file's content into `revertStash`. **The fix is one authority**, `offer_reason`, answering "may this be offered and if not why", consulted by the panel, the rows, the sheet and the bridge: `timeline` now publishes `offer` and `offer_reason`, `revert` names a row only when that row is actionable, `writable_reason` absorbs the symlink and directory refusals (they are answers to "can this be reverted", and keeping them elsewhere is exactly what hid them), and the bridge re-checks before `_stash` — which has to be GATED rather than followed by a raise, since it runs first by design. Two asymmetries kept deliberately: an explicitly chosen version bypasses the automatic refusals but not writability, and FH-3's unenriched terminality stays provisional, which is why `offer` is its own field rather than inferred from `revert`.)* |

### app cloning — a deployed page's source, back on the viewer's machine (2026-07-30)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D196 | **A clone of a deployed page is a DOWNLOAD, not a git clone: no identity, so no update-in-place — and the publisher's consent is mount state, stated explicitly on every deploy** | Two shapes were available for the viewer flow. Reusing §26's GitHub clone path would have inherited its update-in-place branch, which is only safe because `.git` gives it identity: it can prove a folder is a clone of *this* repo before writing into it. An export archive carries no provenance we can verify, so the same code with a zip in place of a remote would happily overwrite a user's edited copy on a second clone. Hence a separate module and separate routes, every clone landing in a fresh `unique_dir` folder, and the two flows sharing only the confirm-step shape. On the publisher side the flag lives on the MOUNT, not the artifact, so turning cloning off is one record write and never needs the bundle a publisher may no longer be able to reproduce; the dialog therefore states the posture explicitly in both directions, because the CLI's preserve-on-omit (right for a CLI) would have made unticking the box silently do nothing. The unpack hardening moved to `zip_import.py` rather than being re-written for clones: the template import already had staging, symlink refusal, path-escape refusal and decompressed-size caps, and a second extractor is how a hardened path and an unhardened one end up side by side. |
| D197 | **A clone keeps the page's files and nothing else, and the app it clones is named after the page — not after the temp dir the export happened to use** | Two names leaked out of implementation details and into what the user sees. The bundle's `manifest.json` was written into the cloned folder as a `.fused-render-bundle.json` dotfile, on the argument that a later `export` could then reproduce the same bundle — but nothing ever read it back (`export_page` recomputes the manifest from the page's own files), so it was write-only clutter in the user's folder, one more path the confirm step had to predict, and a *second* move on the commit path that needed its own rollback to avoid reporting failure with a clone already on disk. Dropping it makes the commit a single rename and the preview's list exactly the page's files. Separately, `share create`/`repoint` derive the app name from the source directory they are handed, and what this module hands them is `tempfile.mkdtemp(prefix="fused-render-deploy-")` — so pages published as `fused-render-deploy-pabxq903`, which is the name the deployments list showed, the name the clone inventory reported, and therefore the name a viewer's clone folder inherited. `--name` is now stated explicitly from the page (its stem, or its folder when the stem names nothing, sanitized), on repoint as well as create, since repoint re-derives it too. The clone's FOLDER is likewise named after the **link** now, falling back to the app name: a deployed page's URL ends in its token, and when the publisher chose that token (`share create --token my-solar-map`) it is the best name available — it is what the link the user followed says, and it survives redeploys, whereas the app name describes the source file and two pages can share it. An opaque CSPRNG token (26+ chars of lowercased base32) names nothing, so the app name wins there; the shape check that distinguishes them mirrors fused's `mounts.is_opaque_token` and is copied rather than imported, since `fused` is an optional extra and cloning must work with no CLI installed. It decides a folder name and nothing else — never a gate. The confirm button lost the folder name for the same reason: "Clone to local" names the action, and the body right above it already says where the files land. |
| D199 | **Pasted and dropped media go through a new multipart upload endpoint, land in a shared `assets/` the template creates, and are named by timestamp — with paste and drop sharing one helper** | Four choices, one feature (SPEC MD-23). **(a) Multipart, not base64 in JSON.** Base64 inflates a payload by a third: irrelevant for a screenshot, very relevant for a pasted video. `python-multipart` is already a core dependency and `templates_api.api_import_templates` is an existing `UploadFile` precedent, so this adds neither a dependency nor a pattern. **(b) A new `POST /api/fs/upload`, not a binary mode on `/api/fs/write`.** `_fs_write`'s contract is "content is a string", and its callers depend on the optimistic mtime lock and the create-exclusive 409 — neither of which means anything for a freshly pasted blob. Overloading it would fork that function down the middle; a sibling route reuses the guard sequence verbatim (X-Fused, the mount-backed/`mount_read_only` branch before any kernel probe, `_writable`, the RO-2 `readonly` 403) without touching the text path. It also needed `fused.uploadFile` **and** `fused.mkdir` on the runtime surface: the runtime had no directory-create at all, so a template could not make the folder it was about to upload into. **(c) The template creates `assets/`, the endpoint does not.** Keeps the upload route's contract identical to `_fs_write`, which deliberately never creates intermediate directories. The cost is one extra `mkdir` round-trip per paste whose `exists` 409 is expected and ignored. **(d) Timestamp names in a shared `assets/`, and one helper for both gestures.** Timestamps never collide, so a paste needs no directory scan and no prompt; a shared folder keeps note directories readable, and the trade-off — deleting a note leaves its media behind — matches Obsidian and belongs to an unreferenced-media sweep, not here. Paste and drop differ only in where the files come from and where the text lands, so `insertMediaFiles(view, files, pos)` takes the position as a parameter rather than reading the selection; two copies of ensure-dir/upload/insert would drift the moment either gained error handling. |
| D200 | **Bare URLs are decorated where they stand — a mark that becomes an anchor — rather than rewritten into `[url](url)`** | Three choices, one gap (SPEC MD-24). **(a) Display-only, never a document rewrite.** The obvious alternative — convert a pasted or typed URL into `[url](url)` — changes what the file says in order to change how it looks, and it only ever helps URLs pasted *after* the feature ships. Decorating at render time instead leaves every note byte-identical and repairs the URLs already sitting in notes people wrote months ago. It also keeps the one rewrite this template does do (⌘K, and paste-over-a-selection producing `[selected](url)`) as the only one, so "does typing change my file?" has a single answer. **(b) A mark, not a replacement widget.** Every other link surface here is a widget because it has markup to hide and a display text that differs from its source. A bare URL has neither: its display text already *is* its target, so a widget would swap those characters for an element rendering the identical characters — no visual gain, and the caret can no longer land inside the URL to edit it. `Decoration.mark` takes `tagName` and `attributes`, so the range becomes a real `<a class="lp-link">` without replacing anything. The angle form `<https://…>` does have markup, and its `<`/`>` therefore hide and reveal under the ordinary caret rule — the two shapes differ only in that. **(c) The grammar answers "is this a URL?", not a regex.** `URL` and `Autolink` are already nodes (the vendored GFM extension emits them), so this is two branches in the existing tree walk rather than a third regex pass with its own idea of where a URL ends and its own collision surface against the wikilink pass. The code guard rides along free — the grammar emits no `URL` node inside a fence or a code span — and where a guard is still written it reuses the narrow `CODE_NODES` set, because the last bespoke "is this code?" list silently stopped every wikilink from rendering (MD-18a). |
| D201 | **Ordered-list renumbering is a transaction filter over text, scoped to the edited list, anchored on its first item** | Four choices, one gap (SPEC MD-25). **(a) A filter, not more key bindings.** `markdownKeymap` renumbers on Enter because `insertNewlineContinueMarkup` calls the grammar package's own `renumberList` — which is not exported, so it cannot be reused, and every other gesture (Backspace, select-and-delete, ⌘X, ⌘⇧K, paste) was left stale. Binding those individually means enumerating gestures forever and still missing the next one; `EditorState.transactionFilter` sees every edit whatever produced it. **(b) Text, not the syntax tree.** A filter runs before the new state exists, and `tr.state` would force one to be built and then possibly discarded on every keystroke. The shape being matched — a marker at the start of a line — is one the tree adds nothing to. The single thing text alone gets wrong is a fenced block, where digits-then-dot is not a list, so fences are tracked in an explicit pass. **(c) Scoped to the list the edit landed in, not the document.** Renumbering every list on every keystroke would sweep a deliberately odd list two paragraphs away into an undo step the user never made. **(d) The first item's number anchors the sequence.** `3.` `4.` `5.` — a list resumed after a paragraph — is textually identical to `1.` `2.` `3.` with its head deleted, so preserving a deliberate start and normalising to 1 cannot both be had. Anchoring is what `markdownKeymap`'s Enter path already does, which makes it the only rule under which Enter and Backspace agree with each other. Undo, redo and programmatic dispatches are excluded through a user-event allow-list (`input`/`delete`/`move`) rather than by naming undo, and the renumbering is appended to the *same* transaction so one undo takes back the edit and its numbering together. |
| D202 | **Vertical spacing is carried by unconditional line decorations, padded at a block's two edges** | Three choices, one gap (SPEC MD-26). **(a) Line decorations, because inline marks cannot hold a margin.** The heading scale was already there as `.lp-h1`…`.lp-h6`, but those are `Decoration.mark` over a range of the source, and a vertical margin on an inline span does nothing. `Decoration.line` is the only place it can live, and `lp-fence-line` was already the precedent for reaching it. **(b) Unconditional, never gated on the caret.** This is the constraint that shapes the whole feature. The editor un-renders the line the caret is on, so if spacing differed between a line's rendered and its caret-revealed state, the document would visibly jump under the cursor on every arrow-down — the worst possible bug in a typography change, because it only appears while editing and never in a screenshot. The heading *size* mark is already applied regardless of the caret for exactly this reason, so the spacing simply matches it. **(c) Edges, not every line.** A multi-line block gets `-top`/`-bot` classes on its first and last line only. Padding every line would render a fence as a stack of separated tinted panels, and margin on every list row would space the rows apart from each other — the opposite of a list reading as one block. A nested list is skipped because it lies inside its parent's range and would put a gap in the middle. Bottom margins are kept small throughout: markdown's blank separator line is already a full line-height, so these rules add only what markdown has no way to express — the space above a heading, and the gap around a block. |
| D203 | **The OS clipboard is bridged through the LOCAL BACKEND, not the web Clipboard API — copy-out and paste-in both carry absolute paths, cut is excluded, and the Linux write serves one file-manager family** | A webview cannot read or write the native file flavors Finder/Explorer/Nautilus use; `clipboardData.files` would upload every byte and drops folders entirely. The Python server already runs on the user's own machine (it binds `127.0.0.1`, D2/D3), so it can be the bridge the webview lacks — and once real absolute paths cross, the *existing* server-side `_fs_copy` does the work: instant at any file size, folders included, no bytes through the browser. **The OS clipboard is a second writer of the EXISTING module clipboard store** rather than a parallel "external clipboard" state, so all four Paste sites, the `disabled: !clipboard` checks and the cut-dimming keep working untouched; rejected threading an `external` flag through `Clipboard` that every consumer must then branch on. **The change token is a content fingerprint, not a native change counter:** macOS has `changeCount` and Windows `GetClipboardSequenceNumber`, but Linux has no analog, so hashing the ordered path list is the only token all three platforms can produce — and it is fully testable with no OS involved. It tracks **last seen**, not last written, which is what stops a stale system clipboard from clobbering a pending in-app cut on every focus change; the cost is that re-copying an identical selection in the file manager is not seen as a new event, which is harmless because adopting the same paths is idempotent. **Reconcile on focus, not on a poll** — returning to the app is the only moment the clipboard can have changed from the user's point of view, and `useRefreshOnReturn` exists for exactly this posture; a copy made while the app is already focused is missed until the next focus change, and a read on Paste-menu-open can be added later if that bites. **Cut is excluded**, in both directions: no platform exposes a reliable cut-vs-copy flag on read, so honouring one would mean deleting the user's source files on a guess. **The Linux write covers one file-manager family and that is not engineerable away** — there is no OS-owned clipboard on X11/Wayland, GNOME and KDE disagree on the format, and `xclip`/`wl-copy` publish one target per invocation with a second call replacing the first; dispatching on `XDG_CURRENT_DESKTOP` serves the session actually in use, reading tries both so pasting *in* works everywhere, and a resident GTK/Qt owner process offering both targets at once is the documented upgrade. **No new dependencies** (pyobjc is already in the `[app]` extra, `ctypes` is stdlib, Linux detects its tools rather than requiring them), and **every failure mode is the same one**: absent pyobjc, a hardened sandbox, no `xclip`, a dead request — all `supported: false`, a normal 200, leaving today's in-app-only behaviour intact rather than erroring in the UI. Cross-platform *testability* was a design constraint, not an afterthought: the Windows `DROPFILES` construction and slash normalisation, and the whole Linux backend behind faked `which`/`subprocess`, are pure functions tested on every platform, so only the real OS round-trips are platform-gated. |

### macOS mount stability — rclone NFS handle cache (2026-08-03)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D198 | **The macOS mount's fragility was rclone's NFS handle cache, not the kernel client: the daemon now runs `RCLONE_NFS_CACHE_TYPE=disk` (env, NOT argv), and the kernel options gain `intr`/`nolocks` while `retrans` is dropped** | Reported symptom, long-standing: *any* recursive walk of `~/.fused-render` — a global editor/agent search, ripgrep, Spotlight, or one flat million-key S3 prefix — permanently killed every mount, healed only by a force-unmount. Every previous mitigation treated it as a kernel-NFS-client or S3-latency problem (D102's rc-routed listings, D156's walk refusals, the HTTP serves) and each helped only by keeping *our* code off the mount; external walkers still killed it. The actual cause is server-side and has nothing to do with latency: go-nfs's default in-memory `CachingHandler` (`helpers/cachinghandler.go`, v0.0.4 = what rclone v1.74.4 pins) scans EVERY cached handle on every handle resolution — `for _, k := range c.activeHandles.Keys()` with a `Peek`+`hasPrefix` per key, under the handler lock, on essentially every NFS RPC. `ToHandle` mints one handle per directory entry and READDIRPLUS calls it per entry, so a walk drives N toward `--nfs-cache-handle-limit` (**default 1_000_000**); the LRU has no TTL and shrinks only by eviction at the limit, so the mount NEVER recovers — exactly the observed signature (permanent, not time-dependent). Measured on a LOCAL 50k-file tree so no S3 latency was involved: per-stat 0.12ms at 0 handles -> 3.58ms at 50k (linear, 29x), and re-listing one unchanged 500-entry directory 80ms -> 1801ms; under `disk` both stayed FLAT (0.12ms, ~63ms). Extrapolated to the 1M limit, `memory` reaches ~70ms per stat, i.e. ~36s for one 500-entry listing. rclone's own `diskHandler.FromHandle` is a sha256 of the path plus one small file read, no scan. Must be set in the ENVIRONMENT: `--nfs-cache-type`/`--nfs-cache-dir` are registered on the `serve nfs`/`nfsmount` command flag sets, so `rcd` rejects them as an unknown flag and refuses to start — pinned by a test, since getting this wrong breaks mounts entirely. Bonus: disk handles derive from the path, so they survive `restart_rcd` instead of handing live clients stale handles. The cache is reset per spawn (the disk handler never evicts, and a new daemon cannot honour handles it did not mint). Kernel side, `intr` makes a process stuck on a slow mount KILLABLE (macOS defaults to nointr — the unkillable beachballed ripgrep) and `nolocks` closes a latent hang: go-nfs registers no NLM program, yet macOS enables remote locks by default, and DuckDB/SQLite opening a mounted file takes a real lock. **Rejected:** `soft` — it converts a stall into a mid-read EIO that DuckDB/rasterio surface as file corruption on a merely-slow read, and on a read-only mount it silently implies `deadtimeout=60`, force-unmounting the mount out from under the app; the stalls it appeared to fix were this handle cache. `retrans=2` was **inert** (mount_nfs(8) defines it for soft mounts; ours is hard) and is removed rather than left reading as live tuning. `timeo=600` kept, with the units recorded: tenths of a second, so 60s. **Also rejected — FUSE-T** (evaluated same day): technically viable (the bundled rclone already ships `cmount` and probes `/usr/local/lib/libfuse-t.dylib`, and is signed with `disable-library-validation`; the rc `mountType` is `cmount`, not `mount`), but its licence is free for NON-COMMERCIAL use only and reserves bundling with commercial software, so it could only ever be user-installed-and-detected like WinFsp (D132). It is also not clearly a fix: its default backend is still an NFSv4 loopback mount, fuse-t issue #105 reports our exact symptom shape (every syscall ~30ms after a 300k-file walk, avoided only with `-o noattrcache`), the FSKit backend that would escape NFS is Experimental + needs a manual per-user System Settings enable + reports 0 bytes free (blocking writes), and for the eventual rw-S3 goal it is actively worse than nfsmount (silent write failures under `-o ro`, mtime-on-read triggering full re-uploads, AppleDouble `._*` sidecars uploaded into the bucket) |

### execution engine — the pref's default flips to fused (2026-08-03)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D204 | **The `engine` pref's default flips from `builtin` to fused-when-available — knowingly re-accepting the install-order-dependent default D70 and D80 both rejected** | `prefs.selected_engine()`'s unset/unknown value reads as `"fused"` instead of `"builtin"`. Nothing else in the resolution changes, and that is what makes it safe: `effective_engine()` already ANDs the selection with a live availability probe, so "fused by default" resolves to fused *iff* the package is importable and to builtin otherwise — a default that depends on the environment, never one that breaks in it. A stored `"builtin"` still **pins** builtin: the default flipped, the choice did not, and an importable `fused` overriding a user who chose builtin is exactly D70's surprise. `FUSED_RENDER_ENGINE` semantics are untouched — still the process override that beats the pref, still validated once at startup by `server._forced_engine` (`=fused` still fails loudly when missing). The Preferences radio stays **two-way**; the availability AND already is what an `auto` value would have meant, so a third stored value would only be a second spelling of the default. The page states the new default and, for the now-common unset-pref-without-the-package case, keeps saying "Currently running: Local (built-in) — falling back to Local while the fused package is unavailable" beside the checked-but-disabled Fused radio. `tests/conftest.py` pins `FUSED_RENDER_ENGINE=builtin` ambiently, so the suite's incidental `/api/run` coverage does not change runner depending on whether the optional extra is installed. | Owner call 2026-08-03: the fused engine's UX has improved substantially since the default was set, so it should be what a fresh install gets. This **reverses** two recorded calls and is recorded as a reversal rather than left to look like drift: **D70** flipped `FUSED_RENDER_ENGINE`'s default from `auto` to `builtin`, and **D80** then chose `builtin` for this pref, both on the same reasoning — an install-order-dependent default was surprising, because the same fused-render install could run pages under two different contracts depending on what else shared the environment. That cost is re-accepted, not forgotten: the owner's judgement is that with the fused engine's UX where it now is, and with **both engines running locally** (PF-3 — neither touches a named Fused environment), the remaining contract difference is a much smaller surprise than it was in July, and the better default is worth it. Rejected an `"auto"` pref value explicitly (the owner's call): the resolver's availability AND already provides that behaviour, and a stored `auto` would make two values mean the same thing while giving the radio a state that is not a choice. |

### Sidecar relocated under home_dir() (2026-07-31)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D205 | **The per-file sidecar (`claudeSessions`/`bookmarkHistory`/`lastSession`/`comments`/`docs`/`excel`/`usd`/`latex`/`slides` keys) moves from a sibling of the target file to `home_dir()/sidecar/<mapped path>.json` — REVERSING D83's explicit "not under FUSED_RENDER_HOME" invariant** | `shell/storage.py`'s new `sidecar_path(file)` (mirrored, stdlib-only, in `templates/shared/appenv.py` for template subprocesses per PY-15) maps an absolute path into a subtree under `home_dir()/sidecar/`: a POSIX path just drops its leading `/`, a Windows drive letter becomes its own single-letter folder (`C:\Users\...` → `sidecar/C/Users/...`), a UNC share nests under `sidecar/unc/<server>/<share>/...`. Built on `ntpath.splitdrive` (not a live `os.path`) so the classifier is pure and testable on any host, the same discipline `_view_url_codec.py` uses. Case is preserved exactly — never folded — so two paths that differ only in case can't collide on a case-sensitive filesystem. All 6 Python `_sidecar_path` definitions (`server/session.py`, `shell/bookmarks.py`, `templates/claude/agent.py`, `templates/annotate/annotate.py`, `templates/slides/slides.py`, `templates/usd/reader.py`) now delegate to it; the JS side gets one shared implementation in `static/runtime.js` (`fused.sidecarPath`/`fused.targetPathFromSidecarPath`, async — the mapping needs a server-provided `sidecar_root` fetched once via `/api/config`) instead of five independent `file + ".json"` copies. `history/template.html`'s sidecar↔target role resolution (HV-3) is now exact (`targetPathFromSidecarPath` either resolves or doesn't) instead of a same-directory `<name>.<ext>.json` shape guess. **Structural side effect, not a separate fix:** every mount-read-only gate that existed ONLY because the sidecar sat on the source file's own (possibly remote, possibly read-only) filesystem — `session.py`'s `_session_put`, `bookmarks.py`'s history mirror, `agent.py`'s `_mount_read_only`, `annotate.py`/`slides.py`/`usd/reader.py`'s `_sidecar_writable` — is now unreachable and removed: the sidecar is always under the app's own writable home dir, so the "sidecar-write incident" (CacheMode=full 403-looping a doomed PutObject) this repo names repeatedly can no longer happen, and reading/writing a sidecar for a mounted file no longer costs a network stat. `_sidecar_writable` (annotate/slides/usd) now answers via a new `appenv.nearest_existing_dir` (walk up to the nearest ancestor that exists, since the sidecar's subtree usually doesn't exist yet) rather than checking the target's own parent dir | Owner call 2026-07-31, no migration (old co-located `<file>.json` sidecars are simply abandoned — "don't care if existing sidecars stop working"). Explicitly accepted trade-offs, discussed and decided before implementation: (1) **no GC for orphaned sidecars** — today, deleting/moving a project folder frees its sidecars for free (co-located); after this, a sidecar survives under `home_dir()/sidecar/` forever with nothing to reclaim it, unbounded growth over time — owner chose to skip building cleanup for now rather than block on it. (2) **read-only source files now get sidecars** — previously an explicit UX gate; now a structural side effect of the mount fix, and the owner took it (recommended default). (3) **exact case preservation over folding to lowercase** — the owner's own example used lowercase (`sidecar/users/vasu/...`), but exact-case was chosen to avoid case-fold collisions on case-sensitive filesystems; not implemented as literally typed. (4) **portability regression accepted implicitly**: zipping/copying a folder no longer carries its sidecar data along, a reversal of the co-location rationale in `docs/CLAUDE-TEMPLATE-POC.md`'s original design notes — not re-litigated, since the owner's stated goal was specifically to stop sidecars from living in the user's own directories. Rejected: server-side templating of the sidecar root into each template's HTML at render time (adds a second injection mechanism where `/api/config` — already fetched once at boot for `mounts_root`/`calls_dir` — covers it for free); storing a `_source` field inside the sidecar JSON for the history view's reverse lookup (unnecessary — the forward mapping is mechanically invertible per-host, so no writer needs to remember to stamp it) *(Follow-up, 2026-08-04: `templates/claude_split/agent.py` — a fork of `templates/claude/agent.py` added on `main` in parallel with this decision, so it never got the migration — moves onto `appenv.sidecar_path` too, making it 7 Python `_sidecar_path` definitions, not 6. Its folder-target special case (a `.claude-split.json` dotfile written INSIDE the target directory, to avoid colliding with a sibling `<folder>.json` under the old co-located scheme) is dropped: that collision risk doesn't exist once the sidecar lives in its own tree under `home_dir()`, so a directory target now resolves through the exact same function as a file target. Its `_mount_read_only` gate is removed for the same structural reason as the other five. `app_git.py`'s `_GITIGNORE` (`*.html.json`/`.claude-split.json`) and claude_split's own mirrored `.git/info/exclude` injection are deliberately left in place, now purely as legacy defense — a repo from before this decision may still carry an old co-located sidecar file, and `git add -A` must still not sweep it into history; no fresh repo will ever grow one of these files again.)* *(Follow-up, 2026-08-04: `fused.sidecarPath`/`fused.targetPathFromSidecarPath` are pulled OFF `window.fused` entirely — owner's call, on review. `window.fused` is `docs/EXPORT.md`'s documented portable-subset contract, kept forever and mirrored by the separately-maintained hosted stub (the `fused` wheel's own copy of `runtime.js`) — every function added there is a promise to user-authored templates, not just to the 6 built-in ones that actually call it. Worse than an undocumented hosted gap: the sidecar is the exact JSON file five other built-in features read-merge-write into (`claudeSessions`/`bookmarkHistory`/`comments`/...), so a third-party template calling `writeFile` on a path it got from a public `sidecarPath()` would clobber that file wholesale instead of merging — a footgun the function's signature gives no hint of. Now `window._fusedSidecarPath`/`window._fusedTargetPathFromSidecarPath` — plain globals, not nested under `fused` — reachable by built-in templates only because they execute in the same window this script is injected into; absent from the hosted stub, and not meant to be added there. Every built-in caller (`docs`/`excel`/`latex`/`usd`/`annotate`/`history` templates) updated to the new names.)* |
| D206 | **claude_split's chat gets two live-app-state channels — a `<live-app-state>` block pushed with every message and an `app_state` MCP tool it can pull mid-turn — and the DOM outline reuses the ANNOTATION layer's element identifiers rather than minting its own** | `templates/claude_split/template.html` snapshots the framed app at send time (`appStateSnapshot`): entry path, title, url, the app's own params, a depth-4 / 60-node structural outline of `<body>` (tag, id, class, own text ≤120 chars — never `outerHTML`), and a 50-entry parent-side ring buffer of `console.error`/`warn`, uncaught errors, unhandled rejections and `reload` markers. `appStateBlock` wraps it in a `<live-app-state>` block; `agent.py`'s `_strip_app_state` peels that back out of everything user-facing (meta.json, the re-attach probe, a restored transcript) so the block is model-only. The pull half is a second tool on the existing approval MCP server, parked in a SIBLING `appstate/` directory (never `perm/` — a snapshot answer is not a verdict), answered from the poll loop with a snapshot taken THEN, and self-bounding on a timeout so an absent page never wedges `claude`. Both blocks that can ride a message (this one and #372's annotation preamble) are composed in ONE function (`composeOutgoing`) and removed by ONE function (`stripBlocks`), because `stripAnnBlock` recognises its preamble only at position zero: an app-state block in front of it silently no-ops that strip and leaks annotation JSON into the transcript, and one placed after the annotation fence leaks itself instead — neither order works with a strip that knows only one block. Outline nodes carry `path` from **`annPathOf`**, the annotation layer's own `tag:nth-of-type` builder, plus `id` — one implementation, called from both places, with a test asserting an outline path round-trips through `annResolve` back to the same element. The `stop · esc` control (button on the working line + Escape, `runEnding` as a pure decision) is ported from the `claude` template; Escape's precedence is a pure `escapeAction(composerOpen, runId)` — an open annotation composer wins over killing the run. | The agent was editing a page it could not look at: whether its change rendered, whether the console filled with errors, whether the page went blank were all unanswerable without asking the user to describe their own screen. **The frame-descent layer this feature originally needed is DROPPED, not merely unused** — it existed because the pane framed `/embed/<app>` (fused-render's React shell, with the app nested one iframe deeper), so reaching `leftframe.contentDocument` described the viewer's chrome and captured none of the app's logging; **#372** (element-level annotations) reframed the pane as `/render?path=<entry>`, the raw rendered document, which makes the frame's own `contentDocument` the app's — so `reachableFrames`/`hasRuntime`/`appFrameOf`/`resolveAppWindow` and the 100 ms `setInterval` they needed are deleted and the instrumentation hangs off the same `load` event #372 already uses to rewire annotations (recorded explicitly so the deletion does not read as drift). Sharing #372's identifier scheme is the deliberate half of that inheritance: the element a user pinned and the node the agent reads in the outline are then literally the same name, so it can act on a pin without guessing, and a duplicated path builder is what D146 forbids. Snapshot is TEXT ONLY for #372's stated reason — structured anchors, no screenshots or raw coordinates, so Claude can map each note straight back to the app's HTML source. `--allowed-tools mcp__fused_approvals__app_state` pre-approves the pull channel and nothing else: the card would read "may Claude look at the screen you are looking at", which is a non-decision the user would answer once per edit, and a prompt with no decision in it trains people to click through prompts that do have one — transparency comes instead from a one-line `read app state` note in the log (and `app state attached` under each pushed turn), and the tool call is genuinely BLOCKED until the page answers, so nothing happens silently. **Accepted costs, stated plainly:** (1) the rendered page's own text — up to ~60 nodes × 120 chars, plus title and 50 console lines — now flows into the prompt on **every turn**; it is local-only (the page is already on screen, no file is read to get it) and capped, but it is a real, permanent increase in what each message carries, and a snapshot with nothing in it returns `null` so a turn with nothing to say looks exactly like a pre-feature turn. (2) A residual logging gap: the `load` hook cannot beat the app's FIRST inline script, so logging from there is still missed — the DOM outline shows what that script did or failed to do, and everything from the next tick onwards is captured. Rejected: a permission card for `app_state` (see above); clearing the console buffer on reload ("these three errors were there before, this one appeared after your edit" is the buffer's whole value, and clearing destroys it); wrapping `console.log` (50 ring slots spent on an app's chatter would push out the errors they exist for); quoting `script`/`style` bodies in the outline (source the agent can read properly, and a clipped 120 chars of it is a misleading fragment). |
| D207 | **claude_split annotations gain a PNG crop per note, attached BY FILE PATH — REVISING, not reverting, #372's "element-level, never screenshots or raw coordinates"** | On send, `templates/claude_split/template.html` rasterises the visible left pane ONCE (`shotPane`: clone the framed `<body>`, copy every `getComputedStyle` property onto the clone's inline `style` because `<style>` rules do not travel with a serialized element, replace each `<canvas>` with an `<img>` of its own `toDataURL` pixels because a `<canvas>` serializes as an empty element, `XMLSerializer` → `<svg><foreignObject>` → `Image` → `drawImage`), then cuts each pending note's crop out of that one bitmap at the rect its pin already uses (`annStageRect` → `shotCropRect`). Each crop is uploaded via `fused.uploadFile` (the binary-safe path — `fused.writeFile` is UTF-8 only and mangles a PNG) into `agent.py`'s new `SHOTS` directory, a SIBLING of `runs` under the template's own 0700 temp root, and the annotation JSON gains `shot` (absolute path, or `null`) and `shotNote` (why it is null). `agent.py`'s spawn line pre-approves exactly that directory with `Read(//<shots>/**)` beside the existing `app_state` tool. Caps mirror the `APP_STATE_MAX_*` style: `SHOT_MAX_EDGE` 640px longest edge, `SHOT_MAX_BYTES` 300KB (halved and re-encoded while over — PNG has no quality dial), `SHOT_MAX_COUNT` 6 per send, `SHOT_MIN_AREA` 64px², `SHOT_TIMEOUT_MS` 6s for the whole capture; crops are pruned at 12h / 200 files. The pull channel's `null`-snapshot handling is fixed in the same work (`appStatePull` + a bounded retry): the push channel's "nothing worth a block" was being handed to `agent.py` as `"null"` and recorded as a permanent "could not read the app's state", which a live-reload — the thing an edit triggers — provoked routinely. | **This REVISES D206/#372 rather than reverting it.** The structured anchors stay primary and authoritative: `anchorId`/`anchorPath`/`tag`/`text` are still what Claude edits FROM and still map onto the app's HTML source, `iu`/`iv` stay FRACTIONAL (never raw pixels), and the crop is an *attachment to* an anchor, never a substitute for one — it exists for the claims a path cannot carry ("this is misaligned", "this chart drew nothing", "the wrong colour"), which #372's text-only model left the user describing in prose. **A file path with a lazy `Read` beats an inline image** because it costs ZERO tokens until the agent decides the visual matters; an inline base64 block (or an MCP image content block through `app_state`) is paid on every turn that carries an annotation whether or not the pixels were ever the question, and most annotations are answerable from the anchor alone. Pre-approving `Read` of one directory is what keeps "choose to look" free of a permission card the user would answer once per screenshot — the same argument D206 made for `app_state`, and the double-slash rule form was verified against claude 2.1.221 rather than assumed (a single-slash `Read(/abs/**)` matches nothing, silently). **One pane capture cropped N times, never N standalone subtree serializations:** a flex child serialized alone renders without its parent's layout, collapsing to its content width, so it stops looking like the thing the user pointed at — the ancestor context IS the picture. It also makes the intrinsic (canvas/img) case fall out of the same code path instead of needing a second one. **Known gap, stated rather than hidden: blank WebGL.** maplibre and deck.gl create their context with `preserveDrawingBuffer: false`, so `toDataURL` on their canvas reads back fully transparent; any annotation that is or contains such a canvas gets `shot: null` and a `shotNote` naming the cause and saying the app is very likely drawing fine, because an agent shown a blank image concludes the app rendered nothing and spends a debugging loop on a bug that does not exist. Forcing `preserveDrawingBuffer: true` by patching `HTMLCanvasElement.prototype.getContext` inside the framed document was considered and NOT done: the parent's `load` listener fires after the app's inline scripts (the same gap D206 records for console logging), so it needs a document-start injection point in `/render` that does not exist, and adding one for every template to serve this one is the wrong shape of change. **Accepted cost: send latency.** The send path is now genuinely async — inlining computed styles is O(elements × ~340 properties) and the `Image` load plus per-crop encode are real work, so a busy app can put a noticeable pause between pressing Enter and the run starting. Mitigated, not eliminated: the user's bubble is appended BEFORE the capture so their words appear instantly, and every failure mode — a throw, a `null` blob, an over-budget crop, the 6s timeout — degrades to sending the annotations WITHOUT shots, because a user losing their typed message to a failed screenshot is unacceptable. Rejected: writing crops into the user's project directory (pollutes their files, and `app_git.py` would sweep binaries into history); a per-run directory (the run dir does not exist until `_start`, strictly after the crops are captured, so it would mean writing them elsewhere and moving them); returning the crop as an MCP image content block through `app_state` (would change that tool's text-only contract for a payload the agent pays for whether it wanted it or not). |
| D208 | **claude_split gains an OPT-IN, per-message screenshot of the WHOLE visible pane, riding its own `<pane-shot>` block as a `view`/`viewNote` pair — EXTENDING D207 rather than replacing it** | A pill in both composers (`viewShotWanted`, `🖼`) marks the NEXT send only; `sendMessage` reads it into a local and clears it before any await. `annCaptureShots(pending, deadline, wantView)` encodes the whole `shotPane` bitmap — `{left:0, top:0, width, height}` — as ONE extra encode of a bitmap that already exists, uploads it as `<stamp>-view.<ext>` into the same `SHOTS` directory (so the same `Read(//<shots>/**)` pre-approval and the same 12h/200-file pruning cover it), and returns `{view: {view, viewNote}, viewThumb}`. It has its OWN caps — `SHOT_VIEW_EDGE` 1600, `SHOT_VIEW_BYTES` 900KB — and explicitly does NOT inherit `SHOT_MAX_EDGE`. `paneShotBlock` wraps it in a `<pane-shot>` block that `composeOutgoing` places after the app-state block and before the annotations, and `stripPaneBlock` removes position-independently BEFORE `stripAnnBlock` (whose preamble is only recognised at position zero). The pane shot is captured before any crop, does not count toward `SHOT_MAX_COUNT`, shares the crops' one `SHOT_TIMEOUT_MS`, and gets a thumbnail row in the send receipt. | **A crop cannot answer the question this is for.** "The whole layout is wrong", "these two panels overlap", "everything shifted down" have no single element as their subject, and a crop of one element actively HIDES the answer by cutting away the relationship that IS the bug — a case D207 left unserved, and one the user was left describing in prose exactly as #372 left the misalignment case. It is cheap because `shotPane` already rasterises the entire pane for the crops: one more encode, no second capture. **Deliberately not gated on `pending.length`:** "the whole layout is broken" often has nothing to click, so a message with no annotations at all must be able to carry one. **A sibling block, not a per-annotation field:** `shot`/`shotNote` are per-anchor, so hanging a pane picture off one annotation is a category error and repeating it on all of them is a lie — and a pane shot can ride a message that has no annotation block to live in. Tag-delimited like `<live-app-state>` so its strip is position-independent; the annotation block's prefix-matched strip is the one fragile one and a second was not affordable. **Never suppressed for blank WebGL, only annotated** (`shotBlankRegions` names the unreadable rectangles): suppressing a blank CROP leaves the other crops, while suppressing the pane leaves nothing at all — so this reuses the `shotNote`-beside-a-real-`shot` contract instead of inventing a second convention. **Rejected: sticky.** A screenshot toggle that stays on quietly becomes an always-on cost — a rasterise, an encode and a file per turn — for a picture the user asked for once. **Rejected: exposing it to `app_state`.** Letting the model request pixels every turn is the very cost D206/D207 avoided; `app_state` already answers "did my edit land" symbolically and keeps its single-TEXT-block contract, and a picture of the user's screen is theirs to offer rather than the agent's to take. **Rejected: inheriting `SHOT_MAX_EDGE`** — a whole pane squeezed to 640px is unreadable, which defeats the only reason to send one. Accepted cost: 1600px is ~6x the pixels of a crop, so the byte cap actually bites here, which is what makes D209's quality dial matter. |
| D209 | **A shot tries WebP and falls back to PNG, and its filename is taken from `blob.type` — never from the format requested — because WKWebView cannot encode WebP and fails SILENTLY** | `shotEncode` tries `image/webp` at `SHOT_WEBP_QUALITY` (0.8 then 0.6) at full size, then `image/png`, and only then halves the resolution and repeats. `shotExt(blob)` returns `.webp` only when `blob.type` is `image/webp`, and BOTH crop and pane-shot naming go through it. Whether the engine really encodes WebP is probed once per page (`shotWebpOk`), not once per shot. | **Measured, not assumed.** Claude Code's `Read` accepts `.webp`: verified with the real CLI reading a webp out of the real shots dir under the real `Read(//<abs>/**)` rule — no permission card, and small text stayed legible down to quality 60. **WKWebView — which the desktop app ships — cannot encode WebP and does not say so:** `canvas.toBlob(cb, "image/webp", q)` yields a blob whose `type` is `"image/png"` at byte-identical size, with no throw and no null. Naming the file from the REQUEST would therefore produce a `.webp` holding PNG bytes, which is worse than never trying, because that path is what the agent `Read`s. Hence the one rule: the name follows the content. **The quality dial belongs before the resolution knob.** `shotEncode`'s halve-and-retry existed only because PNG has no quality parameter, and dropping 640px → 320px → 160px destroys exactly the legibility a picture is sent for; with a real WebP encoder every quality step is spent at full size first. Both formats are tried at each size and the first that FITS wins, because lossy WebP usually beats PNG but PNG genuinely wins on flat UI. **Rejected: JPEG.** Measured on this very UI it LOST to PNG — 13477 → 13945 bytes on a card, 18173 → 91579 on a canvas raster — because flat colour is PNG's strong case. **Stated plainly: a large share of real usage gets no benefit.** The desktop app is WKWebView, so those users get exactly today's PNG behaviour; Chrome/Edge/Firefox users get the smaller file. This is cheap and correct, not transformative. |
| D210 | **Escape in `claude_split` resolves to the least destructive undo available, and is bound on the framed app's document as well as the page's own** | `escapeAction(composerOpen, annotating, runId)` returns `close-composer` → `exit-annotate` → `stop-run` → `""`, and the single handler `onEscape` is registered on BOTH `document` and, on every frame load, the app's `doc` (beside the existing `mousedown`/`mousemove`/`click` handlers). Leaving annotate mode goes through `annSetMode(false)`, the one function that owns the mode. | **The control promises "Esc or click to stop" and nothing implemented it.** `escapeAction` knew only the composer and a live run, so an armed pane with no popover and no run returned `""` and the key did nothing. **Ordering is by cost of being wrong:** dismissing a popover is repeatable, leaving annotate mode is one click to undo, ending a turn loses the turn — so an Escape pressed while annotating must not reach the run killer, even when a run is live. **Two documents, because a keydown does not cross a frame boundary.** Annotate mode is driven with the pointer over the iframe, so that is where the key lands, and binding only the parent is precisely why Escape looked broken while annotating — the same boundary that already forced `mousedown`/`mousemove`/`click` onto `doc`. Focus is therefore covered in both places it actually sits while annotating: the control button (page document) and the app (frame document). **`annSetMode` extracted rather than duplicated:** a second copy of the toggle is a second chance for the control's label to disagree with `annOn`. Accepted limit: an Escape pressed with focus in the surrounding shell chrome reaches neither document, which is correct — Escape belongs to the pane that has focus. Verified by synthetic dispatch on the frame's document (`cmux browser press` injects into the top document only and cannot route into a subframe), which is the wiring that was wrong. |
| D211 | **The annotate control goes back to a small floating icon — 26px, unlabelled, pushed OUTSIDE the pane — after a strip and then a full-width banner were both tried and both cost too much** | `#lefttools` is deleted; `#annbtn` is `position: absolute; top: 10px; right: -40px` inside `#leftview`, a 26px circle holding one inline-SVG comment bubble. `-30px` = its own 26px plus the 4px divider, so it starts just past the divider in the chat pane's left margin and overlaps the app by zero pixels. Accent-tinted (icon + ring on `--panel`) while idle, accent-FILLED (`--on-accent` icon) while armed. The icon is an inline SVG stroked in `currentColor`, like the composer's pane-shot icon. No label in either state; the instruction and both exits live in the `title`. | **Every version of this control trades occlusion against layout, and the earlier two paid too much.** The original labelled pill floated on the app's top-right corner and GREW into a sentence when armed, covering more of the app exactly when the user was aiming at it. The strip (and then the banner) fixed occlusion by converting it into layout — but that charged ~28px of app height plus a line of chrome to **every** project, including the ones that never annotate, which is the wrong default for a feature used occasionally. The negative offset escapes the trade entirely: outside the pane there is nothing to occlude and no height to spend. Nothing clips it — neither `#left` nor `#leftview` sets `overflow`, and `#chat`'s `overflow: hidden` cannot clip a non-descendant — and the space is guaranteed by `#chat`'s `min-width: 340px` with a layout that never stacks vertically. **Accent while IDLE, not just when armed:** a 26px unlabelled glyph parked outside the pane is easy to miss altogether, so being noticed is the one job the idle state has. That makes both states accent, so the invariant that keeps them apart is asserted: idle tints, armed fills. **Size and position identical in both states** — the armed rule may only repaint. A control that grew when armed would cover more at the worst moment, and as an overlay it cannot move the iframe's box at all, so pins and `annStageRect` (frame viewport coordinates) are untouched by toggling; the strip needed a same-height rule to get the same property. **Still a real `<button>` inside `#leftview`, a SIBLING of the iframe:** Enter/Space and `aria-pressed` come free, `aria-label` names an icon with no text, and living in the parent document keeps it structurally unreachable by `shotPane` (which rasterises the framed document), so it can never appear in a capture. **A comment bubble, not a pencil:** a pencil means "edit this", which is what CLAUDE does to the app — this control lets the user leave a note ON it, and what it produces are numbered comment pins. Drawn as an inline SVG rather than a text glyph for the same reason the pane-shot pill was converted: only `currentColor` can follow the accent tint AND invert to `--on-accent` when armed, and a glyph renders at a different weight on every platform. Accepted cost: the icon sits in the chat pane's margin, so it reads as furniture on the seam rather than as part of either pane, and it is 26px of the chat pane's background. |
| D216 | **The canonical skills are also assembled into a Claude Code PLUGIN root under `home_dir()/skill-plugin/` (`skill_plugin.py`), and every chat fused-render spawns is handed it with `claude --plugin-dir <root>` — D185's user-level sync stays, demoted to convenience for sessions we did not launch** (superseded in part by D228: the `claude --help` capability probe described below is deleted and the flag is passed unconditionally — it blocked the pre-bind path long enough for the desktop supervisor to kill the server) | D185 delivered the skills by writing into `<CLAUDE_CONFIG_DIR or ~/.claude>/skills/`, which is a *guess about the machine*, and every way it can miss is silent: the dir we resolved may not be the one the CLI reads (the user set `CLAUDE_CONFIG_DIR` after startup), marker ownership correctly makes us skip a same-named user-authored skill, the user can delete one mid-session, and on a fresh install nothing orders the sync before the first session. In each case the session starts *without* `fused-render-authoring` and says nothing — the model then writes an app against a `fused` bridge it is guessing at, which is the failure this skill exists to prevent. The sessions in question are ones WE spawn, so the guess is removable: `--plugin-dir` loads a directory plugin for that session only, from a path we wrote moments earlier, additive to and independent of the user's own skills/plugins/settings/CLAUDE.md. Rejected: `--plugin-url` / telling the model to fetch the skill from GitHub (needs network, against the no-network-at-runtime stance, and tracks `main` rather than the installed runtime the skill documents — the same reasoning D185 recorded), and asking the user to `claude plugin install fused-render@fused-render` (opt-in, same version skew; the published marketplace entry stays, for people who want the skills in their own sessions). Shape: the loader wants `<root>/.claude-plugin/plugin.json` + `<root>/skills/<name>/`, so the repo root already IS a plugin root (that is what makes the marketplace entry work) — but the end user almost never has the repo, so `sync_skill_plugin()` reassembles the same tree from whatever the install shipped. Source resolution is D106's, same order as `user_skills.py`: repo `skills/` + `.claude-plugin/plugin.json` first (editable installs), else the packaged copies under `fused_render/skills/`. **The packaged manifest is a FLAT `fused_render/skills/plugin.json`, not a packaged `.claude-plugin/` dir**: nothing in the wheel may live under a dot-prefixed path, because whether a hidden path survives the build backend's `artifacts` globs is precisely the kind of thing that fails in a built wheel on a user's machine and nowhere else — the dotted dir is mkdir'd in the *output*, where we control it. Sync is stamped (a fingerprint of source paths/sizes/mtimes beside the root, not inside it) so the common case — every server start — rewrites nothing; a change deletes and renames the staged tree in, because there is no atomic directory swap and a merge would let a deleted skill linger. Staging is a `mkdtemp` beside the root, not a fixed `<root>.new`: `export_skill_plugin_env` is called from the create-app and create-template routes, which FastAPI runs on a threadpool, so a shared name let two concurrent scaffolds each delete the other's half-copied tree and publish whichever fragment won (caught in review). `_is_loadable` takes the skills that must ALSO be present, and the stamp gate passes them — the manifest alone is not evidence of a complete tree, and treating it as such let a gutted root be blessed by a matching stamp and served to every session until the sources next changed, which is the silent-failure mode this whole decision exists to remove. The failure paths deliberately check the manifest only: there a partial tree competes with no tree at all. Best-effort throughout, returning a stale-but-loadable root over none. Whether to pass the flag at all is decided ONCE, server-side, in `export_skill_plugin_env()` (called from `server.export_app_env`, beside the other `FUSED_RENDER_*` exports): it syncs, probes the local `claude --help` for `--plugin-dir` (probe deleted by D228), and publishes the root as `FUSED_RENDER_SKILL_PLUGIN_DIR` — or clears the var. BOTH claude templates (`claude/agent.py` and its `claude_split/` fork) then just read `appenv.skill_plugin_dir()` and append the flag if it is set; they may not import the package (SPEC PY-15) and they deliberately do not re-derive the path from `home_dir()` either, because `home_dir()` branch-nests and only the server knows the resolved answer. The first build DID probe inside `_start` and that was wrong twice over: `subprocess.run` goes through the `Popen` that a dozen existing tests fake (12 failures, all real), and in production it added a subprocess to every turn — `test_the_spawn_path_never_shells_out_to_the_cli` is the scar. The probe fails CLOSED (an unknown option makes the CLI exit before the turn starts, so an older install loses the skills rather than every chat) with one deliberate exception: a `claude` the server cannot FIND still gets the flag, because "not on the server's PATH" is the normal state on Windows — the templates search install locations the server does not — and reading that as unsupported would withhold the skills from exactly the users least likely to notice. The system prompts are deliberately untouched: the starter `CLAUDE.md`s already name the skills, and `_split_system_prompt`'s one job stays naming the app-state tool. Tests: `test_skill_plugin.py` — assembled shape, stamp short-circuit, stale file eviction, packaged fallback, missing-manifest synthesis, no-raise on failure, the no-dotted-path packaging invariant, and the seams pinned (the `SKILLS` tuple and source roots against `user_skills.py`; both templates reading only the appenv helper, with no second spelling of the subdir name); plus the export half in `test_template_appenv.py`. |

| D217 | **The pushed app-state DOM outline travels as a FILE PATH (`dom_path`), not as bytes in the message** | `appStateBlock` stringified the whole outline into the outgoing message, and the message is what the CLI records in its own session transcript — so an N-message conversation carried N full DOM trees in context permanently, re-read on every later turn, and all but the newest already stale. The block's own preamble says it goes stale the moment anything is edited and that `app_state` is the fresh-read channel, which makes the retained copies pure cost. The outline is also by far the largest thing the page sends (deeply repetitive `nth-of-type` ancestor chains, which tokenize badly). Screenshots already solved this exact problem the same way: capture, `fused.uploadFile`, send the path. Rejected: keeping a small inline digest (title/entry/landmarks) beside the path — the hybrid preserves zero-latency awareness and was the recommendation, but the call was made for the pure path, accepting that the model must spend a `Read` before it knows the app's structure and may sometimes act without looking. Also rejected: shortening `path` to be parent-relative, which would halve the blob even inline but changes the anchorPath contract the annotations share. The file goes in the SHOTS directory rather than a new one: same class of artifact under the same privacy argument (a private, short-lived record of what was on the user's screen), and that directory is already 0700-enforced, already TTL+count pruned, and already the one path `--allowed-tools` lets `Read` touch without raising a card — a second directory would need its own copy of all three, and `_shots_dir`'s hardening is ~80 lines of security-critical code not worth duplicating. The write happens in the send path, NOT inside `composeOutgoing`, which is sync and pure because it must stay the exact inverse of `stripBlocks`. Falls back to the inline outline on any failure (no directory, failed write) rather than dropping it: degrading must not leave the agent knowing *less* about the screen than before this existed, and the preamble is built from whichever shape actually arrived so it never promises a `dom_path` that is not there. Names carry a sequence number as well as `Date.now()` — two sends in one millisecond would otherwise overwrite one another's outline while the first was still being read. The tag is unchanged, so both strippers (`_strip_app_state` and the page's `stripAppStateBlock`) keep working untouched. The PULL channel (`app_state` tool) still answers inline: the model asked for it, once, and the answer is fresh. |
| D218 | **The split view shows Skill invocations as log rows and live API retries on the status line** | `_poll` only ever read `system`, `stream_event` and `result` rows, so two things the CLI already reports were discarded. (a) A skill silently changes how a reply was produced — without a row in the log the only visible difference between a session that loaded `fused-render-authoring` and one that did not is that the answers are better, which is exactly the kind of thing that makes D216 unverifiable in the field. Read off the FINALIZED `assistant` row, whose `tool_use` block carries `input.skill` whole; the streamed `content_block_start` for the same call arrives with `input: {}` and the name only as `input_json_delta` fragments needing reassembly, so reading one row and not the other is also what stops a call being counted twice. Keyed on the `tool_use` id because `_poll` replays the entire file every 400 ms — the same reason `notedStates` exists for app-state reads. (b) An overloaded API was indistinguishable from a hang: the status line sat on "Thinking…" with a frozen token count for the whole backoff, which is how a 529 looked to a user for the entire life of this template. `system`/`api_retry` rows carry `attempt`, `max_retries`, `retry_delay_ms` and `error_status`. The live state is deliberately TRANSIENT — anything the model produces after a retry ends it, since rows are in file order — because a banner left up for the rest of the turn is a lie for far longer than it was true; the tally is kept, because "retried four times" is what makes a final failure explainable. Shown on the status line and not as one note per retry: ten attempts would mean ten log rows. 529 and 429 travel as the status rather than a baked-in message (the API is swamped and waiting helps, versus this account is being throttled — calling one the other sends the user to the wrong place), and `max_retries` is read from the row rather than assumed, since the budget is the CLI's. Terminal errors are rewritten in `_poll` rather than in `runEnding`, which is pure and pinned against the claude template's copy so the two keep deciding identically; it never string-sniffs the CLI's error text and keeps the original in parentheses as the string a bug report can be matched on. **The rewrite keys off the retry still IN FLIGHT when the run ended, not the tally.** Keying it off the tally was a bug (caught in review): the tally survives a mid-turn retry that succeeded, so a later unrelated failure — a crashed tool, a bad edit, an auth error — was presented as an API overload with the real cause buried. Because a `result` row clears the live retry like any other post-retry row, the cleared value is held for one row and captured only when that `result` is an error; the abnormal-exit path (killed mid-backoff, no `result` row) reads the still-live retry instead. The scar is `test_a_recovered_retry_does_not_explain_a_later_unrelated_failure` — the adjacent case to one the original tests did cover ("retried, recovered, no error") and which they missed. |

### macOS bundle — the interpreter locates itself, and a ready marker is a claim (2026-08-04)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D212 | **The .app gains one relative symlink, `Contents/lib -> Resources/lib`, so the bundled interpreter self-locates with NO environment variables — and `envinstall.is_installed()` stops treating `.openfused-ready` as proof, verifying it once per venv per process and DELETING it when the venv's own python cannot run** | Reported symptom: on a DMG install, every HTML page whose `.py` carries a PEP 723 `# /// script` header failed permanently with "python execution" errors, and the only workaround was flipping the engine pref from `fused` to `local` (D204 having just made `fused` the default, so this was the out-of-the-box experience). Two linked causes. **(1) The interpreter could not self-locate.** py2app puts the runtime (stdlib zip + `lib/python3.12/`) under `Contents/Resources/lib` but the interpreter at `Contents/MacOS/python`, so CPython's landmark search — `<prefix>/lib/python3.12/…` relative to the executable, i.e. `Contents/lib/python3.12` — misses and falls back to the prefix compiled in at build time: the BUILD MACHINE's Homebrew framework (`/opt/homebrew/opt/python@3.12/Frameworks/…`), measured on a real installed bundle with the env stripped. The app worked anyway because py2app's launcher stub exports `PYTHONHOME=…/Contents/Resources`; what did not work is everything downstream, because `fused`'s `python_compute` STRIPS `PYTHONHOME`/`PYTHONPATH`/`VIRTUAL_ENV`/`PYTHONSTARTUP` from its children. So a `uv venv --python Contents/MacOS/python` (how the install loader builds a header's venv — PY-18, D173, D176) recorded that nonexistent Homebrew path as its base prefix, and every child of that venv died (`ModuleNotFoundError: No module named 'pandas'`, encodings failures). `ln -s Resources/lib Contents/lib` fixes it outright: verified on a copy of an installed bundle, `sys.prefix` becomes `<App>/Contents`, `import fused_render, duckdb, rasterio` works with `PYTHONHOME`/`PYTHONPATH` unset, and a `uv venv` built from that interpreter records a base prefix INSIDE the app and imports a freshly installed package with the env stripped. Zero per-request cost — no probe, no wrapper, no extra subprocess; the link is resolved by the interpreter's own startup path search. Placed after the prune/staging steps that rewrite `Contents/Resources/lib/python3.12` and before both the smokes and the codesign sweep, so it is sealed into the signature; both magic-byte sweeps use `find <dir> -type f`, which does not follow symlinks without `-L`, so the link is neither signed as nested code nor traversed twice, and a re-signed bundle carrying it passes `codesign --verify --strict` ("valid on disk / satisfies its Designated Requirement"). Relative, never absolute — the .app is copied to /Applications, mounted from a DMG, and run from CI temp dirs. **(2) A bad venv was permanent.** "Installed" was solely `os.path.exists(<venv>/.openfused-ready)`, and upstream's `ensure_requirements_venv`/`ensure_bare_venv` short-circuit on `if marker.exists(): return` — a rebuild happens only when the marker is ABSENT. The venv cache key is `sha256("<python path>|<version>|reqs:…")`, and inside the .app both the path and the version are constants, so an app upgrade does not change the key: nothing revalidated, and there was no repair action in the UI. So the marker is now the **index** of installed venvs (its absence is still final, and still a single stat — the common case) but a **claim** that is verified once: one `<venv>/bin/python -c ""` with the env `python_compute` gives children, borrowed from `engine._child_env()` so the probe cannot drift from the real child env. Memoized per venv directory per process (`_VALIDATED`) — `/api/run`'s pre-flight calls `is_installed` on every run of every header script, so a probe per request would put a subprocess on the hottest path. **Because the probe is a subprocess, two placement rules follow, and both were got wrong first (found on review — the second bug in this row's history caused by copying `app_interpreter`'s shape without copying its reasoning):** (i) the pre-flight runs `await asyncio.to_thread(envinstall.is_installed, …)`, never inline — `routers/run.py` awaits `run_python` directly, so an inline `subprocess.run(..., timeout=5)` stalls the whole server (websockets, the file watcher, every other request) for the full budget, which is exactly why the header-LESS branch of the same `if` already does `await asyncio.to_thread(app_interpreter)`. `to_thread` re-raises in the awaiting frame so the existing `except (ImportError, RuntimeError)` containment is unchanged, pinned by a test rather than assumed. (ii) `_venv_is_usable` is **double-checked**: read the cache under the lock, probe OUTSIDE it, re-acquire and `setdefault`. Holding the module-global lock across the probe (which it first did, copying `engine._app_interpreter_lock`) serialized the first probes of UNRELATED venvs, so one venv on a wedged mount blocked every `is_installed` in the process for up to 5s — cached hits for healthy venvs included, and combined with (i) that froze the entire app on one stuck venv. The accepted cost is that two threads racing on the same venv may both probe: it is a read-only `-c ""`, so a duplicate is one extra ~15ms spawn in a rare race, and `setdefault` makes the racers agree on one verdict. Rejected per-`venv_dir` locks — they need their own eviction story for a benefit the duplicate-probe trade already buys. (iii) Because the probe is outside the lock, the venv can be DESTROYED mid-probe, so a **generation** is read before the probe and re-checked after — `_generation_of` = (`_EPOCH`, `_GENERATION[venv_dir]`), bumped by `_discard_verdict`, which is now the only way an entry leaves `_VALIDATED` so that "the verdict is gone" and "the generation moved" cannot disagree. The invariant is absolute and there is no tolerated staleness window: **a verdict is only ever cached against the generation it judged**; if it moved, the prober returns its own measurement to its own caller and stores nothing, and the next call re-probes. Recorded as an invariant rather than a mitigation deliberately: the first cut instead let `_REBUILD_ATTEMPTED` absorb a stale verdict's cost, which quietly made a tunable repair *policy* double as the staleness backstop — so a later tuning of the budget would resurface staleness far from the change that caused it. The `_EPOCH` half exists because a per-directory counter cannot express "invalidate everything": a probe on its FIRST look at a venv has no entry to bump, and that is most in-flight probes, so `reset_venv_validation_cache()` bumps the epoch instead of only clearing the dicts — otherwise the reset is undone by exactly the calls it means to invalidate. The steady-state ceiling is untouched (no discards means no generation changes), pinned by the 8x20-thread test. On failure the marker is **deleted before returning False**, which is the load-bearing subtlety: without it the pre-flight answers `needs_install`, the worker calls upstream, upstream sees the marker and returns immediately without rebuilding, and the loader loops forever against an unchanged `fused`. The cached verdict is dropped alongside the marker (and again whenever the marker is found absent), so a rebuilt venv is judged on its own merits instead of inheriting a permanently negative answer — the mirror image of the bug being fixed. This half is also what HEALS users who are broken today: their venv key does not change when they upgrade, so the symlink alone would leave them sitting on the bad venv. **Two contracts bound what the validation costs when it is wrong (both added on review, both pinned by tests, and neither is defensive padding a future reader should "simplify" away).** (a) **The probe is THREE-valued.** `_venv_runs` answers True (completed, exit 0), False (DEFINITE evidence: completed with a non-zero exit, or the spawn failed on the exe itself — `FileNotFoundError`/`PermissionError`/`NotADirectoryError`), or None (INCONCLUSIVE — a `TimeoutExpired`, or any other `OSError`: `EAGAIN` when the server cannot fork under load, `EMFILE` when it is out of descriptors, `EINTR`). Only False reaches the deletion. The two-valued first cut was a live hazard, not a hypothetical: `TimeoutExpired` IS a `SubprocessError`, so with a 5s budget a few concurrent `/api/run` calls on a loaded machine were enough to unlink the marker of a perfectly healthy venv and charge the user a full uv re-download. `None` is cached nowhere (it says nothing worth remembering, and caching it would make one unlucky fork permanent), touches no state, and answers **True** so the run proceeds and reports whatever really happens — the policy `engine._probe` already had, which reports a failed probe and destroys nothing. The inconclusive log line is worded distinctly from the corruption one so an operator can tell "state was destroyed, a rebuild is coming" from "we never got an answer". (b) **The repair is attempted at most ONCE per venv dir per process** (`_REBUILD_ATTEMPTED`, under the same lock). `runtime.js`'s `installed` flag bounds the retry WITHIN one `runPython` call, but nothing bounded it across calls, and the state that would have — the marker and the memo — is exactly what the repair destroys. For the cohort this decision exists for the rebuild is guaranteed futile: the property that failed is the bundled interpreter's own base prefix, invariant under rebuild, and the key folds in only that interpreter's path and version. So unbounded, every page reload, every LR-2 `watchPath` auto-reload and every param change would pay another multi-hundred-MB download, where before D212 that user got one instant permanent error — a straight regression. The second and later definite failures therefore leave the marker in place and report **installed**, so the script runs and the user sees the interpreter's own stderr, which is the truthful outcome. Per PROCESS deliberately: installing a fixed DMG means a fresh server, an empty set, and one rebuild that works. The bound is NOT cleared on the marker-absent branch (the marker is absent by construction right after we unlink it, so clearing it there would mean it could never engage), and `reset_venv_validation_cache()` clears all three sets together — resetting only the verdicts would leave a bound that silently suppresses the repair the re-probe just asked for, and leaving `_BOUND_LOGGED` would silence a bound that can engage again. **One rule governs every branch that acts on a verdict, and it is stated once rather than per-branch: a verdict may only be acted on while the generation it judged is still current, and a "ready" answer requires the marker to still be there at the moment we answer.** `_GENERATION`/`_EPOCH` enforce the first half for the *cache*; `_marker_stamp` — `(st_ino, st_mtime_ns)`, captured before the probe and re-checked under the lock, named to match openfused's copy so the two read alike — extends it to the marker on disk, and both mechanisms are necessary because of an asymmetry: the counters only know what THIS process did, while the install worker is another PROCESS that can un-mark, rebuild and re-mark without touching them. Identity, not existence, for exactly that reason — the un-mark-and-re-mark case leaves a marker present the whole time this side can observe, so `os.path.exists` sees nothing at all. Three concrete failures this closes, all found on review and all pinned by tests: (1) "stat the marker -> probe -> decide -> unlink" is not atomic between THREADS (the endpoints run in FastAPI's threadpool), so A could record the attempt and unlink while B, which stamped before A's unlink, arrived after A's add, read `already_tried`, announced a rebuild that had not happened and returned True — running a venv known to be broken instead of joining A's install; (2) a *replaced* marker (the worker finishing a rebuild mid-probe) would let the definite-failure branch unlink the marker of a freshly rebuilt, possibly healthy venv and force another multi-hundred-MB download — the same high-severity shape Bugbot found in openfused; (3) a `True` verdict answered after the marker had VANISHED would hand `/api/run` a directory being rebuilt underneath it, a confusing mid-rebuild failure instead of the loader. All three resolve identically — discard the verdict, answer not-installed, let the caller join the install already in flight (`start()` joins rather than duplicating) — so they share one gate rather than three copies of an identity comparison. The INCONCLUSIVE branch is deliberately NOT gated: it answers from no verdict at all, and "I learned nothing" is true whatever the marker did. Deliberately no cross-process lock: this module's only mutual exclusion is `threading.Lock`, a file lock would need a stale-lock policy, and the thread-level race is what needs atomicity; the stat under the lock is the same syscall the function already makes unguarded on entry, so if that one returned the filesystem was answering microseconds ago. And the bound's warning is emitted **once per venv per process** (`_BOUND_LOGGED`, warning on the transition and `logger.debug` after), because past that point every call answers from the cached verdict — every page reload, every `watchPath` auto-reload, every param change — and repeating a multi-line warning per request buries the one occurrence that matters in the log of exactly the incident this code exists to diagnose | The build-time assertion is the real lesson and is why this is recorded rather than just fixed: the property was invisible because **every** existing smoke in `build_dmg.sh` ran the bundled python WITH `PYTHONHOME=Contents/Resources` set, and a bundle whose landmark search resolves to a Homebrew prefix passes all of them. The new smoke runs it with `env -u PYTHONHOME -u PYTHONPATH -u VIRTUAL_ENV` and FATALs unless `sys.prefix` is inside `$APP_DIR` *and* `import fused_render, duckdb` succeeds — the prefix check alone would accept a right-looking prefix over an incomplete runtime, and the import alone would pass on a machine that happens to have the build interpreter installed (i.e. every dev machine, which is the whole reason this reached users). **Rejected:** removing py2app's launcher-stub `PYTHONHOME` export — it is not ours to remove (the stub sets it), the app keeps working with it, and it is now merely redundant rather than harmful. **Rejected:** deleting `engine.app_interpreter()`'s `_wrapper_interpreter` machinery (D172/D175), which exists to give a header-LESS script a `PYTHONHOME`-restoring wrapper when the raw interpreter fails its probe. On a correctly built bundle the probe now succeeds and the wrapper is never generated, so the machinery is self-disabling; it stays because it also covers a bundle built before this change and any future interpreter that genuinely needs an env, and its cost when unneeded is zero. **Rejected:** revalidating from a persisted record (a stamped build id, a version file beside the venv) instead of a probe — it would need its own invalidation story to avoid becoming exactly the stale-forever fact the marker already was, and it cannot see the failure mode that actually occurred (a base prefix that vanished for reasons having nothing to do with our version). **Rejected:** validating by importing the requirements rather than `-c ""` — that is upstream's job, costs a real import, and the property that broke is more basic than any import: the interpreter never reached the point of executing a program. **Left alone knowingly:** `engine._needs_install_dict` still says the script "declares dependencies that are not installed yet", which is misleading in the one call where the real cause is a dead interpreter. A truthful message needs a *reason* plumbed out of `is_installed` — whose contract is a bool — into the wire dict, i.e. new API plus per-venv reason state, which is the cross-cutting refactor this change is not. It also self-corrects within one user action now that the rebuild is bounded: the misleading message appears once, and the very next call runs the script and shows the interpreter's own stderr |

### install progress — liveness instead of a fake percentage (2026-08-04)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D213 | **The install record gets a ~2s heartbeat that refreshes elapsed time and `ts` while `pct` STAYS at 25, the client renders that stage as an indeterminate bar, and a joined install is never painted as 0% — with the worker's terminal write latched so a late beat can never put `done: false` back on the wire** | Reported symptom: the dependency-install splash "sits stuck for a long time" when switching between apps. Not a perception problem — the UI had no mechanism by which it *could* move. `_env_install_worker.install()` wrote exactly three records (`create` 10, `install` 25, `done` 100) and did the entire download inside one `ensure_requirements_venv` call, which runs uv behind `capture_output=True`; so for the only slow phase — minutes on a cold cache with imagecodecs/pyproj — nothing was emitted at all, while the client polled every 500ms and repainted an identical record. Three changes. **(1) A heartbeat** (`_HEARTBEAT_S = 2`) re-writes the `install` record with elapsed time appended (`downloading and installing 3 package(s): pandas, pyproj, s3fs (2m14s)`) and a fresh `ts`, which is the only evidence of liveness that reaches the wire. **`pct` deliberately does not move, and neither does the stage** — the obvious "fix" a future maintainer will reach for is animating the number upward, and it must not be done: there is genuinely nothing to measure behind captured output, the percentage is the one thing a waiting user trusts as fact, and a bar that creeps to 90% and then sits there is a worse lie than one that admits it does not know. **(2) An indeterminate bar** in `runtime.js` for `stage === "install"`: a narrow travelling fill (`bar.dataset.indeterminate = "1"` plus one `<style>` injected on first use and found by id afterwards — inline styles cannot express keyframes, and a per-install copy would pile up in `head`), switching back to a real width for any stage that carries a genuine value so a finished install stops animating. **(3) `showInstall` no longer asserts `0%`.** `start()` JOINS an install already in flight rather than duplicating it, so nothing was ever lost — but re-opening a page four minutes into a download painted 0% and then jumped to 25% on the first poll, and a user switching back and forth saw 0% → 25% → freeze repeatedly and concluded it was looping. The initial state now claims no percentage at all and the first real paint comes from the server's own record, which the `/api/env/install` response already carries. `paint` moved to a module-scope `paintInstall(ui, prog, notice)` so the stage-to-bar rule has one definition and can be driven directly under node | **The ordering is the whole risk, and it is enforced rather than asserted (the D181 rule).** The heartbeat thread and the terminal `done`/`error` write both `os.replace` onto `progress.json`; a beat landing afterwards puts `done: false` back on the wire and the client then polls a finished install forever — the reported symptom, made permanent. Three layers, only the last of which is a guarantee: `Event.wait(_HEARTBEAT_S)` rather than `sleep` so shutdown is immediate; `stop.set()` + `join` in a `finally`, which runs as an exception propagates and therefore precedes the `except`'s error write as well as the success write; and — because `join` takes a timeout and a beat can be parked inside its own `_write` on a slow filesystem — one lock plus a latch inside `install`, so once a terminal record exists nothing may write again. The latch closes AFTER `_write` returns, never before: a terminal write that fails (a full disk) must not shut the file, or the `except` path's error record — the only one carrying the reason — no-ops and `done: false` stays on the wire forever, which is this decision's own symptom reached from the other side. The lock makes write-then-latch atomic, so no beat can slip between them. The heartbeat thread is a daemon: one wedged in a write must never keep the worker process alive after its record says done, or `_pid_alive` reads the installer as still running and the page polls a corpse. `_write`'s temp name gains the thread id (matching `envinstall._write`): pid alone stopped being unique the moment a second writer lived in this process, and a shared temp name means the first `os.replace` consumes the other's file and the loser dies — a crashed installer whose venv was actually fine. The module stays stdlib + `fused` only (D152), so `threading` not anything of ours. **Explicitly out of scope, both deliberate:** real per-package percentages, which would require upstream's `venvs._run_step` to stop capturing output — a `fused` change, not ours; and a cross-key concurrency cap, even though switching apps really does start N detached workers competing for bandwidth (each distinct requirement set is its own key and its own worker, with no cap), because a cap needs a queue, a fairness rule and a cancellation story of its own. **Testing gap, stated rather than papered over:** the indeterminate bar's DOM contract is asserted under node (`dataset.indeterminate` set for `install`, cleared for a real percentage; the keyframes injected exactly once; a joined mid-flight record painted with no 0% frame — via a recording setter on the memoised bar's `style.width`), but no headless test can tell whether the animation LOOKS right, and this repo has been burned by exactly that before (see the CM line-margin note in the memory index). It needs a human glance |
| D214 | **Script venvs are pinned to Python 3.12: the server's own interpreter when it is already 3.12, else a uv-MANAGED 3.12 — downloaded first, as a visible `python` progress stage, under its own progress key. `scripts/dev.sh` pins its `.venv` to 3.12 and rebuilds one that is not** | Reported symptom: a `# /// script` app declaring `tensorflow` failed permanently with uv's "no wheels with a matching Python ABI tag (e.g. `cp314`)", and the user worked around it by switching the engine off `fused` entirely. Cause: nothing ever set the backend's `python_executable`, so `_python_executable()` returned None, upstream resolved that to `sys.executable`, and the base interpreter for every script venv was an accident of how the server had been launched — a `.venv` that a bare `uv venv` had built on uv's newest known CPython. The error named the real cause and no rebuild could repair it, because the missing wheels do not exist. 3.12 rather than 3.13 because all three installers already ship it (`python@3.12` in build_dmg.sh, `uv python install 3.12` in the Linux and Windows scripts), so the pin needs NO packaging change and both run paths — script venvs and the header-less app interpreter — land on one version instead of two | **Preferring our own 3.12 over a managed one is what makes this cheap, and is not a shortcut.** `uv python find --managed-python` matches only uv's own registry, and every bundled 3.12 is copied into the payload rather than registered there — so forcing a managed interpreter would have made every packaged user download a second CPython to reach a version they already had, AND re-keyed every venv they own, since `python_identity` folds the interpreter path into the key. Resolving to None instead is byte-identical to the pre-D214 value, so their keys do not move. Rejected `--prerelease=allow`, which was the other candidate fix: it is not "only when nothing else resolves" (that is `if-necessary`) but "consider prereleases for every package", and since uv prefers the highest compatible version it would resolve tensorflow 2.21.0rc1 over stable 2.20.0 — shipping release-candidate scientific libraries to every user of every app to work around an interpreter choice. The interpreter download reports under a fixed, key-SHAPED `PYTHON_BOOTSTRAP_KEY` because this is the one state where a venv key cannot be computed: the key folds in the base interpreter, and that is precisely what is missing, so any key derived here would name a venv nobody will build. For the same reason the worker acquires and STOPS, and the client's loop guard becomes the SET of keys installed rather than a boolean — two rounds are now legitimate. A not-ready resolution is deliberately never cached, since the download lands in another process that cannot notify us. **Accepted costs:** a source checkout moving off a non-3.12 interpreter re-keys its venvs once and the orphans are never collected (there is no GC), and a `uv python upgrade` of the managed 3.12 re-keys again because the patch version is in `python_identity` and the managed path is not — upstream owns that function. A machine with no uv degrades to the old unpinned behaviour rather than refusing to serve: it can honour no pin, and a working server beats a correct one nobody can start |
| D215 | **The generated wrapper defines `__file__` (the script's real absolute path) and `__name__` (`"__fused_module__"`) in the exec globals, so a page's `.py` gets the same module globals under either engine** | Reported symptom: a working local-tools app failed on first paint with `NameError: name '__file__' is not defined` at its own module level — from `CATALOG_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)), "settings_catalog.json")`, i.e. the ordinary way to find a data file sitting next to your script. The built-in executor's worker loads the file with `spec_from_file_location("__fused_module__", path)`, so CPython's import machinery defines both dunders; `engine.build_code`'s preamble `exec(compile(src, abs_path, 'exec'), globals())` defines neither. The trap is that it *looks* like it does: the real path was already being passed as `compile()`'s filename, which is why tracebacks correctly named the user's file and line — but that argument only LABELS code objects, it does not bind the name `__file__`. So the two engines handed user code different globals, and the difference was invisible in every error message the failure produced. `__name__` is set in the same breath, for parity rather than a reported failure: it was inherited from the backend's runner namespace and arrived as `"builtins"`, which no script can sensibly branch on and which differs from the other engine. Fixed in fused-render alone — `fused` receives a STRING of code and has no path to point `__file__` at, so it is correct for its runner not to set one; the path is known only here | **The cost of not fixing this at the source was already visible in the tree and had been paid twenty times.** Fifteen templates — `bundle/reader.py`, `docs/docs.py`, `claude/agent.py`, `claude_split/agent.py`, `markdown/graph.py`, `latex/engine.py`, `excel/reader.py`, `history/history.py`, `annotate/annotate.py`, `slides/slides.py`, `pano/pano.py`, `usd/reader.py`, `map/map_render.py`, `zarr_aoi/tile_server.py` and `git/log.py` — each carry a hand-written `if "__file__" not in globals(): __file__ = os.path.join(sys.path[0], "<name>.py")` — several with a copy of the same three-line comment explaining the engine's behaviour; five more (`geotiff/tile_server.py`, `geotiff/_tiff_core.py`, `netcdf/grid_tile_server.py`, `netcdf/_zarr_core.py`, `notebook/kernel.py`) instead branch on `if "__file__" in globals()` at the point of use and fall back to `sys.path[0]`, which now simply always takes the first arm. Shipped templates were therefore immunised one at a time while every user-authored page rediscovered the defect from scratch, which is the asymmetry that makes this a source bug rather than a template idiom. Those guards are left in place: they are `not in globals()` tests, so after this change they never fire, and deleting twenty files' worth of dead branches is a separate mechanical cleanup, not something to bury in this PR (their comments are now stale, and that is the flagged cost of deferring it). The reconstructed value was already identical to the real one — `sys.path[0]` is the `script_dir` this same preamble inserts — so nothing changes behaviour for them. **The parity is pinned behaviourally, not structurally:** two rows added to `test_engine_parity.py`'s table, which runs every case through the built-in worker AND the fused wrapper AND the real backend and compares, so a future rewrite of either preamble cannot drop a dunder silently. Asserted on basenames because each engine writes its target under its own tmp subdir. **`__name__` was the one judgement call, and it is safe for a specific measured reason:** neither engine makes it `"__main__"` (`"__fused_module__"` vs the old `"builtins"`), so no `if __name__ == "__main__":` block that was dormant starts running — which matters because `geotiff/tile_server.py`, `latex/install_worker.py` and both `permission_server.py` files use exactly that guard for the SUBPROCESS they spawn of themselves, where `__name__` really is `"__main__"`. A test pins that a script's `__main__` block stays dormant, so the property is enforced rather than reasoned about. **Not extended further:** `spec_from_file_location` also sets `__spec__`, `__loader__` and `__package__`, which are left undefined — `__package__` would only matter for relative imports, which fail under both engines anyway, and inventing a fake spec/loader would make introspection lie about how the module was loaded rather than close a real gap |

### Google Drive mounts — in-app sign-in (2026-08-04)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D219 | **Drive sign-in is handled in-app by spawning `rclone authorize "drive"` and creating the remote over rcd — the same child-process-plus-poll shape the Fused account login already uses — and the user must supply their own Google OAuth client, because rclone's shared one is being retired** | `POST /api/mounts/remotes` (`shell/mounts/endpoints.py`) hardcodes `"type": "s3"` and its docstring sends OAuth backends to a terminal; `views/Mounts.tsx` told the user to run `rclone config`. That instruction is the whole gap — nothing about Drive is architecturally blocked, the endpoint simply had no way to obtain a token. It now spawns `rclone authorize "drive"`, which runs its own loopback callback server on `127.0.0.1:53682`, opens the system browser, and prints the token JSON on success; the server parses that blob and creates the remote through the **rc daemon**, never `rclone config create` on argv — the identical `ps`-visibility reasoning already recorded for the S3 key path. Completion is **polled, not pushed** (a status endpoint reporting `in_flight`), and cancel escalates SIGTERM→SIGKILL — all three lifted from `account.py`'s `_ActiveLogin`/`_pump`/`_watch_exit`/`_ensure_dead`, so this adds no new concurrency pattern. **Scope is full `drive`, not `drive.readonly`:** read-write was the requirement, and `drive.file` — the one non-restricted alternative — only ever sees files the app itself created, which is useless behind a mount. **The remote sets `--drive-skip-gdocs`:** Docs/Sheets have no byte representation, and without the flag they surface as ordinary-looking files whose saves silently fail to round-trip. **Nothing downstream needed changing, which was verified rather than assumed:** `_detect_read_only` (`access.py`) sees Drive advertise `Put`/`PutStream`/`Copy` via `operations/fsinfo`, falls past the S3/GCS-anonymous branches and reports writable, so the mount is read-write with no flag set and `mount_read_only` stays false; `VFS_OPT`'s `CacheMode: "full"` is already exactly what Drive read-write requires, since Drive cannot do partial or random writes; and the S3/GCS fast paths (`signing.py`, `direct_listing.py`) are each guarded by an explicit `type ==` check, so Drive falls through to the generic VFS mount path untouched. `_mount_credential_status` **did** need widening — it returned `"n/a"` for anything that is not `env_auth=true`, so a revoked Drive token surfaced as the generic "reconnect" message that cannot fix it; the gate now includes every OAuth backend type (read off the provider registry — see D224 — rather than a `type == "drive"` special case), and `_BAD_CRED_MARKERS` already carried Google's `"has been expired or revoked"` string, so the classifier itself was left alone. Drive is deliberately **not** added to `_credential_suggestions`: that list is credential-file-driven (it scans `~/.aws`, ADC, env vars) and an OAuth backend has nothing to detect, so it is a distinct provider card in the "Add storage" picker (D224) rather than a detected suggestion. No `drive://` scheme in `resolve_cloud_url` — that resolves bucket URLs and Drive has no equivalent. | Owner call 2026-08-04. **Rejected an embedded webview**, the obvious way to keep the user inside the app: GNOME Online Accounts shipped exactly that for years (a WebKitGTK view scraping the auth code out of the page title via Google's `urn:ietf:wg:oauth:2.0:oob` flow) and was forced to tear it out — Google now rejects embedded user-agents with `disallowed_useragent` and killed OOB outright in late 2022. System browser + loopback + PKCE is where GOA landed, and it is what `rclone authorize` already does, so the correct flow comes free. **Rejected driving rclone's rc config state machine** (`config/create` with `opt.nonInteractive`, then `config/continue`), which would surface the auth URL in-band: more moving parts and more sensitive to rclone version, for no user-visible gain over one subprocess. **On the OAuth client, the original premise is dead and this entry is rewritten: bring-your-own client ID is now MANDATORY for Drive.** The first version of D219 shipped no client ID and rode rclone's built-in shared one, on the reasoning that it is already verified by Google for the full `drive` scope, so an annual third-party security assessment was not our problem. That is no longer available. rclone has been notified that Google will begin **charging for API requests made on rclone's default client ID**, starting later in 2026 after 90 days' notice, and rclone's stated plan is warn → disable → remove it. So every Drive user must supply their own `client_id`/`client_secret`, and a remote that does not carry one is on a countdown rather than merely rate-limited. This is why the sign-in endpoint **refuses a Drive attempt without a client** (a 400 whose message explains the retirement, before the browser ever opens) and why the setup step exists at all in the UI. Drive is the **only** backend affected — rclone's config prompts still say "Leave blank normally" for OneDrive, Dropbox and Box — so those providers are deliberately never shown client fields (D224). **Rejected shipping one bundled fused-render client ID**, the obvious way to keep it one click: under Google's new per-client-ID charging *we* would be the payer for every user's traffic, and the full `drive` scope is a **restricted** scope, so publishing it means a CASA security assessment — roughly $540–1,000, four to twelve weeks, and re-verified annually. **Rejected narrowing to `drive.file`** to escape that: it carries no verification burden precisely because it can only ever see files the app itself created, which behind a mount means an empty folder — the same reason the scope choice above rejected it. **Rejected automating the Google Cloud setup** so the user never sees the console: there is no public API for creating a standard OAuth client at all, and the one programmatic path that exists (`gcloud iap oauth-clients create`) produces clients locked to IAP with an unmodifiable redirect URI and forced-internal brands — it cannot serve the loopback desktop flow `rclone authorize` needs. What is left is to make the manual trip as short as possible, which is what the stepper does: a button per console page, the downloaded `client_secret_*.json` as the primary input, and the pair remembered per machine so it is a one-time cost rather than a per-remote one. **Async upload failure is surfaced, not left to the log** (D221): with `CacheMode: "full"` a write returns success as soon as it lands in the local cache, and a quota or permission rejection would otherwise appear only in the rcd log. That is the most likely source of "I saved it and it vanished" on a read-write mount, so it is in scope here rather than deferred. |
| D221 | **Pending and failed VFS uploads are read from rcd's `vfs/queue` and surfaced on the mount, rather than inferred from the log** | The write path returns success at the cache, so the only authority on whether bytes actually reached the remote is the VFS upload queue — rcd exposes it as `vfs/queue` (per-item `name`, `size`, `tries`, `err`, and the retry delay), which is the same daemon-over-loopback call every other mount fact already comes from. A failing item carries a non-empty `err` and a climbing `tries`, so "queued" and "failing" are distinguishable without guessing from a message string, and the existing per-mount probe in `GET /api/mounts` is where it lands — that handler already fans out one worker per mount and joins with a timeout, so this adds a field to a payload the Mounts page is polling anyway rather than a second poll loop. **Rejected scraping the rcd log**, which was the obvious cheap route: the log is free-form, rotates, interleaves every mount's lines, and would tie a UI state to rclone's message wording — the structured queue is a stable contract that already distinguishes the two states. **Rejected blocking or erroring the write itself**: the whole point of `CacheMode: "full"` is that a write completes at local speed, and a Drive mount that made every save wait on an upload round-trip would be unusable; the honest model is that the save succeeded locally and the sync is a separate, observable thing. | Owner call 2026-08-04: build it with the Drive work rather than after it. A read-write cloud mount whose failures are invisible is worse than a read-only one that says so, and the failure mode is silent data loss from the user's point of view — they saw a successful save. |
| D220 | **`nolocks` stays as-is; `localocks` was evaluated and rejected, and database files on a mount stay unsupported on macOS** | D198 added `nolocks` to the macOS kernel mount options because go-nfs registers no NLM program while macOS enables remote locking by default, so a real `fcntl` lock — which SQLite and DuckDB take on **every** database-file open, including read-only opens — hung against an absent lockd. `nolocks` converts that hang into a fast failure, which is strictly better, but it *is* a failure: database files on a mount are unopenable on macOS, not merely slow. `mount_nfs`'s third setting, **`localocks`**, would handle lock requests on the client instead of forwarding them, which fits the topology exactly — this is a loopback mount, so the only processes needing to coordinate are on the one machine — and would keep the no-hang property while letting those opens succeed. It is a one-line change to `NFS_MOUNT_OPT`'s `ExtraOptions` in `shell/mounts/config.py`. It is **not** being made. | Owner call 2026-08-04: reading a database out of Google Drive is a very unlikely scenario and is not worth optimising for. The supporting reasons are worth recording so this is not revisited on a hunch. The blast radius is far smaller than it first appears: this is about opening a **database file**, and DuckDB reading Parquet or GeoTIFF off a mount takes no `fcntl` lock and is completely unaffected — which is essentially every template in the built-in set. Read-write is separately a bad idea on Drive whatever the lock mode, for reasons that have nothing to do with locking: `CacheMode: "full"` plus Drive's lack of partial writes makes every commit a whole-file re-upload of the entire database, uploads are async and non-atomic so a mid-upload failure leaves a torn file at the remote, and WAL mode needs `-wal`/`-shm` siblings kept consistent while `-shm` is a shared-memory mapping that does not behave over a network filesystem. So `localocks` would only ever have unlocked *reading* database files — a genuine but narrow win, affecting all backends rather than Drive specifically. Recorded as available and deliberately unspent, not as an oversight; it also could not be verified from this project's Linux dev machines, where FUSE handles POSIX locks locally and none of this reproduces, so taking it would have meant a macOS-only change validated only on the DMG. |
| D222 | **A mount probe that TIMES OUT no longer reads as "disconnected": the fast verdict is written before the slow `os.listdir`, so only a listdir that actually FAILS can downgrade it** | `mount_state` (`shell/mounts/lifecycle.py`) ran its whole probe on a thread, `join(timeout)`'d it, and fell back to `"disconnected"` when nothing had been written — so "we didn't hear back in 3 seconds" and "the mount is broken" were the same answer. That is fine for S3 and wrong for Drive. Measured on a real Drive mount on this machine: a cold subdirectory listing takes **18.27s**, a root listing once `VFS_OPT`'s `DirCacheTime: "30s"` (`shell/mounts/config.py`) expires takes **35.45s**, and a warm root takes 0.00s — against `PROBE_TIMEOUT = 3.0`. So a perfectly healthy Drive mount reported disconnected, which the fs-listing endpoint turns into a **503**, and because the dir cache expires every 30 seconds it recurred forever rather than settling. Confirmed live. The fix restructures `probe()`: `_is_mounted`, `_mount_wedged` and rcd membership already classify every genuine failure mode (wedged, stale, unmounted, not-served) and are all cheap, so `out["state"] = "mounted"` is published from those alone and the `os.listdir` runs afterwards, able only to *downgrade* via the existing `except OSError` branch. A probe that outruns the timeout therefore degrades to the fast verdict; a listdir that raises still reports disconnected; the `probe_io=False` health-monitor path is unchanged. | Owner call 2026-08-04. **Rejected simply raising `PROBE_TIMEOUT`**, which is where everyone reaches first: it is paid per **healthy** mount inside `GET /api/mounts`' join budget, so buying Drive its 35 seconds would make the mounts list hang for every S3 user behind one unresponsive daemon — trading a false red dot for a slow page. **Rejected a per-backend timeout** (a longer budget for `type == "drive"`): it still pays real wall-clock on the slow case, it needs a backend lookup inside the probe that the probe currently does not need, and it only moves the cliff — a cold Drive listing has no upper bound we know, so any number picked is a guess that eventually reproduces this. The principle worth keeping is the general one and it is not Drive-specific: **a timeout is the absence of evidence, not evidence of failure.** The probe's job is to catch the states that positively indicate breakage, and it already does that without any I/O. **Amended after review — the first version of this was wrong in a way worth recording.** It claimed the listdir "can only downgrade via the `except`"; a readdir that never returns never reaches any `except`, so the join expired with `"mounted"` already committed. The exposure was real: with rcd alive and listing, `ismount` true, and `_mount_wedged`'s `lstat` served from the dentry cache, **every** fast check passes while the only call that touches the network blocks forever (laptop suspend, VPN drop, dead DNS) — a green dot for the whole outage, with `broken_mount_error`'s Reconnect advice never firing, where the pre-D222 code at least said disconnected. Second-order, each poll (~30s per mount) stranded another probe thread in an uninterruptible syscall that never exits: unbounded accumulation, nothing logged. The fix is to distinguish "listdir finished" from "listdir never answered" — `_claim_listdir`/`_release_listdir` record per mountpoint whether one is outstanding and since when, a probe that finds one already out reports `mounted` under `LISTDIR_STUCK_AFTER` and `disconnected` past it (logging once per episode), and it never starts a second listdir into the same blackhole, which caps the thread leak at one per mountpoint. **The residual cost is accepted, not eliminated:** for up to `LISTDIR_STUCK_AFTER` a genuinely unreachable backend still reads as healthy. That window is the price of the whole decision and it is bought deliberately — a slow listing is *far* more common than a blackhole (every Drive mount pays the slow case every 30 seconds as the dir cache turns over, while a blackhole needs the network to actually die), so optimising the common case and bounding the rare one is the right trade. The threshold is what bounds the exposure, and it is set by measurement rather than taste: 60s is ~1.7x the 35.5s worst-case healthy listing, low enough that a real outage surfaces within two polls and high enough that no measured healthy listing trips it. Lowering it trades false reds for faster detection; that dial is the whole tuning surface and it is deliberately the only one. | 
| D223 | **The user's OAuth `client_id`/`client_secret` reach `rclone authorize` through the ENVIRONMENT (`RCLONE_<BACKEND>_CLIENT_ID`/`_SECRET`), never on argv** | `rclone authorize <backend> <client_id> <client_secret>` accepts them **only positionally** — there is no flag form — which would put a live OAuth client secret on the child's command line. On Linux `/proc/<pid>/cmdline` is mode `-r--r--r--`: readable by **every** local user for the whole life of the child, which is however long the user takes over a consent screen. That is exactly the invariant `_authorize_argv`'s docstring exists to hold, and the same one the S3 secret key and the rcd auth secret are already kept off argv for (they go over rcd's loopback HTTP body instead). `/proc/<pid>/environ` is mode `-r--------` — owner only — so the environment preserves the property while still reaching the child. Verified empirically against rclone v1.74.4: with `RCLONE_DRIVE_CLIENT_ID=TESTID12345` exported, the OAuth redirect carried `client_id=TESTID12345`; with nothing exported it fell back to rclone's shared `202264815644.apps.googleusercontent.com` — the client ID being retired (D219). Passed via `subprocess.Popen(env=...)` as an **overlay** on `os.environ`, since the child still needs `PATH`/`HOME` to run at all. | Owner call 2026-08-04. **Rejected argv**, which is what a reader who has not checked the `/proc` modes will "simplify" this back into — hence the comment at `_client_credential_env` citing the two modes explicitly, and a test asserting the secret is absent from `_authorize_argv`'s output while present in the spawn env. **Rejected driving rclone's `config/create` interactive state machine** to hand the client over in-band: it is the same rejection D219 already made for the token, for the same reasons (more moving parts, more sensitive to rclone version) and it would buy nothing here. The secret is also **cleared off the long-lived `_ActiveAuthorize` record** as soon as the attempt finishes, in a `finally` so every branch clears it — the identical bounded-lifetime discipline already applied to the token in `job.out`, and for the identical reason: that record deliberately outlives the attempt so the status endpoint can report the last outcome, and a credential left reachable for the life of the process is what turns a later crash dump or an added diagnostic into a real leak. | 
| D224 | **The Mounts page presents ONE provider-driven "Add storage" picker over six providers — Drive, Dropbox, Box, AWS S3, S3-compatible, public buckets — and OneDrive and iCloud are deliberately not among them** | The page had three disconnected entry points stacked at the bottom: the Add-mount form, a bare "Sign in to Google Drive" link, and an "Add a custom S3 remote" link. Both links read as footnotes, so the honest description of the old surface is that a user could not tell what was **possible** — and any provider added to it would have been invisible however well it worked. It is replaced by a grid of provider cards, each stating its setup cost up front ("One-click browser sign-in" versus "Needs a Google API client"), each opening its flow in the existing `Modal`. **Dropbox and Box are included because they are genuinely free to add:** the server-side work is one registry entry each (backend type, label, no extra params), they reuse the same authorize child, poll, cancel and rollback path as Drive, and — the actual reason they are cheap — rclone still says "Leave blank normally" for their client id/secret, so neither inherits Drive's console trip. They are explicitly never shown client fields; making the UI "consistent" there would invent setup work that does not exist. | Owner call 2026-08-04. **Rejected OneDrive**, which looks like the obvious fourth: `rclone authorize onedrive` does not finish the job, because OneDrive's config asks a **post-consent drive-picker question** (which drive/site of the several an account may have), and answering it programmatically means driving rclone's `config/create` → `config/continue` interactive state machine with `opt.nonInteractive` — precisely the machinery D219 rejected for the token path, now with a multi-step conversation to maintain against rclone version changes. It is not architecturally blocked; it is a different and much larger piece of work, and it is recorded as deferred rather than impossible. **Rejected iCloud** on three independent grounds, any one of which is disqualifying: rclone rates the backend **Tier 4 (experimental)**; it requires the user's **real Apple ID password** typed into our UI rather than an OAuth consent, which is a posture this project should not take whatever the storage; and its trust token **expires every 30 days**, so the mount would break monthly with the only remedy being to re-enter that password. **Rejected keeping the old links alongside the picker** as a migration courtesy: two ways to reach the same flow is the ambiguity this entry exists to remove. The Add-mount form stays exactly as it was — this is about the **setup** surface, not the mount list, and conflating "connect a provider" with "mount a folder from one" is what made the old page hard to read. | 
| D225 | **Cancel on a browser sign-in does not resolve until the server's `in_flight` drops, and a start refused because one is already running offers a Cancel of its own** | Two halves of the same defect: the client treated a cancel as DONE the moment the kill was accepted. `terminate()` only starts the teardown — rclone's callback server holds `127.0.0.1:53682` until the watcher has reaped the child — so restoring the sign-in button there handed the user a retry that 409s on the sign-in they had just cancelled. An accepted cancel now returns a THIRD outcome, `standdown`, distinct from `cancelled`: the modal stays up with Cancel disabled ("Canceling…") and a second, shorter poll (`oauthStandDownTick`) waits for `in_flight` to clear. That poll deliberately reads a settled failure as `cancelled` rather than surfacing it — the server's own error string there is "the … sign-in was canceled", i.e. the user's own click, and banner-ing it tells them off for it — but still reports `ok`, because consent can succeed and the remote be created inside the gap before the kill lands, and dropping that would leave a real remote invisible until a reload. The mirror case: a `start` refused with "a sign-in is already in progress" cleared `connecting`, which took the Cancel button away with it, so the message told the user to cancel something nothing on screen could cancel — the only exit was the 300s server timeout. The form now ASKS the status endpoint whether one is genuinely in flight (never matching on the error text) and, if so, offers "Cancel that sign-in" — a deliberately separate path from `cancel`, since there is no local poll to stop and no outcome to report: whatever that attempt creates belongs to the name IT was started with. **Rejected** killing the child on unmount, which would abort a sign-in the user may be mid-consent on in another window. |
| D226 | **`replacing` is derived from whether the remote actually exists, never from the client's `replace` flag** | `replace` is PERMISSION to overwrite; `replacing` is what the rollback reads to decide "this remote was the user's, do not delete it on a failed sign-in" (D219). Conflating them broke on an ordinary UI sequence: the Replace checkbox stays ticked while the user edits the name to a free one — the checkbox only stops being RENDERED — so `replace: true` arrived for a name that did not exist, the brand-new remote was marked `replacing`, and a failed `config/create` skipped `_delete_remote`, leaving exactly the half-created remote the rollback was added to prevent. Fixed on the SERVER, which already reads the remote list one line earlier for the collision 409, so the authoritative answer was already in hand and no non-UI caller can get this wrong either; resetting the checkbox client-side would have left the same trap open for any other caller. |
| D227 | **The Sessions sub-app (Claude Code session inbox) ships as bundled html+py content on a second builtin zip mount, not as a native React sub-app** | The fourth sub-app (after apps/explorer/learn) is the Claude Sessions triage inbox — ~2.2k lines of already-working fused-render content (`core_apps/sessions/` in the repo: inbox.html + runPython .py targets scanning `~/.claude/projects`; the learn content moved beside it to `core_apps/learn/` at the same time). It reuses learn's entire shipping mechanism (D123), generalized: `BUILTIN_MOUNTS` in `shell/mounts/automount.py` now drives N builtin zips (`learn.zip`, `sessions.zip` → `ensure_builtin_mounts()`), each mounted read-only via rclone `:archive:` and gated in the UI by a per-mount `*_mount_ready` config flag. The shell renders `${mounts_root}/sessions/inbox.html` at `/sessions` chrome-free, exactly like learn (StatView `variant="learn"`). **The one thing learn's precedent didn't cover: writes.** The app persists triage state (`triage.json`, `session_names.json`); the shipped copies write them to `~/.fused-render/claude-sessions/` (honoring `FUSED_RENDER_HOME`), never next to the scripts — the mount is read-only, and state must survive app upgrades that replace the zip. | Rebuilding in React was rejected for v1: it re-implements a polished working UI for no user-visible gain, and shipping the fused project dogfoods the platform's own authoring story (runPython bridge, params URL sync). Known accepted cost: the inbox polls via `fused.runPython` every 5s, a fresh subprocess per poll — fine at ~300 sessions; a native FastAPI route + React rebuild is the v2 path if this becomes core or the transcript count makes polling hurt. `analyze.py` deliberately keeps its fire-and-forget `claude -p` spawn (its cwd fallback moved off the read-only mount to `~`). fcntl locking is guarded for the Windows payload (no-op there). |
| D228 | **`--plugin-dir` is passed unconditionally — the `claude --help` capability probe D216 introduced is deleted, not moved or cached** | The probe ran inside `export_app_env`, i.e. on the main thread BEFORE uvicorn binds its socket, and it allowed the call 30s (`subprocess.run(..., timeout=30)`) while the desktop supervisor gives the entire child 20s to become ready (`supervisor/core.py:_READY_TIMEOUT_S`, set two weeks earlier in an unrelated refactor, never cross-checked) and then kills it, retries three times, and shows a startup-failure dialog. Two independently-tuned constants: the child's allowance for one subprocess was 1.5× the parent's allowance for the whole server. In the field this is not hypothetical — `claude.exe` is a ~279MB self-contained binary that auto-updates roughly weekly, and the first run after each update pays a Windows Defender first-touch scan: a measured 53.4s startup, confirmed with py-spy sitting in exactly this call, and a fake-shim experiment where a 25s shim made `wait_until_ready(20s)` give up at 20.2s while the same child answered at 28.9s. **The dialog fired for a server 8.7s from healthy.** Considered and rejected: (a) moving the probe off the readiness path fire-and-forget like `_AiSession.prewarm_default` — correct in shape, but it leaves the var unset during the cold window, silently degrading early sessions to D185's fallback, which is the exact silent miss D216 exists to remove; (b) persisting the answer keyed on binary path+size+mtime — pays the cold cost once per CLI update rather than once per process, but adds a cache-invalidation surface to keep right forever; (c) raising either timeout — picks a new arbitrary number for a call whose real distribution is bimodal (0.5s warm, ~1min cold). All three keep a probe whose answer is a foregone conclusion: `--plugin-dir` has shipped since the plugin system itself (CLI **2.0.12, 9 Oct 2025**; its only changelog entries, at 2.1.74/2.1.76 in March 2026, are *modifications* to an existing flag), on a binary that auto-updates weekly, ten months before D216 added a probe for it. Deleting it removes `_claude_bin`, `_supports_plugin_dir`, `_PLUGIN_DIR_SUPPORT` and the `subprocess` import from `skill_plugin.py`, leaves `export_skill_plugin_env` filesystem-only (so its position in `export_app_env` stops mattering), and takes with it a real bug: a probe TIMEOUT was caught by `except subprocess.SubprocessError` and cached as `False` — read downstream as positive evidence of non-support — silently stripping the skills from every session that server spawned, contradicting both docstrings, which said a run failure means "cannot tell". The accepted trade: an install old enough to reject the flag now loses every chat rather than just the skills (an unknown option makes the CLI exit before the turn starts). That population is ~nil and shrinks weekly; the fallback worth building IF it is ever seen in the field is a spawn-time retry-without-the-flag, which tests the real thing instead of a help-text substring and costs nothing on the happy path. Tests: `test_the_export_never_spawns_a_subprocess` pins the pre-bind path as filesystem-only (the scar), and `test_the_real_cli_still_takes_the_flag` shells out to the actual CLI — now the only thing standing behind the assumption, so it fails in CI rather than in a user's session if the flag is ever renamed or dropped. |
| D229 | **The `git` template becomes a VSCode-style Source Control GUI — branch management, staging, stashing, committing and pull/push — rebuilt IN PLACE, which overturns the locked GT-11 "Read-only, always"** | **GT-11 is overturned, on the owner's decision.** Read-only kept the view honest and left it a dead end: every question it answered — "this file is modified", "this is staged", "you are 3 commits behind" — ended in *"now go somewhere else"*, which is the one thing a view that already knows the answer should not say. The reads are unchanged and still never write, never fetch and never take an optional lock; what changed is that `ops.py` sits beside `log.py`. **In place, one mode**: a second `git_scm` template was rejected because two modes for one repository is a switcher the user has to learn a rule for, and history is not a different subject from staging — it became a *section* of the reorganized view. **Two modules, not one op-switch**: a reader that also mutates has no honest place to draw its validation line, and "what can this template do to my repository" must be one file to audit rather than a grep for verbs across an 800-line reader. **Scope**: stage/unstage/discard/stash stay restricted to the opened path by the same `:(literal)<rel>` pathspec the reads use, and the write rule is deliberately *stricter* than the read rule — the reader lists a rename with one side in the scope (GT-7, because "this file left the folder you are looking at" is the change the view exists to show) and lists a collapsed ancestor `dir/`, and neither may be *changed* from here, because listing a change and changing it are different acts. Rejected: repo-wide mutations with a warning (the scope is the whole premise of the view, and a warning is not a boundary). `branch_checkout` is exempt and repo-wide by nature — a branch IS a repository concept — which is expected, not a hole. **Commit is index-based and the UI is honest about it**: `git commit -m` with no pathspec, ever. The rejected alternative `git commit -- <paths>` looks like the scoped answer and is a different operation — it bypasses the index and records the *working tree* for those paths, so a file deliberately staged in one state gets committed in another; a silent data surprise is worse than a visible limitation. The price is that a commit from a `pkg/`-scoped view carries whatever is staged elsewhere, so the reader reports `staged_outside {count, paths}` (collected inside the *existing* unscoped `_status` walk rather than by a second `git status` — GT-7 measured that call once and it should not be paid twice) and the view warns *"N staged change(s) outside this scope will also be committed"* and lists them. Empty-message and nothing-staged are the module's own refusals, because git's are a malfunction-sounding sentence and a wall of status text respectively. **No history rewriting**: no `--amend`, no `reset --hard`, no rebase, no force push, `branch -d` and never `-D`, `pull --ff-only` with a non-fast-forward as a *refusal pointing at a terminal*. Rejected: offering merge/rebase buttons on divergence — both take a decision on the user's behalf that has no undo (a merge writes a commit they did not ask for, a rebase rewrites commits they already have). `git clean` never gets `-x`: an ignored path is where `.env`, virtualenvs and build trees live, a scale of loss unlike "throw away the edit I just made" and one no confirmation can warn about, since those files are by construction invisible in this view. A test greps the module for the forbidden spellings so a future change that needs one is a conversation. **Confirm before anything destructive**: exactly three ops can lose uncommitted work (`discard`, `discard_all`, `stash_drop`), the module names them in one constant the view mirrors so an op added later without being classified ships without a confirmation *loudly*, and each is its own addressable call — never a side effect of a safe one. The confirmation is **inline in the view, not `window.confirm`**: the template renders in an iframe where a native dialog is at best chrome the view cannot place and at worst suppressed, and inline puts the question next to the row it is about. Its pending state is a URL param, so a refresh reproduces the question instead of dropping it. Discarding a *staged* change is two explicit calls (`unstage` then `discard`) composed by the view behind one confirmation, because `restore --worktree` restores from the index and a single call would put back the very content being discarded. `branch_delete` is deliberately unconfirmed — `-d` cannot lose work, git's own refusal is the safety step, and a second one trains people to click through. **Non-interactive by environment stays** (`GIT_TERMINAL_PROMPT=0` + askpass knobs, plus `GIT_EDITOR=false` so a conflicted `stash pop` is an error rather than a wait with no end): pull/push work with a credential helper or ssh-agent and otherwise fail fast, because a prompt nobody in an iframe can answer is a hang. **`GIT_OPTIONAL_LOCKS=0` is DROPPED for mutating commands** and kept for reads — it only ever suppresses locks git takes *optionally*, so carrying it on a write would state a promise the module cannot keep and would suppress exactly the opportunistic index refresh that makes the `git status` right after a mutation accurate. **A push from a branch with no upstream sets one** (`--set-upstream <remote> <branch>`, labelled "Publish branch") rather than surfacing git's "use --set-upstream" advice, which is a sentence no GUI button can act on; it is never a force — it can only create a ref that does not exist, and a remote branch that does exist with commits we lack is refused as any other non-fast-forward. Rejected: failing with git's advice (a dead end in a GUI) and silently pushing to a guessed remote when several exist with no `origin` (there is no defensible guess, so the answer is "no remote" and no button). **Mount-backed repos stay refused for the write path too** (GT-4/MD-11): the gate is the UX, the module is the guarantee, and a hand-written `?_mode=git` URL must never reach a *mutating* git call across an rclone/NFS mount. Branch names are validated by `git check-ref-format --branch` rather than a hand-rolled regex (the rules are git's and change with git), with the one rule that cannot be delegated — a leading `-`, since `check-ref-format` has no `--` terminator — checked first in Python. **Amended after review, four findings that all had the same shape — a value carrying less information than the decision made from it.** (a) **A name GIT produced is untrusted input too.** git echoes refnames and remote names verbatim out of files inside `.git`, and those files are content: a hand-written `.git/HEAD` makes `symbolic-ref --short HEAD` print `--upload-pack=cmd`, a hand-written `[remote "--receive-pack=cmd"]` makes `git remote` print that, and both flow into `fetch`/`pull`/`push` argv — one click of Push, local command execution, and the view reporting success. Neither is reachable through git's porcelain, so such a repository arrived as a tarball or a shared drive (`clone` does not copy config). The leading-dash rule is now applied centrally to repo-derived names and the whole repository is refused, not the bad name filtered out (using the *other* remote silently would hide that it is malformed); `--` was also added before the `push --set-upstream` positionals, the one command that lacked it. Both guards are kept rather than either — depending on one alone is exactly how a single missing `--` becomes that bug. (b) **A stash is addressed by commit id, not only by index.** `stash@{n}` means "the nth entry right now" and any `stash push` — this view, another tab, a terminal — renumbers every entry, while a confirmation can sit in the URL indefinitely; a bounds-checked index would therefore irreversibly drop a different, still-wanted stash. `log.py` now emits `%H` per entry and `ops.py` verifies it, refusing with "that stash moved, check the list again" — deliberately NOT repairing by finding the sha's real index, because the list the user read is stale and silently acting on a redrawn one is the same surprise in a new costume. (c) **A discard confirmation names its section** (`discard:staged:<path>` vs `discard:worktree:<path>`): an `MM` file is in both lists, both rows built the same key, and the CHANGES row's ↩ — whose question mentioned only the unstaged edit — was composing the staged form and destroying the staged version too. Rejected: keeping one key and disambiguating by entry state, which is what produced the bug. (d) **Every network command names its remote and refspec**: a bare `git push` means whatever `push.default`/`remote.pushDefault` say, which can be every matching branch, or a different remote than the success message names. Also from the review: `ask` is cleared after every mutation; the composed staged-discard says so when its second half fails ("the change was unstaged, but discarding it failed: …"); the view mirrors ops' scope rule so a row the module would refuse ships no buttons; both stream-matched git sentinels read stdout AND stderr; and `_brief` picks git's `fatal:`/`error:` line rather than the first three, after a first-attempt `pull` pushed "Not possible to fast-forward" past the cut and downgraded a specific refusal to a generic one. SPEC §33 GT-11..GT-17. |
| D230 | **A `.py`'s environment is the FOLDER's, declared in `pyproject.toml`, built by `uv sync`, and stored centrally under the home dir keyed on the folder's path — per-file PEP 723 headers are no longer read, with no migration path** | A breaking change, taken deliberately and in one step. Under D172 every `.py` carried its own complete dependency list and got its own venv, so two scripts in one folder wanting `altair` built the same environment twice, a page calling five scripts showed up to five progress rows and five cancel buttons, and nothing about the arrangement was reproducible on another machine. The declaration now lives once per project root (the app folder, a template folder, else the **topmost** ancestor holding a manifest — topmost, not nearest, so a stray `readers/pyproject.toml` cannot shadow the app's), every `.py` beneath it shares one venv however deep it sits, and `uv.lock` makes the resolution reproducible. **Central storage, keyed on the folder's absolute path.** Follows MD-7 — derived state goes to `~/.fused-render/venvs/<sha256(abs path)[:16]>`, user data travels with the file — and it is also the only way to guarantee the venv and the uv cache sit on one filesystem, which is what makes uv's hardlinks dedupe instead of silently falling back to full copies. An in-folder `.venv` was rejected outright: for a core template it would be destroyed on every release by `core_templates.py`'s rmtree+os.replace swap, costing a full re-download of numpy/pyproj/imagecodecs/pypandoc-binary each upgrade — and rmtree over a 30k-file venv is slow and fails on Windows while the geotiff and zarr daemons hold files open. `.venv/` is nonetheless added to `app_git._GITIGNORE`, because a user running `uv` in their own terminal will create one and `commit()`'s `git add -A` would sweep it into app history. **The path is hashed as given, not `realpath`'d** — a deliberate divergence from MD-7's canonicalisation. Move-or-rename → a fresh, clean environment is a requested feature, and collapsing symlinks would defeat it; the dangerous direction (two folders colliding on one key) stays impossible either way. That makes orphaned venvs a normal consequence rather than a leak, so `projectenv.gc()` runs once at server startup, off the event loop, and reclaims any venv whose sidecar names a path that no longer exists. A venv with no readable sidecar is left alone: it may be an install in flight, and deleting one out from under a running worker is worse than leaking it. **Staleness is a digest, never an mtime chain.** Upstream's `pyproject → uv.lock → pyvenv.cfg` mtime comparison is simply wrong for templates: `copytree` uses `copy2`, so every release stamps a template's manifest newer than its venv and would force a resync of byte-identical dependencies at each upgrade. A `.fused-source.json` sidecar inside the venv records the sha256 of the `pyproject.toml` it was built from; a mismatch is the signal. The MANIFEST only: `uv.lock` is an OUTPUT of `uv sync`, so folding it in would make the environment's own side effect a reason to rebuild the environment — and, worse, hashing the lock *instead* (on the reasoning that it is the resolved truth) meant a dependency ADDED to `pyproject.toml` changed nothing, the venv read as fresh, no install was offered, and the run failed later on an ImportError with no loader and no explanation. A user must never have to run `uv sync` by hand to fix that, since doing so writes an in-folder `.venv` and diverges from the home-dir store. The cost in the other direction is a resync for a comment edit, which is a fast no-op through uv's cache. For the same reason the worker syncs BARE rather than `--frozen`: uv honours a current lock as-is and re-resolves only what a manifest edit moved, whereas `--frozen` turns that edit into a lockfile error instead of reconciling it. It is deliberately NOT routed through D212's repair machinery — an edit is not corruption, so it must not spend the one-rebuild-per-process budget nor be bounded by it. D212's probe, marker-stamp identity check and repair discipline are otherwise **untouched**; only the key that names the venv directory changed. **The app-satisfies fast path survives, but only for UNLOCKED projects, and only when the declaration is non-empty** (an empty `[project]` table is PY-17: nothing to install means the app's own interpreter, not an empty venv without the bundled stack). A `uv.lock` is a request for exact resolution — the user committed specific versions so the folder resolves the same way elsewhere — and satisfying it "near enough" from whatever the app happens to ship is the one thing a lock exists to rule out. Without a lock the trade is the same one it always was: skip a multi-hundred-MB download the app has already paid for. **No migration tooling, on purpose.** A `fused-render migrate-env` command that unioned every header under a folder into a manifest was built and then deliberately removed: the union is a *guess* at a dependency set the user never wrote (two headers declaring `numpy` and `numpy>=2` have no correct automatic merge), it would have to be trusted without review on the one occasion it runs, and it buys a one-time convenience at the cost of a permanent code path and a permanent expectation that headers are still half-supported. Recorded here for the reason D177 exists: an unwritten deliberate omission is indistinguishable from a bug, and the next reader would otherwise "fix" it by adding the tool back. What replaced it was the error itself — a file carrying a header was REFUSED, the refusal naming the `pyproject.toml` to create and quoting the dead block's packages. **OVERTURNED BY D233:** that refusal is gone and a leftover header is now silently ignored, which this row argued was never an option. The argument was right about the failure mode and wrong about the stakes — see D233 for why a pre-release product with no installed base does not buy that protection. | **Supersedes** the parts of D172/D173/D174 that describe per-file headers: D172's "no baseline is unioned in" survives verbatim (a folder's manifest is its complete list), D173's "sharing one venv across a folder is deliberately not built" is exactly what this reverses, and D174's self-managed `DAEMON_VENV` is unchanged — the geotiff and zarr_aoi daemons still own their own environments, because the built-in executor honors no declaration of ours at all. **Known accepted risk:** a user who runs `uv run` themselves gets an in-folder `.venv` that VS Code will auto-select, so their editor resolves imports against a different environment than the app uses; there is no clean cross-platform answer, the gitignore entry limits the blast radius, and the inspector reports which interpreter the app actually chose. **Deferred:** a UI "Reset environment" action, and a "this folder moved from X" note in the loader. **Every existing user app with a header stops working until its folder gains a `pyproject.toml`.** This repo has no changelog file — GitHub releases are cut with `gh release --generate-notes` (`.github/workflows/release.yml`), so the release notes ARE the merged PR titles. The user-facing description therefore lives in `docs/usage.md` § Dependencies (with the breaking-change callout), and the PR that ships this must carry `BREAKING` in its title so the generated notes say so on the release page. |
| D231 | **Without `uv`, a folder that declares dependencies gets a clear refusal — no `python -m venv` + pip fallback** | D230 makes `uv sync` the builder, and the builder it replaced (`ensure_requirements_venv`) degraded to `<python> -m venv` + pip when uv was absent. That fallback is deliberately NOT restored. It cannot honour a `uv.lock` — pip has no notion of one — so on exactly the machines with the least tooling it would resolve its own versions and produce a DIFFERENT environment than the one the user committed, silently, while PY-16 promises the opposite. It would also be a second builder to keep correct: its own marker discipline, its own sidecar digest, its own answer to the half-built-directory case, all exercised by nobody in CI and by almost nobody in the field. A wrong environment that looks right is worse than an error that names its own fix. **Scope of the loss is small and stated in the error:** every script whose folder has no `pyproject.toml` still runs on the app's own interpreter with the whole `[bundled]` stack (PY-17), which is most of them; only folders that declare their own dependencies are affected. All three packaged builds ship uv (`build_dmg.sh`, `build_linux_appimage.sh`, `build_windows_installer.ps1`), so the population this can reach is a source checkout without uv — which also cannot run `uv lock` to produce the lock the templates ship. `_resolve_script_python` case 4 still answers READY without uv, on purpose: readiness is a question about the INTERPRETER, and refusing there would take the PY-17 path down for a capability most runs never use. The refusal happens in the worker, at the point it is actually true. | Considered and rejected: (a) failing at startup when uv is absent — punishes every PY-17-only user for a capability they may never touch; (b) a pip fallback gated on "only when there is no uv.lock" — the lockless case is precisely where uv would have WRITTEN a lock, so the fallback would leave the folder permanently unreproducible while looking like it worked. |
| D232 | **The `fused` pin moves off the S3 wheel URL to the `fused==2.9.3b3` PyPI pre-release** | The direct-URL pin existed for one reason: the clone flags `deploy.py` sends (`--allow-clone` / `--no-allow-clone`, `fusedio/fused` #351) had landed on main with no release carrying them, and `2.9.3b2` — the newest thing on PyPI — exposed neither, so cloning deploys and *every* repoint failed. 2.9.3b3 carries them, and also carries the `workflow_venv` work (`fusedio/fused` #354) that this branch's upstream half went in as, so the stopgap has served its purpose. The string stays duplicated in three places (`deploy.PINNED_FUSED_REQUIREMENT`, pyproject's `[fused]` extra, its `[bundled]` list) with tests holding all three equal — D78's arrangement, unchanged. | A version pin beats a URL on every axis that matters here: it resolves from any index rather than one S3 bucket, it is cacheable and legible in `pip freeze` and in a `uv.lock`, and it survives the bucket being reorganized. Still a `bNN`, so the exact `==` is load-bearing — a range specifier would exclude the pre-release outright. `allow-direct-references` stays enabled in `[tool.hatch.metadata]`: it costs nothing and the next unreleased upstream landing will want it. Move the pin to `2.9.3` when the final ships. |
| D233 | **A leftover `# /// script` header is an ordinary comment — not read, not refused, not reported** | Overturns the refusal D230 shipped with. `engine.run_python` no longer detects headers at all: `projectenv.has_script_header`, `header_dependencies`, the PEP 723 regex and the `ScriptHeaderNoLongerRead` error are deleted. A file carrying a block runs exactly as it would without one — the folder's `pyproject.toml` is the only declaration (PY-16), and a folder with none runs on the app interpreter (PY-17). | The refusal existed to turn a confusing failure (an ImportError for a package the user can see they declared) into guidance, and it was not worth its cost. It fired only when the folder had NO manifest, so the likelier half-migrated case — manifest written, one stale block left behind — passed straight through it; it never delivered the guarantee it was justified by. It also refused files that were fine: a block with no `dependencies` key, or one whose only dep is marker-scoped away here, declared nothing yet was still rejected — making a file's fate platform-dependent, passing on Linux and failing on macOS. Against that, this is a pre-release product with no installed base to migrate, so the guidance was buying protection nobody needed. Deleting it retires a permanent transitional branch on the run path and a regex scan of every source file. The cost is real and accepted: a stale header now fails at import with nothing pointing at the rule that changed. |

| D235 | **The CHAT half of this decision is OVERTURNED by D237** — there is one chat template, on both kinds of target, named `claude`; the "directory-only *by binding* — no gate needed, and no new code to get wrong" argument below is void for it, and it now ships a `condition.py`. **The HISTORY half is OVERTURNED TOO (2026-08-13)** — the `history` template is DELETED, its 47 file keys with it, and the per-file timeline is now a section of the folder-scoped `git` view (a commit list scoped to the open target; selecting a commit renders the file at that commit via `GET /api/git/show`, nothing materialised on disk). What survives of this row is the `annotate` deregistration and the 47-key authored-file set, which is now `claude`'s binding alone. `git`'s half stands for a NEW reason and after an interruption: it was widened to every file key once its commit log went away, purely to be reachable while a folder had no mode surface, and is folder-only again now that the preview pane peeks folder rows (SPEC GT-2). The durable argument is not "one story, one mode" but "a working tree is the folder's". *Original decision, kept verbatim below because the reversal and its reason are the record:* **Preview modes split by target KIND: a DIRECTORY offers `claude` (chat) + `git` (repo SCM), a FILE offers `claude_split` (chat with the annotation tools) + `history` (that file's history) — and `annotate` is deregistered everywhere** | Owner's decision, and the goal is the annotation/app_state machinery *while editing a standalone file*: it only ever existed in `claude_split`, which the gate offered for project folders alone, so the one file-side chat (`claude`) was the feature-poor twin — 8 mentions of `annotate`/`shot`/`app_state` in its template against 277, and ~15 agent functions it never had. The second goal was **one template set per kind**: a user should not have to learn why the app view and the explorer show different modes for the same thing. So each of the four moved to exactly one side: `claude` and `git` lost every FILE key (they keep the universal `/` key alone, which makes them directory-only *by binding* — no gate needed, and no new code to get wrong), while `claude_split` and `history` gained the same file keys and KEPT `/`. That last asymmetry is deliberate and is not a hole: their gates already narrow `/` to app + linked-app folders, which is exactly the app-builder view (`App.tsx` `APP_MODES`), so an ordinary folder in the explorer still offers only `claude` + `git`. Rejected: dropping `/` from the pair for a clean dirs/files partition — it would have silently removed the chat and the history from `/apps/<tag>/<name>` and broken app creation, which lands on `?_mode=claude_split` over the app FOLDER (`HomeHero.claudeChatUrl`). The `APP_MODES` pin itself is kept, deliberately: it is a curation of the builder view, not a divergence, now that every mode in it behaves identically to the explorer's. **The file set was reviewed rather than copied**: the union of the keys `git` held (source, config, prose, logs, pages), the keys `claude` held (`.parquet .md .markdown .html .htm`), the image assets `history` already carried (`.svg .png .jpg .jpeg`), plus `.csv .tsv .jsonl .ndjson .ipynb` — 47 keys, one set for both modes, so *wherever a chat is offered a history is too*. Including the record streams **overturns GT-2's exclusion of `.jsonl`/`.ndjson`**: that argument was that a commit log over an append-only stream renders no useful *diff*, which says nothing about chatting or about a version timeline. Deliberately still absent: media, 3D, archives, binary documents (`.pdf .docx .xlsx .pptx`), databases, binary geo, rasters and the compressed tabular compounds. Order was normalized while rebinding — the pair takes `claude`'s slot where it had one, else `git`'s, else sits before the trailing meta-mode, with `history` always immediately after `claude_split` (chat then history) — which also moved `history` ahead of `annotate` on the four keys where it had drifted behind it. **`claude_split`'s left pane renders a FILE in that file's OWN default view** (`/render?path=<template>&_file=<file>`), resolved from `/api/fs/stat` by the same "first non-`conditional` entry" rule the shell applies (`Preview.tsx` `defaultTemplate`), skipping the two chat modes so the pane can never frame the view it lives in. Rejected: naming a template per extension (drifts from the registry the moment a binding changes, and ignores a user override, §16) and `/embed` (nests the target one iframe deeper, out of the annotation layer's reach — the same reason #372 moved the pane to `/render`). Its gate tests `os.path.isfile`, **not `not isdir`**: the loose form also swallows every path that does not exist, which handed a `True` to a nonexistent child of a linked-app folder — caught by `test_app_gates_accept_a_linked_folder`, and the reason "cannot tell" must keep reading as "refuse" (CT-12). **`history` now answers for any file in any work tree, and is READ-ONLY there**: the gate asks git (`rev-parse --is-inside-work-tree` from the file's parent, the `git/condition.py` probe) rather than requiring a fused app, `log` is scoped to that one path (`-- :(literal)<name>`), `snapshot` archives only that file and reports it as `file` (with `entry` only when the file is itself a page, so the view frames anything else through its own default template), and `revert` is **refused by kind** in `history.py` — not merely hidden by the gate — because it writes a commit with the `Fused <apps@fused.io>` identity and resets the working tree, and outside a workspace app the repository is the user's own. That is the linked-app rule (`linked_apps.py`) generalized rather than a new one, and fencing it in the module is what makes the gate's read-only promise true for a hand-crafted call (MD-11, the posture `git/log.py` already takes). App-ness is asked FIRST, so a file inside an app still shows the app's auto-commit timeline rather than being demoted to its own single-file log; app snapshot cache keys are left hashing the dir alone so snapshots already on disk stay reused. `git/condition.py` keeps its file branch even though no binding reaches it now — one stat, and a gate that answers only one of the two shapes is a trap for a future binding. **A file target now gets the app_state disclosure too**, which overturns the previous assertion that the file branch is the plain viewer's prompt verbatim: an un-announced tool never gets called, and a file target now has a pane worth reading back. What the two prompts must NOT share is the *description* of that pane — a folder frames the user's own app, a file frames fused-render's preview OF their file — so the file prompt says whose page it is and that the viewer is never to be edited. **`annotate` is deregistered from all 66 core keys, not deleted**: the tools live in the `claude_split` pane now, but the folder still ships so a user can re-bind it themselves (§16), and a test asserts no core key references it — a stale binding is how a superseded mode keeps showing up. SPEC §7.2, §33 GT-2/GT-3. |

| D236 | **A template whose layout needs width collapses ITSELF with a media query; the shell never offers or withholds a mode on account of how wide the host happens to be** | D235 bound `claude_split` and `history` to 47 file keys as well as the `/` directory key, so two **split-layout** templates now render routinely in hosts they were never written for: the explorer's listing **preview pane** (`PANE_MIN_W = 220`, and its default width is *half* its split container — FS-12), a **Panel pane** dragged freely (§14), and **`/embed`**. Below their layout's floor the two halves squeeze past their minimums and then overflow. **The fix is entirely inside the two template files; the shell was not touched** — no `registry.json` field, no new field on stat's template entries, no frontend filter. Each ships one `@media (max-width: 560px)` block that turns the split into **one view at a time with a toggle**, and the breakpoint is derived from each layout's own arithmetic rather than picked: `claude_split` is `#left` 200 + divider 4 + `#chat` 340 ≈ 544, `history` is `#side` 200 + divider 4 + 320 for a still-usable preview frame = 524, both rounded up to **560**, the number this codebase already uses (`duckdb`/`sqlite` 560, `bundle` 640, `map` 650, `log_studio` 780) — plain media queries in a template are the house idiom, not a new mechanism. **Which view leads differs, and follows what the mode is FOR**: `claude_split` opens on the **chat** (that is the mode's subject; the preview is what it is about), `history` opens on the **commit list** (a snapshot has to be picked before there is anything to preview), and in `history` clicking a commit row reveals the preview — placed in the **row click handler, not `select()`**, because `loadLog()` auto-selects the newest commit at boot and would otherwise open the narrow view on a preview with the timeline hidden. **The hidden half of `claude_split` is PARKED, not `display: none`d** (`position: absolute; inset: 0; visibility: hidden; pointer-events: none`): an iframe with no layout box gives its document a **0×0 viewport**, which would make `shotPane` rasterise a 1×1 image (D208) and collapse every element rect the annotation pins are anchored to (§17), so a `display: none` would have converted a hidden pane into two silently broken features. **The inline `width: <split>%` is neutralised differently in the two files, and the difference is a rule, not drift**: `claude_split` carries a hard no-force-flag rule (D146) that a test enforces file-wide, so an inline width there cannot be beaten from CSS and `applySplit()` instead **clears it while narrow and rewrites it from the param above the breakpoint** (the `split` param is never touched, so crossing back restores the user's own ratio with no reload); `history` has no such rule, so `#side` simply gets `width: 100% !important` and `applySplit()` stays unconditional and stateless. `history`'s narrow view is a **body class only** (`body.narrow-preview`, read exclusively from inside the media block and reset on the way back out to wide) — deliberately not a param, since which of two halves a temporarily-narrow host is showing is not state a bookmark should reproduce; `claude_split`'s is the pane-local **`paneview`** param, because there the flip also arms and disarms things. **In the chat-only view a control that acts on the hidden half is ABSENT, not disabled** — `#annbtn`, `#leftmode` and the composer's `.viewshot` pill: a disabled control still claims space and still asserts the feature exists, which is a worse lie than not offering it. `.viewshot` is the interesting one because it still **works** from there (the parked column has a real viewport), so it is hidden under the stronger rule — *a view that shows no preview offers no features of the preview* — rather than because it is dead. Flipping away from Preview also **resets both armed states** (`annSetMode(false)`, `setViewShot(false)`): an armed annotate mode with no visible frame swallows clicks in a document the user cannot see, and an armed shot silently attaches a picture of something invisible to the next message. Two things stay reachable from both views: the **Chat ⇄ Preview toggle** (it is the navigation between the views, not a feature of either) and the **annotation chips** (they are chat content — the user's own words, about to be sent). Shipped in the same change: a **left-pane view picker** for `claude_split` (a pane-local **`leftmode`** param + a `<select>` in the `#anntools` strip, sourced from the single `/api/fs/stat` call the pane already makes, hidden below two offerable views, unknown values falling back silently per PT-9) — the file's left view was previously whatever stat ranked first with no way to change it, and a narrow host makes "which one view am I looking at" a question worth answering. **What was REJECTED: having the shell filter split-layout modes out of narrow hosts.** (a) A pane's width is **dynamic** — the listing pane defaults to half its container, so on a wide window the split fits perfectly and the mode *should* be offered; a host-based ban is simply wrong there. (b) A width-based filter makes modes **appear and disappear from the switcher during a divider drag**, and can yank the **active** mode out from under the user. (c) It needs **per-template width knowledge in the shell** — a new `registry.json` field plus a new field on stat's template entries, which today carry only `mode`/`path`/`icon`/`conditional` (PT-8) — and it would then have to be applied in three separate hosts (`ListingPreviewPane.tsx`, `PaneModeMenu.tsx`, `/embed`). (d) **User templates (§16) would never get it**, whereas a media query in the template works for them for free. SPEC §7.2 PT-15. *(Follow-up, 2026-08-08: every `claude_split` in this row now reads `claude` on disk — D237, later on this same branch, deleted the plain full-width chat template and renamed this one into its place. Nothing decided here changed: the breakpoint arithmetic, the parked hidden half, the `paneview`/`leftmode` params and the absent-not-disabled rule are all the same file. The name is the only edit, and it is recorded rather than applied because the row's reasoning is dated to when there were two chat templates and the `claude_split` spelling is what made "which of the two" answerable.)* *(Amendment, 2026-08-08, same branch: **the breakpoint is now 880 px in both templates, raised from the 560 above.** The mechanism, the parked half, the `paneview`/`leftmode` params and the absent-not-disabled rule are all unchanged — only the number, and the reason it was wrong is instructive. 560 was derived from each layout's **overflow floor** (`claude` 200 + 4 + 340 ≈ 544, `history` 200 + 4 + 320 = 524, both rounded up), i.e. the width below which the halves visibly break. But this row's own strongest argument — that the listing preview pane defaults to **half** its split container — is exactly what makes that floor the wrong one to pick: half of a 1700 px window is ~700 px (measured: 733), comfortably above 560, so the split engaged in a pane that could hold it without breaking and could not hold it **usefully**, and the user's report was "the split view in preview mode feels odd, it needs to be triggered only when we have more screen space". The new figures come from the columns' minimum **useful** widths: `claude` = `#left` 420 (a framed preview that is still a page) + divider 4 + `#chat` 440 (~60 characters of transcript prose) = 864; `history` = `#side` 200 + divider 4 + a **640** px preview frame (the `bundle` family number — the narrowest frame in which the FRAMED template still renders its own wide layout, where the old 320 was a viewport that template had already collapsed itself for) = 844. Both round up to **880**, so the two split-layout templates collapse together, and each file states its sum in the comment beside the query. Two related fixes shipped alongside: the composer placeholder is now **derived from the target kind** instead of hardcoding "this project" (PT-16); and the chat's `/embed` pane URL gained **`modechip=false`** next to `preview=false`, because the embed's corner mode chip (`Preview.tsx` `.preview-browse-chip`, pinned top-right under `body.embed`) read **"Chat"** — a directory's counterpart mode is this chat — and so offered a **second agent and composer nested inside the first one's pane**, the same nesting `preview=false` already closes, one affordance over. It was invisible to source review: the template's own `#viewbtn` is `display: none` outside the media block and provably stays that way, so only the rendered DOM showed a "Chat" button beside a visible split. PT-15/PT-16 amended in place.)* *(Amendment, 2026-08-13: **the `history` half of this row no longer exists** — `templates/history/` is deleted (see D243) and the per-file timeline moved into the folder-scoped `git` view, so `claude` is the only built-in still carrying the 880 px block and the 844 px sum quoted above describes a template that is gone. The RULE is untouched and is the durable part: a template whose layout needs width collapses itself with a media query, and the shell never offers or withholds a mode on account of the host's width.)* |

| D237 | **Gap (2) below — the ordinary folder's left pane — is OVERTURNED by D239, and the pane half of gap (3) with it: an ordinary folder now gets NO pane and a full-width chat, and its system prompt says nothing about a pane. The DELETE, the rename, the gate (1) and the app-folder/file halves of (3) all stand; the paragraphs below are kept verbatim because the reversal and its reason are the record.** **The two chat templates become ONE — the plain full-width `templates/claude/` is DELETED, `claude_split` absorbs the directory case, and is then renamed to `claude` — which overturns the chat half of D235, including its "directory-only *by binding* — no gate needed, and no new code to get wrong" argument** | **The chat half of D235 is overturned, on the owner's decision.** D235 read the two chat templates as *peers* and gave each one kind of target: a directory got `claude`, a file got `claude_split`. They were not peers. `claude` was the **stale side of a fork**: 21 of its 34 shared `agent.py` functions were byte-identical with `claude_split`'s (its `template.html` a 1:1 fork by its own admission), it had exactly one unique function, and it had missed **six** improvements the other side received — retry/529-overload reporting (an overloaded API read as `Thinking…` with a frozen token count, indistinguishable from a hang), message queueing during a live run (#414), the `_defaults` last-used model/effort scan, skill-invocation log rows, `_commit_turn`'s legacy-sidecar exclude sweep, and app-state-aware cancel. A fork that only ever falls further behind is not a mode, it is a bug with a registry key. Its only unique *feature* was the receiving half of `annotate`'s comment handoff, and that was already unreachable code — D235 left `annotate` with zero registry keys. So it went, and `claude_split` — split layout, annotation tools, app_state channel, screenshots, message queueing — became the single chat template for both kinds of target. **One decision, not two, deliberately.** The delete and the `git mv claude_split → claude` are one act, in that order, and splitting them into two rows would separate a cause from its own consequence: the rename is only *defensible* because there is one chat left (a name saying "split" is redundant when nothing is unsplit, and `claude_split` was becoming the switcher's most-seen label, D236), and the rename is also what silently **repaired** the `?_mode=claude` links in `examples_seed/tutorial/claude.html` and `annotations.html` that D235 had broken — the same file keys the deleted template lost are the ones the survivor now holds. Recording the delete alone would leave those repairs unexplained. **Three gaps had to close before the delete was honest, because the survivor's gate and pane assumed an app folder.** (1) **The gate.** `claude/condition.py` (formerly `claude_split`'s) now accepts **any existing file and any existing directory** — the `<workspace>/<tag>/<project>` + linked-app narrowing is gone, and with it D235's claim that the folder chat needs no gate at all. It was **not deleted**, because one refusal is real: `is_mount_backed`. That makes this a **capability deliberately REMOVED**, not an oversight — the deleted template shipped no `condition.py` whatsoever and therefore *did* offer a chat over an rclone/NFS mount, and an agent turned loose on a mount walks and rewrites the tree through FUSE, which is the same reason every peer gate (`app`, `history`, `git`) refuses those paths. Rejected: deleting the gate to restore the old reach (it would re-open exactly the hole the peers close), and keeping the app-folder narrowing (which would have left ordinary folders with no chat at all — the very capability the delete was meant to preserve). (2) **The left pane for an ordinary folder.** *(OVERTURNED by D239 — this pane is removed, not replaced: see there for why closing the throw was the wrong fix for the wrong problem.)* It used to `throw` (`no app entry…`), which would have put a permanent error panel beside a working chat on every non-app folder. Now an **app folder** frames its app entry as before, and a folder with **no** app entry frames **`/explorer/embed/<dir>`** — the chrome-free navigable shell (LM-4/D39), i.e. a real file browser beside the chat, so "what's in here?" is answerable by looking and the user can walk into a file mid-conversation. D235 rejected `/embed` for a FILE target because it nests the target one iframe deeper, out of the annotation layer's reach; that objection is about a target the annotation layer *points at*, and a folder listing is not one — there is no element of a file browser a pin could mean anything about. **The corollary is enforced, not merely documented**: `annSetMode` refuses to arm whenever the pane is the embedded listing, guarded at the *one* function rather than at each caller (arming has four entry points — the button, the boot default, the `annmode` param, the app menu's exit action — and `annmode` defaults ON while the pane resolves asynchronously, so a check per caller is a check the fifth caller forgets), and the button is hidden outright. (3) **The system prompt.** `_split_system_prompt` branches on `shared/app_entry.entry_html` — literally the same rule the pane uses, so the prompt and the pane cannot disagree about what is on screen — and is decided **per run, not cached**, so a folder being scaffolded into starts being described as a project the moment it is one. Three shapes: **app folder** (previous wording — "this is a fused-render project, its HTML is an app fused-render serves"), **ordinary folder** (the deleted template's folder-scoping paragraph, ported *verbatim* rather than reinvented, plus an honest pane description: the pane is fused-render's own file browser, never to be edited, and `app_state` reports the BROWSER, not the folder), and **file** (unchanged). The app_state disclosure is in all three, for D235's reason — an un-announced tool never gets called. **Accepted breaks and fallout.** An old bookmark carrying **`?_mode=claude_split`** now falls through PT-9's forgiving unknown-mode fallback to the target's default view, silently dropping any `session_id`/`run` beside it: accepted, not mitigated (no alias, no redirect — a rename that leaves a compatibility shim behind has not finished). **No data loss anywhere**, because nothing on disk derives from the template folder name: run dirs and transcripts hang off the hard-coded `fused_render_claude-<uid>` literal, `SHOTS` derives from that, the sidecar maps the *target's* path under `home_dir()/sidecar/` (D205), and the permission server's directories arrive as argv. The legacy co-located sidecar filename **`.claude-split.json`** is deliberately NOT renamed — it exists on users' disks, so `app_git.py`'s `_GITIGNORE` and the mirrored `.git/info/exclude` still name it (see the note at `app_git.py:60`). Two **dead links** in `history/template.html` were fixed as part of this: session resume now navigates to `_mode=claude` (it had pointed at a mode files lost in D235, so PT-9 silently dropped the `session_id` — a "resume this session" row that opens the wrong view is the one thing it must never do), and the comment row — which pointed at the deregistered `annotate` — is now **inert**, deliberately not retargeted at the chat, which has no `comment` param and never grew the handoff's receiving half. A new test extracts every `_mode=` literal from that template and resolves it against the real registry for its own target kind, so this bug class now fails loudly instead of degrading into a wrong-but-plausible navigation. `reader/template.html`'s `META` exclusion set (the modes that are not a view *of the file*) had to name `claude` — the survivor is bound to nearly every key `reader` is, so leaving it out offered "read the chat aloud" in reader's own view picker. And `annotate`'s Send-to-Claude handoff is now unreachable for **two independent reasons** — no registry binding *and* no receiver (`claudeComments`/`claudeReturn` never existed in the surviving template) — so its in-template warning was rewritten to say so; the anchor model and review UI are left standing because they are what is worth keeping if the mode ever comes back. SPEC §7.2 PT-14/PT-16, §17, §24 HV-7/HV-8, §33 GT-2. |
| D238 | **The listing preview pane's SELF target (nothing selected) defaults to a neutral placeholder, not to the folder's first opt-in mode — as a STATE (`activePaneMode` → `null`), not as a selectable mode** | *Revised: the diagnosis and the "defaults to nothing" conclusion stand; the `_none`-entry mechanism is **overturned** by the owner. Making "nothing previewed" a real list entry put **"No preview" in the mode menu as something to pick**, and it never was a view: the gesture for "I don't want a preview" is closing the pane. So `PANE_NONE_MODE`, its struck-eye icon and `paneHasRealMode` are deleted, `paneModeList` returns **real modes only** (and an **empty** list for a self target whose every peer is gate-denied), and the no-default rule moves into `activePaneMode`, which returns **`null`** for a self target unless the lead is the folder's own app (`_app`, or the registry `app` hoisted in its place — `hasApp`, the same lone-app probe the list takes, is what distinguishes "this folder HAS an app" from "`app` merely leads because `_listing` was dropped"). `ListingPreviewPane` renders the existing `.pane-hint` empty state for `activeMode === null`, under the ordinary header. Of the three fall-outs claimed below: (a) is answered in the shared control instead — `ModeMenu`'s "one mode is not a choice" rule holds because that one mode is the active one, so it now renders a lone entry when **nothing** is active, which keeps a `claude`-only folder's chat reachable (PT-10); (b) is void — the state is the pane's resting position, so there is nothing to return *to*, and the reverse gesture is closing the pane; (c) cost a `_none` value round-tripping through `_panelMode` and a mode menu entry that highlighted "no preview" as the current view, which is the defect. A **byte-level consequence, deliberately taken**: `platform/lib/mode-name.ts` no longer needs a `_none` label, so this branch leaves that upstream module untouched.* Original entry follows. Toggling the pane on with nothing selected opened the **chat**: an "Ask Claude about this file…" box and a sessions skeleton as a folder's *default preview*. The mechanism was a two-line interaction, not a bug in either half. The self row (the folder already open on the left) deliberately **drops `_listing`** — re-showing the left half in the right half is redundant — and D235/D237 put `claude` on the universal `/` key right after `_listing`, so with `_listing` gone `claude` simply *was* `modes[0]`, and `modes[0]` is the default. **The drop is kept; the default is what changed.** Rejected: pushing `_listing` back for the self row (it restores the redundancy the drop exists to avoid) and filtering the chat out of the self list (it removes a mode the user may well want on the folder they are in, and PT-15/D236 already settled that the shell does not withhold modes for host-shaped reasons). **The fix is a mode, not a branch**: a pane-only `_none` sentinel ("No preview" — the same `Select a file to preview.` hint the empty pane already had) is pushed ahead of every template mode for a self target, so the existing "first = default" rule produces the neutral landing with no exception written into it. The one thing still ranked above it is a lone-app folder's `_app`: an app folder's *own app* is a preview of that folder, not a tool aimed at it, so the fix is deliberately narrowed to the modes that were surprising. Three things fall out of making it a real entry rather than a `null` default, and they are the reason for the shape: (a) the shared mode menu (`BarMenu`'s `ModeMenu`) renders only at **≥2 entries** (PT-10), and an ordinary folder's self list is often `claude` alone — with a `null` default the header would have shown one lone chat button or, worse, no switcher at all, i.e. a neutral pane with the other modes **unreachable**; (b) the choice becomes **reversible** — a mis-clicked chat has a way back, which a "default is nothing" branch cannot offer; (c) the active-mode highlight, the `_panelMode` round-trip and PT-9's unknown-value fallback all work on it for free (`_panelMode=_none` carried onto a *selected* row is simply a mode that row does not offer, so it lands on that row's own default). The neutral branch **keeps the header**, which the pre-existing `modes.length === 0` early-return did not — that return is now reached only when `_none` is genuinely alone (nothing to switch to, so nothing to keep a header for). The ordering rules moved out of `ListingPreviewPane.tsx` into pure functions in **`listing/pane-modes.ts`** (`paneModeList` / `activePaneMode`) so the default is testable without mounting the pane; the component kept icons, fetching and the `gatesPending` skeleton hold unchanged. **`PaneModeMenu` and `/embed` were checked and deliberately not touched**: they resolve a default for an *explicit* path with no self concept, so `_listing` is present and first there and a directory already defaults to its listing — the surprise was only ever the self row's dropped `_listing`. SPEC §6 FS-11. *(Superseded, owner: the self target now has **no mode picker at all**, so it also has no default to argue about. The header shows the folder's icon, its name and its own primary action; the body is always the hint. Everything this entry decided about the self row is deleted rather than left inert — `paneModeList` no longer takes `self` (so `_listing` is never dropped, since only a SELECTED row reaches it) and `activePaneMode` is back to plain first-wins with an override. The reason the picker had to go is the one this entry already identified and then tried to route around: the peers under `/` are heavyweight opt-ins, so the control could only ever offer a chat from a header whose body says "select something" — a `Choose view` chip pointing at a view nobody came for. Fall-out (a) above is void with no menu; (b) and (c) were about reversing a choice that can no longer be made. See FS-11, FS-16.)* |
| D239 | **An ordinary folder's chat gets NO left pane — full-screen conversation — overturning D237's gap (2); the `app_state` tool is withheld where there is no pane to report on; and the narrow-layout view toggle is renamed "Annotate preview" / "Back to chat"** | **D237 gap (2) is overturned, and the reason is that it fixed the wrong thing.** That gap was written as the repair for code that `throw`ew (`no app entry…`) and left a permanent error panel beside a working chat, and the repair chosen was to frame `/explorer/embed/<dir>?preview=false&modechip=false` — fused-render's own file browser — so "what's in here?" was answerable by looking. The throw was a real defect and that did fix it. The real problem was never the throw: **nothing flowed back from that pane.** `claude/template.html` has no `postMessage` and no message listener, so selecting a file in the browser attached nothing, fed nothing to the composer and changed no agent context; annotate was hard-disabled over it by construction (`annSetMode` refused to arm, the button was hidden — gap (2)'s own "enforced, not documented" corollary is the admission), and the `leftmode` picker was inert for it because neither directory branch populates `paneEntries`. So half the width of a folder chat went to a view that reported to nobody, answering a question the agent's ordinary file tools answer directly. The pane is therefore **removed**, not replaced: an ordinary folder (`shared/app_entry.entry_html` resolves nothing — the same predicate the prompt reads, unchanged) gets no `#leftframe`, no divider, no view toggle, no pane-shot pill, no annotate, and the conversation owns the width. **A file and an app folder are untouched** — split, annotate, pane screenshot, `app_state`, the 880 px collapse, the live-reload loop after an edit — and `entry_html` stays the single shared predicate, so a two-page folder or an app at `sub/index.html` reads as an ordinary folder and gets the full-width chat; that is intended, not a gap. **Deliberately given up:** the `state.url` backchannel. Embed navigation rewrote the iframe's path, so `app_state` could tell the agent which folder or file the user had walked into — the one signal that did flow back, and it goes with the pane. **The mechanism is a designed absence, not a missing element**, and that distinction is the whole implementation risk: the frame's `load` hook is wired at top level, so shipping the markup without `#leftframe` throws a `TypeError` there and aborts *every declaration after it* (the agent poll loop, the pane-shot toggle, the composer wiring), and the boot `catch` cannot even report that, because its own first statement removes the same missing element — the throw lands inside the catch and neither the error panel nor `pushAppLog` runs, leaving a blank page with a working-looking composer. So the markup ships the column unchanged, every declaration initialises against it, and `enterNoPane()` removes `#left`, `#divider`, `#anntools` (a child of `#chat`, so it survives removing the column and would otherwise be an empty bordered row of controls for a pane that is not there) and both pane-shot pills **afterwards** — ordering that is guaranteed rather than hoped for, since the template is one `<script>` and the loader reaches that branch only after `await`ing a fetch. A single `noPane` flag then short-circuits `applySplit`, `applyNarrowView`, `renderAnn` and `annSetMode`; `paneEmbedded` is deleted rather than repurposed, since the condition it guarded is now impossible by construction. **Rejected:** hiding the column with CSS and leaving it in the document — the elements stay live, `shotPane` still rasterises them, the hidden controls stay focusable, and a hidden pane is a pane; and guarding each top-level statement that touches the column, which is the shape whose fifth forgotten guard is the bug. **`app_state` is withheld where there is no pane**, and the switch is the CHANNEL's own existence rather than a second flag: `agent.py` spawns `permission_server.py` with the app-state directory only when `_has_pane`, and the server keys both `tools/list` and its dispatch on having that directory, so the roster cannot advertise a tool the server cannot serve. The spawn line's pre-allowance drops with it (pre-allowing a name nothing can call is a rule about nothing); the screenshot directory's `Read` rule stays, being a rule about a directory rather than a claim that this target can annotate. The tool roster varying by target kind is accepted. `_split_system_prompt`'s ordinary-folder branch loses the whole pane paragraph — the file-browser description and "it reports the BROWSER, not the folder" — keeping only the folder-scoping instruction: a prompt that tells the model what the user can see beside the conversation, when there is nothing there, is a false claim about the screen, and announcing a tool the roster does not carry is worse than not announcing one. **Rejected:** offering the tool and answering with an explanatory error (the model calls it after every edit and burns the 20-second app-state timeout learning the same thing once per turn). **Two cleanliness consequences, both of which were live wrong answers waiting to happen:** `appEntry` is write-once and never cleared, and `entry` is the only field in the app-state payload that distinguishes the user's real app from our own UI, so the no-pane path never sets it; and `APP_STATE_UNREADABLE` enumerated causes including "a project may have no app entry", which is now precisely the condition that produces no pane and therefore no tool to ask — that clause is deleted rather than left to explain a structural absence as a failed read. **`?modechip=false` loses its only producer in the codebase**, so its plumbing is removed from its consumer too (`Preview.tsx` no longer reads it; the corner chip has no opt-out). Rejected: keeping the read as tolerance for a caller that no longer exists — a branch nothing can produce is a branch nothing can test, and if another template ever frames an embed of its own counterpart's target the opt-out returns with that caller. `preview=false` is deliberately KEPT: the listing writes it for itself when the user closes the pane (`listing/pane.ts`), so it still has a producer and still means something. With the folder embed gone, `/embed` is no longer used as a pane by any template, so D235's rejection of it for a FILE target stands unqualified. **Stale params are ignored silently and never stripped** — `split`, `paneview`, `leftmode`, `annmode`, `annotations` on a folder URL open a full-screen chat with no error (PT-9's posture for an unknown `_mode`); rewriting them would break the bookmark's round trip for the day that folder grows an `index.html` and gets its pane back. **The view toggle is renamed** "Preview"/"Chat" → **"Annotate preview"/"Back to chat"**, with the `aria-label` as the same single string: the old labels named the two halves and said nothing about why anyone would move between them, and the preview column is where the annotation tools live — the only reason to leave the conversation for it. It stays NAVIGATION and is **not merged** with the annotate switch, which arms the mode once you are there: one control moves the view, the other changes what a click in the frame does, and one button doing both would arm a mode in the same gesture that reveals the surface. The longer labels are why `#viewbtn` gets `flex-shrink: 0` and the annotate switch gets an ellipsis on its own label, so a 220 px host truncates the mode name rather than overflowing the row or half-hiding the only way back; no CSS force flag was introduced (D146). **Fallout found while in there and fixed with it:** the composer's footnote ("files in this **project**") and the annotation block's preamble ("left preview of this **project's** entry HTML") were both still hardcoded to the app-folder noun after D237 made only the placeholder kind-derived, so a chat about `notes.md` described the wrong kind of thing to the user and to the model; all three now read one writer (`setTargetNoun`) and a test asserts no kind noun is hardcoded in the markup. SPEC PT-14/PT-15/PT-16. |
| D240 | **A freshly opened folder auto-selects its FIRST ENTRY — file or directory — overturning FS-16's original skip-directories rule (`firstFilePath` → `firstEntryPath`)** | FS-16's original rule skipped directories on the argument that a folder's preview would "make opening a folder feel like it had navigated somewhere." That argument mistook the pane's directory case for a navigation: selecting a directory shows a **peek** (FS-11's single-app embed or read-only mini child list), the left half stays put, and nothing about the URL or the open folder changes — so landing on a directory is exactly as harmless as landing on a file. Meanwhile the skip had a visible cost with the default dirs-first sort: the highlighted row was NOT the top row the eye lands on but some file below the directory block, and a folder holding only directories auto-selected nothing at all, opening the pane on the `Select a file to preview.` hint it exists to avoid. So `firstEntryPath` returns the first RENDERED row outright; only an **empty folder** keeps the self target. The `byPath` map parameter survives solely as the "is this path actually on screen" check (its `isDir` reading is deleted, so the signature loosens to `ReadonlyMap<string, unknown>`), and the one-shot TIMING in `Listing.tsx` is untouched — this changes only the answer, not when it is asked. Fallout in prose that leaned on "picks a file": the self-target-is-rare arguments in Preview.tsx / ListingPreviewPane.tsx / FS-11 now rest on the folder being **non-empty**, which is the weaker premise they always needed. SPEC §6 FS-16. **Amendment: the shot YIELDS to a selection that already exists** — "takes no reading of the selection" was about the removed `?sel` URL claim, never about the user, and a row clicked in the provisional scaffold during a slow mount (carried over by `recallSelection`) was being overwritten by row one the moment the resolved listing settled. `selectionClaimed(sel)` in the effect spends the shot and stands down; `autoSelectPath` stays pure, since this is a condition on WHEN to ask, which is the effect's half of the split. |
| D241 | **The `git` view's commit box gets an AI "write this message" sparkle (`fused.ai`), fed by a new bounded READ op in `log.py`** | VS Code's parity feature, and the one thing in this view that is expensive to type. Three things it deliberately is not. It is **not a commit**: it fills the textarea and stops, so the user still reads what will be recorded and still presses ✓ Commit (or Ctrl+Enter) — an AI that both writes and commits removes the only review step there is. It is **not in `ops.py`**: the diff it reads (`op="pending"`) forks `git diff` and touches no ref, index or file, so it belongs with the safe reads; routing a read through the write module's confirmation machinery would say it can lose work. And it is **not the pane's diff budget** — the prompt cap is `MAX_PROMPT_DIFF_BYTES` 80 KB / 1 500 lines against the pane's 400 KB / 3 000, because a diff that is merely long for a human is billed per token and summarises worse, and the truncation is reported so the prompt can say the diff was cut rather than let the model describe a change it half saw. What it reads mirrors GT-14: the staged diff **unscoped**, because `git commit` records the whole index wherever it lives, and a message written from a scoped diff would describe less than the commit makes; with nothing staged at all it falls back to the working-tree changes under the open scope (VS Code's own fallback), untracked files named rather than diffed. Streamed via `onChunk` at `effort: "low"` — a commit message is not a thinking problem — into the same `msg` param the textarea already round-trips, which is what lets a mid-stream repaint pick up where it left off. Failure is a `flash` like any git refusal, and `ai_unavailable` reads as "AI is not available on this server", never a traceback. SPEC §33 GT-18. |
| D242 | **The TEMPLATE this row is about is REMOVED (2026-08-13) — `templates/history/` is deleted and the per-file timeline is now the folder-scoped `git` view's Commits section. The MECHANISM stands where that section inherited it: `git/log.py` bounds its log with `--skip`/`--max-count=limit + 1`, keeps the extra record back as the `has_more` probe, and the view grows the list with a "load more" row rather than page numbers — plus a `capped` signal the row below did not have, so a clamped request cannot offer an endless "load more" that never advances. Everything below about page numbers, date cursors and the append-only pager is the reasoning that section runs on; `history.py::PAGE_SIZE` and its `rev` deep link are gone with the template.** *Original form:* **The `history` timeline is PAGED — 20 commits a page, `--skip=N --max-count=21` with the 21st row kept back as the `more` probe — and the view grows it with one quiet "Show older" row rather than page numbers** | `_log` ran `git log` with no bound. That was tolerable only while an app's own auto-commit history was the whole story; D235 widened the target rule to a FILE or an ordinary DIRECTORY in the **user's own repository**, where the subtree can carry decades of commits — all of them formatted, JSON-encoded and shipped across `/api/run` to fill a spine whose first screen is twenty rows. The page size lives in exactly one place (`history.py::PAGE_SIZE`); the view asks for the next page by `skip` alone and reads `more` off the payload, so the two halves cannot disagree about where a page ends. **The +1 probe rather than a second `rev-list --count`:** one git process instead of two, and at exactly PAGE_SIZE commits there is no overflow row, so "show older" is never offered for a page that would come back empty. **Skip/limit rather than a `before`/date cursor:** the log is newest-first by git's own default and pages are only ever APPENDED, so the window only shifts when a commit lands on HEAD mid-paging — one repeated row, deduped by sha in the view, and the only action that can add one (`revert`) reloads from the first page anyway, the new commit being the newest. **No page numbers and no "back":** a timeline is append-only, so the pager is one row at the FOOT of the spine, styled as chrome rather than as a commit, carrying its own loading state and its own error line — a failed page is reported where it was asked for and leaves the framed snapshot alone. The append repaints the whole list (selection, the `latest` badge and the pager row are all derived from one state, and one writer cannot leave them disagreeing) but saves and restores the scroll offset, so the new rows appear below the reader rather than under them. **The one selection consequence:** a `rev` deep link may name a revision that is not on the first page, so the prefix is selected as given (the backend resolves it) instead of silently falling back to HEAD, and the view adopts the full sha the snapshot returns so the row highlights the moment paging reaches it; a junk `rev` still falls back, being shape-checked first. SPEC §7.2 PT-14. |
| D243 | **REMOVED (2026-08-13) — the mode this row gave the word to is itself DELETED. `templates/history/` (the git commit spine: log, snapshot, revert) is gone, bound to zero keys, and the question it answered — what did THIS file look like at THIS commit — is now a section of the folder-only `git` view (SPEC §33): a commit list scoped to the open target, and selecting a commit renders the open file as of that commit through `GET /api/git/show`, resolved on read out of the object database. Both halves of the "two things deliberately do NOT follow the rename" note are moot with it: nothing writes `~/.fused-render/app-versions/<key>/<sha>/` any more — no `git archive` runs and no tree is materialised, so the `?snapshot=1`/`IS_SNAPSHOT` framing has no producer left, though both it and `mount.py`'s read-only guard over that prefix STAY, because trees an older version already extracted are still browsable by URL, and `?_mode=versions` and `?_mode=history` alike now fall back to the target's default view, which is still PT-9's ordinary behaviour. The mode-label collision guard this row cites (`mode-labels.test.ts`) stays — it catches the shape of the bug, not this pair.** *Original form:* **The `versions` mode is renamed `history`, and the template that already held that name — the sidecar inspector of D96 — is DELETED rather than renamed around** | Two modes were reading "History" on the same keys. `versions` (the git commit spine: log, snapshot, revert — §33/PT-14) is what a user means by a file's history, and `versions` is not what they call it; the D96 template (`.*.json` default, appended to `.html`/`.parquet`) was a read-only merged timeline of the `<ext>.json` sidecar — claude sessions, bookmark history, the last-session card, annotate comments — and by D237 its one interactive affordance, the comment deep link, was already removed, leaving a viewer for a store the surfaces that own it already display in context. So the word goes to the mode that earns it and the other view goes away: `templates/versions/` → `templates/history/` (`versions.py` → `history.py`), `templates/history/` deleted, `".*.json"` falls back to `["tree", "code"]`, and `.html`/`.parquet` lose the duplicate entry while keeping the surviving mode in the slot `versions` held. The collision this closes is the one `frontend/src/apps/explorer/listing/mode-labels.test.ts` was written for; that guard stays, because it catches the shape of the bug, not this pair. **Two things deliberately do NOT follow the rename.** The snapshot cache `~/.fused-render/app-versions/<key>/<sha>/` keeps its spelling — it is a materialisation directory no user sees or types, and renaming it would strand every snapshot already extracted on disk behind a prefix nothing looks under (`server/mount.py`'s prefix check is the other half; both ends carry a comment saying so). And no URL alias is shipped for `?_mode=versions`: a stale deep link falls back to the target's default view, which is PT-9's ordinary behaviour, and a permanent alias would keep the dead token alive in every registry a user forks. SPEC §24 is kept as a tombstone so the HV-* ids other sections cite still resolve. |
| D244 | **Long-running work reports to a SERVER-side job registry, and the shell draws one download manager for all of it — reporting is decoration the work cannot depend on, and cancel is a request the reporter honors** | Pages that start work outliving their own call (a model download, a checkpoint pull, a minutes-long generation) each drew their own progress bar inside themselves. The shell replaces a page's frame on every navigation, so browsing to another file while an 8GB model downloaded left multi-GB of traffic running with nothing on screen to say so — indistinguishable from a hang, and unstoppable short of quitting the app. Three choices make the fix: **(1) the record lives on the server** (`fused_render/jobs.py`), not in the page, because the reporter and the viewer are different documents — a rendered page is an iframe the shell tears down, and may not even be in the shell's browser tab — so the record has to outlive the document describing it; the same registry then answers a detached Python worker POSTing to `/api/jobs` with no second mechanism. In memory, deliberately: this is the live picture, the call log (§31) is the durable one. **(2) Reporting can never break the work.** `fused.trackJob()` is fire-and-forget end to end: no method rejects, a transport failure is swallowed, and the page downloads its model whether or not a single report lands. The one concession to authoring is a single console warning when the SERVER rejects a report (a bad id, a missing title) — silence there leaves an author with a working page and a manager that never shows it. Reports are serialized per handle because they are deltas and two in flight can land out of order, which walks a bar backwards. **(3) Cancel is a REQUEST.** The server has no idea what the work is or which process is doing it, so the ✕ only sets `cancel_requested`; the page reads it back in the reply to the progress tick it was already sending and stops the way it knows how. The row keeps saying "running / Cancelling…" until the work really stops — flipping it to "cancelled" while a four-minute download carried on underneath would be a lie told to look responsive — and a job that never declared itself cancellable shows no ✕ rather than a dead one. The same honesty rule produces `stalled`: a running record nobody has updated in 30s says *"no longer reporting — the page that started it was closed"*, dimmed, still listed, because the reporter is gone and the work very likely is not. Computed on read (a late tick un-stalls it with no timer), dropped after 10 minutes so a dead reporter cannot wedge the list — and dismissible, which is the same rule as "a live row can only be cancelled, never hidden" rather than an exception to it: nothing is reporting, so the row conceals nothing the app could otherwise tell you. What a dismissal then refuses is precisely a LATE TICK and never a fresh start — a tick is a delta or a terminal state, while the opening report a `fused.trackJob()` handle sends is the only one that states `running` outright, so that report clears the dismissal and re-opens the row. A plain time window was tried first and is worse at both ends: too short and a slow poll loop resurrects the row anyway, too long and a page reusing a stable id (the documented re-attach pattern) is invisible for its next real run. **Accepted cost, recorded as the follow-up:** a job whose page is closed goes stalled rather than continuing to show real progress, because nothing is left to report it. The fix is for the WORKER to report — it already outlives the page — and the HTTP contract is deliberately plain JSON on `FUSED_RENDER_ORIGIN` (which every spawned child inherits) so a worker can do it with stdlib `urllib` today. What is not built is a server-side poller that would adopt a status file and report on the worker's behalf: that needs a scheduler and a mapping language, and it invites runaway subprocesses. Placement follows the notification stack's existing rule rather than opening a new surface: the manager is a card in the bottom-right column (§3), between the toasts and the server card, because that column is ordered by lifetime — seconds, minutes, the session — and anything long-lived must sit below anything short-lived or its rows shift under the pointer whenever a "Path copied" expires. `/api/jobs` joins `/api/calls` in the call log's skip list for the reason that list exists: ~160 ticks per download are bookkeeping about a call, not calls, and they would spend the rate budget the real ones need. `fused.trackJob` is a no-op in the hosted runtime rather than an export blocker like `fused.ai`, since a page that reports progress should still deploy — it just has nowhere to report. **AMENDED 2026-08-27 (D562, status bar redesign):** the manager no longer lives in that fixed, floating column -- see D562, which moves it (and the repo-updates card) into a status bar reserving space inside `#main` instead, because a fixed column overlays page content even collapsed. |
| D245 | **A manual edit COMMITS NOTHING — the app-repo history is the scaffold commit plus one commit per completed Claude turn, and nothing else; the debounced committer is deleted** | Editor and file-manager mutations through `/api/fs/*` used to be committed for the user: every write, upload, compress, delete, rename and copy inside an app folder marked its repo dirty and a single background worker (`app_commit_queue`) turned each editing burst into a commit, debounced 1s quiet / 5s max because the code editor autosaves every 250ms. It worked, and that is the problem with it: the timeline it produced was a machine's record of *typing*, not a human's record of *changes*. "Edit index.html" thirty times in an afternoon is not history — it is an undo stack with commit hashes, and it buried the commits that DO mean something (the scaffold, and each completed Claude turn, which are bounded units of work with a description someone can read) under an arbitrary number of pause-shaped ones. The `history` spine (D242/D243) and the `git` view (GT-2) both get better by subtraction: the timeline is short enough that paging is about long-lived apps rather than about one session, and the Source Control view finally shows a working tree the user can stage and commit deliberately, instead of one that had already been committed out from under them a second after they stopped typing. **Deleted, not disabled:** `app_commit_queue.py` in full, the startup/shutdown hooks it needed in `server/app.py`, and `fs_mutate._commit_mutation` with its six call sites — the debounce, the serialization, the requeue-on-cancel and the shutdown flush existed ONLY to make automatic commits survive contention with each other, so with the automatic commits gone the whole apparatus is dead weight rather than a switch worth keeping. **`app_git.py` STAYS** and is unchanged: `init_repo` still gives every new app its boilerplate commit (`POST /api/apps/new`), `app_dir_for` is still the scoping rule `projectenv`, `linked_apps` and `history` ask, and `commit()` remains the helper the Claude turn path mirrors (templates/claude/agent.py, D166) — the module's hard app-dir scoping is the reason a Claude turn in a linked folder still writes nothing to the user's own repository. What is **rejected**: (a) keeping the queue behind a preference — a per-user switch here means every downstream surface (the history spine's contents, what `git` shows as uncommitted) has two shapes to reason about, for a behaviour whose whole defence was that the user never had to think about it; (b) coarsening the debounce instead (commit per minute, per session) — that trades a noisy timeline for an arbitrary one, since a five-minute window is no more a unit of work than a one-second one; (c) committing only "significant" mutations (delete, rename) and not writes — the split is not one a user could predict, and half a safety net is worse than none. **The accepted cost:** an editor mistake in an app folder is no longer recoverable from an automatic commit. That is deliberate — the fused-render editor is not the thing standing between a user and their own file, the editor's undo is, and an app whose changes matter is one the user can commit from the `git` view in the same window. |
| D246 | **The claude chat's transcript renders the turn's SEGMENTS — one collapsible chip per tool call (diff, output, todos), a folded thinking block, markdown prose — and `segments` is authoritative wherever it is non-empty, so the flat `text` field is never rendered beside it. Supersedes POC §6 ("only text turns — and approval cards — render")** | The page received one flat string per turn, so everything that was not prose either vanished (an *allowed* tool call streamed past invisibly behind the spinner; approval cards weren't in the transcript at all, so they disappeared on the next reload) or was silently concatenated into the reply as if Claude had typed it. `agent.py::_segments_from_rows` now returns the turn as an ordered text/thinking/tool record and `renderSegments(container, segments, {typer})` is the one renderer for it — used by the live poll, by history restore and by the re-attach repair path, so a turn looks the same whether it is streaming, restored from disk, or recovered after the frame died mid-run. **Segments are authoritative and `text` is NOT rendered as well:** on a run that carried stream deltas the text segments join back to exactly that string, so rendering both prints the reply twice; `text` survives for the error paths (which produce no segments) and for the token estimate, which must keep counting the same bytes it always did. **Tool input and output go in through `textContent`, never `renderMd`/`innerHTML`** — they are bytes off the filesystem, a command's stdout, an MCP server's answer, and markdown-rendering them would put a `<script>` from a grepped file into the page; the probe suite asserts the payload lands as text and reaches no node's innerHTML. Which chips open by default was a per-tool list here (`Edit`/`Write`); **that clause is superseded by D267** — the transcript now opens exactly its most recent card. The `-`/`+` diff formatter is extracted (`formatEditDiff`) and shared with the approval card, so the change you approve and the change you are shown are formatted by one function. **The typer became re-targetable** (`makeTyper(null)` + `retarget(el)`): only a text segment at the very END of the list is still growing, so when a tool call appears after the streaming prose that prose is final — rendered whole, highlighted once, and the typer moves on or parks. Chips update in place on later polls (a status flip must not snap shut a chip the user opened) and their dirty-key deliberately excludes the input, which agent.py reads only from finalized rows and therefore cannot revise mid-turn — keying on it would re-stringify a `Write`'s whole file every 400 ms. **A thinking block is only rendered when it HAS a body.** Whether the reasoning is on the wire at all is model-dependent on the shipping CLI — measured over the real `out.jsonl` files this template wrote during live runs, `claude-haiku-4-5` streams the trace while `claude-sonnet-5` sends `{"type": "thinking_delta", "thinking": "", "estimated_tokens": 50}` and finalizes `{"type": "thinking", "thinking": "", "signature": "…"}`: redacted, with only the token estimate surviving. The two surfaces always agree inside a run, so there is nothing to recover where it is redacted, and a builder that opened a segment per delta produced the shipped bug — a "Thought for a moment" disclosure that unfolded to nothing. So an empty chunk opens no segment and an empty segment is dropped rather than returned, and the finalized `assistant` row's `thinking` block is read whenever no delta carried text — on its own gate, separate from the text one, because that covers every row set `_history` reads (a persisted transcript has no `stream_event` rows at all, so a restored turn's reasoning previously had no source and never appeared). **Kept as-is, and worth revisiting:** an errored run still drops its whole reply bubble (`runEnding`'s `keepText`), which now discards a record of tool calls that really ran, not just half a sentence. |
| D247 | **`AskUserQuestion` is un-disallowed and renders as an ANSWERABLE question card: the answer rides back on the same `allow` as `updatedInput` = the parked input plus a top-level `answers` record keyed by the exact question text. Supersedes D161's `--disallowed-tools AskUserQuestion,ExitPlanMode` (and the POC's "this chat renders neither") — only `ExitPlanMode` stays off** | The tool reaches the prompt tool like any other request (it is gated on `requiresUserInteraction`, so `auto` and `acceptEdits` escalate it to a card rather than auto-approving — measured, not assumed), and `permission_server._permission_result` grew the one branch that makes it answerable: for `tool_name == "AskUserQuestion"` ONLY, the decision file's `answers` is validated and merged over the parked input as `updatedInput`; everywhere else the input still passes through byte-identical, and `updatedPermissions` is not built on that branch at all. `decide` takes an `answers` param — a JSON string, like `app_state`'s `state`, because params cross into python string-shaped — `poll` returns the recorded `answers` alongside each request so a re-attaching frame can show the choice, and `template.html` routes `tool === "AskUserQuestion"` to `buildQuestionCard`: header chip, question text, one control per option (label bold, description small, all `textContent`), a click answering a single question outright and checkboxes/radios + "Send answer" for a multi-select or a multi-question card, collapsing to "✓ You chose: …". A payload with no answerable question shows itself verbatim with a Dismiss button rather than controls that could only send an invalid answer. | **The wire was spiked before any UI was written, because three shapes look right and only one is** (CLI 2.1.226): a plain `allow` reaches the model as `The user did not answer the questions` — a card the user answered and a turn that learnt nothing; a `deny` carrying the text as its message *does* reach the model, but as `is_error: true` filed under `permission_denials`; `answers` nested inside a question object, or as a `[{header, answer}]` list, or with `options` pruned to the winner, is silently dropped and reads as no answer. What works is a flat record keyed by the **exact `question` string** (not `header`) whose value is the chosen option's **`label`**, and for multi-select the chosen labels joined with `", "` — a JSON list also arrives but 2.1.226 joins it with `","`, which fails the CLI's own label check and downgrades the tool_result to the weaker "follow what they actually say" phrasing. `answers`/`annotations`/`response` are declared fields of the tool's *input* schema, recovered from the binary rather than from `--help` — the same "read the binary, not the docs" caveat D161 already carries, and the same thing that can silently come back. Verified end to end through the real CLI with the shipping (unpatched) server: tool_result `Your questions have been answered: "Alpha or Beta?"="Beta"`, `is_error` unset, no denials, the model's reply `VERDICT: Beta` — and again for a multi-select under `--permission-mode auto`. **`updatedInput` is the parked input PLUS the key, never a rebuilt object:** the CLI re-validates it against the tool's schema, so dropping `questions` is a `<tool_use_error>` the model reports as a broken tool, not a silent no-op. **The blocking prerequisite was that `_permission_result` hardcoded `"updatedInput": tool_input`** and never read the decision file — the answer channel could not exist without opening that, which is why it is opened for exactly one tool name and nothing else: this is the first place the PAGE authors model-visible tool input, so anywhere else `answers` would be an arbitrary key added to input the user already approved. **Fail-closed in both writers, and the validator is duplicated on purpose.** `_answers_from` validates against the PARKED REQUEST's own questions and option labels — every value must be a label that request offered for that exact question — and returns None (⇒ deny with a sentence the model can act on) for a non-dict, an unknown or header-keyed question text, a non-string or non-matching label, a joined answer to a single-select, or a question with no usable option; an *omitted* question is allowed, because the CLI reads it as unanswered and that is true. It lives in `agent.py` (validating the click before it is latched) and in `permission_server.py` (validating before the CLI is told), because that server is spawned standalone by the CLI and imports nothing of ours — so, D146, one test runs both copies over one table rather than a comment asking them to agree. Multi-select is matched by CONSTRUCTION (is the value the `", "`-join of a run of labels in option order?) instead of by splitting on `", "`, because a label may itself contain `", "` and splitting would either accept a label the request never offered or reject one it did; the walk is over reachable offsets, so a question with many options cannot cost exponential time. An answerless `allow` on a question is recorded as a deny rather than forwarded — the model must never be told the user did not answer when they clicked. **Never grantable, never escalatable:** `WHOLE_TOOL_GRANTABLE` deliberately did not grow ("allow all AskUserQuestion in this reply" would answer the *next* question without asking) and a `setMode` is dropped by tool name rather than by trusting the card not to send one, since answering a question says nothing about how much to auto-approve. Every latch invariant of D161 is untouched: the expiry path still shows an abandoned question as unanswered, a double-click still renders the answer that WON the latch (the card shows what `decide` read back, not what was clicked), and a failed send re-enables the controls because the subprocess is still blocked. The card is also the disclosure surface — the question, every label and every description go in through `textContent`, whole, and the `answers` record is built with `Object.fromEntries` for the same reason the leftover dump is (a question whose text is `__proto__` would otherwise vanish into the prototype setter). Pinned by bridge tests over the server's own stdio JSON-RPC plus node probes that run the shipping card builder over a stub DOM, and by a real-browser check (Chrome, both themes) for the things a stub cannot show: radio grouping per question, a `<script>` label rendering as inert text, and no horizontal overflow. |
| D248 | **`ExitPlanMode` is un-disallowed and renders as a PLAN card: "Approve plan" sends a plain `allow` (optionally carrying a `setMode` landing mode), "Keep planning" sends a `deny` whose message agent.py composes from a fixed sentence plus the user's own note. A new `plan` permission mode (picker: "Plan first") maps onto `--permission-mode plan`. Supersedes the `ExitPlanMode` half of D161's `--disallowed-tools AskUserQuestion,ExitPlanMode` (D247 already lifted the other half) and the POC's "this chat renders neither"** | `agent.py` drops `ExitPlanMode` from `--disallowed-tools` — and drops the FLAG entirely rather than passing it with no tools left, which the CLI would read as a tool literally named `""`. `PERMISSION_MODES` gains `"plan": "plan"` (`--permission-mode plan`) alongside the existing three, and `template.html`'s picker gains a fourth `<option>` ("plan first") leading the list. That the CLI touches nothing before a plan is parked is ITS OWN enforcement, not ours, and is unverified here against a live headless run (queued for the end-to-end task) — the prompt tool stays wired exactly as in every other mode, so an ordinary permission card can still surface for some other tool while planning and stays fully answerable if one does; `permChoices` additionally never offers its "Allow, and let Claude decide from here" mode-switch on such a card while the run's derived mode is `"plan"`, so an ordinary card cannot become a side door out of plan mode — the plan card itself is the only intended exit. That derived mode (`_live_mode`) LEAVES `"plan"` the moment a plan allow lands — onto the granted `setMode` when the approval carried one, `"prompt"` (the CLI's own default) otherwise, the same landing the picker write-back computes — because by then the CLI has already left plan mode itself; a guard that stayed on for the rest of the run would be suppressing the affordance for a mode nobody was in. A "keep planning" deny moves nothing, and an ordinary allow with no `setMode` still moves nothing: `ExitPlanMode` is the one tool whose bare approval re-points the derived mode, because it is the one the CLI acts on by itself. `buildPermCard` routes `tool === "ExitPlanMode"` to the new `buildPlanCard`, a third kind of card (not an approval, not a question): it renders `input.plan` through `renderMd` + `attachCodeCopy` — the ONE tool-input payload on any card that goes through the markdown funnel rather than `textContent`, safe only because it is genuinely markdown Claude wrote for the user to read, and because nothing else on the card takes that door — with the disclosure rule unchanged (`leftoverInput` prints anything besides `plan`, and a request with no usable `plan` key falls into that dump wholesale rather than implying one was read). "Approve plan" sends `{decision: "allow", mode}`, where `mode` is the picker's CURRENT choice and only when it is in `SWITCHABLE_MODES` — the same validated channel D161's escalation button uses, so `bypassPermissions` is unreachable from this card by construction, not by convention. "Keep planning" sends `{decision: "deny", note}`; `agent.py::_decide` composes the actual deny `message` itself (`KEEP_PLANNING` = "Revise the plan — the user wants changes.", plus `" The user's note: " + note` when a note was typed) and does so ONLY when `tool == PLAN_TOOL`, so a `note` param riding any other card's deny is silently dropped rather than rewriting its message. The note is bounded (`NOTE_LIMIT` = 2000 chars, mirrored on the page as the textarea's own `maxLength` so an honest user never reaches the cut) and stripped to `\n`/`\t`/printable — a pasted file must not become the sentence the model reads, and control characters that are not whitespace have no business in a JSON string headed for the CLI. The cut is never silent (D241's precedent): a note over the limit gets `NOTE_TRUNCATED` (`"\n[note truncated at 2000 chars]"`) appended to the composed message, rather than being quietly shortened with no record that anything was lost. `ExitPlanMode` deliberately stays out of `WHOLE_TOOL_GRANTABLE` (one plan; "allow all" would pre-approve the next, unseen) and answers on it are still refused by the `AskUserQuestion`-only `ANSWERABLE_TOOL` gate, so a plan card cannot be tricked into the answer channel either. | **Spiked before any UI was written** (CLI 2.1.226, same discipline as D247): a plain `{"decision": "allow"}` is sufficient — the CLI leaves plan mode itself the moment it sees one, emitting `system/status permissionMode:"default"`, and the tool_result the model reads back is "User has approved your plan…"; the plan itself rides back through `updatedInput` byte-identical, because this card authors no tool input (unlike `AskUserQuestion`'s `answers` merge). A `setMode` in that same allow is purely optional and changes only which mode the CLI SESSION lands in once planning ends. **The picker is a separate, second thing the approval has to move, and an approval always moves it:** each turn is a fresh `claude -p --resume` (D161), so the picker's `permission` param is what chooses `--permission-mode` for the NEXT spawn — if it stayed on `"plan"` after an approval that sent no `setMode` (picker parked on "plan" or "prompt", where `landing()` correctly sends nothing to the CLI), the very next turn would silently re-enter plan mode and replan forever, with no card ever explaining why. So `buildPlanCard`'s `send` always calls `fused.params.set("permission", …)` on a landed `allow` — to the granted `setMode` when there was one, `"prompt"` (the CLI default) otherwise — and does so off the AUTHORITATIVE read of what landed (`res.decision`), never off the click, so the losing half of a double-click cannot move the picker for an approval that never actually took. "Keep planning" touches the picker not at all. `deny` + a `message` is what keeps the model planning rather than reading a bare refusal and giving up on the task, which is the one reason this card composes a message at all when every other allow/deny on this bridge (bar D247's answer-refusal) does not. **Why the flag is DROPPED, not emptied:** `--disallowed-tools` with nothing left in it is not "disallow nothing", it is a tool named `""` on the CLI's own parsing — the same class of bug the empty-list case would have reintroduced silently. **Why `plan` leads the picker rather than trailing it:** it is the other end of the auto/acceptEdits/prompt spectrum from "Claude decides" — the CLI is expected to hold off touching anything until a plan is approved — so it reads best first, and `PERMISSION_LABELS`/`SWITCHABLE_MODES`/the four-way agreement test (D146-style) all grew the fourth entry together rather than the picker silently offering a mode the backend does not label. Pinned by bridge tests over the server's own stdio JSON-RPC (plain allow, `setMode` present/absent/rejected per `SWITCHABLE_MODES`, the composed deny message, the note's bounding/truncation-marker/character filter, the `WHOLE_TOOL_GRANTABLE` exclusion, the `ANSWERABLE_TOOL` gate refusing answers on a plan, `permChoices("Bash", "plan")` never offering the mode-switch, the note textarea's `maxLength` agreeing with `NOTE_LIMIT`) and by node probes that run the shipping `buildPlanCard` over a stub DOM (markdown routing including a hostile plan through the real funnel, exactly two buttons and no "allow all", the note surviving as a plain string that never reaches the DOM, re-attach/expiry/double-click latch parity with every other card, and the full landing-mode → picker-write-back matrix: an approval always resets the param, off "plan" onto the granted mode or `"prompt"`, while "keep planning" and a losing double-click move nothing). |
| D249 | **The Hugging Face cache gets a page of its own, read-only, sized by what is actually on disk** | Anything in the app that touches a model — a `transformers` import inside a page's Python, a diffusers pipeline, a template someone pasted in, an `hf download` run in a terminal beside the app — writes into one shared cache under `~/.cache/huggingface` (or wherever `HF_HOME` points), and until now nothing in the product ever named that directory. It grows in multi-GB steps, the app is what grew it, and a checkpoint pulled by a page opened once is still there a month later with nothing on screen to say so. §37 makes it a sidebar entry: one row per cached repo, biggest first, with a click through to the folder. **Read-only on purpose** — evicting a blob out from under a half-loaded pipeline is a different feature with a different set of confirmations, and the row already lands in the explorer, where deleting a folder has the file manager's own confirmation rather than a bespoke one. Three details are load-bearing rather than cosmetic: **(1) the cache location follows huggingface_hub's four-variable precedence** (`HF_HUB_CACHE` > `HUGGINGFACE_HUB_CACHE` > `$HF_HOME/hub` > `$XDG_CACHE_HOME/huggingface/hub` > `~/.cache/huggingface/hub`), because hardcoding `~/.cache` would report "nothing cached" on exactly the machines that care most — the ones with a shared model disk pinned by `HF_HOME`. **(2) Size is measured with `lstat`, skipping the snapshot symlinks and de-duplicating hardlinks by `(st_dev, st_ino)`.** Every file under `snapshots/` points back into the same repo's `blobs/`, so the obvious walk multiplies a repo by its revision count: a page whose entire purpose is disk footprint would be wrong by hundreds of GB on a real cache, and wrong in the flattering direction. The same rule makes the per-row sizes sum to the reported total, which is what lets the header state one number. **(3) The sidebar row is gated on the cache directory existing**, so a machine that has never pulled from the Hub is not offered a page that can only ever say "nothing here" — but unlike the Claude-config gate beside it, this property can flip mid-session (the first download creates the directory), so the probe caches a confirmed *yes* for the session and a *no* for only a minute, and the page's own load refreshes it. **Rejected:** (a) folding this into the Preferences page — it is an inventory that changes on its own, not a setting, and it would have been the one section there that answered differently every time it was opened; (b) scanning on focus/return like Mounts does — that walk is every blob in the cache, and re-walking tens of thousands of files each time the window came back is a disk storm for a page nobody was reading, hence mount + an explicit Refresh; (c) reporting apparent size (following the symlinks) as "what you would download again" — two numbers on a row, one of them not a disk fact, for a page whose one job is disk facts. **Accepted cost:** the page cannot show what a repo is *for* (no metadata is read from the snapshots, and nothing is fetched from the Hub), so a repo id someone does not recognise stays a repo id they do not recognise — the folder is one click away, which is where the config files that would answer that live. |
| D250 | **The AI Models page gets three deletions — a repo, a revision, and a prune — and the revision one is refcounted, not a rmtree** | D249 shipped the cache inventory read-only, on the grounds that "what is on my disk" was the whole question and that evicting a blob out from under a half-loaded pipeline wanted a different set of confirmations. The inventory answered the question and immediately raised the next one: a page whose entire purpose is *this is costing you 40GB* and whose only affordance is a link to the folder is a smoke alarm with no off switch. So the page now manages the cache, in exactly three widening steps — one repo, one revision of a repo, everything unread for N days — and nothing wider. **The revision case is the load-bearing one**, and it is why this is a server feature rather than a delete button over the explorer: revisions of a repo SHARE their blobs, so removing a revision's snapshot directory and everything under `blobs/` it points at would silently corrupt the sibling revision that was pointing at the same weights. The delete resolves every snapshot's links, subtracts the blobs any other revision references, and removes only the remainder — plus the refs that pointed at the deleted commit (a dangling ref makes the next `from_pretrained` resolve to nothing), plus the whole repo folder when the last revision goes, which is what `huggingface_hub`'s own delete-cache does. The same arithmetic drives what the UI SHOWS: a revision's number is its exclusive bytes, with the shared amount stated beside it, because two revisions of a 7GB model differing in a config file are 7GB shared and a few KB each and a row claiming 7GB apiece would be a lie in the one column this page exists for. **Prune is deliberately not a server-side rule.** The dialog filters the listing on screen by last-read time, shows every repo that qualifies with its size and age, and sends the resulting NAMES — so the thing deleted is the list that was confirmed, not a threshold re-evaluated against state the user never saw, and the atime caveat (`noatime` volumes never update it) is stated where the decision is made rather than in a doc nobody opens mid-delete. **The safety rules are three:** (1) a target is a cache FOLDER NAME, validated as one path segment with a known kind prefix and joined onto the server's own cache dir — a path from a request body would have made this an arbitrary-rmtree endpoint; (2) a symlinked repo folder is refused rather than followed, since it points at another disk and deleting through it leaves the endpoint's scope; (3) the POST carries the D3 `X-Fused` guard, because it removes multi-GB directories and a blind cross-origin POST must not reach it. A malformed revision is an error rather than a fallback to deleting the repo — the widest possible reading of the narrowest possible request is exactly the bug this class of endpoint gets. One more rule fell out of building it: resolving revisions means READING ref files, which bumps their atime, which is the signal prune depends on — so the atime is restored after the read, or the page would quietly exclude from the next prune everything it had just looked at. **Rejected:** (a) a delete on the explorer's folder view instead — it has no idea blobs are shared, so it can only ever offer whole-repo deletion, and would present it as an ordinary folder delete; (b) a server-side `POST /prune?days=` — one round trip cheaper and it deletes things the user never saw, since the threshold would be re-applied to fresher state than the list they approved; (c) an undo/trash — the bytes are re-downloadable by definition, so the honest recovery story is the Hub, not a second copy on the disk we were asked to free. **Accepted cost:** a delete raced against a live download can take a blob out from under it. Detecting that reliably means reading `*.incomplete` markers and racing anyway, so the page does not pretend to: the deletion is what the user asked for, named in a confirmation. |
| D251 | **A card says what the model is FOR and how big the model is — read from the cache, never from the network, and absent rather than guessed** | The inventory could say a repo was called `black-forest-labs/FLUX.1-schnell` and cost 23GB, which is two facts short of the question people actually ask: *what is this, and is it the big one?* Both are answerable from the bytes already on disk. **Purpose** comes from a chain of evidence, best first: the model card's `pipeline_tag` (the Hub's own answer — nothing is inferred when the card came down with the weights), a diffusers `model_index.json`, a sentence-transformers marker, the `architectures` head in `config.json`, a `.gguf` file. Every answer records WHERE it came from and the card shows it on hover, because a `pipeline_tag` is a fact while an architecture is our reading of one, and rendering them identically would be overclaiming. **Size in parameters** is summed from the SHAPES in the safetensors headers — the format opens with a u64 length and that many bytes of JSON, so an exact count costs one small read per shard instead of the multi-GB file — and is simply absent for `.bin`/`.gguf` weights, where the only alternative was file size ÷ assumed dtype: a guess dressed as a measurement, in a page whose whole credibility is that its numbers are real. **Release date is the one ask that was refused as posed.** It is Hub metadata, not cache metadata; answering it means a network call, and this app's first principle is that it reads the local disk. What the card shows instead is `added` — the oldest file in the repo, i.e. when this machine got it — which is a different question honestly labelled rather than the asked one approximately answered. **A rule fell out of it, again:** model cards and safetensors headers are reached THROUGH the snapshot symlinks, so reading them bumps the blobs' atime — the signal prune is built on (D250). Every read in the module now goes through one atime-preserving helper, and the metadata is cached per snapshot directory keyed by its mtime (a snapshot is immutable once written), so a Refresh over forty repos re-reads nothing. **Rejected:** (a) fetching the Hub's model index for real release dates, task tags and downloads — it makes an offline page online, and a cache inventory that phones home about which models you hold is a different product; (b) parsing the front matter with a YAML library — the package does not depend on one, and the two keys that matter (`pipeline_tag`, `library_name`) are always plain scalars, so a documented scalar-only reader that degrades to the config.json evidence beats adding a dependency to read two lines; (c) a curated model_type -> friendly-name table — it is a list that goes stale every time the ecosystem ships an architecture, while the architecture SUFFIX convention has been stable for years and degrades to silence rather than to a wrong answer. |
| D252 | **A cached model gets two read-only views — what it is, and how it tokenizes — as ordinary gated templates on the folder** | The AI Models page (D249–D251) could say a repo was 7.5GB of `image + text to text`, and then "open" dropped you into a folder of `model-00001-of-00004.safetensors`. The folder is the thing people have; making it answer for itself needs no new surface, because the template registry already resolves DIRECTORIES through a universal `/` key and already lets a template gate itself per-path with `condition.py` — which is exactly how `zarr_aoi` appears on Zarr stores and nowhere else. So the two views are ordinary templates sitting beside it: **`model_card`** (name, parameters, config, weights table, largest tensors, files — instant on a 40GB checkpoint because the counts come from safetensors HEADERS, quantization arithmetic included) and **`model_tokenizer`** (a textarea, token counts, and the text painted token by token). **Read-only, and no framework:** nothing loads weights or imports torch, which is what keeps opening a model as fast as opening a text file and is the difference between a view and an inference feature. The tokenizer view is the first template here to need a library the app does not bundle, and it takes the documented route — its own `pyproject.toml` (PY-16), installed by the fused engine on first use — with the page SPLIT so the half that needs nothing (vocabulary size, kind, merges, special tokens, read from `tokenizer.json`) works regardless, and the half that needs `tokenizers` explains its absence instead of failing. **The gates are the load-bearing part**, because the universal key means they run on every folder anyone opens, including folders on remote mounts where a listing blows past the mount's timeout: constant-time probes only, and for the `config.json` case one bounded read to confirm it is a MODEL config, since `config.json` is among the most common filenames there is and every project folder in a home directory would otherwise sprout a model view. **Reads restore atime** (HF-15's rule, inherited): looking at a model must not protect it from the next prune. **Rejected:** (a) a new page in the shell for model detail — the folder already resolves templates, and a second surface would have needed its own routing, its own navigation and its own answer to "what about a model outside the cache"; (b) making `model_card` the default mode for model folders — CT-12 forbids a gated template from being a default while an ungated one is in the list, and rightly: the listing is what a folder IS, so the AI Models card asks for the mode by name instead; (c) reading the tokenizer with a hand-rolled BPE — real tokenization is merges, normalisers and pre-tokenisers, and an approximation of it would produce plausible wrong token counts, which is worse than saying the library is missing. **Accepted cost:** under the built-in engine the tokenizer view shows facts but cannot encode. That is stated on the page rather than hidden, and it is the same trade every dependency-declaring template makes. |
| D253 | **The model templates resolve the cache repo themselves, and the tokenizer playground is built for a runtime that keeps nothing** | Two things the first cut of D252 got wrong, both of them the same mistake: a rule written in one place and not the other. **(a) The entry path.** The AI Models cards navigate to the cache REPO folder (`models--org--name`), which holds no model files — they live under `snapshots/<commit>/`. `model_card`'s reader already resolved that (through `refs/main`, the revision a load would get); the tokenizer's gate and reader did not, so the tokenizer view was never offered from the one page built to open models, even for models that plainly have one. Both now resolve the same two shapes (MV-6), at a cost of one bounded read of a 40-byte ref — no listing, because these gates run on every folder anyone opens. The gate and the reader keep **separate copies** of the rule, since a template is scripts the engine runs and cannot import a sibling module (PY-15/D166); the drift is guarded by a test that pins the two to the same answer over a matrix of folder shapes, the same way the AI Models task labels are pinned across their two evidence paths. **Rejected:** making the gate list `snapshots/` to find a revision when `refs/main` is absent — a gate may not enumerate at all over a mount, and "no ref" is rare enough that failing closed costs a view nobody was promised. **(b) The cache that could not exist.** `tokenize_text.py` held a module-level dict keyed by (path, mtime), with comments claiming a keystroke never re-reads the vocabulary. Every `runPython` is a fresh subprocess (PY-6), so that dict was empty on arrival every time — dead code reading as an optimisation, and worse, comments asserting a property the code did not have. Deleted, and the page restructured around what actually costs: the whole-file parse for the facts is done **once** on load, on its own request channel (RH-9's default channel is the `.py` path, so a keystroke would otherwise supersede the facts call and it would never settle), and keystrokes ask for encoding only. **Accepted cost:** the library's own `Tokenizer.from_file` still runs per keystroke. It is Rust and takes tens of milliseconds, the debounce covers it, and the real fix — one tokenizer held open across calls — needs a resident process, which is a different design from a script the engine runs. **(c)** The gates read files too, so they now restore atime like the readers (MV-5), and the mount shim **drops** `os.utime` on a mount path rather than routing it — the atime discipline is a local-cache concern and a kernel SETATTR on the mount is the call that shim exists to prevent. |
| D254 | **One model view, not two: the tokenizer becomes a section of the model card** | `model_card` and `model_tokenizer` (D252) were two gated templates on the same folder, which meant the mode switcher offered two answers to "what is this?" for one model, and a person who wanted to know how it tokenized had to leave the page describing it. They are one page now. The merge is mostly subtraction — the tokenizer template, its gate, and its second entry in the universal `/` registry list are deleted — and what remains is a **Tokenizer section** below the files table, present only when the resolved revision has a `tokenizer.json`. **The layout now has exactly one owner.** `inspect_model.py` already worked out which revision `refs/main` names in order to describe it; the page hands that resolved `root` to `tokenize_text.py`, which therefore resolves nothing at all. The previous commit had given the tokenizer its own copy of that rule and a test pinning the two copies to the same answer — passing the answer instead removes the drift rather than testing for it, and the copy, the ref parsing and the agreement test all go. **The section is inert until someone types**, which is what keeps the card instant: `tokenizer.json` is routinely tens of MB, and parsing it on open is work nobody has asked for yet. The first keystroke asks for the facts and the encoding together (one visit to one file, not two), every keystroke after that asks for encoding only, and a facts-only call never loads the library at all — the previous shape paid `Tokenizer.from_file` twice on open, once for a page-load facts call that discarded the tokenizer and again for the first encode. **The gate gained one marker and one ordering rule:** `tokenizer.json` is now decisive on its own (a tokenizer-only repo is a real shape, and the merged view has something to say about it), and the `config.json` case probes with `isfile` **before** `os.stat` — over a mount the first can be answered from the listing the endpoint already took and the second cannot, so statting first made every ordinary folder open pay a remote round trip for a name already known absent. **Accepted cost:** `model_card` now carries the `pyproject.toml` declaring `tokenizers`, so the folder has a venv under the fused engine where before it ran on the app's interpreter. `inspect_model.py` stays stdlib-only, so the card renders identically either way and the declaration only ever costs the tokenizer half. **Rejected:** keeping `model_tokenizer` as a second mode for folders the card's gate declines — after adding `tokenizer.json` to the markers there is no such folder, so the second template would have existed only to be equivalent. |
| D255 | **The AI Models page gets a Discover tab: the Hub, joined to your disk — and no download button** | The cache inventory (D249–D251) could tell you what you had and what it cost, and had nothing to say about what you did not have. The obvious answer is "search the Hub", and the obvious objection is "a browser tab already does that" — which is exactly why the **join** is the feature rather than the search. huggingface.co cannot tell you that the model on screen is already in your cache, was last read three weeks ago, and opens for free; this page can, because its sibling tab already measured precisely that. So every result carries a `local` verdict — downloaded, **partly** downloaded (blobs with no materialised snapshot: an interrupted pull, and calling it "downloaded" would send someone to a model that cannot load), or not — and the estimated size sits next to the name **before** the decision, on the same page whose prune dialog exists because caches fill up. **Read-only, deliberately.** There is no download call and adding one is a different decision: gigabytes of someone's disk, a progress surface, a resumable transfer, a half-written cache to clean up after a cancel. Every route is a GET, so nothing needs the D3 guard. **The server fetches, not the page** — one place for the token, the timeout, the TTL cache, and one place to audit what this app sends to a third party; the host is fixed so no request can redirect it, `HF_ENDPOINT` is honoured but validated as http(s), the query is `urlencode`d rather than concatenated, and the sort is a fixed set of names so a client cannot pass a raw field through. **Nothing reaches the network until the tab is opened**, because a local file explorer that quietly queried a third party on mount would be a surprise; the caption names the host, typing is debounced into one request, and identical queries inside the window are answered from memory. The **local** half of the join is deliberately outside that cache — the Hub's answer is stable for the window and your disk is not. **Sizes are recovered from the dtype→count map the Hub publishes, with the same arithmetic and the same `≈` the model card uses locally**, so a model cannot read 16GB on one tab and 8GB on the other; a repo without safetensors metadata reports no size at all rather than a guess someone would plan a download around. **Rejected:** (a) deriving the task filters by reversing the glossary — "image generation" and "video generation" are this app's reading of a diffusers `_class_name`, not tags anyone publishes under, so those filters would have returned nothing; the Hub's real tags are listed explicitly in the module that talks to the Hub, and a test pins each one to a label the glossary explains. (b) fetching from the page — it would have scattered the token, the timeout and the retry policy across a component. (c) caching errors — the network comes back, and the next keystroke has to be allowed to find out. |
| D256 | **Two doors per card, and three controls removed: no Prune, no Refresh, and a tab strip that never moves** | Four changes to the AI Models page, all of them about a control meaning one thing. **(a) The name goes to the Hub; "Explore" opens it here.** A repo id *is* a Hub address, and the page it names holds the licence, the full model card, the discussions and every revision — none of which is on this disk. Opening the local copy is a different act with a different destination, so it gets its own control in the card footer rather than competing for the same click. One control could not serve both: a name that sometimes meant "read about this" and sometimes meant "open the local copy" is a coin flip. The same rule now holds on the Discover tab, where the name previously changed destination depending on whether you had the model — the one place the ambiguity was already visible. Explore is exempt from the hover-to-reveal treatment the ✕ buttons get: an affordance you must hover to discover is one most people never find, and that rule exists to keep a grid of delete buttons from reading as a page about deleting. **(b) Prune is gone.** A dialog that selects models by an age threshold is one confirm away from a multi-GB re-download, and the number it selects on — `lastUsed`, i.e. filesystem atime — is not written at all on `noatime` volumes. The dialog printed that caveat, which was honest and did not help: a caveat cannot make a bulk list built on an unreliable signal safe to confirm in one action. What the signal *is* good for survives — "used 4 months ago" on a card is a fact someone weighs before deleting **one** model. The multi-target delete API stays, because a revision delete uses it. **(c) Refresh is gone.** It asked the user to know when a re-walk of every blob in the cache was worth paying for, which is not a judgement anyone has the information to make. The scan runs once per mount, and the one action that changes the cache from this page — a delete — already answers with the fresh listing it just measured. **(d) The Cached/Discover toggle never moves.** With Prune and Refresh gone the page head holds the tab strip and nothing else, so the toggle is in the same place in every state instead of sliding as buttons appear and disappear beside it. **(e) The model card view links back out too.** Same reasoning one level down: that view reads the folder on this disk, and the licence, the discussions and the other revisions are on the Hub page it cannot show — so its header carries a `Hugging Face ↗` link, built in `inspect_model.py` where `_KIND_PREFIXES` already lives (one owner for "which URL does this kind get"), with the id passed through `quote()` rather than f-stringed into a URL. Absent for a folder that is not a cache repo: someone's own checkout has no Hub page, and a link built from a local directory name would point at a stranger's repo. **Rejected:** keeping Prune behind a confirmation with stronger wording — the problem was never the wording; and keeping Refresh "for large caches" — a cache large enough to want a manual refresh is exactly the one where a re-walk is most expensive to trigger by accident. |
| D257 | **The AI API grows a local tier: a runner registry, resident worker processes, and one door for cloud and local alike** | `fused.ai(prompt)` meant one thing — the Claude Code CLI — while local models lived inside apps. `local_chat` carried its own MLX server, dependency declaration, download reporter and curated model list; the image app carried the same apparatus again for diffusers. Two copies of "how do I run a model", neither reusable, and an AI Models page that knew what was on disk but not what was RUNNING. **The seam is the slash.** `/api/ai` already took a `model`; a value containing `/` is a Hugging Face repo id and therefore local, one without is a Claude alias. Not a heuristic — a Hub id is always `org/name` and no Claude alias has a slash — so a page reaches a local model with no new parameter, an identical streaming shape, and no change to any call that omits `model` (which still means Claude, and is what keeps every page written before this working). **A runner is a FOLDER**, holding a `pyproject.toml` and a `worker.py`, and its environment is built by the existing `envinstall` loader (PY-18) rather than by anything new: fused-render's own venv must stay a file explorer's and must not carry a Metal-only dependency into a Windows wheel, and "torch is downloading" is a progress surface that already existed. **The worker contract is four HTTP routes** (`/health`, `/generate`, `/cancel`, `/quit`) on an ephemeral port the CHILD publishes — anything the parent reserved could be taken between its bind and the exec — with a per-worker token in a header. Adding a capability is writing a worker. **One resident model per capability, auto-evicting**, which is arithmetic rather than taste: two 8GB models on a 16GB machine is a swap storm and a swap storm reads as a hang. **Load is a job, not an awaited request** — a cold load is a multi-GB download, so it returns a job id into the existing download manager under a deterministic `sys:ai-model:<repo>`, and generating with a model that is not resident starts the load and fails with that id rather than blocking for minutes or refusing flatly. **Rejected:** (a) running models in the server process — a wedged model would take the file explorer with it, and the app would stop starting on machines that have never run a model; (b) a pluggy/entry-point plugin system — there is no out-of-tree consumer yet, and an in-tree table with the interface pinned is what makes the second backend cheap without building the machinery twice; (c) resolving the runner from the model id — the same repo can be servable by two backends and the choice belongs to the machine, not to a string. **Accepted cost:** text generation is Apple-Silicon-only in this cut (MLX is Metal), so a Windows user sees the capability listed with its reason rather than silently missing; the cross-platform text runner is a new module in this table and nothing else. |
| D258 | **Jobs get an owner, and the server's ids are reserved** | The download manager was built (D244) for work a PAGE runs: it reports, and the ✕ sets a flag the page reads back and honours, because the server cannot know what stopping means. Local inference (D257) makes the server a reporter too, and there the ✕ can really kill the process — same control, two meanings — so a record now carries `owner` (`page` / `server`). Two consequences worth writing down. **The ids are reserved.** Server ids are deterministic so the AI Models page can join a job row to a repo card by name; that determinism means a page could otherwise post `state: "done"` for a download still running, and nothing could catch it. `POST /api/jobs` refuses the `sys:` prefix, and `owner` follows from the id at creation rather than being settable from a body. **Observing needed an API.** `trackJob` only writes, so a page that starts server-owned work had no way to show its progress; `fused.watchJob(id)` is the read half. Named as trackJob's sibling on purpose: D244 chose `trackJob` over `job` because a bare `fused.job(...)` reads as the job rather than as a handle, which still holds — TRACK takes a spec and creates a row, WATCH takes an id and looks at one. |
| D259 | **"Loaded" is said loudly, and "downloaded" is not said until it is true** | Two reports from the first real use of the AI Models page, and both are the same kind of bug: a state drawn from the wrong evidence. **(a) A 7px bullet is not a state indicator.** Loaded is the only thing on this page that costs something *continuously* — gigabytes of RAM, right now — and it was a small quiet dot in the middle of a card, in a grid where every other card looked identical. A grid is read by SWEEPING before it is read by reading, so the loaded model has to survive the sweep: a filled badge beside the name and a colour change over the whole card, in the same green as the sidebar's live dot (one colour means "running" across this app, rather than one per surface). **(b) Pressing Download made the card claim it was downloaded**, seconds into a 4.6GB pull. Two causes, both of them a page believing the wrong witness. The "you have this" set was built from any `models--org--name/` folder — and `huggingface_hub` creates that folder on the first byte — so the check-mark rule now requires a **materialised snapshot**, which is the same partial-vs-downloaded line the Hub result cards already drew. And a *running* pull was not a state the page could show at all, because a weights-only download appears in no worker row: `GET /api/ai/runtime` now reports `downloading` beside `loaded`, which is also what tells the page to read job rows — without it, an 8GB fetch reported progress to a poller that had gone idle, and the manager eventually declared the row abandoned. **(c) The bytes themselves were nonsense** — "10 / 11 B" for that same 4.6GB model — because `snapshot_download` exposes only its outer *file* counter through `tqdm_class` and the runner reported that counter as bytes. Progress is now measured from the DISK, on a one-second poll that doubles as the heartbeat the stalled-row detector needs. **Rejected:** keeping Discover's own cache walk beside the page's and just fixing its predicate — two walks meant two definitions of "on this machine" and a window where the two tabs disagreed about the same repo, so the page's one walk is handed down instead. **Found while fixing it:** the port-handshake file was named per capability, so two overlapping bring-ups deleted each other's published port; it is per bring-up now. |
| D260 | **"Cached" becomes "Local", and the toggle moves into the URL** | Three small changes to the AI Models page head, one theme: the page should say where things are and let you go there. **(a) The tab is "Local".** "Cached" names the MECHANISM — a Hugging Face cache directory — where the user's question is about the thing: what does this machine have. Local/Discover is the pair that reads; Cached/Discover asks you to know what a cache is first. **(b) The choice lives in the URL** (`?tab=discover`, default omitted), the shape Preferences already uses for its own tabs. Bookmarkable is the small win; the real one is the BACK BUTTON — a two-tab page where back leaves the page entirely makes the browser's own undo useless for the only state on screen. This forced a structural change: `/ai-models` is now the one shell route NOT keyed on the navigation epoch. Every other route remounts on any navigation, which is fine for a page whose data is one small GET, and wrong for this one — remounting to change a page's own view state would re-walk every blob in the cache and throw away whatever was typed into Discover's search. The page subscribes to the URL itself instead; arriving from another route still mounts it fresh, because the branches differ in their children. **(c) The captions' destinations are links** — the cache directory opens in the explorer, the Hub host opens in a new tab. The app IS a file explorer, so printing a path as plain text asks the user to copy it into the thing they are already looking at; and the host line exists to disclose who this page queries, which is worth being able to go and check. The host comes from the endpoint the server reports, so a machine pointed at a mirror by `HF_ENDPOINT` names and links the mirror rather than huggingface.co. **Also:** "43 GB" reads "43 GB total", because a bare figure at the end of a list of per-model sizes looks like one more of them. |
| D261 | **Images join the AI API, and the worker contract moves into one module** | Phase 3 of D257: `fused.ai.image({prompt})` renders locally through the same door text already uses. **The reply settles everything but the pixels.** `POST /api/ai/image` returns a `jobId` AND the path and the seed, chosen server-side — so no second endpoint answers "what did I get", the job record needs no result field, and an unseeded render is still repeatable (a seed invented inside the worker and never surfaced would not be). The reply carries the CLAMPED request rather than the submitted one, because a caller echoing its own numbers would mislabel the picture: a 4096² render at 500 steps is an OOM, not a request. **An image WAITS for its model where text does not.** `fused.ai` fails fast with a job id because a chat box must not hang through a cold multi-GB load; an image caller is already watching a row for work that takes minutes either way, so the wait belongs inside the job. Two rows, two truths — the load reports its bytes to `sys:ai-model:<repo>`, the render its steps to `sys:ai-image:<uid>`, one row per render so a second does not overwrite the first. **The worker contract moved to `runners/worker_base.py`.** The first cut had it inline in mlx_text/worker.py; copying that for a second runner would have put the SUPERVISOR'S contract — the auth header, the status file's shape, the state vocabulary, the disk-measured download — in two places, which is the shape of every bug this feature has had. It is stdlib-only both because anything imported there becomes a dependency of every backend forever and because it makes the contract testable on CI, which neither concrete worker is (Metal; several GB of torch) — `tests/test_ai_worker_base.py` drives the real routes with stub callables. **Quantized components are a table, not a heuristic:** which quantized checkpoint replaces which part of which model is the same editorial judgement the catalog makes, and it decides what NOT to download — FLUX.2 klein's base repo carries the ~8GB bf16 transformer that the ~2.6GB GGUF replaces. **Rejected:** streaming `/generate` for images (an artefact is not a sequence; its progress is steps, and those already have a home in the job row); a result field on the job record (the POST reply already knows everything); and inferring the quantized file from the repo listing. |
| D262 | *(Its app-card half is REVERSED by D269 — a card opens the app's entry PAGE, not the folder's listing. Everything else here — the deleted `/apps/<tag>/<name>` route, the dropped links, `preview.png` as a thumbnail — stands, and D269 revives none of it.)* **The `/apps/<tag>/<name>` app-view route is DELETED — an app card opens the app's FOLDER in the file explorer, as a plain listing with no `_mode` — and old links are dropped rather than redirected. A root `preview.png` becomes an app's / folder's thumbnail** | Three changes with one premise: an app is a folder, and fused-render already has a first-class way to be at a folder. **The route was a second namespace for the same thing.** `/apps/<tag>/<name>` decoded against `fused_dir` (with the virtual `linked` tag resolved through the registry over an extra `GET /api/apps/linked-path` hop the route had to hold a blank frame for) and rendered the folder under `StatView variant="app"`: no breadcrumb, the builder's own sidebar, and the mode switcher pinned to an `APP_MODES` allowlist. So the SAME folder had two URLs and two chromes, and which one you got depended on how you arrived — the D235/PT-14 note that `APP_MODES` "is a curation of the builder view, not a divergence" was true and was also the tell that the pin was buying nothing but the absence of `git`/`graph`/`zarr_aoi`, which are perfectly sensible over an app folder. Deleted with it: the router codec (`APP_ROUTE_PREFIX`, `appUrlForFsPath`, `appRouteSegments`, `fsPathFromAppRoute`), `Preview`'s `allowModes`/`appChrome` — reachable only from that variant, including the "Open in explorer" overflow whose whole job was getting back OUT of the route — `BuilderSidebar` and its Recents section, `apps/builder/lib/recents.ts`, and the `linked-path` endpoint that existed solely to resolve that route's `linked` tag. The server's `/api/apps/recents` routes are deliberately LEFT standing though nothing now reads them: removing a store is a separate decision from removing the surface that displayed it. **Old links are dropped, not redirected** (owner call): `/apps/<tag>/<name>` 404s at the server, which is D243's posture for `?_mode=versions` restated — a permanent alias keeps a dead shape alive in every bookmark that has one. **The card opens the PLAIN listing, not `?_mode=app`** — also the owner's explicit call, and the part with a real trade in it. The card used to pin the mode, which meant it chose the view; the folder's listing is the door to everything (read a file, open a chat, see its history, AND the app view, which `templates/app/condition.py` offers for exactly these folders), so landing there costs one click and buys back every other thing you might have wanted. `?_mode=app` therefore has exactly one producer left, the explorer's own **"Open as app"** button (`apps/explorer/lib/app-button.ts`, which is where `APP_OPEN_MODE` now lives) — because that button IS the explicit request for that view, where a card is not. Its target is an ordinary fs navigation now rather than a builder-route URL, and its "half-known identity falls back to the folder's own page" branch survives for an unchanged reason: `tag`+`name` both present is how `link-status` says "this folder is exactly the shape `condition.py` admits", so without them `_mode=app` would resolve to nothing and fall back to the listing the button exists to avoid. **`preview.png` at a folder's root is its thumbnail**, resolved server-side into a new `preview_image` key on the shared `app_listing.app_dict` shape (so linked apps get it for free) and rendered through `/api/fs/raw`. It precedes the live scaled iframe of `entry_html`, which is honest but shows the page in whatever state it comes up in — empty, mid-load, asking for a file — and costs a whole page load per card; an authored still is a statement about what the app IS. Resolved inside `app_dict` rather than by the caller, unlike `entry_html`, and the asymmetry is the whole comment: `isfile` cannot distinguish unreadable from absent, and does not need to, because both mean "no picture, fall back". One exact NAME, not a `preview.*` search — a user adding a thumbnail should never have to work out which candidate wins. The explorer's folder card ranks it above `index.html` in its peek order and renders it as an `<img>` rather than framing a whole embed page for one picture, leaving the one-live-preview-per-card budget alone. **Rejected:** a legacy rewrite for the old route (above); keeping `APP_MODES` as an explorer-side curation for app folders (the same "two answers for one folder" that the route was); and letting the card carry `?_mode=app` "just for apps with an entry" — that is the route's mistake one layer down, a surface deciding a view on the user's behalf. |
| D263 | **A freshly opened folder auto-selects its first PAGE in rendered order, falling back to its first entry — superseding D240's ANSWER while leaving the rest of D240 standing** | D240 replaced "first file" with "first row", on the argument that the top row is where the eye already is and that a directory peek is harmless. Both halves of that still hold; what it did not weigh is that the rows are not interchangeable as PREVIEWS. The pane exists to show something, and in an ordinary folder the page is the one row that renders **as itself** rather than as a listing of a directory or a dump of text — a folder holding an `index.html` is opened to look at that page far more often than at whatever the sort happened to put first, and under the default dirs-first sort that is reliably a subdirectory. So `autoSelectPath` walks the rendered rows for the first page and falls back to `firstEntryPath` unchanged; an empty folder still selects nothing. **Rendered order, deliberately NOT a favoured `index.html`** (owner's explicit call): the order is the one the user is looking at, so the winning row is the qualifying one nearest the top of their table and re-sorting re-answers the question — where a favoured name would select a row that may be scrolled off screen, and would need a tie-break story of its own the moment a folder held two of them. **The row KIND is checked, not the name**: `build.html` as a DIRECTORY is a real shape (an exported site tree), and peeking it in place of the page the user can see is the bug the name-only test would ship — which re-tightens D240's own loosening of the `byPath` parameter from `ReadonlyMap<string, unknown>` back to `{ isDir: boolean }`, the reading D240 deleted, now for a different question than the one it was deleted for. **`.htm` counts here and does NOT count in `lib/folder-app.ts`**, and the split is the answer to each question rather than drift: the app gate makes a claim the SERVER also makes (`app_listing.app_entry`), so two surfaces have to agree to the letter and a stray `.htm` was a real bug (the review finding that produced that module); this picks a preview row, where the registry binds `.html` and `.htm` to the same mode list so a `.htm` previews identically, and refusing it would land a folder whose only page is `page.htm` on some adjacent text file for no gain in correctness. **Untouched, and each for D240's own reason:** the one-shot TIMING in `Listing.tsx`, the yield to an already-claimed selection (`selectionClaimed` — this changes the answer, not when it is asked), the blindness to the URL (a `?sel=` is seeded into the selection at mount and the yield covers it), and `searchAutoSelectPath`, which is not a folder — results re-rank per keystroke and "the top hit" is the whole of what that one means. SPEC §6 FS-16. |
| D264 | *(Partly superseded by D269: the app DESTINATION is back — a folder with a top-level page previews and opens as that page, at every surface. The app CONCEPT stays deleted exactly as decided here: no `app` template, no `?_mode=app`, no `_app` sentinel, no "Open as app"/"Add as app" buttons, registry still read-only. D269 needed none of them — an entry page is an ordinary file, and the explorer has always opened files.)* **The app CONCEPT is removed from the UI: the `app` template mode is deleted, its buttons with it, and linked-app registration goes because nothing is left to register for. A folder is a folder — it opens as its listing** | Owner's call, and taken to both of its aggressive readings when asked. The visible ask was "no more Open as app / Add as app buttons", but a button removed while its destination survives is a feature you can only reach by typing a URL, so **the destination goes first**: `fused_render/templates/app/` and its `condition.py` gate are deleted, `"app"` leaves the universal `/` key, and with it go `?_mode=app`, `APP_OPEN_MODE`, and the whole `lib/app-button.ts` module — `appButtonSpec`, the link-status probe, both bar renderings, the embed corner chip and the `pane-action-slot` portal that carried the button across the divider. **The pane-only `_app` sentinel goes too, and that is the part that would have been easy to miss.** It existed so a folder with no registry `app` binding still got an app preview in the listing pane; with the registry entry gone it would have become the ONLY carrier, and the concept would have survived intact under a sentinel labelled "Preview". `paneModeList` loses `hasApp` and its hoist rule, and the pane previews every folder as its embedded listing. **The in-page entry rule (`lib/folder-app.ts`) is deleted in the same pass, days after it was added** — it existed to make the button's gate agree with `app_listing.app_entry`, a real bug at the time, and there is no gate left to agree with. `isAppEntry` goes as well, and with it the row menu's "Open App" wording: a `.html` row is opened, like every other row. **Linked apps: registration is removed, the registry is kept READ-ONLY, and that split is the judgement call.** "Add as app" was `linkApp`'s only caller, so `link_app`/`unlink_app` and the `POST /api/apps/link`, `/api/apps/unlink` and `GET /api/apps/link-status` routes are all dead and deleted. Deleting `fused_render/linked_apps.py` outright was rejected on two facts about installs that already have a registry file: those folders still list on the /apps hub under the `linked` tag, and dropping them would be silent data loss from a change that was about BUTTONS rather than about the user's folders; and `export_linked_apps_env` still fed `templates/history`, which asked `is_linked_app_dir` before it would `revert` — a linked folder is the USER's own repository, so an unexported env var would turn a safety refusal into a quiet no-op for exactly the folders it was written for. *(2026-08-13: that second reader is gone with `templates/history/` (D243) — the per-file timeline is now the read-only `git` view section, which writes nothing and so has no refusal to make. The module and the export still stand on the FIRST fact alone, the `/apps` hub listing; `is_linked_app_dir` currently has no caller.)* *(2026-08-14: OVERTURNED — the owner asked for linked-app plumbing removed completely, no backward compat. `fused_render/linked_apps.py` is deleted with its tests, `GET /api/apps` lists the workspace walk alone, the `FUSED_RENDER_LINKED_APPS` export and the `appenv.py` helpers (`linked_app_dirs`/`is_linked_app_dir`/`linked_app_dir_for`) are gone, and an existing `~/.fused-render/linked_apps.json` is simply inert — no migration, no cleanup code.)* So the module survives with its readers, `write_entries` is the one remaining way in (tests, and a user editing the JSON), and the `linked` tag stays on the hub for entries that already exist. **Explicitly kept:** the `/apps` hub itself, whose cards open `/explorer/view/<folder>` as plain listings (D262), and `app_listing.py`'s `entry_html`/`preview_image`, which those cards still use for thumbnails — "app" as a WORD for a folder on the hub is not the thing being removed; a second way to LOOK at that folder is. **Noted rather than acted on:** `templates/shared/app_entry.py` now has one template consumer (`claude/app.py`, whose split view frames a folder's page) plus `history`'s per-commit "does this revision render as a page" question, so it stays shared rather than folding into the chat; and the `condition.py` machinery keeps several users (`claude`, `git`, `history`, `zarr_aoi`, `model_card`), so nothing about the gate mechanism changes. *(2026-08-13: `templates/history/` is deleted (D243), so the chat is now `app_entry.py`'s only template consumer — it stays shared anyway, since the server keeps its own copy of the rule and a second consumer is one binding away — and `history` drops off the `condition.py` list, which still has `claude`, `git`, `graph`, `zarr_aoi`, `model_card` and others.)* **This supersedes D262's central argument** — that "Open as app" is the deliberate one door left into the app view, which is why its gate had to be the server's exactly — and the review findings built on it (the `.htm`/dotfile mismatch, the link-status mount refusal, the linked-tag collision carve-out) evaporate with their consumer rather than being reverted. D262's OTHER half stands untouched and is what makes this coherent: a card already opened the folder's plain listing, so removing the app view changes nothing about where a card goes. SPEC §3 (FS-11), §6, §7.2's `/` row, PT-13b, PT-14. |
| D265 | **The AI Models sidebar entry loses its gate — the row is unconditional, and the probe, the session cache and the publish channel behind it are deleted rather than left unused. The empty Local tab answers with a button to Discover** | Reported as "why is AI Models missing from the bottom left". Nothing was broken: HF-8 hid the row on any machine with no `~/.cache/huggingface/hub`, which is most machines that have never run a model, and the reporter's was one. **The gate's premise expired when Discover shipped.** It was written for a page that could only ever READ the cache (D249's read-only inventory), where an entry leading to "nothing here" is a row that wastes a click. Discover (§39) made the page a place you can DOWNLOAD from — the suggested models, through the local runner (HS-1, amended by D258) — so the machine the gate was hiding the page from is precisely the machine that needs it, and the gate's failure mode inverted: it hid the front door because the room behind it was empty. Getting a first model now requires either knowing the URL or downloading a model some other way FIRST, which is a bootstrap the sidebar should not impose. **The whole mechanism goes, not just the condition.** `GET /api/ai-models/status` (one `isdir`), `getAiModelsStatus`, and `frontend/src/shell/aiModelsAvailable.ts` — a module-level cache with two TTLs (a *yes* held for the session, a *no* for 60s because the first download can create the directory mid-session) and a listener set the page published into on load and after deleting the last repo, so a sidebar already on screen would not sit out the TTL with a missing row. Every line of that existed to decide whether to render one `<NavItem>`; with the answer fixed at "yes" it is dead, and this codebase's own precedent (D264) is that a destination with no caller is deleted, not parked. `AiModels.tsx` keeps `exists` — the listing already reports it, and the empty state still distinguishes the two nothings. **The empty state gains a control, because that is what makes the ungated row honest**: "Nothing cached here yet" / "No Hugging Face cache on this machine" now sits above a **Search the Hub** button that switches to Discover. A sentence naming the tab would be an instruction pointing at a strip the user has already scrolled past; the button is the move itself. **Rejected:** keeping the gate and inverting it to "show the row unless the cache is missing AND Discover is unavailable" (Discover is never unavailable — it is a search box); and keeping the probe endpoint for a future caller (there isn't one, and a docstring that says "cheap availability probe for the sidebar entry" beside a sidebar entry that no longer probes is a lie the next reader has to disprove). SPEC HF-8 revised. |
| D266 | **uv's children stop inheriting the app's Python environment, the diffusers runner stops building from git, and a waiting render reads the worker RECORD instead of the worker TABLE** | One user report — FLUX image generation failing in the packaged macOS app — with two independent bugs stacked, and the second is why the first took five attempts to understand. **(a) The environment could not build, and only inside the .app.** `uv sync` was spawned with `dict(os.environ)` (`_env_install_worker`), so py2app's `PYTHONHOME=<App>/Contents/Resources` reached it. uv is a Rust binary and does not care — but a dependency it must BUILD is compiled by a build backend in an interpreter uv creates, and that interpreter inherits the environment: it resolved its stdlib and site out of the bundle, imported the app's frozen `_distutils_hack` in place of the one belonging to the setuptools actually doing the build, and died with `ModuleNotFoundError: No module named 'jaraco.text'`. The traceback names it exactly — bare `_distutils_hack/override.pyc` and `importlib/__init__.pyc` frames (py2app's frozen zip) interleaved with absolute `.py` frames from uv's fresh build env. **Wheels never touch a build backend, which is why nothing else in the app ever hit this**: `mlx_text` declares three PyPI packages, and `diffusers_image` was the only environment in the product with a `git+` pin. Every uv invocation now goes through `_uv_env`, which strips `PYTHONHOME`/`PYTHONPATH`/`PYTHONEXECUTABLE`/`PYTHONSTARTUP`/`VIRTUAL_ENV` — the union of what `engine._child_env` and `supervisor._child_env` already strip, restated rather than imported because this worker must not import `fused_render` (D152) and paired to the backend's list by a test. Safe to strip now for the same reason it was not before: `Contents/lib -> Resources/lib` (D212's symlink) made the bundled interpreter self-locate with no environment at all, and a build smoke asserts it. **(b) The git pin goes anyway.** It existed because FLUX.2 support was on main and nowhere else; `diffusers>=0.39` is the first release carrying `Flux2KleinPipeline`, so the condition its own comment set is met. Beyond the bug this removes an unpinned `HEAD` from a shipped product — any upstream commit could break the runner between one user's install and the next — and a source build on a laptop is minutes of compiling for something a wheel answers. A test now refuses any ` @ ` dependency in a runner declaration; if one is ever genuinely needed, that test is the conversation. **(c) The image row lied.** `_bring_up`'s failure path stamps `state="error"` on the worker AND deletes it from `_workers` in one critical section, so `_wait_ready`'s `current.state == "error"` branch was unreachable: a waiter polling the table could only ever see the model gone, and reported "was unloaded before it could be used" — which reads like a race, so the user pressed generate five times and re-ran the doomed `uv sync` five times (8.5s the first time, ~1.0s after, the git clone being cached). The real error was on `sys:ai-model:` the whole time; the row they were watching was `sys:ai-image:`. `_start_resident` (split out of `load`) now returns the worker record beside the reply, and the wait reads state, error and eviction off that record in ONE locked block — the same lock the failure path writes under, so "table empty, error not yet written" cannot be observed. **"Unloaded" is kept, not deleted**: a record that never errored and was taken away — evicted by another model, unloaded from the page — has no failure to report, and that is what those words honestly describe. **(d) `build_dmg.sh` stops shipping the orphan.** The prune deletes `setuptools`, `pip`, `wheel` and `pkg_resources` but not `_distutils_hack`/`distutils-precedence.pth`, which setuptools installs as top-level names beside the package rather than inside it — so the bundle carried a distutils shim with no setuptools behind it, which is what (a) then imported. Belt and braces: (a) is the fix, this is a shim for a package that is not there. **Not done, and recorded so it is a choice:** each retry still re-runs the whole failed `uv sync` from scratch — `_ensure_venv` raises on the first round's error and nothing remembers that this environment failed a second ago. Worth a short negative cache, but a retry that re-attempts the real work is defensible where a retry that reports a stale failure is not, and the user-facing damage here was the wrong MESSAGE, not the repeated attempt. SPEC AI-2a, AI-9c. |
| D267 | **Exactly ONE collapsible card in the chat log is open — the most recent one — and a click on a card is a permanent per-card override in both directions. Supersedes D246's per-tool `TOOL_OPEN_BY_DEFAULT` list** | The old rule was a list of tool NAMES (`Edit`, `Write`, `TodoWrite`) that opened themselves, and it does not scale with how the agent actually works: a turn that writes four files was four open diffs plus everything the user had opened earlier, so the reply sat below a screen and a half of tool bodies, while a turn that ended on a `Bash` had nothing open at all. Position is the honest predictor of what is still news — the card that just arrived — so `registerCard` opens the newest card and folds the one it replaced, and nothing else is open. **The user's click outranks the policy, forever, in both directions:** a card they opened is never auto-folded, one they closed is never auto-opened, not even while it is the newest. The override is recorded from the SUMMARY's `click` (synchronously, before the disclosure's own default action flips `open`) and NOT from `toggle`, which fires asynchronously and fires for programmatic opens too — so a `toggle` listener cannot tell the user's fold from the policy's. **Overrides are keyed, not held on the element.** The re-attach repair branches and `loadHistory` both rebuild the `<details>` nodes from `segments`, so state on the node is state the user has to re-apply; a tool call is keyed by its `tool_use` id, which is stable across every re-render of that call, and a thinking segment (which has no id) by its index inside a numbered segment container — that survives the 400 ms poll but not a rebuild of its turn, which is the accepted cost of thinking having no identity in the payload. **History replay needs no branch of its own:** turns are restored through the same `renderSegments`, so the last card built is the last card in the log and a restored transcript opens on it alone. `resetCardPolicy()` clears the overrides at the two places that replace the whole visible transcript (the Back-to-chats handler and `loadHistory`), or a card the user folded in one session would come back folded in a session they have never touched. Probes in `tests/test_claude_template_segments.py` drive the shipping renderer over a stub DOM for each arm: newest-only, arrival folding, sticky open, sticky close across a rebuild, an unchanged poll tick, replay, and the reset. |
| D268 | **The once-per-visit history push (D8/PR-3) is spent only on a param write attributable to a USER GESTURE. A write made before this document has seen any interaction — a page seeding a computed default at load — takes D99's coalesced `replaceState` path instead AND leaves the entry pristine (no `fusedParamEntry` flag), so the user's first real change still gets its one entry** | A view that seeds a param during initialization made the browser's Back button permanently unusable. Concretely: an app whose boot path computes a default download folder and writes it with `fused.params.set("dir", …)`. Entry A (pristine, no `dir`) → the view loads → the init-time write PUSHES entry B → the user presses Back → they land on pristine entry A → the shell remounts the view → its boot path seeds `dir` again → pushes B again, truncating the forward branch. The URL bounced straight back, `history.length` never shrank, and there was no number of presses that got out of the view. D8's flag-on-`history.state` design is what makes it a *loop* rather than a one-off: the flag is what travels back with the entry, so the re-seed is always looking at a pristine entry and always qualifies for the push. The principle the old rule was missing: **a param the page computed for ITSELF at load is the as-loaded state, not a step the user took** — a history entry is a record of navigation, and nobody navigated. So it is folded into the entry the user is already standing on, and because `replaceState` leaves `history.state` untouched, the entry stays pristine and the visit's one push is still available for the first thing the user actually does (after which Back correctly restores the seeded default, which is the state they saw on arrival). Detection is the runtime's OWN capture-phase `pointerdown`/`keydown` listeners on its document (plus the target's, when the target is an ancestor shell — a click in the shell's chrome is still the user), setting a sticky `sawGesture` flag. **Not `navigator.userActivation`:** WKWebView is a supported host (§25) and cross-frame activation semantics differ between engines, where a flag we set ourselves from a real event is the same answer everywhere; capture phase so a template that stops propagation on its own controls still counts, and sticky because the question is "has this document been interacted with at all", not "within N ms". Everything D99 owns is untouched — the pending-search overlay, the 400 ms coalescing, the try/catch around every write, the `fused:urlchange` dispatch, the no-op guard — and the seed now takes the *cheaper* path, so the WebKit 100-writes/30 s budget only gets safer. **Rejected:** having views not seed params (the seed is what makes the URL bookmarkable and is the whole point of D8's "URL wins on load"); a per-template opt-out flag (D8 already rejected per-param configuration, and this is the same burden with a worse failure mode — silently trapping Back is not something an author would think to opt out of); and suppressing the push for some fixed window after load (a timer is a guess about how fast a page boots, and a slow `runPython` in the boot path would push right past it). Sibling fix at the template layer: PT-15's `annmode` no-op guard, where a boot default normalising an absent param to its own meaning cost an entry for the same reason — that one is a page not writing what it does not need to, this one is the runtime not charging for what a page legitimately writes. `frontend/src/platform/lib/runtime-params.test.ts` evaluates the shipped `static/runtime.js` against a fake entry stack with a working `back()` and asserts the whole sequence, including the trap. |
| D269 | **REVERSED, on the owner's instruction: a folder with a top-level `.html` IS that page, and every surface that shows or opens the folder shows or opens THAT PAGE. The listing's preview pane resolves a selected folder to its entry page instead of embedding a nested listing, an app card opens the entry FILE instead of the folder, and the server's entry rule widens to the shared one. Supersedes D262's card half and D264's destination half; revives none of their machinery** | The owner's words: *"a folder that has a top-level .html file is an app, and every surface that shows or opens that folder should show/open THAT PAGE — not a listing of the folder."* Two symptoms, one rule. Selecting `task-forge` in a listing filled the preview pane with a nested listing OF `task-forge` — a file table rendered beside a file table, with the thing the folder is FOR sitting in it as one row called `index.html`; and clicking an app card on the apps hub — a card whose picture IS that page and whose title is read out of that page's `<title>` — landed on the same file table. **A card that shows you a page and then hands you a directory is not keeping its own promise**, and the listing is one crumb-click from the page where the page was several clicks from the listing.<br><br>**This reverses a decision that was right when it was made, and the reversal is narrower than it looks.** D262 deleted the `/apps/<tag>/<name>` route and D264 the `app` template behind it; with no "open as an app" left, the folder's listing looked like the only destination there was. It was not. The entry page is an ORDINARY FILE, and the explorer has always been able to open one, so **D269 brings back none of the machinery those two removed** — no builder route, no `app` template (`templates/app/` stays deleted; the `/` registry key keeps its seven directory modes and gains no eighth), no `?_mode=app`, no `_app` pane sentinel, no "Open as app" / "Add as app" buttons, and linked-app registration stays read-only. What comes back is a DESTINATION, not a CONCEPT.<br><br>**Resolve-at-the-edge, not a conditional template mode** — the design choice, and the reason this is ~40 lines rather than a template. The pane RETARGETS the selected row: a directory row is resolved to its entry page and everything downstream (stat → template list → the `_render` iframe → the expand button) runs on the page, unchanged, exactly as it does for a page the user clicked directly. A card does the same with the `entry` the server already reports. Nothing new renders anything; the folder branch that remains is the one for a folder that genuinely has no page. *Rejected: reintroducing a gated `app` directory mode. It would have meant a new template folder, a `condition.py` gate, a new row in the universal `/` key, a hoist rule in `paneModeList` — and a second answer to "which page is this folder" for D264 to delete again the next time the app concept goes out of fashion.*<br><br>**ONE rule, now in three places instead of two, and pinned by a parity test.** The canonical statement is `templates/shared/app_entry.py::entry_html` — `index.html` if present, else the first non-hidden top-level `.html` in NAME order. `fused_render/app_listing.py::app_entry` (behind `GET /api/apps`) was deliberately NARROWER: the single top-level html, with several meaning `None` — "ambiguous, so open the folder". **That divergence is deleted, not preserved**, because it produced precisely the outcome the owner's rule forbids: a two-page app whose card opened a file listing. "Ambiguous" was never a better answer than the deterministic first page — it is the same answer the chat pane, the history template and the auto-select have all been giving for that folder already. Its ONE remaining divergence is kept and asserted: `app_entry` RAISES `OSError` where the shared copy returns `None`, so `two_level_apps` can still tell "unreadable, skip it" from "no entry, list it as a folder". The third copy is the shell's (`apps/explorer/lib/app-entry.ts`), needed because the pane resolves a folder from a listing it already has in hand and no endpoint answers the question; `tests/test_shared_app_entry.py` asks both Python copies the same nine folder shapes, and the bun suite asks the TypeScript one the same questions. *(Not to be confused with `lib/folder-app.ts`, the narrow gate D264 deleted: `lib/app-entry.ts` is a successor on the broad rule, not that module returning.)*<br><br>**Deliberately NOT changed.** Navigating INTO a folder (`/explorer/view/<folder>`) still opens the folder's LISTING with its page auto-selected in the pane (FS-16 / D263) — the owner did not ask for a folder view to become a page view, and "show me what is in here" is a different request from "open this app". `.htm` still counts for that auto-selection and still does not count as an entry page, for D263's own reason. Community/showcase cards still open a cloned app's folder: the catalog payload carries no entry, and guessing `index.html` for a folder the server never resolved would be the one thing this decision is against. SPEC §3 (FS-11, FS-16), §7.2's `/` row. |
| D270 | **A coalesced history write is queued as a key→value DELTA plus the pathname it was aimed at — never as a formed URL. The flush MERGES the delta into the live location; a history TRAVERSAL or a PATHNAME change discards it instead. And `popstate` joins `fused:urlchange` as a notification source, because a traversal changes the visible params while writing nothing. Amends D99, and is what makes PR-4 true off the browser shell** | D99 defers the write up to 400 ms and, as first built, queued the fully-formed URL to replay at flush time. **A formed URL is a snapshot of where the browser was up to 400 ms ago**, and three things move underneath it inside that window: the shell navigates (a breadcrumb click, a tab open), the user traverses history, or another writer edits the very entry we are about to write (`sel` on every arrow-key move, `_mode` on every mode switch). Replaying that snapshot does not land LATE, it OVERWRITES — five bugs, each reproduced in a browser: a pending write dragged a freshly pushed entry's path back to the old view, so the breadcrumb and the address bar disagreed and a reload or bookmark went somewhere the user had never been; a Back inside the window let the flush land on the entry just traversed TO, destroying the pristine entry and inverting the buttons (Back appeared to do nothing, Forward went backwards); the flush reverted concurrent shell writes to other keys; a pane traversal left the top-level `_layout` stale, so a reload or a bookmark silently undid the Back; and a traversal never re-notified the template at all.<br><br>**So the flush is a MERGE, not a replay.** Only the keys `set()` actually touched are remembered, and the URL is recomputed at flush time from the LIVE `target.location.search` — with the raw `_layout=(…)` span preserved byte-for-byte and emitted last (D51) — so everything the runtime did not touch survives by construction. The pending VALUE is real user intent and is kept; only the stale surroundings are dropped. The same delta is what the pending-search overlay applies, computed per read rather than cached as a string, so the overlay and the flush can never disagree about what the URL is about to say.<br><br>**Two conditions DISCARD the queued write instead of landing it, and both say the same thing: the entry it was meant for no longer exists.** A **`popstate`** — a traversal is a LATER and STRONGER user action than the param write it interrupts, and the write was aimed at the entry we just left; dropping the value is not a loss of intent, it is honouring the newer one. A **pathname change** — a merge fixes the case where the SEARCH moved underneath us but not the case where the whole PAGE did, and folding `zz=8` into a view the user navigated to does not land late either, it INVENTS a param that page never had: visible in the address bar, bookmarkable, and carried by every subsequent write. `pagehide` still FLUSHES, so a genuine unload does not lose the write, and a shell tear-down of an embed frame is caught by the pathname check rather than by that flush.<br><br>**`popstate` is the other way params change (PR-4).** `fused:urlchange` covers writes — the runtime's own `set()` and the shell's wrapped `push`/`replaceState` — and cannot cover Back/Forward, which writes nothing. Both route through the same snapshot-diff guard (D46), so a self-set still never notifies twice. It is listened for on THIS window and on the target/ancestor chain, since which browsing context the event fires in depends on whose entry moved, and any of them moving invalidates a write aimed at the old one; the pending write is cancelled BEFORE the notification, so a listener reading `getAll()` sees settled state rather than the value the user just navigated away from. In the browser shell the gap was masked by App's nav-epoch remount (the view is rebuilt and re-reads its params on a fresh document); on a standalone `/render` page, the hosted stub and the WKWebView popover — the hosts D99 and PR-4 exist for — nothing remounts, so the URL was correct while the template's UI stayed stale indefinitely. The layout shells subscribe to `popstate` per embed frame for the same reason: a pane traversal must re-encode the top-level `_layout`, or the reload/bookmark silently undoes the Back.<br><br>**Caught in the same pass:** `Panel`'s push no longer passes `history.state` through, which copied `fusedParamEntry` into the new entry so it was born pre-flagged and the user's next param write silently got no entry (D8). Latent today — `/panel` sets `_fusedParamBoundary` — and asymmetric with `Tabs.pushHistory`, which never did it. **Rejected:** keeping the formed URL and re-validating it at flush time. There is nothing to validate against: a URL cannot say which of its keys this runtime meant and which it merely inherited from a base that has since moved, which is exactly the information a delta carries for free. Every new test was mutation-checked — six mutants applied, each killed by a named test, then reverted. |
| D271 | **The owner's policy on what belongs in the URL: a default the READER COULD HAVE CHOSEN is stamped into it with `replace`; a value the view DERIVES from something the URL does not record is kept OUT of it entirely, and the MODE that produces it is the bookmark. `fused.params.set()` grows an options object — `{history: "replace"}` and `{default: d}` — to make both cheap to say** | Two audits asked for the same two knobs independently, and the policy is what decides which one a given write wants. **A true default belongs in the URL**: `mode`, `sheet`, `offset`, `sort`, `panel` are values the reader could have picked, so the address bar should show the whole state and a copied link should carry it — the stamp just must not cost a history entry, because it is a description of the state the page already loaded in rather than a step anyone took. **A derived value does not belong in the URL at all**: a map viewport, a fitted camera (D272), a canvas size, an auto-stretch range computed from the pixels currently on screen. `autostretch=1` IS the bookmark; reopening it recomputes the same number from the same data.<br><br>**What derived state in the URL actually did**, in `netcdf` and `h3`: both computed a stretch range from the MAP VIEWPORT — which is not in the URL at all — and wrote it back as `stretch` from a data-loaded callback (`netcdf`'s histogram fetch, `h3`'s `moveend` → `viewportStats`). Every pan therefore spent the visit's one entry (D8) on a number nobody chose, so Back landed on the old derivation over the new viewport: a state that was never on screen. Worse, it was a Back trap BY CONSTRUCTION — Back restores the pristine entry, the restored value disagrees with what the current viewport computes, the queued refresh writes it again, and because a gesture HAS been seen by then, D268's no-gesture gate does not apply, so it pushes over the branch just returned to. Forever. The existing `s !== P.stretch()` value guard never touched this: it stops the render loop, not the trap, because every genuine pan really does change the number. The fix is the general one — keep the derivation in a variable behind an accessor, put only the mode in the URL — and turning the checkbox OFF commits the frozen value, which is a real choice and earns its one entry. Old bookmarks still open: the value they carry was derived from a viewport they never recorded, so restoring it was wrong anyway. A second-order bug fell out with it — writing `stretch` first re-entered `syncControls()`, which re-asserted `checked` from a URL not yet carrying the new `autostretch`, so the second read wrote back the value the user had just turned off; both templates now read the checkbox once up front.<br><br>**The options object.** `{history: "replace"}` forces D99's coalesced replace path whatever the gesture state and leaves the entry PRISTINE, and two callers need it because neither can be served by a value guard — both genuinely change the URL. The stamp is one. The other is a **load-time write behind an `await`** (`annotate`'s sidecar hydration, `pdf_studio`'s doc fallback): D268's gate is read when the write LANDS, not when boot started, so a click during a Python round trip opens it before the seed arrives and the seed takes a real `pushState`; asking for a replace at the call site is the only thing that can see the difference. `{default: d}` makes an ABSENT param MEAN `d`, so writing `d` over an absence drops — `set()` can only short-circuit on a byte-identical search string, and which absence means which default is per-key knowledge only the caller has. Absence is read through `get()`, so a hand-typed global (D72) counts as PRESENT and writing our own copy over it is a real change; and through the D99/D270 overlay, so a drop inside the coalescing window cannot let a stale queued value land. Composed, `{history: "replace", default: d}` is the load-time write that may or may not change anything. **Both throw on a bad option rather than ignoring it**: silently dropping `{history: "replce"}` hands back the very push the option was passed to avoid, and the symptom — a dead Back press — surfaces nowhere near the cause. Two-arg calls are unchanged. Nine private helpers are deleted where the options subsume them; five are kept, because they encode per-key default knowledge AND choose the history mode, which is the stamp itself.<br><br>**The stamp applies `replace` only on the RESTATING branch, never unconditionally**, and that is the part with a trap in it. Once the default is present, every later restatement is byte-identical and the runtime drops it unaided, while a real change still buys its one entry — so Back keeps undoing a mode switch, a slide move and a page turn. An unconditional `replace` at those shared writers would have silently killed Back for all three. **Carve-out, and it is the only one:** `geotiff`'s auto-stretch is derived but NOT re-derivable, because geotiff records no viewport — so unlike `netcdf`/`h3`/`zarr_aoi`, `autostretch=1` alone is not a bookmark; reopening recomputes from whatever viewport the file lands at rather than the one the link was taken from, and dropping the number would lose the colours from a shared link. It stays in the URL as a `replace`-write. Its old `s !== P.stretch()` guard goes with the others: it never stopped the trap, and it suppressed the ABSENT case, which is exactly the one worth recording. SPEC PR-3, PT-15, PT-17.<br><br>**EXTENDED by two more shapes, neither of which the provenance question reaches on its own.** **In-flight bookkeeping** (`claude`'s `run`): the id of a currently-streaming run, put in the URL so the page can re-attach to it after a reload. All five writes replace — the launch, the poll loop's completion, the two dead-run cleanups in `resumeRun`, and the Chats button's clear — and the four clearing sites compose `{history: "replace"}` with `{default: ""}`. **The composition is not what fixes the bug**, which is why it stood: on a fresh chat every clear writes `""` over an absence and `{default: ""}` drops it unaided. Resume onto a live run and the id is PRESENT, so pressing Chats was a real change made by a real gesture and took the visit's push, leaving an entry whose entire content was a run id that had already finished. The general form is that **a value's LIFETIME decides this as much as its provenance**: a param the page will itself erase before the visit ends cannot be a destination, because the state it names will not exist by the time anyone travels back to it. Pinned over the source rather than through the test harness, because the harness ignores `set()`'s third argument — a site regressing to a two-arg call records the same `[key, value]` tuple and every existing expectation still passes.<br><br>**A draft** (the `git` view's `msg`): the commit message, in the URL and always replacing. It is written on every keystroke and on every AI chunk because the textarea is rebuilt by repaints it does not control and the param is what refills it. Typing is a gesture, so the first character would spend the push on an entry holding half a word. And a draft key is by nature outside the view's structural-repaint comparison — repainting per keystroke would tear the textarea out from under the caret — so Back across such an entry clears the param while `draw` short-circuits and leaves the old text on screen, the URL and the DOM disagreeing until something else forces a repaint. The rule worth stating: **a key the view excludes from its own repaint comparison must never push**, because the entry it creates is one the view cannot honour when the user returns to it. Enforced at the view's `setParams` funnel through a `DRAFT_KEYS` set rather than at the three writers, so a fourth cannot forget, and spread into the caller's options rather than substituted for them, so a later `{default: …}` is not dropped for exactly the keys that most need it. `panel`, `pages` and `ask` are real navigation and keep the push. SPEC PT-17. |
| D272 | **A template records where the READER went, not where the DATA is: a viewer that frames itself on load must not write the resulting camera to the URL** | `zarr_aoi` fitted the map to the store's bounds on load and wrote the resulting camera out as params. **D268's pre-gesture gate does not cover it**: the fit lands after an `await` on the daemon's `/meta`, so any click the reader got in during that wait — the Browse button, the explorer row that chose this file — has already opened the gate, and the fit takes a real `pushState`. And it POISONED THE NEXT VISIT, which is the worse half: the camera params are what boot reads to decide whether to fit at all, so one seeded default made every later open of that store skip the fit and show the whole world. Same shape, same answer as D271's derived values — a fit is the view describing the DATA's extent, and the URL is a record of the reader's navigation.<br><br>**The programmatic move is tagged at the SOURCE** — MapLibre `fitBounds` `eventData`, which rides onto the `moveend` the fit produces — because a fit and a drag settle on byte-identical camera state and neither carries an `originalEvent`. *Rejected: a timestamp window around the fit* — an instant fit and an animated one settle on wildly different schedules, and a window wide enough for the slow case swallows a real drag that starts inside it. *Rejected: gating on `originalEvent`* — the NavigationControl's `+`/`−` buttons have none, so a reader who navigates only by those buttons would never get a shareable URL at all.<br><br>**The same commit closed the always-live controls**, which are the non-camera form of one write that means nothing: a **Reset** that is not disabled when there is nothing to reset (`sqlite`, `geotiff`), a **Load** button over a box seeded from the resolved path, so pressing it merely copied that path into a second param (`geotiff`, `zarr_aoi`), and a **"back to the list"** that is live on a list you never left (`claude`'s Chats button on a session-less chat). Each spent the visit's push on a screen that did not change, leaving a Back press that visibly did nothing — the generalisation is SPEC PT-17. |
| D273 | **The shell tells the user when they're on a stale version — and distinguishes "refresh the page" from "restart the app"** | Before this, nothing signaled that an update had left a tab or a running server behind: install a new DMG, keep using an old page against an old process, and nothing on screen knows. The signal rides the existing ServerStatusBanner probe — it already hits `/api/config` every 5s for down-detection, so the version check parses the body that fetch was discarding and costs zero extra requests. Two distinct staleness cases with two distinct verbs, because conflating them tells the user to do something that won't work. **(a) Tab stale (`update-refresh`):** vite bakes `__BUILD_VERSION__` (read from `fused_render/__init__.py`, same regex as setup_py2app) into the bundle; when the served `version` differs, a refresh fixes it, so the card offers "Refresh page". If the served version is seen to MOVE — across a "down" gap or between two healthy probes (a restart can be quicker than the down threshold, or happen while the tab is hidden) — the page reloads itself without asking: a version only changes under a process swap, the user asked for it or was blocked by it, and views are URL-synced so the reload is lossless. The card is for the mismatch that was already there on first observation (an old tab restored after an update). **(b) Server stale (`update-restart`):** `/api/config` now also reports `installed_version`, read from the bundle's Info.plist (`installed.py`, py2app only — unpackaged runs and Windows/Linux report null and never trigger this). A DMG install replaces the .app under a still-running process, so plist ≠ running `__version__` means only an app restart helps — the card says so and never offers a refresh. Restart outranks refresh when both drift (disk 0.5.0, server 0.4.9, tab 0.4.8): a refresh there still leaves a stale server, and auto-reload is suppressed for the same reason. The decision table is a pure module (`server-status.ts`, bun-tested); the component keeps the polling and timers it already had. **The restart card's button is a new `fused-render://relaunch` deep link** — payload-free under D128's strictness rules — because the page cannot be allowed to kill the app over HTTP: `/api/desktop/shutdown` is token-guarded precisely so a web page can't, and handing the token to the shell would undo that. The OS routes the link to the running app, which parks a detached `/bin/sh` relauncher on its own pid (a dying app cannot `open` its own successor — `open` on a live bundle only foregrounds it — so the wait happens in a process that survives us), then quits through the normal teardown (D-quit path); the relauncher sees the pid die and runs `open -a <bundle> fused-render://launch` — the launch action so the successor opens no home tab and steals no focus (a plain `open` is a normal launch, D128), `-a` so the deep link can't resolve to some other registered install — the down-card covers the gap, and the probe auto-reloads the page onto the new version. Relaunch no-ops when unpackaged, when a quit is already in flight (it rides begin_quit's locked claim bool, not an unlocked `state["quitting"]` read — respawning an app the user is quitting would be worse than ignoring the link), and when the process already RUNS the disk version or the plist is unreadable: the OS may launch a fresh instance just to deliver the link (app crashed, or a second click after the old pid died), and that instance quitting itself to boot itself again would be a pointless cycle that can race a second instance past the pidfile the teardown already removed. On Windows/Linux (winopen, supervisor) the link degrades to launch semantics — ensure running, no tab — instead of falling through to a /clone error page. **Accepted exposure, stated rather than implied:** any web page can carry `<a href="fused-render://relaunch">`, so the gate is the browser/OS "open FusedRender?" confirmation — weaker than the shutdown token, and a bigger blast radius than `launch` (a full quit-and-respawn vs. a foreground). Accepted because the action is the user's own Quit+reopen: the same graceful teardown every quit surface funnels through, followed by a respawn — a nuisance if tricked, never a loss, and materially different from `/api/desktop/shutdown`, which kills the server and brings nothing back. |
| D274 | **Before deferring a read behind a click, MEASURE which part of it is slow — and when the answer is "a cosmetic sub-step", make that step optional instead of making the whole feature optional. The `claude` snapshots panel lists on arrival; `file_history.timeline` grows `deltas=`; the file index is explicitly NOT the enumerator** | The panel was collapsed behind a "Show version history" row on the theory that a timeline is a worker round trip most file opens never want (FH-18). The theory named the wrong cost. Profiled on `claude`'s own `template.html` — 453 KB, 12 checkpoints — the read is 292 ms, and **`difflib` inside `_delta` is 290 of them** (4.66M calls in `find_longest_match`), against **7 ms** to read all twelve versions off disk and **0.2 ms** to enumerate them out of the store. The expensive thing was the `+9 −52` on each row; the thing being deferred was the entire feature. So `_delta` takes `exact=`, threaded out as `timeline(deltas=…)`, the page asks for `deltas: "0"`, the read lands in ~6 ms — **~49×** — and the list is simply there when the panel mounts. The exact diff still exists where it is read: `snapshot_plan`, one version, on the click that opens a row.<br><br>**What makes the cheap read safe to act on** is that `differs` is a BYTE comparison, so `_locate`, `position`, `revert`, `offer` and `unique_current` are bit-identical either way — the softened pair is decoration, and the revert semantics (FH-3) never see the flag. The knob defaults to EXACT, the opposite direction from `enrich`, because a hand-written call that did not think about it should get the whole truth; and it only ever narrows, so `deltas=True` cannot buy back the quadratic diff `DIFF_BYTE_CAP` exists to refuse. **One rendering trap fell out, and only the running app caught it:** a net is a SINGLE number by construction (`max(0, ver − cur)` against `max(0, cur − ver)` — at most one side is ever non-zero), so printing it in the exact delta's PAIR shape put `~+0 −43` down every row of a file that has only grown. Every test passed and the panel looked worse than before the change; a screenshot of the real thing is what showed it. It now renders one signed term (`~−43`), or the word "changed" when both sides are zero — an edit that replaced as many lines as it removed, which is most of them.<br><br>***Rejected: answering the enumeration from the file index* (§30).** It was the owner's suggestion and the measurement inverted it. The `listdir` walk it would replace is 0.2 ms — 0.07% of the read — while the same answer from the index measured **8.6 ms** cold and 3.2 ms warm *in-process*, before the HTTP hop and the `/api/index/status` probe the JS bridge adds. And it would import a staleness this panel specifically cannot tolerate: the index is a background scan (no `fsevents.json` on Linux), `snapInvalidate` refetches the moment a turn ends, and the checkpoint Claude wrote four seconds ago is precisely the row the user came to find. **The generalisation: the file index wins for machine-wide questions over a large corpus, and loses to a 44-entry `listdir` on a keyed lookup in a small directory that changes under the user's hands.**<br><br>**Showing the list is what exposed FH-4**, and the fix is the same lesson twice: the number was fine, the framing was wrong. Version numbers restart per session, so five chats over one file give five chains each starting at v1, and the flat merged list showed "v2" three times — *"why do we have multiple snapshots of the same version"*. Rather than dropping `vN` (it is real, and it says where in a run of edits a checkpoint sits) or globally renumbering it (inventing an ordering the store never wrote), the panel now draws the chain: contiguous per-session runs, each with a heading. **Contiguous runs, not a group-by** — the row order is load-bearing, since `_locate` walks the merged timeline positionally to pick a revert target, so the grouping may only insert boundaries; a session that came back to the file later gets two headings, which is what happened. The heading is **named from the sidecar's `preview`**, the same string the "Recent chats" rows above it use, so the two sections agree; a miss is ordinary (the store sees terminal sessions this file's sidecar never had) and falls back to "chat" plus the short session id. *Rejected: labelling the fallback "a chat outside this app"* — a chat started from another file's page in the app is also absent from this file's sidecar, so the claim would be wrong.<br><br>**Two controls died with the deferral** — the placeholder `.snap-row` and its `hide ▾` — which is the third revision of this heading and the last, since there is now nothing to open. `snapInvalidate` had to grow a `snapMounted` gate on the way: it fires when every turn ends, including in a FOLDER chat where the panel never mounted, and a hidden section would have gone on spending a round trip per turn for an answer `_snapshots` refuses. **Also recorded rather than fixed:** the store keys on `sha256(abspath)`, so the same file in a git worktree has its own empty chain — dogfooding inside a worktree makes a working panel look broken. SPEC FH-2, FH-18, FH-19. |
| D275 | **Model weights are fetched by US, not by `snapshot_download`: up to 4 `Range` segments per file, with segments across ALL files as the units of work in one pool capped at 8 connections, and per-segment offsets fsynced to a `.fusedpart` sidecar as the bytes land so a cancel, a crash or a quit resumes instead of restarting. Any failure or incapability falls back to `snapshot_download` / `hf_hub_download` exactly as they were** | Two properties of the thing being downloaded make the library's shape the wrong one. A modern model's bytes are **one shard**, not many — `snapshot_download` opens one connection per file and one file at a time, so a 4.6GB safetensors moved at exactly one connection's speed no matter how much link the machine had. And the fetch is **killed on quit** by design (AI-5e), so every interruption cost the user the whole download; the on-disk state is what makes the supervisor's kill cheap rather than destructive, which is why resumability shipped in the same change rather than as a follow-up. It is stdlib-only `urllib.request` because `worker_base` is imported by every runner's interpreter and is the only part of the worker contract CI can run at all (AI-9a). **The correctness argument is one ordering:** snapshot the cursors, fsync the DATA, then write the sidecar — so a recorded offset is always a durable byte, never a byte still in flight that a kill would lose and a resume would then skip. `os.pwrite` on a shared fd (no userspace buffering) is what makes that claim true at the syscall level, and its absence on Windows is one of the fallback conditions rather than a buffered approximation.<br><br>***Rejected: hf's own `max_workers`.*** It parallelises across FILES, which is the axis that does not matter here — the one big shard that dominates a model download stays on a single connection, so the case that motivated this is exactly the case it does not improve. ***Rejected: writing into hf's `.incomplete` file*** so the two downloaders could share resume state. hf resumes one of those by seeking to its current length; our segments write out of ORDER, so a partial file of length N does not mean the first N bytes are present, and hf would append onto a hole and produce a blob of exactly the right size that is silently wrong — the worst available failure, since nothing downstream would notice until a load. A suffix of our own also means the fallback path is clean: hf never sees our state at all. ***Rejected: verifying a hash after download.*** `huggingface_hub` does not either — it relies on TLS plus `Content-Length` — and re-reading every gigabyte off disk to answer a question the transport already answers would give back a chunk of the speed this was for. What IS checked is completeness, and the first cut checked it the wrong way — against the part file's length, which `ftruncate` makes correct before a single byte arrives, so the check was dead code and a sparse file of pure holes would have published as a blob. It is the per-segment CURSORS, the same durable-byte accounting the sidecar records; the code says so, so it does not read as an oversight. ***Rejected (scope): a bandwidth limit, a detached daemon that survives app quit, per-segment UI.*** The first two are the download-manager prototype's requirements, not this one's; quitting the app stopping the download is a decision (AI-5e), not a gap.<br><br>**What it costs.** A second downloader to maintain beside the fallback, and a class of bug the library was insulating us from — a lying `Content-Range`, an expiring presigned CDN URL, a server that answers the probe with a 206 and then ignores `Range`. That last one is why a 200 at a non-zero offset is refused outright: written where it was asked for, one body's bytes land at four different offsets and the file is the right LENGTH and entirely wrong, the one failure a size check cannot catch. **Review found three more members of that family and they are the reason to state it as a family rather than as three bugs:** a *206* whose `Content-Range` starts somewhere else (a clamping proxy — the same scattering wearing a legal status code), a re-resolve that swallowed a whole new metadata answer when a repo was updated mid-download (new bytes at old offsets, published under the old etag), and the dead length check above. All three end in the same place — a plausible blob under a correct etag, which hf then serves from cache forever — so anything that could put bytes somewhere other than where they were asked for now aborts to the fallback rather than being retried into. Each of those is a test in `tests/test_ai_hub_fetch.py` driving a real `http.server` that misbehaves on purpose, because a stubbed reader cannot lie about a header. **The most expensive finding was not a corruption but an invisible non-event:** we sent the Hub token on to the presigned CDN URL, which S3 refuses as two authentication mechanisms — so for any user with a token set, which is everyone pulling a gated or large model, every probe and every segment failed and the download quietly fell back to the thing this replaced, SLOWER than before and with nothing on screen to say so. A fallback designed to be invisible is a fallback that can hide the feature never running at all, and the lesson generalises past this PR: anything that degrades silently needs a test asserting the fast path was TAKEN, not merely that the result was right. **A third round found four more defects and all four were in the RETRY LOOP** — worth recording as a shape rather than as four fixes. That loop is the only place here where two things can disagree, the connection and the cursor, and every one of the four was that disagreement: a budget counting BYTES ARRIVING rather than the cursor MOVING, which is an infinite loop in one direction (a server that ignores `Range` and truncates re-sends the same prefix, and the safe reading of such a body rewinds the cursor, so the budget never expires and the bar oscillates 0%→50% until someone kills the process) and an abort in the other (the flag was read from a drain that a raising `read()` never returns from, so half a gigabyte on disk under a connection reset counted as a failed attempt); a `finish()` outside the guard that sets the stop flag, so a failure while publishing let the pool's own shutdown wait out every remaining segment of every remaining file; and a resume that skipped the range probe on principle, where only a probe could tell it the server had stopped honouring ranges — the refusal that follows takes down the whole repo and the fallback then deletes the progress the un-probed resume existed to protect. **A loop whose exit condition is DERIVED rather than observed earns more review than the code around it**, and three of the four fixes were the same move: observe the thing that matters instead of a proxy for it. A fifth round (a bot, on the merge commit) found the last one: the file list came from the repo's DEFAULT branch and the fetch was hardcoded to `main` — two sources of truth agreeing by coincidence, and where they differ we fetch a different revision than the list described, write a ref for it, and stay internally consistent while doing it, because every etag still matches its content. One listing now yields the total, the names AND the commit, so the pinning is structural rather than a matching default; `_segmented_fetch` takes the revision with no default at all. **The through-line of all four rounds: every defect was one fact represented twice** — a cursor and a flag, a layout and a probe, a listing and a revision — which is the same failure this module's own docstring was written about. It is also why the probe answer is three-valued — "no" and "could not ask" drive opposite decisions about a recorded layout. Progress needed one repair on the way in: the part file is created at its final size and filled sparsely, so `bytes_on_disk` counts a `.fusedpart` by allocated BLOCKS — by length it reported a 4.6GB download as complete before a byte arrived. That in turn makes **sparse files a requirement, not an optimisation**, so a cache filesystem that allocates on `ftruncate` (where pre-sizing every file would also reserve the repo's full 25GB before a byte downloads) is a fallback condition, detected once per download with a 4MB throwaway file. ***Rejected: an hf-style cache lock.*** The etag names the CONTENT, so two app instances fetching one repo write identical bytes at identical offsets; the only race is a rename pulling a part file out from under the other, whose next syscall fails and whose download falls back — slower, never corrupt. Inside one app the deterministic job id already joins the second Download onto the first (AI-5a). SPEC AI-5b, AI-5i. |
| D276 | **`[bundled]` is a size budget, not a wish list: 542 MB — 57% of the installed set — leaves the extra and moves into the manifests of the templates that use it. But the unit of that move is the FOLDER, which is what decides each case** | **Measurement method, stated because three passes over this branch produced three different absolutes** (425.1 / 440.7 / 423.0 for the same map environment) while agreeing on every delta: each figure below is the sum of `os.path.getsize` over the files each distribution's own metadata RECORDS (`importlib.metadata.Distribution.files()`), across the dependency closure resolved from installed metadata with markers evaluated for this environment — macOS/arm64, CPython 3.12, the site-packages of a `.[dev,bundled,fused]` venv. That counts installed files and NOT `__pycache__`, which is why a `du` of the same tree reads higher. Deltas are robust across all three methods; **absolutes are only comparable against this one**, so quote the delta unless you also quote the method. On that basis: **polars 197.0 MB** (`polars` + `polars-runtime-32`, which carries the actual engine), **scipy 70.3 MB**, **matplotlib 25.0 MB** (+contourpy/cycler/kiwisolver), **pymupdf + pikepdf 68.9 MB**, and the **geo stack 180.1 MB** (geopandas/rasterio/rio-tiler/shapely/zarr, whose exclusive transitives are the bulk of it: pyogrio 76.7, pyproj 17.9, numcodecs, morecantile, numexpr, affine and the pystac-ext-* fan-out). **541.9 MB** in total: the installed `[bundled]` + core set goes from **954.3 MB to 412.4 MB**. The shipped macOS bundle shrinks by an order of **~450 MB off ~800 MB** — an EXTRAPOLATION, not a measurement: no DMG was built for this change, and the only scaling factor available (469/556, from a verifier's pass) predates the fpdf2 reversal it would be applied to. Quoted with that precision deliberately; anyone needing the real number must build one. What justified each removal individually, because "it's big" is not a reason on its own: **polars is imported by NOTHING** — no template, no core app, no server code; it was advertised to user `runPython` code and nothing else, and a fifth of the download for everyone is not what an advertisement is worth. **scipy's only consumer, `netcdf/grid_tile_server.py`, already installs it into the venv it manages itself** (DAEMON_DEPS, D174), so the packaged copy was downloaded a second time and the shipped one was never imported — the same was true of `zarr` for `zarr_aoi` and `rasterio` for `geotiff` and `pyramid`. **matplotlib** had two consumers, `map/geo_classify.py` (now declared there) and `notebook/kernel_body.py`, which only harvests figures the user's own cell created and does nothing when it is absent. **pymupdf/pikepdf serve `pdf_studio` alone**, and `pdf_studio` is a whole-folder tool — every action in it is a PDF action — so the venv it gets is the venv it needs. The **geo stack** genuinely serves `map`, `vector` and `geometry_editor` — the deepest per-user cost in the extra, paid by everyone who never opens a raster. **`fpdf2` was moved out and then PUT BACK, and that reversal is the most useful thing in this row**, because it is where the rule stops being about size. PY-16's unit is the FOLDER, not the import: declaring `fpdf2` in `excel/pyproject.toml` and `slides/pyproject.toml` put those ENTIRE templates behind a project venv, so opening any .xlsx, any .csv in Excel mode, or any .pptx would have blocked on a first-render install of duckdb + pyarrow + openpyxl / python-pptx + pillow — roughly 150 MB of packages the app already ships — in order to add 1.5 MB for an export path most users never touch. The PDF export is not separable from the folder that contains it. Measured marginal cost of keeping it: **14.1 MB** (fpdf2 1.5, fonttools 12.6, defusedxml 0.1 — and fonttools has no other user left now matplotlib is gone), against two everyday templates gaining an install gate. The first cut of this row got that wrong and claimed "the Excel PDF *export* now pays a one-time install", which the folder rule makes impossible. Both manifests and their locks were deleted; `tests/test_engine_requirements.py`'s part 4 is what PROVED neither folder needed one for any other reason — it stayed green through the deletion, which it could not have done if either template imported anything else the app lacks. **The general rule that falls out: judge a removal by what the whole folder costs on first render, not by the size of the wheel.** Four folders gained a `pyproject.toml` + committed `uv.lock` (SPEC PY-16): `geometry_editor`, `map`, `pdf_studio`, `vector`. `geotiff`, `netcdf`, `zarr_aoi` and `pyramid` gained nothing, deliberately — every heavy import there is behind a self-managed venv, and a folder manifest would download the same wheels twice (D174). Two manifests name `pyarrow` for a reason no import scan can see: geopandas' parquet IO is an OPTIONAL backend, so `to_parquet`/`read_parquet` fail at RUNTIME in a venv where every import succeeded — the `pypandoc`-vs-`pypandoc-binary` trap again, and the reason "is it importable" is never the completeness question. **`BUNDLED_EXCLUDED` was the wrong tool and stays empty.** It excludes from the macOS BUNDLE while the extra still promises the package, so Linux and Windows would have kept carrying all 541.9 MB — D176's defect with the platforms swapped. The mechanism is unchanged and still guarded; it is for something the extra needs and py2app cannot carry, not for something the product no longer wants. **The cost, stated plainly:** the first render of a `.tif`/`.shp`/`.gpkg` map, a vector preview or catalog browse, a geometry edit and a PDF Studio session now pays a one-time PY-18 install instead of being instant — and the honest number for that is the whole folder closure, not the removed part: map's environment resolves to **472.8 MB** because the declaration is complete (numpy/pandas/pyarrow/pillow are in it too, D172) and it additionally carries duckdb + requests for user-supplied map targets, against 180 MB of geo actually removed from the app. `.xlsx`, `.csv` and `.pptx` do not — that is what the fpdf2 reversal bought. That is the trade the numbers were weighed against: everyone pays 541.9 MB of download and disk, once per release, versus the minority of users who open those formats paying an install, once per folder. **Three residual costs, accepted and recorded rather than hidden.** (0) **A Python map TARGET loses the app's stack, and this is the sharpest edge in the change.** `map/worker.py`'s `build()` loads and executes a user's own `.py` IN THIS TEMPLATE'S PROCESS — it has to, because the descriptor is built from the live object the script returns, so there is no process boundary to put between them — and that process is now map's environment rather than the app interpreter. A target that does `import duckdb` (or requests, openpyxl, msgpack, httpx, botocore) therefore fails, where before it worked, and the user's OWN folder manifest is not consulted for this call so nothing they write near their script fixes it. Blast radius, measured rather than assumed: `.py` is **not bound to `map` in the registry**, so this path is only reached by deliberately typing a script path into the Map Viewer's target box — but `geo_classify`'s entire purpose is classifying an arbitrary object a user's script returned, so it is a first-class feature, not an edge. **Mirroring the app's set in `map/pyproject.toml` was rejected** — a hand-kept second copy of `[bundled]` living inside a template (D177), which could never be complete since a map target may import anything, at +49.5 MB for duckdb/requests/httpx/openpyxl/msgpack or +108.5 MB with botocore/google-auth. **The owner then bought the common case anyway, with that argument in front of them: `duckdb` and `requests` only, a measured +47.7 MB (map's environment 425.1 -> 472.8 MB).** The distinction is kept visible in the manifest itself — those two are there for a USER-supplied target, not because anything in the folder imports them. Everything else gets `worker.py`'s `_missing_module_help`, which names the environment the target actually ran in, ENUMERATES what is in it (derived from the manifest, after a hand-copied list shipped omitting the two packages added for this exact purpose), and says a pyproject.toml beside the script will not change it. **Which of the two messages a user sees depends on whether the failed module is one map DECLARES**, and the honest statement of that is: not declared (`xarray`, `torch`, the user's own) -> worker's message survives; declared but absent on this interpreter -> `explain_missing_module` REPLACES it. The second case is the real packaged-app-with-builtin-engine state, so worker's extra sentence is lost exactly where it would be most tempting to keep — and that is the right trade, because in that state the actionable fact is the engine setting, not a suggestion to rewrite the target. The real fix is for the target to run under the environment PY-16 gives the USER's folder, which needs the returned object to cross a process boundary; that is a follow-up, not this PR. (0b) A stdlib-only entrypoint in a declaring folder inherits the folder's install: `vector/browse.py` (the file picker) and `map/discover.py` are pure stdlib and now sit behind their template's venv. Assessed and left: PY-16's root is the folder, a nested manifest is inert, so the only split is moving these into `templates/shared/` — which would deduplicate four near-identical copies (the `vector` one's docstring still says "nc_preview") but is a cross-template refactor touching four `template.html` call sites and the mount-safety behaviour `test_pyramid_mount.py`/`test_geotiff_ltile_mount.py` guard, and it cuts against the standing rule those files cite for duplicating the helpers verbatim. It costs nothing user-visible today: neither template is bound to a directory, so both are only reached by opening a file that needs the stack anyway. **Two residual risks.** (1) Folder manifests are honoured only by the fused engine; the built-in executor ignores them (D174). D204 makes fused-when-available the default and `[bundled]` ships the engine on 3.11+, so packaged apps are on the honouring path — but a user who explicitly pinned `builtin` in Preferences (a first-class UI toggle), or a 3.10 `pip install` where the `fused` requirement's marker skips the engine, would have got a bare `ModuleNotFoundError` from `map`/`vector`/`pdf_studio` where before it worked. **The owner's call was a hard error naming the cause; `executor.explain_missing_module` is what that became, and the correction is worth more than the original ask.** The first build refused BEFORE the run, on the folder's state: resolve the project root, ask `projectenv.missing_from_this_interpreter` what this interpreter lacks, refuse if anything does. Correct in spirit and wrong in granularity, at a scale nobody predicted — it took down **five templates that predate this change entirely**. `docs`, `geotiff`, `latex`, `model_card` and `pano` have each declared a heavy optional dependency for months while their entry points stay stdlib-only ON PURPOSE: `geotiff`'s `ensure()`, and `model_card`'s `inspect_model.py`, whose own manifest promises the card "renders identically under either engine". A folder-scoped refusal is collateral damage for any folder with one optional heavy dependency and stdlib-only siblings, which is most of them. **The fix is to explain at FAILURE time instead of refusing before it.** The run proceeds; if it comes back with a `ModuleNotFoundError` whose module resolves to a distribution the folder DECLARES and this interpreter LACKS, the bare message is replaced with one naming the folder, its `pyproject.toml`, everything missing, and both fixes (engine = fused in Preferences, or `fused-render[fused]`). Every stdlib-only path keeps working, nothing has to predict which imports a script will reach, and the explanation lands exactly where something broke. Applied in `run_python` rather than inside either execution path, so the subprocess one and the in-process helper one (D72) are both covered by one hook. **An AST pre-scan was considered and is the same mistake one level down**: the lazy in-function imports that let `pdf_studio`'s cheap `health` action answer while its venv is still building are precisely what a static scan would refuse. **Independent corroboration arrived from a different PR while this one was in review**: #513 (latex) hit the same D174 gap from the other end — its `export` action does `import pypandoc`, which fails under the built-in executor because the folder venv is never built — and its answer was to keep running and fall back to an on-demand venv at the point of use, not to refuse up front. Two unrelated pieces of work reaching for the same shape is the strongest evidence available that it is the right one; and note that a folder-scoped refusal would have broken #513's new fallback outright, since `latex` declares `pypandoc-binary` and a packaged app does not have it. Strictly gated — a missing module nobody declared is passed through untouched, because blaming the environment for a user's typo is worse than saying nothing. The module -> distribution step is normalisation plus a small table (`projectenv._MODULE_TO_DIST`) for the genuine mismatches, of which `pypandoc` -> `pypandoc-binary` is load-bearing: the two share an import name and only the `-binary` wheel carries the executable, so `latex` and `docs` declare the heavier sibling and a naive lookup would never connect their failure to their manifest. `tests/test_engine_requirements.py` makes divergence between that table and its own `_IMPORT_TO_DIST` a failure — which immediately found five it was missing (mpl_toolkits, multipart, and pyobjc's three). `tests/test_builtin_executor_project_env.py` is the file that did not exist: the gap was invisible precisely because **nothing exercised a folder-manifest template under the built-in engine**, which is how CI stayed green on a real regression. Its first version then *asserted* the refusal for those same five folders — a test pinning a regression in place, worse than no test — and its replacement drives every case through **simulated absence**, because on a dev venv with the whole stack installed the interesting branch is never taken and the assertions are vacuous. (2) `matplotlib` in a notebook cell now depends on the kernel's interpreter, so inline figures need a user-picked env. **Enforced, not asserted (D177).** The three-part PY-16 invariant only ever constrained folders that ALREADY had a manifest, which made this change invisible to the suite: a fourth part was added first — `test_a_template_declares_whatever_the_app_does_not_ship`, judged against the BUNDLE and exempting self-managed files — and watched go red for exactly the six folders before any manifest was written. `pyproj` and `pyogrio` joined `_IMPORT_TO_DIST` in the same pass: never in `[bundled]`, but on the app interpreter via geopandas/rasterio, so `map/vector_engine.py` importing them directly had never been checked. The advertising side got the same treatment, because a promise is a list too: `check_libs.SUPPORTED` may only name what the bundle ships, and the Learn page's static table and `fused-render-authoring`'s bullet list must equal SUPPORTED exactly. Deriving those three at build time was considered and rejected — the packaged app ships no `pyproject.toml`, the skill is read as plain Markdown, and `importlib.metadata` cannot tell a promised library from a transitive one — so this is D177's third rung: the copies stay, their divergence fails. |
| D277 | **The two interpreter probes in `engine.py` spawn with `close_fds=False`, and a probe that reached NO VERDICT is not cached — a spawn that died says nothing about the interpreter it never ran** | Reported symptom: under `scripts/dev.sh`, `/api/run` on a `.py` whose folder declares no `pyproject.toml` failed with `InterpreterUnavailable` — "This app could not resolve a usable Python interpreter" — intermittently, and once it started it never recovered until the server restarted. It was never a wrong-interpreter problem: `dev.sh` already exports a correct `FUSED_RENDER_APP_PYTHON`, and running that same binary by hand worked. **Two linked causes, and the second is what made a flake permanent.** **(1) The probes forked.** `engine._probe` and `engine._probe_app_packages` omitted `close_fds=False`, so CPython took the `fork()+exec` path instead of `posix_spawn`. Both spawn from the SERVER process, which has libproj resident (pulled in transitively by `fused`/geopandas), and `fork()` runs every registered `pthread_atfork` child handler in the child before exec: PROJ's handler `sqlite3_close()`s a handle that is no longer valid and takes SIGSEGV at ~1ms, `returncode == -11`. This is the third distinct site of that exact crash in this codebase (the pyramid worker and the `git` gate are the other two, GT-3), which is why the SPEC sentence claiming every subprocess already passed the flag was worth checking rather than trusting — an AST sweep found ~110 sites that do not, so that sentence was corrected to state the rule by its real scope (spawning from the server process) instead of as a codebase-wide habit. The ~110 are left alone: in a worker or daemon PROJ is not resident and there is nothing to fix. **(2) The crash was read as an answer.** `_resolve_app_interpreter` treated `-11` as an ordinary non-zero exit, i.e. "this is not a usable interpreter", and `app_interpreter` cached that `None` for the life of the process — no HTTP path calls `reset_app_interpreter_cache`. So one unlucky spawn disabled every header-less script until restart, and `watchfiles` re-rolling the dice on each reload is exactly why it presented as random rather than sticky. **The fix makes the probe three-valued**, which D212 had already done for the sibling-venv probe in `envinstall.py` — and D212's own text names `engine._probe` as the model it was following, so this was a rule the codebase had written down and not applied to the function it cited. A candidate that RAN and answered is definite and cached; a spawn killed by a signal, timed out, or failing with a transient `OSError` is inconclusive and leaves the cache untouched. The split is by exception TYPE rather than errno, matching D212: `FileNotFoundError`/`PermissionError`/`NotADirectoryError` are facts about the path, anything else is transient. Two consequences worth stating because they are easy to miss. **Rung 2 cannot rescue rung 1:** the PYTHONHOME wrapper is built FROM the candidate, so an inconclusive direct probe makes the whole resolution provisional even when the wrapper reached a real verdict. **`app_packages()` had to change in the same commit:** it cached its `None` on the explicit documented premise that the interpreter answer was "per-process and terminal", which this decision voids — left alone it would have traded a permanently broken interpreter for a permanently lost PEP 723 fast path, and there is no subprocess in that branch to repeat anyway. **What is NOT claimed:** the real PROJ crash cannot be reproduced in a test (it needs a resident libproj holding a live handle in PROJ's SQLite cache), so `tests/test_engine.py` covers it the way `tests/test_worker_forksafe.py` does — assert the spawn flag at both sites, and pin the caching half behaviourally with a faked `-11`, `TimeoutExpired` and `EAGAIN`. **Two sibling sites are flagged and deliberately not touched here**, because each deserves its own change with its own evidence: `envinstall._venv_runs` — D212's own probe, also spawning from the server process without the flag, and two-valued about signals, so a `-11` there reads as definite and makes its caller delete the venv's ready marker and force a multi-hundred-MB `uv` re-download — and `server/ai.py`'s `create_subprocess_exec`, whose neighbour ten lines up does pass the flag. |
| D278 | **Opening a folder selects NOTHING — the folder auto-select is deleted outright, superseding D263's answer and D240's with it (`autoSelectPath`, `isPageRow` and `selectionClaimed` all go; only SEARCH still auto-selects)** | Owner's call. D240 ("the first row") and D263 ("the first page, else the first row") were two answers to the same question and never revisited the premise under both: *the pane exists to show something, so put something in it.* That premise treats a guess as free, and it is not. Auto-selecting takes a real action on the user's behalf the instant a folder opens: it highlights a row nobody chose, it mounts a `/render` iframe — and with it a template's Python — for a file the user may never look at, and it aims the keyboard's target and every row-scoped action (delete, rename, the pane's expand button) at whatever the active sort happened to put first. **An empty pane costs one click; a wrong selection has to be noticed before it can be undone**, and the two are not symmetric. So a freshly opened folder now has an empty selection and its pane shows the `Select a file to preview.` hint that FS-11 already specified for the self target — which stops being the rare state (an empty folder, a deliberate Escape) and becomes the state every folder opens in.<br><br>**Deleted rather than disabled**, which is the part worth recording: the effect in `Listing.tsx`, its one-shot ref, the decision function `autoSelectPath`, its `isPageRow` page test, and `selectionClaimed` — a guard whose ONLY job was to hold that shot back when something already owned the selection. A helper kept "in case" is an invitation to re-add the caller, and the tests pin the absence at both ends (no export, and no reference from the listing) for exactly that reason. `firstEntryPath` STAYS: it is not the folder rule, it is "the first row that is actually on screen", and `searchAutoSelectPath` calls it.<br><br>**Three things this deliberately does NOT touch.** `?sel=` still seeds the selection at mount, so deep links and reloads land on their row — a URL naming a row is not a guess. A click in the pre-stat provisional scaffold still carries across the swap (`recallSelection`) — that one was asked for. And **search still lands on its top hit**: typing a query is itself a request to look at something, which is precisely what opening a folder is not; that surface also re-ranks on every keystroke and so was never the same shape as the folder's single shot. What is lost with D263 is the one place `.htm` counted as a page where `lib/app-entry.ts` accepts `.html` only — a divergence that needed its own paragraph to justify and now needs none. SPEC FS-11, FS-16. |
| D279 | **A `?sel=` that MISSES selects nothing, not row one — the vanished-row re-anchor stops treating "never seen in these rows" as row zero (`selectionAfterVanish`)** | Found reviewing D278, and it predates it: the reconcile's re-anchor read `lastSelIndexRef.current` — the last slot the lead occupied — through `Math.min(Math.max(idx, 0), len − 1)`, and the ref's "never seen at all" marker is `-1`, which that expression turns into **0**. So opening `/explorer/view/proj?sel=old.txt` from a bookmark or a recents entry after `old.txt` was deleted or renamed seeded a lead the folder had no row for, found it among no rows, and selected **row one** — a file nobody named, with a `/render` iframe nobody asked for. The upward hop's `?sel=<child>` reaches it the same way when the child has since been renamed. That is precisely the "wrong selection the user has to notice before they can undo it" that D278 argues against, arriving by another door, and the owner's request was that no file be selected by default.<br><br>**The fix is a distinction, not a removal**, and both halves are pinned by test: the re-anchor is RIGHT when the row vanished **while the folder was open** — an external delete, a move to the bin, a rename the user made themselves — because the user was demonstrably on that row and something took it away, so the highlight belongs on whatever occupies its slot (clamped to the last row when the folder shrank past it); going empty there would punish a user for an edit they asked for. It is WRONG for a lead that was **seeded rather than chosen**, which is the only way a `-1` can reach it: there is no slot to re-anchor to because the selection never had one. **A link that misses is a link that missed, not a request for row one.** The decision moves out of the hook into `selectionAfterVanish` in `listing/selection.ts` — the reconcile needs a DOM and a React renderer the frontend test setup does not have, and this is another selection rule pulled out of that effect for exactly that reason (`nextSearchSelection` was an earlier one).<br><br>**One clamp deliberately KEEPS its `Math.max(…, 0)`:** the abandoned-pending-target path, reached only when the user renamed or duplicated something and the target row never arrived. An operation the user asked for is what left the selection dead there, so row one beats nothing; a `?sel=` miss has no such gesture behind it. SPEC FS-16. |
| D280 | **A selected FOLDER is previewed as a folder, never as the page it holds — D269's pane RETARGET is deleted, and `claude` takes the lead of the universal `/` directory key** | The owner, watching the listing's preview pane with a folder row selected: *"we don't want rendering."* What they were looking at was the folder's app running in the sidebar — the full UI, its buttons live, its Python executing — for a folder they had done nothing but HIGHLIGHT. The cause was D269's rule ("a folder holding a top-level `.html` IS that page, and every surface that shows the folder shows the page"), which this pane implemented by RETARGETING the selected row to that page and previewing it as an ordinary file. On a hub CARD that rule is right — a card whose picture is the page must open the page. **On a SELECTION it is not: highlighting a row is not asking for anything to run.** So the retarget goes, with the `/api/fs/list` per selected folder it cost and the frontend copy of the entry rule (`apps/explorer/lib/app-entry.ts` — this pane was its only caller, so the module and its test are deleted outright). D269's other half stands untouched: the cards still open entry pages, and the server (`app_listing.app_entry`) and the claude template (`templates/shared/app_entry.py`) keep the rule for the surfaces that ask which page a folder is.<br><br>**The registry's universal `/` key is reordered to `["claude", "_listing", …]`** — the owner's ask, that a folder default to the chat rather than to a view of itself. *Not* dropping `_listing`, which was the first attempt and which makes every folder UNBROWSABLE: with the sentinel gone a plain directory resolves five all-conditional templates, so the full-screen folder route resolves its default through `defaultMode`'s "first entry without a gate", finds none, takes `visible[0]` — a gated chat — and renders a conversation where the file table should be, with a one-entry mode menu that hides itself so there is no way back. Demoting the sentinel instead is safe for exactly the reason dropping it is not: it stays the one UNCONDITIONAL entry, so `defaultMode` still picks it from second place and opening a folder still lands on its listing. Verified on the running server (`/api/fs/stat` for a real app folder) rather than reasoned about.<br><br>**One trap fell out and only the template's own source showed it.** The chat template has a preview pane OF ITS OWN, and for a folder it fills that pane by resolving the folder's entry page — so making `claude` a folder's default view would have put the app render straight back, nested one level deeper. `chat_only=1` is the cure (the template checks it BEFORE the entry lookup) and the rule now lives in one place with two callers, `paneChatOnly`: the pane's `claude` SIDE, which always sent it, and the row-mode embed, which had no reason to until now. SPEC FS-11, PT-13. |
| D281 | **A selected folder has NO `preview` side: the pane's pill drops the Preview row for a directory row and lands on Claude** | Reported by the owner one build after D280: *"I still see an entry for 'preview' in the dropdown being selected even though I see the claude template."* Both halves were true. The pane has TWO mode controls and D280 moved only one of them: the row's default MODE became `claude` (the registry lead), while the pane's own three-mode `_side` pill still defaulted to `preview` — so the pill read "Preview" over a rendered chat. Relabelling was the wrong repair, because the deeper fact is that `preview` had nothing left to mean for a folder: the page-render is gone (D280) and the only other candidate, the embedded `_listing` peek, is the listing already open on the other side of the divider. **A folder is not a thing this pane previews.**<br><br>So `paneSideList` takes the previewed row's kind: a directory row offers the COMPANIONS only, and since an absent `_side` parses as `preview`, `activePaneSide` — changed from "fall back to the `preview` constant" to "fall back to the first mode on offer" — lands it on the chat about that folder. The menu asks the list rather than deciding again (`paneSideMenu`), so the pill can never draw a row the pane may not be on nor drop the one it is on. An explicit `?_side=git` is a choice and still wins. **The SELF target keeps its Preview** — nothing selected is also a folder, and its Preview is the `Select a file to preview.` hint (FS-11), so the flag is "a folder the user SELECTED", one row and a directory.<br><br>**Where NEITHER companion is offered, `preview` comes back**: a mount-backed folder, both gates refusing. The pane must show something, and there the row's default mode is the unconditional `_listing` sentinel — a peek that renders no template and runs no Python — so the fallback is never heavier than the default it stands in for. That is the deliberate answer to "what if `claude` is denied", and it means the folder pane can never resolve to a chat the gate refused. **Said exactly** — because review found the loose version of it here — a denied `claude` falls to the next OFFERED side, which is `git` where the folder is in a work tree, and only to `preview` when neither companion is on offer. So the `_listing` peek is the floor, not the immediate next step. The intermediate case is practically unreachable and worth stating anyway: `claude` refuses only a mount-backed or nonexistent path, and `git` refuses those too on top of needing a work tree, so a folder that denies the chat while offering the tree is not a shape the built-in gates produce.<br><br>**The owner then refined the requirement to "no preview option in the dropdown menu", for BOTH dropdowns — and the second one needed no change, which is worth recording rather than acting on.** The other dropdown is the list of TEMPLATE modes (the full-screen topbar menu, the panel/tab pane menus, Open With), and it never carried a "Preview" for a folder: it renders `modeTitle(mode)`, and a directory's modes read `Claude, Listing, Git, Graph, Zarr aoi, Model card` — verified against the running server, not inferred. **`_listing` is labelled "Listing"** (`mode-name.ts`'s sentinel names), and that separation is the whole reason the requirement is safe to honour: "no Preview for a folder" can be true everywhere while the full-screen route keeps a plainly-named entry to the file table. Removing the LISTING mode instead is what makes folders unbrowsable (D280), and the owner accepted that caveat explicitly. So the second half is a no-op, kept one by a guard that reads the shipped registry and fails if any directory key binds a mode humanizing to "Preview" — the deleted D185 folder-preview template's own name being the likely route back. A hand-typed `?_side=preview` cannot resurrect it either: it is not on offer for a folder row, so `activePaneSide` resolves it onward to the chat.<br><br>**A PENDING PROBE IS NOT A DENIAL, and missing that shipped the reported bug twice.** The first fix covered only the RESOLVED case: `Listing` nulls both companion entries while `lib/dir-mode` is still answering (a stat plus a `condition.py` fork per gate), so "no companion offered" and "not answered yet" arrive in the same shape — and the `preview` fallback fired in the window after a folder opens, putting the pill on "Preview" over a rendered chat, which is verbatim what the owner reported. Worse, when the verdict landed the side flipped to `claude`, `paneKey` changed, the iframe remounted and `agent.py` spawned a SECOND time — the exact cost the settle above exists to stop. So `paneSideList` answers an **empty list** for an undecided folder row, meaning *undecided*, and the pane holds its skeleton: no mode resolved, nothing mounted, one spawn when the answer arrives. `paneSideMenu` reads that same list, so the pill draws its two companions as spinners and no Preview row. Both `paneSideMenu` and `lib/dir-mode` already distinguished pending explicitly; this one list did not, and nothing covered it — the pending cases are now tested directly.<br><br>**Accepted cost:** the pill's ENTRY LIST now depends on the selected row, which `pane-side.ts` argued against — it is what let the pill hold still as the user arrows down a listing. Arrowing from a file row to a folder row swaps Preview for nothing and moves the active pill. The alternative kept a control that was on offer, selected, and wrong.<br><br>**The cost that was NOT acceptable, found by review and fixed rather than recorded: subprocess spawns per keystroke.** The first draft of this paragraph said "remounts the chat frame per row", which undersold it by a whole category; the second said one spawn per mount, which undersold it again. `paneKey` keys `claude` on the row path, so every lead change tears the iframe down and loads a new one — and **a fresh chat over a FOLDER opens with four `/api/run` spawns**, counted in the merged template rather than assumed: `app.py {dir}` (the pane resolver's entry lookup, which for a directory runs BEFORE the `chat_only` short-circuit — the page is not rendered, but the resolution is paid for), `agent.py {action: "defaults"}` at module scope, `agent.py {action: "sessions"}` for the landing's recent list, and `artifacts.py {action: "list"}` for its artifacts tab. Only `mountSnapshots` opts out, early-returning on a non-file target. Holding ↓ through a folder of forty subdirectories was **a hundred and sixty** of them. Two things make that unshippable rather than merely wasteful. A folder row cost **zero** Python before this decision — the embedded `_listing` peek is a shell component, and this decision's own text praises it for running none — so arrow-key browsing got materially more expensive, not differently expensive. And `/api/run` spawning is the path with this repo's fork/PROJ-atfork SIGSEGV history, where a burst is exactly the shape that bit. So **the pane's row is the lead ONCE IT HAS SETTLED** (`pane-settle.ts` + `useSettledLead`, 250 ms): a move from REST still mounts immediately — a click and the first press of a key are destinations, and delaying them is latency for no saving — while a move made mid-burst re-arms, so the rows passed THROUGH cost nothing and only the row the user stops on is mounted. The whole pane reads the settled lead (the row, the mode key, the folder/file question behind the pill), because reading the live one anywhere puts the mount back. **The count is a moving target, which is itself the argument:** merging #520 (per-target artifacts, tabbed home lists) added the fourth spawn — `artifacts.py {action: "list"}`, against a second module, so a second interpreter — without anyone touching this branch. A per-mount cost that grows when a template gains a feature is one the pane must not pay per keystroke. It is the same "settle before acting on the lead" the `?sel=` URL write has done since `SEL_URL_DELAY_MS`, and it fixes the identical burst for FILE rows, which had it all along whenever the pane was on Claude.<br><br>**Known limitation, recorded rather than fixed:** the companions' gate verdicts are the OPEN folder's, not the previewed child's (`sideEntries`, from `lib/dir-mode`). Selecting the mounts root FROM a non-mount parent therefore aims a chat at a mount-backed path, which `claude/condition.py` refuses — the one case that escapes, since `is_mount_backed` treats the mounts root itself as mount-backed and so every other mount-backed row has a mount-backed parent. It predates this decision (the `claude` SIDE always aimed the folder's entry at the selected row) but this decision makes it reachable without a click. The fix is a per-ROW gate verdict before the chat may mount, which the pane already resolves on its `preview` path — a change of shape, and its own decision. SPEC FS-10, FS-11. |
| D282 | **ONE WIDTH FOR BOTH COMPANION COLUMNS — 30 %, shared as a single constant — and NO CONDITIONAL LAYOUT LOGIC LEFT IN THAT PATH: the 700 px pane gate, the 30/50/70 width tiers, the file sidebar's small-screen 50 %, and the `ResizeObserver` all three needed are deleted** | The owner, after D280/D281: *"update the logic for the folder sidebar and file sidebar to have the same width of 30% now. they are the same concept now"*, then *"remove any complicated breakpoint logic"*. The first half follows from D280 — once a selected folder previews as the same companion a file gets, a width rule that distinguishes them is vestigial in exactly the way the `preview` side was. **The difference was never per-ROW**, which is worth recording because it is what the request sounded like: the two numbers belonged to two SURFACES, the listing's preview pane (a fraction of its split container, tiering 30/50/70 %) and the file view's sidebar (30 %, or 50 % at or below a 720 px container, resolved to pixels). Nothing branched on file-vs-folder anywhere in either, so the fix is that both now read `COMPANION_FRAC = 0.3` from one module rather than spelling a literal each — two literals being precisely how they drifted to 50 % and 30 % in the first place. The SELF target needed no special case for the same reason: the width never asked what was selected.<br><br>**The second half is the deletion, and it is bigger than the number.** `PANE_SPLIT_MIN_W` (700 px) decided whether a folder had a pane AT ALL; the tiers decided how wide; and both needed the standing measurement of the split container (`useSplitWidth`, plus `useSplitIsWide` for its second reader, Preview's browse chip). All of it is gone rather than left computing something nothing reads — the same treatment as `autoSelectPath` (D278) and the pane's `preview` side (D281). What answers "is there a pane" now is the three flags that were always the honest half of it: not an embedded Listing, not a frozen-tree snapshot, not a panel pane. **Those are questions about the SURFACE, and each is a case the measurement got wrong** — a snapshot's listing and a panel pane's are both top-level listings handed a column by a framer, comfortably past 700 px while being exactly the two that must not split, which is why each already needed a flag of its own beside the width test.<br><br>**The cost, stated plainly because the owner should hear it rather than discover it:** a narrow window now shows the pane like any other, so at ~400 px the listing gets what is left after the pane's 220 px CSS floor — cramped, where the gate used to hand the listing the whole column. `_side=off` and the reopen button in the search row are the way out, unchanged and already there. The pixel floors STAY (`clampPaneWidth`, and the `.listing-pane-slot` / `.listing-main` `min-width`s): they are clamps a drag and the CSS must agree on, not conditions on the layout, and they are the only reason a 30 % pane on a small window is still a column rather than a sliver. **One concrete regression found while removing the gate, reported rather than designed around:** `.preview-browse-chip` — the embed-only corner control that reaches a directory's members — was hidden whenever the pane covered its corner, which was "when the container is wide"; with the pane always on it is hidden for a listing at every width, so a NARROW embed of a `.zarr` folder can no longer switch from the listing back to its map (the chip still appears while the MAP is showing, which is the direction that matters more). Review then established it is worse than a degradation: the chip renders only while `!listingPaneOpen`, which is now `!isListing`, so it **never** appears while the listing shows — and in an embed the whole `.preview-header` and its switcher are hidden too, so a `.zarr` folder embedded at ANY width can go map → listing and then has no control at all to get back. A dead end. Left standing deliberately: the honest fixes are a chip that does not pin under the pane's corner, or an embed that gets no pane, and re-gating either on a WIDTH is the thing this decision removes. Its unreachable label branches (`"Back"` / the counterpart's name) are deleted rather than left implying they can happen, and the dead `bodyRef` the measurement used goes with them. **Owner's call.**<br><br>**Second thing for the owner, found by the same review: below ~285 px of container the split now OVERFLOWS horizontally.** The pane's 220 px floor plus the 5 px divider plus the list's 60 px floor exceed the container, `.listing-split` is a plain flex row with no `overflow` rule, and nothing clamps the RENDERED fraction — `dragPaneFrac`'s 280 px refusal is a rule about recording a choice, not about painting one. The 700 px gate made this unreachable. Clamping it, or withholding the pane under the floor sum, is a width condition of exactly the kind this decision deletes, so it is not something to slip in here; SPEC FS-9 now states the real floor and both regimes instead of reasoning only down to the sum. Reachable in practice only in a very narrow embed or a heavily zoomed window. SPEC FS-9, FS-10, FS-12, FS-13. |
| D283 | **The small-container step comes BACK — the companion column takes 50 % at 1000 px and under, 30 % above — because D282 deleted it on an argument that was arithmetically wrong. One step, not the ladder: the tiers and the 700 px gate stay deleted** | The owner, on a small browser window: the 30 % companion is too narrow there. **D282's stated reason for removing the step was that the 280 px column floor "already says so" — and it does not: 280 of 720 is 39 %, not 50 %.** A floor stops a column being unreadable; it does not give a cramped layout the half it needs. So `companionFrac(containerW)` replaces the bare constant, both surfaces call it (the file sidebar and the listing's pane — one function, so they cannot drift, which is the half of D282 that was right), and the threshold is **1000 px**: it shipped for one commit at 720 — `SMALL_W`'s old value, chosen because `preview.css` calls that width narrow — and the owner then asked for 50 % at "a viewport a bit bigger than the current one", their window being on the 30 % side of 720. 1000 is the old `PANE_MID_W`, the deleted ladder's FIRST boundary, so it is a number this codebase already reasoned about rather than a third invention; sharing a boundary with something deleted is not reviving it, since what remains is one comparison with two outcomes against the ladder's two comparisons, three outcomes and a floor folded into the fraction. 1000 itself is small (`<=`, matching CSS `(max-width: 1000px)`); an unmeasured container is deliberately NOT small, since guessing half and snapping to a third on the first measurement is the one visible failure here.<br><br>**What this does NOT bring back, stated because the owner asked for the ladder's removal and got it:** the 30/50/70 % tiers, `PANE_MID_W`/`PANE_WIDE_W`, `defaultPaneFrac` as an identifier (the re-export keeps the function's own name for that reason), and above all the **700 px visibility gate**. This decides how wide the pane is; FS-9 still decides whether there is one, from the surface alone. **It does cost the container MEASUREMENT back** (`useSplitWidth`, a `ResizeObserver` in a `useLayoutEffect`) — one boolean's worth, on the CONTAINER and never the viewport, so an embedded pane in a small frame is small like a small window. That is the one place the owner's two instructions pulled against each other: keeping the share in one shared constant pair *and* stepping it by container width cannot both hold without knowing the width.<br><br>**Checked at the awkward sizes rather than assumed.** For the sidebar, re-run at the new boundary: 1000 → 500 px leaving the content column 500, 720 → 360, 680 → 340, 1001 → 300 (the general share, over the 280 px floor), 600 → 280 with the content column keeping its 320 px floor exactly. **The content floor is never broken anywhere in the range**, and its tightest point is 600 px, where the two floors meet exactly — the same place it was before this. For the pane: **below ~440 px both shares paint identically**, because half of anything narrower is under the 220 px slot floor that already governs — so the sub-285 px horizontal overflow D282 left open is **unchanged by this**, neither better nor worse. **Raising the boundary also CLOSED the flex-basis/`min-width` wrinkle the 720 version had** (30 % of 721 was 216 px against a 220 px floor) and opened none at the new one: 720 is now on the 50 % side at 360 px, and 30 % of 1001 is 300 px, both clear of the floor. Below ~440 px they still disagree, as they always did and as both shares do alike. SPEC FS-9, FS-10, FS-12. |
| D284 | **NOTHING SELECTED is a folder subject too: the no-selection state loses its `preview` side and its "Select a file to preview." hint, and lands on the chat about the OPEN FOLDER — D281 applied to the state every folder opens into** | The owner: *"the 'Select a file to preview.' and the preview template in the dropdown when nothing is selected is wrong."* D281 established the rule — **a folder is not a thing this pane previews** — and applied it to a SELECTED directory row only. The no-selection state is the OPEN FOLDER, which is a folder by the same reading, and FS-16/D278 had meanwhile made it **the state every folder opens into**. So the half D281 skipped was the half a user meets on every single folder open: a pill reading "Preview" over a hint telling them to select something, for a subject that cannot be previewed. Same argument, same fix, and the more visible of the two.<br><br>**The pure rule did not change at all** — `paneSideList`'s flag is renamed `rowIsDir` → `subjectIsDir` and now means "the pane's SUBJECT is a folder", which is two states: a selected directory, or nothing selected. Callers answer it (`sel.paths.length === 0 || !!paneRow?.isDir` in Listing; `!!row?.isDir` in the pane, dropping the `!row.self` exclusion that was the code half of the reversed clause). Everything downstream was already built: `paneSideTarget` and `paneKey` fall a null row back to the folder, so "claude about the folder" needed no new plumbing, and the UNDECIDED rule (an empty list while the companion probes are out → the caller holds its skeleton) carries over for free — which matters most here, since resolving `preview` in that window is exactly the bug 7c37acf4 fixed and this path is now taken on every folder open.<br><br>**MULTI-SELECTION is deliberately out of scope**: several rows are not a folder, so `selCount > 1` keeps its `preview` side and its "N items selected" placeholder. The two are told apart by the caller (`=== 0` versus `> 1`) rather than lumped.<br><br>**The `self` PaneTarget still earns its keep, but only as ONE case**: neither companion offered (a mount-backed folder, both gates refusing), where the hint is the pane's only possible content. Its branch comment now says that rather than describing the always-taken path it used to be, and `paneKey`'s `preview:self:` key identifies that fallback rather than the ordinary no-selection state. SPEC FS-11, FS-16. |
| D285 | **The whole-pane screenshot comes back as a BUTTON that captures on CLICK and attaches a visible chip — the same `<pane-shot>` wire block D208 defined, written by a control that answers every objection that deleted D208's toggle** | Both composers get `#viewshot`/`#hviewshot`, a camera pill beside the model/effort/approvals selects. A click runs `shotCapturePane` — `shotPane`'s bitmap encoded whole at `SHOT_VIEW_EDGE` 1600 / `SHOT_VIEW_BYTES` 900 KB (explicitly NOT `SHOT_MAX_EDGE`), uploaded as `<stamp>-view.<ext>` into the crops' own shots directory, so the same `Read(//<shots>/**)` pre-approval and the same pruning cover it — and the result lands in one module-level `paneShot` = `{view, viewNote, thumb}`, drawn by `renderAnn` as a chip in BOTH `.annchips` rows: the annotation chip's pill, with the thumbnail where the note's words would be and the same ✕. `sendMessage` takes it into a local and clears it before anything is awaited, passes `{view, viewNote}` (never `thumb`, a blob URL) as `composeOutgoing`'s fourth argument, and shows it as the first row of the receipt; a send that never launches hands the picture BACK to the composer rather than revoking it. Bounded by the crops' own `SHOT_TIMEOUT_MS` with the same abandon-and-clean handlers, one capture at a time (`shotBusy`), hidden wherever `annCapable()` is false and removed outright by `enterNoPane`. | **The feature request is the same one D208 served and the deletion was of the CONTROL, not the need.** D208's pill was a per-message toggle, and every complaint that killed it is a complaint about arming rather than about pictures: what went out was a picture nobody had seen, of a moment nobody chose, behind a switch that had to be noticed, armed and re-armed every turn, with the capture running inside the send where a failure could only degrade silently. Capture-on-click answers all four — **the user sees what is attached** and takes it off before sending; **the moment is theirs** (a hover state, a half-drawn frame, a bug that just appeared) rather than whenever the send's capture happened to run; **a failure is reportable**, because no message is waiting on it, so it becomes a chip that says "screenshot failed" with the reason in its title and still rides the wire as `{view: null, viewNote}` — degrading to "no image" is right, degrading to "no evidence anything was asked for" is the silent-failure shape; and **nobody who does not click pays anything**, which is the always-on cost the toggle was deleted for. **The wire format is D208's, unchanged, and that is the point of reusing it:** `stripPaneBlock`, `MARKER_VIEW`, `isMarkerOnly` and the re-attach probe all survived the deletion as READERS with no writer (sessions on disk carry those blocks), so a new format would have meant a second reader for the same picture. The tests that pinned the reader against a hand-built legacy shape now run against the live writer, with one kept hand-built on purpose — the old caption is a shape nothing produces today and the strip keys on the TAG, not the caption. **The chip is chat content, the button is a feature of the preview**, and the narrow layout draws exactly that line: `body.view-chat` hides `.viewshot` (a view showing no preview offers no features of the preview — the capture still WORKS from there, so this is the stronger rule and not "it cannot work") and keeps the chip, and `applyNarrowView` does NOT drop a captured shot the way it disarms annotate mode, because an armed mode is an invisible promise while a chip is a visible attachment. **Not persisted** to any param, unlike `annotations`: a note is the user's words, this is a temp file and a blob URL that a restored page would hold dead handles to, and a bookmark re-attaching yesterday's picture of a changed screen is worse than one attaching nothing. **Still out of `app_state`'s reach** (D208's rejection stands): a picture of the user's screen is theirs to offer, never the agent's to take. **Rejected: a spinner in the button** — a capture is a second at most and a 9px ring inside a 13px pill reads as a smudge; `disabled` plus a dim is the whole state. **Rejected: keeping more than one shot** — "the pane, now" has one answer, and a second click replaces the first (revoking its blob URL) rather than stacking pictures of the same screen. |
| D286 | **A screenshot the user cannot SEE is not a screenshot feature: the capture flashes the pane it photographed, every thumbnail opens a full-size viewer, and a sent turn keeps its picture across a reload** | Four changes to `claude/template.html`, no new endpoint and no wire change. **`shotFlash`** puts a white sheet over the photographed pane for 340ms (up in 60ms, held ~90ms, out) — appended to `annHl.parentNode`, which is our own `#leftview` in the split layout and the injected shadow root over the host's pane in the sidebar, so one line covers both; styled inline and animated with the Web Animations API; fired on the click, before the capture. The chip gains a 220ms scale+fade entrance. **`#shotview`** is a modal viewer: `shotThumbBtn` makes every small picture a real `<button>` (pending chip and sent receipt alike), and it opens the image full size with the agent's path, the `viewNote` caveat, Discard (shown only when the shot IS `paneShot`, by identity) and Close; scrim, button and Escape all dismiss it, and Escape takes it ahead of every other claimant. Clicking the picture toggles fitted ⇄ natural size with the box scrolling and the caption bar sticky. **`shotReceipt`** builds the sent row for the live send and for a restored one, and **`paneShotIn`** reads `{view, viewNote}` back out of the `<pane-shot>` block so `loadHistory` can render the picture from `fused.rawUrl(view)`. The button moved beside Send, became a camera, and got a one-sentence verb-first tooltip. **`sessionTitle`** strips wire blocks — including truncated ones — out of the chat list's row titles and the snapshot headings. | **The first version shipped and the user could not tell it had done anything**: "not obvious that it took a screenshot and attached it; no way to preview it before sending or while drafting; not intuitive." Every one of those is the same defect — the picture was never visible — and D285's own claim that "the user SEES what is attached" was true only of a 22px thumbnail that proves a picture EXISTS and cannot show what is in it. **The flash goes on the PANE, not the button**, because the gesture has to say "I photographed THAT"; a control lighting up says only "I was clicked". It is opacity-only so it survives `prefers-reduced-motion` instead of being switched off with the movement, and it is HELD rather than peaked — a symmetric triangle measured as a flicker, and being missed is the whole failure being fixed. **One `shotThumbBtn` for every thumbnail** because a picture that opens while drafting but not after sending is exactly what makes a control feel arbitrary. **Fitted ⇄ actual size is not a nicety**: `position: fixed` is the template's viewport, the sidebar is a ~440px column the viewer cannot escape (no postMessage, D3/D4), and a 1600px capture fits into it at ~330px — enough to recognise, not enough to read the UI it is a picture of. **The restored receipt cost nothing to build**: the path was in the transcript the whole time and `/api/fs/raw` already serves any absolute path, so a reopened session showing four words instead of the picture was a choice nobody made deliberately. A pruned temp file says so in words — a broken-image glyph reads as a bug in the chat rather than as a file that expired. **`sessionTitle` was found in the running app**, not in review: the chat list read "<pane-shot> The user attached a pi…", the page's own wire quoted back at the human as the name of their conversation, and `stripBlocks` could not fix it alone because a preview is TRUNCATED before the closing tag every strip matches on. **Two bugs were caught in the browser that no source review would have found**, and both are recorded because they are the same shape: an element with its own `display` rule beats the UA's `[hidden] { display: none }`, so the "closed" viewer was an invisible full-bleed sheet eating every click (the third time this file has hit that trap, after `#annbtn` and `.viewshot`); and a column flex container's `align-items: stretch` pinned the zoomed image to the box's width, so lifting `max-width` alone toggled a class and changed nothing visible while the cursor promised otherwise. **Rejected: a label beside the camera** — the sidebar composer is ~440px with three pills and Send already in it, and a second line costs more than the word buys; the seat next to Send carries the meaning instead. **Rejected: opening the viewer outside the template** — it would take postMessage, which this template deliberately does not have. |
| D287 | **A screenshot that is not a picture of the screen is worth less than no screenshot: the capture puts back every inner scroll box and inlines every image before it serializes — and the composer now takes the pictures the user already HAS, by paste and by drop** | Three changes to `claude/template.html`, no new endpoint. (1) **Scroll.** `shotInlineStyles` now also records `{clone, x, y}` for every element with a non-zero `scrollTop`/`scrollLeft` (never the root — that offset IS `win.scrollY`, which `shotPane` already applies), and `shotApplyScroll` puts each one back by prepending `transform: translate(-x, -y)` to each CHILD's inline style, composing with any transform the style walk already wrote and skipping `position: sticky`/`fixed` children. (2) **Images.** `shotInlineImages` fetches every `<img>` src (`currentSrc`, so a `srcset` resolves to the picture actually on screen) and every `url(…)` in the clone's inline styles, and rewrites them to `data:` URLs — one fetch per DISTINCT url, capped at `SHOT_IMG_MAX` 30, re-encoded down through a canvas past `SHOT_IMG_MAX_BYTES` 256 KB, with the last `SHOT_IMG_MS` 1500 ms of the capture budget reserved for it (the style walk now stops that much sooner). A cross-origin URL with no CORS headers gets a second chance through the already-loaded element and a canvas; a genuine failure becomes a dashed "image not captured" box of the same size and is counted into `shotImageNote`, a BOUNDED caveat that rides the pane shot and every crop and deliberately is not one of `shotPaneNote`'s `incomplete` causes. `<picture><source>` elements and the `srcset`/`sizes` attributes are removed so nothing re-resolves over the rewritten `src`. Order in `shotPane` is load-bearing: styles → images → `shotRasterise` → scroll. (3) **Paste and drop.** `paneShot` becomes `shotAttached`, a LIST of `{kind, view, viewNote, thumb, name}`; `shotPasteHandler` (on both textareas) and a four-listener drag set on `#chat` upload any image in a `clipboardData`/`dataTransfer` through the existing `fused.uploadFile` into the same shots directory, capped at `SHOT_ATTACH_MAX` 4 and `SHOT_ATTACH_MAX_BYTES` 4 MB, each landing as its own chip beside the camera's. The `<pane-shot>` block's payload is now always a JSON ARRAY whose entries carry `kind` (`"pane"`/`"image"`) and, for a file, its `name`; `paneShotIn` reads both that and the bare object older sessions hold, `shotRestoreReceipt` draws one receipt per entry, and `MARKER_IMG` ("🖼 images") joins the marker set for a wordless send of pasted pictures only. | **Both capture bugs were found by taking a picture and looking at it, and both are properties of the TECHNIQUE rather than of any app.** Scrolled mid-document, the README preview photographed as its own title on an empty page: `cloneNode` copies attributes and `scrollTop` is a PROPERTY, so a clone of a scrolled page is a clone of that page at the top — and the markdown preview is a CodeMirror editor that only renders the rows near its viewport, so the top of an unscrolled clone of a scrolled editor is spacer. `shotPane` had compensated for the WINDOW's scroll and nothing else, which is right for a document that scrolls itself and wrong for every app in this repo, all of which scroll an inner `overflow: auto` box. The fix has to be expressible in MARKUP, because markup is all the serialized SVG carries — hence a transform on the children rather than an offset on the parent, which also changes no layout, so a flex scroller (`.cm-scroller` is one) is not rearranged by being photographed. **The images were the same class of silent lie:** an `<svg>` loaded through an `<img>` renders with external resource loading disabled at EVERY origin — a deliberate browser rule, not CORS and not something headers can fix — so the README's hero screenshot, served by our own `/api/fs/raw`, came out as a broken-image glyph and its alt text. A `data:` src is the only one such an image can follow, so the bytes have to be fetched while we are still an ordinary page with an ordinary network. **A placeholder ONLY on a real failure**, and a placeholder rather than nothing: a broken glyph reads as a bug in the page being photographed, and deleting the element would redraw the layout around a hole the user's screen did not have. **The annotation crops got both fixes for free** — they are cut out of that one bitmap, so a note on a scrolled-into-view element used to be cropped against whatever sat at that viewport position in an unscrolled render. **Paste and drop are the half the camera cannot serve:** a crop from the OS shortcut, a photo of a phone, a mock a designer sent — none of it is on the pane, no button here can photograph it, and every chat app the user touches takes it with ⌘V or a drag, which is why the absence read as a missing feature rather than as a boundary. **One list, not two collections**, because everything downstream treats them identically (chip, viewer, ✕, receipt, wire, restore) and `kind` decides only the words — but the pane keeps its single SEAT inside that list, since D285's "a second click replaces the first" is a fact about the subject ("the pane, now" has one answer) and not a cap, while a picture the user brought in has no such uniqueness. **`kind` is what the array made necessary:** two paths with no way to tell them apart would have the agent read a photo the user pasted in as a picture of the pane it is being asked about, and act on it. **The array is written even for one picture** — a shape that changes with its length is a shape every reader has to branch on — while the READER stays forgiving of the old object, because reading an old wire format is a permanent obligation and sessions on disk carry it. **The same shots directory deliberately:** it is already the one path `--allowed-tools` pre-approves a `Read` of, already pruned (12h/200 files), already served back through `/api/fs/raw` for a restored turn; a second directory would have meant a second grant, a second pruner and a second reader for what the agent sees as the same thing — a path to a picture. **A refusal is a chip, never silence** — over the cap, over the size limit, or unwritable — because a file the user dropped and cannot see anywhere is indistinguishable from a bug, and the `{view: null, viewNote}` shape a failed capture already wears carries it with no new plumbing. **`preventDefault` only once a picture is found**, so an ordinary text paste still reaches the textarea and a text drag keeps the browser's own move/insert behaviour; the drop target is the whole `#chat` column (a one-line input is a target people miss) and its highlight is driven by a COUNTER, because dragenter/dragleave fire per child element and a plain toggle flickers off the moment the cursor crosses a chip. **Rejected: a second wire block for pasted images** — the agent's question is "what am I looking at", which `kind` answers inside the block that already exists. **Rejected: a server-side screenshot through `fused.runPython`** — there is no headless browser to ask, and an OS-level capture would photograph windows that are not this app's to send. **Rejected: inlining web fonts** — measured across this repo's templates there are no `@font-face` rules to inline (system stacks throughout), and speculative machinery for a case with no instance is machinery nobody can test. |
| D288 | **Speech to text joins local inference as a third capability, on CTranslate2 rather than MLX — so that for the first time a local-AI feature works on every machine this app runs on. `POST /api/ai/transcribe` is job-backed like an image, and its result is a FILE** | The registry's own docstring says adding a backend is adding a folder (AI-2), and this is the first test of that claim by someone other than its author: `fused_render/ai/runners/faster_whisper/` plus a `Runner` row plus a glossary line, and the AI Models page, the Discover tab, the download manager and the supervisor all picked the capability up with **no change to any of them** — including the React shell, which is `catalog.map(group => …)` and needed nothing.<br><br>***Rejected: mlx-whisper.*** It is faster on Apple Silicon and it was the obvious first reach. It would also have made ASR the **third** Apple-Silicon-only feature in a row: text generation is already unavailable on Windows, Linux and Intel Macs, and the honest reading of that is that this app needs more capabilities that work everywhere, not fewer. faster-whisper is CTranslate2, which publishes wheels for macOS on both architectures, Linux and Windows, and is comfortably faster than real time on laptop CPU cores. **What was rejected is the ORDER, not the runner** — an `mlx_whisper` folder registered ABOVE this one takes the Macs and leaves everything else here, which is precisely the first-match-wins ordering AI-2 documents and has never had a user.<br><br>***Rejected: a system `ffmpeg` dependency.*** Every Whisper wrapper's happy path shells out to it for anything that is not a 16kHz wav. This app bundles rclone, not ffmpeg — so that runner would have worked on the machine it was written on and failed on a user's with a subprocess error they cannot act on. faster-whisper decodes through PyAV, whose wheels CARRY the ffmpeg libraries, so the whole audio dependency stays inside the folder's `pyproject.toml`: the property the runner-folder design exists to preserve, extended from "no torch in the app's venv" to "no binary on the user's PATH".<br><br>***Rejected: folding it into `fused.ai(prompt)`.*** The same argument that kept `fused.ai.image()` separate, and it lands harder here: the input is a FILE, not a prompt, and the output is `{start, end, text}` segments, not a stream of words. Flattening those segments to a token stream would throw away the timestamps, which are most of what makes a transcript useful next to a player. ***Rejected: one "audio" capability with a direction flag*** covering ASR and TTS. AI-4 holds one resident model per capability, so the two would evict each other on every alternation; they stay separate entries in `NO_RUNNER_YET` until something serves them. ***Rejected: streaming the transcript back instead of writing a file.*** A 90-minute recording is minutes of decoding — the same argument the PNG already made — so it lands in `<home>/ai/transcripts/` as a `.json` (segments, language, duration, model) and a `.txt`, and a page that navigated away mid-run still finds it.<br><br>**What it costs.** The runner loads **CTranslate2 conversions only**, so `openai/whisper-large-v3` — the repo everyone reaches for — does not load, while the AI Models page offers it a Load button all the same, because the format is not in the task label. That is the existing design (text generation has it with GGUF and AWQ repos), so it is accepted rather than fixed, and made LEGIBLE instead: the loader checks for CTranslate2's `model.bin` by name and raises an error naming the cause and a repo that works. And **`int8` on CPU is a real quality trade**, stated in the code rather than buried — roughly twice the speed and a quarter of the memory for a small cost in word error rate, with the model size as the lever for anyone who wants the accuracy back.<br><br>**Deliberately NOT built: a path allowlist.** The request carries an absolute path and the worker opens it directly (it is a process on this machine; there is nothing to upload). `/api/fs/raw` already serves any absolute path, because this app IS a local file explorer — the protection is D3/D36's `X-Fused` guard plus same-origin, and the worker's port already needs the token the supervisor generated. So the route normalizes the path and refuses one that is missing or is not a regular file with a 400, before any job row opens; anything more would be a second, weaker copy of a boundary that already exists. Progress is **seconds of audio**, and the per-segment loop is where the ✕ lands — `transcribe()` returns a generator that decodes as it is consumed, so unlike a `pipe()` call there is a real interruption point. **Zero changes to `worker_base`** — the image runner had already solved the artefact-writing and per-callback-cancel problems, and needing one would have been the signal that this was being built wrong.<br><br>**NOT ONE SECOND OF AUDIO HAS BEEN TRANSCRIBED, and that is the honest state of this row.** No test in this repo decodes audio: everything above the worker runs against a FAKE worker speaking the contract, and the worker's own logic runs against a STUB model. The second of those was added under review — `faster_whisper/worker.py` is stdlib-only at import time, so it can be imported and driven on CI, and doing that immediately paid for itself by catching a real bug (below). What remains unverified is Whisper itself: the numbers a real `info.duration` and `segment.end` supply, the CPU-vs-CUDA placement, the int8 quality trade, transcription quality, and whether PyAV opens the containers users will point at it. First real run against `deepdml/faster-whisper-large-v3-turbo-ct2` is the outstanding verification, and until then this row describes a design proven only down to the model's door.<br><br>**Three things review caught, and all three were the docs being braver than the code.** *(a)* `GENERATE_TIMEOUT_S` is 900s and the worker sends nothing until the decode finishes, so the 90-minute recording used as the motivating example everywhere in this row — ~18 minutes at CPU int8 — would have timed out the socket, errored the row, and left the worker decoding to a transcript nobody was told about, with `GENERATE_LOCK` still held so every queued request repeated the failure. A transcription now has its own `TRANSCRIBE_TIMEOUT_S`; *rejected: having the worker reply immediately and report completion through the row*, which reads cleaner but would release `GENERATE_LOCK` while the model is still decoding and let a second request run concurrently on it — the exact thing that lock exists to prevent, and unfixable without editing `worker_base`. *(b)* `unit: "s"` reached the job row and stopped there: `jobAmount` special-cased only `"bytes"`, so the seconds claim rendered as `720 / 5400`. It renders as a clock now. *(c)* **The cancel guarantee did not hold for the first phase** — `transcribe()` looks lazy because it returns a generator, but it decodes the whole file and runs the VAD first, and that window carried one plain `report`, which cannot bring a cancel back. It is ticked from a thread now, and the test that pins it was mutation-checked: with the ticking removed it fails on the assertion that no segment tick had yet been sent.<br><br>**A later round found what the timeout fix itself cost, which is the more interesting half.** Raising the cap to four hours meant one worker can hold `GENERATE_LOCK` for hours, and a second transcription blocks inside `_single` BEFORE that handler reaches `heartbeat()` — so a merely-QUEUED row got no ticks, went stalled at 30s, was swept at 600s, and the bridge then told the page that a transcription still happily running had failed. Two 90-minute recordings back to back was the whole repro. **The wait moved to the supervisor's side of the request**, behind its own lock: waiting there is what makes the wait describable at all — the row says "Queued behind another transcription…", keeps saying it, and its ✕ is honoured, none of which is reachable from inside a blocked `urlopen` that has sent the worker nothing to cancel. *Rejected: refusing a second request outright*; queueing is what a user pointing at a folder of recordings means, and a 409 would make them do the serializing by hand. Two more of the same species: a cancel unwound the handler but left the decode thread running while `_single` released the lock (so a ✕-then-retry ran two decodes on one model — the abandoned thread is now waited for), and the ETA charged the entire eager decode to the first segment, announcing "~1079 min left" for an 18-minute job. **The pattern across all of them: this feature's failures are one component reporting honestly to another that cannot hear it** — `unit: "s"` to a formatter that only knew bytes, a queued row to a heartbeat that had not started, an elapsed clock to an arithmetic that assumed it began at the first segment.<br><br>**Left alone and written down instead:** `export.py`'s `_UNSUPPORTED` matches `fused.<name>(`, so `fused.ai.image(`, `fused.ai.transcribe(` and `fused.ai.models.*` all slip past a check the EXPORT.md table claimed covered them. Pre-existing (it arrived with `fused.ai.image`), and tightening the regex is not obviously right rather than merely untouched: a page that reaches for a local model only behind an `env === "local"` branch is legitimately exportable, which is exactly the argument that already keeps `fused.fileIndex` a non-blocking obligation. So the docs now describe what the exporter DOES rather than what it was assumed to do, and the design question is stated as open.<br><br>**A fourth round found the queue fix's own cost, which is the same lesson a third time: a lock is only as good as what it is taken BEFORE.** `generate_transcript` resolved the worker and then took `_TRANSCRIBE_LOCK` — putting the one destructive step in the path (`_wait_ready` -> `_start_resident`, which EVICTS the resident model when the requested one differs) outside the lock that exists to serialize it. A page asking for `faster-whisper-small` while a 90-minute run was mid-decode on the default terminated that worker, lost its transcript, failed its row with "the transcription process did not answer", and then queued behind a lock nobody held. The identical-model case failed more quietly and is the one that survives manual testing: a worker handle captured before a wait that can last hours points at a process an unload may since have killed. Both are one reordering. Two smaller ones with it: a ✕ landing on the FINAL per-segment tick raised past the file write and discarded a transcript that was already complete (the loop now asks the generator for one more segment before honouring a cancel — a cancel is only worth honouring while there is work left to stop), and `bool(body.get("vad", True))` read a JSON `null` as False, so a page spreading an options object with an unset key got the OPPOSITE of the documented default.<br><br>**Round 5 (Bugbot + review) found the same shape twice more, and it is worth naming as the pattern this feature keeps producing: a guard that is correct on the path it was written for and absent from a second path into the same state.** `_await_turn` checked `cancel_requested` only inside its polling loop, so the UNCONTENDED fast path — added so a lone transcription's row is untouched — returned holding the lock having never read the flag, and a ✕ on "Preparing…" with a resident model still started the eager full-file decode that the next request then had to wait out. There is now exactly one such check, placed below both routes so no acquire can skip it: an optimisation that bypasses a guard is the guard being in the wrong place. And queued rows turned out to be the FIRST thing `jobs._sweep` evicts (running rows sort by `updated_at`, and a queue is precisely what produces more than `MAX_JOBS`) — with eviction permanent, because a reopening report needs a `title` and the ticks omitted it, so `upsert` raised, `_report` swallowed it, the ✕ went dead and the page was told a transcription that finishes fine had failed: the round-3 bug exactly, reached by a different route. Ticks now carry the title and run at the decode's cadence.<br><br>**Two smaller ones, and one of them is a lesson about fixing a bug halfway.** A relative `path` resolved against the SERVER's cwd while every sibling call (`readFile`, `stat`, `rawUrl`) resolves against the calling page — so `transcribe({path: "clip.m4a"})`, the natural spelling, either 400s on a path nobody wrote or silently transcribes a same-named file under whatever directory the app was launched from; it now takes `base` exactly as `/api/fs/raw` does. And `_clock` truncated where the manager's formatter rounds, so one row stated the same number two ways. The instructive part is the fix: `round()` would have closed the common case and left the bug on the boundary, because Python's `round` is BANKER'S rounding and JavaScript's `Math.round` is not — `round(88.5)` is 88, `Math.round(88.5)` is 89, and segment ends arrive at two decimals so an exact half is reachable rather than theoretical. `floor(x + 0.5)`, verified by comparing both implementations across every 2dp value from zero to two hours: 102,858 compared, zero disagreements. **Two implementations of one rule have to encode all of it, including the tie-break nobody writes down.**<br><br>**Round 6 is the end of a CHAIN, and the chain is the lesson.** The queue (round 3) made rows evictable; fixing the eviction ORDERING (round 5) by ticking queued rows at the decode's cadence made the running row equally evictable; and restating only `title` on a rebuild (round 5) made the row exist without making it the same row. Each fix was correct about the failure in front of it and created the next one, because each answered "why must the row come back" without answering "what IS this row". The answer is now written once — `transcribe_row_fields`: title, kind, unit, cancellable, plus a `state: "running"` each caller adds — and every reporter on a transcription's lifecycle restates it: the opening report, the queue ticks, the terminal done/error/cancelled reports, and all four of the WORKER's, which are in another process and get it by having the supervisor send it in the request body rather than re-spelling it there. A tick missing `title` is refused outright (`upsert` will not open a row without one) so the row never returns and the ✕ is dead for good; missing `cancellable` rebuilds a row the manager draws with a DISMISS cross — operable looking and inert; missing `unit` reverts the seconds clock; missing `state` cannot reopen a row that was forgotten rather than evicted. **No change to `worker_base` was needed**, which is the part worth recording: `report(**fields)` already forwards arbitrary fields into the body `upsert` reads, so a worker can carry its row's identity without the contract module learning about titles. The image runner has the same gap — only now REACHABLE here, because queueing is this feature's designed usage rather than an accident — and is deliberately left alone; a shared mechanism in `worker_base` is the clean fix and belongs in its own change. **Round 7 closed the last reporter and the last piece of wrong reasoning.** `_wait_ready` — shared with the image path — was still ticking with only a `detail`, and it is the LONGEST reporter here (a cold model is a multi-GB pull), so it was the likeliest of all of them to meet an evicted row and the one that could least afford to: the row stayed gone for the whole download, no progress and no ✕. It takes the identity as an OPTIONAL argument, so the image path is byte-for-byte unchanged until someone gives it one. With it came the tri-state `_cancel_state`: a missing row is not "the ✕ was not pressed", it is not knowing, and reading False off it is a guess that is usually right and occasionally loses a cancel silently — the worst combination. `_cancel_requested` keeps its boolean contract for every other caller.<br><br>**And the cadence comment was wrong in the direction that matters most — it justified the opposite of what it did.** Round 5 set the queue tick to 1s reasoning that it "matched the worker's per-segment cadence" so every row would be equally recent; the running row's worst case is actually `worker_base.HEARTBEAT_S` (5s), which is what re-states a decode sitting on one long segment, so every queued row was FRESHER and the sweep ate the ACTIVE decode's row first. Reporting now runs at twice the heartbeat and the ✕ is polled on a separate one-second cadence — opposite questions, separate constants, so the eviction ordering is not bought with cancel latency. The relationship is pinned by a test that READS `HEARTBEAT_S` out of `worker_base` rather than restating it, verified by mutating each side; a runtime import was rejected because it would make `supervisor` fail to import if a runner file were missing, which is a terrible failure mode for a scheduling constant. **A wrong comment is worse than the bug it describes**: the behaviour was recoverable in a tick, the reasoning would have been trusted by the next person to touch it.<br><br>**Round 8 stopped tuning cadences and named the guarantee.** Splitting the cadences had quietly made the WRITE cadence the mechanism for the row's survival: an evicted queued row waited up to 10s to be rebuilt while `fused.watchJob` gives up after five misses (~3.5s), so the page was told a merely-queued transcription had stopped reporting — the round-3 failure again, bought with the round-7 fix. The poll already knows the answer (`_cancel_state` iterates the same list and returns None when there is no row), so the rebuild happens **on detection** and the tick is nothing but an idle heartbeat: the cap still sheds queued rows first, the ✕ is still read every second, and a vanished row returns within one poll. The property is now written down — **maximum row-absence < the watcher's give-up window** — and tested by reading both sides' constants (`runtime.js`'s miss count and interval, `worker_base`'s `HEARTBEAT_S`) rather than restating them, so tuning either side fails loudly.<br><br>**Measured against that property, the class is NOT closed, and the number says where.** Queued rows are absent at most `_QUEUE_POLL_S` (1.0s) and the cold-model wait at most 0.5s, both inside the 3.5s window; the ACTIVE decode's row is refreshed by the worker, whose worst case is the 5s heartbeat when one long segment produces no per-segment ticks — **5.0s against a 3.5s window**. So the same failure survives on the running path, and it cannot be fixed from this side: it would take either `worker_base.HEARTBEAT_S` (shared with downloads, and off-limits in this branch) or making the row stop being evictable. **The honest diagnosis after four rounds of this is that the job row is overloaded** — it is simultaneously the queue's state, the cancel channel, the progress display and the completion signal, while being evictable under a cap sized for a handful of downloads rather than for sixty queued recordings — and every fix so far has moved pressure around that overload rather than removing it. The shape change that ends it is to stop evicting rows that describe LIVE work: `jobs._sweep`'s cap branch currently sorts running rows by `updated_at` and drops the oldest, and exempting server-owned running rows (which are minted by the app and bounded by real work in flight, unlike page-owned ones which a buggy page could mint without limit) removes evictability for exactly the rows that are a channel rather than a cache. That is a small change — a predicate and a test — but it touches shared `jobs` behaviour and changes a global invariant, so it belongs in its own change with its own review, not as a rider on transcription.<br><br>**The next round proved the compensating approach exhausted, and named the second defect.** Rebuilding the row supervisor-side cannot help once the PAGE has given up: `watch` resolves null after five consecutive misses and a settled promise cannot be un-settled by a row that returns. So there are TWO independent defects behind this whole chain, and neither is a cadence. **(A) Rows describing live work are evictable** — the trigger, fixed by not cap-evicting server-owned running rows. **(B) The page treats "row absent" as "work over"** — the consequence, and a category error: absence conflates *never existed*, *finished and retention-dropped*, and *evicted while live*. Every round from 3 to 8 was a compensation for (A) — keep restating the row so the eviction heals — which is why each fix produced the next; (B) is not reachable from the supervisor at all. Fixing only (A) leaves (B) for page-owned rows and genuinely dropped ones; fixing only (B) leaves the ✕ dead and the progress blank while the row is away. Together they end it: after (A) a live row is never absent, and (B) is the backstop for what (A) does not cover.<br><br>What landed HERE is (B) in its contained form: `fused.ai.transcribe` treats the transcript FILE as the authority on an absent row — keeps looking for it, resumes watching if the row returns, rejects only after a bounded grace so the promise still always settles. Contained because `watchJob` is shared with `fused.ai.image` and the download manager, and raising its threshold wants reviewing against all three; **`fused.ai.image` has the identical bug today and is deliberately untouched.** Two more in the same commit: `_TRANSCRIBE_LOCK` was acquired outside any `try/finally` — `_await_turn` returned holding it and the post-acquire cancel check walked `jobs.list_jobs()` before a caller's `finally` existed, so one exception there would hold a module-global lock for the life of the process and wedge every later transcription on "Queued behind another transcription…" with nothing running (guarded, and the caller now uses a context manager, which is the shape that cannot regress). And the last-segment cancel probe could turn a cancellation into an ERROR: it decodes the tail of the file, exactly where a container fault surfaces, and that exception replaced the `Cancelled` in flight so the user was told the transcription they cancelled had failed — unknown now means "assume there is more", which re-raises the cancel.<br><br>**ROUND 9 ENDED THE CHAIN, by doing (A) instead of hardening (B) again.** The contained page-side workaround had produced a promise that could never settle — a re-entered `watch` leaves `seen` false, so a row that vanished twice was waited on forever — which is a worse failure than the reject it replaced, and the owner's call was that continuing to compensate on the page for an eviction that should not happen was the round-3 mistake one layer up. So `jobs._sweep`'s cap branch **no longer evicts a RUNNING row owned by the server**. That is the whole of (A): for work the app itself runs the row is not a view of the work but its only channel — the ✕'s route to the process and the completion signal `watchJob` polls — so dropping it does not show less, it tells the page the work stopped. `MAX_JOBS` was written for a pathological reporter and a queue of sixty recordings made "more than 64 live rows" ordinary; page-owned rows stay capped, which is where the unbounded risk actually lives (`trackJob` can mint rows a page never finishes), finished server rows stay evictable, and the age sweep still drops a silent reporter, so a crashed worker cannot pin a row.<br><br>**What that let us DELETE is the point.** With the row unable to vanish mid-run, the page-side grace loop went away and both of its bugs went with it rather than being patched (the never-settling resume, and a transient `/api/jobs` failure counted as a hard error where `watch`'s own `get` counts it as a miss); `fused.ai.transcribe`'s absent-row branch is the same two lines `fused.ai.image` has. The queue's write cadence stopped being sized against `worker_base.HEARTBEAT_S` — that relationship existed only to decide which live row the cap shed first, a question that no longer exists — and is now justified by the one constraint it really has, `STALE_AFTER_S`. **Both the code and its rationale were re-derived rather than left pointing at a reason that had stopped being true**, which is this feature's own lesson applied to its own fix. The generalisation worth keeping: **a durable-looking store that is actually a cache will be treated as durable by everything downstream, and every compensation for that is a layer that has to be removed later.** Nine rounds is the price of not asking, early, whether the row was a display or a channel.<br><br>**Round 10 was (A) one step along, and it is the same category error in miniature: an exemption from being EVICTED is not an exemption from being COUNTED.** The cap was still measured against the whole list while refusing to evict most of it, which bounded nothing and moved which row paid — over the cap with 64+ live rows, the only remaining candidate was whatever had just reached a terminal state, so it was deleted on the very next `list_jobs()`, the same read `watchJob` polls. A watcher therefore never saw the outcome: a success survives it because the transcript is on disk, but a failure or a cancel has no artefact, so the page reported "no longer being reported" instead of the reason — on precisely the large queue the exemption exists to serve. The cap now counts only what it can shed. *Rejected: a grace period before a terminal row becomes evictable* — it needs a new constant, protects one field rather than the invariant, and would still lose to a watcher polling slower than the grace; measuring the cap correctly also fixes a second thing the grace would only have hidden, namely that the cap was silently shortening BG-6's own retention promise (a finished record stays `FINISHED_TTL_S` so it can be noticed) to zero under pressure. **Both tests assert the OUTCOME a watcher observes rather than the row count, because the row count is exactly what made this look healthy** — the list was the right length the whole time. Worth recording about the verification too: single-site mutation reported both tests VACUOUS, because the cap's guard and its slice mask each other, and only reverting both to the real pre-fix code reproduces the bug. A mutation check that changes less than the original defect did proves nothing — which applies to every multi-site fix here, not just this one. **The other testing lesson from the same fix: an exemption is a rule about the RELATIONSHIP between two populations, so it cannot be tested by either population alone.** The three tests written with (A) covered server-only and page-only lists and never a mixed one, and the mixed list is where the arithmetic bit: with the count taken over the whole dict and the slice applied to the evictable list, seventy live transcriptions deleted every page row and left the cap still unmet, relocating the exact harm the exemption exists to prevent onto pages — whose reporters also send deltas and whose `watchJob` also settles null after five misses. Now tested directly (70 server + 3 page → 73 rows, 3 page rows intact).<br><br>**Answered, so it is not re-litigated:** the "10s queue-tick absence window" was reported three times and is obsolete from (A) onward. Every removal path in `jobs` was walked against a row of the exact shape `_await_turn` maintains (`sys:`-owned, RUNNING, ticking at `_QUEUE_TICK_S`) and none reaches it — cap eviction is exempt, the age sweep needs `STALE_DROP_S` (600s) of silence against a 10s tick, and `dismiss`/`clear_finished` both refuse a RUNNING row that is not stalled. One remote path remains and self-heals: a tick thread starved past `STALE_AFTER_S` makes the row dismissible, and the rebuild-on-detection reopens it, because the restated payload carries the `title` and `state: "running"` a forgotten id needs. That is what the queue's poll cadence is now justified by, and the only thing it is.<br><br>**Two smaller ones, both of which were claims the code did not support.** `_await_orphan` waited for an abandoned decode with no deadline while its docstring said it was "bounded by the file the cancelled run was reading" — true only of a decoder that RETURNS, and a wedge inside PyAV or the VAD (a malformed container is exactly where a decoder hangs) then blocked every later transcription on "Finishing a cancelled decode…" for the life of the process, with unloading the model the only escape. A hang behind a spinner is the worst failure available: nothing says what is wrong and nothing can be done from the page. It is bounded at `_ORPHAN_WAIT_S` now and refuses with a message naming the remedy; short rather than generous because the ORDINARY case wants that too — cancelling five seconds into a 90-minute file leaves an eager decode of the whole thing running, and the next recording must not sit through a decode of the one the user abandoned. The orphan reference is deliberately KEPT on the deadline, so a decode that does finish clears it and the next request sails through: refusing is temporary, not sticky. And `fused.ai.transcribe`'s `done()` could reject UNTYPED — it does I/O, so a transcript deleted between the row going `done` and the fetch, or a truncated file failing `JSON.parse`, arrived as a bare `SyntaxError` with no `.type` and no `.jobId`, falling a caller through to its unknown-error path on the one failure it could most easily explain, while the sibling branch three lines below was typed. (`fused.ai.image` has no equivalent exposure: its `done()` does no I/O.) **Fixing it is also where this bridge finally got REAL tests** — `aiTranscribe` is now lifted out of `runtime.js` and driven under node against stubs, the same extraction the claude suites use, because a source assertion cannot tell a typed rejection from an untyped one. Also added: `_await_turn` NOTICES when the row it is polling is not the row it opened (`started_at` is stamped once, so a change means a rebuild) and says so, instead of reading `cancel_requested` False off a record seconds old and treating that as "not cancelled" — it does not abort on it, since eviction under capacity pressure is the anticipated case for a queue and aborting would fail the main scenario to guard a sub-second window. SPEC AI-10, AI-10a. |
| D289 | **Scheduled Claude messages are sent BY THE APP, and the OS is asked only to have the app running** | Nothing in fused-render could say *later*: `claude_spawn` starts a detached session on demand, and the only trigger was an HTTP POST. The obvious implementation is an external scheduler — a crontab line, a Task Scheduler entry — invoking `agent.py` directly, and it was rejected on the environment. `supervisor/paths.py:child_environment` injects ~20 variables into every child the app spawns (state/cache/runtime/temp/log dirs, the bundled rclone and uv, `TMPDIR`, and the `CLAUDE_CONFIG_DIR` passthrough whose relocation once logged users out of a CLI they were already signed into), and `_plugin_argv` hands a session fused-render's skills only when that contract is present — so a cron line reproduces none of it and the scheduled turn silently becomes a DIFFERENT install: other state dir, no skills. On macOS it is worse than different: D72's finding is that a process which is not the app does not inherit the app's Documents/Desktop TCC grants, so a cron-launched turn touching a protected folder raises a consent prompt with nobody present to answer it, and Claude Code's credentials live in the login Keychain of a GUI session cron is not in — and both spawn paths run headless (`claude -p`), where the `/login` the CLI prints can never be actioned. Firing from inside the server process makes a scheduled turn environmentally identical to one the user typed. **The cost is stated, not hidden: nothing fires while the app is closed.** Two mechanisms make that survivable and both are load-bearing. (1) **Wall-clock comparison, never tick-counting** — each tick asks what is due *now*, so a laptop that slept through a due time fires on the tick after it wakes and a quit app fires on the tick after it starts; catch-up is not a feature, it is what the absence of tick-counting gets for free, which is also why the loop is started as a startup event and does not sleep before its first pass. (2) **A bound on how late is still worth sending** (`FUSED_RENDER_SCHEDULE_MAX_LATE`, default 24h) — unbounded catch-up is its own bug, since a message meant for Tuesday's standup, fired unattended on Friday against a repo that has moved on, is worse than one that never fired; past the bound an entry becomes `missed`, visible and never sent. **The claim is written before the spawn**, so a process that dies mid-helper leaves the entry `sending` rather than `pending` and the next boot does not resend it — a stuck entry a sweep later reports as interrupted, which is the safe direction to fail (an unsent message is a disappointment; a message sent five times over five crash-restarts is an agent running unattended five times). Permission mode is per-entry and defaults to `auto` for the apps API's reason (nobody polls `decide`, so the strict default parks the first tool call until the timeout denies it) with the extra wrinkle that this turn is unattended by definition — so it is recorded ON the entry rather than being a property of scheduling. The mount refusal lives in the router, not the model: a scheduled turn is an agent turned loose on a path, and scheduling one against a FUSE mount would route around the gate `templates/claude/condition.py` exists solely to be. **The OS half (`schedule_wake`) sends nothing** — it asks the platform to LAUNCH the app at the times something is due and lets the app's own first tick do the rest. macOS gets a LaunchAgent (`launchd`, not cron: it runs in the Aqua session, so the app it starts has the Keychain and can prompt for consent, and it runs a missed `StartCalendarInterval` when the machine next wakes, which cron does not do at all); intervals are written in LOCAL time because that is what launchd evaluates against, and `open -g -a` launches without stealing focus from whatever a 3am wake interrupted. Windows and Linux deliberately get NOTHING new: both already have a start-at-login toggle the supervisor owns (`_win32/startup.py`'s Run key, `_linux/startup.py`'s autostart entry), and a second schedule-specific timer would be a third mechanism that can disagree with those two about whether the app should be running. What that leaves those platforms is weaker and the page says so rather than implying a guarantee it does not have. `claude_spawn.py` is the extraction that made this possible at all: the apps API and the scheduler need the identical posix_spawn discipline (calling `agent._start` in the server process fork()s with libproj resident and SIGSEGVs the child), and that reasoning is the kind that gets paraphrased into something false on the second telling, so it exists once. |
| D290 | **A scheduled message reports what it is doing, and says so on its own — the one kind of work with no audience** | D289 shipped the schedule with a gap it named: nothing told the user a scheduled message had fired or failed. You found out by visiting `/scheduled` or opening the folder's chat — which is precisely the wrong requirement for the one kind of work in this app that by definition happens while nobody is looking. Two surfaces close it, and they answer different questions. **(1) The job registry (D244) answers "what is it doing right now":** one `task` row per send, id `sys:schedule:<entry>`, opened `running` and held for the whole TURN rather than the spawn — the spawn takes a moment, the turn can take minutes, and the minutes are the part worth seeing. Its detail carries phase and tokens, and reports **waiting for permission** when `_poll` shows a parked card, which is the reporting decision that earns its keep: from the outside a turn nobody has approved is indistinguishable from a slow one, and for an unattended session it is the likeliest way to be stuck. `cancellable` is honest here in a way it is not for most reporters — this process owns the run, so the manager's ✕ calls `agent._cancel` and is an ACTION, not a request (`jobs.OWNER_SERVER`). **(2) An append-only event log answers "what happened while I was away":** `GET /api/schedule/events`, polled app-wide by `useScheduleEvents`, turned into toasts. Deliberately a SEPARATE endpoint from the listing, for the reason mount-health's log is separate — this poll runs in every shell forever and must not carry the page's payload — and deliberately the same shape as `useMountHealth` down to the silent first-successful-poll baseline, because opening the app must not replay last week's outcomes as toasts. `done` is an info that auto-dismisses; `failed` and `missed` persist with an action onto the page. **`missed` is worded differently from `failed` on purpose**: nothing went wrong, the app simply was not running inside the catch-up window — but it still needs a person, because the user asked for something that did not happen, which is why it is not an info. The decision table is a pure module (`schedule-toast.ts`, bun-tested) with polling left in the hook, the split `server-status.ts` established. **`state` and `turn` became two fields rather than more states**: `state` says whether the message was SENT, `turn` how the session it started then went. They fail independently — a message can send perfectly and its turn die on the first tool call — and folding the second into the first would report a dead turn as a send failure, sending the user to look in the wrong place. The page therefore labels a sent row by its turn ("Running…" / "Ran" / "Turn failed") and counts a still-running turn as live. **The watcher wraps `record_session_when_ready` rather than replacing it:** that function owns the sidecar write and the commit, which must happen whether or not anything observes, so it gained an optional `on_tick` — called BEFORE its `done` check, since that final tick is where the outcome is, with its exceptions swallowed so an observer can never abandon a run whose sidecar is unwritten. Every report is best-effort throughout: a registry that refuses a field must not cost a message its send, which is the same posture D244 set for reporting generally. |
| D291 | **Scheduling belongs in the composer that already holds the message, and three review findings on the first cut** | Two rounds of correction to D289/D290, kept as one record because they share a cause: the first cut put the feature where it was easiest to BUILD rather than where it is used. **(1) Scheduling moved into the claude template's composer** (the `Send now` pill beside model/effort/approvals) and OUT of the settings page. The composer already knows which folder (the template is bound to one `FILE`) and already holds the message, so a settings page asking for a path and a prompt again was making the user do twice what they had done once — and hand-typing an absolute path was the worst affordance in the feature. Presets rather than a date field, because the pill row is a fixed vocabulary a `datetime-local` would break, and they resolve at SEND time so "tomorrow 9am" means tomorrow from the moment of committing. The pill resets after every send: the other pills persist in `fused.params` because they describe how the CHAT behaves, while "at 6pm" describes ONE message and a deferral that outlived its send would silently defer the next thing typed. Annotations are deliberately NOT folded in (a note is a crop of the pane as it is NOW plus a pointer into this render; by the time the message runs the pane may show something else), so an annotation-only send is refused with a reason. The page keeps the cross-folder list and cancel, which has nowhere else to live. **(2) The event log's delivery mark moved to the SERVER.** The first shape copied `useMountHealth`'s "first successful poll is a silent baseline", which is right for mounts — they emit nothing at startup by design — and exactly wrong here: the catch-up pass emits its `missed` verdicts on the scheduler's FIRST tick, long before a shell has loaded, so the baseline swallowed precisely the events the log was added to deliver. Now the server serves only undelivered events and the shell POSTs an ack after narrating — a client that dies in between gets a duplicate toast rather than a silent miss, and a reload is quiet without the client guessing. An ack POST rather than a drain-on-read, because a GET with that side effect would let any page the user visits silently consume their notifications, which is the shape D3's header guard refuses. **(3) The claim became per-entry rather than per-batch.** Marking every due entry `sending` in one write inverted the point of claiming at all: `tick` sends sequentially, so a process dying inside the first helper stranded every SIBLING as `sending` with no spawn behind it, and the stuck sweep then reported messages that had never been attempted. The claim protects the one message in flight; anything not yet attempted stays `pending` for the next tick or the next launch, and the claim re-reads under the lock so a cancel landing in that window wins. **A fourth finding was investigated and rejected**: that a naive `datetime-local` value without seconds would fail `datetime.fromisoformat` on the 3.10 that `requires-python` still promises. It parses there — verified on 3.10.18, not argued from memory — but the gap it gestured at was real, since every existing test built its input with `.isoformat()`, which always emits seconds, so the page's actual wire shape was untested. It is pinned now. The same round's `PERMISSION_MODES` copy in `schedule.py` was found to omit `acceptEdits` while its own comment called the list four words long, which refused a schedule from any composer sitting on that mode with a 400 naming modes the user had never chosen; a test now holds the copies together, the technique agent.py's `SWITCHABLE_MODES` comment already names. |
| D292 | **The two spawns that stand between a user and a local model must not `fork()`: the venv readiness probe passes `close_fds=False` AND stops reading a signal as a verdict, and the install worker takes its session for itself with `os.setsid()` instead of being spawned `start_new_session=True`** | Reported while loading a Qwen model on macOS: first `cannot import name 'AutoTokenizer' from 'transformers'` — raised inside mlx-lm, printed beside a repo that had downloaded correctly — and then, on every retry, *"the installer exited without finishing (last stage: spawn — starting installer for mlx_text) — and worker.log … is empty, which means it was killed rather than failing"*. Both are the crash D277 documents, at the two sites D277 named and deliberately left alone: this is the evidence it said each of them would need. **(1) `envinstall._venv_runs` spawned with the default `close_fds=True`**, so CPython forked, the child ran PROJ's `pthread_atfork` handler against the server's live `proj.db` SQLite handle and died of SIGSEGV at ~1ms — `returncode == -11`, which fell through to `returncode != 0` and read as *definite* evidence that the venv could not run its own python. `is_installed` then unlinks the ready marker, and a runner environment that was completely fine is rebuilt from scratch. **(2) `envinstall._spawn` passed `start_new_session=True`**, which forces the same fork path (`Popen._execute_child`'s fast path requires `not close_fds and not start_new_session`), so the installer it spawned to do that rebuild died the same way — before writing a single record, hence the empty log, the stale `spawn` stage, and a retry loop that repeats for the life of the server process. **The fixes are the flag and, for the probe, a fourth classification.** A NEGATIVE returncode is now inconclusive (`None`), not False: the child was killed, so it never answered, and D212's rule — state may only be destroyed on definite evidence — applies to a signal exactly as it already applied to a timeout. That half is not redundant with the flag: it is what stops an OOM SIGKILL or a shutdown SIGTERM from costing a user a multi-GB re-download. For the spawn, the detachment MOVES rather than being given up: `_env_install_worker._detach` calls `os.setsid()` as the first statement of the run, so the worker still leads its own session — `envinstall._kill`'s `killpg` still reaches the `uv` it is waiting on — with no fork in the server process. Windows keeps `DETACHED_PROCESS` + `CREATE_NEW_PROCESS_GROUP`: there is no fork there, and `CTRL_BREAK_EVENT` needs the group. **Two diagnosis changes come with it, because both failures were unreadable.** `_crash_diagnosis` now reports the `waitpid` status `_pid_alive` was already collecting and throwing away — "killed by SIGKILL … almost always an out-of-memory kill", "asked to stop (SIGTERM)", "crashed on startup (SIGSEGV) … which is what a spawn that took the fork() path looks like" — in place of the list of four things it might have been; the old wording survives for a pid we did not reap. And the MLX runner wraps its `from mlx_lm import load` so an unimportable environment is named as one (`sys.prefix`, "incomplete", how to rebuild it, and that the weights are not re-downloaded) instead of surfacing a transformers ImportError against an innocent model. **What is NOT claimed:** the real PROJ crash cannot be reproduced in a test — it needs a resident libproj holding a live handle — so, like D277 and `tests/test_worker_forksafe.py`, the tests assert the spawn flags at both call sites and pin the behaviour with a faked `-11`. `ai/supervisor.py`'s two worker spawns (`close_fds=True` + `start_new_session=True` + `cwd=`) are the same shape and are deliberately NOT touched here: the reporter's worker did start and reach `load()`, so there is no evidence for them yet, and posix_spawn would additionally require dropping their `cwd`. |
| D293 | **Text generation runs on Windows and Linux too: a `transformers_text` runner registered BELOW the MLX one, a catalog keyed by RUNNER rather than by capability, and the DEVICE a model landed on reported because a model on a CPU works and looks broken** | MLX is Metal-only, so the app's flagship local capability — `fused.ai` against a model on your own machine — was something a Windows or Linux user could read about and not use. That is the exact complaint D288 answers for transcription ("an app whose local AI is something most users read about is not the app this is meant to be"), left standing for chat. **The backend was chosen on PACKAGING, not on benchmarks.** llama.cpp is the obvious pick and AI-2a refuses it: `llama-cpp-python` publishes an sdist and *no wheels at all* on PyPI, so declaring it would put cmake and a C++ toolchain — MSVC, on Windows — between a user and the Download button, with its prebuilt wheels on a private index that is a second thing to trust and a second thing to go down. torch is what the image runner has already been building on users' machines since D257, so its install path, its wheel coverage and its failure modes are known rather than guessed at, and *any* causal LM on the Hub loads — including the repos the AI Models page already offers a Load button for. `onnxruntime-genai` (tiny, fast int4 on CPU, DirectML reaching every Windows GPU) was DEFERRED rather than dismissed: it loads only pre-converted ONNX repos, so the page would offer Load on models it then refuses, and as a second text runner it would break the rule that a model id never picks the runner. **The registry needed nothing new** — AI-2's first-match-wins ordering already existed for this, and the comment predicting it ("a future cross-platform text runner sits after mlx_text and picks up the machines MLX turns down") turned out to be the entire integration: Apple Silicon keeps MLX, everything else falls through to torch. **The CATALOG is what a second runner really changed.** A suggestion is only meaningful for the backend that will load it — `mlx-community/…` is packed for Metal kernels and is an unusable multi-GB download on a PC — so `SUGGESTIONS` moved to RUNNER keys, and `describe()` now resolves the runner the way a load does. It used to take the first runner REGISTERED for a capability whatever its availability, which with two rows would have told a Windows machine text generation "needs Apple Silicon" while a runner sat ready to serve it, under a heading whose four suggestions it could not load. Curation rules, each a failure already shipped once: unquantized safetensors only (every other format needs a package the runner does not ship — the CTranslate2 trap of D288), ungated only (a licence-gated repo 401s partway through a download for a user who did nothing wrong), and sized for a machine with no GPU. A consequence surfaced one layer away: an unavailable runner also has no curated default, so `POST /api/ai/image` started answering "no image model is configured" — true, useless, and hiding the actionable "the Diffusers runner is not built yet" underneath; `registry.unavailable_reason()` tells the two apart, because no runner is a fact about the MACHINE and no suggestion is a fact about the catalog. **The device reporting is the user-visible half, and it exists because of a packaging fact nobody states out loud: the PyPI torch wheel is CPU-only on Windows** (its `nvidia-*` deps are all marked `platform_system == "Linux"`). So the ordinary outcome on a Windows box with a graphics card is a perfectly healthy model answering at a few words a second, presented as a green LOADED card with a healthy memory figure and nothing on screen to explain the speed. `worker_base.STATE` grew a `device` each runner sets in its own `load()` — same argument as D257's resident bytes, only the process holding the weights knows — surfaced twice as two different KINDS of statement: a MEASUREMENT on the loaded card (**on CPU**, warning-coloured, beside the memory), and a STANDING FACT about the backend in Discover above the cards, before any download, which is when it can still change a decision. All three runners report it; the image runner has had this same problem since D257 and never said so. **Windows CUDA was deferred deliberately**: reaching it means pulling torch from `download.pytorch.org` via `[[tool.uv.index]]`, costing every Windows user a ~3GB CUDA runtime to serve the ones with an NVIDIA card. **Unverified, and labelled as such** (AI-11c, following D288's precedent): torch cannot run on CI, so the runner's refusals, dtype-keyword choice, placement and prompt encoding are tested with stubs and no text has ever actually been generated — the four suggested repos carry `size_gb: None` because D255 forbids a figure nobody has measured. **(Withdrawn in part by D416 — the `transformers_text` runner, its two hardware variants and `runners/torch_text.py` are removed on a measurement llama.cpp won on every axis. What this record established SURVIVES: the catalog is still keyed by RUNNER, the device is still reported, and a capability can still have two runners — `mlx-text` then `llamacpp-text`. What is gone is the backend, and the packaging argument that chose torch over llama.cpp.)** |
| D294 | **The Transformers text runner serves Windows and Linux directly and Apple Silicon as the fallback below MLX, while every request owns a stop signal whose producer is joined before the request ends** *(corrects D293's “everything else” wording)* | Intel macOS is not a distribution target, so wheel availability alone is not enough to make it a supported product surface: registry availability drives the catalog and Load button and therefore refuses that platform. Apple Silicon is different because the worker's MPS path is operational and was verified directly; first-match-wins still prefers MLX, but Transformers must remain available when MLX is absent or unavailable. The other correction is a new runner-specific concurrency bug rather than a reason to replace the one-resident-model architecture. `TextIteratorStreamer` requires `model.generate()` to run on a producer thread; when a client disconnected, the consumer's `write()` raised before `thread.join()`, `worker_base` released `GENERATE_LOCK`, and a retry could enter the same model while the abandoned producer was still generating. A request-local stopping criterion is set in `finally` and the producer is joined there, so disconnect, cancellation, model failure and normal completion all preserve the serialization boundary. **(Withdrawn by D416 — this runner no longer exists. The request-owns-its-stop-signal contract it describes was transformers-specific: `llama_text.generate` needs no producer thread at all, because `create_completion(stream=True)` IS the token loop, so there is nothing to join.)** |

| D295 | **Curated model sizes may be full-snapshot download ESTIMATES from Hub file metadata; they are not limited to completed filesystem measurements** | The Transformer and CTranslate2 Whisper cards previously showed “—” for every suggestion because D255's no-guess rule had been read as requiring a completed local download. That throws away a better source the downloader itself follows: each Hub snapshot lists the byte size of every file it will fetch. `size_gb` now sums those bytes and rounds decimal GB to one place, covering weights, tokenizer, configs and auxiliary payloads rather than deriving bytes from parameter count. The number is approximate because a mutable repo can change after the catalog was curated and filesystem allocation is not download size; the UI already labels the Download action with `~`. `None` remains the answer when file metadata is absent. |
| D296 | **Recurring runs are a template materializing one-shot occurrences; missed recurring runs are skipped; the page answers "when" with a calendar** | Three decisions from the same review (Akshil, 2026-08-14), building on D289–D291. **(1) `repeats` (a 5-field cron line) makes an entry a `recurring` TEMPLATE that is never claimed or sent; a sweep (`_materialize`) keeps exactly one `pending` one-shot occurrence ahead of it** (carrying `template_id`), so the claim discipline, job rows, event log and watcher all handle only the one-shots they already knew. The alternative — re-arming one entry in place after each fire — was rejected because it erases history: each run's sent/missed/error outcome is worth a durable row, and the calendar wants past runs as first-class objects. The next time is computed from the latest occurrence ever materialized (never earlier than now), so an early-finishing run cannot pull the next one forward and a cancelled occurrence stays skipped — cancelling an occurrence MEANS "skip this one, keep the schedule", while cancelling the template cascades to its pending occurrence. Cron parsing is in-house (`fused_render/cron.py`: the classic grammar, dow 0/7 both Sunday, dom-OR-dow, naive LOCAL arithmetic because "daily at 9am" is a wall-clock promise) rather than a dependency, since exactly one question is ever asked of it; a template whose stored line stops parsing goes to `error` and is ANNOUNCED, silently-never-fires being the one outcome the feature may not have. **(2) A missed recurring run is skipped, never caught up** — occurrences carry `max_late: 120` against the one-shot day: the user chose skip-entirely explicitly, because replaying "daily at 9am" at 2pm is not what the words meant and the next run is already coming, where an unsent ONE-SHOT is gone forever and stays worth chasing for a day. The two minutes absorb tick jitter, nothing else, and the missed wording says "skipped" so the card does not read like a fault. **(3) The page's default view became a week calendar** (card list one persisted toggle away): the page's first question is "when", which a list answers badly. Future runs of a recurring job draw as dashed ghosts from `upcoming`, a projection the SERVER computes on the GET (14 days) precisely so the client never grows a second cron parser that could disagree with the first; a ghost's cancel is honestly the template's. Creation now lives in BOTH the composer pill (which knows the folder and holds the message — D291's reasoning stands) and the page's New job modal, because the calendar-first direction ("what should run Monday 9am?") has no chat to borrow from; an empty-slot click opens the modal with the time filled in, and the preset UI is a cron BUILDER whose generated line is always shown, so Custom is a continuation rather than a cliff. || D297 | **"Open app" on an external folder REGISTERS it: a new `registered_apps.json` puts the folder on the /apps hub (tag `linked`) with its own last-opened, partially reviving what D264/#525 removed — on the owner's explicit ask** | The explorer's "Open app" button (pane strip + shut-pane fallback) now fire-and-forgets `POST /api/apps/recents/open` with the FOLDER before navigating. The endpoint splits on `_workspace_rel`: inside the workspace it records in `app_recents.json` exactly as before; outside, it falls through to `registered_apps.record_open`, a NEW module writing `~/.fused-render/registered_apps.json` (`{path, openedAt}`, deduped by path, capped like recents). Opening IS registering — no link/unlink lifecycle — so the store doubles as the external apps' recents, and a hub card's own click (appEntry.ts `recordAppOpen` posts the same endpoint) refreshes `openedAt` for free. `GET /api/apps` merges the entries in via the shared `app_dict` shape, so `category` (metadata.json), `preview_image` and `title` come along unchanged; `name` derives from the basename at read time so a rename can't stale it. **Written FRESH, not resurrected**: the old `linked_apps.py` (deleted #525) served consumers that stayed dead — the `app` template mode, `_app` sentinel, link/status routes, and the `FUSED_RENDER_LINKED_APPS` export the gates read; none return, and nothing outside the /apps listing consumes this store. Its lessons are kept: registry over symlink (app_git's prefix scoping, Windows symlink privilege), skip-don't-delete on read (folder may come back), corrupt file reads empty, workspace/ancestor entries filtered on read AND refused on write (an ancestor entry is a card for the whole disk; an inside entry double-lists the walk's app). Beyond the old module: `MountGuard().blocks()` BEFORE any syscall per entry in both the listing loop and `record_open` — an external path is more likely a mount than a workspace one, and a stat under a wedged rclone mount blocks the serving thread (the walk's own ordering). Validation is server-authoritative (`isabs`, `isdir`, `app_entry` non-None) because the button's client gate feeds nothing the store can trust. Tag is the reserved `linked`, reused deliberately; a workspace folder literally named `linked/` merges chips with it, accepted. The old inert `linked_apps.json` stays untouched — no migration, #525's posture. This row is the record that the revival is deliberate and scoped, not a silent reversal of D264. |

| D297 | **The Schedule page's list view is the Inbox's row anatomy, the New task path field is a combobox over recently-used folders with Browse one level below it, and every text surface on the page carries an explicit overflow contract** | A refinement round on D296's surfaces, all from one review (Akshil, 2026-08-15). **(1) The list view stopped being a card grid** — "this is not a list view, this is a card view" — and now renders the Inbox's rows: pill + bold one-line ellipsised title + time on the right, muted path line under it, compact (26px) actions last. Full-width rows, because a list is scanned down one column, and the two views now say what their names say: Board keeps the card shape for spatial reading, List is for scanning. The card grid's CSS and its width arithmetic went with it; `tests/test_schedule_css.py` re-pins the new invariants (failure states keep a coloured left stripe, titles hold one line) instead of the old grid's. **(2) The path field is a combobox**: focusing it drops the last folders actually used — the user's own picks from localStorage first (try/catch throughout, the PR #538 posture), padded with the folders existing tasks point at, because a first open on a fresh browser profile showed nothing but Browse and read as broken. Browse itself moved INTO the dropdown as its last row: the picker is a level below the recents, so its footer says Back — and Back refocuses the field, which reopens the recents, making the two levels an actual hierarchy rather than two disconnected panels. Escape closes only the dropdown (stopPropagation), never the modal around it. **(3) The description grows like a note**: with the text, to five lines of the title face, then scrolls — height synced from scrollHeight on every change so deleting lines shrinks it back, with the cap in CSS so the mechanism and the limit live where each is testable. **(4) Overflow is a page-wide contract now**, not per-bug whack-a-mole: chips, row titles, board card names/times, popover prose and paths, recents rows, picker breadcrumbs and folder names each state their policy (ellipsise what is recognised, wrap-and-cap what is read — errors scroll past 72px, the popover prompt past 130px). Two cascade traps documented on the way: the settings page's `.prefs-section button { align-self: flex-start }` skin shrink-wraps any flex-column button row that does not restate `stretch` (the Browse row shipped shrink-wrapped), and the segmented view toggle now has its CONTAINER own the pill border with borderless buttons inside, because per-button borders + negative-margin overlap could not survive the page's two hover skins repainting individual edges. |
| D298 | **The chat's annotate feature is called "Comment" in every string a person reads or hears; the ids, params, wire format and function names keep the `ann`/`annotate` spelling** | "Annotate" is the word for what the code does, not for what the user is doing. The gesture is pointing at something on the page and saying a sentence about it to Claude — which every reader already has a word for, and it is the word the pins were named after in the markup ("the comment pins over the app") while the button above them said something else. So the visible strings move: the switch reads **Comment** — statically, in BOTH states: a label that flipped to "Commenting" changed width on every toggle and made the right-anchored row shuffle, one mode wearing two names reads as two features, and the g's descender was clipped by the label's `line-height: 1` clip box (widened to 1.4; the track's accent fill is what says armed) — its `aria-label` is **"Comment on the " + paneNoun** (the kind-derived noun of D239 is untouched — "the preview", "the app", "the file"), its idle tooltip is *"Comment on the &lt;noun&gt;, then send the notes to Claude"*, its armed tooltip is *"Comment mode on — click an element to add a note · Esc or click this button to stop"*, and the narrow-layout view toggle reads **"Comment on preview"** outbound (SPEC PT-15, D239's own rename, relabelled again here) with "Back to chat" unchanged. **Grammar is part of the decision:** "Annotate" is a transitive verb and took its object directly, so a mechanical `Annotate`→`Comment` would have shipped "Comment the preview". Every rewritten string is written as a phrase rather than substituted, which is why the rename is a fixed list of strings and not a regex. **NOTHING BELOW THE SURFACE MOVES, deliberately**: `#annbtn`, `#anntools`, `#annpins`, `annOn`, `annSetMode`, `annPathOf`, the `annotations` and `annmode` URL params, the `data-fused-annotate` layer mark and `data-fused-annotate-target` all keep their names. Two reasons, and the second is the binding one. The ids and params are a **wire format**: `annotations` and `annmode` ride on every bookmarked and shared chat URL (D72's pane-local channel), `data-fused-annotate-target` is a seam the SHELL stamps and this template reads (the sidebar arrangement), and a rename is a silent break of every URL and a cross-repo change for a label. And the internal name still describes the mechanism accurately — an anchored note attached to an element — where the user-facing name describes the intent; a UI word and a code word being different is normal and is cheaper than a migration. `formatAnnotations`, the `The user annotated ` preamble and the `MARKER_ANN` handoff marker are likewise untouched: those are **model-facing**, and the agent's prompt contract is not a place to spend a rename. The audit is by SINK, not by word — the same shape D239's kind-noun test uses — so `title`/`aria-label`/`placeholder`/`alt`/`textContent` were the search space and the noun "annotation" ("Remove annotation", "where the annotation tools are") was left alone as a different word from the verb that was the subject of the change. |

| D299 | **EVERY collapsible card in the transcript ships folded — the newest and the still-streaming one included — and the user's click is the only thing that opens one. Supersedes D267's newest-card-open rule; the per-card override survives unchanged** | D267 replaced D246's per-tool name list with a positional rule (the newest card opens, the card it replaced folds itself) on the argument that position is the honest predictor of what is still news. That argument holds for a FINISHED turn and fails for a live one, which is when people are actually watching. The transcript grows every few seconds while a run is in flight, so "the newest card is open" meant the log **reflowed on every tool call**: an arriving chip expanded to full height and shoved everything above it up the screen, then folded again a second later when the next one arrived, and a reader trying to follow the reply was reading a moving target. The auto-fold made it worse rather than better — the card that vanished from under the cursor was one the reader had just started on, and the only way to keep it was to click it, which is a race against the agent. **All cards closed by default** makes the transcript a column of one-line summaries: the shape of the turn is legible at a glance, the reply stays where the eye left it, and expanding a body is a deliberate act whose result stays put. That the newest card's body is the *most likely* one to be wanted is accepted and is not enough — it is a guess made about ten cards a minute, and the cost of a wrong guess (the page moves) is paid by the reader while the cost of a right one (one click) is trivial. **The override half of D267 is kept exactly as it was, and is now the whole policy**: it is still recorded from the SUMMARY's `click` and not from `toggle` (which fires asynchronously and for programmatic opens, so it cannot tell the user's fold from a repaint's), still keyed rather than held on the element (a tool call by its `tool_use` id, a thinking segment by its index in a numbered segment container), and still cleared by `resetCardPolicy()` at the two places that replace the whole visible transcript — but with the default now `false`, the leak that reset prevents is a card sitting OPEN in a conversation the user has never touched. Since nothing folds a card any more, `registerCard` has exactly ONE write to `.open` and the `newestCard` bookkeeping is **deleted rather than left unread**: a live variable nothing consults is the next reader's false lead, and its clear in `resetCardPolicy` was the only other thing that function did. `buildToolChip` and `buildThinkingView` are unchanged — open-or-folded was never their call. The probes in `tests/test_claude_template_segments.py` are rewritten per arm (all-folded on a turn, an arrival that opens and folds nothing, a user's open surviving arrivals and a rebuild of its turn, a re-close, an unchanged poll tick, replay, the reset), plus a source-inspection test pinning the `: false` default and the absence of `newestCard`. |
| D300 | **Account-state failures from the Claude CLI — logged out, plan limit hit, binary missing — are rewritten into an instruction the user can act on, with a link to the download page's new troubleshooting anchor; the raw CLI text rides along in parentheses** | The CLI's own words name fixes that only work somewhere the user is not: "Invalid API key · Please run /login" prescribes a slash command that exists INSIDE an interactive claude session, shown to a user looking at a fused-render chat; a missing binary surfaced as the LAST line of a helper traceback, which for _claude_bin's multi-line FileNotFoundError is the "Also looked in: ..." tail with the actual instruction cut off. Onboarding (app creation) is where a fresh install meets these first, and each read as a bug in this app with no way out. `_account_error` in the claude template's agent.py (same shape and placement as `_overload_error`, composed after it — the overload rewrite's own output never matches the login/limit patterns) matches on substrings of the CLI's `result` row or stderr, never model output, so a false positive would need the CLI itself to phrase an unrelated failure in login words; an ordinary failure passes through byte-identical, which an existing test already pins. `spawn_helper` maps "claude CLI not found" the same way. The messages point at https://render.fused.io/#troubleshooting with a per-error suffix (-login, -notfound, -limit) the page resolves to the matching panel (Bugbot caught the shared anchor always opening the login fix) — a new "User guide" section on the download page (install Claude Code, log in, test it; DMG → Applications; the common errors verbatim with their fixes) that ships with the next release's page upload. The original error text is kept in parentheses because it is the only string a bug report can be matched on. |
| D301 | **`<meta name="fused-app">` in a page's head is THE app marker — the only signal, no `index.html` fallback: a folder's entry is its first tagged top-level page, at every depth 1-3, and rendering a tagged page IS the open (recency recorded and external folders registered by GET /render, superseding D297's button flow). A one-time startup migration stamps existing workspace apps; everything else is the author's to stamp** | Detection was name-based (D269: `index.html`, else first `.html`) — a guess about intent read off a filename, which made every checked-out repo a grid of cards and made an app invisible unless its entry wore the blessed name. The marker is the author STATING the intent, so detection is deterministic anywhere on the system, and the owner's explicit call was meta-ONLY: `index.html` keeps zero status, not even as a tiebreaker among tagged pages, because a name rule kept "just in case" is the old guess sneaking back in. One consequence accepted out loud: untagged apps — external folders, registry entries validated through `app_entry`, anything user-authored elsewhere — are invisible until their author adds the one line, and the authoring skill now carries that as the migration instruction; the showcase clone and deeplink clones are likewise invisible until the community repo carries the tag upstream (owner: "I will fix the upstream repo"). The depth rules COLLAPSE: one rule at depths 1-3 (the old per-depth strictness bounded how far a filename could be trusted; the marker needs no bound), a tagged entry owns its subtree below depth 1, entry-less folders stay walked shelves. Both rule copies updated in lockstep (`app_listing.app_entry`, `templates/shared/app_entry.entry_html` — PY-15/D166, parity held by tests); matched from the first 4 KiB only (the title read's budget) so the walk stays a bounded scan; a `meta` tag rather than a second `<html>` attribute on the user's own words. **Opens move server-side**: GET /render records the open when the page it serves carries the marker — workspace apps into `app_recents.json`, external folders into `registered_apps.json` (opening IS registering, D297's own posture, now without its button: `postAppOpen`/`recordAppOpen` are deleted from the shell, the endpoint survives for older clients). Card/preview iframes pass `_preview=1` to opt OUT; the default records, deliberately — a preview missing the flag pollutes recency (minor), a real open wrongly flagged never registers an external app (breaks the only path in). Templates render through /render too and carry no marker, so they never record. **The migration** (`meta_migration.py`): run-once (bookmarks-D97 shape, stamp file under `~/.fused-render`), on a daemon thread at both server entry points, discovering by the OLD name rules frozen privately inside it (the live walk can no longer see untagged apps). It inserts after `<meta charset>` else after `<head>`, skips what it can't safely edit (no anchor, non-UTF-8, unreadable), commits the stamp only in a repo that was CLEAN (a dirty repo stays dirty — never sweep wip into a migration commit), and **skips any repo with a git remote outright, subtree included**: externally synced trees (showcase, deeplink clones, user checkouts) would be left permanently dirty and their `--ff-only` pulls broken, and "has a remote" is the one discriminator that separates them from managed repos (fresh `init`, no remote) without hardcoding paths. Community installs/updates stamp their own copies (`_stamp_fused_meta` in staging and after `_replace_contents`) — those are remote-less repos we already commit into, and an un-stamped update would strip the tag and vanish the app. |

| D302 | **Speech to text runs on Metal through an `mlx_whisper` runner registered ABOVE the CTranslate2 one, and which engine serves a capability becomes a user preference — a REQUEST the registry may refuse rather than an instruction it must obey** | Two changes with one cause: a capability now genuinely has two backends on one machine, so "which one is running" stopped being a fact and became a choice. **(1) The runner.** D288 chose CTranslate2 for transcription precisely because it builds everywhere, and said an `mlx_whisper` runner could be added later above it; this is that addition, and it takes the Macs while no other platform loses anything. It cost three things the CT2 runner gets for free. **ffmpeg**: `mlx_whisper.transcribe(path)` calls openai-whisper's `load_audio()`, which spawns the binary this app deliberately does not ship (it bundles rclone), so the runner decodes with `av` in-process — 16 kHz mono float32, resampler flushed — and passes the WAVEFORM, which the library accepts on the same argument. That is the constraint that would otherwise have shipped a runner working on the machine it was written on and failing on every user's. **Progress**: SPEC AI-10a and `runtime.js` promise `onProgress` in seconds of audio, and faster-whisper delivers it for free by yielding segments as they decode, while `mlx_whisper.transcribe()` is one blocking call that returns everything at once — so a naive port draws a frozen bar for the length of the recording, which is a public-API regression rather than an internal difference. Chunking the audio ourselves was REJECTED: whisper's own window seeking (re-seek to the last timestamp, condition each window on the previous text) is what makes it accurate across a boundary, so hand-rolled chunking trades transcript quality for a progress bar. Instead the library's OWN frame counter is borrowed — it runs its window loop inside `tqdm.tqdm(total=content_frames, unit="frames")` — by swapping that module binding for the duration of the call. This reaches into another package's module globals and is named as the cost it is: guarded, restored on every path including cancellation, and falling back to honest indeterminate ticks (never an invented percentage) if a future version stops keeping one. Resolution is coarser than the CT2 runner's, one update per decoded window rather than per segment, in the same unit and never ahead of reality. It also buys a cancel the CT2 runner cannot manage: the hook is inside the decode loop, so an abandoned `transcribe()` raises at the next window instead of running a 90-minute file to its end. **Memory**: MLX's own allocator, not RSS — Metal's unified buffers are real and invisible to RSS, so the AI Models page would price a resident model at nothing. **(2) The preference.** `prefs.json` grows `engines`: capability → `"auto"` (the default, and the absence of a key — exactly today's behaviour) or a runner code, honoured by `registry.resolve()`, which every consumer already goes through so the supervisor, the catalog and the API cannot disagree. **An override naming a runner that cannot run HERE is ignored rather than honoured**, and that asymmetry is the whole design: prefs.json travels — people sync home directories, copy them between machines, restore them from backups — so a preference for MLX Whisper set on a Mac and obeyed on a Windows box would take speech to text away entirely. A preference that quietly does nothing is recoverable; a capability that has silently vanished is a bug report. The stored value is never rewritten to match reality either, because a choice corrected on read is one the user can neither see nor restore; the page shows it as stored with the registry's own reason beside it. Three knock-ons follow. Changing an engine EVICTS that capability's resident model, since one capability holds one and it belongs to the backend that loaded it — left alone it holds gigabytes for a runner nothing will route to again. The catalog's suggestions move with the engine (a suggestion is only meaningful for the backend that will load it, D293), which is correct but must not be silent, so the page says so at the moment the switch is made. And `runtime.js`'s `runners` array grew `active` beside `available`: those were the same answer while resolution was purely first-available, and with a preference in the middle they are not — on an Apple Silicon machine both whisper runners are available and exactly one is serving, and a page that cannot tell them apart cannot say which engine transcribed for it. **Unverified, and labelled as such** (SPEC AI-10d, following D288's and D293's precedent): the runner's flow is driven end to end against stubs, and the library contract it depends on — an ndarray accepted by `transcribe`, the frame counter reporting audio position — was checked by hand against mlx-whisper 0.4.3 on an Apple Silicon machine, but no CI can run Metal and no automated test has ever transcribed real audio through this runner. **One migration cost was weighed and ACCEPTED rather than missed**: registering `mlx-whisper` above `faster-whisper` re-resolves every existing Apple Silicon install on upgrade, so a Mac user who already has a CTranslate2 Whisper model cached is switched to a backend that cannot read it — their next transcription meets the format error, a fresh runner-environment sync and a new model download. Migration, pinning the previous engine, and a format-aware fallback were all considered and all rejected: the preference above already makes the old behaviour one click away, the error names the cause and a repo that works, and every mechanism for avoiding the switch would permanently encode "which engine did this machine happen to install first" into a resolution rule whose whole value is that it is decided by the machine and the user rather than by history. |

| D303 | **Recurrence speaks Google Calendar: a structured `rule` (freq/interval/byday/nth-weekday/until/count) runs beside cron, the repeat select says Google's sentences verbatim, and the when-row is a date chip over a month grid plus a time chip over a 15-minute list** | The cron sentences D296 shipped ("daily at 09:00") were reported unreadable — "the wording of repetition is hard to understand… copy the Gmail style of repeat as is" (Akshil, 2026-08-15) — and copying Google's words exposed that cron cannot SAY them: "Monthly on the second Wednesday" needs dom-AND-dow (classic cron ORs them), "every 2 weeks" needs an interval, "ends after 13 occurrences" needs a counter. So the vocabulary got its own engine rather than a lossy translation: `fused_render/recur.py`, dependency-free, naive-local like cron.py (a wall-clock promise), with the anchor being the form's picked date-time — the first run, and the date every derived part (weekday, day-of-month, nth) is read off. **Cron is NOT migrated or deprecated**: existing templates keep firing, the form edits them under a key of their own that submits the stored line verbatim (silently rewriting a rule the user wrote in another vocabulary is how an edit becomes a corruption), and the form simply stops WRITING cron. Semantics chosen where Google is silent: months and years SKIP rather than clamp (no 31st, no fifth Friday, no Feb 29 → no run, RFC 5545's posture — a run on the "wrong" day is worse than none); weeks are Sunday-anchored blocks counted from the anchor's week; `until` is inclusive of its date; `count` counts MATERIALIZED occurrences, so a skipped run still consumes one (the user cancelled that instance, the series did not stretch); an exhausted template stays `recurring` with an empty projection rather than growing a new state. The wording itself is a pinned contract — `describeRule`/`repeatChoicesFor` in schedule-lib with the exact strings under test — because the select's labels, the cards, the board and the popover must be one voice. **The when-row followed the same reference**: the one opaque `datetime-local` box became Google's two chips — a date field dropping a paged month grid, a time field dropping a 15-minute list centred on the current pick, still editable by typing ("8:30pm", "20:15") — both dumb views over the modal's single `when` string, so nothing about validation or dirty-tracking moved. **The folder browser was rebuilt on the same review** ("ugly and not intuitive"): the single ‹ Up chevron became clickable breadcrumb segments with the bold unclickable tail saying where you are, a filter box that narrows the listing and clears on every navigation (a surviving filter reads as "this folder is empty"), and a hover › on each row saying "this descends". |

| D304 | **Scheduling from the chat is one calendar button and a confirmed hop; the form's dropdowns are custom and position:fixed; Browse is a slide-in explorer beside the card; errors paint the field; "open" on a finished run lands in Explorer with the chat in hand** | One review (Akshil, 2026-08-16), six decisions. **(1) The composer's send-later pill and the kebab's Schedule item are GONE**; in their place, a calendar icon beside Send that asks first — a small popover ("Schedule this as a task?" / Continue / Cancel) — because the click navigates away from a drafted message, and losing a draft to a mis-click is the one cost this button must never have. Continue carries the whole handoff in the deep link: the draft as `message`, the folder as `target`, the open conversation as `session_id`, and the chat's own URL as `back` — so the form opens prefilled, offers **Back to chat** (only when a chat sent us; the round trip chat → schedule → adjust → again is the point), and a ONE-OFF continues that conversation while a repeating task always opens fresh chats (resuming one session on every run compounds its context forever). **(2) Every dropdown the modal owns is `position: fixed`**, measured off its trigger and flipping upward when the viewport below is short: the modal body scrolls, and an absolutely-positioned panel was CLIPPED at its edge — the month grid shipped cut off mid-row. Fixed positioning is also why the card SLIDES (left offset, not transform) when the explorer opens: a transformed ancestor re-anchors every fixed descendant. **(3) One control vocabulary**: the native `<select>`s (repeat, permissions, recurrence units) became the same quiet chip + fixed menu as the date and time fields, and the date/time chips were made pixel-identical — the pair had diverged because the modal body's generic `.deploy-body button` skin boxed the button half and not the input half, the third cascade-skin trap on this page. **(4) Browse stopped being a window inside the card** ("too small to see anything"): it is a fixed panel that slides in on the card's right at the card's own height, showing folders AND files (a folder click descends, a FILE click is the pick — a task may target a file), with the crumb bar and filter carried over and the listing taking the full height. **(5) Errors point at the field, not just prose**: a past time reddens both when-chips, and the path is validated EARLY (debounced listDir; a file resolves via its parent) with the red line under the row — the message alone under the form was "very easy to miss" while Save sat disabled with no visible reason. **(6) "Open in Inbox" became "Open in Explorer"** on rows, popover and board: the target folder opens with `_side=claude&session_id=` (the Inbox's own open-dir codec), landing the reader next to the files the run was about with the conversation loaded, instead of at a chat that forgot its files. The page's two description paragraphs were removed outright — the standing app-must-be-open essay explained a mechanic no one asked about at the moment they were reading it. |
| D305 | **The bundle ships the WHOLE importable standard library, not the part py2app traced — and a runner's failure is reported from the root of its exception chain, not from the wrapper** | Reported from a built DMG, and it is the real cause behind the `AutoTokenizer` report D292 addressed the wrong half of: loading a Qwen model failed with *"Could not import module 'AutoTokenizer'"*, while the actual exception was `transformers/dynamic_module_utils.py:17 import filecmp` -> `ModuleNotFoundError: No module named 'filecmp'`. transformers 5.15.0, tokenizers 0.22.2 and mlx-lm 0.31.3 were all installed correctly and the install was never interrupted. **The bundled interpreter's stdlib was incomplete.** py2app freezes the stdlib its modulegraph reaches from `app_entry.py`, so what shipped was the intersection of the standard library with whatever fused_render happens to import — and nothing in fused_render imports `filecmp`. That subset is not the app's own business: `Contents/MacOS/python` is the BASE INTERPRETER of every environment the app builds (PY-18, D176's self-locating symlink), so the hole is inherited by every project venv, every runner venv, and every third-party package inside them — and no rebuild can fix it, because the rebuild uses the same base. **`setup_py2app.py` now forces the whole importable stdlib** (packages whole, modules as includes, builtins skipped, anything `find_spec` cannot resolve on the build host skipped so a macOS build never asks for `msvcrt`), with `STDLIB_EXCLUDED` naming the handful left out and why — tkinter (Tcl/Tk is pruned), turtle (imports tkinter), idlelib, turtledemo, ensurepip (DP-3), lib2to3, antigravity, this. Same rule the `[bundled]` distributions already live under: shipped, or excluded with a reason, never merely absent. **build_dmg.sh verifies it twice** (step 4b-ter), with the environment stripped as 4b-bis does: once against `Contents/MacOS/python`, and once against a `python -m venv --without-pip` built ON it — the second is the path that actually broke, and a check of the app alone would have passed on a bundle whose venvs were unusable, which is 4b-bis's own bug one layer down. **The second half is the report.** transformers' lazy-module machinery swallows the ModuleNotFoundError and re-raises it as a sentence about the model, so `worker_base` now reports `describe_failure(e)` — the top message AND the root of the `__cause__`/`__context__` chain — from the load path and both generate paths. When that root names a module in `sys.stdlib_module_names`, the message says so explicitly and says that rebuilding the environment cannot help, because the distinction between a missing dependency and a missing stdlib module is the difference between one retry and an infinite loop of them. `mlx_text`'s wrapper drops its guess about an interrupted install: it was wrong here, and a confident wrong cause sent the user to delete an environment that had installed perfectly. |
| D306 | **Comment mode (D298's `annmode`) starts OFF — `=== "1"`, not `!== "0"` — and "Open in Claude Code" COPIES `cd '<dir>' && claude` instead of firing the `claude-cli://` deep link** | Both are the same complaint from one review (Akshil, 2026-08-17): the chat was taking over things the reader had not asked it to take over. **(1)** A pane opened in the right side panel arrived armed, so the first click on the app it was showing became a comment pin instead of a click — the comment layer's capture-phase swallower is exactly as invisible as it is effective, and reading a pane is the common case while commenting on it is the deliberate one. Absent now reads as disarmed; only an explicit `annmode=1` (the toggle, which still writes the param) arms it. The narrow-view reset guard is unchanged and its boot exclusion is now free: `annSetMode(false)` over an absent param writes nothing, and a URL that arrived carrying `annmode=1` keeps it. **(2)** The deep link handed the folder to whichever app the OS had registered for the scheme, which is a session started somewhere the user was not looking, in a terminal they did not choose. Copying the command is the move the config app's plugin/marketplace install commands already make — the clipboard, a toast, and the paste happens in the terminal and the session the user already has. New session only (a plain `claude`): resuming is not what this menu item ever meant. No starter prompt rides along either, because a prompt on the command line is SENT, while the deep link's `q` was deliberately a prefilled unsent line. The path is POSIX single-quoted (`'` → `'\''`), since the whole point is that it pastes verbatim. `templates_api.api_open_in_claude` still builds a deep link for the template inventory — a different surface, untouched. |
| D307 | **The chat's liveness signals stop being derived from things that only look live: the verb SHIMMERS for as long as a turn runs, following the tail is one reader-state flag instead of a per-frame geometry read, and "is this chat still running?" is a question for the server (`live_run`) rather than a param the URL may have lost** | Four reports from one session (Akshil, 2026-08-17), all the same shape — the page inferring state instead of holding it. **(1) The status line looked frozen.** Its seconds are recomputed from a start timestamp, so they cannot drift; what stalls is the *timer*, and both writers of that line are timers (the 1s interval and the 400ms poll), so a hidden or occluded window clamps them together and the line keeps whatever second it last drew. A `visibilitychange` redraw repairs it on the way back — one draw is enough precisely because the value is recomputed, never incremented. The same stall is reachable with the window in front: `renderMd` re-parses the WHOLE reply through marked + DOMPurify on every animation frame (O(n²) over a turn), so the typer now paints at ~25fps with the final frame always painted. And `w.stop()` moved out of the `isConnected` guard in pollLoop's `finally` — a detached working line used to leak its interval forever, one per turn. **(2) The shimmer is the honest signal.** A counter that ticks once a second reads as static in peripheral vision, and the token count beside it only moves when the model emits usage — a long tool call leaves both sitting still on a perfectly healthy run. A gradient sweep over the verb costs no layout and no element; `awaiting` (blocked on the user's approval) deliberately does not shimmer, because nothing is running there. **(3) Scrolling fought the reader.** "Am I at the bottom?" was re-derived from a 60px threshold at every write site including the typer's rAF, so a scroll up that had not yet cleared 60px was answered in that same frame with a write of scrollTop back to the bottom — which also kills the gesture's inertia. Escaping meant out-running the stream inside one frame, hence "sometimes I can't scroll". The same threshold failed inverted: anything that shrank the viewport mid-run (artifact strip, growing composer) widened the gap past 60px on its own and stopped the follow for good. Now `followTail` is set by the reader (a wheel-up drops it with no threshold; returning to the bottom restores it; a content *shrink* is not a gesture and is ignored), streaming output goes through `followBottom()`, and only a permission card — which the run is blocked on — still scrolls unasked. **(4) A chat reopened from the list was dead.** The run id lived in exactly one place, the `run` param on one history entry, so Back-then-reopen lost it: the detached process kept streaming into `RUNS/<id>` while the page rendered a mid-flight transcript that legitimately ends at the user's own message. `_live_run(file, session_id)` scans the newest run dirs for one that is `_alive` and belongs to this chat — matching `resumed_from` OR the CLI-minted id, now written to a `session` file beside the sidecar update, since `--fork-session` makes those differ — and `adoptLiveRun` puts the answer back on the URL and hands it to `resumeRun`, which could always adopt a run this frame did not start. Asked from the row click and from any boot carrying a session but no run. |
| D322 | **A task IS a Claude session: one noun, one thread, many messages — and with that, missed work queues instead of expiring, a repeat appends instead of forking, and repeat itself goes quiet behind a checkbox** | One design conversation (Akshil, 2026-08-17), and it reverses three earlier decisions on purpose. The app had two parallel worlds — a scheduled-message store it owned and a Claude session store Claude Code owned — joined by one field, and the page spent its effort reconciling them. They are now one thing. **A TASK is a session**; a task owns a **THREAD**; the thread's **MESSAGES** are every prompt sent into it, whether typed in a chat, typed in the template's chat, or fired by the scheduler — the three differ only in how the message arrived. What the app previously called a task (a prompt plus a due time) is one scheduled *message*. **(1) A project is the FOLDER**, because `_workdir` already makes it one: a file target's transcripts land in its parent's bucket, so files and folders were never separate session pools and pretending otherwise would have let one session belong to two projects. `TASK-nnn` numbers per project, allocated once and never renumbered; `MSG-nnn` per task. **(2) Titles come free** — Claude Code already writes an `ai-title` record into every transcript and refreshes it each turn, so the precedence is the user's own title, then that record (last one wins), then the first line of the first message. Nothing is summarised and no model is called. **(3) Reverses D296's "missed recurring runs are skipped" AND SCH-3b's 24h bound.** Missed work now QUEUES and runs when the app next opens, unbounded — but only because the queue became *visible and cancellable* first (the Calendar's Queued strip, cancel-each and cancel-all, a running entry carrying its own cancel). A bound was the old answer to "do not fire unattended"; a cancel affordance is the better one, and a bound without it only discards work silently. A repeating backlog still never replays: it **coalesces** to its latest run and counts the rest as skipped in one event. Scheduling into the past is now a legitimate way to say "run this now". No migration was needed, because the sweep persists `MISSED` and only ever acts on `PENDING`, so nothing already expired could be resurrected. **(4) Reverses D304's "a repeating task always opens fresh chats".** A repeat now appends into one thread, and the old worry (context compounding forever) becomes the user's explicit choice: **"New task each run"** mints a fresh task per occurrence instead. The mechanism is worth writing down, because the obvious reading of "a task is a session" is that chaining falls out for free — and it does not. A template is created with **no** session, because none exists yet: Claude Code mints the id on the first turn. So the template **learns its thread on first run** — when a run reports the session it actually ran in, that id is written back onto the template's `session_id`, and every later occurrence inherits it. Three guards, each of which is a bug if missed: write only while the field is empty (so a re-report cannot thrash it, and a session the user chose deliberately is never overwritten); never write when the template is set to fork; and fix up any *already-materialized* pending occurrence in the same pass, because `tick` materializes the next run before the previous turn has reported, so run 2 normally already exists carrying `""`. `_busy_sessions` also has to union `session_id` with `claude_session_id`, or run 2 resumes run 1's thread while its turn is still open. A repeating task handed off from a chat still refuses that chat's session and starts its own — it must not hijack the conversation the user was in — but once it has learned its own, editing the task carries that id through the cancel-and-recreate rather than orphaning the thread. Which of the two an id is, is RECORDED where it is written (`session_learned`, stamped by the writeback, copied onto occurrences, re-stated by the edit) and never inferred from whether the entry repeats — that inference could not survive a repeat → one-off → repeat round trip, where the learned thread came back looking like a chat handoff and was dropped. An absent marker reads as "the user supplied it", which is what every id in a store written before the marker existed is. **(5) Repeat goes quiet.** It was an always-visible dropdown; it is now a checkbox beside the time, with the rule revealed only when ticked, because most tasks do not repeat and the form should not argue otherwise. **(6) Unread is per message and means "arrived since I started using this"** — a read floor is stamped once per task at first initialisation, or day one would light up 174 of 192 rows and the badge would mean nothing. **(7) The calendar chip is a TASK, not a message**, like the other two views: one chip per task per day, anchored at that task's earliest run that day, later runs behind a `+N` and named in the popover. The alternative (a chip per message) was rejected for showing one task many times in a single day, which List and Board never do; the accepted cost is that a 7pm run has no chip at 7pm. Inbox lost its sidebar link — Tasks does its job — but keeps its route so nothing already open breaks. |
| D323 | **A model the user DOWNLOADED joins `catalog()`'s per-capability list, appended so it can never become the default** | The reported bug, and it had no workaround from a page. A user finds a model on the AI Models page's Discover tab, presses Download, the bytes land in the hub cache — and the model then appears in **no page's model picker at all**. Every page reads `fused.ai.models.catalog()`, which was `catalog.SUGGESTIONS` and nothing else; `fused.ai.models.list()` reports only resident workers and in-flight fetches; and `/api/ai-models`, the one endpoint that actually scans the disk, was never on the `fused.ai.models` surface. So no merged view existed anywhere, and a user who deliberately downloaded a model could not use it. `/api/ai/catalog` now unions each capability's curated list with the cached repos whose inferred capability matches. **The union is in the PAYLOAD, not in each page**, because three shipped apps (speech-to-text, local-text-chatbot, text-to-image) read this reply identically — find the capability, map `models[]` for `{id, label, size_gb, note}`, select `default` — so one change fixes all three and any page written next. **In `ai_runtime.py`, not in `catalog.py`.** That module is curation ("Curated, not fetched" is its first heading), has zero filesystem awareness, and teaching it to walk the hub cache would put a disk scan under `default_for()` — the hot path of a bare `fused.ai.image()`. The router already imported the cache reading for D321's capability inference, so the join costs it one import and costs the curation nothing. **APPENDED, and that is the whole of the care.** Nothing here reopens the smallest-first rule: `entry.default`, `catalog.default_for()` and `catalog.for_capability()` keep resolving over the curated list alone, because `default_for()` decides what a bare `fused.ai.transcribe()` loads and a rule that sorts on `size_gb` with the default at position 0 would hand that slot to whatever near-empty folder happens to be in the cache — the tiny repo is the danger, not the huge one. The cached tail is then sorted by the same rule (smallest first, nothing measurable last) so the two halves read as one list. **The capability comes from `ai_models.cached_models()`, built on `cached_capability()`** — the very reading the Load button is drawn from — so a picker offering a model and a `load()` refusing it cannot disagree (D321's discipline). Component repos, datasets, Spaces and anything whose capability cannot be inferred are not injected: inventing a capability for a repo that has none is the bug D321 closed. **And a capability is NOT ENOUGH — code review caught the first version injecting on it alone, which broke AI-11a's per-runner invariant inside the very array that invariant protects.** One capability's backends read mutually unloadable formats, so `openai/whisper-large-v3` (a speech model NEITHER shipping speech runner opens) landed in every page's speech picker, and an MLX conversion landed in the text list of a Mac switched to Transformers — pointedly, the exact trap this change's own skill-doc pitfall tells authors to avoid by taking ids from `catalog()`. `CachedModel` therefore carries `loaders`, the format's own answer from `ai/runners/formats.py`, and a repo is injected only when the runner the row resolved is among them. **Dropped rather than flagged `available: false`**: `models[]` has no availability field and every consumer reads it as "things I may offer", so a flag would leave all three shipped apps offering the unloadable repo until each learned a new key — the failure being fixed, not a fix. Nothing is hidden: the Local tab lists the repo with the actionable engine reason ("switch it on the Engines tab"), which a dropdown option cannot carry. **Three additive fields** — `source` (`"curated"`/`"cached"`), `downloaded`, `loaded` — let a picker mark the states without a second request; a repo that is both curated and on disk appears ONCE, as its curated entry with `downloaded: true`, because the hand-written label and frank note are the reason it was curated. `note` is `null` on a cached entry, never prose generated from a repo id, and `size_gb` is the real measured footprint. `loaded` is read live from `supervisor.resident_models()` (a dict read under the lock, no health probe — `describe()`'s per-worker request is the wrong cost for a polled inventory) rather than through the memoised scan, because residency changes on a second's notice and a disk inventory does not. That function reports `state == "ready" and _alive(w)`, both found by review: a Worker enters the table at `starting` and spends a multi-minute `uv sync` and a multi-GB fetch there, so returning every row lit the mark the instant Load was pressed and held it lit through the whole download; and skipping `refresh_memory`'s health PROBE is not a licence to skip its liveness check, or a crashed worker would read as held forever. **The memo is keyed on MTIMES, not on a timer** — four stats per repo (the folder, `blobs/`, `snapshots/`, `refs/`) where the alternative is `_scan_repo`'s recursive walk of every blob, so ~120 syscalls answer "has anything changed" for a thirty-repo cache. The first version used a 5s TTL over a signature built from the cache directory's entries alone, and review found that costing tens of thousands of syscalls several times a minute on a polled route; it also had a hole I had put there myself, since a repo folder's own mtime does not move when a blob renames into its `blobs/`. Both are gone: the signature composes the per-repo ones, and the minutes-long TTL now backstops only the single change no directory mtime can see, a listed file still growing — which is a download in flight, whose bytes the job row reports live and far better. A TTL alone would have reintroduced the exact bug — the model you just downloaded, missing from the picker. **Rejected:** doing the union in `catalog.py` (a disk walk under the curation, and under `default_for()`); a `default:` field so the merged list could be ordered one way and defaulted another, which catalog.py's docstring explicitly forecloses; and a separate `fused.ai.models.cached()` call for pages to join themselves, which is the duplication that left three apps identical and all three broken. The Discover tab filters to `source == "curated"`, because its grid is the shortlist and the Local tab is already "what is on my disk" — the one legitimate exception to "render every entry", and the skill doc now carves it out by name rather than stating a blanket rule the repo itself breaks. **The invariant is stated as what actually holds**, review's last catch: wherever anything is curated, index 0 is curated and agrees with `default`; a runner with no `SUGGESTIONS` key reports `default: null` with cached entries in the list, because there is genuinely nothing to recommend and dropping them would hide the only models that runner has. So the contract is read `default`, never `models[0]`, and `source` is what lets a consumer inventing a fallback refuse an uncurated one. SPEC AI-11e added. |

| D308 | **The image recipe's download scope is an ALLOW-list of what `from_pretrained` reads, `size_gb` means every byte a Download fetches across every repo it touches, and a repo this app pulled as PART of something else says whose it is on the AI Models page instead of wearing "no engine"** | Three defects with one root: nobody had checked what the FLUX.2 download actually puts on the disk. **(1) The skip list saved nothing.** AI-9b's recipe ignored `transformer/*.safetensors` to avoid the 7.75GB bf16 transformer its Q4_K_M GGUF replaces — and `black-forest-labs/FLUX.2-klein-4B` publishes those same weights a second time as a root-level `flux-2-klein-4b.safetensors`, the ComfyUI-style single-file bundle, which no `transformer/*` pattern matches, `AutoPipeline`/`from_pretrained` never opens, and `download_snapshot` therefore fetched. Measured against Hub metadata: 23.74GB whole repo, 15.99 kept by the old deny-list (7.75 of it the unread root file), plus the 2.60 GGUF = **18.59GB where the model needs 10.83** — the deny-list bought nothing at all beyond the file it added. Adding one more skip pattern was REJECTED: a deny-list has to predict every extra bundle a repo may carry and repos gain them without warning, while what `from_pretrained` READS is knowable and finite. So `keep` is an allow-list — `model_index.json`, `scheduler/*`, `tokenizer*/*`, `text_encoder*/*`, `vae/*`, and `transformer/config.json` but not its weights (`from_single_file(config=…, subfolder=…)` reads that config, and a download that cannot load offline has not done what the button said). `download_snapshot` takes `allow_patterns` as a first-class argument beside `ignore_patterns`, because one that only reached `snapshot_download` would fetch a tenth of a repo behind a bar priced at all of it; both go through one filter (`worker_base.selects`) carrying huggingface_hub's own semantics — fnmatch on the repo-relative path, `*` crossing `/`, ignore winning over allow — so the bar's total, the segmented fetch and the fallback cannot disagree. The old test grepped worker.py for the pattern STRING, which is exactly why a recipe that saved nothing passed; the new one applies the recipe's patterns through `selects` to a frozen listing of the real repo and asserts the SET that lands on disk. **(2) `size_gb: 2.6` described none of the download** — the GGUF alone — while the `mflux-image` entry beside it has always meant the whole snapshot, and since AI-7d that field both sorts the list and picks `default_for()`. Two adjacent lists whose field means different things is a silent ordering bug the moment either gains a second entry, so it is 10.8 (8.23 of base components + 2.60 GGUF) and the module docstring states the rule where the ordering rule is stated. **(3) Two repos on the page were nobody's models.** The GGUF (2.4GB) and `onnx-community/silero-vad` (2MB, pre-fetched by AI-10f so an offline machine still has the detector) both read `cached=True, capability=None` and wore the quiet "no engine" tag — true, and no help at all to someone reclaiming disk: deleting the first breaks the image model, deleting the second only makes transcription slower. The listing now carries `component` and the card says "part of FLUX.2 klein 4B" with the consequence in its hover. **They are NOT hidden or folded into the parent's card**: this page's job includes showing what is eating the disk, so a 2.4GB row that vanished would be a worse page than one with a mystery on it; and they stay deletable, because the cost is now stated rather than discovered. The registry is `runners/formats.py`'s `COMPONENT_REPOS`, for the reason `MFLUX_VARIANTS` lives there — the ids are named inside runner folders that are separate venvs the server process cannot import — with the workers reading the FILENAME back out of it instead of keeping a second copy, and a test asserting every recipe's component repo appears there so a new recipe cannot reintroduce an unexplained row. `cached_capability` reports them as "a speech detector that belongs to MLX transcription", which is a better refusal than D321's "model repo" reading. **One consequence for anyone who already downloaded FLUX.2 through the old recipe** (the author included): the 7.75GB `flux-2-klein-4b.safetensors` blob is already in their cache, and nothing deletes it — it is orphaned rather than reclaimed. It is reachable and reclaimable by hand, since a snapshot's file list is visible on the page's revision rows; an automatic sweep of "files a recipe no longer wants" was not built, because deleting from a user's cache on a version upgrade is a much bigger promise than fixing what the next download fetches. |
| D309 | **`transcribe({diarize: true, speakers: N})` labels every segment with who said it, from ONE implementation at the runners root that both whisper engines import — sherpa-onnx over two ungated ONNX models, with `speakers` required rather than guessed** | The third thing `fused.ai.transcribe` promises about audio and not about backends, after D302's second engine and D320's shared VAD. **The count is required, and that is the load-bearing decision.** sherpa's clustering takes either a number of speakers or a cosine threshold, and a threshold is not a number a page author can hold an opinion about — the same recording answers 2, 4 or 7 across its plausible range. A guess therefore produces a transcript that is confidently wrong about how many people are in the room, which is worse than a refusal, so `speakers` is validated in `runtime.js` (rejecting `bad_request` before a job row exists, beside the `path` check), at `/api/ai/transcribe` (because the bridge is not the only door — anything on this machine holding `X-Fused` can POST), and inside each worker (because neither of those is either). `true` and `"2"` are refused explicitly: `{diarize: true, speakers: true}` is a plausible copy-paste that `isinstance(x, int)` alone reads as one speaker and labels the whole transcript "Speaker 1", and a count read out of an `<input>` without a `parseInt` is the common way this argument arrives wrong. **The implementation is ONE file at `runners/diarize.py`**, beside `worker_base.py` and `formats.py` rather than inside either whisper folder, and that placement is the whole of AI-10c's "identical labels": the models, the one-based `Speaker N` naming, the overlap arithmetic and the tie-break exist once, both venvs reach them through the same `sys.path` insert that reaches `worker_base`, and there is no second copy free to drift — a property a test pins structurally (no `diarize.py` under either runner) rather than by asserting the two engines agree, which two copies would also pass. **sherpa-onnx over pyannote.audio, and the dependency was the decision again.** pyannote needs PyTorch, which `mlx_whisper/pyproject.toml` refuses on principle for a small helper model — the same argument D320 makes about Silero, one model further — and `pyannote/speaker-diarization-3.1` is `gated: auto`, so an unattended download fails without a Hub token on a machine nobody is sitting at. sherpa-onnx runs on ONNX Runtime, which is the same inference engine the MLX runner already carries for the VAD — **though not the same COPY, and the original wording of this entry ("the ONNX Runtime the MLX runner already carries") was wrong in a way that cost a shipped-broken feature.** sherpa links its own `libonnxruntime.dylib`, published in a separate `sherpa-onnx-core` distribution; `onnxruntime`'s copy is `onnxruntime/capi/libonnxruntime.1.28.0.dylib`, which is not on sherpa's rpath and cannot serve it. So the honest version of the argument is that sherpa needs no PyTorch and no new KIND of dependency, not that it needs no new bytes — it adds ~30MB of its own runtime to each venv. **That mistake had a direct consequence.** `sherpa-onnx` 1.13.5 publishes two shapes of wheel under one version — the `linux_armv7l` ones self-contained (12MB, bundled `libonnxruntime.so`, no `Requires-Dist`) and every other one a ~2MB shim declaring `Requires-Dist: sherpa-onnx-core==1.13.5` — while its sdist's PKG-INFO declares no dependencies at all. `uv lock` is a UNIVERSAL resolution, one dependency set per package across all platforms, so it took the empty one: `sherpa-onnx-core` never entered either lock, `uv sync` installed the shim without the library it links against, and the first diarized transcription on BOTH engines died with `Library not loaded: @rpath/libonnxruntime.dylib`. It survived review because `uv pip install` resolves for the CURRENT platform, reads the real wheel's metadata and pulls the companion — so the hand-built scratch venv every verification above ran in worked perfectly, and only a venv the shipped installer produced failed. Both `pyproject.toml`s now name `sherpa-onnx-core` directly, with the mechanism written down beside it because it reads exactly like a redundant line, and `tests/test_ai_runner_deps.py` fails any runner that declares one without the other. **The lockfiles are gitignored, so no test can assert on them**, and no test can build a runner venv either (`conftest.py` redirects `FUSED_RENDER_HOME`, and CI has no Apple Silicon) — so the declaration guard is the automatic half and a from-scratch rebuild is a manual one. Both runner venvs were deleted and rebuilt through `envinstall`'s real detached `uv sync`, after which `import sherpa_onnx` succeeded, `sherpa_onnx/lib/` contained `libonnxruntime.dylib` beside the extension module, and a real two-speaker recording came back correctly labelled on each engine. **The embedding repo was chosen by measurement, not by name.** `Wespeaker/wespeaker-voxceleb-resnet34-LM` is the WeSpeaker team's own, ungated, CC BY 4.0, and carries exactly the ONNX the name promises — but that file has no `model_type` in its ONNX metadata, and sherpa-onnx does not raise on that: it prints "Unknown model type for speaker embedding extractor!" and ABORTS THE PROCESS from C++, which would reach the job row as "the transcription process did not answer" with no Python traceback anywhere. `csukuangfj/speaker-embedding-models` is the same weights with sherpa's metadata added and is what ships; CC BY 4.0's attribution requirement is met in the repo's `COMPONENT_REPOS` `what` text, which is what the AI Models page renders. Both models are registered there rather than given a capability constant or a dropdown entry, because they are components of transcription and not a capability — `of: None`, since the same two files serve every transcription whichever whisper repo is loaded, on either engine. **Diarization runs on the FULL waveform, before and independent of the VAD.** The segmenter finds its own silence and is better at it than a threshold over Silero probabilities, and feeding it VAD regions would cluster voices across cuts made for a different purpose. It composes because `_transcribe_regions` already maps every segment back into original-recording time (D320), so the join is a join in one coordinate system and not a second remap: each segment takes the speaker it overlaps MOST, summed PER SPEAKER rather than per turn (one speaker holds several turns inside one sentence when an interjection splits theirs, and taking the longest single turn hands the segment to the interrupter), with ties going to whoever started speaking first and then to the lower index — because the alternative is dict iteration order deciding it, and then the same recording labels differently on two runs and a page's speaker colours shuffle between them. A segment overlapping no turn is `null`, not "Speaker 1": Whisper heard words where the segmenter heard nobody, and inventing a speaker is the confident version of a wrong answer. **The progress contract did not move.** `done`/`total` stay SECONDS OF AUDIO of the TRANSCRIPT (AI-10a, and `runtime.js` promises it to pages); the pre-pass reports as its own stage — a `detail` line with an indeterminate bar, which the job row already offers and which the audio decode above it already uses — because filling them would either inflate `total` past the recording or run the bar to 100% before a word was decoded. **faster-whisper does not normally hold a waveform, and that was the real risk.** It is handed a path and decodes inside the library, so the samples sherpa needs do not exist in that process. Decoding a second time with `av` was rejected (a 90-minute recording decoded twice, and `av` named as that folder's own dependency to reimplement a function faster-whisper exports); giving that engine a cheaper diarization or none was rejected outright, because "the CT2 runner labels worse" is exactly the two-behaviours-one-argument failure the runner split exists to prevent. What ships is `faster_whisper.audio.decode_audio` — the very function `transcribe()` calls on a path — with the array handed back to `transcribe()`, which accepts one on the same argument: one decode, one library, no new dependency, and the diarized and undiarized paths differing by an argument rather than by a pipeline. The rate comes from `model.feature_extractor.sampling_rate` rather than a constant, because `transcribe()` does not expose `sampling_rate` and an array decoded at another rate transcribes a chipmunk with no error anywhere; `diarize.speaker_turns` asserts it against the session's own rate for the same reason. **Output is additive**: `diarize` defaults false, so a run without it writes the bytes it always did, key for key — pinned by a test rather than assumed. **One unrelated bug fell out of building it.** `worker_base.download_file` had no way to say which job a fetch belongs to, so `report(job=None)` fell through to the process's `JOB_ID` — the model's own load row, finished long ago. Fetching the two models on the first `diarize: true` reopened a completed download as running, for something nobody asked for, while the transcription row the page was watching said nothing; `job`/`row` now thread through, with the row's identity restated on every fetch tick because the manager can rebuild any row at any tick and a report with no `title` is refused outright. The speech detector fetched inside `_speech_regions` on a machine whose whisper download predates D320 had the same bug, fixed by the same closure. **Binding the row made a second gap live**: the fetch ticked with a plain `report`, which was enough while these fetches owned a model-load row (whose ✕ the supervisor answers by killing the process) and is not enough on a transcription row, where the ✕ has to stop THIS work — so the user pressed it, the manager set `cancel_requested`, and 33MB carried on downloading behind a row still reporting running. It ticks through `report_or_cancel` now, and reads the `/cancel` route's `CANCEL` flag only when a `job` was passed, because that flag belongs to whatever holds `GENERATE_LOCK` and reading it on `_bring_up`'s own thread would abort a model download because some earlier generation was cancelled. A ✕ landing as the fetch FINISHES is not honoured, by the rule `_call_with_ticks` already states: the bytes are on the disk. Two callers followed — `download()`'s VAD pre-fetch caught `Exception` broadly and would have swallowed a real ✕ and reported the download done, and `_speech_regions`' docstring called that hazard latent when it is now live. **The MLX ETA had the same shape of bug as the progress unit**: `transcribing_since` was set ABOVE the speaker pre-pass, so `_transcribe_regions`' `elapsed / speech-decoded` rate carried the whole pre-pass while `done` was still ~0, inflating every early ETA by minutes on a long recording — the failure that variable was introduced to prevent for the audio decode, reintroduced one phase later. It sits below the pre-pass now, where `faster_whisper/worker.py` already had it. **Not pre-fetched by Download, unlike the VAD**, and the asymmetry is deliberate: `vad` defaults true, so a model downloaded on wifi and used on a train would lose a feature the user never asked for, while `diarize` defaults false and charging every whisper download 33MB for a flag most callers never pass buys nothing. The consequence is stated rather than hidden — the first diarized transcription on a machine downloads them, and an offline machine's first one fails. **Verified by hand on this machine** (no CI can run sherpa-onnx plus 33MB of models): `csukuangfj/speaker-embedding-models`' own two-speaker English recording (16.0s) came back as four turns, 1.58-3.41 and 4.40-6.46 as one speaker and 9.35-11.47 and 12.16-14.64 as the other, which is that file's truth; its four-speaker Chinese recording (56.9s) found four; and the whole `faster_whisper/worker.py` `generate()` was driven end to end on the first of those against `Systran/faster-whisper-tiny.en`, producing two correctly-attributed segments, the top-level legend, and a byte-identical output shape on the same file without `diarize`. What remains unverified is everything about REAL meetings: crosstalk, a speaker who joins late, a noisy room, and whether the `speakers` a user types matches what the clustering wants when one person barely speaks. |
| D310 | **Image generation gets an `mflux` runner on Apple Silicon, registered ABOVE the diffusers one: the Mac default, with its memory ceiling accepted as a known risk rather than treated as a blocker** | The same MLX-beside-torch move D302 made for transcription, and arranged the same way — all three multi-runner capabilities now put MLX first on the machine it serves. **The case for it is real and was measured** on this machine, same model, prompt and seed at 1024x1024, `black-forest-labs/FLUX.2-klein-4B` via `mlx-community/FLUX.2-Klein-4B-4bit`: load 0.59s vs 4.67s, cold time-to-first-image 23.3s vs 44.6s, steady-state 4-step generate ~34s vs ~41s, and — the cleanest win — a single 4.6GB download instead of the ~10.1GB two-repo split AI-9b's GGUF recipe needs, since a quantized transformer still requires the bf16 base repo for the text encoder, VAE and tokenizer while the MLX conversion is 4-bit throughout. **The risk accepted in promoting it is that the benchmark is one machine.** MLX's allocator reported a ~23.6GB `get_cache_memory` high-water — larger than torch's ~19.1GB Metal driver allocation for the same render — on a 34GB box that was already 5.6GB into swap with 1.5GB of swap headroom left; the 28-step comparison was contended and is unusable; image QUALITY was not compared at all; and nothing has been run on a 16GB Mac, which is precisely the machine AI-9b exists because full-precision FLUX OOMs on. A step-shaped run-over-run slowdown (21s → 35s → 33s → 34s at 4 steps) is *consistent* with allocator-pool pressure but equally consistent with one-time MLX kernel compilation or thermal throttling; no per-run memory time series was captured, so it is circumstantial. That is the evidence this default rests on, and it is written down here because the failure it would produce — a small Mac swapping or OOMing on a render that used to work — will not look like an ordering decision when it arrives. The engine preference shipped in D302 is the way back, and the row's `note` names the ceiling before a download starts; moving the row down again is one line. **Two library-shape details are load-bearing and are written down because neither is guessable.** mflux's `generate_image()` takes no callback argument: progress and cancellation go through `model.callbacks`, a per-MODEL `CallbackRegistry` whose `register` APPENDS — so the reporter is registered once at load and reads the in-flight request from a module slot, because registering per call would have the tenth render report every step ten times, nine of them to job rows that are closed. And `GeneratedImage.save()` defaults to `overwrite=False`, which does not raise but RESOLVES the collision by writing somewhere else — so the save passes `overwrite=True`, since the server chose `out` and already told the caller about it. **`memory()` reports `get_active_memory` (~14.1GB), not `get_cache_memory` (~23.6GB)**: the pool is buffers reserved from Metal and not returned, and the AI Models page puts this number in a column beside the torch runner's *allocated* bytes — reporting a reservation there would say a resident image model costs two thirds of the machine. The pool is a fact about memory pressure and is recorded here rather than in a field that means something else. **Unverified:** the runner's own flow is driven entirely against stubs (no Metal on CI), the library contract was read off mflux 0.13 by hand, and no image has been rendered through this worker as opposed to through the benchmark script it was modelled on. |
| D311 | **The transcript becomes visible AS it decodes, through a `<out_base>.partial.jsonl` the worker appends to and `runtime.js` tails off the poll it was already running — not through the job row and not through a held-open stream** | `fused.ai.transcribe` wrote its `.json` and `.txt` once, at the end, so a 90-minute recording gave a page twenty minutes of a moving bar and no words — while every one of those words had existed since minute one, in a list in the worker's memory. **Both channels that already existed were rejected, and for different reasons.** The job row is a download-manager record: `done`/`total` and a capped one-line `detail`, rebuilt at any tick from whatever `report` last sent, polled by every watcher of every row. Widening it into a data channel would make each poll carry the whole accumulated transcript to everyone, and `jobs.py`'s record is a fixed dataclass precisely so that this is a decision rather than a field somebody adds. `worker_base._stream()`'s chunked NDJSON is the other, and it is what the text runners use with `streaming=True` — but `supervisor.generate_text` relays that stream only while the browser holds the connection open, and transcription is deliberately job-shaped: fire-and-forget, outliving the calling page, surviving a navigation. Putting the words on a held-open connection would trade the one property that makes this call different from `fused.ai()`. **A file keeps both**: a tab that navigated away and came back can read what landed while it was gone, and the run costs one `write` per segment. **One implementation at the runners root** (`runners/partial.py`, beside `worker_base.py`, `formats.py` and `diarize.py`), by the rule AI-10c states and D309 followed — the line shape, the flush rule and the lifecycle exist once, both venvs reach them through the same `sys.path` insert, and a test pins it structurally, because a second copy under either runner folder would fail no behavioural test: both copies would pass their own. **Flushed per segment, one `write` of a complete line**, because the reader is tailing rather than waiting: a buffered writer would show nothing until the end (the exact failure this exists to fix) and a flush landing mid-line would hand a reader half a JSON object. **`ensure_ascii=False` matching the final dump is load-bearing beyond consistency** — the reader advances by BYTE offset, and escaping would not corrupt anything but would put every non-ASCII transcript's offsets out of step with its own content. **Diarized lines carry their speaker immediately**, which is only possible because the segmenter runs as a pre-pass over the full waveform before a word is decoded (D309); it is the same `speaker_for`/`label` arithmetic `assign_speakers` runs at the end, so a label cannot change under a page that already rendered it. The sink never mutates the segments it is shown — they ARE the list about to be dumped — which is what keeps the final `.json` and `.txt` byte-identical for every existing call, pinned by test on both engines rather than asserted. **Lifecycle: removed when the real output lands, removed on cancel, LEFT on error.** The first two are duplicate bytes in a directory the user browses; the third is the only salvage from a run that died halfway, and deleting 80 minutes of a 90-minute transcript to keep `ai/transcripts/` tidy is the worst trade available here. Which exception counts as a cancel is an ARGUMENT rather than an import of `worker_base.Cancelled`, for the reason `diarize.model_paths` takes its downloader as one: the server imports this module too, to derive the path it advertises. **`runtime.js` does the tailing, and that is the actual deliverable** — a page must not have to implement byte offsets, partial lines, multibyte boundaries and a file that disappears mid-poll to render words that already exist. `onSegment` rides the tick `onProgress` was already costing: no second loop, no new transport, and no extra request at all for a caller that does not pass it (pinned, because a bridge that tailed unconditionally would add a request per poll to every existing transcription for a file nobody is reading). **It reads by `Range`, and that was verified against the route rather than inferred**: `/api/fs/raw`'s local-file exit hands off to Starlette's `FileResponse`, which answers 206 with a `content-range` and 416 once the offset is at or past the end — driven through the real app before the reader was written, because "FileResponse supports ranges" is a claim about a library and the route wraps it in four other exits. Read-and-skip was the fallback and is not needed; it would have re-downloaded the whole transcript every 700ms to learn a few hundred bytes. **The finished `.json` stays the source of truth and drains whatever the tail did not reach**, reconciled by delivered COUNT rather than contents (the file's Nth line is the transcript's Nth segment by construction, since both engines append in the order they build the list). That is what makes "every segment, in order, exactly once" hold in the cases the tail cannot cover: a worker that finished before the first poll, a tab asleep through the whole run, a server too old to advertise the path, a 200 from something in front of the route that ignored the Range — and the last segments, which are written and then removed inside a single tick nearly every time. **One gap was found by writing the SKILL entry**, not by a test: the salvage file was written, advertised on the POST reply and documented, and *unreachable* from the only situation it exists for, because a caller of the bridge never sees the reply — only the rejection. `err.output`/`err.outputPartial` now ride the failure, on `cancelled` as well as `ai_error` so a page checks the read rather than the rejection type. **Unverified:** no real long recording has been transcribed through this — every claim above is driven against stubs (a fake decoder for the workers, a node harness with a stub `fetch` over a growing buffer for the bridge), so what is proven is the logic and not the behaviour under a real 90-minute file: whether the flush cost is invisible at thousands of segments, what a page actually looks like rendering them live, and whether a real browser's `fetch` caches a ranged read of a file that keeps changing under it. |
| D312 | **SUPERSEDED by D313 — the removal was reverted before it left the branch; the diagnosis it rests on was right and the remedy was too broad.** The Discover tab's Hub search is deleted — box, task filter, sort, read-only result grid, `POST /api/ai-models/hub/search`, `GET /api/ai-models/hub/tasks`, `routers/hub_models.py` and `tests/test_hub_models.py` — leaving the curated catalog as the whole tab, laid out in the Local tab's section language** | The search was never able to hand anyone a model. **A repo runs in this app only if a registered engine reads its weight format** (the Local tab's engine tag is that verdict, and the three formats of Whisper are mutually unloadable), so a query returning `sentence-transformers/all-MiniLM-L6-v2` and `google-bert/bert-base-uncased` was offering a browsing surface over tens of thousands of repos to a page that could act on about a dozen — and it said so itself, in the caption under the box: "Search results are read-only; the suggestions above can be downloaded." A control whose own caption explains that it does nothing is dead weight, not a rough edge. HS-1 shipped read-only because a download had no progress surface; D258 built one and made the CURATED entries downloadable, which is the moment the search results became the only cards on the page that could not be acted on. **Everything the section argued for survives without it.** HS-2's join — "already on this machine" — is the ✓ on a suggested card, answered from the page's own cache walk rather than a second one inside the search. HS-4's consent posture is strictly stronger: with `hub_models.py` gone this app makes no outbound request of its own at all, so the token handling, the fixed host, the `HF_ENDPOINT` validation, the TTL cache and the errors-are-never-cached rule are all moot rather than merely unused. HS-1a's guarded-POST asymmetry existed solely to protect that outbound call. **The deletion is total** (D264's precedent, the same one D265 followed): the router module, its 31 tests, the four `Hub*` types and two functions in `api.ts`, and the `.am-hub-*` styles, rather than a dead endpoint left for a caller that is not coming. **What replaces it visually is the Local tab's own vocabulary**: each capability becomes an `.am-section` with the ALL-CAPS `.am-subgroup-title` over a rule and the "via MLX LM" runner in the head's right-hand slot where Local puts its byte subtotal — the lowercase inline label it replaced read as a caption belonging to the first card rather than a heading over all of them. The runner notes go MUTED from `--warning` orange: none of them reports a problem ("transcribes on the GPU", "reserves much more memory"), they describe what a backend is LIKE, and three orange paragraphs down one page teach the reader that orange here means nothing. **Rejected:** keeping the box and filtering results to loadable formats — the Hub does not expose "is this mlx/gguf/diffusers-shaped" as a query, so the filter would be a client-side pass over a page of results that mostly returns nothing, which is a worse experience than no box; and keeping the endpoint for a future caller, which is the parked-destination anti-pattern D265 named. SPEC §39 retired; ARCHITECTURE's `/api/ai-models/hub/*` section replaced with the removal note. |
| D313 | **Hub search stays, rescoped: the box moves to the TOP of Discover, results REPLACE the curated sections rather than sitting under them, and the server constrains both the filter menu and the results to pipeline tags a registered runner can actually load — so every card on the tab carries a working Download button** | D312 read the complaint ("search functionality of things we don't support") as an argument for deleting the search. It was an argument about WHAT THE SEARCH RETURNED. The shortlist cannot answer "what else is out there", which is a real question, and the fix is to make the answers actionable rather than to stop asking. **Three changes, and the third is the one with teeth.** (1) The box is at the top: it was below three screenfuls of suggestion cards, so the only way to reach the search was to scroll past the thing it exists to be an alternative to. (2) One grid at a time — a query swaps the curated sections out and clearing the box brings them back, because a shortlist answers "what should I even get" (the question you have BEFORE you type) and showing both asks the reader which of two grids is talking to them. (3) **The constraint lives on the server, and it is asked of the registry rather than listed**: `supported_tags()` filters an editorially-ordered candidate list through `registry.capability_for_task`, the SAME function that decides whether a repo already on disk gets a Load button — so "searchable" and "loadable" cannot drift, and adding a text-to-speech runner makes that filter appear with no edit to `hub_models.py`. Four tags survive today (`text-generation`, `image-text-to-text`, `text-to-image`, `automatic-speech-recognition`) against twenty-six offered before. **The menu constrains what can be ASKED; `_model_row` constrains what comes BACK**, including for an unfiltered query where the Hub answers with whatever it likes — a row is dropped for an unrunnable tag, for NO tag (we will not promise a button for a repo we cannot classify), and for `gated` or `private`. Dropping gated is the one real cost of this row and it is deliberate: the pill that used to explain it was honest and still left a button that would 403, and "every card here works" is worth more than reaching a licence-gated repo through a search box when the Hub link and the curated catalog both still reach one. Every surviving row carries a non-null `capability`, which is exactly what the Download button hands to `/api/ai/runtime/download`. **An unfiltered query over-fetches 4x (capped at 200) and truncates AFTER filtering**, so `limit` means "rows you will be shown" — truncating the Hub's page first would turn a page of embedding models into two results; with a task filter the Hub has already constrained, so it asks for exactly what it shows rather than spending someone's rate limit on rows it throws away. **The filter is by capability EXISTENCE, not by what resolves on this machine** — the Engines tab lists all three capabilities on every platform, a runner that cannot run here refuses the download with its own sentence, and making search results host-dependent would answer one query two ways for reasons neither user could see. **Rejected:** filtering client-side (the app would still be spending the user's rate limit fetching rows to throw away, and the count of visible results would depend on how many junk rows the Hub happened to send); one outbound request per supported tag to get an OR (three times the rate limit per keystroke for better recall than over-fetching already gives); and keeping the read-only results with a clearer caption, which is what D312 correctly judged to be dead weight. SPEC §39 rescoped rather than retired; ARCHITECTURE's `/api/ai-models/hub/*` section rewritten. |
| D314 | **Both faces of Discover carry a heading that names them — "Suggested models" and "Search results", at the SECTION tier with the capabilities demoted to subgroups under it, each heading with a muted count-and-provenance beside it — and the state-dependent prose moves underneath its heading. One vertical spacing scale (`--am-gap-*`) serves both tabs.** | D313 made a query REPLACE the curated sections, which is the right layout and left the swap unlabelled. The two states differed by one thing: a paragraph reading "Suggested models — picked to run on this machine" was present or it was not. That is a difference only a reader who happened to be watching the moment it changed could perceive; search, scroll through twenty-four cards, look back up, and the top of the page says nothing about whether these are vetted suggestions or whatever the Hub sent. **The fix is that each face says its own name in the same slot**, in the language the page already uses for "this run of cards is a group" — `.am-section-head` with an ALL-CAPS `.am-subgroup-title` and one muted fact at the far right, which is exactly what a capability group looks like. The muted fact is the count AND where it came from, because that pairing is the whole distinction: `11 picked for this machine` against `"whisper" · 24 on huggingface.co`. **Two deliberate weights.** The heading is unmuted over a 2px rule where a capability's is muted over 1px, so it reads as the PARENT of the three heads below it rather than as a fourth sibling — in a run of identical ALL-CAPS heads, weight is the only vocabulary left to say "this one is above those". And the query is quoted only when somebody typed one (a task filter alone is still a search, and `""` beside the heading would report a query nobody entered) with the count omitted entirely while the request is in flight, because `0 on huggingface.co` over an empty grid is a wrong answer where silence is a missing one. **The prose follows its heading.** The suggested lede loses its bolded `Suggested models —` prefix (the heading now says that) and keeps the reason; the host disclosure moves from above the whole tab to directly under `SEARCH RESULTS`, so both faces are heading, note, grid in that order and swapping one for the other reads as a label changing rather than as the page rearranging itself. **The heading is one string on `DiscoverChrome`, not two conditions** — the same rule `showsPreamble` follows, for the same reason: two `&&`s that are opposites today are two chances for the page to name itself twice or not at all. **Rejected:** a badge or tinted border on the result cards to mark them as Hub hits — a heading that says `SEARCH RESULTS` over a grid whose every card already links to the Hub does not need twenty-four repetitions of the same fact, and D313's point is that these cards are as actionable as the curated ones, which per-card chrome would quietly contradict. Also rejected: giving the suggested face a heading and leaving the results face bare, which is the asymmetry that caused the complaint. SPEC §39 gains HS-0c. |
| D315 | **A runner's `note` moves off the Discover tab's capability headings and onto the Engines tab, under the row of the engine actually serving that capability** | Three of the six runners define a `note`, so on Discover it appeared over some capability sections and not others and the page read as blotchy — the user called the lines random noise and asked for them gone. **Blotchiness is the symptom; the placement is the fault.** A note says what a BACKEND is like, and Discover is a page about MODELS: over a grid of downloads it is a fact with nothing to act on, printed above cards it does not describe. The `mflux-image` one is not even a fact — it is a caution (`Reserves much more memory than Diffusers and is untested below 32GB`), and D310 accepted MLX FLUX's memory ceiling as a known risk **precisely because a user who hits it can move back to Diffusers from the engine picker**. That sentence is the instruction for doing so, and it was sitting two tabs from the control. Under the picker it is beside the decision it is about, and it changes when the picker does. **Rendered for the EFFECTIVE runner only** (`engineNote`, the discipline `servingLine` already follows): a note about a backend this machine refused to use is a sentence about somewhere else. **All of them or none, never a special case for FLUX** — the rows are uneven, and on this tab that unevenness MEANS something ("this engine has a caveat and that one does not") where on Discover it meant only that somebody had written more prose about one runner. **Nothing is deleted**: `Runner.note` and the `runnerNote` field on `catalog.describe()` both stand, because "what is the runner serving this capability like" is a fact about the catalog and not about where a page chose to print it — `tests/test_ai_runtime.py` still pins it there. **Rejected:** keeping it on Discover for the capabilities that have one (the blotchiness is real and the FLUX caution still lands nowhere useful); deleting the notes outright, which was the literal request and would take the memory ceiling — a documented, accepted risk — off the screen entirely; and folding the note back into the `<option>` labels, which `choiceReason` already removed once as a paragraph inside a dropdown. SPEC §40's mflux clause updated. |
| D316 | **PARTIALLY SUPERSEDES D313's drop rule: gated repos come back into the Hub search results, carrying their gate (`gated`: "auto" \| "manual" \| null); private repos stay out. The rule is "an engine here can run it", not "nothing further is asked of the user"** | D313 dropped `gated` and `private` together, on one sentence — "every card here works" — and that sentence conflated two unlike things. A PRIVATE repo is unreachable: there is no step an ordinary account can take, no licence to accept, no queue to join, and it is visible at all only because this machine happens to hold a token that can see it. A GATED repo is a repo you get by **signing in and clicking accept**, which is a step the reader can take today; several of the most-searched-for models on the Hub sit behind exactly that, so the drop made a search that claims to cover the Hub quietly answer "no such model" for Llama. The owner's ruling is the line: gated is fine as long as the only thing between the user and the download is logging in. **The gate travels rather than the row being filtered on it**, which is what makes this different from restoring the pill D313 deleted. That pill named the problem and left a Download button beside it that would 403 — honest and useless. Here the gate decides the ACTION too: with a token on this machine the card offers an ordinary Download (the token is what turns "you cannot have this" into "you may already have accepted this", and the Hub is the one that gets to refuse); without one, the button is REPLACED by a link to the repo's own Hub page, worded for the gate. **`manual` is its own value, not a flattening**, because it is the one case that needs more than logging in — the owner grants access by hand — and somebody told to "accept the terms" on one of those goes hunting for a button that is not there. An unrecognised truthy gate reads as `manual`: guessing "just sign in" about a gate nobody here understands is the guess that wastes an afternoon. The decision lives in `discoverView.gateChrome(gated, authenticated)`, a pure function with the copy in it, for the reason `discoverChrome` is there. **No new credentials plumbing, deliberately**: `_token()` already reads where `huggingface_hub` reads (`HF_TOKEN`, `HUGGING_FACE_HUB_TOKEN`, `$HF_HOME/token`) and the search reply already carried the boolean `authenticated`; the app has no sign-in UI for the Hub and this does not invent one, so the hover names `huggingface-cli login` instead of offering a box to paste a secret into. **Rejected:** keeping gated rows out and telling the user to search on the website (the same "we have a search box that hides things" complaint D312/D313 went round once already); showing a gated card with a live Download button and letting the 401 land mid-pull (the failure arrives after the user has committed to a multi-GB fetch, in a job row, in the runner's words rather than the Hub's); and building a token field in Preferences, which is a credentials surface nobody asked for and a secret this app would then own. SPEC §39 HS-0 narrowed and HS-0d added; ARCHITECTURE's `_model_row` paragraph rewritten; `tests/test_hub_models.py`'s drop-the-gated assertion replaced. |
| D317 | **Leaving the Discover results is an explicit control, in two places, and it resets the query AND the task filter in one act — never just the text** | D313 made a query replace the curated sections and said the way back was clearing the box. That is only obvious for a query somebody typed, and it is not the state people got stuck in. **Picking a task from the select is a search too** (`discoverChrome` has always treated it as one, correctly — a task filter is a question about the Hub exactly as much as typing is), so choosing "Text generation" swaps the shortlist out with an EMPTY search box: nothing visible to clear, no suggestions, and the only route back is realising the menu has an "Any task" option and that it restores them. Nobody discovers that. **The reset is one act over both inputs, and that is the requirement rather than a nicety**: a control that emptied the text and left the task set produces the worst outcome available — the reader does the obvious thing, the box goes empty, and the suggestions still are not there, which reads as the page being broken. It is also why the platform's own `type="search"` ✕ is **hidden in CSS** rather than used: it clears the text only, and two ✕s in one field doing different things is worse than one. **Two affordances, deliberately.** The ✕ inside the field is the one you find when you go looking; "← Back to suggested models" in the results heading row is the one you cannot miss, in the row somebody staring at unwanted results is already reading, and it names its DESTINATION rather than its mechanism ("clear" describes the erasing; the reader's question is how to get the suggestions back). Escape in the box does the same, and does not `stopPropagation` — nothing else on this page listens for Escape while a text field has focus, and swallowing it would break the next overlay that does. **The visibility decision is `showsReset` on `DiscoverChrome`, derived from the same `asked` as the view** rather than asked as a second question, for the reason the heading is one string: two conditions that are opposites today are two chances for a results page to have no way off it. The ✕ alone reads the LIVE controls instead of the settled query — it belongs to the box, and a clear button that arrives 350ms after the first letter or lingers 350ms after emptying is the control contradicting the field it sits in. The **sort** is untouched by the reset: it does not push the tab out of the curated state, so resetting it would be the button doing something nobody asked for. **Rejected:** an ✕ on the search input alone (absent in exactly the state that prompted this); a "Clear filters" control that only appears once a task is set, which is the same discoverability problem one level down; and making the task select not count as a search, which would be undoing a correct rule to work around a missing control. SPEC §39 HS-0b extended. |
| D318 | **`speakers` becomes OPTIONAL: given, it fixes the cluster count exactly as before; omitted, the count is ESTIMATED by clustering the voices at a fixed cosine threshold, and the transcript then carries `estimatedSpeakers` saying what it decided. A count that is present and unusable is still a 400** | D309 shipped diarization with the count REQUIRED, and wrote the reason into `diarize.py`: sherpa's clustering takes either a number of clusters or a cosine threshold, the threshold is "a number nobody outside a lab can set meaningfully", and a wrong guess relabels the whole transcript with total confidence — so refusing was said to be the honest answer. **The premise was right and the conclusion did not follow.** The threshold is indeed not a number a PAGE AUTHOR can hold an opinion about; it is a number the APP can set once, against the embedding model it ships, which is exactly what every serious transcription product does — a count is estimated by default and supplying one is an optional hint. Ours was the only one that refused, and the refusal landed on the commonest case there is: somebody with a recording who does not know how many people are on it. `{diarize: true}` alone, the request that reads as obviously correct, was the one request that could not be made. **The two paths are one argument, not two code paths**: `speakers_or_raise` answers `None` for an absent count and `diarizer` reads `None` as `FastClusteringConfig(num_clusters=-1, threshold=CLUSTER_THRESHOLD)`; the fixed branch is left spelled character for character as it was, with no `threshold` passed, because a caller who gives the count must get yesterday's transcript byte for byte and the surest way to promise that is not to touch its call. `CLUSTER_THRESHOLD = 0.5` is **sherpa-onnx's own default for these WeSpeaker embeddings**, restated as a constant for the reason `MIN_DURATION_ON` is — the number the labels come out of must not be whatever the installed wheel defaults to this month, and the two engines must not pick up different ones. There is no measurement of our own behind it, which is stated in the docstring and in the skill: an estimate can merge two similar voices or split one person across a change of microphone distance, so a caller who KNOWS the count should still pass it. **A bad explicit count stays a 400 in all four places** (bridge, endpoint, both workers): `0`, `-1`, `true`, `"2"`, `2.5` and 101 are typos — arithmetic gone wrong, a copy-paste of the `diarize` flag, an `<input>` read without a `parseInt` — and reading any of them as "work it out yourself" would hide a caller's mistake inside a quietly different transcript. The three spellings of "I did not say" (`undefined`, `null`, `""`) are the same three in JavaScript and in Python, because a form that submits an empty field must not be refused by the bridge and accepted by the endpoint. **`estimatedSpeakers` is additive and means "worked out", not "resolved"**: a run that supplied the count already knows the number and its JSON keeps exactly the keys it had, which is the same additive rule `speakers` itself follows. It counts the SEGMENTER's voices and can therefore exceed the legend — someone who spoke where Whisper transcribed no words is in the first and not the second — and both engines write it or neither does (AI-10c). **Label ordering is untouched**, deliberately: labels stay `Speaker <sherpa index + 1>` on both paths, because a first-appearance relabelling for the estimated path alone would be a second labelling rule in the module that exists to have one, and applying it to both could change a fixed-count transcript that this record promises is unchanged. **Rejected:** exposing the threshold as a caller option (D309's argument survives intact — it is the number a page author cannot reason about, and one app-wide value is what makes the two engines agree); estimating a count and then re-clustering to it (two passes for the same answer the first pass already gave); and writing `estimatedSpeakers` on every diarized run for consistency, which would change the bytes of a transcript whose behaviour this record says is identical. `runtime.js` contract doc + validation, `ai_runtime.py` (the key is sent only when it carries a number), both workers, `skills/fused-render-ai/SKILL.md`, and the four affected test files updated. |
| D319 | **A third speech-to-text runner, `parakeet_mlx` (NVIDIA Parakeet-TDT through `parakeet-mlx`, Apple Silicon only), registered BELOW `mlx-whisper` so the default does not move. Its audio comes in by SWAPPING the library's `load_audio` binding rather than by reimplementing its chunked decode, and the three options it cannot honour are REFUSED by name. `vad.py` moves to the runners root as its second caller arrives** | Parakeet-TDT-0.6b beats Whisper large-v3 on English word error rate, runs several times quicker again on the same Metal, is CC-BY-4.0, and does not hallucinate sentences over silence the way Whisper can. That is a better engine for most recordings — and **it is deliberately not the default**, which is the whole of the ordering decision. v3 covers 25 European languages against Whisper's ~99, so promoting it would silently regress every page that relies on `language` being detected, on recordings this model has never heard a word of; the failure would be a confident transcript in the wrong language rather than an error. So it is registered directly under `mlx-whisper` and a user opts in per capability on the Engines tab — **D302's machinery, used for exactly the case it was built for, with no new preference plumbing in this change**. The registry `note` is the honest one-liner that picker needs: quicker and more accurate in English, 25 European languages only. **The audio is the interesting engineering.** `mlx_whisper.transcribe` accepts an ndarray; `parakeet_mlx.transcribe` takes a path and calls `load_audio`, which SPAWNS ffmpeg — the binary this app refuses to depend on (`faster_whisper/pyproject.toml` states the rule). Two ways out, and the second is much better: reimplement `transcribe` around the public `get_logmel` + `generate`, which costs the library's 60s chunking AND its overlap token merge (`merge_longest_contiguous`, falling back to a longest-common-subsequence alignment) — most of what makes a long recording come out right at a seam; or swap the module's `load_audio` binding for one call and hand over the waveform `av` already decoded, keeping the chunking, the merge and a real `chunk_callback` for progress. It is the same reach into another package's globals that `mlx_whisper/worker.py`'s `_watch_progress` makes for tqdm, with the same guard — a MISSING binding raises a sentence rather than falling through to ffmpeg, because "FFmpeg is not installed" from inside a library is not a failure the user can act on. The callback fires BEFORE each chunk, so what the row reports is that chunk's START: behind the truth, never ahead of it, which is what `runtime.js` promises. **Options it cannot honour are refused, not ignored**: `task: "translate"` (transcription-only), `language` (no such argument — v3 detects and cannot be pinned) and `initialPrompt` (a transducer has no text to condition on). The alternative — accept and quietly do something else — is the failure this app rates worst: a page asks for English and gets French with nothing saying which engine decided. The messages name the ENGINE and the way out, because the page is usually correct and simply resolved to a runner it was not written for. This is a real, deliberate crack in AI-10c's "a page cannot tell which engine ran", and it is the honest place to put one: a loud refusal a page can handle beats a silent difference it cannot see. **`vad.py` moved from `mlx_whisper/` to `runners/`** — the agreed trigger was a second caller, and this is it. A copy under `parakeet_mlx/` would be two implementations of what `vad: true` means, free to drift on the threshold, the minimum silence and the padding, with neither copy failing a test because each would pass its own; a test now refuses a `vad.py` inside any runner folder, and the Silero card on the AI Models page stops naming one of the engines that use it. On this engine the VAD is a wall-clock saving rather than a correctness fix (Parakeet does not need protecting from silence), and it is wired in anyway because `vad: true` is a promise about the ARGUMENT, not about why an engine wants it. **Format detection is on the CONFIG, not the filename** (`formats.is_parakeet_checkpoint`): a Parakeet snapshot carries `model.safetensors`, which is also every transformers repo's, so what settles it is `config.json`'s `target` naming a `nemo.collections.asr.models.…` class — narrowed to the ASR collection, since NeMo also ships TTS and LLM models this runner cannot load. A match returns that runner ALONE, because the text branch would otherwise claim the same directory of safetensors and the page would offer to load a speech model as a chat model. **Rejected:** making Parakeet the Apple Silicon default (the language regression above); reimplementing the chunked decode to avoid touching a private binding (worse transcripts at every seam, and a second implementation of the library's own merge to maintain); accepting `language` and ignoring it (the silent-wrong-answer failure); and giving this engine no VAD (it would make `vad: true` mean one thing on two engines and nothing on a third). SPEC AI-10g added; `skills/fused-render-ai/SKILL.md`'s runner table, format list and mistakes list rewritten for three ASR engines; catalog list added (smallest-first: `parakeet-tdt_ctc-110m`, then the two 0.6B models). **Untested on real hardware in this change**: no Parakeet model has transcribed anything here — the fakes are built to the 0.5.2 API read out of the wheel, and a live run on an Apple Silicon machine is an outstanding manual check, exactly as AI-10b/AI-10d record for the other two engines. **(Withdrawn by D406 — engine removed, maintenance cost not justified by use. The registry row, catalog list and `engine_options.UNSUPPORTED` entries described here are gone; `formats.is_parakeet_checkpoint` and the early-return branch survive, now claiming no runner instead of `parakeet-mlx`, and `vad.py` stays at the runners root.)** |
| D320 | **`vad: true` runs a real Silero filter on the MLX whisper runner too — ONNX Runtime, region-at-a-time transcription, and progress still denominated in seconds of the ORIGINAL recording** | Closes the last behavioural gap D302 left between the two speech-to-text engines. `fused.ai.transcribe` takes a `vad` flag; faster-whisper ran a Silero VAD filter and dropped the silence, while the MLX runner could only map the flag onto mlx-whisper's per-window `no_speech_threshold` — one API, one argument, two behaviours, which is precisely what AI-10c says a second runner for a capability may not be. **The dependency was the whole decision.** Silero's exact model is already present twice in this app's dependency graph and neither copy is reachable: importing it out of `faster_whisper` would pull `ctranslate2` into a Metal-only venv, which is the thing the runner-folder split exists to prevent, and the PyTorch export would cost gigabytes for a 2MB detector in the venv whose selling point is that it is small. So the ONNX export on `onnxruntime` (2.2MB, `onnx-community/silero-vad`, ungated — the rule `catalog.py` states about gated repos applies to everything this app downloads, not only to models a user picks). **Regions are transcribed SEPARATELY rather than concatenated**, and that is the ordering-sensitive half: splicing non-adjacent speech into one waveform would put unrelated sentences inside a single 30-second window and let `condition_on_previous_text` condition each one on text from a different part of the recording. Cutting instead means every boundary falls in at least `MIN_SILENCE_S` of quiet, where a sentence has already ended — which is also why this is not the fixed-offset chunking AI-10c refuses for progress; that cut goes through a sentence, this one goes through a pause. Conditioning across the gap is still lost, and carrying the previous region's tail in as `initial_prompt` was considered and REJECTED: it invites the model to continue a sentence that finished before the silence, the known route into a repetition loop, and it would make each region's output depend on the previous region's mistakes. **Progress had to be remapped or the public contract would have silently changed units.** The borrowed tqdm counter (AI-10c) denominates the waveform it was handed, so with silence removed it counts seconds of SPEECH — a 90-minute recording with 30 minutes of silence would have climbed to 60 against a total of 90 and stopped, a bar that can never finish, in a unit `runtime.js` promises is seconds of audio. `done` is now `region_start + counter`, clamped to the region end (Whisper times against a padded 30s window, so the last segment of a short clip can end past it), through the same region table the segment timestamps are remapped by. **Degradation is deliberate and loud**: a detector that cannot be fetched or loaded falls back to transcribing everything, reporting the reason on the job row and in the worker log, because the VAD is an optimisation over a run that works without it and an offline machine should get a slower transcript rather than no feature — and it is pre-fetched during Download for exactly that machine. **Two bugs were found by writing the tests, both invisible in practice and neither left in.** The region end was one 32ms window early (an inclusive index used as an exclusive bound), which the 200ms padding hid completely until somebody turned the padding down; and the overlap-merge branch is unreachable with today's constants, since a split needs 0.5s of silence and padding closes only 0.4s — it is kept as defence, with the invariant `MIN_SILENCE_S > 2 * PAD_S` now asserted so that changing either number is a decision rather than an accident. **Verified by hand on this machine** (no CI can run onnxruntime + a model download): `say`-generated speech, 4s of digital silence, the same speech again — 19.51s in, `[(0.0, 8.01), (11.61, 19.51)]` out against a truth of 0-7.76 and 11.76-19.51, the gap dropped, edges widened by the padding, 18% of the file skipped. What remains unverified is everything about REAL recordings: music, overlapping speakers, a noisy room, and whether the accuracy cost of cutting at region boundaries is measurable on a long file. **Renumbered from D306** on the merge with main, which had landed a different decision on that number while this branch was open; the branch's own commits, and any review comment written against them, say D306 for this record. |
| D321 | **A load with no `capability` infers one from what the repo IS — the cached snapshot first, then the catalog — and refuses with a sentence when a cached repo is one nothing here reads, instead of defaulting to text generation** | `_capability_of(body, default=TEXT_GENERATION)` made an omitted argument mean "text generation", so `fused.ai.models.load("mlx-community/FLUX.2-Klein-4B-4bit")` resolved to the mlx-text runner and died in `mlx_lm.load` on a missing `config.json` — a file that repo has never had — while `fused.ai.image()` rendered from the same snapshot, because `/api/ai/image` is capability-bound by construction. **The report a user gets from a silent default is the worst available one**: not "you omitted an argument" but a library's complaint about a corrupt model, pointing at the repo rather than at the dispatch. It had already fired once — the whisper demo's Preload sent no capability, a whisper repo went to mlx-lm, and it surfaced as missing safetensors — which is what made this a design fix rather than a second one-off. **Inference reads `ai_models.cached_capability`, the AI Models page's own join**, rather than a second reading of the same filenames: the page already decides which engine would load a repo from `runners/formats.py`, and a card that offers Load beside a load that then refuses is the exact disagreement `formats.py` exists to prevent. One rule is added on top of the page's: where the format is not DECISIVE but every runner that reads it shares a capability, that capability answers — a bare directory of safetensors is read only by the two text runners, and this is what keeps every existing `load(id)` on an unlabelled chat repo working (pinned by test, not by assertion). **The ordering problem is that `formats.loaders` needs bytes on disk and a cold load has none.** Three options were weighed. Inferring only after the fetch was rejected: the fetch is the expensive half and the whole point is not to pull multi-GB into the wrong runner. A Hub metadata call was rejected for the default path — it puts a blocking network round trip on a route that has none, and fails exactly where a user is most likely to be loading from cache (offline). What was taken instead is `catalog.capability_of`: every id this app RECOMMENDS is already keyed by runner, so the whisper-Preload case is answered by a dict lookup before a byte moves. **An uncached, uncurated id still defaults to text generation, deliberately.** Nothing local can classify it, and refusing would break every page that preloads a chat model by id; the cost of a wrong guess there is now bounded, because each runner checks its format BY NAME before importing anything and says which format it got. The one case that refuses is a repo that IS cached and that no engine reads: `cannot tell what <repo> is for, so 'capability' cannot be left out: it is in this machine's model cache and it looks like <what>. Pass one of text-generation, text-to-image, automatic-speech-recognition — for example fused.ai.models.load('<repo>', {capability: 'text-generation'}).` **An explicitly passed capability is unchanged**, right or wrong, which is what makes this additive; `/download` takes the same helper because it had the same bug through the same default. Also documented where it went missing: the SKILL's `fused.ai.models` table said `load(id, opts?)` and never mentioned that a capability existed. **Renumbered from D307** on the merge with main, for the reason the record above gives. |
| D343 | **The `claude` template's annotation layer gets a second way to make a note: `#annrec`, beside `#annbtn`, records audio while the reader clicks — each click is stamped and stays wordless until the whole recording is transcribed and split back across the clicks by time.** | Typing a note per element is fine for one or two remarks and a chore for a real walkthrough, where the reader wants to click through a page narrating as they go. `#annrec` is that: press it, talk, click the things you are talking about — no composer opens, so nothing takes focus away from the page — press it again, and every clicked element gets the words that were said about it. **The click handler is the SAME one `#annbtn` already installs** (`annWireTarget`'s capture-phase listener): recording only adds one branch at the anchor-building tail — `annRecOn` skips `annOpenComposer` and calls `annRecMark` instead, which pushes the anchor straight into `annotations` with `content: ""` and a `t` (seconds since the mic started). Everything downstream — the pin, the chip, editing, deleting, sending — is the SAME pending annotation a typed note is; a spoken one is just born with its `content` filled in later rather than never. **The words are assigned by the click's OWN timing, not the mic's — each transcript segment to its NEAREST click by start time, not to a fixed lead window.** OpenPointer's rule (`fused-render-sandbox/Sina/OpenPointer`, the whisper-annotation tool this was modeled on) gives each click a window from LEAD (2s) seconds before its `t` to the next click's `t`, and that was tried FIRST here, verbatim — then broken by hand: two clicks 1.5s apart, "red" right after the first and "blue" right after the second, came back as note one = "red", note two = "red. blue". The window design double-counts BY CONSTRUCTION whenever two clicks are closer together than LEAD: click i's window already reaches forward to click i+1's own timestamp, and click i+1's window reaches the same LEAD seconds backward from there, so anything said in that shared stretch — which is most of a quick walkthrough, where clicks land well under 2s apart — lands in both notes, not one. Nearest-by-start-time has no shared stretch to double-count: a segment's distance to each click is a single number, so exactly one click is closest, and the note it fills is the ONLY note it fills. It also does not need a tuned constant — a remark said right after a click and one said right before the next both resolve correctly because "closest in time" means the same thing on either side of a click, at whatever pace the reader actually talks. **No bundled transcription engine.** OpenPointer ships its own `faster-whisper` subprocess (`transcribe_worker.py`) because it has no host to ask; a template does — `fused.ai.transcribe({path})` (SPEC AI-10) already runs faster-whisper locally on every platform this app supports, so `#annrec` uploads the recording and calls that instead of vendoring a second copy of an engine the app already resident-loads for every other page. A template may not add a dependency (PY-15) and this adds none. **The audio rides the SAME scratch channel the pane crops do**: `shotDirPath`/`shotJoin`/`shotStamp` name it a file in `agent.py`'s `SHOTS` temp dir, `fused.uploadFile` writes the bytes, and `_prune_shots`' age/count cap cleans it up — one temp directory and one pruner for every throwaway file this template produces, audio included. It differs from a crop in the one way that matters: the recording is never handed to Claude (only the transcribed WORDS end up in a note's `content`), so it needs no `Read(//SHOTS/**)` rule — `fused.ai.transcribe` opens it locally and the file's job ends there. **Hidden and removed in lockstep with `#annbtn`**, not on a rule of its own: `annCapable()` gates it (`annPollTarget`'s hosted poll, same as the switch) and `enterNoPane`'s removal list carries `"annrec"` beside `"annbtn"` — a folder with nothing to annotate has nothing to record either, and a second gate would be a second place for the two switches to disagree about whether there is a target. **The mic is released on every path that takes the target away**, not only the "press Stop" path a happy recording ends on: `annSetMode`'s two disarms (`!annCapable()` — the target vanished; the explicit `!annOn` — Escape or the switch itself) both call `annRecEnd()` if a recording is running, and the hosted `pagehide` teardown stops the `MediaRecorder` directly rather than routing through the full transcribe pipeline, since the document showing the result is what is being torn down. A recording that ends with nothing said or nothing clicked uploads nothing — `annRecEnd` returns before touching the network if the blob is empty or no click was stamped. **Rejected:** a bundled whisper worker mirroring OpenPointer's (rejected above — a template dependency and a second implementation of a capability the host already provides); opening the composer per click during a recording (defeats the point — the composer is exactly the focus-grab hands-free recording exists to avoid); and auto-sending the notes the moment transcription lands (a spoken note is a raw transcript same as any dictation and deserves the same review a typed one gets before it rides a message — the reader still presses Send). |
| D344 | **A spoken-walkthrough click that hits no element (background, whitespace, a gap) becomes a `kind: "point"` note instead of being dropped — page x/y plus a screenshot with a crosshair burned in AT CLICK TIME, both kinds riding the same recording with no mode switch between them.** | D343's click handler only ever knew one outcome for "nothing under the cursor": `return`, silently, because D235's tools are element-anchored and there was nothing else for a click to become. That is fine for the manual composer, where a background click has no text box to open either, but it throws away exactly the remarks a walkthrough most needs — "there's a big gap here", "nothing tells you this saved" — commentary about the PAGE rather than any one control on it. OpenPointer has this as a second capture mode the reader switches into by hand (`Capture: div` / `Capture: point`); the owner asked for it WITHOUT a mode switch — a recording should just know which kind of note a click makes from where the click landed. So the branch already in `annWireTarget`'s click handler (`if (!el ...) return` / `if (!anchor...) return`) grew one more line each: while `annRecOn`, call `annRecMarkPoint` instead of returning. Element clicks and point clicks freely interleave in one recording; nothing chooses between them but the click itself. **The screenshot is taken at the CLICK, not at send.** Every existing crop (`annCaptureShots`) resolves an element and crops AROUND it, lazily, at send time — cheap to defer, because the element is still there to re-measure. A point has no element to defer to: by send time the reader may have scrolled, clicked five more things, or the app may have re-rendered, and "a screenshot of nothing in particular" loses the one thing that makes it findable if it is not the thing the reader was actually looking at. So `annRecPointShot` rasterises the pane immediately (`shotPane`, the same rasteriser the send-time crops use) and crops a fixed 480×360 window centred on the click — a picture the size of the thing it is about, same reasoning `shotCapturePane`'s crops already follow, just centred on a point instead of an element's box. `annCaptureShots`/`annApplyShots` both grow a one-line `if (c.kind === "point") continue`, so the send-time batch neither tries to re-resolve a point (there is no element to find) nor clobbers the shot the click already took — `annApplyShots`'s `delete c.shot` runs for every OTHER pending note precisely so a stale path never rides a resend, and a point's shot is the one case that is never stale, because it is immutable from the moment it is taken. **The crosshair is drawn on the CROP, not kept as a separate overlay.** An overlay is a fact about a live page; the picture is a fact frozen at one instant, and a marker that only exists in the DOM would be invisible in the file Claude actually reads. `shotEncodeMarked` forks `shotEncode`'s exact budget ladder (WebP quality steps, then a PNG fallback, then resolution halved and retried) and adds one call — `annPointCrosshair`, a red-on-white cross scaled to match whatever size the ladder settles on — between the crop being drawn into the output canvas and it being encoded, so the mark survives every size the budget might land on. Forked rather than parameterised: `shotEncode` has one caller today and no notion of "mark this pixel"; a `mark` argument every other crop would pass `null` for is a worse shape than a second, nearly-identical function. **Coordinates are PAGE, not viewport — `annPointXY` converts back through whatever the CURRENT scroll is**, the same discipline an element note's own `getBoundingClientRect()` already follows by being live: a pin drawn later, or a chip's edit-click, has to land where the spot is NOW, not where the viewport happened to be at the moment of the click. Pin rendering and the chip's edit-placement (`renderAnn`, its `txt.onclick`) both branch on `c.kind === "point"` to call `annPointXY` instead of `annResolve` — the ONE new fact a point brings to code that otherwise treats every pending note identically (same chip, same delete, same send). **`shot: null` from creation, never an absent key** — `annApplyShots` already writes real-or-null for every element note's `shot`, and a point that left the field unset until the capture finished would make "no picture yet" and "no picture, ever" indistinguishable to a reader of the wire JSON; both read the same to Claude (there is nothing to open) and this keeps them reading the same in the object shape too. The live send receipt (`.annsum`) picks up a point's thumbnail from `fused.rawUrl(c.shot)` rather than a blob URL — a point has no blob left lying around the way an element crop's `shots.thumbs[c.id]` does (`annRecPointShot` uploads and discards it), and the server can already serve the exact path it just wrote. **`formatAnnotations`'s preamble had to change words, not its OPENING ones** — `stripAnnBlock` collapses this whole block to a `📌` marker in the transcript the user reads by matching the literal prefix `"The user annotated "`, so the noun after it moved from the now-inaccurate "element" to "thing" and the sentence describing the wire shape grew a paragraph for `kind: "point"`, but the four words `stripAnnBlock` looks for did not move (`tests/test_claude_kind.py`/`test_claude_shots.py` pin exactly this, and caught the first draft breaking it). **No new permission rule**: a point's screenshot lands in the SAME `SHOTS` directory an element crop does (`shotDirPath` — see D343), which already carries an unconditional `Read(//SHOTS/**)` grant, so Claude can open it with no change to `agent.py` at all. **Rejected:** a separate capture-mode toggle (OpenPointer's own design, and the one thing the owner explicitly asked NOT to build — the click already says which kind of note it wants); deferring the point's screenshot to send time like an element's (nothing to re-measure by then, and the exact moment being described is often long gone); and an in-DOM highlight ring instead of a burned-in crosshair (invisible to the one reader — Claude, off the file — that the mark exists for). |
| D345 | **The manual comment composer learns the point note: a click on empty background opens it pinned to the exact spot, Alt-click forces the spot even over an element (crosshair cursor while Alt is held says so before the click commits), and the composer grows an anchor chip — "`<button> Save`" / "⌖ exact spot" — that names the target before the words are saved and, when the click had both readings, flips between them** | D344 gave point notes to the RECORDER only; the manual composer still silently swallowed a background click, and had no way at all to say "this spot ON the button, not the button" — the owner's two target modes with no affordance between them. The design rule is D344's, extended: **the GESTURE picks the target, never a mode toggle.** An element under the cursor means the element (the hover ring already says so); empty background means the spot; Alt is the one modifier, meaning "the spot, wherever I am" — mirrored into the recorder's click handler too, so the two flows cannot disagree about what Alt means. Every click now builds BOTH readings (`pointAnchor` beside the element anchor) because every click names both, and the unchosen one rides along as `annDraftAlt` — which is what makes the chip a FLIP rather than a label: same click, other reading, no second aim. The chip is disabled when there is only one reading (background click; editing a saved note, whose anchor is settled) and its label is the same digest the wire carries, so what it says is what Claude is told. **The point crop moved from click time (D344's rule for recordings) to SAVE time, awaited in `annCommit`** — different from the recording for a reason and not a contradiction of D344: a recording's click is fire-and-forget with the words arriving minutes later, so "now" is the only honest moment; a composer holds the pane still while the user types, a cancelled composer should cost no capture, and — decisive — auto-send composes the wire the moment the save returns, so a fire-and-forget capture would race it and ship `shot: null` for a picture one beat from existing. Page coordinates convert back through the CURRENT scroll (`annPointXY`), so a pane scrolled mid-thought still crops the spot the note is about. The chip/crosshair/ring triad is the whole mode representation: ring = element, crosshair = spot, chip = the commitment, named before Enter. **Rejected:** a capture-mode toggle (D344 already rejected it once; the manual flow does not get one either); point-only-on-background without Alt (leaves "this spot on the element" — the owner's actual case — unreachable); a chip that is a label but not a flip (the wrong guess would cost a re-aim for a fact the click already carried). |
| D346 | **Voice becomes a property of the COMPOSER, not a mode: a 🎙 in the note popover records one note, stop transcribes and commits through the same `annCommit` the Enter key uses — auto-send included — and the batch walkthrough (annrec) now auto-sends too, once its transcript actually lands words** | The owner's three modes (written, voice-on-element, voice-on-point) threatened three toggles; the shape that keeps them one feature is orthogonality — D345's gesture picks the TARGET, the composer picks the INPUT, and the walkthrough stays the batch flavour for "many comments in one breath". So the mic lives beside the textarea and inherits the target for free: element, point, and flipped-mid-composition all Just Work because the mic never knew which it was. **One commit path is the point**: typed and spoken words reach `annCommit` identically, so they cannot save, crop, or auto-send differently — the mic is sugar over the textarea, not a second pipeline. The rules around it are all about which words the user means: typing while the mic runs CANCELS it (they changed their mouth mid-thought; typed wins); closing the composer cancels it (the note the words were for is gone — discard, never transcribe into limbo); transcribed text APPENDS to a half-typed draft rather than clobbering it (one thought, two mouths); and the words are identity-checked on return — the composer may have closed or moved to another note during the multi-second transcription, and the one legal move in the meantime is the anchor chip's flip (same click, other reading), so `annDraft === draft || annDraft === alt` is the gate, not object identity alone. Silence (`!blob.size`) transcribes nothing. The audio takes the walkthrough's exact path — same temp dir, same pruning, same "the file never reaches Claude, only the words do". **The walkthrough's missing auto-send** was simply an oversight once typed notes gained it: "record, talk, stop" ended at a pile of pending chips and a fourth step nobody was told about. It fires only when `annRecAssign` actually landed words on an unsent note — a failed or empty transcription leaves the clicks pending and editable (annRecEnd's existing posture) rather than sending wordless notes — and never over a live run or a typed draft (`annPrefillComposer`'s own refusals, shared with every other auto-send). The mic's styles exist TWICE, like every composer rule, because the popover is portaled behind a shadow root (`ANN_LAYER_CSS`) where the page's stylesheet cannot follow — with its own `@keyframes` copy, since an animation name does not cross the boundary either (`tests/test_claude_annotation_modes.py` pins the pairing). **Rejected:** a global per-note-voice mode beside Comment and Record (a third toggle for what the composer already scopes); transcription filling the textarea for the user to press Enter on (the owner asked for auto-send; a review step is the composer staying open, which cancelling the commit still allows); voice-activity auto-stop (a tuned constant where a second tap is unambiguous). |
| D347 | **The Element/Point choice becomes a VISIBLE TOOL: a two-seat segmented picker (`#anntool`) appears in the control strip while comment mode (or a recording, which arms it) is on, the selected tool decides what every click pins, and Alt demotes from "force the spot" to the momentary override of EITHER tool — `(annTool === "point") !== e.altKey`, one XOR shared by the click handler and the crosshair cursor. The transcriber is also warmed the moment any recording starts** | Overturns half of D345 at the owner's explicit request, a day later: "the UX for changing the pointer to element selector or pin point is not there… a toolbar appears… which has both options as tools user can select". D345's gesture-only design made the spot reachable (Alt, background) but never VISIBLE — a modifier key is muscle memory for whoever designed it and undiscoverable for everyone else, and during a walkthrough recording, where the composer's anchor chip never appears, there was NO discoverable route to a deliberate point note at all. The picker fixes precisely that: it exists on screen only while a click is armed (`annSetMode` shows/hides it — a picker for a click that cannot happen is noise), it reads as one family with the controls beside it (#annrec's quiet bordered idiom, the selected seat wearing #annbtn's armed accent), and it is a `radiogroup` because exactly one answer is ever in hand. The tool is session-local and sticky across arm/disarm — deliberately NOT a param: which tool is in hand is a fact about the hand, not about the page, and a bookmark that re-arms "Point" weeks later would be D46's stale-param bug wearing a new key. **Alt survives, demoted to symmetric override**: Element tool + Alt pins the spot (D345's exact behaviour, so nothing learned is unlearned), and Point tool + Alt pins the element — the XOR is what keeps "the exception works in both directions" one line rather than four branches, and the aim cursor derives from the same expression so the pointer can never promise one thing while the click does the other. Empty background stays a point regardless of tool — there is no element reading to prefer. The picker joins `hostedControls` and `enterNoPane`'s removal list exactly as #annbtn/#annrec do: same target, same lifecycle, same reason. **The warm-up** (`annWarmTranscriber`, called by both `annRecBegin` and `annMicBegin`): the multi-second-to-multi-GB cost of a cold speech model used to land at STOP, exactly when the reader is waiting for their words — now `fused.ai.models.load(default, {capability: "automatic-speech-recognition"})` fires at start, inside the dead time the reader spends talking. Opportunistic by construction: failures swallowed (transcribe loads a cold model inside its own job anyway, so the warm-up can only ever save latency, never gate the feature), skipped when a model is already `loaded`, skipped when the engine names no `default` (the transcribe call's own default — the same first-curated entry — resolves it then), one in flight at a time. The capability is named explicitly per the D321 trap: an undownloaded whisper repo with no cached files to sniff would otherwise fall back to the text runner. **Rejected:** a floating toolbar over the pane (it would need the same dual page/shadow rendering the pins carry — real cost for the hosted layout — where the strip already exists in OUR document in every layout, beside the controls that arm it); dropping Alt now that the picker exists (the picker is the discoverable path, Alt is the fast one, and they compose rather than compete); persisting the tool in a param (see above); awaiting the warm-up anywhere (a recording must start the instant the mic does — the model's arrival is the transcription's problem, which already handles it). |
| D326 | **A file opens with its companion sidebar OPEN, at the standard companion share, EVERY time — an absent `_side` means open (as it already did on a folder), shut is the word `off`, and NOTHING about the sidebar is persisted anywhere: the width leaves `useState` for a module store, and `_side` is stripped out of the per-file session sidecar at both ends** | The owner's report, from a file that kept coming back with a sidebar its neighbours never had: *"we don't want any persisted preference. similar to folder sidebars, they should always open at a fixed 30% size on page refresh. any other changes being made (open/width) must be persisted only for the session."* **This overturns two documented decisions.** The first is the **file-vs-folder `_side` asymmetry** that `listing/pane-side.ts` and `lib/preview-side.ts` both stated at length — *file: absent = CLOSED, because the sidebar is an extra beside a content view that is already complete on its own, so nothing is the same as nothing; folder: absent = OPEN.* That reading was defensible on its own terms and still wrong in one consequence: it made the sidebar's PRESENCE a preference, and a preference has to live somewhere. It lived in the URL — and from the URL in the file's session sidecar, which is the second decision overturned, **LSN-3's rule about which params persist**: `_side` counted as a qualifying non-`_mode` param, so opening the sidebar on one file STARTED that file's session and replayed `_side` on every later bare open. A refresh could not undo it, because a refresh is exactly when the sidecar is replayed. So one file wore a sidebar for months while its neighbour never had one, and the app had no reachable way back to a uniform starting state. **The fix is to stop treating any of it as a preference.** `_side` becomes a request for THIS DOCUMENT: read off the URL at mount (`parseSide`), resolved against what the file offers on every render (`resolveSide` — so a landing gate verdict can never leave the screen disagreeing with the lists), carried across the shell's pushState navigation because the URL carries it, and gone on a refresh because a bare URL opens at the default again. Closing is `_side=off`, **deliberately the folder pane's own spelling** (`PANE_SIDE_OFF`): one param name already spans the two surfaces, so a second word for the same state would have made a param carried between them mean different things depending on which way the user walked. **What an absent param opens is `defaultSide`, and it is THE LEADING COMPANION OR NOTHING — never the best of whatever happens to have settled.** While the leader (`all[0]`, the list being in ranking order) is unresolved the answer is null, however much has settled behind it, and that one sentence closes two opposite bugs — both found in review, the second only after the first was fixed. Two kinds of pending are excluded, for the same reason and on different evidence: the borrowed placeholder whose parent probe is still out (a file whose only candidate is a `git` that may not exist opens NOTHING rather than flashing a column open and taking it away), *and an OWN condition-gated companion whose verdict has not landed* — which `settled` deliberately includes, because for the split's EXISTENCE an own gate resolving is a different question (see the module header's asymmetry note). Missing the second was a real bug in the first cut of this change, caught in review: **`claude` ships a `condition.py`**, so on *every* file it is pending until `/api/fs/conditions` answers, and auto-opening it put an empty ~30% column on screen (its `src` null, so the caller's spinner) which then vanished on a mount-backed file, where that gate says no and `git` is refused too — the content pane's width jumping twice, seconds apart on a cold mount, for a sidebar the file never gets. **"Settled enough to be listed and toggled to" is not "settled enough to open by itself"**, and a gated entry never being a default is the rule the content pane already follows (`defaultMode`, CT-12, and the server's own `_mark_conditions` docstring: *"a conditional entry is never the client's default when an unconditional one exists"*). **The second bug is the mirror image and the worse of the two: a SETTLED FOLLOWER taking the default and then being displaced.** Withholding the default from a pending leader is not enough on its own, because "the best resolved companion" was still an answer — and inside a repository that answer is Git: the borrowed `git` probe is cached per FOLDER, so on every file→file hop it is settled at first paint while *this* file's `claude` gate is freshly in flight. The column opened on Git and swapped to Claude a moment later, changing content under the eyes of whoever was already reading it, on the common path rather than in a corner. **Note where the CT-12 analogy stops**: for the content pane "a gated template is never the default while an unconditional one exists" is a STABLE rule — an allowed conditional does not later become the default, so nothing moves. Here it would, because `claude` leads the ranking; and skipping the gate permanently is not available either, since `claude` is gated on *every* file and such a rule would mean the sidebar never opens by itself anywhere. So the gate is **awaited**, which is the same short wait the first fix already accepted for the majority of files (the ones with no repository `git` at all) — the repository case was simply jumping the gun and then correcting itself. **LIVE RECOMPUTATION IS SAFE once the rule is this, and a latch would have been wrong:** a verdict only ever REMOVES a companion, never adds one, so nothing can appear later and outrank the leader, and the only movement left is the one that must keep working — the column falling away, or shifting to the next companion, because the mode it was on stopped existing. A DEEP LINK to either kind of pending companion is still honoured, and so is a user's later switch — `resolveSide` resolves a NAMED mode against `all`, so it neither waits for the leader nor follows it; only the default is withheld. **THE FOLDER PANE HAD THE IDENTICAL DEFECT and is fixed the same way**, which is the second time this decision has made the two surfaces agree: `activePaneSide` resolves an absent `_side` to `offered[0]` on every render, and the pane's two companion probes are independent `useDirMode` calls with their own caches, so Git answering first offered Git alone, put the pane on it, and jumped to Claude when the verdict landed. `paneSideList` now reports UNDECIDED (its empty list, on which the caller already holds a skeleton) while the LEADER is unanswered, not merely while both are. For the same reason the reconcile leaves `_side` untouched while nothing is settled: writing `off` there would shut the sidebar for good on a file whose companion was about to prove it exists. **An unhonourable request now falls to the default rather than closing** (a gate-denied mode, a `_side=preview` carried in from a listing): a denial says "not this companion", never "shut it". **The default gets the CLEAN URL** (`sideParam`, PT-9's rule and the pane's `selectSide` normalisation), so a plain open grows no param at all and the legacy `?_mode=claude` migration is usually just the deletion of `_mode` — which matters beyond tidiness, since a param is precisely what got recorded before. **Width follows the same policy in `lib/side-store.ts`:** a module variable for the life of the document, no `localStorage` and no `sessionStorage`, only a COMPLETED drag recorded, the resize clamp deliberately not recording anything. It was mount-local `useState`, and since `StatView` is keyed by path the column remounted on every file→file hop and the divider sprang back — the same bug D282 fixed for the listing pane, arrived at from the other side. **Two stores, not one**, because they hold different quantities (a fraction of the container there, pixels here — the sidebar's floors are pixels because what it holds is a chat composer); they share the DEFAULT, `companionFrac`, which is the part that actually has to agree. **And `_side` is stripped from the sidecar on write AND ignored on read** (LSN-12): stripping alone would leave every sidecar already on disk replaying its stale `_side` until that file's next qualifying change, and ignoring alone would keep writing a key we then have to keep ignoring — together an old file is inert on the next read and clean on the next write, so it self-heals with no migration pass. Refusing the write instead was the alternative and is worse on both counts: `_side` arrives alongside perfectly good params, so refusing would discard a real session update to punish one key the caller did not choose to send. A sidecar whose only param WAS `_side` therefore reads as **no session**, not as an empty-string one — an empty session is still a `lastSession` dict, which is what LSN-3 keys "a session already exists" off, and one reader (`_stored_session`) now serves both the GET and that gate so they cannot disagree. **Three more places had to agree with that rule, all found in review, and each one was the preference sneaking back in by another door.** (a) The restore's SKIP GATE still read the raw query, so `?_side=off` — which a close click now *writes*, where it used to delete the param — counted as "opened with params" and switched that file's replay off; it reads the stripped query now, and `restoredSearch` carries the LIVE `_side` through the write so a replay cannot silently reopen a column the user shut. (b) **RECENTS** records `currentUrl()` verbatim on every url change, to DISK, so every close was persisted and every later open from the list came up shut — stripped at `recordRecentOpen`, the single funnel. **Bookmarks deliberately keep theirs**: SB-2 says a bookmark captures the URL verbatim and it is an explicit "save this view" gesture, so the same param gets the opposite answer, because one capture is chosen and the other is a side effect. (c) Both `_side` WRITERS went through `URLSearchParams`, whose round trip re-encodes what it merely passes through (`stretch=2,1471` → `stretch=2%2C1471`) — harmless while the reconcile only fired on a disagreement, but the default-normalisation makes it fire on every ordinary `?_side=claude` link and `setSide` now runs on the first close of every auto-opened sidebar, and the rewritten string is what reaches the sidecar, against LSN-2. One textual writer (`writeQueryParam`) for both, matching the policy the strip itself already follows. **And the width clamp reads the store as well as narrowing toward it**, so a window narrowed and widened again returns to the dragged width instead of disagreeing with the number the next remount would restore. |
| D324 | **The /apps hub mounts a card's live preview iframe only while the card is near the viewport, and unmounts it again once scrolled well past** | Cards without an authored `preview.png` render the app itself in a scaled sandboxed iframe, and the old `loading="lazy"` attribute only defers the FIRST load — the browser never reclaims an iframe once it has loaded, so scrolling through a big workspace accumulated one whole live page + JS runtime per card, permanently. Each thumb now carries an `IntersectionObserver` (`useNearViewport`, AppPreviewCard.tsx) with an 800px vertical `rootMargin`: the iframe mounts shortly before the card scrolls into view (no monogram flash at scroll speed) and unmounts once well past, showing the existing no-entry monogram in between — an offloaded card reads as "no live preview", never as broken. **The observer is rooted at the `.apps-page` scroller, not the default document viewport, and this is load-bearing**: the grid scrolls inside that `overflow-y: auto` div while the document itself never scrolls, and `rootMargin` only expands the ROOT's rect — rooted at the document, the expansion happened on a box that never clips anything while `.apps-page`'s own unexpanded box silently cancelled it back out, so cards only mounted once fully on screen (observed live before the fix). The callback reads the LAST batched record, not the first — multiple intersection flips can coalesce into one callback and only the newest reflects where the card is now. One observer per card, not a shared one: at real workspace card counts (dozens) the observers are noise next to the iframes they guard. `preview.png` thumbs and monograms are untouched — an `<img>` costs nothing to keep mounted. **Rejected:** keeping `loading="lazy"` alone (defers first paint only, reclaims nothing); `content-visibility: auto` (skips rendering but keeps the iframe's document and JS alive, which is the actual cost); virtualization of the grid (heavier machinery for the same effect, and it would also unmount the cheap png cards that are better left alone). |
| D325 | **A card with an authored `preview.png` swaps to the live app on hover, and back to the still on hover-end — same behavior on the Home strip and the /apps hub** | The authored still wins the resting state (precedence unchanged: it is the frame the author chose, and by far the cheapest), but it is also static — hovering is the natural "show me it running" gesture, and both surfaces render the same `AppPreviewCard`, so one component change keeps them consistent by construction. **The swap never shows a blank frame**: on mouseenter the iframe mounts UNDERNEATH the img in the stacking order, and the still only fades (0.15s opacity) once the iframe's `onLoad` has fired (`liveReady`); a slow-booting app keeps its png until it has actually painted. Mouseleave unmounts the iframe outright and resets `liveReady`, so the png is back instantly and the app's runtime is reclaimed — hover is a lease, not a toggle. The hover iframe is additionally gated on the same `useNearViewport` band as the resting live preview (D324) — nearly free (a hovered card is by definition on screen) and it keeps every iframe mount in the component behind one signal. **Rejected:** swapping `src` on a single element (loses the png-under-iframe layering that hides the boot); keeping the iframe mounted after hover-end (defeats D324's reclamation — a browse across the grid would pin every app it grazed); a hover-intent delay before mounting (the mount is cheap next to the app's own boot and the png hides the whole warm-up anyway; add one only if real use shows churn). |
| D327 | **`/api/ai` counts what it does — tokens, failures by kind, seconds generating, and which tier — in memory, in this process, one hour of 10-second buckets — and /ai-models grows a Usage tab that graphs it** | The app had no answer to "is anything generating right now, how much, how fast, and is it even working". `fused.ai` is the one place model time is spent (a page's chat box, the Resolve-with-AI call, a resident local model), and the only trace a completion left was a line in a disposable app log — so a page re-asking the model on every keystroke looked exactly like an idle machine, a chat box whose every call timed out looked like a page nobody opened, and the local models page could say what was LOADED but never what was RUNNING or how fast. **Four counters, and the three beyond token volume are what make the tab a diagnostic rather than a curiosity**: failures by kind, because a timeout, a missing `claude` binary and a model still downloading send a user to three different places and all three draw as an empty graph; seconds and the tokens/second they divide into, because that is the number somebody picks between two local downloads with, and the explanation when a model that landed on the CPU (AI-11b) "feels broken"; and the Claude/local split on AI-1's own `/`-in-the-id seam, because this page's subject is the local half and one merged figure answers neither "what is this laptop doing" nor "what am I sending out". **The scope is deliberately the smallest thing that answers them.** In memory, so there is no file to prune, no schema to migrate and no store to get wrong; per process, so `since` is the whole honesty story (the tab says "counting for 2h 14m", never "today"); and no cost, no pricing and no bill anywhere — a usage ledger is Claude Code's own to report, and a graph dressed as one would be wrong the first time the app restarted mid-morning. **REJECTED: reusing the call log** (§31). It already records every `/api/run` and `/api/fs/write` with durations and outcomes, and `/api/ai` could have joined it — but that store is durable JSONL under the shell home with retention, rate-caps and a viewer, and the question here is "the last fifteen minutes", which is exactly the window a disk-backed log is the most expensive way to answer. **REJECTED: counting tokens ourselves.** The relay sees the text, so it could estimate; it does not, because a number derived from the characters would sit under the same label as the one the model reported and disagree with the `usage` the very same response handed the page. `record()` is therefore fed the caller's own dict at the terminal frame, on all four exits (Claude/local × streaming/not), which makes agreement structural rather than a thing to test for. What that costs is stated in AI-12a and not hidden: a client that vanishes mid-stream contributes nothing, since only the terminal frame carries a count. **A failure is never folded into `completions`** (AI-12b); a malformed request is not a failure at all; and neither is a 409 from a model that is still loading, which did exactly what AI-5 designed it to do — otherwise the one number that means "the AI is not working" would also mean "somebody sent a bad body" or "a download is running". For the same reason the tab shows failures ONLY by kind — no headline total, no tile, no column: one number over several unrelated conditions is one a reader cannot act on, while `2 × timeout` names the next step. **The tab is the fourth on /ai-models rather than a page of its own**, because every other answer about running a model locally is already there — which model is resident, on which engine, costing what memory — and it covers the Claude tier too, since `fused.ai` is one door (AI-1) and a graph counting half of it would be the wrong number under the right label. Bars, not a line: the series is a count per interval, and a line would draw a slope through minutes in which nothing ran. **Input tokens are measured in the WORKER** (AI-3): the local text runners now report `input_tokens` beside the tokens they generated, counted with the model's own tokenizer before the first token — the only process that can answer, since a characters/4 estimate upstream would carry the same label as the model's own number and disagree with it. **Cost is the one figure here that is not measured** (AI-12d): each model row carries a `$ per million output tokens` box, default 1 and output-only, and every place the result appears says "estimated". A built-in price table was rejected — it would go stale silently and be believed anyway — and the rate is page state rather than a stored preference, because persisting a what-if would make a guess look like a setting the app stands behind. Failures are shown in the ordinary weight and never in error styling: a refused call is a fact about a session (a Stop, a model still downloading), and a red figure made one of them read as the app being broken. |

## Open items (small, non-blocking)

- Shell visual design: v1 = clean minimal; real design pass later.
- **Param defaults accreting on a folder URL.** The listing's preview pane runs a
  template against the FOLDER's URL, so a folder accretes the stamped defaults
  (D271) of every file type previewed in it —
  `…/local?sel=x.zip&sort=&offset=0&zipPath=&zipMember=`. It costs no history
  entry and reproduces correctly, but it is visible in the address bar and rides
  into a copied link. Undecided: whether the stamp should be suppressed for a
  pane whose target is not the URL's own path.
- **Making the nav epoch discriminate param-only `popstate`s**, so Back stops
  remounting the whole view now that D270 notifies without one — and **reserving
  `sel`/`sort`/`dir`/`snapshot`** as shell-owned keys — are both still open owner
  decisions.
- `fused.ready()` promise: not needed in v1 (params readable synchronously from parent URL).
- Fused account (§27): a manual DMG smoke of the login round-trip on macOS is
  still outstanding — the packaged-app path (bundled CLI, Keychain secrets
  store) can't be exercised from CI/Linux.
- Drive bring-your-own OAuth client ID: **shipped, and no longer optional** — see
  the rewritten D205. It was held as a nice-to-have for users hitting rclone's
  shared-client rate limits; Google retiring that shared client made it the only
  way Drive works at all.
- Drive OAuth: the `rclone authorize` round-trip needs a manual smoke on a real
  Google account; CI has no browser and no credentials to complete a consent.

## How to continue this project

1. Read `SPEC.md` (what & why), `ARCHITECTURE.md` (exact contracts), this file (decisions).
2. M1 definition-of-done = ARCHITECTURE §9 verification checklist.
3. Don't re-litigate decisions D1–D18 without the owner asking.
4. After M1: pick from follow-up list with the owner.
| D328 | **When something AROUND the app is broken — Claude Code missing, not signed in, over its limit, a config that will not load, a template registry that will not parse — the app says which of those it is in plain words, links to the matching answer on the download page, and hands the user one copyable block carrying the installation and the verbatim error** | Every one of these used to surface as a red string naming an endpoint or a CLI: "Failed to load config: HTTP 500", "App created, but Claude didn't start: …", or — for a broken registry — nothing at all, just files whose own bindings had quietly stopped applying. All of them are fixable by the person at the machine in about a minute, and none of them said which minute to spend. **The four cases are the download page's four cases** (`notfound`/`login`/`limit`/`raw`, its own `data-err` tabs) rather than a taxonomy invented here, because that page already explains each one and already accepts a deep link its `honorHash` reads — two vocabularies for one problem is how a user ends up reading about installing something they already have. Classification is ORDERED and tested for exactly that reason: a signed-out Claude matches "not found" patterns too, and "install Claude Code" is the worst possible advice for someone who needs `/login`. **The copyable block is the point of the card** — everything else nudges toward a page, but the copied text is what a user pastes into their own AI or an issue and gets an answer from, so it leads with what the app was doing (a paste that opens with a traceback makes the reader work out the question first) and carries the install path, version and platform: the facts every diagnosis needs and nobody can look up for themselves. The error itself is never reworded, because rewording makes it unsearchable. Warning-toned rather than error-toned, on the same argument as the modified badge: the app is running, something beside it is not. One component in two sizes: full on the otherwise-empty page a boot failure leaves and under the builder's composer (where the app folder WAS created and only the session failed, so there is something to say beyond the error), compact wherever it lands in a notification stack, because a 340px column is not a place for a wall of text. The preview's fallback is deliberately NOT one of them: #585's `RegistryFixNotice` already states the same unreadable-registry error there and offers a button that repairs it, and a copyable report is what you offer when nobody present can act — a one-click fix beats it outright when somebody can. The boot case is the one that cannot describe itself, since `/api/config` is what failed, and it degrades to the error plus the help link rather than printing empty labels. And `template_error` is finally READ: the server has reported it since PT-8 and nothing consumed it, so a `~/.fused-render` registry that would not parse looked exactly like a folder full of unremarkable files. The case this still owns is the PARTIAL one, which no fallback card can reach — the built-in registry keeps matching, so files preview and only the user's own bindings stop applying, which is the symptom that was actually reported ("apps aren't rendering properly") — announced once per distinct error as a toast rather than once per file. |
| D331 | **An embed framed by a NON-explorer page gets a fourth framing flag (`IS_FOREIGN_EMBED`, same ask-the-host climb as `IS_PANEL_PANE`), and under it the "Browse contents" chip navigates the TOP window to the folder's real explorer page (`viewUrlForFsPath`) instead of switching the embed's own `_mode`** | The owner's report, from the canvases workspace: *"When I click it only navigates the right panel. I would rather navigate the entire page /canvases/canvas_1 → /explorer/…/canvases/canvas_1."* The chip's in-place `_mode` switch (D81) is the right behavior inside the explorer's own surfaces — a panel pane or a tab owns its layout, and a top nav would blow away the split the user built — and the wrong one under a foreign host, for two reasons that compound. First, the embed there is a COMPONENT in someone else's composition: the canvases page put a chat column beside the hosted workbench, and the switch turns that column into a chrome-free file listing — half a browsing surface sitting where the host meant a conversation, with the host's own header still claiming otherwise. Second, it walks straight into D282's recorded dead end: an embed listing always has its pane, the pane suppresses the chip, the embed hides the header — no way back short of reloading the host page. Navigating the top window answers the actual intent ("show me this folder's contents") with the full explorer, and dissolves the dead end for this host class instead of ruling on it: Back returns to the canvases page, because the assign is an ordinary cross-document navigation. **The flag follows `IS_PANEL_PANE`'s idiom exactly** — climb the ancestor chain, no marker param (navigate() drops queries; the host's pathname IS the sentinel), read once at module init since a document cannot be re-parented — and its predicate is "no ancestor pathname under /explorer": the explorer's surfaces (panel, tabs, bookmark-card peeks) all live there, so they keep the in-place switch by construction, and any page that borrows the embed as a component gets the top nav without registering anywhere. A cross-origin ancestor answers FALSE, the safe direction: an external site framing us must never have its top window steered by our chip. **`viewUrlForFsPath` exists because `urlForFsPath` could not say this**: it stamps the CURRENT page's prefix, which inside an embed is the embed prefix — the chip would have framed the top window itself chrome-free. A helper pinned to the view prefix is the inverse of `embedUrlForFsPath`'s existing pin, not a new idea. **Rejected: postMessage to the host** (chip asks, host decides) — a protocol with two ends for a decision one end can already make correctly, and every future foreign host would have to implement the listener or inherit the dead end. **Rejected: making the chip top-navigate in ALL embeds** — kills the in-place toggle for panel/tab panes, which is the case D81 built the chip for. |
| D332 | **The canvas workspace loses its header strip entirely — no back button, no name, no sync pill, no "Open local files", no local-wins note; the page is the two panes at full height, and the only chrome left is the error banner when a push fails** | Owner call, verbatim: *"let's remove the top bar that have canvas back btn, name, synced, open local files"*. Everything the strip carried is redundant or better answered elsewhere: back is the browser's Back button (the same ruling PT-13b made for a folder's mode views — a navigation is undone by the navigation control the user already has); the canvas name is the URL and the tab title; "Open local files" duplicates what the chat pane's own surface offers over the same folder (and D331's chip now takes the whole page there); and the local-wins caveat was a permanent sentence about a rare event — the place to say it is the moment it bites, not a banner over every session. The sync pill is the one real loss, and it is accepted BECAUSE of its shape: three of its four states (Watching, Pushing…, Synced) are reassurance, not information — nothing the user should act on — and the fourth (Push failed) still surfaces, larger than the pill ever was, through the error banner this decision deliberately keeps (and which D336 grows into the transcript + fix card). The status poll keeps running untouched: it feeds that banner and the watcher self-heal, both of which are about the sync working, not about a strip saying so. **Rejected: collapsing the strip to an overlay/hover chrome** — a smaller version of chrome nobody asked to keep; if a per-state need reappears (e.g. wanting "Pushing…" feedback during a slow push), it should come back as a transient indicator tied to that state, not as a resident bar. |
| D329 | **Per-file session restore is REMOVED OUTRIGHT (retiring D84 and SPEC §21) — both halves, not just the replay: nothing writes `lastSession` and nothing reads it** | The owner's instruction was to remove the restore functionality altogether, and taking only the replay half was rejected on the spot: with nothing reading `lastSession`, the tracking `PUT` would keep firing on every param change of every file to maintain a key no code path consults — a write-only log nobody asked for, and the exact kind of residue this repo's own `viewstate.ts` argues against ("a key the app no longer reads is not harmless"). So the whole machinery goes: `server/session.py` with `GET`/`PUT /api/session`, `_session_get`/`_session_put`, `_strip_side`, `_has_non_mode_param` and `_stored_session`; `platform/lib/session.ts` (`useSessionRestore`/`useSessionTracking`); `getSession`/`putSession`/`LastSession` in `platform/lib/api.ts`; and `restoredSearch` in `platform/lib/session-params.ts`. **Two things had to survive the deletion, and each is why this is not a pure `git rm`.** (1) `_is_file_mount_safe` moves to `server/common.py`: it is the mount-safety rule that a cold `os.path.isfile` on a mount-backed path is the GETATTR that enumerates the whole parent prefix and drops the mount, it lived in `session.py` only because `/api/session` was its first caller, and `/render` (`routers/render.py`) is now its only one. (2) `stripSessionParams` and the `_side` never-persisted policy (LSN-12, D326) survive in `session-params.ts`, because the RECENTS store still needs them — recents captures `currentUrl()` verbatim and lands on disk, so without the strip every close of the companion sidebar is persisted and every later open from the Recents list comes up shut. That rule now has ONE implementation instead of two halves that had to agree, the server's `_strip_side` having gone with its module. **User-visible consequence, stated rather than hedged:** opening a file with a bare URL now renders it at its defaults, every time. A view you want back is a bookmark (SB-2, which captures the URL verbatim and is the explicit "save this view" gesture) or a Recents row (RC-3, which tracks live param changes) — both already existed, both are chosen rather than inferred. What is lost is the unchosen convenience: a Finder double-click or a listing click no longer resurrects the params you happened to leave on that file. **A latency win falls out of it:** `StatView`'s `ready` gate is gone, so no file open ever holds the preview behind a session read — the `~2s` cold-mount stall the `writable` opt-out existed to dodge (and the read-only-mount "Loading…" incident that gate was added for) is now structurally unreachable rather than avoided case by case. | Owner call 2026-08-18. **No migration, by the same reasoning D205 used for the sidecar move:** a `lastSession` key already on disk is simply never read or written again and sits inert beside the sidecar's live keys, which are untouched — the sidecar file itself stays exactly where it is and keeps serving `claudeSessions`, `bookmarkHistory`, `comments`, `revertStash`, `slides`, `docs`, `excel`, `latex` and `usd`. Stripping the stale key on every read-merge-write was considered and rejected: it would touch all nine writers (four Python, five JS templates) to tidy a key that costs nothing to leave, and a one-time purge pass over `~/.fused-render/sidecar/` was rejected more firmly still — it is the only option that eagerly rewrites every user's sidecar, including files we would otherwise never open, to no functional end. SPEC §21 is kept as a tombstone rather than deleted, following §24's precedent (D243): FS-10 cites `§21's LSN-12` and RC-9 cites `LSN-6 posture`, so the ids have to resolve to an explanation instead of to nothing — and LSN-12's surviving half is restated there as a live rule about recents. `tests/test_server_session.py` is deleted with the endpoints it tested; `test_sessions_inbox_layout.py` keeps its tests and loses the premise in its docstring — that page's localStorage fallback was justified by session restore skipping read-only mounts, and is now simply the only mechanism there is. |
| D333 | **Canvas cards get their real preview image from the control plane, and the canvases list stops going through `fused canvas list` when the CLI is our own interpreter — a new in-interpreter shim (`_fused_canvases_list.py`, same spawn pattern as `_fused_token.py`) lists lite collections directly and resolves each entry's preview server-side; typing a space in the new-canvas name lands an underscore instead of graying out Create** | The CLI's `canvas list` prints bare sorted names, which is why cards were letter monograms: no id, no timestamps, no preview. The lite collection payload carries all three, plus `preview_image_url` in the workbench's own encoding — a public https URL (git-published, used as-is), the `fused_uploaded_preview` sentinel (image in the private bucket; the shim exchanges it for a 1h presigned URL via the control plane's `GET /collection/sign-image?collection_id=`, 404 → no preview), or null. Signing happens inside the ONE shim call, not per-card endpoints — a per-canvas endpoint would pay the fused import cost per thumbnail, and an `<img>` can't carry the X-Fused guard header anyway, so the browser is handed the S3 URL directly. The shim honors `FUSED_ENV` exactly the way the CLI's `--env` envvar does (`fused._env`), verified live: the same account lists 7 collections on prod and ~90 on unstable, so skipping this would have listed a different environment than pull/push target. List entries gain `preview_url` and `updated_at` (epoch from `last_updated`), which also fills the sort/modified hole for never-cloned canvases; the card thumb becomes an `object-fit: cover` image when `preview_url` is set, monogram otherwise. Shim failures keep `_run_cli`'s re-authenticate→401 sniff so expired credentials still land on the sign-in flow, and an external `FUSED_RENDER_FUSED_BIN` (not a Python we can drive) keeps the old CLI path — bare names, null preview fields, monogram cards. The name-input fix is one line: `replace(/\s+/g, "_")` on change — spaces are the only near-miss users type from a canvas title; other illegal characters keep the existing disabled-Create rule. Covered by three new tests (shim entries surface preview/updated_at + clone enrichment, shim 401 sniff, external-fallback nulls). | User report 2026-08-18: creating "test create from render" just grayed the box out, and the list "should be able to get the preview image for each canvas from server". Sign-image mechanism confirmed against the live unstable control plane before building (one canvas with an uploaded preview returned a presigned `fused-image-assets.s3` URL). |
| D334 | **Every Claude session fused-render spawns can run the `fused` CLI directly — the server writes a `fused` wrapper under `home_dir()/fused-bin`, puts that dir on the PATH every child inherits, and the claude template pre-allows `Bash(fused:*)` and discloses the CLI in its prompt, both exactly when the wrapper exists** | The ask was direct: fused-render already drives the `fused` CLI for the canvases sub-app (`workbench canvas push/pull`, D-numbers around §27), so a chat session should be able to push the user's work itself instead of describing shell commands the user must run elsewhere. Three gaps stood between a spawned session and a working `fused`, and each gets its own half of the fix. **PATH and interpreter poison**: the claude spawn passes no `env=` — the child inherits the server environment verbatim, which in the packaged .app means no `fused` anywhere on PATH plus bundle-scoped `PYTHONHOME`/`PYTHONPATH`, exactly what `fusedcli.child_env` exists to scrub for canvases' own runs. Fixed the way `_export_bundled_uv_path` fixed the same problem for uv: `fusedcli.export_fused_cli_env` (called from `export_app_env`, filesystem-only) writes a tiny wrapper around whatever `fused_cli()` resolved — the `FUSED_RENDER_FUSED_BIN` override or the in-interpreter shim — chmods it, prepends its dir to the server's own PATH so every child inherits it with no per-spawn plumbing, and publishes the dir as `FUSED_RENDER_FUSED_CLI_DIR` for the templates (SPEC PY-15 / D166: the template may not import `fused_render`, so the fact travels as env). The wrapper BAKES IN what canvases' `_cli_env` sets explicitly and a bare shell would miss, and this is the load-bearing line: `FUSED_ENV` defaults to `workbench_env()` — moved into fusedcli so canvases and the wrapper read ONE knob — because a bare `fused workbench canvas push` targeting the CLI's own default environment would silently push somewhere other than the workbench the canvases iframe shows, the exact failure the feature exists to avoid; a default (`${FUSED_ENV:=…}`), never an export-over-the-top, so an explicit `FUSED_ENV=prod fused …` from the model still wins. External CLIs additionally get the `PYTHONHOME`/`PYTHONPATH` unset inside the wrapper (same scrub, same reason as `child_env`); the shim keeps them, they are what make `sys.executable` work in the bundle. **Permission friction**: Bash is deliberately not session-grantable (`WHOLE_TOOL_GRANTABLE`), so every push would raise a card — for the one command this app itself runs unprompted on the user's behalf in canvases.py's sync loop. `_start` adds `Bash(fused:*)` to `--allowed-tools`, the third pre-allowance and the only Bash one, present exactly when the wrapper var is set — never as a guess about PATH. The FULL CLI rather than a read-only subset, because "push directly" was the point and the sync manager already pushes the same folders with no prompt at all; a prefix rule by construction, so compounds (`cd x && fused …`) still card. **Rejected: read-only allowlist (`list`/`whoami`) with carded pushes** — it re-creates the friction the feature removes while protecting against an actor the canvases feature already trusts with unattended pushes. **Disclosure**: a tool the model is never told about is a tool it never calls (D235's rule), so `_fused_cli_note` rides EVERY target shape's `--append-system-prompt` — the CLI is a fact about the machine, not the target — and says the three things that guard real failures: run it as a BARE `fused …` command (the prefix rule is why); NEVER run `fused workbench login`/`fused cloud login` (a browser round-trip a headless session hangs on — auth errors route the user to the Canvases page instead, and auth may legitimately be absent); and never hand-push inside a canvas clone under `~/.fused-render/canvases`, where the `_SyncManager` already pushes every edit and a second pusher races it. Absent wrapper, absent everything: no rule, no paragraph — a prompt promising a command the machine does not have is worse than silence. |
| D330 | **The explorer's Cmd+Z stack takes the TRASH delete, because a trash is a rename the server itself named — the hard delete stays excluded, and at the time of this decision fused-render had no bin backend on Windows or Linux, so there was nothing there to undo** *(the platform half is SUPERSEDED by D335, which adds those backends; the naming rule and the exclusions below are unchanged and still govern)* | `lib/fs-undo` held relocations only (drag-move, cut-paste, rename) and excluded delete on a stated premise: "the inverse is a restore we do not own (the OS Trash)". That premise was **wrong for the recoverable delete**. `_move_to_trash` (`server/fs_mutate.py`) implements Trash as `os.rename(path, ~/.Trash/<Finder-deduped name>)` — *we* compute the destination — and then threw it away, answering only `{deleted, trashed: true}`. Returning it (`trashed_to`, threaded through `deleteEntry` → `TrashOutcome.to` → `trashUndoPairs`) makes a trash delete **the same symmetric primitive the stack already runs**: undo renames the entry out of `~/.Trash` to exactly where it was, redo renames it back in. No new undo capability was added — `applyFsOp`, `invertFsOp`, the epoch, the in-flight guard and `UNDO_CAP` are untouched, and the new `"delete"` kind exists **for the toast's wording alone** ("Undid the delete"); a `"delete"` branch appearing anywhere in the mechanics would mean the symmetry claim is false. **What stays excluded, and why the rule is still closed:** the HARD delete (the confirm dialog, whose message says "This can't be undone") has no inverse but data destruction; copy-paste, duplicate, new file/folder and compress remain out on the original argument, which is untouched — an asymmetric op has no good answer for "what does redo mean after an undo that deleted something". **This is a macOS-local-filesystem promise and nothing wider** (SPEC FS-17 states the four cases): darwin + a same-device local path is the undoable case; darwin + cross-device (any `OSError`) falls back to `osascript`/Finder, which is recoverable **in Finder** but not undoable here, because Finder picks the location and an unnameable destination must not be recorded as a pair; a mount-backed file is a deliberate 501 (`trash unsupported` — trashing would pull the whole file through the kernel) and routes to the irreversible hard delete; and Windows/Linux are 501 too, since `_trash_supported()` is `sys.platform == "darwin"`: **both platforms DO have an OS-level bin** — the Recycle Bin, and the freedesktop.org XDG trash — and the accurate statement is only that *fused-render has no backend for them yet*, so there the Delete shortcut still ends in permanent erasure behind the confirm dialog. (Written as "the platforms lack a bin" in an earlier draft, which was wrong and is corrected here rather than left in the record; **D335 supplies the two backends**.) Two accepted losses, stated rather than discovered: the stack is **in-memory and module-level, so this undo is session-scoped** — a reload, or eviction past `UNDO_CAP`, leaves the entry sitting in `~/.Trash` for the user to drag out; and the fast path is a plain `os.rename`, which writes **none of Finder's own put-back bookkeeping** (Finder keeps that in the Trash's private `.DS_Store`, not on the item — no trashed item carries a put-back xattr), so Finder's "Put Back" is not a substitute restore path for what we trashed. `notePathDeleted` also drops the entry from Recents and the undo does not re-note it: the file returns to the filesystem, its place in the recents list does not. |
| D335 | **Every desktop platform gets a RECOVERABLE delete — macOS `~/.Trash`, the freedesktop XDG trash, the Windows Recycle Bin — and undo follows wherever the SERVER named the destination; Windows is recoverable-but-not-undoable under the rule D330 already wrote, not a new exception** | Supersedes the platform half of **D330**, which was correct about the naming rule and wrong about the world: it recorded that Windows and Linux had no bin to undo into, when what was actually true is that *fused-render had no backend for theirs*. Both platforms have an OS-level bin (the Recycle Bin; the XDG trash spec), so the D330-era behaviour — ⌘/Ctrl+Backspace on Windows or Linux falling through to the irreversible confirm-then-hard-delete — was the app erasing files the OS would happily have kept. Three backends now, behind one predicate and one dispatcher (`_trash_supported`, `_move_to_trash`), and the **six-case matrix is the normative statement** (SPEC FS-17): macOS local → rename into `~/.Trash`, recoverable + **undoable**; macOS cross-device → `osascript`/Finder, recoverable, not undoable; Linux same-device → XDG trash, recoverable + **undoable**; Linux cross-device (`EXDEV`) → **501**, hard-delete fallback; Windows → Recycle Bin, recoverable, not undoable; a remote mount on any platform → **501**, unchanged. **Undoability is decided by one question — did WE name the destination? — so Windows needs no new rule.** `SHFileOperationW` puts the entry in the bin as a `$R…`/`$I…` pair under `C:\$Recycle.Bin\<SID>\` and restores it through the shell rather than by a path rename, exactly as Finder chooses the location in the macOS cross-device fallback; D330 already says a destination we cannot name is never recorded as an undo pair, and both rows fall under it unchanged. **Linux cross-device is refused rather than copied**, and that is the sharpest call here: `shutil.move`'s cross-device fallback reads and rewrites the entire file, which is the very hazard the mount case refuses trash for (D330), and per-volume `.Trash-$uid` directories are declined for the same reason plus their own permission/ownership rules. A delete must not become the most expensive operation in the app, so `EXDEV` raises `_TrashUnsupported` → the same **501 `trash unsupported`** the platform and mount gates answer → the existing confirm-then-hard-delete, with the file still in place when the dialog appears (the one 501 raised *after* an attempt, which is safe precisely because the attempt moved nothing). `_TrashUnsupported` is deliberately distinct from an `OSError`: only "unsupported" may invite the user to erase the file, so a merely-FAILED trash stays a 500. **The XDG sidecar is where a trash stops being a plain rename, and it is answered with ONE endpoint rather than a branch in the undo mechanics**: `POST /api/fs/trash-move {from, to}` renames and, in the same call, writes `info/<name>.trashinfo` when the entry moves into `Trash/files` and removes it when the entry moves back out. It delegates to `_fs_rename` for every guard (X-Fused, absolute paths, snapshot, mount, readonly, 404/409, overwrite always off) so the endpoint cannot be a looser way in, and its sidecar branch fires only for a path whose *parent* `realpath`s to the server-computed trash `files` dir with the info name taken as a `basename` — a security boundary, since the endpoint unlinks what that computation names. The frontend consequence is exactly one line: `applyFsOp` picks `trashMove` for a `"delete"` op and `renameEntry` otherwise, and **that is its only branch on `kind`** — pairs stay plain absolute paths, and `invertFsOp`, the reversal semantics, the epoch, the in-flight guard and `UNDO_CAP` remain ignorant of trash. Restoring with a plain rename would have left the sidecar behind, with the entry home and the bin still listing it. The spec's ordering for the XDG write is followed rather than improvised: the `.trashinfo` is created **first** with `O_CREAT|O_EXCL` as an atomic name claim, then the entry is renamed in, and a claim we fail to fill is removed again; names must be free in `files/` **and** `info/`, so a stale entry is never overwritten; `Path=` is percent-encoded and `DeletionDate=` is local time with no zone suffix. **Two honesty notes rather than pretences.** `FOF_ALLOWUNDO` still erases permanently on UNC/network shares, most removable and FAT volumes, and over the bin's quota — undetectable up front, so the delete succeeds and is reported `trashed: true`, and the docs say so. And undo remains **in-memory and session-scoped** (D330): a reload or an eviction past `UNDO_CAP` leaves a perfectly recoverable entry to be retrieved through the OS's own bin UI, which is now a real answer on all three platforms rather than a macOS-only one. Windows uses `ctypes` + `SHFileOperationW` rather than pywin32's `IFileOperation` (available, but a larger change for the same result); the struct's `pFrom` is a void pointer to a buffer we own so the double-NUL list terminator stays inspectable, and both the return code and `fAnyOperationsAborted` are checked — a zero return with the abort flag set would otherwise report a file into the bin that never went there. |
| D336 | **A failing canvas push keeps its FULL CLI transcript (`error_detail`, one entry per line, capped at 50) beside the one-line `error`, the workspace strip renders the transcript under the pill, and a "Fix with Claude" button (`POST /api/canvases/fix`) spawns a detached session on the clone primed with that transcript — the chat iframe attaches to it via its `run` param** | The reported symptom: an auto-push failing with *"Canvas validation failed with 4 error(s). Fix the errors or use --no-validate to push anyway."* and nothing else — because `cli_error()` keeps the LAST stderr line, which is the right shape for a status pill and exactly wrong for validation, where the CLI prints one line per problem (a node without a source file, an edge naming a missing node, a widget ref that does not resolve) and then a summary counting them. The pill said "4 error(s)" and threw the 4 lines away. **Keeping the transcript verbatim is D328's own rule** (an error reworded is an error made unsearchable): `error_detail` is every non-empty stderr+stdout line of the failing push, summary included, cleared on the next good push and on the resume-rebaseline path that already clears `push_state`. Capped so a pathological run cannot bloat a status payload polled every 2s. **The button follows the precedent D328 itself names**: a one-click fix beats a copyable report whenever someone present can act — and here the actor does not even have to be the user, because the errors are exactly the shape a Claude session dispatches mechanically. The endpoint mirrors the apps API's session spawn wholesale rather than inventing a second pattern: `claude_spawn.spawn_helper` (fork-safe, prompt over stdin, never argv), permission mode `auto` for the reason recorded at `_APP_SESSION_PERMISSION_MODE` (nobody polls `decide` until a page attaches, so `prompt` parks the first tool call for an hour), and a `record_session_when_ready` thread so the run lands in the folder's sidecar. It 409s when the push is not currently failing — the button only renders on an error, so reaching that means the push recovered between click and POST, and spawning a fixer for a solved problem would be pure noise. **The session is PRIMED, not pointed**: the first message carries the transcript plus the two guard rails that make the loop converge — check with `fused workbench canvas validate .` (the CLI the session has since D334), and NEVER `canvas push` (with or without `--no-validate`), because the watcher auto-pushes the folder on every quiet period; a hand-push races it and a `--no-validate` push defeats the reason the session exists. That interplay is also what closes the loop with no completion protocol at all: Claude edits files → the watcher pushes → validation passes → `push_state` returns to idle and the banner (with the button) disappears, all through the machinery that was already there. **The page attaches rather than watches blind**: the response's `run_id` goes into the editor iframe's existing `?run=` param (the same attach the apps flow uses), so the fix runs in the chat pane the workspace already shows, its permission cards answerable there — kept attached after recovery, since the session may still be writing its final message. **Rejected: pushing with `--no-validate` from the sync manager** (turning validation into a non-blocking badge) — defensible as philosophy (a mirror should mirror; mid-edit canvases are legitimately invalid) but it ships broken canvases to the hosted workbench the user is looking at in the left pane, and the owner picked the fixing path over the tolerating path. **Rejected: prompt-only** (teach the spawned session to run `validate` — no UI) — cheapest, but it only helps once a chat is already open; the failing state lives on the workspace strip, where a session has to be one click away, not one paste away. **A second click is guarded server-side, not guessed at from the client**: `_SyncManager.active_fix_run_id` + `fix_lock` make the whole check-then-spawn-then-set sequence atomic (spawn_helper's subprocess alone can run for seconds — plenty of room for two workspace tabs to both pass a lockless guard), and the id clears when the recorder thread that follows it exits for ANY reason (done, an exception, or the poll cap), not only on an observed "done". **Rejected: client-side transcript-liveness polling** (`session_liveness.session_running`, gating the button on "no recent activity") — a code-review pass found it inherently racy (a stale poll for the previous run could poison a shared ref) and its 45s no-activity grace window, fine for a status badge, would misread a slow `fused workbench canvas validate .` mid-fix as "finished" and let a second click through. |
| D337 | **The Fused workspace moves out of iCloud's reach: the default is `~/Fused`, not `~/Documents/Fused`, and an existing workspace is migrated on the next start** *(the sidecar-subtree half of the move — (b) below — is SUPERSEDED by D359, which deletes the sidecar outright: the migration moves the folder and rewrites the app's own state, and nothing else)* | macOS "Desktop & Documents Folders" sync is on for a large share of iCloud users — it is on for the owner's machine, where the workspace is 408 MB holding **eight git working trees** (`sandbox`, `showcase`, five installed `local/` apps). Syncing a working tree churns `.git` objects and the index through the sync daemon on every commit and exposes them to iCloud's large-file eviction, which replaces a file with a dataless `.icloud` stub — an evicted object or index is a repo that fails to read at a moment nothing in this app chose. macOS offers **no supported per-folder exclusion** from Desktop&Documents sync; the two workarounds are a `.nosync` suffix or replacing the folder with a symlink, and neither is acceptable as a shipped default (the first renames the user's folder, the second makes every absolute path in the app's state point through a link the user did not ask for). So the only real fix is to live outside the synced roots. **`~/Desktop/Fused` is explicitly rejected: Desktop is synced by the same feature** — it is the other half of the setting's name, not an alternative to it. `~/Fused` is a top-level home folder the sync feature never touches. *Secondary, and stated as secondary rather than as the reason:* `~/Fused` is also outside the macOS TCC protected folders (Desktop/Documents/Downloads) that D72, D73 and D289 exist to work around — a workspace there does not need the app's Documents grant at all. **`FUSED_RENDER_DIR` is unchanged and still wins everywhere** (it is what `tests/conftest.py` redirects so no test sees a real workspace), and it is also the whole of the opt-out: a user who set it is skipped by the migration entirely, because moving a location someone deliberately chose is worse than leaving it where it is. **The migration (`workspace_migration.py`) runs at the two real process entry points, and strictly BEFORE `seed.ensure_fused_dir`** — onboarding creates the destination, and a destination that exists is exactly what the migration refuses to move into. Never from `create_app`, preserving the property that importing the server in a test touches nothing real. In the menu-bar app it runs **in `main()`, before the AppKit run loop starts — not in `_start_server_thread` beside `meta_migration`**, because rewriting on-disk state is a precondition of every component that READS that state, and the pin reads early: `PinController.__init__` caches the pinned absolute path, and the boot timer builds it before the server thread exists, so migrating in the server thread left the popover on the pre-move path for the whole upgrade session — the one session the migration ever fires in. An audit of everything `app.main()` touches before the bootstrap thread found the pin is the ONLY such reader (the pidfile/portfile and the desktop-instance token are Application Support state the migration never touches; onboarding, `community.refresh_in_background`, `meta_migration` and `create_app` all live in the server thread and are therefore still strictly after). The main-thread cost is a same-filesystem `os.rename` — metadata only, no bytes move — plus a few small JSON reads, so the run loop starts imperceptibly later. It moves **three** things, because the workspace's absolute path is written down in three places: (a) the folder, by `os.rename` — it holds git working trees, and a copy-and-delete would rewrite every object and risk a half-copied repo; (b) the **sidecar subtree** (`home_dir()/sidecar/<mapped abs path>`), because sidecars are keyed by the source file's absolute path and moving the workspace orphans all of them — and they hold real data, not cache: `comments` (the annotate append-only log with tombstones, D101), `docs` version history, `revertStash`, `bookmarkHistory`, `claudeSessions`, slides/excel/latex/usd settings (the inert `lastSession` key D329 stopped reading or writing rides along untouched — it is not a reason to move anything). The mapping is a pure path transform, so **one directory rename** re-homes everything and no file contents are patched; both sides are derived through `storage._sidecar_subpath` so drive-letter and UNC shapes stay correct, and through `home_dir()` so a `FUSED_RENDER_BRANCH` install operates on its own nested home; (c) the absolute paths in the app's own state — community `installs.json` records, bookmark and recents **view urls** (decoded, re-rooted, re-encoded per segment, query string carried through byte-for-byte because it holds the page's params), scheduled-message `target`s (a stale one fails a 400 on an unattended run), and the menu-bar pin's `path` in `pin.json` (D97, SPEC §25 PV-7 — a stale one leaves the pinned view unopenable until the user re-pins). The pin is the one state file that does **not** live under `home_dir()`: it sits in `APP_SUPPORT_DIR` (`~/Library/Application Support/fused-render`), which is why it was missed on the first pass; the migration re-derives that dir through the same `_branch.branch_dir` on the same base rather than importing it from `app` (a cycle — `app` already imports and calls the migration), so a `FUSED_RENDER_BRANCH` install rewrites its own nested pin, and it merges the rewrite in place because `size` is a window preference that survives re-pins (`pin_store.save_pin`). `opened.json` (slug → timestamp) and `app_recents.json` (workspace-RELATIVE paths) were checked and need nothing. **Safety is the whole design:** it moves only when the source exists AND the destination does not — any other combination logs and does nothing, never a merge and never a clobber; it deletes nothing; a failure at any step is logged and swallowed so the app starts anyway; and the bookkeeping steps re-run on every start under their own "old shape present, new shape absent" gate, so a process that died between the folder move and the state rewrite heals on the next start instead of leaving orphaned sidecars forever. The frontend's `DEFAULT_TARGET_SUFFIX` follows (its comment claimed `~/Desktop/fused`, which was wrong then and is the specifically wrong answer now), `community.py`'s hand-copied mirror of the default is **deleted** in favour of importing `fused_dir`, and `templates/shared/appenv.py`'s copy stays only because template subprocesses are stdlib-only and cannot import `fused_render`. **Deliberately not done:** `static/runtime.js`'s third copy of the sidecar mapping (deferred), and the orphaned pre-D205 co-located sidecars in the old workspace (user files — nothing here deletes anything). |
| D338 | **Canvas sync becomes a real two-way channel: per-file three-way merge against a persisted sync-point base, driven by a cheap remote manifest probe that runs clean OR dirty — replacing "local-wins wholesale + poll only while clean"** | The old model's loss case: while the clone had unpushed edits (a Claude session mid-work), remote polling was suspended entirely and the next `canvas push` REPLACED the remote UDF set — a workbench edit made in that window was silently destroyed, deletes included. Two facts found in the application repo shaped the fix. (1) `collection.last_updated` does NOT bump on a UDF edit (only `crud.update_udf` bumps `udf.last_updated`), so the obvious collection-level probe is dead — but `GET /collection/by-name` returns per-UDF `udf_body_hash` (a DB-computed md5) plus `last_updated`, and collection updates (layout/dashboard) DO bump the collection stamp: together one GET is a complete change manifest, no zip download (the old probe was `pull --dry-run`, which downloads the whole bundle every 10s). (2) The exported file layout does not correspond to `md5(udf_body)`, so **server hashes are only ever compared server-vs-server** (previous manifest vs current); local change detection uses our own per-file md5 base from the last sync point. That base + the manifest live in `<canvases_root>/.sync/<name>.json` — deliberately OUTSIDE the clone, because the CLI's `pull --force` deletes any in-dir file not in the bundle. Merge rules (dirty clone, remote moved): local untouched since base → remote wins (including remote deletes); local changed → local wins (the debounced push publishes it); local deleted → stays deleted (the push propagates it); no base (unknown provenance) → local wins wholesale, i.e. exactly the old behavior. The zip for the merge comes from a new `_fused_canvas_zip.py` shim (`download_collection_toml_zip`), applied by our own extractor with the CLI's path sanitizer reimplemented (absolute/`..`/drive-letter members rejected) since the CLI's no longer protects us. The same probe+merge runs immediately before every push, so a push can never blindly replace a remote that moved since the last sync point. Clean clone + remote moved keeps the CLI `pull --force` wholesale path. **Known blind window, accepted:** an edit landing on the remote between push-complete and the post-push re-probe is absorbed into the new manifest baseline (server hashes aren't computable locally); it converges on the next workbench save. External `FUSED_RENDER_FUSED_BIN` (no interpreter for shims) degrades to the previous behavior verbatim. **Rejected: focus-baton half-duplex** (host↔iframe focus transitions as sync barriers) — considered at length and dropped: iframe focus detection is finicky, workbench autosave lags focus-out (a pull-on-refocus reads stale remote then clobbers the late save), it does nothing for edits from another tab/machine, and it fixes no correctness hole the merge doesn't already close; it only buys ~seconds of freshness over the poll. **Rejected: If-Match/version on the push endpoint** — right long-term, but a backend project; probe-before-push shrinks the race to the CLI call itself. Follow-ups: the workbench blocks autosave in embed mode by policy (`use-canvas-route-owner-state.ts`) — written precisely because the old sync clobbered; the merge removes that rationale and the block must be lifted in the application repo for iframe-originated edits to exist at all. Also deferred: wiring the persisted base into the `_FRESH_WINDOW_S` startup-dirty heuristic. `merge_seq` joins `push_seq`/`pull_seq` in sync status. |
| D339 | **Three stability guards on the D338 merge: echo guard (a superseded remote state is re-pushed, never pulled), trash (every sync overwrite/delete is snapshotted first), and a post-merge validation gate (an unpushable merge rolls back)** | Each guard answers a field incident. **Echo guard** — RCA: host push landed, then a stale browser tab autosaved its pre-push in-memory state back over the remote (workbench collection saves are last-writer-wins with no revision precondition), and the sync faithfully pulled that resurrection down, deleting a locally-created file. Detection: keep a ring of the last `_HISTORY_MAX` superseded remote manifests; a probe whose per-UDF hash set exactly matches one of them (within `ECHO_WINDOW_S` of it being superseded) while differing from the current baseline is a stale writer, not a new edit — never pull it, queue a push that re-asserts local; the push's success re-probe re-baselines past the echo. Deliberately narrow: only the UDF-level revert (the data-loss shape) is catchable — a layout-only echo is indistinguishable from a real layout edit because a stale save stamps a fresh `collection.last_updated` either way; and past the window an identical state is honored as a deliberate workbench revert. Bounded window chosen over "never accept an old state" precisely so intentional reverts stay possible. **Trash** — `.sync/trash/<name>/<ts>/` receives a copy of every file a merge overwrites/deletes (per file) and a whole-clone snapshot before the CLI's `pull --force` (whose writes we can't intercept), pruned to `_TRASH_MAX` snapshots. Whatever logic hole is found next, the answer becomes "restore from trash", not "the file is gone". **Validation gate** — RCA: Claude renamed a node while the workbench moved the same node; the merge ran mid-rename (local `canvas.toml` not yet rewritten → looked untouched → remote toml won) and produced toml-references-old-name + file-has-new-name, a clone `canvas push` rejects forever — the "invalid local state, unable to push" dead-end. A per-FILE merge cannot see cross-file invariants, so after any merge that applied changes, run `fused canvas validate`; failure rolls the merge back file-by-file (rollback list carries prior bytes + prior base entries), bumps `merge_rollback_seq`, and leaves the clone dirty so the push re-asserts coherent local wholesale — the merge may lose the remote's concurrent layout tweak but may never manufacture a state no one wrote and nothing can push. `_remote` still advances to the probe so the broken mix isn't re-attempted every poll. Validate returning "can't run" (no CLI, timeout) is treated as valid — a broken CLI must not wedge every merge into rollback. **Rejected as the fix: server-side optimistic concurrency** (revision + If-Match on collection saves) — it is the *right* fix for the whole stale-writer family and stays the roadmap (kills the echo at the write instead of healing after), but it's an application-repo change on both save endpoints plus client 409 handling; these three guards are client-only, ship now, and remain correct under it. Follow-up (unbuilt): node-level semantic merge of canvas.toml (geometry from remote-if-changed, structure from local-if-changed) so rename+move both survive instead of local-wins dropping the move. |
| D340 | **Every canvas clone is seeded with a CLAUDE.md that points the session at the `fused:*` plugin skills (canvas-toml, fused-udfs, json-ui-schemas, fused-cli), tells it to STOP and have the user run `fused claude plugin add` when they're missing, and restates the two sync rules (never hand-push/pull; validate after structural edits) — plus a `.fusedignore` keeping both files out of every push** | The Claude session working in a clone (opened by the user, or spawned by the D336 fix button) otherwise starts blind: it doesn't know the folder is watched (a hand `canvas push` races the watcher — the exact failure D336's prompt guards against, now stated where every session sees it) and doesn't know the format skills exist. Seeding must be invisible to the sync in BOTH directions or it becomes churn: the two basenames are excluded from the fingerprint/hash walks (never dirty, never in the merge base), `.fusedignore` (which the push's GitignoreFilter honors, and which lists itself) keeps them out of the bundle, and the merge's bundle reader skips them defensively (a --no-ignore push from elsewhere must not overwrite the seed). The CLI's `pull --force` deletes them (local-only files not in the bundle), so clone and both pull legs re-seed afterwards — which also means a user's local edits to the seeded CLAUDE.md do not survive a pull; it's a maintained artifact, not a user file. **Gated on shim availability (not seeded under an external FUSED_RENDER_FUSED_BIN)**: the external path syncs via `pull --dry-run`, which reports seeded local-only files as a diff on every poll — permanent pull/reseed churn. The real fix for that is one line in the CLI (`_PULL_DELETE_IGNORE_BASENAMES` += CLAUDE.md/.fusedignore, application repo) — filed separately; the gate stays regardless as old-CLI protection. |
| D341 | **Index search FILTERS AND RANKS on the server (`GET /api/index/rank`), and the home page stops fetching a corpus at all** | Measured on the running app, the home page's first search after a refresh took ~2 s, and almost none of it was ranking: `/api/index/search?fmt=columns&root=~` spent 0.43-0.46 s server-side and shipped **19.8 MB raw / 5.4 MB gzipped** — 164,405 rows as four parallel arrays, `rels` alone 15.7 MB — with the rest going on browser decode and main-thread contention with the page's own load burst. Worse, that corpus was **silently truncated**: the index held 571,429 files under home, `MAX_CORPUS` cut it to 200,000 depth-ordered rows and the gitignore filter took that to the 164,405 the client received, so **~71% of the user's files could not be found by the home search at all** while the response cheerfully said `truncated: true`. **So the ranking moves to Python.** `fused_render/index/rank.py` is a faithful port of `platform/lib/fuzzy.ts` + `listing/search.ts`, and the JS ranker stays the AUTHORITY rather than being replaced: the in-folder search still ranks a live streamed walk in the browser, and only a browser-side ranker can rank a stream as it arrives. Two rankers answer the same box depending on coverage, so parity is pinned by `tests/fixtures/rank-parity.json`, generated from the JS side by `bun scripts/gen-rank-fixture.ts` and asserted in BOTH languages (`tests/test_index_rank.py` and `listing/rank-parity.test.ts`) — an intentional ordering change on either side now fails loudly and forces the other to follow. `search_ranked()` is two stages because neither alone is affordable: SQL narrows and coarse-orders over all 571k rows, Python does the real fuzzy scoring on a bounded 2,000-row slice (scoring every subsequence candidate in Python is the 186 ms case). Stage A itself is a **lossless ladder** — substring `ILIKE` first, subsequence regex only when the cheap pass cannot fill the returned limit, worth ~3x on common queries (`render`: 30,319 rows / 51 ms against 176,505 / 143 ms) and lossless because `fuzzy_match`'s substring branch sets `longest_run = len(q)`, the maximum the subsequence branch can never reach, and `rank_compare` orders on `longest_run` first. **Gitignore-filter BEFORE the cut to `limit`, not after** — filtering after is precisely the bug that made the old corpus report more than it held. | Owner call 2026-08-18, run as an experiment whose success criterion is what the user FEELS, not a benchmark — a change that lowers total work and makes the box feel hesitant has failed. Hence, in `FilesSearch`: the first keystroke fires on the LEADING edge (the 120 ms debounce coalesces a burst, it does not delay the first request, which answers in ~40-50 ms); the in-flight request is aborted, never queued; the previous query's rows are **never blanked** while the next answer is in flight, only dimmed, because results → nothing → results is the most visible way this could feel worse than local ranking; a pending note appears only after 200 ms so the common fast answer never flashes one; a 20-query session memo (cleared wholesale when the index moves) makes backspacing instant; and one `requestIdleCallback` call warms the endpoint on mount, which is a few KB now rather than 20 MB. `positions` are deliberately NOT on the wire — the client re-runs `fuzzyMatch` over the ~200 returned rows — so `fuzzy.ts` remains the single source of truth for what highlights. Rejected: a depth cap in stage A (depth is already a tie-break in the ORDER BY, so shallow-first comes for free, while a hard cutoff would hide a deep exact match and cost a second round trip exactly when the first answer was wrong); and touching `search_under` / `fmt=columns`, which is the `fused.fileIndex.search` bridge contract user pages are written against and which the in-folder search still uses. **Known and logged rather than hidden:** stage A's 2,000-row cap can in principle drop a row stage B would have ranked first — tier ordering makes it unlikely, and a cap that bites is a debug line in the log and `truncated: true` in the response (a WARNING there fires on every keystroke of any broad query, and a line that appears whenever the app is working normally trains everyone to ignore the log), because silent truncation is what this change removes, not what it reintroduces. |
| D342 | **The in-folder search is answered by the INDEX, per query, and the live streamed walk survives only for the folders no scan can ever cover — with the server, not the client, saying which those are** | Phase 2 of D338. The box still ran a RACE: it fetched the whole folder corpus from `/api/index/search?fmt=columns` and, if that had not produced within `INDEX_RACE_MS` (150 ms), started a live `walkDirStream` alongside it, first to produce taking the entire answer (`listing/source-race.ts`). Two things ended that. The payload is the size of the folder where a ranked answer is a few KB — the same 19.8 MB / 164k-row measurement D338 was built on, since it is literally the same route. And the reason the index cannot answer a folder is a **fact only the server holds**: the mount policy is `MountGuard`'s (the same object `runner.start` refuses with), the ignore list is the scan config's, and "a package is one opaque row" is a shape of the store — so racing a second source against it was guessing at a question that has an answer. `GET /api/index/rank` therefore grew a **`reason`** (`""` | `mount` | `package` | `ignored` | `uncovered` | `scanning`) and the client switches on it in one pure module (`listing/index-source.ts`): answer, ask for a scan, poll, or walk. **`ignored` is not in the original plan and is load-bearing**: without it, searching a `node_modules` would ask for a scan of it on every keystroke — spawning a worker that indexes nothing and rewrites the whole store to say so — and then report "no matches" for a folder the walk used to search fine. **The client's whole rule is "walk when the server says it cannot answer AND cannot be made to"**, which covers the fourth case too: a folder still uncovered after its own scan (another filesystem, a scan that failed) falls back rather than looping or blaming the user's files. **Uncovered folders are scanned on demand** through `POST /api/index/scan-folder`, which is `runner.start` plus the startup scheduler's own `SCAN_DEBOUNCE_S` floor — never a second scheduler — and which **never errors**: a search box that could read a refusal as transient would retry it at keystroke rate, so `refused`/`debounced` are durable answers that hand the folder to the walk. The FOLDER is the scan root, not an enclosing configured one, because compaction keeps every row outside the root it is given. While a run covering the open folder is live the box re-asks every 1.5 s and repaints; the ordering shifts as rows land, which is accepted, and the list never goes empty between repaints. **Honest limit, stated rather than implied:** rows do NOT trickle in partition by partition — a run writes its output in ONE compaction at the end (`index-store.md §4`), so within a single scan the trickle is one step, and what the poll actually buys is a correct repaint the moment that step happens instead of a stale answer until the next keystroke. | Owner call 2026-08-18. **The freshness gate is replaced rather than kept.** `indexMayAnswer` pinned a folder this app had renamed into — plus its ancestors and its descendants — to the live walk for the whole session, because the index would win any race and answer with the pre-rename name. With the walk reserved for uncoverable folders, the honest fix is to make the index right: every `/api/fs` mutation now reports the folder it touched (`server/index_touch.py`) and that folder is rescanned — coalesced over a burst, outermost folder only, never a mount and never `/`, and **deferred while a scan already covering it runs rather than joining it**, since a live run may have walked past that folder already and joining it would report success and fix nothing. Row-level patching of the store was rejected outright: it would be a second implementation of what a scan does, over a format whose only writer is the compaction. **What that rescan is allowed to cost, and where it may point, is bounded — review found all three unbounded and they are the sharpest corrections in this phase.** A scan ends in a COMPACTION, which re-sorts and rewrites every partition plus `dirs.parquet` (keeping the rows outside the root is a query predicate, not an incremental write), so the cost is a function of the whole index and not of the folder: with the markdown editor autosaving every 2 s, reporting every write rewrote a 571k-row store for as long as somebody was typing a note. So only a mutation that can change the NAME SET reports (a write reports a create, not an overwrite — the index stores names, and the size and mtime beside them are not what search ranks on), and a 20 s per-folder floor DEFERS anything else that repeats. Deferring rather than dropping is the whole difference from the scheduler's 15-minute `SCAN_DEBOUNCE_S`: a refused rescan leaves a renamed file unfindable, which is the failure this mechanism exists to prevent, and the existing 120 s deadline is the escape so nothing is held for ever. The queue also asks the ignore list (a save inside `node_modules` would otherwise spawn a worker that indexes nothing and rewrites the store to say so — the same waste the `ignored` reason exists to avoid) and refuses a root on a **different filesystem than home**. That last one is new policy rather than a bug fix and is stated as such: before this phase every scan root came from the configured roots, `MountGuard` knows only fused-render's OWN mounts dir, and `scan_dir_once`'s `root_dev` guard is defeated by construction when the root IS a user's SMB/NFS volume. The live walk did crawl such paths, but it was abortable, entry-capped and tied to an open search box where a detached scan is none of those — and a kernel walk has permanently wedged a mount in this repo more than once. A refused folder falls back to the live walk exactly as it did before phase 2. What survives on the client is one bit — `indexRescanPending` — which the chip renders as "indexing…" until a scan completes, closing the window before a status poll can see the run. **Perceived performance is again the acceptance criterion, and the two boxes are now literally the same code** (`platform/lib/instant-search.ts`): leading-edge fire with a trailing 120 ms coalesce, abort never queue, a 20-query session memo (bypassed while a scan is landing rows, since a remembered mid-scan answer is exactly the stale one), a 200 ms threshold before any spinner, and **never a blank list**. That last one is where this box deliberately departs from `lib/search-hold`'s rule that rows are only ever rendered under the query they were computed for: with a local corpus that rule was free, and with a round trip per keystroke it is the difference between "instant" and a list that flashes empty per character — so the ranked rows stay on screen, dimmed and captioned, exactly as the home page's do. The walk keeps the query-tagged hold, the corpus hold and the re-rank throttle unchanged, because re-ranking a corpus in hand really is free. **Deleted as unreachable:** `listing/source-race.ts`, `listing/index-corpus.ts`, the `indexSearch` client and `INDEX_RACE_MS` — the `/api/index/search` ROUTE stays regardless, as the `fused.fileIndex.search` bridge contract user pages are written against. `tests/fixtures/rank-parity.json` **keeps its full scope** and gains a second justification: the JS ranker now answers only mount-backed and uncoverable folders, so "a mount folder and a local folder must rank alike" is the original reason — but the stronger one is that ranking (Python) and HIGHLIGHTING (`fuzzy.ts`, re-run in the browser over the returned rows) are now split across the two languages for the SAME rows, so a divergence would land highlights on the wrong characters of a hit the other language ranked first. |
| D348 | **The `_nofocus=1` focus contract (D-lineage of the pane's autofocus fix) now covers every DISPLAY-ONLY thumbnail too — /apps cards, Home/bookmark card peeks, and the nested renders a peeked embed shell builds — and the module that states it moves to `@platform/lib/frame-focus.ts`** | The bug read as an iframe rendering glitch: scrolling the /apps grid jumped to the very end. The mechanism is the pane's old bug wearing a different symptom. Focusing an element inside a same-origin frame ALSO scrolls that frame into view, and the scroll propagates out through the embedder to its own scroll container — so a card whose live thumbnail mounted mid-scroll and whose app focuses an input on boot yanked `.apps-page` down to that card's row. Measured in Chrome over CDP before the fix: `scrollTop` 1200 → 2423.5 out of a 2466 maximum, at the instant `document.activeElement` became the tutorial app's thumbnail iframe. The pane already had the whole answer (`_nofocus=1`, honoured by runtime.js: strip `autofocus`, no-op `HTMLElement.prototype.focus` until a real pointer gesture) and the thumbnails simply never asked for it — they carried `_preview=1` alone. So this is a COVERAGE decision, not a new mechanism: the same stamp, on every frame the shell mounts as a picture rather than as a use. Three places build such a URL and all three stamp now — the card's `thumbSrc` helper (both branches, hover-live and no-png), `LivePreview`, and the embed shell's forward, which pairs `_nofocus` with `_preview` *on one condition* so it INHERITS through nested frames via `ancestorIsPreview` (a peeked page that itself embeds an app stamps the inner render too — the gap `withNoFocus` on the outer URL alone would leave). Pairing them is the honest expression of the rule: a thumbnail is display-only in both registers — it records no open AND takes no keyboard. No shell-side reclaim guard for cards (the pane keeps its own): the runtime half was verified sufficient, including the case the runtime's own comment warns about — a queued `autofocus` candidate cannot be dequeued by stripping the attribute — because the focusin bounce lands before the layout scroll matters. Measured both ways with the same probe app (autofocus + `focus()` on load): bare frame 600 → 2465.5, stamped frame 600 → 600 with focus left on `<body>`. The module moves from `@apps/explorer/listing/` to `@platform/lib/` because two apps now depend on it and `check-boundaries.mjs` forbids app→app imports; it stays router-free and DOM-free either way. |
| D349 | **The MLX Whisper format check recognises the THIRD weight spelling — `model.safetensors` beside whisper's native config — and the worker bridges the released library to it with one symlink; `mlx-community/whisper-tiny.en-8bit` joins the suggestions and, by the D-rule, becomes the bare-call default on Apple Silicon** | mlx-community publishes three layouts of the same format: `weights.npz` (most conversions), `weights.safetensors` (the large-v3-turbo era), and — on the newer quantized re-uploads like whisper-tiny.en-8bit — `model.safetensors`, the filename every transformers repo also carries. `formats.MLX_WHISPER_WEIGHTS` knew only the first two, so a repo the library itself reads FIRST (`load_models` looks for `model.safetensors` before either) was refused with "has no weights.npz". The filename alone cannot be the test — it is Parakeet's and every text checkpoint's too — so `is_mlx_whisper_snapshot` requires the whisper-native config beside it (2+ of `n_mels`/`n_audio_ctx`/`n_vocab`, OpenAI's own `ModelDimensions` fields, which transformers spells `num_mel_bins`/`d_model` and NeMo doesn't spell at all). The match is DECISIVE and now early-returns out of `loaders()` like Parakeet's — which also fixes a pre-existing leak: the quantized re-uploads carry an MLX `quantization` block and `.safetensors` counts as torch weights, so the text branch offered `mlx-text` on a speech model (the `weights.safetensors` era had the same leak all along). One trap remains after the format check passes: the RELEASED `mlx-whisper` (0.4.3, and the whole 0.4.x line this runner pins) reads only `weights.safetensors`→`weights.npz` — `model.safetensors` support exists on mlx-examples main, unreleased — and dies inside `mx.load` with "[load_npz] Input must be a zip file…". The worker therefore leaves one relative symlink `weights.safetensors → model.safetensors` in the snapshot before priming; drop it when the pin moves past a release that reads the shared name itself. tiny.en-8bit (0.05GB) leads the suggestion list and so becomes what a bare `fused.ai.transcribe()` loads on a Mac — English-only, which is the D-rule's smallest-first trade applied as chosen (2026-08-16), not an oversight. |
| D350 | **A Claude session in a canvas clone publishes with the STANDARD `fused workbench canvas push`, made safe by intercepting it in the bundled CLI's Python entry point and running the watcher's own `_push` server-side; the watcher holds its debounced auto-push while a session is live but stays the backstop; and the embedded workbench is locked from a PID-based signal, released only after the work has landed and the workbench has had time to re-hydrate** | Three places disagreed about whether the agent may push (`agent.py`'s D334 pre-allowance said "the point is push directly", the seeded CLAUDE.md called a manual push "usually redundant", `_fix_prompt` forbade it outright) and the permissive readings were **advertising a footgun**. A raw `canvas push` from inside a clone skips the probe+merge+abort guard at the top of `_push`, which is the only thing between `canvas push`'s wholesale REPLACE and a concurrent workbench edit — and that clobber is **unrecoverable**: `.sync/trash` only ever protects LOCAL files, and the watcher's next probe sees remote == local so the merge no-ops. It also moves the remote behind the watcher's back, where a clean clone cannot tell it from a workbench edit and takes the force-pull branch: the agent's own push comes back down, every unignored file the push did not publish is deleted, a phantom "pulled from workbench" appears, and (before this) no post-pull recheck re-armed the dirty flag, so a file written DURING the force pull was overwritten **and adopted as clean**. Nothing in the suite simulated an out-of-band push, which is why none of it was caught; there is now a regression test that pins the hazard itself. **Interception lives in `fused_render/_fused_cli.py`, not in the generated sh/cmd wrapper**, because the packaged app bakes the pre-release fused into its own interpreter, has no console scripts, and `fusedcli.fused_cli()` spawns `[sys.executable, _fused_cli.py]` — so that one file is the path 100% of shipping pushes take, and it has a real argv to match against. The matcher is deliberately conservative (unknown flag, `--id`, `--help`, wrong positional count → fall through), and falling through is always SAFE because every fall-through case — no `FUSED_RENDER_ORIGIN`, target outside the canvases root, no watcher, unreachable server — is a folder with no merge base to protect; `--no-validate`/`--no-ignore` are the one exception and are REFUSED, since falling through there would run the very unguarded push this removes. **The same file being both the server's push mechanism and the interception point is a reentrancy trap that shipped and broke sync outright** (`_push` POSTed back to its own endpoint, was refused because a push was already running — itself — and recorded that refusal as a CLI failure: `push_state "error"` with `push_seq` stuck at 0, plus a Fix-with-Claude button offering to fix nothing). The fix is an env marker set in `_cli_env`, the one chokepoint for every fused child canvases.py spawns, and checked first in `maybe_intercept`: an env var survives exactly one process hop, so it means "launched BY the sync manager" and a session that merely runs `fused` inherits the server's environment, which never carries it. Concurrency refusals are now shaped as `code: "busy"` and touch no manager state — a benign race must never reach `push_state == "error"`, which is what `/api/canvases/fix` gates on. **The lock signal is `agent_active`, from a live PID (`_live_run`), never transcript activity**: a "no recent activity" read has a grace window that is fine for a badge and wrong for a lock, since a slow tool call mid-edit reads as "finished" and would unlock the workbench under a session still writing — the same argument `active_fix_run_id` was written from, and that field is not reusable here because it only knows about fix sessions the server spawned, not a chat the user started. `_live_run` gained an unbounded form for this: its 60-dir cap is right for a page re-attaching to its own run and wrong for a lock, because nothing prunes `RUNS` and a missed live run means silently not locking. **Release requires no live run AND nothing pending/pushing AND a grace window longer than the embedded workbench's ~10s upstream poll**, because on unlock the workbench flushes its dirty in-memory state and last-writer-wins would overwrite a push still in flight — releasing early reproduces the D339 incident with our own unlock. A FAILED push releases anyway (no success is coming, and holding until a human fixes a validation error is a lock with no end condition), and a dropped watcher releases from the page's own polled state so a restart cannot strand a read-only pane. Enforcement itself lives in the **deployed workbench**, which does not support it yet, so the message is behind a capability ack and the un-acked fallback is a scrim plus a banner stating plainly that this workbench cannot be locked — an overlay stops clicks but not the workbench's own autosave timers, and the UI must never imply protection it lacks. Two related corrections ride along: the clone's CLAUDE.md named `fused:*` skills and told the USER to run `fused claude plugin add` (renamed to `workbench`, and that subcommand no longer exists at all), so the app now discovers the workbench plugin and hands it over as a second repeatable `--plugin-dir` — per-session, additive, no mutation of the user's global Claude config — and DEGRADES to the folder's own conventions when it finds none, never instructing a reader who cannot act; and the "in terminal" hand-off now prepends the wrapper dir to PATH, because a shipping user has no fused of their own and `fused` in their shell is `command not found`, not merely a wrong version. Rejected: pausing the watcher for the session's whole duration (keeps the merge-guard bypass, widens the baseline-adopt blind window, and adds a "watcher silently dead" mode with no watchdog, for the same cost); a wrapper-script/curl affordance (needs a blanket `Bash(curl:*)` that widens to the whole network, and teaches a command that is not the one the model already knows); and installing or symlinking the bundled fused into `~/.local/bin` or the user's shell profile, which would win globally but mutates their machine outside the app's storage without consent. |
| D351 | **D350's "falling through is always safe" claim was too broad: for a positively-identified canvas clone, `_canvas_push.py` now REFUSES (instead of falling through to the raw CLI) on a `no_watcher` 409 and on a request timeout, and `_post_push` distinguishes a genuine connect failure from a timeout-after-connect** | Code review on D350 found two of its four "safe" fall-through cases were not actually inert once the target is a clone. **Timeout**: `_HTTP_TIMEOUT_S` is 200s to outlast the endpoint's own `PUSH_TIMEOUT`, but a `socket.timeout` (a `TimeoutError` since Python 3.10, itself an `OSError` subclass) was caught by the same broad `except (urllib.error.URLError, OSError, ValueError)` as a connect failure, so a slow-but-still-running server push read as "unreachable" and fell through to the raw, unguarded CLI push — running it CONCURRENTLY with the guarded push that was merely slow. `_post_push` now catches `TimeoutError` ahead of the general `OSError` branch and returns a distinct `_TIMED_OUT` sentinel; `maybe_intercept` refuses on it rather than falling through, because on localhost a bare `connect()` either succeeds almost instantly or fails immediately with a distinct (non-timeout) `OSError` — a `socket.timeout` surfacing after the full window means the connection was made and the server-side push may still be in flight. **`no_watcher`**: "no live manager for this folder right now" was read as "sync was never engaged here, so there's no merge base to protect" — true for a canvas that has never been opened in fused-render, but false the moment a `.sync/<name>.json` exists from a prior session (the manager reloads it in `_load_base()` on next construction) or the remote has moved through the hosted workbench while nothing watched. Since D350's terminal hand-off makes this endpoint the standing recommendation for a session's every push, `no_watcher` is now common enough (server restart before a workspace tab reopens the canvas; sync explicitly stopped) that treating it as inert stopped being a safe bet. `_canvas_push.py` now refuses on this code too, pointing the session at opening the canvas in fused-render (which starts the real watcher) rather than running the raw push. The other two fall-through cases are unchanged and still inert: no `FUSED_RENDER_ORIGIN` and a target outside the canvases root both mean sync was never engaged for that folder at all, not merely paused. |
| D352 | **The canvas workbench lock's trigger changes from "a Claude session is live" to "a sync operation is actually moving the clone's files" — locking only while a push or a pull/merge is in flight, never for the length of an edit or a chat** | Owner correction from live use: a plain "hi" with no edits locked the workbench, because `agent_active` (a live PID, D350) drove the lock directly. That was backwards — Claude may make many changes over a long chat, and the user must not be blocked from the workbench for the whole length of it, only for the seconds a sync operation is physically writing the clone's files. `agent_active` keeps being reported (informational — a badge or copy elsewhere may still want it) but `canvas-lock-lib.ts`'s `decideLock` no longer reads it at all; the two triggers are `push_state` (`pending`/`pushing`, unchanged from D350) and a NEW `pulling` boolean on `_SyncManager`, true for the duration of `_poll_remote`'s force-pull/merge leg or the legacy `_pull_if_remote_changed` leg (both already serialized under `_op_lock`), surfaced in `status()`. **Both triggers now START a lock unconditionally** — D350's "only editing extends, publishing/settling only extend" precondition is dropped for these two, because a sync op in flight is never safe to ignore even on a page that just loaded (the old precondition existed only to keep a merely-live session from locking a quiet canvas, a problem that no longer exists once liveness isn't a trigger at all). **The grace window after release is push-only.** A push moves the remote out from under the embedded workbench's own ~10s upstream poll, so — exactly as D350 reasoned — unlocking the instant push_state goes idle risks the workbench flushing stale in-memory state over a push not yet visible upstream (the D339 shape, reproduced by our own unlock). A pull/merge only writes LOCAL files from a manifest already fetched; it never moves the remote, so there is nothing upstream for the workbench's poll to race, and it releases immediately, no grace window. `decideLock` tracks this with a `prevHold` parameter (the hold IT returned last call) rather than a plain "was engaged" flag: the window arms only when `prevHold` was `"publishing"` or `"settling"` itself, so a pull ending with `prevHold === "pulling"` releases straight to null. **Accepted trade-off, recorded rather than hidden:** a user editing in the workbench WHILE Claude is mid-edit, between sync operations, is no longer prevented by this lock at all — D338's per-file three-way merge is what absorbs that now, and a same-file conflict may surface (local-wins-since-base semantics) where the lock used to simply forbid the collision. The owner accepted this explicitly: blocking the user for the length of a chat was the worse failure mode. Finding 6 (a repeatedly failing status poll stranding a lock on, since `sync` never updates on a caught failure) is folded into the same fix: the frontend poll now treats `FAILED_POLLS_BEFORE_RELEASE` (3) consecutive misses as a dropped watcher (`sync.watching = false`), which `decideLock`'s existing `!status.watching` branch already releases unconditionally. |
| D353 | **Enforcement of the canvas workbench lock is delegated entirely to the workbench: fused-render blocks pointer events only as a fallback for a workbench that never acks `fused-embed-lock`, and even that fallback is a light translucent scrim, never the opaque blackout it shipped as** | Live use found the lock unusable: the scrim was a near-black 45%-opacity div covering the whole workbench pane, and — because it sat above the iframe — it ate every pointer/wheel event, so the canvas could not even be panned or zoomed while locked, regardless of whether the workbench itself would have allowed navigation. The fix separates "informing" from "enforcing." The workbench's own `fused-embed-lock-ack` reply is authoritative: once THIS lock engagement is acked, fused-render renders no blocking layer at all, only the existing banner (now explicitly `pointer-events: none`, though it never overlapped the iframe to begin with) — the iframe receives every event, and the workbench enforces read-only and permits its own pan/zoom on its own. Only the unacked case (an old or production workbench that ignores the message, or one that simply hasn't answered yet) falls back to a blocking scrim, and that scrim is now `rgba(20,20,25,0.14)` — enough to read as "something is happening" without hiding the canvas, matching what the banner already says about it being a courtesy, not a guarantee. **Ack tracking resets per engagement, not once per page** (`canvas-lock-lib.ts`'s `nextAckState`, a pure 3-state reducer: `"waiting" → "acked" | "unacked"`): a brand-new lock always starts `"waiting"` and re-runs the ack timeout, rather than inheriting a previous engagement's answer — the safe default for a brand new lock is to assume nothing yet, and an ack that arrives late still upgrades an `"unacked"` fallback straight to pass-through (a stale timeout can never downgrade an already-`"acked"`/`"unacked"` state, only a still-`"waiting"` one). |
| D354 | **`decideLock`'s "publishing" hold now engages only on `push_state === "pushing"`, not `"pending"`** | D352 fixed "locks the instant Claude is live" but left a narrower version of the same bug: the sync watcher sets `push_state = "pending"` the moment a Claude session writes its first file, and holds it there — because auto-push is deliberately HELD while a session is live (D350) — for the entire edit/chat stretch until the session runs the explicit `canvas push`. Since `decideLock` treated `"pending"` the same as `"pushing"`, the workbench still locked from Claude's first file write and stayed locked for the whole session, exactly the failure mode D352 already ruled out, just reached through the queued-not-in-flight state instead of `agent_active`. `"pending"` records queued, held local edits with no sync operation actually touching the clone's files — the D339 clobber risk (unlocking while a push is mid-flight, so the workbench flushes stale state over work still landing) only exists once a push is actually MOVING the remote, i.e. `"pushing"`. So `"pending"` is now treated exactly like idle for locking: no hold, no settling grace armed. `"pushing"`, `"pulling"`, and the post-publish settling grace are unchanged. The only coverage this gives up is the no-session auto-push's own ~1.5s debounce window between a file write and its automatic push starting; D338's per-file three-way merge already covers a same-file collision there, so nothing is actually lost, and the user is no longer blocked from the workbench for the length of a chat — D352's stated intent, now actually delivered. |
| D355 | **The /canvases workspace chat gets its annotation tools back over the HOSTED workbench, as point notes: the chat is framed directly beside the workbench iframe (`chat_only=1`, `data-fused-annotate-target` on the workbench — the explorer sidebar's exact contract), and the claude template grows an `annXO` branch — when the marked frame's document is CROSS-ORIGIN, a click-catching overlay in the host's own document lays over the iframe's box and every armed click is a `kind: "point"` note** | The /canvases workspace framed its chat through `/explorer/embed/<clone>?_mode=claude`: a folder target with no app entry, so `enterNoPane` removed every annotation control — and the one thing worth commenting on, the live workbench canvas next door, is cross-origin, which walls off all three of the template's mechanisms (layer injection, element anchors, `shotPane`'s body clone). Marking that iframe without this branch produced the WORST state, not a degraded one: `annCapable()` answers off the mark alone, so the switch armed a mode whose layer injection silently failed and whose clicks went nowhere. The fix takes what cross-origin does NOT wall off — the frame's BOX in the host's same-origin document — and D344's point note, which never needed more than a spot and some words. **Two host-side moves make the template's existing seam reachable**: the chat is framed DIRECTLY via `/render` (the sidebar's own construction, `chat_only=1` included) rather than through the embed shell, because `annMarkedFrame` is parent-scoped and one extra nesting level hides the mark; and the `run` handoff moves from the iframe src to the page URL, where `fused.params` actually reads it (the embed shell's URL used to be that param surface; the iframe's key-remount survives, since the template only adopts `run` at boot). **The overlay reuses every downstream piece**: it answers `annInjectLayer`'s exact `{root, pins, hl, stage}` shape through the same `layer` variable in `annSyncTarget`, so the pin painter, the composer portal (`annPortalPop` — the host's document is same-origin, that is what marked the frame) and `annPlacePop` run unchanged; typed and spoken notes commit through the same `annCommit`; the walkthrough recorder marks the same `annRecMarkPoint`. Coordinates are OVERLAY-relative with `win` null — the framed canvas's pan/scroll is unreadable, so there is no page coordinate to convert through, and `ANN_XO_SCROLL` (a zero-scroll stand-in) keeps `annPointXY` the one converter instead of growing a second. **What hides, hides honestly**: the Element/Point picker (no element can ever resolve — every click is a spot) and the hover ring (nothing to trace). **The screenshots survive, off the TAB instead of the document** (owner follow-up, same day): `shotPane` — the one rasteriser every capture path shares — branches to `shotXOPane`, which grabs a frame from a `getDisplayMedia` tab share and crops the marked iframe's rect out of it, answering shotPane's exact shape with clean doubt fields (live pixels have no style walk, no image inlining, no WebGL readback to caveat) — so the composer-row camera, the whole-pane shot and a point note's save-time crosshair crop all run unchanged, and the crop's coordinates line up with the pins because the tab crop IS the overlay's box. The share prompt is paid ONCE — the stream is cached until the user ends it, the target stops being cross-origin, or pagehide — and raised at ARM time, where the user activation actually is (a mic commit or a walkthrough click's fire-and-forget crop may have none); a declined prompt degrades every path to notes-without-pictures, which each already handles. The catcher is `display: none` while disarmed, not pointer-events, so a hidden overlay can never intercept a drag that started armed; the overlay's rect re-syncs on every render and the 750ms target poll, which is what tracks the splitter drag without an observer of its own; `pagehide` removes it beside the injected layer it stands in for. **The known limits are the feature's shape, not gaps**: a pin marks a spot on the SCREEN — panning the canvas under it moves the content, not the pin — and the note carries no element digest, so Claude gets coordinates, words and the crop. True node-level anchoring needs a postMessage seam in the hosted workbench itself (a two-repo feature, same deferred posture as D338's server CAS). **Rejected:** a local `canvas`-template pane inside the chat (built first, rejected by the owner on sight — a second canvas beside the live one, and notes that point at OUR render of the file rather than the workbench the user is actually looking at); postMessage into the workbench now (the two-repo seam above — out of scope for "simple annotations"); leaving the embed-shell nesting and teaching `annMarkedFrame` to walk ancestors (a second lookup rule forever, to save one URL construction once); rejecting `getDisplayMedia` for the pictures (the first cut did, fearing a prompt per note — caching the stream and prompting at arm time reduces it to one, and the owner wanted the pictures). |
| D356 | **The `duckdb` and `sqlite` grids reload on a change to the params their query is BUILT FROM (`_file`, `table`, `offset`, `sort`, `filters`) and no longer on any param at all.** | Both were wired `fused.params.onChange(load)`, and that listener does not fire for the pane's own params — it fires for every visible param on the HOST URL. In the single-file view the content pane and the `claude` sidebar are both plain children of the shell window with no param boundary between them (only `Panel.tsx` marks one), so they share one URL, and the sidebar keeps its annotation state in it: `annSave` writes `annotations` on every note made, edited or deleted. Every pin therefore re-ran reader.py in the grid beside it. On a CSV it did worse than re-query — the keep-the-grid-on-screen path needs a warm parquet schema cache, so a non-parquet file wiped to "Loading…" and rebuilt mid-annotation, which is what it looked like from the outside: annotating a `.csv` reloaded the table under the notes. Fixed in the grids, not in the runtime or the sidebar: `onChange` firing for every param is the documented contract (the `claude` template guards its own listener the same way, and `las` had the pattern first), a host URL shared by the panes is what makes a param survive a pane reload at all, and `annotations` cannot become a reserved `_`-param because `params.set` refuses reserved keys outright. The guard is a key of the watched values rather than a per-key subscription, so a no-op rewrite (the same `table` over itself) is free too; in `sqlite` the listener also moved BELOW the boot-time `sort`/`offset` default stamps, which used to spend a second load restating what the first one already read. tests/test_grid_param_guard.py pins both the watch list and the ordering. |
| D357 | **The macOS quit no longer returns control to C `exit()`: every quit surface ends in `app.hard_exit` (`os._exit`), and duckdb's DEFAULT connection is closed alongside the reader's stash** | INCIDENT 2026-07-29 came back on 2026-08-19 (FusedRender-2026-08-19-133416.ips) in its general shape. The log shows the ordered teardown SUCCEEDING — "quit teardown finished in 11.5s (steps: server, duckdb, unmount, rcd)" — and the process aborting 20ms later, on the main thread, in `-[NSApplication terminate:]` → `exit()` → `__cxa_finalize_ranges` → `~DuckDBPyConnection` → `PyEval_SaveThread` → `_Py_FatalErrorFunc` → `abort()`. The mechanism is the one the 2026-07-29 fix already named, applied to a handle that fix could not reach: pyobjc releases the GIL for the duration of an ObjC call, so `exit()`'s static destructors run WITHOUT it, and duckdb 1.5.5 builds its default connection EAGERLY AT IMPORT into a C++ global — so merely `import duckdb` (which the in-process server does for /api/index, parquet, search, git_repos, h3, excel and tableau) guarantees the abort, with no stash and no reader run involved. Measured on the shipped bundle interpreter with `ctypes.CDLL(None).exit(0)` called off the main thread, the exact condition AppKit creates (10 runs per row, deterministic): never import duckdb → rc 0; `import duckdb` alone → SIGABRT; open-and-close a connection → SIGABRT; `duckdb.default_connection().close()` → SIGABRT; **but** `dc = duckdb.default_connection(); dc.close()` → rc 0, and so does `dc = duckdb.default_connection()` with NO close, while adding `del dc` to either → SIGABRT; die via `os._exit` instead → rc 0. Read carefully, that table says the close is not the variable — the surviving Python REFERENCE is. `~DuckDBPyConnection` runs when the LAST reference dies, `__cxa_finalize` dropping the C++ global's reference is what runs it, and any live Python wrapper (open or closed) keeps the refcount above zero so the destructor never executes. Which means the only "fix" in that column is deliberately leaking a reference to dodge a destructor — strictly weaker than it looks, and a worse thing to own than the crash. So closing handles is whack-a-mole we cannot win — there are ~20 `duckdb.connect()` sites and the same class of global in GDAL/rasterio, pyarrow and torch — and the fix is structural: once `quit_ready` is set (or its hard deadline has fired), the process dies by `os._exit`, skipping atexit, Python finalization and `__cxa_finalize` entirely. What that gives up is either absent or unwanted: the tree registers NO atexit handler (logging's flush is the one that mattered and `hard_exit` does it by hand), Python finalization only frees memory the kernel reclaims anyway, and AppKit's own termination is precisely the step that aborts. It is safe ONLY because the quit already shuts down explicitly and in order — `quit_teardown` drains the server, closes duckdb, detaches every mount and reaps rcd, and `begin_quit` removes the pidfile on the calling thread — so `_exit` is the last statement of a completed shutdown, not a shortcut around one. Concretely: `main()`'s `_terminate` drops `rumps.quit_application()` and its `callAfter` hop (`os._exit` is thread-safe and needs no main thread), and the AppKit delegate hook (Dock Quit / ⌘Q / logout-restart) hard-exits where it used to answer `NSTerminateNow` or call `replyToApplicationShouldTerminate:` — both of which resume `exit()`. `reply` is KEPT, as the documented last resort for the single unreachable case where the process is still alive after `exit_now` returned or raised: an app AppKit never hears back from cannot be quit at all, and that property is worth two lines over a comment claiming it can't happen. The `QUIT_APPKIT_REPLY_WAIT_S` backstop is unchanged in intent — it now hard-exits rather than replying, on the same trade `QUIT_HARD_DEADLINE_S` already makes (a mount left attached beats an unquittable app). The default-connection close ships anyway, documented as hygiene rather than the fix, and the two duckdb closes are honestly unequal: the reader's stash close LATCHES (a late read cannot restash), while the default-connection close is a one-shot release with no barrier behind it — the next `duckdb.sql(...)` simply builds a fresh one, and duckdb's module API is not ours to mediate the way the stash is. It also CONSTRUCTS a connection when none is live (8.7ms measured, versus 0.03ms to return the existing one) because duckdb exposes no liveness probe — `_clean_default_connection` is a PyCapsule, not a callable — which costs nothing on 1.5.5, where the import has always already made one. **A second ordering consequence of dying by `os._exit` had to be repaired in the same branch:** `begin_relaunch` used to get "the spawn precedes the death" for free, because death was `AppHelper.callAfter(rumps.quit_application)` — queued onto the main run loop, so it could not run until `application_openURLs_` had returned. `os._exit` off the watchdog thread has no such hop, and an instance with nothing mounted can finish the whole teardown while the main thread is still inside `Popen` (a fork+exec of a large process under `start_new_session`): the app quits and nothing comes back. `begin_quit` therefore takes an `on_claim` hook that runs inside the claim, before the teardown is started, and the relaunch spawn hangs off that instead of off statement order. Same class of reasoning applies to `hard_exit`'s log flush: `logging.shutdown()` acquires every handler lock, and the wedged-teardown scenario the backstop exists for is exactly the one where another thread may hold the RotatingFileHandler lock mid-rollover, so the flush runs on a daemon thread joined for `QUIT_LOG_FLUSH_S` (0.5s) and the process exits regardless — a try/except catches raises, not hangs. |
| D358 | **Workbench canvases is PRIMARY NAV once this machine is signed in to Fused — a row under Tasks (and a rail icon under it collapsed) — while the settings menu keeps its entry, now spelled "Workbench canvases" in both places; the signed-in fact is a shared store the page publishes into, never a one-shot probe** | The sub-app was reachable only through the Settings popover, two clicks behind a trigger whose other entries are Templates/Mounts/Preferences — i.e. filed as machine setup, which is what it IS while you are signed out (the row would lead to a sign-in wall) and exactly what it is NOT once there is an account behind it: a canvas is then one of the two or three places this machine is FOR, alongside Home and Tasks. So the gate is `logged_in` ALONE, not `cli_found`: a missing CLI is a state the page itself explains, and hiding the concept for it would repeat the mistake the menu entry's own "ungated like Tasks" note already rejected. **The menu entry stays even while the row shows** — the menu is where someone looks for a destination by name, and dropping it would mean the renamed item the rename was asked for is missing in exactly the signed-in case people test — so the double-selection the Tasks note forbids (a lit row AND a lit Preferences trigger over one destination) is resolved in `prefsActive` instead, which excludes `/canvases` while the primary row is up. `active` is EXACT `pathname === "/canvases"`, on Home's reasoning: `/canvases/<name>` is a workspace you opened, not the list page. **The probe cannot be `claude_config/available.ts`'s cached single fetch**: availability there is a property of the server's installation and cannot flip, whereas signing in and out is a thing people do mid-session, and a cached "no" would hide the row for the rest of it. It is `shell/tasksPulse`'s shape instead — one shared store, subscribers get the last answer so the row never blinks across the sidebar's per-navigation remount, a slow 60s self-poll only to catch a login that happened elsewhere (another window, `fused login` in a terminal), and the Canvases PAGE publishes every status it learns, including its 1.5s login-in-flight poll, so the row appears the instant the browser sign-in lands rather than up to a minute later. A failed poll is not a sign-out (the row survives a server restart), and a departed poll losing to a fresher publish drops its answer. **A 401 IS REMEMBERED AS A STAMP, not applied and forgotten** (bugbot, 2026-08-19): `/api/canvases/status` answers `logged_in` from the credentials FILE EXISTING and nothing more, so a present-but-unrefreshable store — the case the page already handles by 401ing a guarded call and showing the sign-in flow — reads as signed in there forever, and the store's own poll wrote that back a minute later, returning the row over a sign-in wall. The page therefore publishes the whole status rather than the boolean, and a refusal records WHICH store was refused (`creds_stamp`), which the poll then discounts. A stamp and not a flag because a re-login over a stale-but-present store never flips `logged_in` — the mtime changing is the entire signal, the same one the page's own login poll watches — so a new stamp means someone signed in again and is taken at face value. It lives in `apps/canvases/logged-in.ts`, imported directly and not through the barrel, for `available.ts`'s bundling reason: `index.ts` re-exports Canvases/CanvasWorkspace, so probing through it would drag the whole sub-app into the shell's main bundle for one boolean. |

### The per-file sidecar is deleted outright (2026-08-18)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D359 | **The per-file sidecar (`home_dir()/sidecar/<mapped path>.json`, D83/D205) is DELETED OUTRIGHT — the mapping trio (`shell/storage.py sidecar_path`, `shared/appenv.py`'s mirror, `runtime.js`'s `_fusedSidecarPath`/`_fusedTargetPathFromSidecarPath` + `/api/config`'s `sidecar_root`), the `fs_mutate` mkdir carve-out, and every feature the file backed: claude's `claudeSessions` list entries and `revertStash`, annotate's `comments` log + tombstones + deep-link hydration (D100/D101), bookmark history (`bookmarkHistory`, D83/SB-7, endpoint and frontend caller included), slides' deck-title key, docs' versions/author/title metadata, excel's saved scripts, latex's last-open memory, usd's persisted viewer settings and `sidecar_writable` probes** | Owner call 2026-08-18, after a full audit of what the file drove: *abandon all of it* — no migration, no replacement store, the file must not exist. What each surface becomes: the claude session list is now the disk scan alone (`_cli_sessions` over the cwd's `~/.claude/projects` dir — every transcript, whether started in the page or a terminal, so the list survives with mtime-based ordering and 80-char head previews; the `source`/"cli" tag died with the second source, and `_migrate_session` with the recorded `cwd` it replayed); revert (both claude's snapshot panel and annotate's, SPEC §34) keeps its plan/confirm/apply chain but loses the pre-revert stash — `unique_current` now always reads as "destroys the only copy", and the confirm copy says so plainly instead of branching on a stash predicate; annotate's comments live in the URL and nowhere else (the byte-budget eviction note about recoverability-through-history is deleted with the log, and the `comment` deep link focuses only ids present in the URL); bookmark history is simply gone (SB-7 retired; delete-never-touches-history is vacuous with no history); slides/docs/excel/latex/usd fall back to their in-page defaults every load. Structural wins that fall out: `writeFile` loses its one mkdir-p exception (the general "no mkdir -p on arbitrary paths" rule is now unconditional), `/api/config` sheds a key, and the RO-3/RO-6 sidecar-writability probes (`status` actions, `_sidecar_writable` in three templates) disappear rather than being kept accurate. **No data cleanup**: existing `~/.fused-render/sidecar/` trees on disk are never read, written, or purged — the same inert-residue posture D329 took for `lastSession`, now applied to the whole tree. **Rejected: migrating any feature to a new store** (git-tracked, sqlite, per-feature files) — offered in the audit, declined by the owner per feature; **rejected: keeping the mapping helpers for future use** — a path mapper with no callers is exactly the kind of dead contract `viewstate.ts`'s rule argues against; **rejected: purging users' sidecar trees** — an eager rewrite of files we would otherwise never open, to no functional end (D329's reasoning, verbatim). **D337's workspace migration loses its (b) half with the store**: `_move_sidecars`/`_rename_sidecar`/`_sidecar_root` and their five tests go, and with them the last caller of `storage._sidecar_subpath` — the migration now moves the folder and rewrites the app's own state, two things instead of three. Leaving the rename in was rejected on the same no-cleanup rule above: re-homing a tree nothing reads is work in service of nothing, and the code could not have run anyway once the mapper it called was deleted. |

### Canvas skills come from an app-owned clone; the pull leg and the workbench lock (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D360 | **The workbench canvas/UDF skills come from a runtime shallow clone of the PUBLIC `fusedio/skills` repo that the app owns under `home_dir()`, and are handed ONLY to a canvas-clone session; discovery of a `workbench` plugin installed on the machine is deleted outright** | D350's last correction had the app *discover* the plugin — scanning Claude Code's own `~/.claude/plugins/{marketplaces,cache}/<market>/workbench` — and hand the winner to EVERY session it spawns as a second `--plugin-dir`. Both halves were wrong. **Discovery reads private storage by directory layout, so its only signal is shape, and shape cannot tell installed from UNINSTALLED**: the uninstall leaves the cache dir standing, tombstoned with an `.orphaned_at` marker the scan knew nothing about, so the app resurrected a plugin the user had deliberately removed and loaded it into their sessions (verified live on the owner's machine, which is how this surfaced). No amount of extra marker-sniffing fixes the class of the bug — the layout is Claude Code's business and may change without notice, and a "did the user want this?" question has no honest answer in a directory tree. **The second half is scope**: the workbench skills are named by exactly one file, the seeded clone CLAUDE.md, and are meaningless to a file chat, an app-folder chat or a plain-folder chat; discovery framed the root as a fact about the MACHINE, which is what made handing it to everything look natural. So the root is now app-owned — `git clone --depth 1 https://github.com/fusedio/skills.git` into `home_dir()/workbench-skills`, HTTPS never SSH (a shipping user has no key and no reason to have one), the plugin root being `<clone>/workbench`, whose committed `.claude-plugin/plugin.json` keeps the name `workbench` because that name IS the `workbench:canvas-toml` skill prefix the CLAUDE.md already writes; renaming it would silently rename every skill the seeded file names. Its presence now means *we* put it there and its lifetime is ours. **Where the fetch runs is a correctness constraint, not a preference**: `server/app.py`'s `export_app_env` runs PRE-BIND, and a git clone on a slow network there is a server the desktop supervisor kills at 20s and reports as a failed start — the same trap `claude --help` fell into on that line once already. So startup stays filesystem-only (publish a clone that already validated, "nothing there yet" being normal) and the fetch/refresh lives on `POST /api/canvases/clone`, the one path that is about to use the skills: users who never open a canvas pay nothing, a skills release lands without a server restart, and the existing try/except there keeps a clone succeeding when the fetch does not. Everything is bounded and best-effort — explicit subprocess timeouts, `git` absent is a normal "no root", and a first clone goes into a staging dir validated with `_is_loadable(root, WORKBENCH_SKILLS)` before the swap, so a failure never publishes a half tree and never disturbs a clone that already worked. **Two rate-limit intervals, not one, and the distinction is the difference between a working new-user path and a broken one**: the stamp is written per ATTEMPT (so an offline machine costs one attempt per interval, not one per canvas clone), but skipping a REFRESH is free — a stale-but-complete plugin is a working plugin — while skipping a RETRY leaves no root at all, and a session then gets no `--plugin-dir` while the CLAUDE.md seeded beside it names `workbench:canvas-toml`. Sharing one 6h interval (as this first shipped, caught in review) put "first canvas ever, one transient network failure" six hours from working. So `WORKBENCH_REFRESH_S` is 6h when a loadable root exists and `WORKBENCH_RETRY_S` is 60s when none does. For the same reason, refresh-vs-clone branches on `workbench_plugin_root()` and NOT on the presence of a `.git` directory: a `.git` FILE is a normal repo (worktree, submodule), and the clone path's `rmtree` is only sound under "nothing loadable is there" — the directory test would have deleted a working tree. The refresh is `fetch --depth 1 origin HEAD` + `reset --hard FETCH_HEAD`: the branch name is the repo's business, and the hard reset is what makes a clone that went dirty or diverged self-heal instead of staying stuck. `close_fds=False`, an absolute `git`, `-C <dir>` and never `cwd=` — the app_git.py discipline, because a fork in this process runs PROJ's atfork handler into a SIGSEGV before exec. **The gate lives in `templates/claude/agent.py:_plugin_argv`**, which now takes the target path and adds the workbench root only when the target is inside the canvases root — abspath (callers do not all normalize; `_terminal_command` does not), realpath (macOS `/tmp` vs `/private/tmp`), normcase (Windows paths differing only in case are the same path, as `apps.py`'s containment check already knew) and commonpath rather than a string prefix (`<root>-evil` would otherwise qualify). Every one of those guards the same silent failure: the gate's default answer is "no extra skills", so a resolution mismatch withholds them from a real clone and says nothing; the root is reached through a new `appenv.canvases_root()` fed by a new `FUSED_RENDER_CANVASES_DIR` export, since SPEC PY-15 forbids the template importing `canvases.py` and a re-derived rule would drift the moment either side moved — exporting the server's already-resolved answer is what keeps the duplicate honest. The template's FALLBACK is therefore the server's own hardcoded `~/.fused-render/canvases`, pointedly not `home_dir()/canvases` as every other helper in appenv would suggest: canvas clones are the one thing the app does not nest per branch, so resolving through `home_dir()` answered `<home>/branches/<ref>/canvases` on a branch build and disabled the gate there (also caught in review). A test pins all three copies — appenv, `canvases.py`, `_canvas_push.py` — against each other in both branches. Rejected: `claude plugin install` / a marketplace add, which would work and would load the workbench skills into EVERY `claude` on the machine, including the user's unrelated sessions — precisely the leak the gate exists to prevent, plus a mutation of their config we have no consent for. Kept: `FUSED_RENDER_WORKBENCH_PLUGIN` as an explicit dev override pointing straight at a plugin root, which bypasses all fetching (that tree is the developer's, not ours to reset), and `WORKBENCH_SKILLS` + `_is_loadable`, which remain the evidence that a root is the real plugin rather than a same-named stub — a root that loads cleanly and teaches the model nothing is the silent failure this whole mechanism exists to remove. |
| D361 | **The sync watcher's periodic remote-poll leg is HELD for as long as a Claude run is live in the clone (first-look baseline excepted), and `_pulling` marks only the window in which a leg is actually writing** | D354 got the push half of the lock right and left the flicker in the other leg. Two separate defects fed it. **One: the leg ran through the whole session.** D350/D354 deliberately kept it that way, with the comment above it saying so ("the remote-poll leg below must keep running while a session works, or workbench edits would stop arriving for the whole length of a chat") — and that reasoning is now superseded, in both of its claims. Files moving under a session mid-change-set is the exact hazard the seeded CLAUDE.md warns about and the workbench lock exists to prevent, so the leg was fighting the rest of the design; and every poll that *did* pull was a `pulling` window the frontend engaged the lock on, so a chat of any length flickered the embedded workbench read-only every `PULL_POLL_S`. The hold reuses the auto-push hold's signal and shape (`agent_run_id()`, i.e. a live PID — D350's reasoning about transcript activity being a badge signal and not a lock signal is unchanged) and keeps its backstop property: `_last_pull_poll` is deliberately NOT stamped on a skip, so the leg fires on the very next 1s tick after the session ends instead of up to `PULL_POLL_S` later, and `_remote` is NOT rotated while held, or a remote move made during the hold would be forgotten and never applied at all. **The first look is exempt** (`_poll_remote`'s `self._remote is None` branch): it writes nothing to the clone, it only adopts `_remote` and hashes the disk as the merge base, and gating it would leave `_base_files` None for the entire session — after which every merge silently degrades to local-wins wholesale, discarding workbench edits to files Claude never touched. The exemption is expressed as `_baseline_pending()`, which also checks the manifest shim: under an external CLI the legacy leg never sets `_remote` at all, so a bare "`_remote is None`" would read as "harmless first look" forever and leave that leg's destructive force-pull ungated. **Two: `_pulling` wrapped probe-and-decide**, so a poll of an unchanged remote — the overwhelmingly common case — still registered a full lock engagement that the 2s status poll could sample: a read-only flicker for a pull that never happened. It now moves inside the legs, past `_remote_moved`, the stale-echo check and the dirty/clean branch decision, via a `_pull_writes()` contextmanager (the clean and legacy branches' writing halves split out as `_force_pull`/`_apply_legacy_pull` so the window wraps exactly the writes), with `finally` on every path including the merge's validation-failure rollback — a `pulling` that never clears is a permanently read-only pane. **Accepted cost, recorded rather than hidden:** a workbench edit made while a session is live no longer arrives mid-chat. It arrives at the next pull, which is also step 1 of the push, where D338's per-file three-way merge folds it in with local winning ties — so a same-file collision surfaces at PUSH time instead of at edit time. Nothing is lost; the timing moved, and it moved toward the model the rest of the design already assumes. The seeded CLAUDE.md's "files change under you ... every few seconds" section is rewritten to match, since it documented the behaviour this removes. |
| D362 | **The workbench lock's release grace window is armed by ANY hold ending, not only a push's, and a re-engagement inside that window no longer re-arms the enforcement ack** | D352 made the window push-only on the (correct) ground that only a push moves the remote out from under the embedded workbench's ~10s upstream poll, so only a push can reproduce the D339 clobber through our own unlock. What that missed is that the window also does a second job: it is the only thing that makes consecutive operations ONE lock engagement. A real publish is pull → resolve → validate → push, and a chat produces several of them back to back; with a push-only window the pull's end released the lock outright and the push starting a second later engaged it again, so the user watched the pane cycle read-only/editable once per leg, and N pushes inside a few seconds became N lock/unlock cycles. `decideLock` now treats "anything at all was holding a moment ago" as grounds to keep holding through the window (`prevHold === null` is the only release), which collapses the sequence into one engagement while leaving both unconditional escapes — a dropped watcher and a failed push — untouched, since those are the only ways out of a lock the user cannot otherwise clear. The accepted cost is stated in the file: a LONE pull now keeps the workbench read-only for the grace window where it used to release at once, which buys nothing against D339 (a pull moves nothing upstream) and is paid purely for steadiness. **`nextAckState("engage")`'s unconditional reset was examined and deliberately KEPT.** The ack handshake had the same flicker shape one layer up — every engagement resets to `"waiting"`, which re-shows the fallback scrim for the ack timeout — and an exemption for "a re-engagement inside the grace window" was written, shipped to review, and reverted. It was unsound twice over: coalescing means `locked` can only go false->true after a FULL release or one of the two unconditional escapes (dropped watcher, failed push), so every engagement that reaches the reset genuinely IS new and the exemption fired on all of them; and inheriting the previous engagement's answer is unsafe precisely there, because the workbench frame may have reloaded in between (the ready-message re-assert path exists for exactly that) — an inherited `"acked"` shows a lock with no fallback scrim AND no workbench enforcement, i.e. protection the page implies and nothing provides, which D353 forbids by name. The concrete case is a failed push: the error branch releases at once, the user hits Fix with Claude, a push engages 3s later. Anti-flicker is entirely coalescing's job; the capability probe stays a fact this page re-asks for every engagement. |
| D363 | **Two field fixes from the first real canvas session on D360/D361: the workbench-skills fetch fires when a canvas is OPENED (not only when one is cloned), and the seeded CLAUDE.md's missing-skills fallback is a hard BOUNDARY — stay in the folder, never walk `~/.fused-render/mounts`** | Both were found by one manual test and they compound, which is why both are fixed even though either alone would have hidden the other. **The fetch had exactly one call site, `POST /api/canvases/clone`** — chosen because it is canvases-scoped and off the pre-bind path — and that quietly means the feature only ever works for a canvas created AFTER this branch shipped. A canvas cloned earlier (i.e. nearly every real canvas) never triggers it: no clone on disk, `workbench_plugin_root()` None, no second `--plugin-dir`, and a session whose own seeded CLAUDE.md names `workbench:canvas-toml` cannot load it. Verified on the owner's machine — `~/.fused-render` held no `workbench-skills` at all while `canvases/canvas_1` was being edited. The fetch now also fires from `POST /api/canvases/sync/start`, which is what the canvas page calls when it opens a canvas (and again on every poll that finds the watcher dropped), so "open an existing canvas and start a session" is covered end to end. Both routes go through one `_kick_workbench_skills()`: **off-thread**, because both requests are awaited before the page renders and a shallow clone against a dead network sits for the full clone timeout; **one in flight per process** (non-blocking lock acquire), because the re-arm path repeats and a flaky network would otherwise pile up threads each waiting out its own timeout; rate limiting left where it belongs (`skill_plugin._attempt_due`), so repeated opens cost a lock check; and swallowing everything, since a canvas open must never fail over a skill fetch. Off-thread does mean the root can land a moment after the page opens, which is harmless: what must be set is the server's env by the time a SESSION spawns, and that is a user typing. Rejected again: fetching from a startup event, which would make every user clone a repo for a feature most never open (D360's reason), even though a post-bind background thread would not risk the bind itself. **The second half is worse than a missing feature.** With the skills absent the session took our fallback ("carry on without them: follow the conventions of the files already in this folder") as licence to find the format SOMEWHERE, and ran `find / -iname "pipeline.md"` plus a recursive walk of `~/.fused-render` — which wedged every rclone NFS mount on the machine, a documented failure mode in this repo, not a hypothetical. Its own account was "I got sidetracked digging through internal app files", i.e. it recognised the detour only when told, so the text has to prevent the walk rather than discourage it. The fallback now names the boundary and the destinations: no `find`/`ls -R`/`grep -r`/glob or any recursive walk over `/`, the home dir, `~/.fused-render` or the fused-render source tree (the app's internals are not documentation), and never list, walk, glob or read anything under `~/.fused-render/mounts` because a walk there breaks the mounts for every app on the machine; if the folder does not answer the question, say what could not be determined and stop. A test pins the boundary language, so a future edit cannot soften it back into a nudge. **Also caught while reproducing: the test suite was doing real network clones of `fusedio/skills`** — several tests POST /clone or /sync/start, nothing is published under the per-run `FUSED_RENDER_HOME`, so the retry interval said "due" and git went out. An autouse conftest fixture stubs `skill_plugin._git` to a failed-git no-op (an exception would be invisible, since the fetch swallows by design, and a non-zero rc exercises the real "nothing to hand" path), and `FUSED_RENDER_WORKBENCH_PLUGIN_DIR` joins the appenv vars a test may not leak — it is published from a background thread, so a leak would arrive at an unpredictable moment in a later test. |
### Canvas preview signing comes off the listing's critical path (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D364 | **`GET /api/canvases/list` no longer signs preview images. The list shim reports an uploaded preview as `preview_pending`, and the client signs the whole batch afterwards through a new `POST /api/canvases/previews` (`_fused_canvas_previews.py`), which signs on an 8-thread pool** | `/canvases` was measured at **2.21s warm / 3.95s cold** on a 7-canvas account before anything rendered, and the breakdown said the shape of the cost was worse than its size: spawn + `import fused` + `fused._env` 0.35–0.50s, `api.list_collections(whose="self", lite=True)` 0.90s, and then **one blocking `GET /collection/sign-image` per canvas holding an uploaded preview, in a `for` loop** at ~0.93s each. That last term is what made this a design bug rather than a slow endpoint: the listing's latency scaled **linearly with the account's canvas count**, entirely for decoration. A 20-preview account waits ~18.6s to see a single card, and *nothing on the page needs a thumbnail to be usable* — the cards' name, UDF count, modified time, cloned pill and click target are all already in hand when the sign loop starts. So the split is along the line the data already draws: what the list call returns for free stays inline (a **public https `preview_image_url` is used as-is** — it costs no round trip, and routing it through the batch would have made the fast case pay for the slow one), and only the `fused_uploaded_preview` sentinel — the one that *requires* a control-plane call — becomes `preview_pending: true` with a null URL. Measured after: `/list` **2.21s → 1.39s** warm (cards paint ~0.8s sooner), and the previews batch, now off the critical path, does **20 ids in 3.37s against ~18.6s sequential**. The pool is **8 workers, and the headers are generated ONCE outside it**: `api._generate_headers()` may refresh the credential, and N threads racing to refresh the same one is both wasted work and the shape that trips a token endpoint's rate limit. `POST`, not a guarded `GET`, for one reason only — the ids are a list, and a comma-joined query string for a large account runs past what proxies will carry; it still takes the same `X-Fused` guard as the other executing endpoints, since it does execute control-plane calls. Two failure postures are deliberate and unequal: a **failed signature is never an error** (the response simply carries null for that id and the card keeps its letter monogram — identical to what a canvas with no preview shows, so there is nothing to report), while a failed *batch* leaves the client's asked-set un-marked so the next refresh retries rather than costing thumbs until a reload. `PREVIEWS_MAX_IDS = 500` bounds one request's work — far above any real account, and there so a malformed body cannot ask for unbounded signing. The **external-CLI path answers `{"previews": {}}` rather than erroring**: that path (`canvas list`) never reports a pending preview in the first place, so empty is the truthful answer, not a degradation. Client-side, `fillPreviews` is deliberately **not awaited** by `refresh` and tracks asked-ids in a **ref, not state** — reading `previews` state there would make `fillPreviews`, and through it `refresh`, a new function on every thumb that landed, and `refresh`'s own effect would re-run forever. **Rejected: parallelizing the sign loop in place, inside the list shim** — it fixes the scaling term but keeps every canvas's thumbnail ahead of the first painted card, so the page still waits on the control plane for something it does not need; **rejected: a server-side TTL cache over the whole listing** (the other option offered) — strictly better *felt* latency and still worth doing, but it is orthogonal: it makes the repeat visit free while leaving the cold visit's cost exactly where it was, and it composes with this change rather than competing with it; **rejected: dropping previews altogether** — the thumbnails are how a canvas is recognized at a glance, and the cost was never the feature, only where it sat; **rejected: persisting signed URLs** — they expire, so a cache of them is a cache of soon-to-be-404s, and the existing `brokenPreviews` monogram fallback already makes an expired one degrade quietly. |
| D365 | **An app card with no preview shows an empty thumb, not a letter — and the monogram's last holdout, `AppCard.tsx`, is deleted along with every home.css rule that #359 orphaned** | The monogram was decoration standing in for information it did not have: a big `A` over `application` says nothing the card's own title, tag and folder name do not already say, and at grid scale a wall of tinted capitals reads as a placeholder pattern rather than as a set of apps. Removing it leaves the `.app-pcard-thumb` box intact — 16/10 aspect, `var(--bg)`, top border — so the card keeps its shape and only stops asserting a thumbnail it hasn't got. The fallback chain is otherwise unchanged: authored `preview.png` first, live scaled iframe second, empty box third, and an image that fails to decode still drops to the live render. **One consequence taken deliberately, against D324's stated reasoning:** the near-viewport offload and the `usePreviewStart` queue both used the monogram as their in-between state, and D324 argued it made an offloaded card "read as no live preview, never as broken". Those cards now show the empty box instead. That is the same thing a no-entry app shows, so the two states remain indistinguishable — which was D324's actual requirement — and a blank 16/10 panel inside an otherwise complete card does not read as a broken card the way a blank *card* would. **The dead-code half is the same decision, not a drive-by:** the tinted-hue helper `hueFor` and its palette lived in `AppCard.tsx`, a component that renders the `.home-app` row tile and has not been mounted anywhere since #359 replaced Home's rows with card grids — deleting the monogram left it with no caller at all, so the file goes and `AppPreviewCard` loses its last import from it. #359 also stopped rendering the doorways, the hairline section rules and the recents list while leaving their CSS in place, and a class-by-class audit of home.css against every `.tsx`/`.ts`/`.html` in `frontend/src` (both directions: rules with no user, and classes used with no rule) found exactly that set dead: `.home-app*`, `.home-apps`, `.home-recents`/`.home-recent*`, `.home-doors`/`.home-door*`, `.home-rule*` and `.home-loading` — 269 lines of home.css, plus AppCard.tsx's own 50. `.home-section` and `.home-empty` are kept (both still used), as are the three classes the audit flags in the other direction (`home-link`, `home-relative`, `home-search` have no rule on `main` either — pre-existing, not this change's business). **Scope stops at home.css**: a repo-wide dead-CSS sweep is a different task, and the rest of this set traces to one commit. The canvases grid keeps its own monogram plate, a different card with its own preview pipeline (D333/D364). Comments that pointed at the deleted rules are rewritten rather than left to rot — apps.css claimed its tiles reused `.home-app`, and home.css's header still described rows-with-monograms as the page's middle register. | User request 2026-08-19: "remove the alphabet based placeholder for the thumbnail inside the fused app card which is rendered on homepage and apps page". |

### Local speech-to-text: VAD region packing and the already-cached model path (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D366 | **The MLX Whisper VAD path PACKS speech into full 30-second clips instead of one `transcribe()` call per region — the regions are CONCATENATED and every timestamp is mapped back through the join, mirroring faster-whisper's `collect_chunks`** | `vad: true` was a pessimisation on the MLX runner, and measurably: mlx-whisper pads every call's mel to `N_FRAMES = 3000` (30 seconds), so a 0.8-second speech region cost a full encoder window. On a 216-second recording that is 92% speech — 31 regions, min 0.8s, median 5.8s, max 14.0s, every one under 30s — `large-v3-turbo` took 8.32s for the whole file and 23.30s for the 31 regions: the flag COST 15 seconds on a 3.6-minute file rather than saving anything (`tiny.en-8bit`: 1.48s vs 2.42s). Packed to <=29s clips it is 9.31s and 2.05s, so essentially all of it comes back. Since `runners/vad.py` exists precisely so both MLX engines mean one thing by the flag (AI-10f, D319), two engines 2.8x apart on it was a parity defect, not a missed optimisation — faster-whisper never had it, because its own `vad_filter` calls `collect_chunks`, which concatenates speech to a maximum duration and remaps the timestamps afterwards. **Rejected: span-merging** (one contiguous slice from the first region's start to the last region's end). It is simpler and needs no inverse mapping, and it puts the silence back INSIDE the clip — which gives up the entire reason `vad: true` exists, since Whisper hallucinates in silence; it would have made the flag cheap by making it not do its job. **Rejected: `mlx_whisper.transcribe`'s own `clip_timestamps` argument.** It would collapse 31 calls into 1, but it SEEKS per clip range, so each range still pays a padded window — it does not fix the padding and is not a substitute for packing. **The packing lives in `vad.py`, not in the runner**, beside `speech_regions` and `slice_samples`: what the decoder is actually handed is part of what `vad: true` means, and that is defined in one place for both MLX engines (the module's stdlib+numpy-only constraint is respected — the arithmetic needs no onnxruntime). The budget is **29.0, not 30.0**: a clip that tips a hair over the window — by sample rounding, or by `PAD_S` on each region — buys a second nearly-empty encoder pass, which is the exact cost this removes. A region LONGER than the budget passes through alone and is **never split**, because cutting mid-speech loses words and Whisper's own seeking chunks a long input better than a boundary chosen here. The inverse maps each endpoint of a segment INDEPENDENTLY, because a segment can span a join: the decoder hears continuous speech across it, so its start and end can fall in different source regions, and one shared offset would put both in whichever region was picked. **It is TWO functions, `vad.original_start` and `vad.original_end`, and the asymmetry is load-bearing** — a review caught the single shared mapping being wrong for an end: a time landing exactly on a join is the next region's first moment for a START and the previous region's last moment for an END, so with start semantics a segment lying entirely inside one region and merely ending on the join came back stretched across the whole dropped pause (clip `3.0-5.0` reported as recording `3.0-30.0`). That is not a float coincidence worth shrugging at, which is why the two flavours are separate named functions rather than one with a comment: the join IS where the pause was, so it is the most natural place for Whisper to end a segment, and region ends land on its 0.02s grid regularly (`WINDOW / 16000` steps and `2 * PAD_S` are both multiples of it). The consequence was worse than a late caption — `diarize.speaker_for` sums turn overlap across the stretched span, so a sentence could be attributed to whoever the segmenter heard inside the silence Silero discarded. A time one grid step PAST the join is a different case and still maps into the next region, since such a segment genuinely continues into it. The progress tick uses the END flavour, so a bar sitting on a join reports the region just finished rather than one not begun — behind, never ahead. **A second consequence of spanning segments, caught in PR review (Cursor Bugbot) rather than in testing: the SPEAKER label.** `diarize.speaker_for` sums per-speaker overlap across a segment's published interval, and diarization deliberately runs on the FULL waveform (D309/§40 — the segmenter finds its own silence and is better at it than a threshold over Silero probabilities), so a segment straddling a dropped pause was scored against turns that live entirely inside that pause: with ten seconds of one voice in the gap outvoting three seconds of the voice that actually said the words, the sentence was attributed to somebody who said nothing that was transcribed. **The fix masks the SCORING, not the published times** — `speaker_for(…, spans=…)` and `assign_speakers(…, spans=…)` intersect each turn's overlap with the intervals where speech actually was, so time inside a removed silence contributes zero to every speaker's total. The interval itself stays as it is, because the words really do sit either side of the pause and a page seeking a player wants where they are in the recording. **`spans=None` is the default and means "score the whole span"**, so `faster_whisper`, `parakeet_mlx` and a non-VAD `mlx_whisper` run are byte-identical to before the argument existed — none of them drops silence out of the middle of a segment, so a mask there could only be a no-op with a risk attached. The packed path passes `regions or None`, because an EMPTY region list is the detector finding no speech at all: the whole file is then decoded, nothing is dropped, and a mask of no intervals would score every segment against nobody and unlabel the entire transcript. | **`parakeet_mlx` is deliberately left decoding one region per call**, with the reason written in its `_regions_to_decode`: it is a transducer with no fixed window (it chunks only above `chunk_duration = 60.0`), so its cost is proportional to the audio it is handed and per-region decoding costs it nothing extra, while packing would add a second timestamp mapping to keep correct for no measured gain. Same meaning of the flag, different batching — the two engines still agree on everything AI-10f actually promises (silence dropped, cuts only at detector boundaries, original-recording time). Progress is unaffected in its units and better in its arithmetic: the borrowed frame counter now maps through the pack rather than through a single region offset, and the ETA keeps dividing by seconds of speech actually decoded rather than by the remapped position. |
| D367 | **A model already complete in the Hub cache is resolved with `local_files_only=True` and NO network call — bring-up latency and offline operation chosen over picking up a newer revision of the same branch** | Measured on this machine for `mlx-community/whisper-tiny.en-8bit`, fully cached: `worker_base.download_snapshot` **483ms**, `download_file` **456ms**, against **~14ms** for the actual weight load inside mlx-whisper. Every millisecond of that is Hub traffic — `HfApi().model_info(files_metadata=True)` is 228ms on its own and hf's `snapshot_download` spends another ~220ms revalidating etags — so a bring-up of a model the user had already downloaded spent about a second saying "Fetching weights…" before reading a byte, and it is where the "You are sending unauthenticated requests to the HF Hub" line in every worker log came from. The same two answers off the cache alone are **0.13ms** and **0.14ms**; end to end the two helpers now return in **0.2ms** and **0.1ms**. **The trade is the decision, not the speed-up: a complete cached model will NOT pick up a newer Hub revision** under the same branch until something else forces a re-check (a cache clear, a fetch of a file the snapshot lacks, a differently-scoped download). Chosen deliberately — these are pinned snapshots a user downloaded on purpose, not a moving dependency, and weights silently changing under a name the user picked is the worse surprise; offline operation is the same property from the other side. **Rejected: `HF_HUB_OFFLINE`** — it is process-wide and would also silence the networked fallback, turning a first download into a failure; `local_files_only` is per call, which is exactly the scope wanted. | **First-download behaviour is untouched, and that is the hard constraint the shape follows from:** the local attempt either answers completely or is discarded, and everything below it — the one Hub listing (AI-5i), the bar's total, the segmented fetch, the progress reporting — runs as before. **What makes "complete" checkable — twice corrected in review, and the second correction is the design: the app records its own fetches.** `_record_fetch` writes `<repo folder>/.fused-fetch-<commit>.json` after a download returns, holding the SCOPE it was asked for and the FILE NAMES that landed, and `_cached_path` serves a request only when the record is for the commit hf resolved, at the scope being asked for, with every recorded name present and settled. Every record is keyed by the commit the RETURNED snapshot is rather than by the sha that was asked for, the fallback to `snapshot_download` is pinned to that sha (a branch name resolved twice is two answers, and a repo that moved in between would leave the record naming a directory that does not exist — cold forever), and a fetch that landed LESS than its scope asked for writes NO record — the inverse failure, and the one review caught: a record is verified by looking its own names up, so building the list from what LANDED (the previous fix, now superseded) made it self-certifying, and a fallback delivering 1 of 50 files would have recorded one name and had an incomplete snapshot served forever. The list is the LISTING's, checked against the disk at write time, because the listing is the one thing here the fetch did not choose; a shortfall leaves the repo cold and names itself on stderr. Three smaller corrections from the same pass: `_commit_of` returns None rather than `""` for a path that is not a plausible commit (the empty string was a key that read back, so a record filed under nothing came back as a hit); the fallback's pin is merged into one options dict that `kwargs` updates, so a caller's own `revision` wins instead of raising `TypeError: got multiple values`; and the `.writing` staging file is unique per writer and never deleted by anyone else — two loads sharing one cache are separate processes, and sweeping that name to save a round trip made the other's `os.replace` fail and its record never appear, which is this decision's own failure mode reintroduced as tidying. Three follow-ups from the next review pass, all in the same function: the staged name carries a random token as well as the pid (two containers over one mounted cache have separate pid namespaces, and a reused pid would let two writers interleave into ONE file and `os.replace` publish mixed JSON as truth — worse than the cold repo the sweep cost); the skipped-record line is worded as the diagnostic it is, because it fires on a download that SUCCEEDED and a user reading it as a failed load is a real cost; and a falsy snapshot path is refused in the early return rather than joined as `""`, which had turned every presence check into a CWD-relative one — a working directory holding a `config.json` would have satisfied the shortfall check and recorded a fetch nobody had located. **Which of these silences is deliberate is itself written down**, after a review round found the refusal contradicting the docstring above it: the two ANOMALIES (a shortfall, and a fetch that returned no snapshot path — both reachable only after a download succeeded) are named on stderr as diagnostics that say the download worked, while the three ORDINARY nothings (no cache folder, a revision that is not a commit, a listing that selected nothing) stay silent because they are shapes the world produces and a line on each would be noise on every `local_dir` download. A hard-kill `.writing` leftover is accepted as permanent for the same reason the token exists — no writer can tell it from a live stage, and a by-age sweep could strike one — at a cost of a few hundred bytes invisible to every consumer and removed by the user's own `hf cache delete`. The record is the only evidence accepted, so a cache with no record — every machine that already holds models — takes the networked path exactly as before and gains one on the way out; there is no migration step and nothing is ever served on the strength of a record this app did not write. **The first attempt leaned on hf's own completeness check and was wrong to.** hf's `_raise_if_incomplete_snapshot` verifies a snapshot against `trees/<commit>.json` and its own code says that with no tree listing cached "we cannot tell, so we do nothing" — and `_segmented_fetch`, which is the NORMAL path (hf's downloader runs only on fallback), publishes blobs and hand-writes `refs/<branch>` and never writes a tree. So for essentially every repo this app fetched itself that check is a no-op; calling it a layer, and calling the gap unreachable, was simply false. **The second attempt — refusing scoped CALLS — was reachable-wrong, which is the more useful kind of correction.** Scoping is a property of the on-disk STATE: `diffusers_image` fetches `black-forest-labs/FLUX.2-klein-4B` scoped to `recipe["keep"]`, `mflux_image.download` fetches whatever id it is handed UNSCOPED, and `/api/ai/runtime/download` chooses between the two runners from the user's image-engine preference while `weights_only=True` stops before the `load()` that would refuse the format — so downloading that id on the MLX engine after downloading it on the Diffusers engine asked for the whole repo against a cache holding a tenth of it, and would have reported success having fetched nothing. Removing a GGUF recipe flips the same id from scoped to unscoped with no user action at all, and `from_pretrained` then fails on a component nobody fetched. Verified on the real cache: with the FLUX repo cached at the recipe's scope, the unscoped record lookup returns None while the scoped one returns its 17 files. A recorded scope closes both directions AND lets a scoped call use the fast path, which refusing scoped calls did not. **Recording the names is also what makes a MISSING file detectable:** A blob removed under a complete-looking snapshot — `hf cache` pruning, a partial copy of somebody's cache, a cleanup script — leaves a dangling symlink, and returning the folder unread would make that state PERMANENT: before the fast path, pressing Download again re-listed and re-fetched what was missing, so short-circuiting it silently removed the app's only repair route. a blob pruned together with its snapshot entry leaves nothing for a directory walk to trip over, and before this path existed pressing Download again re-listed and re-fetched exactly that — so short-circuiting without a list of what should be there quietly removed the app's only repair route. Checking recorded NAMES (`os.path.exists` follows symlinks, `_settled` rules out a blob still being written) also removes the dependence on `os.walk`, which does not follow directory symlinks by default and so used to refuse a snapshot whose files sit under a linked subdirectory. **Measured on the real cache**: 0.5ms end to end for a 5-file whisper snapshot and 1.2ms for the 17-file scoped FLUX one, against 483ms before — and 0.6-1.1s for the record-less first call, which is the old cost paid one more time. The returned path is verified rather than merely checked to exist, because it comes from a call this code did not make itself. The part-file check is per BLOB, not per repo: a leftover `.fusedpart` is the resume state and persists by design (AI-5i), so a repo-wide scan let one cancelled quantization in a multi-GGUF repo disable the fast path for the dozen files it already had, permanently. **Two rules the first cut got wrong, both caught by a test this branch never touched (`tests/test_ai_hub_fetch.py`, on the full suite):** (i) the fast path must not CALL a hub download function for a repo the cache has never held — that test states "pressing Stop must not start a download" and enforces it by counting `snapshot_download`/`hf_hub_download` calls, correctly, because "we only passed `local_files_only`" is precisely the claim a later refactor breaks — so the gate is a filesystem look first (`_has_cached_snapshot`, and `try_to_load_from_cache` for the single-file path, which has no download in it at all) and hf is asked only to confirm; (ii) `Cancelled` must never be read as "not cached": it is an ordinary `Exception`, so the original `except Exception` turned a ✕ into a fallback and would have started a fresh multi-gigabyte download out of pressing Stop. The catch is now an explicit `_NOT_CACHED = (OSError, ImportError)` — hf's own `LocalEntryNotFoundError` and `IncompleteSnapshotError` are both `OSError`s, so the tuple names them without importing them — and a hub version inventing a not-cached error outside it costs a slow bring-up, which is the right way round. |

### Canvases cards: flat, no monogram, map tiles for a missing preview (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D369 | **The `/canvases` cards lose their chrome and their monogram: no card border or surface, the preview image *is* the card (16/9, 6px radius, a single `--border` hairline), and the body under it is three lines — name at 20px/400, UDF count (or the clone hint), and `Last modified: <date>`. A canvas with no preview gets a quiet "No preview" plate instead of a giant tinted initial** | The old card was a bordered plate wrapping an accent-washed monogram, and at grid scale the monograms were the loudest thing on the page: a **wall of capital letters competing with the real thumbnails beside them**, each one saying nothing the card's own title didn't already say a line lower. The same call was made for app cards one PR earlier (D365) for the same reason, so this is the canvases half of one consistent rule: *a missing image is shown as a missing image, never as decoration standing in for one.* Removing the monogram then removes the reason for the plate — a bordered container existed to frame the letter — so the card flattens onto the page background and the thumbnail carries the visual weight, which is what the hosted workbench's own gallery does and what the redesign was measured against. Three consequences are deliberate. **The hairline on the thumb stays** even though the dark theme barely shows it: in light mode `--bg` is `#ffffff` and a pale preview would have no edge at all, so the one surviving piece of chrome is the one light mode cannot do without. **Hover moves to the thumb border + name colour** (`--accent` mix, `--accent-soft`) because the card is still a `<button>` and stripping its surface removed the only hover cue it had; `:focus-visible` keeps its own outline, now offset 3px since there is no border to sit against. **The modified line now falls back `mtime ?? updated_at`, the same expression the sort orders by** — the old meta line showed a date only for cloned canvases, so a listing sorted by a date the card refused to display was a listing whose order looked arbitrary. The UDF count cannot get the same treatment: `list_collections(..., lite=True)` does not carry one, so a count exists only for a local clone, and the line degrades to **"Not cloned yet — click to clone & open"** — kept verbatim rather than shortened, because for an uncloned canvas that sentence is the card's only affordance. **Rejected: keeping the `cloned` pill** — with the stat line stating the clone state in words, a corner pill restated it in a second visual language; **rejected: mirroring the workbench's "No UDFs present in the canvas" placeholder text** — it looks like the right string to copy but answers a different question (that canvas has no nodes; ours has no *uploaded preview*), and a card whose thumb claims the canvas is empty when it merely lacks a screenshot is worse than one that says nothing; **rejected: `object-fit: contain` on the thumb** — letterboxing every preview reintroduces the plate this change deletes, and cover crops a canvas screenshot's empty margins, which is the part worth losing; **rejected: leaving the grid at `minmax(230px)`** — the 20px title needs room, and at the old column width the names ellipsed a third of the way in. |
| D370 | **A canvas with no uploaded preview shows the hosted gallery's map tile once per UDF — laid out in the workbench's own grid shapes — and the UDF count that drives it comes from the listing itself (`n_code_udfs`, computed from the `udf_ids` node list `lite=True` already returns), so every canvas has a count and a thumbnail before it is ever cloned. Only a canvas with zero UDFs falls through to text ("No UDFs present in the canvas"), and the tile PNG is vendored into the bundle rather than hot-linked from S3** | D369 replaced the letter monogram with a "No preview" line, and on a real account that read as *seven empty boxes*: the account's canvases mostly have no uploaded preview, so the honest-but-bare answer was the answer for almost every card. The hosted workbench solved this years ago and the fix was sitting in `client/src/udfrun/modals/udf-collection-modal.tsx` — one dark map tile per UDF in a grid whose shape rounds the count up into a fixed layout (5 tiles use the 6-layout with an empty cell), which is why the original screenshot this redesign was measured against shows six tiles for "6 UDFs" and one for "1 UDF". Both halves of that are copied deliberately: `getGridTemplateByCount`'s layout table verbatim, and `getCodeUdfCount`'s exclusions verbatim (`note_*` sticky notes and `widget_*` widgets are nodes but not code; a node whose `udf_type` is `app` is a published app) — a card that prints a different count than the workbench prints for the same canvas is worse than a card with no count. **The count is free**: `list_collections(whose="self", lite=True)` already returns `udf_ids` with a slug and type per node, so the shim counts what it was handed and adds no round trip — a listing-latency regression here would undo D364 for decoration, which is the exact trade D364 rejected. That in turn **repairs D369's weakest line**: the count no longer exists only for a local clone, so the stat line reads "9 UDFs" for an uncloned canvas instead of a bare hint, and the clone hint survives as a suffix (`· click to clone & open`) because it is still the only thing telling an uncloned card what a click does. D369 also rejected the workbench's "No UDFs present in the canvas" string on the grounds that the page could not know the count — **that premise is now false**, so the string is adopted for the one case it is true of. Where the two counts disagree the **local clone wins** (`n_udfs ?? n_code_udfs`): its `*.py` files include edits the workbench has not been pushed yet, and a card that contradicts the files the user is editing is the more confusing of the two. **The tile is vendored** (`frontend/src/assets/canvas-preview-tile.png`, 160KB, the workbench's `preview_thumbnail_1.png`) because this app runs on localhost and often offline; hot-linking it would make a card's whole appearance depend on reaching S3, and the one asset is cached once no matter how many cells reference it. Two mechanics are load-bearing and were got wrong first: the grid template arrives as an **inline `gridTemplateColumns`/`gridTemplateRows`** (the shape is data, so it cannot live in the stylesheet) — the first cut passed React the legacy code's own `{columns, rows}` keys, which are *real* CSS properties, so every card silently rendered a multi-column stack of full-width slivers instead of a grid; and the count is **capped at 16 tiles**, since past that each cell is smaller than the map's line weight and the grid turns to mud. **Rejected: keeping "No preview" for the no-image case** — it is accurate and useless, and the user's own reading of the two side by side settled it; **rejected: generating a per-canvas thumbnail locally** (rendering the clone's `canvas.toml` to an image) — that is a real feature with a real cost, and it competes with the *uploaded* preview the workbench already stores rather than with this placeholder; **rejected: computing the count from the local clone only, as D369 had it** — the data was already in the payload, and the listing was withholding it; **rejected: varying each tile's crop so the repeats look like different neighbourhoods** — prototyped (`object-position` walking a fixed anchor ring, every third cell zoomed) and dropped on the user's call: the workbench's plain repeat is what the design was measured against, and the variation was solving a problem the reference does not have. |
| D371 | **A chat task's target is the FILE its pane was on, read back out of the transcript's own `<live-app-state>` block — while that file still exists; scheduled entries keep their own target, pane-less chats keep the folder** | The transcript's `cwd` is always a folder (`agent.py:_workdir` resolves a file target to its directory before Claude Code ever sees it), so "open this task" landed on the folder even when the whole chat happened on `index.html`. The only durable record of WHICH file is the `<live-app-state>` block the Claude page prepends to a send — its `entry` field names the document the pane is about, with the url as older-block fallback — because the per-file sidecar is on its way out (D335) and Claude Code records no target of its own. `tasks_store.head` therefore grew a fourth answer: the block's `entry`, else the url's `_file` param, else its `path` param — in that order, because a templated preview's url is `/render?path=<template>&_file=<file>` and `path` there names OUR template, a real file on disk that would sail through the isfile check (Bugbot, PR #637) — taken from a LEADING block only (a tag further in may be something a human typed, the `_LEADING_BLOCK` discipline) and parsed as JSON rather than regexed (a title containing `\"url\":` must not win). Alternatives were weighed and declined (Akshil, 2026-08-19): a purpose-built machinery tag is new wire surface for a fact the block already states, and a durable session→file index is D335's sidecar by another name — the block also works RETROACTIVELY for every transcript already on disk, which neither alternative does. A pane file gone from disk falls back to the folder rather than opening a view of nothing, and a scheduled entry's target still wins: it is the thing the job was pointed at. |

### The nav row is "Canvases", the page is "Workbench Canvases" (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D372 | **The nav calls it "Canvases" and the page it lands on calls itself "Workbench Canvases" — the qualifier moves from the label to the destination, and the browser tab follows the page** | D358 spelled the entry "Workbench canvases" in every site on the reasoning that one destination should be recognisable across the three places it appears, and that half still holds — all three sidebar sites still say the same thing. What changed is WHICH thing. The row sits directly under Home and Tasks, one-word siblings, and "Workbench canvases" was the longest label in the rail for the least product meaning: the reader on this machine has no second kind of canvas to disambiguate from, so the qualifier was paying rent as noise. It is not deleted, though — it moves to the `<h1>` on `/canvases`, which is read once you have arrived and is exactly where "whose canvases are these" is a live question (they are the account's canvases in the hosted workbench, cloned locally). **`useDocumentTitle` follows the page, not the nav** (`App.tsx`): the tab title names the document you are looking at, and a tab reading "Canvases" beside a page headed "Workbench Canvases" would be the one place the two names visibly disagree. `sidebar-canvases.test.ts` keeps its same-name-everywhere claim and inverts it, now anchored on the `label:`/`label=` forms rather than the bare string — the word alone also appears as the route, the CSS class and the component name, so a bare count would pass on the wrong matches — and it gained the PAGE half of the swap (the `<h1>` says the long name), because a rename that leaves both sites short is the failure this row exists to prevent. |
| D373 | **The seeded canvas `CLAUDE.md` is re-seeded when a canvas is OPENED (`POST /api/canvases/sync/start`), not only at clone and after a CLI force pull** | Seeding ran on the creation paths alone, which means the file a clone got is the file it keeps FOREVER — every later edit to `_CLONE_CLAUDE_MD` reaches new canvases only, and nearly every real canvas predates any given change. The live consequence: clones made before D360's rename still name the pre-rename `fused:*` skills, and a user who once ran the `fused claude plugin add` that the even older text instructed has that stale plugin installed globally — so the session finds `fused:canvas-toml` in its available-skills list, loads it, and never touches the current `workbench:*` root the app hands it per-run. Nothing surfaces: stale skills load cleanly and teach outdated formats, which is the same silent failure `_is_loadable`/`WORKBENCH_SKILLS` exist to remove one layer down. Fixed at the same call site as D363's fetch and for the same reason — open is the only hook that reaches an EXISTING clone, and it fires on every open and every watcher re-arm, so a text change now lands on the next open rather than on the next `pull --force`. Free of the churn that kept seeding off the poll path: both basenames are excluded from the fingerprint, hash and merge-base walks (`_SYNC_IGNORED_BASENAMES`) and from every push (`.fusedignore`), so rewriting content is invisible to the watcher, and the helper's existing shim gate keeps the external-`FUSED_RENDER_FUSED_BIN` leg — where a seeded local-only file reads as a permanent `pull --dry-run` diff — untouched. Unchanged and still deliberate: the file is a maintained artifact, so a user's local edits to it do not survive (they already did not survive a pull). |
| D374 | **The pre-rename `fused@fused-marketplace` plugin is programmatically DISABLED (`enabledPlugins[id] = false`) on the canvas paths, beside the workbench-skills fetch** | Supplying the current `workbench:*` skills is only half of D373's fix. Before the rename the canvas skills shipped as a `fused` plugin the app itself instructed the user to install (`fused claude plugin add`, printed in the seeded CLAUDE.md of the day); that install is still enabled NOW, globally, and its skills are stale copies of the ones the app hands a canvas session per-run. Stale skills are the worst failure shape available here because they do not look like one — they load cleanly and teach a `canvas.toml` format that no longer exists — and the old CLAUDE.md named the `fused:` prefix, so a clone seeded back then points its session straight at them. The stale copy does not merely coexist with the right answer, it outranks it. **This mutates the user's global Claude config, which D360 refused and still refuses for the INSTALL side**: adding skills to every session on the machine is a leak, and `--plugin-dir` exists precisely so the app never has to. Retiring a plugin the app itself told the user to install is the opposite transaction — taking back our own footprint, not planting a new one — and it is done in the narrowest reversible form there is. **Only `fused@fused-marketplace`, matched exactly**: never a prefix or marketplace-wide sweep, because `agent-core@fused-marketplace` is a live plugin from the same marketplace and none of our business, and `fused-extra@fused-marketplace` / `fused@other-marketplace` are not ours either. **Only when the key is already present and truthy**: a machine that never installed it keeps a settings.json we never touched, since writing `false` for an absent plugin would state on disk that something is installed-and-off when nothing is installed at all. **Flipped to `false`, never deleted** — `false` is exactly what the Preferences page's own toggle writes, so the plugin stays listed there and one click puts it back, where a delete would make it vanish with no trace of what happened or how to undo it. Serialized through `claude_config.lib.config_lock()` (the same thread-lock + flock pair the Preferences page takes), because that page can be rewriting settings.json from another request or another process at that moment and a read-modify-write without it drops whichever change lost the race. **The git commit is conditional on a repo already existing**: `lib.commit` calls `ensure_repo`, which `git init`s `~/.claude` and writes a .gitignore into it — right when the user pressed a button in a config UI built around versioning, not something an unattended canvas open may do to their home directory behind their back; a user who IS versioning their config is also the user who will go looking for an unexplained settings change, so when the history exists the flip lands in it, and a commit failure never reports the retirement as failed (the setting is already written, and a caller retrying on False would keep re-reading a config it already fixed). Wired into `_kick_workbench_skills` — same off-thread hook, same canvas-only gate (a user who never opens a canvas gets their config left alone), ordered AFTER the fetch so the fallback answer is only removed once the real one is on disk — and idempotent, so the repeat opens that re-arm the watcher cost one settings read. **A new autouse conftest fixture (`_no_real_claude_config_writes`) repoints every import-time `claude_config.lib` path at a scratch dir**, sibling to `_no_real_workbench_skills_clone` and necessary for the same reason: several tests POST /api/canvases/clone or /sync/start, so unredirected the suite would silently disable a plugin in the config of whoever ran it — the tests reaching out and changing the machine, which is worse than the real network clone that fixture was written to stop. |
| D375 | **The seeded CLAUDE.md's skills section moves to the TOP of the file, names the `Skill` tool explicitly, maps each skill to the task that requires it, and declares `workbench:` authoritative over any same-named skill under another prefix** | D373/D374 made the right skills present and removed the stale ones; neither made the session actually LOAD them, and the old text was three separate invitations not to. **Position**: the section sat at line ~102 of ~130, after every word of sync mechanics — an instruction that must be acted on before the first edit cannot live at the bottom of a long file a model skims top-down, so it is now the first thing after the one-line intro (pinned as an ordering against the four later headings, not a line number, so the file can grow). **Mechanism**: "Load these before editing" named nothing to call, which reads as a suggestion; it now says to invoke the `Skill` tool, and a table maps each skill to its trigger (`canvas-toml` = ALWAYS before the first edit, `fused-udfs` = any `.py`, `json-ui-schemas` = any `.json`, `fused-cli` = any `fused` command, `canvas-comments` = comments) so "does this apply to me?" is not left to judgement. **Justification, because a model that sees no reason to spend the call will skip it**: the text now states that this folder's files cannot be edited correctly from their own appearance — a `canvas.toml` that looks obvious has required fields and node/edge rules an example does not show, and getting them wrong is rejected at push time and reaches the user as a broken canvas. Also explicit that skipping one because the change "looks small" is not allowed. **The prefix rule is the load-bearing correction**: the old fallback read "if they are absent, OR LISTED UNDER A DIFFERENT PREFIX, just search your available skills for the matching names", which is precisely the licence under which a stale `fused:*` plugin got used in place of the skills the app supplies — the text itself endorsed the wrong answer. `workbench:` is now named as correct, a `fused:`-prefixed match is named as a stale copy from an older release to be ignored, and the work-from-this-folder fallback is reachable ONLY when no `workbench:`-prefixed skill exists at all. D374 disables the stale plugin, but the two fixes are independent on purpose: a plugin can be re-enabled by hand or arrive from a marketplace we do not control, and the file must not re-open the hole. The D363 boundary paragraph (no recursive walk, never touch `~/.fused-render/mounts`) travels with the section and keeps its pinning test — it is the fallback's guardrail and is meaningless separated from it. |

### A read-only project folder syncs in a manifest mirror, and a bundled one keeps its venv key (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D376 | **A project whose folder cannot be written to is synced in a manifest-only MIRROR at `<venvs_root>/<key>.src` (`_env_install_worker._sync_root`), and a project folder that ships INSIDE the app is keyed on its path relative to the `fused_render` package (`projectenv._venv_identity`)** | Reported from a Linux AppImage: every AI model download died with uv's `failed to write to file `.../fused_render/ai/runners/faster_whisper/uv.lock`: Read-only file system (os error 30)`. Two separate consequences of one fact — **the runner folders of §40 are project folders that live inside the app** — and the packaged app's tree is read-only in the shapes it actually ships in (the AppImage runs from a squashfs mount; a Windows install under `Program Files` is not user-writable). (1) `uv sync` WRITES `uv.lock` into the directory it runs in. For a user's folder that is the whole point (the lock is source, gets committed, MD-7), so the sync still runs in place whenever the folder is writable; when it is not, it runs in a mirror beside the venv holding `pyproject.toml`, `uv.lock` and `.python-version` — the venv, cache and base interpreter are unchanged, so the environment built is the same one and only the directory uv may litter moves. The mirror **persists between builds**, which is what earns it: a read-only folder ships no lock and can never gain one, so without a kept mirror every rebuild re-resolves torch/ctranslate2 from PyPI. Source still wins — a lock or manifest the folder does ship overwrites the mirror's copy every time — and `gc()` reclaims a mirror WITH its venv (it has no sidecar of its own, so on its own account it would be a permanent orphan). Writability is asked by PROBING — create-exclusive a pid-named file in the folder, unlink it — not with `os.access(dir, os.W_OK)`, which on Windows reflects only the read-only *attribute* and so answers "writable" for an ACL-protected `Program Files` install, i.e. it mis-answers the exact platform this exists for. The mirror's kept lock EXPIRES on a manifest change: the mirror's own copy of `pyproject.toml` is compared byte-for-byte against the source and the mirror's `uv.lock` is dropped when they differ, because bare `uv sync` re-resolves only what the manifest invalidates and a WIDENED ceiling (`mlx-lm>=0.31,<0.32` → `<0.33`, the documented pattern in those manifests) invalidates nothing — the packaged builds would otherwise behave as if a lock had been committed and never refreshed while dev checkouts re-resolved, inverting what the manifests rely on. Mirror copies of source-tracked names the source no longer has are removed (`uv.lock` excepted — there the mirror is the only copy), so a withdrawn `.python-version` or `uv.lock` cannot govern every later build forever. `gc()` also reclaims a `<key>.src` with no `<key>` beside it at all, which is what a resolver failure or a project deleted mid-sync leaves behind. (2) With (1) in place the AppImage would then have re-downloaded that environment **on every launch**: the mount directory is a fresh `.mount_FusedRxxxxxx` each time, so a key that is the sha256 of the absolute path is a new key each time, the correct multi-gigabyte venv from last launch is unreachable, and `gc()` cannot even reclaim it (a vanished mount reads as *unreachable*, which it deliberately keeps rather than deletes). So an in-package folder's identity is `<fused_render>/ai/runners/faster_whisper` instead — stable across launches and across upgrades, with staleness still the manifest digest, so a release that edits a runner's dependencies rebuilds and one that does not keeps. The venv's sidecar records that same identity rather than the mount path, and `gc()` resolves it back against the current package dir — otherwise a bundled venv reads as merely *unreachable* forever and a runner folder an upgrade removes strands a multi-gigabyte environment nothing can collect. **The accepted cost:** `home_dir()` is `~/.fused-render` for any copy of the app without `FUSED_RENDER_BRANCH`, so two installed copies (an old and a new AppImage side by side, an AppImage plus a pip install, either plus a checkout) share one venv and one mirror per runner — not a case only a developer can reach. While their manifests agree that IS the feature; when they differ the two alternate-rebuild instead of coexisting. Folding an install identity into the key would defeat the cross-launch and cross-upgrade reuse the change is entirely for. **Rejected: staging the runner tree into the home dir** the way `core_templates` stages templates — it fixes both halves and is the closest precedent, but `registry.RUNNERS_DIR` is a module-level constant baked into frozen `Runner` rows at import time, so it would have meant resolving every runner's folder lazily (and doing filesystem work at import, before tests can point `FUSED_RENDER_HOME` anywhere) for a tree the app only ever READS; **rejected: `uv sync --frozen` with committed runner locks** — `--frozen` turns a manifest edit into a hard error instead of reconciling it (the reason it was removed from `_build` in the first place), and it does not help the general read-only case at all; **rejected: keying bundled folders on a normalised mount path** — it needs a rule about what an AppImage mount looks like, which is exactly the kind of guess that goes stale. |

### Annotations ride one labelled overview screenshot, and the walkthrough's opening words become the prompt (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D377 | **One overview screenshot per send, a lettered badge burned in at every pending comment's spot — typed and spoken alike — replacing the per-note element crops and the point notes' click-time crosshair crops** | The old shape gave every element note its own crop at send time (capped at six) and every point note a crosshair close-up at click time: N pictures for N comments, each showing a fragment with no way to see how the comments relate on the page, two capture paths to maintain, and a cap that silently starved later notes. Now `sendMessage` stamps each pending note's `label` (stored once, at send — `annotations.indexOf` shifts as notes resolve, so deriving the letter twice is how the picture and the JSON drift apart), and `annCaptureOverview` takes ONE `shotPane` capture, re-resolves each element (points convert through `annPointXY` with the current scroll, the cross-origin overlay through the same `ANN_XO_SCROLL` stub the pin painter uses), and `shotEncodeBadged` burns an accent disc wearing the letter at each spot, in fit-space after scaling so the badge stays legible at every size the budget ladder tries. An image/canvas note that stored `iu`/`iv` badges the clicked pixel inside the painted content box — the same spot the on-page pin marks — with the box center reserved for element notes that carry no fractional click (Bugbot, #661). A note whose spot cannot be found — element gone, or scrolled out of view — goes out with an `offscreen` reason instead of a badge, because a model told to find a letter that is not in the picture will hunt for it. The overview rides the existing pictures block as `kind: "overview"` (one writer, one reader — `paneShotIn` — so a restored turn shows it for free), at the pane shot's own caps rather than the crops' 640px, and the annotation preamble now explains the reconciliation: `label` ↔ badge for the pixels, anchor for the source, `offscreen` for the gaps. `shotBlankCover`/`SHOT_BLANK_BLOCK`/`shotRectOverlap` (crop-suppression geometry) went with the crops; blank-WebGL news now rides the overview's `viewNote` via `shotBlankRegions`, never suppressing it — suppressing the one picture leaves the message with none. |
| D378 | **Everything said before a walkthrough's first click seeds the composer as the message's own prompt, and `t` is explained to the model as walkthrough order** | The nearest-segment matcher pulled the user's framing ("overall, make this cleaner, and…") onto click 1 as if it were a comment about that one button. `annRecAssign` now returns those pre-first-click segments as INTRO, `annPrefillComposer(seed)` seeds the composer with it (outranking the canonical "apply the comments" prefill, never a typed draft), and the auto-send gate counts intro as words landing (the seeding itself is NOT gated on `!sending` — a walkthrough ending mid-run parks its words in the composer to ride the next send instead of evaporating, Bugbot #661; only the auto-submit waits) — a walkthrough that is all framing and no per-click narration still sends, and a recording with NO clicks at all is the degenerate case of the same rule — the whole transcript seeds the composer as the prompt (the old code discarded it). The preamble tells the model what `t` means (seconds into the recording when the spot was marked, words are what was said nearest that moment, the typed message is what came before the first mark, read in `t` order to replay the walkthrough) — `t` was already on the wire and unexplained, which made it noise. |
| D379 | **A receipt's comment rows open a "what was sent" popup — the overview, every comment with label/anchor/timing/caveats, and the exact composed text — and a reloaded session rebuilds it from the wire** | The receipt proved something went; nothing showed WHAT. Clicking any comment row (no separate button — a "view what was sent" pill was tried and looked like chrome) opens `#sentpop` with the overview image (click-through to the full-size viewer), the other attached pictures, one row per comment, and the exact message text the agent received. On reload the same receipt is rebuilt: `annotationsIn` parses the annotation block's JSON back out of the wire (the same forgiving posture as `paneShotIn` — anything unparseable is an empty list), `shotRestoreReceipt` draws the rows through the same `annReceiptRow` builder the live send uses, and the popup's `outgoing` is the stored wire text itself — so a reopened session keeps the labels, the words and the popup instead of collapsing to the bare "📌 annotations" marker. Live and restored deliberately share every builder, because the alternative is two receipt renderers that drift. When comments ride the message, the receipt's screenshot thumbs open this SAME popup rather than the bare full-size viewer (the picture belongs to the comments, and the popup shows it with a click-through to the viewer); only a picture-only send keeps the plain viewer on its thumb, by never being wired to the popup at all. The popup sits BELOW the viewer in the stack (80 vs 90) and its capture-phase Escape peels the viewer first, the popup second — the click-through has to land on top or it looks dead (Bugbot, #661). |
| D380 | **An annotation-only send goes out with an EMPTY message — the canned "apply the comments" prefill is deleted; the spoken intro and any typed draft ride along instead** | The prefill existed so auto-send had something to submit, but canned words told the model nothing the notes did not, and they occupied the seat the user's OWN words belong in. `annPrefillComposer` no longer manufactures text or refuses a typed draft: it appends the walkthrough's intro to whatever is in the composer (never clobbering — the user's extras are theirs and go too), and a save's auto-send fires bare (`if (isNew && !sending)`) — the composer path already accepted a wordless send (notes alone are sendable, D-era guard `!message && !annPending().length`), so the bubble collapses to the "📌 annotations" marker exactly as a wordless send always did. The old typed-draft refusal (note saved → stays a chip, draft untouched, nothing sends) is gone by request: a draft present at note-save time is the user deciding to send extra context WITH the comments, not a reason to block them. `annDone`'s flush follows the same rule: a bare `annAutoSubmit()` with nothing seeded — its old gate on `annPrefillComposer()`'s return value read the now-valueless call as "don't send" and stranded every pending note (Bugbot, #661). |
| D381 | **The two torch runners split into per-hardware variants — CPU, CUDA, ROCm — and the CPU build became what `auto` installs on every non-Apple machine; the accelerated rows are reachable only by explicit choice and are hard-gated on the device being there** | One `transformers-text` row and one `diffusers-image` row made the accelerator an invisible property of whichever index the manifest pinned: PyPI's Linux torch IS the CUDA build, so every Linux user paid ~2.19 GB of `nvidia-*` wheels whether or not a card was fitted, while every Windows user got the CPU build with no way to opt out — the name was honest on exactly one class of hardware and nothing on the page said which they had. There are now three folders per torch library (`transformers_text`, `_cuda`, `_rocm`, and the same trio for `diffusers_image`), identical manifests except for where `torch` resolves from, sharing one hoisted worker each — `runners/torch_text.py` and `runners/torch_image.py`, `git mv`d to the runners root with a five-line `worker.py` shell in every folder, per the rule `runners/preview.py` states about shared logic drifting. Folders rather than flags because PY-16 keys a venv on the folder path (two rows in one folder share one venv and cannot hold two torch builds) and `_env_install_worker` runs a BARE `uv sync` there, so the accelerator is pinned inside each `pyproject.toml` with `[[tool.uv.index]]` + `explicit = true` + `[tool.uv.sources]` and no install machinery changed: CPU pins `whl/cpu`, ROCm pins `whl/rocm7.1` and must also declare `triton-rocm` in `[project.dependencies]` (ROCm torch's `Requires-Dist: triton-rocm==3.7.1` names a distribution published only on that index, and `[tool.uv.sources]` governs declared deps only), CUDA resolves from PyPI on Linux, whose wheel already IS the CUDA build, and overrides to `whl/cu130` on Windows ALONE — `torch = [{ index = "pytorch-cu130", marker = "sys_platform == 'win32'" }]` — because PyPI publishes no CUDA-enabled Windows torch and `_cuda()` admits Windows, so without the marker the row would be offered there and quietly install a CPU wheel, which is the exact outcome the hard gate exists to prevent; the marker keeps Linux resolution byte-for-byte unchanged and the lock simply carries two torch entries. **Measured download bytes, cp312/linux-x86_64: CPU ~0.20 GB, CUDA ~2.73 GB (15 `nvidia-*` wheels, 2.19 GB of them), ROCm ~6.18 GB** — so the new default is a ~13x smaller install than what every Linux user silently got before. macOS survives the CPU pin: `whl/cpu` resolves darwin to plain `2.13.0`, the same macosx wheel PyPI ships with MPS compiled in, so Apple Silicon keeps the fallback it needs when MLX is unavailable. `code` is UNCHANGED on the two original rows, so a stored engine preference keeps meaning what it meant. Two new probes gate the accelerated rows, stdlib-only, no subprocess, no caching (every failure they report is one fixed while the app runs): `_rocm()` walks EVERY `/sys/class/kfd/kfd/topology/nodes/*/properties` (node 0 is the CPU, `gfx_target_version 0`), decodes the target with minor and step as single HEX digits (`90010` -> `gfx90a`, `120000` -> `gfx1200`) and checks it against the targets the pinned wheel actually ships, asking permission of `os.access(R_OK|W_OK)` on `/dev/kfd` and a `/dev/dri/renderD*` rather than modelling a mode or a group (this box's kfd is `crw-rw-rw-` with the user in neither `render` nor `video`; its card node carries an ACL mode arithmetic cannot see); `_cuda()` gates on `/dev/nvidiactl` + `/dev/nvidia[0-9]*` + `/dev/nvidia-uvm`. Deliberately no `nvidia-smi`: SPEC's no-system-binaries rule (the ffmpeg one), and 50-500 ms cold against the ~22-41 µs measured for the sysfs walk on a path that runs per page render. Naming: a hardware variant carries its accelerator in BOTH `label` and `short_label` ("Diffusers (CUDA)"), because "(CUDA)" is the engine's identity and not a fact about the reader's machine — three rows printing "Diffusers" would render as one engine everywhere but the picker — while the MLX rows keep their platform qualifier on the long name only, so a bracketed qualifier in the SHORT name is now the marker of a hardware variant. `(PyTorch)` is gone from the family: the library stopped distinguishing these rows, the accelerator does. `catalog.py` aliases the variants onto their sibling's suggestion list (`_SHARED_SUGGESTIONS`, resolved in `for_runner()`) rather than copying four keys, which keeps `capability_of()`'s "one repo id belongs to exactly one runner's list" invariant TRUE, and the notes were rewritten hardware-neutral — the old ones said "the one to pick with no GPU" and "the only one that needs a GPU" (tautological or backwards once a row is explicitly CPU-only) and conflated system RAM with VRAM, which is concretely wrong on ROCm where pytorch#184880 has a 16 GB RX 9060 XT reporting ~7915 MB usable. `formats.py` gained `TRANSFORMERS_RUNNERS`/`DIFFUSERS_RUNNERS` wired into `loaders()` plus the diffusion variants in `DECISIVE`, because a code missing from `loaders()` loses its Load button and drops the repo out of AI-11e's cached-model injection. **Rejected: not splitting at all** — leaves every CPU user paying 2.19 GB for CUDA libraries they cannot use, and leaves the row's name true on one platform. **Rejected: a ROCm `faster-whisper`** — CTranslate2 has a native HIP backend (v4.7.0+) and does build gfx1200, but its ROCm wheels ship only as GitHub release assets inside a zip with filenames byte-identical to PyPI's CUDA wheels; uv cannot read a wheel nested in a zip (it treats the archive as an sdist, and `#subdirectory=` is rejected), a relative `path =` source does not survive D376's read-only manifest mirror (`_sync_root` copies only `pyproject.toml`, `uv.lock`, `uv.toml`, `.python-version`) while an absolute one is unusable because the AppImage's mount path is fresh every launch, and the runner-dependency test forbids `" @ "` URL pins outright — and those wheels deliberately exclude ROCm itself (`auditwheel repair --exclude "/opt/rocm/lib/lib*"`), so they need a multi-GB system ROCm, the same class of promise the no-system-ffmpeg rule refuses. ASR on AMD therefore stays on the CPU, and there is no ROCm ASR row. **Rejected: auto-selecting the accelerated variant where one is available** — declined by the owner in favour of explicit opt-in: a default that silently requires a multi-gigabyte download and a working driver fails hardest on the machines least able to explain why. **(Withdrawn in part by D416 — the three torch TEXT rows are removed; the three Diffusers rows and the CPU-by-default rule are untouched, and `llamacpp-text`/`llamacpp-text-vulkan` are now the text family this rule applies to. One consequence is inverted: `auto` on a non-Apple machine no longer reaches an unaccelerated TORCH row, it reaches `llamacpp-text`.)** |

### The hardware probes stop refusing machines that work, and a row is described by ONE probe (2026-08-19)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D382 | **The device probes D381 introduced are corrected on four counts: a missing `/dev/nvidia-uvm` is no longer fatal, WSL2 is admitted on `/dev/dxg` + `/usr/lib/wsl/lib/libcuda.so.1`, ROCm's render node must belong to a `0x1002` card, and an absent device is a different sentence from a closed one — and a serialised engine row now reads `available` and `reason` off ONE `available()` call** | Every one of these was a hard gate telling a user with working hardware that they had none, which is the failure a hard gate is supposed to prevent rather than cause. **uvm:** `/dev/nvidia-uvm` is created LAZILY — `nvidia-modprobe` loads `nvidia_uvm` the first time a process creates a CUDA context, and the display path needs only `nvidia`/`nvidia_drm` — so a freshly booted desktop whose `torch.cuda` works has `/dev/nvidiactl`, `/dev/nvidia0` and no uvm node; both CUDA rows were greyed out there and the reason blamed "a driver update without a reboot", the opposite of the truth, with no override anywhere. It is now asked for PERMISSION only, when it exists (a container handed the nodes without the access is real and unfixable by waiting). The genuine driver-mismatch case the existence check was aimed at is not visible from here anyway: a mismatched module fails at load, not at `os.path.exists`, and torch's own error reports it. **WSL2:** GPU-PV exposes the card as `/dev/dxg` and bind-mounts the CUDA driver library under `/usr/lib/wsl/lib`, so a WSL2 user with a working `torch.cuda` has none of the Linux nodes and was told so — true, irrelevant, and it left the engine unselectable. That shape is checked FIRST, because the Linux nodes' absence is not evidence against it, and with two `os.path.exists` and nothing else: dlopening `libcuda.so.1` would be proof rather than evidence and would initialise a driver on a per-page-render path, which AI-6 bars for `nvidia-smi`'s reasons (AI-6 now says so in as many words). **Render node:** `_usable_render_node()` accepted ANY readable `renderD*`, and an Intel iGPU's `renderD128` is world-openable on most distributions — so a hybrid box with an open Intel node and a restricted AMD one passed the gate on a device HIP never touches and paid ~6 GB for an install that then failed inside HIP. `_amd_render_nodes()` reads `device/vendor` out of the DRM class, the same walk `_amd_gpu_present` already does, so the vendor answers WHICH node too. **Two sentences:** a `/dev/dri` with no AMD render node in it is a container started without `--device /dev/dri`, and "add your user to the `render` group, then log out and back in" cannot fix that — the container sentence existed but lived in a later branch the early return made unreachable. **One probe:** `describe_engines` and `_engine` each called `available()` twice, once to choose or to fill `available` and again for `reason`. Free while every probe was a `platform` fact, a race the moment it was a device read: the two can straddle a `modprobe`, a container restart or an eGPU being unplugged, and the row then serialises `available: false` with `reason: null` — a disabled `<select>` option with NOTHING saying why, since the page has no copy of the reason and must not — or an available row still carrying the refusal that picked it. Bound to a name once (`_choice`, and one `[(runner, available())]` pass in `_engine`) so the second read is not expressible. The CPU rows' `note`s were wrong for a related reason: `torch_text._placement()` returns `("mps", float16)` and `torch_image._place()` does `pipe.to("mps")`, so "Runs on the CPU… at a few words a second" and "minutes per image" printed a CPU claim beside a loaded card reporting `mps`, on the very Mac the `whl/cpu` pin was chosen to keep working. `code`, `label` and `short_label` are UNCHANGED — a stored preference keys on `code`, and "(CPU)" names the BUILD (AI-2c) — only the notes moved. **Rejected: `ctypes.CDLL("libcuda.so.1")` to settle WSL2 exactly** — it loads a driver, on a path that runs per page render, for a question two `stat`s answer well enough; the same objection as `nvidia-smi`. **Rejected: keeping the uvm existence check behind a "probably fine" warning** — the row was DISABLED, so there was nothing for a warning to soften; a probe that cannot distinguish "not loaded yet" from "cannot load" must not be the thing that refuses. **UNVERIFIED ON HARDWARE:** there is no NVIDIA GPU, no WSL2 and no Intel iGPU on the machine this was written on, so findings 1, 2 and 6 are pinned against fake `/dev` and `/sys` trees under `tmp_path` — the shape of the evidence, not the hardware. **(Amended by D416 — the two torch-text `note` corrections this record made are gone with their rows. The RULE they were made for stands and now lands on `llamacpp-text`, whose own note names Apple Silicon's GPU for the identical reason: its `whl/cpu` wheel links Metal, so a CPU speed claim under the picker would contradict the card beside it.)** |

### A ROCm render can take the desktop with it, and the row says so (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D383 | **The ROCm image row's `note` warns that a long render can stall the desktop, and the hazard is documented in `registry.py` rather than mitigated or hidden** | Measured, not feared. A sustained submission on an RX 9060 XT (gfx1200), kernel 7.1.4-zen, produced `ring gfx_0.0.0 timeout` -> `Starting gfx_0.0.0 ring reset` -> `[drm] device wedged, but no recovery needed`, and the process the kernel named was **Hyprland** — the compositor, not the renderer. Compute and display share that ring on a single-GPU machine, so the GPU recovered without a reboot and the SESSION did not: on Linux that reads as the whole machine locking up. The row promised "Seconds per image on a supported AMD GPU under Linux" and said nothing about what paying for it can cost, which is the same class of omission D381 fixed on the other side (a CPU default that never said it was the CPU). **Honest about provenance:** the stall was produced by a continuous matmul loop, not by a render, and a ~90s FLUX.2 klein render on the same card finished cleanly — so the note says a long render CAN stall the desktop, not will, and the paragraph in `registry.py` says which of the two was observed. **Rejected: mitigating it.** The fixes are outside this app — CU masking, queue priority, driving the display from a different card — none verified here, and shipping an unverified knob makes a promise on the driver's behalf; the no-system-binaries rule (SPEC AI-6, and ffmpeg before it) is the same instinct. **Rejected: capping steps or resolution for ROCm.** The page owns those arguments; a silent cap would make the engine quietly worse instead of honestly documented, and would be undiscoverable to the author wondering why their slider stopped mattering. **Rejected: putting the account in `diffusers_image_rocm/pyproject.toml`** beside the other gfx1200 realities, which is where it belongs by subject — `projectenv.state_digest` hashes `pyproject.toml` WHOLE, comments included, so a paragraph there marks every already-built ROCm environment stale and charges existing users a resync to read it. It lives in `registry.py`, which nothing hashes. The text row is deliberately left unwarned: generation submits short per-token kernels rather than one long one and has not been seen to do this. |

### Both sidebars close and reopen by drag (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D384 | **Both sidebars close and reopen by DRAG, not only by button: a seam dragged past its floor sticks there for half the floor's width and then shuts, and the edge it leaves behind pulls back open at 32px** | The seams already resized and already clamped, and the clamp was the problem: dragging inward past the minimum simply stopped, which says "this is as narrow as it goes" — while the panel plainly *can* go narrower, all the way to shut, reachable only from a button elsewhere in the chrome. The floor was lying by omission, dead-ending the gesture that means "less of this" a few pixels short of the state the hand was reaching for. One rule now serves both edges of the window (`platform/lib/panel-drag`, pure and tested; the global sidebar at 180px and the file preview's companion column at 280px each pass their own floor): above the floor, the ordinary clamp; between the floor and `floor - floor/2`, the panel **sticks** and the cursor walks away from the edge it is holding — resistance is how a seam says "there is one more thing past this"; beyond that, shut. The half-the-floor overdrag is **borrowed from the workbench**, whose `allotment` (VS Code's SplitView) snaps at `floor(minimumSize / 2)` of travel — so the two apps resist by one law, and scaling with the floor rather than fixing a constant keeps the overshoot proportional to what is at stake (90px on the sidebar, 140px on the column) instead of making the wider panel the cheaper one to lose. Closing **never records a width**: shutting a panel and narrowing it are different acts, and one drag must not quietly do both, so the opener brings back the width you had and not the floor the drag stuck at on the way down. Reopening is the reverse gesture at the same edge — the sidebar's handle now rides the **collapsed rail** as well, and a shut companion column leaves a zero-width strip hanging a 6px seam off the split (`SideReopenEdge`) — gated at a flat, deliberately un-symmetric **32px**: closing takes away what is on screen and wants an overshoot, opening only puts it back and must not feel stuck. *Rejected: the workbench's own reopen, a floating chevron, because a gesture that only works in one direction teaches you not to trust it.* Two mechanics make it survive: the sidebar's handle is a SIBLING of `#sidebar` rather than a child, and the column's edge captures on `documentElement` (the row drag's trick), because both elements are unmounted at the exact instant the pull succeeds and capture on a detached node is capture lost — a gesture that dies on success. Which rule a drag is read by is fixed at pointerdown and never re-read from the live state: the open and close thresholds sit at different widths on purpose, so a gesture that switched rules on opening would flap once per pointermove across the whole band between them. Buttons are untouched, and `_side=off` is still what a shut column writes. |
| D385 | **A fused app exports to a single double-clickable `.fused` file — a zip of `manifest.json` + a `files/` payload mirroring the WHOLE app folder — built by `appfile.export_app_file`, downloaded from the card menu's "Export App File" via GET `/api/appfile/export`** | The unit of identity for an app is its FOLDER (D301), so the export walks the folder rather than reusing `export.py`'s per-page static scan: the scan exists to build a *minimal hosted* bundle from one page and misses sibling pages and computed-path assets, while a `.fused` targets the opposite trade — carry everything the author shipped, because the opener is a full fused-render runtime, not a hosting layer. The payload keeps bundle v2's single-payload-dir shape (`files/<page-relative path>`, docs/bundle-v2-design.md) so the two artifact families read alike; the manifest records `fused_app_file: 1`, the app name, and the entry page resolved at export time by the one shared entry rule (`app_listing.app_entry`) so exporter and hub can never disagree which page opens. Excluded: dotted names (`.git`, `.claude`, `.venv`, `.env` — which can hold secrets), `node_modules`/`__pycache__`, symlinks, and `CLAUDE.md` (the authoring contract; the artifact has no editing surface behind it). *(Amended 2026-08-27: the folder's gitignore rules are honored too — app-local, parent-folder or repo-toplevel `.gitignore`, `info/exclude`, global excludes, via the same `check-ignore` oracle the explorer walk uses; an ignored entry page or a mid-walk oracle failure fails the export loudly rather than shipping a half-filtered payload; budgets are measured post-filter.)* `pyproject.toml`/`uv.lock` ship — the extracted copy resolves its env through the unchanged per-folder machinery. **RH-11 holds file-wide:** any page in the payload containing `fused.ai(` blocks the export with the same textual match as `export.py` — the ask was explicit that the artifact is render-only ("no claude"), and a page that needs the author's CLI is broken on arrival anyway; a `fused.env` guard deliberately does not exempt it, exactly like the hosted exporter. *(Reversed by D388: fused.ai() now ships — the owner clarified "no claude" meant the editing surface, not the AI runtime.)* Budgets: 4000 files / 512 MB, loud errors. Non-destructive: refuses an existing out path; built staged, one `os.replace` in. **Rejected: `.zip` extension** — the whole point is a Finder/Explorer double-click landing in FusedRender, which requires owning the extension (D99's `.bookmark` precedent); **rejected: per-page `.fused`** — the user asked for "the entire application", and a page-scoped artifact already exists (the deploy bundle). |
| D386 | **Opening a `.fused` is confirm-then-extract-then-embed: the OS double-click routes (shared codec) to the server-served `/openfused` confirm page; only its explicit click fires the X-Fused-guarded POST `/api/appfile/open`, which unzips through `zip_import`'s hardened extractor into a content-addressed dir under `~/.fused-render/appfiles/`, chmods every file 0444, and lands the browser on the entry page in EMBED mode** | Each clause closes a specific hole. *Confirm page* (`static/openfused.html`, the D110 clone-confirm posture verbatim): a `.fused` that arrived by mail runs Python through `/api/run` the moment its pages render, so a bare double-click must execute nothing — the page names the app, its size, and says "can run Python code on this machine" before anything touches disk; deeplink clone earned a confirm for a weaker threat. *Hardened extract*: one unzip implementation for every archive the app accepts (zip-slip, symlink entries, entry-count and decompressed-size caps on bytes actually written — declared sizes are attacker-controlled); a second extractor "just for apps" is how a hardened and an unhardened path end up side by side. *Content-addressed cache* (`<name-slug>-<sha256[:16]>`): re-opening the same file re-uses the extract, an edited re-export lands fresh instead of mixing with the old bytes. *0444 files* (RO-7's bit): `stat` reports `writable:false`, `/api/fs/write` refuses, `fused.writeFile` surfaces the existing `readonly` error — the no-edit contract enforced by machinery that already exists, zero new surfaces; a read-only folder's env install is already solved (D376's mirror). *Embed mode* (`/explorer/embed/…`): chrome-free by construction — no sidebar, no breadcrumb, no editor, no Claude panel — so "renders the app as it is" is the page-load mode, not a filter to maintain. The entry must still carry the fused-app marker on extraction (the manifest names it, but the marker is what D301's open-recording and the hub key on). **Rejected: rendering straight out of the archive** — every runtime service (`runPython` cwd, env resolution, rawUrl, the call log) assumes real files in a real folder; **rejected: extracting into `~/Fused`** — the workspace is the user's authored space, and an artifact cache that a re-open regenerates does not belong in it. *(Revised by D389: the confirm page is REMOVED — open is a gate-less extract-and-redirect; every other clause of this row still holds.)* |
| D387 | **`.fused` is a first-class OWNED file type on all three platforms — macOS `CFBundleDocumentTypes` Owner rank + exported UTI `io.fused.render.app` (conforming to `public.data`, not `public.zip-archive`), Windows/Linux via a hardcoded extra in `winopen.extensions()` — and an opened app reaches the /apps hub through D301's render-time recording, not an explicit registration call** | The Owner/UTI pair copies `.bookmark` (D99/DM-8) for the same reason: no other app defines the format, and without an exported UTI LaunchServices treats the extension as dynamic and the rank binds unreliably. Conforming to `public.data` rather than the zip UTI is deliberate — physically it IS a zip, but declaring that invites Archive Utility and every unzip tool to claim and helpfully explode it, which is the one interaction the format exists to prevent. Windows/Linux associations derive from `templates/registry.json` keys, where `.fused` can never appear (it is not a preview template — the codec routes it to `/openfused` before any template resolution), so `extensions()` seeds it explicitly; `scripts/file_associations.json` regenerated, the runtime/AppImage parity test unchanged. Hub registration: POST `/api/appfile/open` records nothing itself — rendering the marker-carrying entry IS the open (D301), and for a folder outside the workspace that IS registration (`registered_apps.record_open`), so the extracted app appears on the hub and in recents through the existing single pipeline. **Rejected: `_preview=1` semantics for opened app files** — a user who double-clicked an app wants it in their recents like any other app they opened; hiding it would make the hub lie about what ran. **Rejected: Alternate rank** — an extension we invented has no user default to respect. *(Revised by D390: `.fused` IS a registry key now — the fusedapp template's binding — so the winopen seed is removed and Win/Linux associations flow from the registry like every other extension.)* *(Revised by D396: the render-time D301 recording no longer registers the extract dir — registered_apps refuses paths under the extract cache, and the open's hub identity is the `.fused` FILE, recorded by POST /api/appfile/open into appfile_recents.json.)* |
| D388 | **`fused.ai()` SHIPS in a `.fused` app file — the export-time RH-11 gate D385 added is removed; the recipient's runtime answers, and a machine with no claude CLI / resident model surfaces the API's own `ai_unavailable` state** | The owner clarified the contract D385 had over-read: "no edit or claude" meant no EDITING surface in the opened experience — which embed mode already strips — not no AI. The technical premise differs from the hosted exporter's too, which is why RH-11 does not transfer: RH-11 exists because a *hosted* page has no machine behind it at all, while an opened `.fused` runs inside the recipient's full local fused-render, where `/api/ai` is a real endpoint. A recipient without the claude CLI or a local model is a case the API already handles gracefully — the call rejects with `.type "ai_unavailable"` and a message saying what to install, a state the authoring contract tells every page to render as friendly UI rather than an overlay — so blocking at export protected against a failure mode that was already soft. **Rejected: keeping the gate with an opt-out flag** — a flag on the export dialog for a policy nobody holds is UI debt; **rejected: stripping `fused.ai` calls from the payload** — silently mutating an author's pages is worse than either shipping or refusing them. AF-3 rewritten; the D385 row carries the reversal note. |
| D389 | **The `.fused` open gate is REMOVED (owner call): `GET /openfused?file=` now extracts (or re-uses the cached extract) and 302-redirects straight to the entry page's embed URL — a double-clicked app opens like a document, no confirm page, no second route** | The owner's contract for the artifact is "double click → the app experience", and a confirm page between the click and the app is friction on every open forever, paid mostly by people opening their OWN files. D386's gate defended against a mailed `.fused` running Python on first render; the owner weighs that as no worse than every other way fused-render already renders received content (a cloned folder's pages, a downloaded html opened in the explorer — none of which gate per-open), and accepts it. What REMAINS is everything else D386 built, which is where the real hardening lives: the hardened extractor, the content-addressed cache, the 0444 payload, the marker check, embed mode. The GET now mutates (an extract) — accepted with the same posture as `GET /render` recording an open: idempotent, confined to the app's own cache dir, and re-entrant (a concurrent open of the same bytes resolves to the winner's extract). The confirm page, `GET /api/appfile/info`, and `POST /api/appfile/open` are deleted rather than kept "in case" — three surfaces nothing calls is maintenance debt, and the deeplink-clone confirm remains the reference implementation if a gate is ever wanted back. Errors answer a minimal HTML page, not JSON: the URL is reached by OS double-click navigation. **Rejected: a first-open-only confirm** (gate once per content hash) — clever, but the owner asked for no gate, and a sometimes-appearing page is harder to explain than either always or never. *(Revised by D390: the /openfused redirect hop itself is gone — a .fused renders at its own /explorer URL via the fusedapp template; the no-gate stance stands.)* |
| D390 | **The `/openfused` URL is REMOVED: a `.fused` renders at its own explorer URL — `.fused` becomes a preview-template binding (`registry.json` → new core template `templates/fusedapp`), whose page calls the internal X-Fused-guarded `POST /api/appfile/open` and iframes the entry's embed URL; the OS double-click codec maps to `/explorer/embed/<path>.fused` (the app experience), an in-explorer open is the ordinary `/explorer/view/<path>.fused` (shell chrome kept)** | Owner call, and the shape is better on every axis than D389's redirect hop. (1) **The URL is the file**: `/explorer/embed/Users/…/sine.fused` is the same URL grammar as every other preview — bookmarkable, shareable, no second grammar to learn, and view-vs-embed falls out of the existing prefix semantics instead of needing a dedicated route to pick one. (2) **The special case dissolves**: the frontend codec's `.fused` branch is deleted outright (an explorer click is just a template resolution now), the Python codec's branch shrinks to "OS opens land on embed", and winopen's hardcoded extension seed dies because `.fused` is a registry key like any other previewable extension — one mechanism, not three. (3) **Extraction stays server-side and single**: the template is a ~40-line loader that POSTs `/api/appfile/open` (restored as an internal route; templates must not import `fused_render`, PY-15, so the hardened extractor is reached over HTTP exactly like the clone confirm reached its API) and hands the viewport to an iframe of the answered embed URL. The extract cost rides the first render; a re-render re-uses the cache (content-addressed, D386). **Rejected: extracting inside the shell before navigation** — puts filesystem mutation in the frontend router, the one layer that must stay a pure URL codec; **rejected: a reader `.py` doing the extraction** — a per-call subprocess re-implementing `zip_import`'s guards outside the one hardened path is exactly the two-extractors failure D386 refused. |
| D391 | **Export App File is surfaced on FOUR surfaces, not one: the /apps card right-click menu (D385), a hover-revealed chip on the card thumbnail, the explorer folder-row context menu (every folder, beside Compress), and an "Export App" preview-header button shown only when the previewed page IS its folder's app entry** | Owner ask: the card context menu alone made export invisible — a right-click-only action on one page is an easter egg. The three additions cover the three places a user actually is: browsing /apps (the chip makes it one visible click; hidden until card hover so the grid stays quiet, focusable so keyboard users get it without hover), browsing files (folder-row menu — offered on EVERY folder rather than probing app-ness up front, because a top-level menu item cannot be async (ContextMenu only lazies submenus) and the export route already answers the precise "not a fused app" reason, which the toast shows), and looking at the app itself (header button — this one DOES probe, via the shared entry rule `/api/apps/entry`, because a button that renders on every html page would be noise where the menu item's folder scope is not; embed mode hides the whole header, so an opened .fused never shows it). All four call the one `downloadAppFile` helper — same download, same error toast. **Rejected: a kebab ⋯ menu on the card** — a fourth chrome element to expose a menu the chip's one action already covers; **rejected: probing app-ness for the folder-row item** — a lazy submenu just to gate visibility trades a harmless no-op item for a hover round-trip on every folder. |

### Word timings, on the one engine that has them (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D392 | **`fused.ai.transcribe({words: true})` times each WORD inside a segment, on MLX Whisper only — and is answered BEST-EFFORT rather than refused where an engine has none, so a page reads `segment.words \|\| []` instead of branching on hardware** | A segment is a sentence or several — the smallest real recording in the repo comes back as one 7-second span holding "Test 1. This is another comment. This is the third comment." — so `{start, end, text}` cannot drive a karaoke highlight or a click-a-word-to-seek player at all. **The model already has this and we were not asking**: `mlx-whisper` 0.4.x ports OpenAI's cross-attention DTW aligner whole (`timing.py`'s `find_alignment`, `dtw`, `median_filter` at width 7, reached through `forward_with_cross_qk`), and `transcribe(word_timestamps=True)` returns a `words` list per segment. Verified on the real recording above against `mlx-community/whisper-small-mlx`: 11 words, 0.14–0.46s each, the pause between sentences visible as a 1.5s gap. **The alignment-head worry was checked and is unfounded** — `mlx_whisper/whisper.py` defaults `alignment_heads` to every head in the last half of the decoder (72 of them for small) and never calls `set_alignment_heads`, which would have mattered: "Whisper Has an Internal Word Aligner" (arXiv 2509.09987) measures ~80ms median boundary error from SELECTED heads and materially worse from averaging all of them. But the mlx-community conversion ships `alignment_heads` of shape (10, 2) inside `weights.npz` and the loader picks it up, and those ten indices are **byte-identical** to the base85 blob OpenAI ships for `small`. So the curated heads are what run. **Accuracy, honestly, from the one paper that publishes numbers for this exact mechanism:** WhisperX (2303.00747) Table 2 scores word segmentation as precision/recall at a 200ms collar with exact string match, and its "Whisper" row *is* the cross-attention DTW path with curated heads: 78.9/52.1 against forced alignment's 84.1/60.3 on AMI-IHM, 85.4/62.8 against 93.2/65.4 on Switchboard. Recall is where it hurts — roughly half the words on hard conversational audio land more than 200ms out — and §3.4.1 says it plainly: Whisper alone "significantly underperforms in word segmentation precision and recall". It is nonetheless the better of the two cheap mechanisms: whisper.cpp's `-ml 1` reads the probability of a timestamp token (`token.pt > thold_pt`, default 0.01) the model was never trained to emit meaningfully, though *that* comparison is design rather than measurement — no source found publishes a median word-boundary error in ms for any of these, so none is claimed here. Good enough for a highlight, not for phonetic work, and the skill says so. **The work is the remap, not the flag.** With `vad: true` a word is timed against a PACKED clip whose silence this runner removed, so each word travels the same inverse its segment does; unmapped, a word from a clip made of `0-5` and `30-35` lands 25 seconds out. **A word travels that inverse as a SPAN, not as two endpoints, and the difference is a bug Bugbot caught after the merge**: `vad.original_start`/`original_end` are asymmetric about a join on purpose, so a word STRICTLY CONTAINING one (`4.9-5.1` on the pack above) had its start mapped through the first region and its end through the second and came back `4.9-30.1` — a 0.2s token stretched over the whole 25s pause, freezing a karaoke highlight there for the rest of the file. Clamping into the parent segment cannot catch it, because a segment is ALLOWED to straddle that join. The invariant a word has and a segment does not: packing only REMOVES time, so a word's recording span must be no longer than its packed one — a segment spanning a join is honest (Whisper hears continuous speech, and both sides carry real words), one WORD spoken across removed silence is not. `vad.original_word_span` places a word in the single region holding its packed MIDPOINT (majority of what was heard; a midpoint exactly on the join takes `original_start`'s side) and clamps its interval into that region, so a straddling word comes back SHORTER — it keeps the part inside its region and the highlight lets go early instead of sitting through the silence. A word touching a join at either end is unchanged, which is why the rule is a midpoint and not "map both ends through the start's region". *Rejected: preserving the length by shifting the far end back (`end = start + packed duration`)* — it asserts the tail was spoken inside silence the file no longer contains, exactly what `original_end`'s asymmetry exists to refuse. The arithmetic lives in `vad.py` beside the packing (AI-10f) and the word POLICY stays in the worker; both public mappings and the span now share one `_region_of` walk so they cannot drift on which side of a join a time belongs to. *A word that inverts after mapping is CLAMPED, never dropped* — the opposite of `_transcribe_regions`, which drops such a segment, and deliberately: a dropped segment loses text never spoken in the recording's timeline, a dropped word loses text that IS in the transcript and breaks the one invariant a caller will build on (the words of a segment are its text, in order, complete — which is also why `word` keeps the library's leading space). **Rejected: refusing `words: true` on the two engines that lack it**, which is what every other unanswerable option does (`engine_options.UNSUPPORTED`) and what this shipped as first. The rule there exists because an ignored option is *undetectable* — a page that asked for English and got French has nothing to check — and `words` is not like that: honouring it puts a list on the segment and declining leaves the key off, so the caller sees which happened without asking anything about engines. Refusing would make one page fail on a CTranslate2 machine and work on a Mac, forcing exactly the hardware branch the identical-reply rule (AI-10c) exists to prevent. *And rejected with it, in review: the capability TABLE that refusal left behind* — a `WORDS_RUNNERS` frozenset and a `words_available(code, task=)` in `engine_options`, which shipped first. Once nothing refused, the layer had one caller passing its own code, so the frozenset membership was tautologically true and the whole thing reduced to `task == "transcribe"`: a one-member set, a lookup, a `RUNNER_CODE` constant and seven truth-table tests spelling a two-term condition. The decline is inlined in `mlx_whisper/worker.py`, `engine_options` keeps a three-line note that `words` does not belong in its table, and which engines produce words is recorded here, in SPEC AI-10h and in the skill — the only thing that layer honestly bought. **Rejected: wiring the other two engines now**, and they are not equally close. faster-whisper is nearly free: `transcribe()` takes `word_timestamps` and returns `segment.words` from the same DTW aligner (confirmed in its installed source), so it is a mapping into `{start, end, word}` and nothing else. **Parakeet is more work than "it already has the times", and the easy reading of `_sentences` is wrong**: an `AlignedToken` is a SentencePiece SUBWORD, not a word, and `parakeet-mlx` ships no word aggregation — words have to be rebuilt by grouping tokens on a leading space, which is how the library's own sentence splitter counts them. Its timings are also on a coarser grid: they come from the TDT duration head rather than DTW (no second model, no alignment), quantized to `subsampling_factor / sample_rate * hop_length` = **80ms** on parakeet-tdt-0.6b-v3, against whisper's 20ms frames rounded to 10ms. Not heuristic — the model's own trained duration prediction — but a highlight would visibly step. Neither engine is tested here, and this landed against the model that could actually be run; no page needs editing when either arrives, because a page that reads `words` when present already handles both answers. **A translation gets none on any engine**: word timings are positions in the audio and a translation's words were never spoken in it, so there is nothing to align them to — the library only *warns* here and returns numbers anyway, which reach a page as a list indistinguishable from a usable one and highlight the wrong word for the whole file. **Opt-in, because it is not free** (unlike `diarize`): an extra teacher-forced forward pass per 30s window, measured through the real worker on the 7.4s recording at 0.16s → 0.22s warm (+40%; the standalone library measures 0.13s → 0.19s), plus a **one-time ~1.3s** on the first worded call in a process while Metal compiles the cross-qk graph — the second call is 0.22s, so a cold first transcription looks disproportionately expensive and is not. It also changes the DECODE — the library gates its hallucination pruning (`word_anomaly_score`, the `hal_next_start` skip) on `word_timestamps`, so the same file can return a different number of segments with the flag on. That is documented in `_decode_clip` and the skill, because "same file, fewer segments" is otherwise filed as a bug. **No per-word confidence**, though DTW has one: it is a number only some engines can produce, and publishing it would make the reply depend on which one ran (and Parakeet's is entropy-based rather than a probability, so the two would not even mean the same thing). **`words` had to be named in `partial.py` as well, and that was a real bug caught in review**: `Sink.add` rebuilds each line key by key rather than copying the segment — deliberately, since that rebuild is what keeps logprobs and temperatures out of a file a page reads — so a public field is silently dropped unless it is named there. The symptom was narrow and permanent: `onSegment` handed a page segments with no timings while the final `.json` had them, and because the reader counts DELIVERED LINES a segment sent live is never re-sent, so a karaoke view built on the callback stayed empty for the whole run while the same page worked fine off `rec.segments`. The first test for this spied on the dict passed INTO `add` and passed regardless; it now re-reads the written file, and fails without the fix. The general rule is in `partial.py`: anything added to a segment that a caller is meant to see has to be added there too. |
| D393 | **claude's spoken walkthrough matches the transcript to clicks per WORD rather than per segment: `annRecEnd` asks for `words: true`, and `annRecAssign` flattens each segment to its word list when the engine timed one and to the whole segment when it did not — per SEGMENT, so a mixed reply degrades a segment at a time** | The nearest-start matcher (D346, D378) was only ever as precise as the units it matched, and a whisper segment is a sentence or several: the repo's own 7-second recording comes back as ONE span holding three separate comments, so three clicks put the whole thing on click 1 and left two notes empty. Word grain is what makes the ordinary way a walkthrough is spoken work — talk, click mid-thought, keep talking — because the split now falls at the click that interrupted the sentence instead of at whichever sentence boundary the decoder chose. Nothing else in the matcher moves: word starts are on the same absolute timeline as segment starts, so "nearest click wins" and the intro rule (everything before the first click is the message's own prompt, D378) are the same rules on finer units — the intro simply cuts mid-sentence now, which is the point. **The fallback needs no capability probe**, because D392 made `words` best-effort instead of refused: an engine without word timings leaves the key OFF, which is indistinguishable here from a run that never asked, so degrading is one ternary on the segment rather than a branch on which machine the page is running on. A segment whose words are not ALL timed falls back whole rather than dropping the untimed ones — no words are lost on either path. `tests/test_claude_annotation_modes.py` pins the mid-sentence split, the word-grained intro cut, and the wordless / partly-timed / mixed replies; the pre-existing segment-only tests are the regression check that the old path still behaves. **Rejected: asking for words only when the walkthrough has clicks** — the flag costs an extra forward pass per decoded window plus a one-time Metal compile (D392), which on seconds of speech does not pay for a conditional; D392's other cost is free here for a different reason — `word_timestamps` changes the decode (hallucination pruning is gated on it, so the same audio can come back as a different number of segments) and nothing on this page reads segment COUNT. **Rejected: a helper function for the flattening** — one caller, eight lines, and the matcher is exactly where "what a unit is" belongs; a one-caller layer around it is what D392's own review deleted. |

### A headless run asks for the checkpoints the snapshots panel reads (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D394 | **The spawned session sets `CLAUDE_CODE_ENABLE_SDK_FILE_CHECKPOINTING` (via `setdefault`), because file-history checkpointing is OFF by default in a non-interactive session and every run this template spawns is `-p`** | The panel shipped structurally blind to its own chat's work. Diagnosed on a real canvas: one session had made 4 `Edit` calls against `showcase/local-transcription/index.html` plus a `Write` of a new file, and `~/.claude/file-history/<that session id>/` **did not exist** — so the landing page printed "Claude has no recorded versions of this file" for a file it had rewritten minutes earlier. Confirmed by controlled experiment rather than inferred from the absence: a `claude -p` edit in a scratch dir wrote 0 store files, while 79 of the 111 interactive sessions in the same project dir had checkpoint dirs. The gate is `fileHistoryEnabled`: a remote workspace is always false, a NON-INTERACTIVE launch returns `CLAUDE_CODE_ENABLE_SDK_FILE_CHECKPOINTING && !CLAUDE_CODE_DISABLE_FILE_CHECKPOINTING`, and only an interactive one consults the `fileCheckpointingEnabled` config. **An env var is the only reachable switch** — `--settings` cannot touch a branch that never reads settings — and once set the CLI writes the same `<session>/<key>@vN` layout `file_history._scan` already enumerates, verified by a second run whose `@v1` held the exact pre-edit bytes. So the reader, the numbering, the ghost boundary and `_snap_target` are all untouched: the fix is one line in `spawn_env`. **`setdefault` rather than assignment** because the value is coerced (`1/true/yes/on`, everything else false), so a user's explicit `=0` is a real opt-out and this template must not re-enable a store they turned off; `CLAUDE_CODE_DISABLE_FILE_CHECKPOINTING` is ANDed into the same branch and wins regardless. **Rejected: a `PreToolUse` hook writing our own checkpoints**, which was the design before the gate was found. It works — hooks do fire under `-p`, `--settings` merges with the user's own hooks rather than replacing them, and the payload carries `session_id` and `tool_input.file_path` — but it costs a python spawn in front of every edit, has to reimplement the CLI's cross-session `@vN` numbering (of 564 chains in one real store, 288 start at v1 and 265 at v2, so the counter follows the FILE, not the session, and a per-session guess would print two rows both labelled v1), cannot represent the did-not-exist boundary that lives only in the transcript (FH-3, `_ghosts`), must refuse mount-backed paths before any stat from inside the tool's critical path rather than a render (CT-12), must never exit non-zero or it BLOCKS the edit, and makes `file_history.py`'s "strictly read-only with respect to the Claude config dir … a guest in it" false. Every one of those is a way to get a wrong chain; the env var has none of them. **Rejected: naming it in `--settings` beside the per-run `mcp.json`** — settings are not consulted on that branch, so it would have looked configured and done nothing. **Named for the SDK and absent from the public settings docs**, so it may move: the comment at the call site names the gate (`fileHistoryEnabled` → the non-interactive branch) so the next person can re-check it if snapshots go quiet again. **Does not backfill** — the edits already made are still invisible, and nothing can reconstruct them. **Still not a complete chain:** only the edit tools checkpoint, so a `sed -i` from Bash remains unrecorded, which is the fact FH-3 already refuses to paper over. |

### An annotation-only chat is named by the note on its pin (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D395 | **`_cli_preview` falls back to the `content` of the message's annotations when the machinery strip leaves nothing — as a SECOND source beside `_strip_machinery`, never folded into it** | The page has let a message carry annotations and no words since the "apply the comments" prefill was deleted (`annPrefillComposer`: "the comments ARE the content, so an annotation-only send goes out with an EMPTY message"), and no reader was told. `_strip_machinery` peels the app-state block, the pane shots and the annotation preamble — correctly, they are machinery — and for such a send that is the WHOLE message, so the preview came back `""`. That is not merely a nameless row: `_cli_sessions` **drops** a session with no preview, so two real chats about `showcase/local-transcription/index.html` were absent from "Recent chats" altogether, and the snapshots panel below could only call their chains "chat" plus a short id. Both symptoms, one `""`. The words were in the record the whole time — each pin carries a `content` field, which `formatAnnotations` puts on the wire along with everything but `id`/`sent`/`createdAt`. **A second source rather than a wider stripper**, because `_strip_machinery` answers "what did the human TYPE in the composer", it is duplicated as `tasks_store.strip_machinery`, and the two are pinned character-identical over a shared corpus — widening it would change every reader of both copies at once, including the Tasks list's is-this-worth-a-row test (`strip_machinery(...)` is what `tasks_store` asks). So the fallback lives at the one call site where a nameless row costs the whole session. **Free text still wins**: a send with both is named by the typed words, which are the thing written to be read; the pins are consulted only on `""`. **Multiple pins join in `t` order** with " · " — that is the order the walkthrough was given in and the caller truncates to 80 chars regardless. **A pin with NO note still yields `""`**, which keeps the empty answer's original job: a bare screenshot and an unwritten pin are not titles. **The corpus was the reason this survived review:** `_machinery_records.ANNOTATION` was trimmed from a real 2026-08-17 transcript whose pin had no `content` at all, so the fixture agreed with the code and the test suite was green over a send shape the page produces daily. A second record (`ANNOTATION_NOTED`) now carries the field, taken from the transcript that exposed this. **All FOUR readers move together**, which is the whole point of this family of functions: the same send was a nameless row in the Tasks list and in the shell's session picker, both of which read `tasks_store`. So `ann_notes` is a **third copy** of an annotation rule (`template.html`'s `stripAnnBlock`, `agent.py`'s `_ann_notes`, this) — unavoidable under D166, a template may not import `fused_render` — and it is pinned to the second one over a shared corpus by `test_claude_sessions_merged.py`, the malformed payloads included, because a row's title is not worth an exception on either side. **Rejected: leaving the server readers for later**, which would have restored the exact divergence the shared stripper was introduced to end — two surfaces disagreeing about what one chat is called, with nothing failing. |

### Exported apps are discoverable on the hub (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D396 | **Exported `.fused` files are discoverable and first-class on the hub: /api/apps merges every indexed `.fused` on the machine under the reserved virtual tag `Fused-App` — which names the ARTIFACT and prints on the card's own tag line but is deliberately NOT offered as a Repo facet chip (`fused_render/exported_apps.py` — one duckdb query over the index files table, `ext='fused'`, ~20s TTL cache, git_repos-style junk_path/MountGuard/isfile screening and freshness nudge, but NO not-ready states: an unanswerable index is zero exported rows), unioned with a new `~/.fused-render/appfile_recents.json` store that `POST /api/appfile/open` feeds — which is also what puts opened exports on Home's recents row and in recency order everywhere. Rows carry `kind: "appfile"` with `entry` = the `.fused` path itself and `entry_html` null; the card's thumbnail is the payload's `preview.png` via `GET /api/appfile/preview` (bounded single-member zip read, 404 → empty thumb, zero zip IO in the listing), and export bakes a tab-capture screenshot in as `files/preview.png` when and only when the folder has no authored one (X-Fused POST export variant; PNG magic + 8 MiB cap). `registered_apps` refuses paths under the extract cache** | The file index already held every `.fused` as an ordinary row, so discovery is an index fact like the repos tab — no new scan, no walk. Silent-omit on index failure (vs git_repos' 502/not-ready) because /api/apps must keep serving workspace + registered apps whatever the index's condition; the recents union is what bridges index staleness for the file the user just exported. `entry`=the file/`entry_html`=null makes the whole open path fall out of appEntry.ts unchanged (a click is a template resolution, D390) while keeping /render unpointable at a zip. Recency records at the open POST — the one moment the SOURCE path is known; the extract-cache refusal kills the duplicate `linked` card whose path the user never chose (revises D387/AF-8). Thumbnails read the zip member card-side on demand rather than probing membership in the hot listing. The tag reads `Fused-App`, not `exported`, because the chip and the card's tag line print the string VERBATIM (there is no label table): a card's tag answers "what is this", and the act that produced the file is not its identity — every other row on the hub is named for a thing (a tag dir, `linked`) rather than a verb. **And it is not a Repo chip at all** (`repoChips`, tested beside `orderCategories`): that facet groups by SOURCE — the workspace tag dir a folder was scanned out of — and a `.fused` sitting anywhere on disk has none, so a chip there would have been the only one that filtered by kind while claiming to filter by origin, and it duplicated a label the card was already showing. The exclusion keys on `kind: "appfile"` rather than matching the tag text, so rewording the tag cannot quietly restore the chip; the rows stay reachable by an explicit `?tag=` URL (the All chip clears it) and by the search box, which is what keeps this a chrome decision rather than a filtering one. The capture is getDisplayMedia with current-tab hints (the annotation shots' mechanism, owner pick): live pixels have no blank-WebGL failure mode, where DOM serialization rasterizes canvas/map apps blank and would bake the blank into the artifact forever; the cost is one share prompt per capture, and every capture failure exports plain. **Rejected: reusing registered_apps.json for appfile recents** — its record_open validates isdir+app_entry, and loosening a store whose every row feeds syscalls weakens the listing's posture; **rejected: reading manifest `name` for card titles** — a zip open per file per listing render for a string the filename already approximates. |

### A read-only app file has a way out (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D397 | **A `.fused` clones into the workspace from a preview-header button — `<workspace>/local/<slug>`, plain files, and the DESTINATION FOLDER EXISTING is the whole definition of "already cloned" (the button then reads "Go to local version" and only navigates)** | An opened `.fused` is read-only on purpose (0444 payload, RO-7/D386) and embed mode has no editing surface at all, so up to now the artifact was a dead end: you could run someone's app and not develop on it. Clone is the way out, and it is deliberately the SHOWCASE CLONE's shape — `CloneCommunityButton` copies a showcase app into `Fused/local/<slug>` and opens the copy, so the second thing in the app that hands you an editable copy of a read-only one should not invent a second vocabulary, a second destination, or a second button position. **Presence, not a record, is the state** (owner's call). A records file keyed on the source path loses the answer when the file moves; keyed on the content hash it loses the answer when the app is re-exported — and both go stale against a folder the user renamed or deleted, which is the one thing an `isdir` cannot get wrong. So `clone_target` is one bounded manifest read plus one `isdir`, cheap enough to run on every preview of a `.fused`, and there is no store to migrate, reconcile or garbage-collect. The named cost is the converse and is accepted rather than hedged: an UNRELATED folder the user keeps at `local/<slug>` reads as this app's clone and the button offers to open it. *Rejected: `community._claim_dir`'s `-2..-9` suffix loop*, which is what the neighbouring code does on a collision — the owner rejected it outright, and the reasoning holds on its own: a suffix invents a path nobody asked for, and "clone twice, get `sine` and `sine-2`" is a worse answer to "I already have this" than "here is the one you have". A second Clone is therefore a REPORTING no-op — it copies nothing, so a user's edits in the clone cannot be clobbered by a button they pressed twice. **The copy rides `open_app_file`, not a second unzip** (D386's one-hardened-extractor rule), so a file never opened before extracts on the way through and nothing re-implements the zip guards. Two mechanics are load-bearing: `copytree` carries the extract's **0444** across, so the bits are lifted to 0644 **in staging, before the rename** — a clone that shipped them read-only would be an uneditable "development copy", i.e. the whole feature failing silently, and lifting after the rename would make the copy briefly visible in the workspace as unwritable; and staging sits INSIDE `local/` so the claim is a same-filesystem `os.rename` (community's reason: a home-dir staging area can be on another volume, where the rename fails outright). A lost rename race answers `cloned: true`, the same reading as finding the folder there to begin with. **The manifest `name` is attacker-controlled zip data** and reaches the filesystem only through `_slug`, whose character class admits no separator, no dot and no drive letter — `"../evil"`, `"/etc/evil"` and `"C:\\evil"` collapse to ordinary segments — and it is the SAME slug the extract cache key already used, extracted into a shared helper so the two namings cannot drift. An empty name falls back to the **file stem**, not a shared `"app"` literal: two unnamed app files would otherwise collide on one `local/app` folder and the second would read as the first's clone, which is the presence rule turned into a bug. *Rejected: `git init` with a pristine first commit*, which the showcase Clone does — owner's call, and the asymmetry is real: the showcase clone has an UPSTREAM to diff against and to fast-forward from, while a `.fused` is a snapshot whose only baseline is the copy itself; the app-git machinery picks the folder up once it is edited anyway. *Header-only, so a double-clicked `.fused` shows no Clone* — embed mode hides the whole topbar by construction (D386/D390), which is also why `ExportAppButton` never appears there. Owner's call over adding a bar inside the fusedapp template: the embed stays chrome-free, and reaching Clone means opening the file in the explorer. The GET probe is unguarded like `/api/appfile/preview` (it reports a path and touches nothing); the POST carries the D3 X-Fused guard like `/api/appfile/open`, because it writes. |

### The explorer menus say what they do (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D398 | **The listing's right-click menu for a single FOLDER is two items — "Reveal in Finder" and "Open in New Tab" — while a FILE keeps the full list, which gains "Open in New Tab"; and the clipboard action is labelled "Copy Claude session command", not "Open in Claude Code"** | Three findings, one menu. **(1)** The folder menu was the file menu plus Compress/Export, which meant a folder's most common intents (walk into it, show it to the OS) sat below eleven rows the reader had to scan past, several of which act on a whole tree from one mis-click (Delete, Rename, Paste). Two items is the whole common case; the destructive tree operations remain reachable from a file's own menu and from the selection (multi-row) menu, which are unchanged, as is the background menu. Accepted cost: Compress has no surface until it is given one — the action (`loadCompress`/`doCompress`) is retained rather than deleted, and its now-unreachable branch in the file list says so, because re-adding an entry is a line and rebuilding the git-repo probe is not. Export App File still lives on the app card menu and the preview header. **(2)** "Open in New Tab" opens exactly `urlForFsPath(path)` — the url the row's ordinary click pushes — via `window.open(url, "_blank", "noopener")`, the same shape Tabs/Panel already use for their new-tab items. No search string, since `navigate` also starts a destination on a fresh query string, so the tab lands where a click would. **(3)** The old label promised an action the item stopped performing at D306: it copies `cd '<dir>' && claude` to the clipboard for the user to paste in the terminal they already have. A menu item that says "Open" and opens nothing is read as broken, and the toast ("Command copied — paste it in your terminal") was the only thing telling the truth. Renamed on all three surfaces that offer it (row menu, background menu, and the crumb bar's file menu in `lib/bar-menus`, whose comment already bound its wording to the background menu's). |
| D399 | **A right-click on an ANCESTOR crumb in the path bar opens the two-item ancestor menu ("Reveal in Finder", "Open in New Tab") for THAT folder — not the bar's own menu; and the listing's background menu gains "Open in New Tab" beside "Reveal in Finder"** | Two gaps left by D398. **(1)** The crumb bar answers every right-click with the menu of the view under it (`topbar-menu.ts`), and over a listing that is `folderBarMenu(backgroundMenu(), splits)` — New Folder, New File, Paste, Refresh, Reveal, Copy path, Copy Claude session command, Split right, Split down. Every one of those acts on the folder being LISTED, so right-clicking "Fused" in `~ / Fused / lle` offered to create a file two levels below the crumb the pointer was on, and threw in two splits that had nothing to do with a path segment. The bar cannot build a menu (it has a path and nothing else — the whole argument in `topbar-menu.ts`), so the crumb NAMES itself to the owner instead: `TopbarMenuOpener` takes an optional `crumb` path, and both publishers (`listing/useFileOps`, `Preview`) answer it with `lib/bar-menus`' new `crumbMenu` — item for item the folder-row menu of D398, because a crumb and a folder row are the same thing pointed at from two places. **Ancestors only, decided by selector** (`a.path-crumb:not(.last)`): the current folder's crumb is a `<span>`, and the root's carries `.last` when the listing IS the root, so "the folder you are already in" keeps the bar menu — there the crumb and the current directory are one folder and the full list is the right answer. The path comes from `data-fs-drop-path`, already on every crumb for the spring-load drop targets, so no second source of truth for what a crumb points at. **(2)** The background menu had no "Open in New Tab" at all, though it is the one menu whose subject is a directory the user is standing in; it is added beside "Reveal in Finder" — the pair reads "this folder, but elsewhere" — on the current directory (`normDir(base)`), through the same `doOpenInNewTab` the row menu uses. |
| D400 | **The `claude` chat is bound to EVERY key in the built-in registry, not a hand-picked authored-file set — D235's file-side binding is widened rather than curated, and the "is this target worth a chat" question moves out of the registry and into `claude/condition.py`** | The binding was chosen extension by extension on the rule "chat only where a human authors or analyses the bytes", which read as principled and behaved as a guess: it decided that a `.mp4`, a `.duckdb`, a `.tar.gz`, a `.glb`, a `.xlsx` and a `.pdf` were nothing anyone would want to ask about, while a `.txt` was. Every one of those is wrong in an ordinary session — "what is in this archive", "what does this warehouse hold", "summarise this PDF", "transcribe this clip" — and the shape of the mistake is the point: an EXTENSION cannot answer whether there is something to say about a file, so a list keyed on extensions is the wrong place to ask. Owner call (2026-08-20). **What replaces it:** nothing new. The gate the mode already ships is the filter now, and it asks about the target actually in front of it — an existing regular file or an existing directory, and never a path under the mounts dir, since an agent turned loose on a FUSE mount walks and rewrites a remote tree. A curated key list on top of that gate was never a second line of defence, only a second, staler opinion. **Ordering:** `claude` is a companion, never a default, so it is appended after the content views on every key — but AHEAD of `reader`, which keeps the trailing seat it has held since D235 (five keys — `.xlsx`, `.xlsm`, `.docx`, `.odt`, `.pdf` — were reordered to restore that, and the rule is now pinned over the registry rather than sampled per key). The universal `/` directory key is the one exception to "never first": it LEADS there, unchanged, for D280's reason. **`.zarr/` had to be edited by hand**, though directories already carry the chat from `/`: `_match_registry` picks ONE winning key and uses its list whole — it never merges — and `.zarr/` has a segment where `/` has none, so it outranks and REPLACES the universal key. Left alone, a Zarr store would have been the only directory on the machine with no chat. **Rejected:** keeping the curated list and extending it type by type as complaints arrived — that is the same guess with a longer tail, and it puts a registry edit between the user and a capability the gate would already have allowed. |

### An app's Python entrypoints, curated into MCP tools (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D401 | **An app folder declares its Claude-callable tools in its own `mcp.toml` `[[tool]]` tables, and registration points an MCP host at `fused app serve <folder>`** | The capability was already there and unreachable: an app is a page over Python that runs locally with the user's own credentials, and a Claude session on the same machine had no way in. What was missing was a DECLARATION — which entrypoints are worth calling, under what name, with which parameters fixed — so the manifest is the whole feature and both halves are built around it. This app owns the authoring half (SPEC §43: the folder gate, the AST read of the app surface, the drift check, the `fused.ai` proposal, the write) and the `fused` package owns the serving half (openfused `spec/serve/app-mcp.md`: one MCP tool per `[[tool]]`, its schema the entrypoint's signature minus the pins, executed on the LOCAL compute backend). Neither repo parses the other's files; the TOML is the seam. **`mcp.toml`, not `openfused.toml`:** `fused` resolves projects by walking up from the cwd looking for that second name, so an app carrying it would silently start resolving as a project for every command run inside it. **Rejected: a section in `metadata.json`** — that is this app's file, and the server would have to learn to read it. **Rejected: generating a standalone MCP server per app** — a script per folder to keep working, versus one CLI subcommand that reads a declaration. **Registration reuses `claude mcp add-json --scope user`** through the existing `/api/claude-config/mcp` module rather than writing `~/.claude.json`: one owner for the host config, name guard and CLI included. The entry pins the CLI **the server itself exported** (`appenv.fused_cli_dir()`, D334) and never a PATH lookup: this command is written into a global `~/.claude.json` and run later, unattended, so `shutil.which("fused")` — which is what it did first — would let a stray pipx shim on a machine whose venv lacks the `[fused]` extra be registered as this app's server, with no `FUSED_ENV` and failing where nobody is looking. When no CLI is available Register is disabled with the reason on screen. **The app's PAGE is the tagged one** (`<meta name="fused-app">`, D301) for both the gate and the pin hints — an `index.html` rule was tried, is cheaper by a listing, and was wrong twice over: no pill for an app whose entry is `mail.html`, and pin hints read out of an untagged `index.html` sitting beside the real entry. **A pin keeps its TYPE** end to end; a pinned `bool` False written as the string "False" is read as truthy by the entrypoint, i.e. a pinned-off safety flag behaving as on. **Pins are applied over the caller's arguments, not under them:** a pinned parameter is absent from the schema, but a client can send whatever JSON it likes, and a pin a fabricated argument could override would be a curation the operator only believes is enforced. **Drift is a signature snapshot this app writes and the server ignores** — a re-derived schema means a drifted tool keeps working, so the panel reports it (and Save re-records it) instead of refusing to serve. **The AI is a proposal, never a writer:** the model sees names, signatures and the page's own `runPython` arguments — never file contents — and its output lands in an editor, because it is guessing at intent and the manifest is what a Claude session will act on. Manual entry is the full-capability path, so a machine with no Claude CLI loses the suggestion and nothing else. **No gating layer beyond the host's per-call approval:** the person who curated the tool is the person the host will ask. |

### Signing in to Hugging Face: hf owns the token, the app owns the button (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D402 | **Preferences gets a "Log in to Hugging Face" button that drives `huggingface_hub`'s own device-code browser login; hf persists the token and this app stores no credential at all. `huggingface_hub` becomes a core dependency, and `hf_auth.token()` (= `get_token()`) is the one reader everything downstream goes through** | Reported as the line every worker log carries: "Warning: You are sending unauthenticated requests to the HF Hub. Please set a HF_TOKEN to enable higher rate limits and faster downloads." Not cosmetic — anonymous means a lower rate limit, slower CDN service, and no gated or private repos at all — and the app's previous answer was HS-0's: **"there is no credentials UI and this does not add one"**, sending the user to a CLI they may not have installed to fix a warning in a log file they have no reason to open. Read literally, that clause survives this change and is more true than a paste box would have made it: **there is still nowhere in this app to type a secret**, because the credential never passes through it. **The flow is a device code because the middle of it is a person.** `POST /api/hf/login` asks hf for a code and returns the URL and short code **without waiting**; a daemon thread parks in `poll_device_token` (up to the code's ~15-minute life, which is why it cannot be a request); `GET /api/hf/auth` is what the page polls, carrying the pending code, the account, or the failure. A second login POST **joins** the one in flight — two device codes are two codes on the Hub's page with only one of them being polled, the supervisor's join-don't-restart rule — and building that reply inside the flow lock deadlocked it (`_state()` takes the same non-reentrant lock), which the test written for the join is what caught. Cancel works by raising from hf's `on_pending` hook, the only way out of a thread inside that loop; pinned by a test that drives hf's real polling loop with the Hub stubbed one layer down, rather than by reading hf's source. **Logout removes the ACTIVE token's entry by name** (`_logout_from_token`), never hf's public `logout()`, which deletes every token on the machine and the git credential too: a settings button must not sign the user out of logins it did not create. That works because hf's store holds several NAMED tokens with one active, which is also what lets a login made here coexist with one the user made themselves. **`HF_TOKEN`/`HUGGING_FACE_HUB_TOKEN` still win and the page says so** rather than showing a dead control: hf reads them ahead of its own files, so both login and logout 409 while one is set, naming the **variable** and never its value; an exported-but-empty value is not force (D148). `account` is the username from a login this process performed, else hf's stored token name with its `oauth-` filing prefix stripped, else null — "signed in but unnameable offline", because a `whoami` per status poll would put a network round-trip behind a settings page. **One reader, no second resolution:** the Hub search calls `hf_auth.token()` and a download reaches the same answer by calling hf inside the worker, so the two cannot disagree about which credential the machine holds; nothing is cached, because hf refreshes an OAuth token in place as it nears expiry. `supervisor._child_env` writes **no** `HF_TOKEN` — an inherited one is passed through untouched. **Two consequences that had to be handled, not discovered later.** (1) `worker_base`'s stdlib-only rule was enforced by hf being ABSENT from CI — a module-scope import would have failed on import — and with hf installed that check would pass forever while the rule rotted, so it is now enforced by reading the module's own module-scope imports out of its source. (2) The macOS bundle force-lists hf, `hf_xet` and `filelock`: every import of hf in this app is INSIDE a function, which is exactly the shape that ships a DMG whose button reports an `ImportError` — the `pip install python-pptx` failure `setup_py2app.py` documents, in a new place. Windows and Linux install the wheel, so a core dependency reaches them for free. **Accepted:** hf is a core dependency (hf + `hf_xet` + `filelock`, ~16MB, into a wheel that already ships pyarrow at 152MB). This is NOT SPEC AI-2 being widened — that rule keeps ML *frameworks* (Metal-only mlx, GB-scale torch) out of a file explorer's dependency set, and each runner still declares hf for its own venv. **Also accepted:** four of hf's underscored names are load-bearing (`_save_oauth_token`, `_logout_from_token`, `request_device_code`, `poll_device_token`, plus `utils._auth.get_stored_tokens` for the offline account label). hf's own `cli/auth.py` drives the same pair, so it is a seam rather than a trespass, and `test_hf_auth.py` imports them explicitly so a release that moves one is a red test here instead of a login that fails in front of a user. **Rejected: storing a pasted token in prefs.json ourselves** — built first, and it made the app a credential store: a masked-hint payload to describe the token without leaking it, printable-ASCII validation because the string reached an `Authorization` header and a child's environment, a three-level precedence rule of our own, and a line injecting `HF_TOKEN` into every worker. Letting hf own the storage deleted all of it, and fixed something that design could not: an OAuth login carries a refresh token hf renews, where a pasted token silently expires. The objection that killed it earlier — that hf-owned storage needed either a network round-trip on save (`login()` validates via `whoami`) or a destructive `logout()` — is avoided rather than accepted, by driving the writers underneath both. **Rejected: an "hf management venv"** — a tiny in-package project folder declaring only hf, built by `uv sync` (measured: 24MB, sub-second) and driven by a subprocess per save. It keeps hf out of the app's dependency set, and that purity is not worth a `uv sync` on a settings write, a venv to garbage-collect, and a save that fails when `shutil.which("uv")` does: the app refuses model *loads* without uv, but a settings control should not. **Rejected: writing `~/.cache/huggingface/token` by hand** — reimplementing hf's storage rather than delegating to it, and it would leave hf's second file (`stored_tokens`, whose section name comes from `whoami`) disagreeing with what is active. **Rejected: a paste-a-token box beside the button** (owner's call) — one path, and the app never handles a token string; somebody who needs a specific fine-grained token exports `HF_TOKEN`, which the section reports as being in force. |

### Default model and Hugging Face get their own tab (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D403 | **Default model and Hugging Face move off the Render preferences tab into a new AI tab (`?tab=ai`), and the signed-in state drops its trailing "downloads use this account" line** | Neither control is about rendering, which is the same objection that moved the engine picker off this page entirely (D125-era note in `Preferences.tsx`: it was "the one control on this page about MODELS rather than about rendering"). The difference is where they went: the engine picker had a page where its question is actually asked (/ai-models, whose Discover and cached-model rows are the consequence of changing it), and these two do not — a default Claude model is consulted by the chat and by `fused.ai()`, and a Hub login by every model download, so no single page owns either. So they stay in Preferences and get a tab of their own instead. **Cost of the old placement:** on a five-section Render tab, a reader who came to sign in to the Hub scrolled past Appearance, Default model, and Call log — and one who came for the model picker passed Appearance to reach it and then three more sections that had nothing to do with it; the two AI controls were adjacent to each other and to nothing else they belonged with. **One tab, not two:** they are one question asked twice — which model, and with whose credentials — and a tab holding a single `<select>` is a tab that reads as an oversight. **Named for the subject, not the contents** ("AI", not "Models" or "Model & Hugging Face"): a third control lands in it without renaming it, and "Models" would collide in the reader's head with /ai-models' engine picker, which is a different question about a different noun. **Render preferences stays the clean-URL default** and an unknown `?tab=` still falls back to it: unlike `?tab=engines`, this move owes no `rewriteLegacyUrl` entry, because `/preferences` still resolves to the page it always did — two sections lighter, with the tab strip naming where they went. The gated-repo copy that says where to get a token (`discoverView.gateChrome`, and `hub_models`' 401/403 message) now says **Preferences → AI**: "in Preferences" was accurate when the section was on the landing tab and became a small scavenger hunt the moment it was not. **Also dropped (owner's call):** the muted line under a successful sign-in, "Model downloads and Hub search use this account." "Signed in as ‹name›" beside a Log out button already IS that state, and the paragraph above the button promises what signing in buys; a third restatement made the settled case the noisiest of the three. The two states that still carry a sentence are the ones no control can show: a `HF_TOKEN` overriding hf's store, and what staying anonymous costs. The intro paragraph lost "and keeps it renewed; this app never holds it" for the same reason — custody is the subject of SECURITY.md and D402, not of a settings blurb. |
| D404 | **The listing's right-click menu for a single FOLDER is the full list again (Open, Open in New Tab, Open With, Delete, Rename, Duplicate, Compress, Export App File, Cut/Copy/Paste, Copy Path, Reveal in Finder, Copy Claude session command) — the two-item menu of D398 survives only where D399 put it, on an ANCESTOR CRUMB** | D398 read one complaint about the crumb bar as a complaint about folders everywhere and trimmed both surfaces; the owner's clarification is that only the crumb was ever meant to be trimmed. The two surfaces are not the same thing pointed at twice, which was D398's premise: **a row is a thing you selected**, so the editing verbs have a subject and Finder puts them there; **a crumb is a navigation handle** on the way to somewhere else, and Rename/Delete/Paste on a segment of the path you are standing inside is an answer to a question nobody asked. So the row branch is deleted, not re-tuned — a single row, file or folder, falls through to the one shared list, and `row.isDir` now only gates the two entries that genuinely are folder-only (Compress, Export App File), which stops being dead code the moment the branch goes. **Two things from D398 stay, both of them right on their own merits:** "Open in New Tab" is on the folder row beside Reveal (it is what a folder row most wants that the list did not offer), and the label "Copy Claude session command" keeps telling the truth about what the item does (D306). `crumbMenu` and the background menu's new-tab item are untouched — D399 stands as written; only its comment loses the sentence that defined the crumb menu as "whatever the folder ROW gets", which is now false and was always the wrong direction of dependency. **Cost undone:** Compress and Export App File get their surface back, so the "unreachable but retained" note D398 accepted is gone with the branch that created it. |

### The tasks List sorts by recency inside each status (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D405 | **Inside a status rank, the tasks LIST orders rows by the time the row itself PRINTS (`taskWhen`) — descending everywhere, ascending in Upcoming, and a row with no time at all goes last. Ties keep the server's order, compared by incoming index rather than left to engine stability** | The rank was only half an order. The other half was "whatever the server said" (`sortByLane` was a bucket-and-concat precisely so it would be), and that read as principled in the source and looked random on screen: the server sorts the FULL list by `last_active`, but a row does not print `last_active` — it prints its next run, else its last run, else `last_active` (`taskWhen`) — so two adjacent Done rows could show "2h ago" above "10m ago" with nothing on screen able to explain it. **The key must be the key the reader can see**, which is why this sorts on `taskWhen(task, now).at` and not on `last_active`, and why a test feeds a row an old run with a fresh `last_active` and pins that the order does not move. **The DIRECTION is read off the board's own map** (`LANE_SORTS`, the same map `taskWhen` consults to decide which stamp a row gets): the one rank whose rows print the run AHEAD — Upcoming — runs ascending, so an overdue run is the top row, the same spirit as the board's Upcoming lane; every other rank runs most recent first. Archive takes descending too, though its board lane is `server`: the board leaves Archive alone because a truncated message window is a worse key there than `last_active`, and that argument is about which TIME to sort by, not about whether an ordered rank should be ordered — the list has already committed to printing a time on every archived row. **`kind: "none"` (the em-dash row) is last in its rank in both directions**, held apart from real times as `null` rather than sorted as its `at` of 0, for `laneTime`'s reason: 0 is 1970 and would lead an ascending rank by accident rather than by decision. **Ties compare the incoming index explicitly** (`sortLane` rule 1) — `Array.prototype.sort` is stable in every engine this ships to, but relying on that makes "two runs in the same second do not trade places between polls" an accident of the runtime instead of a rule the file states. **`now` is read once per call** and handed to every rank, so the whole list is placed against one instant. **The caller does not thread `now` through a memo dep**, matching what the Board already does with `groupByColumn`: the order can only go stale when a run happens, and a run that happened is a new `tasks` from the poll — so the poll that invalidates the order is the same poll that re-runs the memo with a fresh clock, where a ticking `now` would re-sort every second to produce the identical list. **Rejected: sorting by `last_active` to match the server's own key** — cheaper, and it is the key the row does not show; the complaint was never that the order was wrong in the data, it was that the order was unreadable. **Rejected: headers or dividers per rank to explain the grouping** — settled already and for the same reason it was settled then: the grouping is a sort, and this change is what finally makes the sort legible without them. |

### Withdraw the parakeet-mlx speech-to-text runner (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D406 | **Remove the `parakeet-mlx` (Parakeet-TDT) speech-to-text runner D319 added — the registry row, its `parakeet_mlx/` folder, its catalog shortlist, and its three `engine_options.UNSUPPORTED` entries — leaving `automatic-speech-recognition` served by exactly two runners, MLX Whisper then Faster Whisper (CTranslate2). `formats.is_parakeet_checkpoint`, `NEMO_ASR_TARGET` and `PARAKEET_WEIGHTS` are KEPT, and so is the early-return branch that reads them — it now claims NO runner instead of `parakeet-mlx`** | Owner call: the engine was not used enough to justify carrying it — a Metal-only runner with a bespoke `load_audio`-swapping decode path, its own refusal table, and its own shared-module obligations (`vad.py`, `diarize.py`) for a capability the other two engines already covered. **The trap this removal had to walk around, and did not fall into:** a Parakeet/NeMo ASR snapshot is a directory of safetensors (`model.safetensors`) shaped exactly like a transformers text checkpoint, distinguished only by a `nemo.collections.asr.models.…` `target` in `config.json`. D319's `loaders()` branch matched that shape and returned `("parakeet-mlx",)` with an early return, precisely so the text branch below it could never also claim the same directory and offer a speech model as a chat model. Deleting the whole branch along with the runner would have deleted the return with it, and the text branch would then have claimed every cached NeMo ASR repo — turning "this app cannot load this" into "this app offers to load this as a chatbot and fails several frames into mlx-lm or transformers." So the branch stays and still returns early; only its payload changed, from one runner code to none. `DECISIVE` loses the `"parakeet-mlx"` entry (there is no code left to list) but the reasoning comment stays, now pointing at the early return in `loaders()` as where a NeMo ASR snapshot's decisiveness is actually enforced. **`engine_options.UNSUPPORTED` is now `{}`.** The module and `unsupported_or_raise` stay — both the transcribe endpoint and (structurally) a future worker still need one place to declare a refusal in two doors — but nothing registered today refuses anything, so the table is honestly empty rather than deleted along with the one engine that populated it. `tests/test_ai_engine_options.py` pins the empty table explicitly rather than leaving it implicit. **`runners/vad.py` stays at the runners root** rather than moving back into `mlx_whisper/`: it moved there under D319 because a second caller existed, that caller is gone, but the shared location costs nothing with one caller and avoids a second move the day another ASR engine needs it. **Accepted, known gap: an MLX conversion of a Parakeet checkpoint is still discoverable and downloadable on the Hub Search tab.** Such a repo reports `library_name: "mlx"`, which `hub_models.py`'s `_UNRUNNABLE_LIBRARIES` denylist cannot block without also hiding every repo MLX LM, MLX FLUX and MLX Whisper actually load — the denylist is keyed on library, not on library-plus-config, and reading `config.json` per search result to catch this one case is a cost this filter deliberately does not pay (see its own "what this claims" comment). So a user can still download `mlx-community/parakeet-tdt-0.6b-v2` (~2.5GB) from Discover and land on a card with no engine tag and no Load button — `formats.py`'s trap still catches it, just after the download instead of before it. Recorded rather than fixed: fixing it needs a new mechanism (`hub_models.py`'s comment names the shape), and this change is a removal, not a new filter. Same reasoning for `diarize.py`. **Comments and docs updated, not silently dropped**: SPEC AI-10g is marked REMOVED with the reason and points at what survives; D319 above carries a withdrawal note; `skills/fused-render-ai/SKILL.md` (the copy shipped into every user's `~/.claude/skills/`) had its runner-count table, its "three engines" section heading, its progress-resolution note and its whole Parakeet-refusal bullet rewritten for two engines — the refusal bullet was deleted outright rather than kept as a hypothetical, since no engine refuses anything now. **Rejected: deleting `is_parakeet_checkpoint`/`PARAKEET_WEIGHTS`/`NEMO_ASR_TARGET` along with the runner** — the format constants have no runner to serve any more, but they are what makes the early return possible, and the early return is the only thing standing between a cached NeMo ASR snapshot and a false chat-model offer; a "clean" removal that deleted them would have reintroduced the exact bug D319 fixed. **Rejected: moving `vad.py` back into `mlx_whisper/`** — tidier for a single caller today, but it is a refactor this change does not need and it undoes work for free the day a second MLX ASR engine is added. |

### The /apps hub paints a partial grid from recents while the exhaustive catalog loads (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D408 | **The /apps hub draws Home's recent-first row as a REAL partial grid while the exhaustive catalog loads, keeps the last catalog for the next visit, and lets a card's queued preview outrank the lookahead rows by being ON SCREEN** | The hub's cost was never one number. `GET /api/apps` is a recursive workspace walk plus a full-table index query — a few hundred ms on a cold page cache, ~50ms warm — and the page showed a skeleton for all of it; then it mounted every card at once and the two-slot preview queue (D365's iframes) admitted them in REQUEST order. So three separate waits, and the fix for each is different. **The partial grid is real cards, not a placeholder:** `/api/apps/home` already answers the same `app_dict` shape from the two recents stores by explicit path, and `sortApps` is recency-first, so its rows are exactly the ones the catalog will rank first — the swap APPENDS rather than reshuffles, cards are keyed by path, and the previews that started during the partial phase survive it. That is the point: the slow part of the hub is preview iframes, so the earliest possible start for the twelve the user is most likely to want is worth more than the listing latency it also hides. **Fired in PARALLEL, never as a gate:** the row is only fast for a user whose recents fill it — with fewer than twelve the server falls back to the same walk — so awaiting it first would pay the walk twice for exactly the users it does not help. **And only from COLD, for the same arithmetic**: a revisit painting from the remembered catalog, or a refetch after a create, discards the row on arrival anyway (the state guard refuses to overwrite a full grid), so firing it there would spend that same double walk on an answer nothing reads — the head start is for a skeleton, and there is no skeleton. Its failure is silent for the same reason: the catalog is the answer. **Chips, the count and the empty state stay catalog-only.** A chip row derived from twelve recents would DROP options as the rest arrived, and "No apps match" is a claim about a catalog that has not landed; the row is held open through the partial phase so the grid does not jump under a pointer already on a card. **A failed catalog keeps the partial grid** and shows the banner beside it — the error moved into its own state so the phase type has no member that blanks the cards. **The remembered catalog is module scope, not a server cache:** the warm walk is already ~50ms, so the wait worth removing is the one a returning visitor sees on a page they just left, and the staleness window is one fetch — a card for a since-deleted app 404s on open exactly as one deleted while the page sat open does. **The preview rank is a GETTER read at admission, not a dependency:** `usePreviewStart`'s effect restarts the iframe whenever its deps change, so promoting a waiting card through the deps would tear down a running one; the queue instead asks each waiting task whether it is on screen at the moment a slot frees. **The second observer feeds a REF, not state** — `near` is state because mounting an iframe is a render, but ranking an already-queued start is not, and the real viewport edge is crossed by every card on every scroll: as state it would re-render the whole grid twice per scroll-past for a value no render reads, and charge Home's bookmark cards for a tuple slot they never destructure. A hover keeps the `true` fast lane (skip the idle wait, head of the queue) because the on-screen cards are hot too and a gesture has to outrank them. **Rejected: streaming /api/apps (NDJSON, or a server-side payload cache)** — both spend backend machinery on the wait that measurement says is the small one. **Rejected: raising the two-slot preview limit** — each slot is a whole document running the app's own JS and its `runPython` calls; the complaint is order, not budget. **Left open: a persistent thumbnail cache.** A captured still served as `<img>` is the only thing that makes a cold hub instant, and it is blocked on capture, not on caching — tab capture needs a gesture per shot (appShot) and DOM rasterization is blank for the map/WebGL apps that most need it, so it waits on the native capture API (PR #700) being able to photograph a page without one. |

### Recording the screen, the mic and a still, natively (2026-08-20)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D409 | **`fused.capture` — native screen / microphone / still capture on the runtime bridge, ScreenCaptureKit + `AVAudioRecorder`, macOS-only with a reasoned refusal everywhere else; the manager's ✕ discards and `maxSeconds` stops** | A page could already call `getDisplayMedia`/`getUserMedia`, so this had to buy something a browser cannot: **system audio** (impossible in a browser on macOS), no picker per recording, and — the one that pays for the rest — **a FILE whose path is known before the recording stops**, which makes `fused.ai.transcribe({path: rec.path})` the next line instead of a blob round-trip and a `writeFile`. The shell UI runs in the user's DEFAULT BROWSER, so "native" means the app's own process does the capture; on macOS the server runs in-process inside FusedRender.app (`app._start_server_thread`), which is what makes the TCC grant belong to this app rather than to Chrome. **Named `capture`, not `record`** (the owner's first instinct, revised in review): a screenshot is an instant, not a session, so `record.screenshot` was a category error — and all three verbs share one TCC grant, one display list and one output-path rule, so a root-level `fused.screenshot()` would have been a second door onto one permission model. The word `record` survives as the header doc's first line, which is what a reader greps. **`SCRecordingOutput` and `AVAudioRecorder` write the files themselves — not one sample buffer passes through Python**, which is why the backend is ~300 lines and why a 90-minute recording costs no CPU here at all. **`/usr/bin/screencapture` was the obvious cheap alternative and was rejected on capability, not purity**: it cannot capture system audio, cannot record audio-only, and has no stop that is not a signal — the AI-10 "binaries this app does not ship" argument was the second reason, not the first. **Two Apple APIs were tried for the microphone and the first one cannot work in this app.** `AVCaptureSession` + `AVCaptureAudioFileOutput` was chosen first because it takes a device id; it then hung every stop for the full timeout while leaving a perfectly valid file on disk. `stopRecording` marshals with `performSelector:onThread:waitUntilDone:YES` and blocks until a RUN LOOP runs the work — and `start` and `stop` are two HTTP requests, so they arrive on two different uvicorn worker threads, neither of which has a run loop; funnelling every AVFoundation call through one owned run-loop thread did NOT fix it, which is what identified the target as the main thread (sampled: `-[AVCaptureAudioFileOutput stopRecording]` → `-[_NSThreadPerformInfo wait]`, pinned indefinitely). A packaged app has a main run loop (rumps) and a source install has none, so that API is a capability that would work in the DMG and hang for anyone running from a checkout. `AVAudioRecorder` has no affinity — measured 5 ms for a cross-thread `stop()` with a valid file when it returns — and the whole owned-thread mechanism was deleted with it. **The cost is REFUSED, not hidden**: `AVAudioRecorder` records the system's current input with no device selection, so `capture.audio({device})` rejects `bad_request` naming where a microphone CAN be chosen (a screen recording's `audio: "mic"`, which goes through `SCStreamConfiguration.microphoneCaptureDeviceID`) — the D319 posture, because a silently-wrong microphone is a recording the user has to make twice. **The delegate is not the contract either.** `SCRecordingOutput`'s delegate does arrive (dispatch queues), and `stop()` waits on it so the caller is never handed the path to an unfinalised movie — but bounded, then falling back to watching the file settle, since a movie that was written is not a failure however quiet its delegate was. **The probe must not prompt**, which is a real constraint and not a nicety: `SCShareableContent` — the only route to a content filter — raises the TCC dialog, so `sources()` answers from `CGPreflightScreenCaptureAccess` + `AVCaptureDevice.authorizationStatus` and lists displays from `CGGetActiveDisplayList` instead, and the dialog rides the first real capture. Same rule the GPU probe follows (D382): a page asking what is possible must change nothing. **✕ discards, `maxSeconds` stops**, and the second follows from the first: a recording survives its page, so once the tab is gone the ✕ is the only control left — if the cap discarded too, there would be no ending that kept the file. `atexit` finalises the rest, because a .mov with no `moov` atom does not play and a job row saying "done" over an unplayable file is the worst outcome available. **macOS-only, with the reason in the payload rather than the routes being absent** (the owner's call over a `getDisplayMedia` fallback in `runtime.js`): a page must be able to ask, so `sources()` answers on every platform and `start` is a 409 naming the machine, never a 500. Consequently there is **no `via` field** — one implementation, nothing to branch on — and the floor is stated as what it really is: **macOS 15** for recording (`SCRecordingOutput`) and 14 for the still, against `LSMinimumSystemVersion` 11.0, so "macOS" alone would have been a lie on a 12–14 Mac. **Measured before it was added**: both pyobjc framework wheels plus their transitives are 3 MB installed, which is what let them into core deps (with a darwin marker, like the pasteboard bridge) rather than the `[app]` extra — D276's budget is the gate, and a capability that exists only in the DMG is the mistake that comment already describes. **Retina was a bug found by looking**: `SCDisplay.width` and `CGDisplayPixelsWide` are both POINTS (1800 on the machine this was written on, against 3600 real pixels), so the first recordings and screenshots came out half-size and soft with nothing in the API to suggest why; the scale comes from the display MODE, which is the only thing that knows both numbers. **Verified end to end on this machine, not just unit-tested**: a 4 s screen recording with `audio: "both"` and an 800×600 rect → h264 1600×1200 + one mixed aac stream, 4.13 s; a mic recording → valid 3 s aac; a still → 3600×2338 png. The suite stubs the backend and tests what decides behaviour instead — the refusals that must land before a microphone turns on, where the file goes, the job row, and all three endings. **Four things were CUT in review rather than shipped, three of them from the public surface** (a bridge method is a forever contract, so this is the cheapest moment): `onTick` — a recording already publishes a job row and `fused.watchJob(jobId)` is the documented way to read one, so a private callback was a second spelling of a public mechanism, and deleting it removed the whole watcher block; `format` on the still — a `path` and a `format` can disagree, and "shot.jpg" holding PNG bytes is a file every other tool misreads, so the OUTPUT FILENAME picks the container and an unknown extension is a 400; `get(id)`, which had no caller in any layer; and audio-only's `source` check, which ran the shared `_audio_mode` (naming 'mic', 'system' and 'both' as valid) and then refused two of them a line later — two sentences contradicting each other about one argument, now one. `list()`/`attach()` were KEPT against the same pressure, because after a page reload they are the only route back to a live recording's `stop()` and the ✕ discards: without them a reload can only destroy what it finds. **Four correctness bugs were found by the same review, and one of them was invisible to its own test**: `atexit.register(stop_all)` is dead code in the packaged app — every quit surface there ends in `os._exit` (DM-9, which exists because reaching `__cxa_finalize` aborts on a native extension's destructor) and runs no handler — so a quit mid-recording left an unplayable .mov behind a row that said "recording", while `test_stop_all_finalises_everything_on_the_way_out` passed by calling `stop_all()` directly. It is now a `"capture"` rung in `app.quit_teardown` (before the unmounts: a recording writing under a mount holds it busy) plus an ASGI shutdown handler for the plain server, with `atexit` demoted to a backstop and a test that reads both call sites rather than the function. The other three: the client memoized the settled stop RESULT instead of the promise, so a double-clicked stop button fired a second request that 404'd as an unhandled rejection; `recordingOutput_didFailWithError_` stored an error nothing read until `stop()`, so a stream that died at minute two ticked "Recording" for the rest of its cap over a file nothing was writing (the watchdog now asks the backend each tick, best-effort so a second platform need not implement it); and `_settle` returned the same nothing whether the file went quiet or the deadline passed, so a still-growing movie could be handed back as done — it answers a boolean now and an unsettled file is an error. **Deferred on purpose**: window-id capture, pause/resume, webcam, multi-track output, live frames to the page. **`screenshot()` is IN, though** (owner ask), and it is the piece that makes two existing problems solvable rather than solved-here: the hand-rolled tab-capture crop in `claude/template.html` and D349's cross-origin-pane limitation both exist because a browser still needs a readable document and a share prompt, and a native one needs neither. Nothing in the claude template is rewired by this change — that is a separate call about a page with its own tests. |
| D410 | **`fused.capture` works on Windows and Linux, split by CAPABILITY rather than by platform: the STILL is native on both (GDI `BitBlt`; `org.freedesktop.portal.Screenshot`), and RECORDING is the page's own `MediaRecorder` streaming chunks over a WebSocket into a file this server writes (`capture/_sink.py`, one implementation shared by both)** | Asked for as "all features, simple architecture, industry standard", and the Windows half of that is a real constraint rather than a preference: **there is no OS API a non-packaged Python process can reach that will write a movie with system audio in it.** `AppRecordingManager` needs MSIX identity (the installer has none), `Windows.Graphics.Capture` hands out D3D surfaces with no muxer behind them, Media Foundation has no screen source, and — checked against ffmpeg's own device documentation rather than assumed — **ffmpeg's Windows inputs are dshow, gdigrab and vfwcap, with no WASAPI indev at all**, so the ShareX-style route cannot do loopback without a third-party virtual driver. What remained was OBS's answer (WGC + WASAPI + a sink writer) which is native code, or Chromium's, which is already installed and is what Loom, Zoom web, Meet and every Electron recorder ship. **So the browser encodes and the server owns the file**, which keeps every property that made this feature worth having: the path is decided at start (CP-2), it is a job row with the same three endings, and `fused.ai.transcribe({path})` is still the next line. **This reverses the earlier "graceful error, no getDisplayMedia" call** (D409) on two facts that were not established then — the ffmpeg fallback is provably dead on system audio, and this is not a degraded browser fallback but a server-written file with a known path. **Chromium on Windows shares system audio; on macOS it cannot** — which is the whole reason macOS stays native and is why the split is principled rather than convenient. **One sink, two platforms**, and that deleted the expensive half of the plan: no portal ScreenCast state machine, no PipeWire fd hand-off, no `gst-launch-1.0` detection and no gst-libav container question, because Chromium already goes through that exact portal. Linux is left with one native call and Windows with one `BitBlt`. **The still stays native on purpose**: it is instant, needs no picker and no permission, and keeps `display`, `rect` and `cursor` working — a `getDisplayMedia` frame would put a share dialog in front of a screenshot. **What cannot be honoured is REFUSED with a pointer, never ignored** (the D319/AI-10 posture): `display`, `rect` and `cursor` on `screen()` (the browser's share picker owns the region, and silently dropping `display: 2` hands back the wrong monitor with nothing in the reply to explain it), `display`/`cursor` on a Linux still (the portal's interface has neither), and a `path` whose extension contradicts the container. Going the other way, **`device` DOES work on `audio()` here** and does not on macOS — the asymmetry is a sentence on one platform, not a flag. **CP-8's "no `via` field" survived gaining a second implementation**, which took work: `transport` and `streamToken` exist on the wire so `runtime.js` can open the socket, and `client` on `sources()` says "this platform's recording capability is the browser's" — all three are consumed by the bridge and DELETED before a page sees anything, with a test that fails if one leaks into the handle. **The probe had to move for the same reason**: whether a recording is possible is a fact about the BROWSER (has it `MediaRecorder`, will it share system audio) and a server route cannot know which one is asking, so those three keys are answered client-side — Firefox reports `video.available: false` naming Chrome on the same machine where Chrome reports true. **Ordering is the whole correctness story on this path, and four orderings are load-bearing.** (1) The share picker opens BEFORE the server allocates anything, so a cancelled picker leaves no job row and no empty file — the path is still decided before the first frame, which is what CP-2 actually promises. (2) Chunks go through ONE promise chain, because `Blob.arrayBuffer()` is async and two chunks read in parallel can be sent out of order, which is a corrupt container rather than a glitch. (3) The socket opens before `recorder.start()`, since a chunk produced before it is a hole in the middle of the file. (4) An **`eos` round-trip** precedes the stop request: the stop travels on a different connection and would otherwise close the file ahead of the last timeslice, and frames on one socket are ordered, so the server's reply to `eos` is the only actual proof the tail landed. **A closed socket is an ENDING, not an error**, and this is the one real regression against macOS: the encoder lives in the page, so a full reload ends the recording. It fails safely because fragmented mp4 and WebM are playable AS WRITTEN — there is no `moov` atom to lose, so the close keeps the file, marks the row done and says why, and `list()` stops showing a recording that is not happening. The handle is created in the TOPMOST same-origin window (D72's climb, deliberately ignoring param boundaries — this is about object lifetime, not params), so iframe navigation and switching files do NOT end a recording; only a shell reload does. **Two failure clocks, because neither raises anywhere**: a share dialog nobody answered (`ATTACH_S`) and a renderer that froze with its socket still open (`STALL_S`) both reach the watchdog's existing `failure(handle)` hook, so no row ticks "Recording" for thirty minutes over a file nothing is writing. **A byte ceiling as well as a time cap**, since the bytes are the page's to choose and `maxSeconds` does not bound them. **The socket cannot carry `X-Fused`** — a browser will not put a header on a handshake — so it is guarded by the per-recording token from its own start reply, an `Origin` check, and a refusal of any second attach (two encoders appending to one file interleave two containers into something unplayable). **A found bug with no test until now**: returning from a websocket endpoint does not close the socket, so the byte-ceiling break left the page encoding into a file nothing was reading — the explicit `ws.close()` in the `finally` is the fix, and it is what makes every server-side ending visible to the page. **The neutral half grew a finished-record cache** for the loser of an ending race (a double-clicked stop, or a socket that closed a moment before the stop request landed): it answers with the same record instead of a 404 about the caller's own recording — with the one exception that a `cancel` arriving after that still DELETES the file, because a cancel that reports success and leaves the file is the worse bug. **The seam held**: adding two platforms took `ext(mode, spec)` and `refuse(mode, spec)` as optional hooks, a raw `cursor` (so a backend can tell "the caller asked" from "the caller said nothing"), and the id in the spec (the sink must be findable before `start` returns) — everything else in `capture/__init__.py` is untouched, and the macOS-only prose moved into `_darwin.refuse` where a sentence naming System Settings is actually true. **Dependencies: `pillow` (both) and `dbus-fast` (Linux) promoted from the desktop extras into core with markers.** Both were already installed by the packaged builds for OTHER reasons (tray icons), and leaving them there would have reproduced exactly the mistake the `pyobjc-framework-Cocoa` comment describes: a source install running the same shell and the same pages, reporting `screenshot.available: false` forever. dbus-fast is pure Python with manylinux wheels, so the portal call brings in no GTK, no PyGObject and no system package. **Rejected: bundling LGPL ffmpeg in the Windows installer** — 40–60 MB, a license review, and it still cannot capture system audio, so it fails the actual requirement. **Rejected: WGC through `MediaStreamSource`** — per-frame Python and a D3D surface copy per frame for up to four hours, against a design whose stated property is that no sample buffer passes through Python. **Rejected: a native helper .exe** — a new build and signing pipeline for one feature. **Rejected: PyAV with `x11grab`** — the wheels carry ffmpeg (the D288 precedent) but frames would run through Python and x11grab is dead on Wayland. **Rejected: enumerating displays on Wayland** — no client may, without prompting, and CP-7 forbids a probe that prompts; `displays: []` plus a refusal sentence is more use than a list nothing accepts. **NOT VERIFIED ON HARDWARE**: there is no Windows box and no Linux desktop in this environment. What that costs is stated rather than glossed — the ctypes calls, the portal round trip and the browser's own behaviour are the parts with no test — so the pure halves are split out and tested on every platform (monitor union and rect offsetting, the portal's three reply endings and its file URI), and the sink is tested END TO END through the real WebSocket, where a test can simply BE the browser: 38 tests covering the token, the `eos` ordering, both failure clocks, the ceiling, the cap and the closed-socket ending. The smokes that need a human are the two pickers and a Wayland compositor. |
| D417 | **`fused.capture` records on macOS 13 and 14: the app supplies the muxer `SCRecordingOutput` would have been (`capture/_darwin_mux.py`), the microphone comes from an `AVCaptureAudioDataOutput`, and `audio: "both"` is mixed here against a drift-corrected ring — so the floor drops from 15 to 13 and becomes PER VERB rather than one number** | The floor was never really "macOS 15": recording was 15 (`SCRecordingOutput`), the still 14 (`SCScreenshotManager`) and audio-only already worked on 12.3, because `AVAudioRecorder` needs none of ScreenCaptureKit and nothing gated it. **What 15 actually bought was the WRITER, not the capture.** `SCStream` delivers frames from 12.3 and system audio from 13.0, so below 15 the missing piece is an `AVAssetWriter` and an `SCStreamOutput` delegate to feed it — which is the "later decision" the D409 comment deferred, taken now because system audio on 13–14 was asked for directly. **The floor that remains is the floor of the CAPABILITY**: a page can already record a screen with `MediaRecorder`, and system audio is the one thing it cannot do on macOS (CP-6), so 13.0 — `capturesAudio` — is where native stops being worth doing at all. Below that the answer is unchanged and is a sentence, not a raise. **12.3–12.x was deliberately NOT claimed** even though the muxer would run there: with no system audio it is strictly worse than the browser, and a band where the native path is the weaker one is a matrix hole pretending to be support. **This reverses D409's proudest property on this path and the reversal is stated rather than buried**: sample buffers DO pass through Python at 13–14. They are not READ — the delegate checks a status attachment and calls `appendSampleBuffer_`, so the cost is a bridge crossing per frame and not an encode — and the real risk was identified as LATENCY rather than throughput, since every callback needs the GIL and `SCStream` drops buffers whose handler is slow. Checked rather than assumed: user Python (`_child.py`) and AI inference (`ai/supervisor.py`) both run OUT of process, so the in-process competition is uvicorn's I/O and the index scanner's `os.walk` bursts — an earlier estimate that called this a large risk was wrong about where Python runs in this app. Mitigations are the two that cost nothing: `queueDepth` at its maximum of 8, and every dropped frame COUNTED on the handle, because a recording that silently shed a third of its frames is the failure this design could actually have. **Three audio modes turned out to be three data paths and only one is hard.** `"system"` appends ScreenCaptureKit's audio untouched; `"mic"` appends `AVCaptureAudioDataOutput`'s buffers with no byte surgery, and keeps `device` selection working exactly as `microphoneCaptureDeviceID` does on 15; only `"both"` mixes. **The microphone FORMAT is requested on both mic paths, not only the mixing one** — a review catch: `"both"` needs it so the add has one agreed format, but `"mic"` needs it too, because most built-in microphones are MONO, the writer's audio input is stereo, and a channel-count mismatch is something `appendSampleBuffer_` can refuse on every buffer — a whole failure class that cannot be reproduced from here. **The microphone can come back at all because a DATA output is not a FILE output**: D409 documents `AVCaptureAudioFileOutput.stopRecording` hanging every stop forever via `performSelector:onThread:waitUntilDone:YES` against a thread with no run loop, and a data output has no such call — it delivers to a dispatch queue and is stopped with `stopRunning`, which blocks briefly and marshals nowhere. **`"both"` corrects drift rather than tolerating it, which is a better property than the ask required.** Two sources, two clocks, no resampler: system audio is the master, the microphone goes into a ring, each system buffer pulls the matching bytes — silence-padded on an underrun, oldest-discarded when the ring runs long. Because a correction fires whenever the ring passes its high-water mark, the offset stays bounded by one buffer instead of accumulating, so what is accepted is a rare faint discontinuity (~one per few minutes at a typical 100 ppm device offset) and never a recording a second out of sync by the end. The sum is CLIPPED, not attenuated: a blanket −3 dB would make every `both` recording quieter than the same content recorded either way alone, while clipping only touches samples the encoder was going to clip anyway. **Four implementation facts were established by probing pyobjc rather than by assuming, and two of them chose the design.** (1) `CMBlockBufferGetDataPointer` is unusable from pyobjc — it marshals the memory into a TUPLE OF INTS rather than a writable view — but `CMBlockBufferCopyDataBytes` out and `CMBlockBufferReplaceDataBytes` back round-trips, which is what lets the mix land IN PLACE in the system-audio buffer and the recorder append the sample buffer it was handed; constructing a `CMSampleBuffer` through pyobjc, the fiddliest thing this backend would otherwise need, is avoided entirely. (2) pyobjc's libdispatch bindings are not installed and both `SCStream` and `AVCaptureAudioDataOutput` want a serial `dispatch_queue_t`, so `dispatch_queue_create` through ctypes on libSystem is wrapped by `objc.objc_object` into the `OS_dispatch_queue_serial` both selectors accept — no new dependency, the ctypes-for-one-platform-call posture `_windows.py` already uses, and the queues are created once at module scope because the ownership semantics of a ctypes-made object behind a pyobjc proxy are not worth discovering per recording. (3) The three ObjC protocols (`SCStreamOutput`, `SCStreamDelegate`, `AVCaptureAudioDataOutputSampleBufferDelegate`) all resolve, so the delegates declare conformance rather than relying on `respondsToSelector`. (4) Accelerate's `vDSP_vadd`/`vDSP_vclip` reach through ctypes and were run on real numbers. **The mix uses vDSP and not numpy on purpose**: numpy is `[bundled]` while this backend is core with a darwin marker, so a numpy mixer would work on some installs and not others — the exact mistake `pyproject.toml`'s Cocoa and pillow comments keep naming — and Accelerate is a system framework that costs nothing. **Three correctness traps are handled and each one is invisible until it bites**: ScreenCaptureKit emits sample buffers for frames it did NOT redraw and their `SCStreamFrameInfoStatus` says so while the pixel buffer is empty, so an unfiltered muxer produces a one-frame movie on an idle screen; `startSessionAtSourceTime_` must ride the FIRST VIDEO frame with earlier audio discarded, or the movie's zero is wrong and everything after it is offset; and `stop()` must halt the SOURCES before `markAsFinished`, because the other order races a straggling frame into a finished input and raises out of a dispatch queue where nothing is listening. **One thing got BETTER than the 15 path**: `finishWriting`'s completion handler is a real "the moov atom landed" signal, so this path needs none of `_settle`'s watch-the-file-stop-growing heuristic, and a recording that captured no frames raises instead of leaving a file that does not play. `SCStreamDelegate.stream:didStopWithError:` supplies the per-tick `failure(handle)` hook that `SCRecordingOutput`'s delegate supplies on 15, without which a stream dying at minute two would tick "Recording" to the cap over a file nothing is writing. **The still came along because a floor of 13 with a broken screenshot is incoherent**: `CGDisplayCreateImage` covers 13, returns backing pixels so the Retina rule holds for free, and cannot draw the pointer — so `cursor: true` is REFUSED there naming macOS 14, the D319 posture, rather than handing back a still that silently lacks it. **Rejected: two audio tracks in the .mov** — legal and cheap, but `transcribe` and ffmpeg read track 1, so a `both` recording would transcribe differently on 14 than on 15, which is the backend-observable divergence CP-8's no-`via` rule forbids. **Rejected: a second file for the microphone** — also cheap, but the handle's SHAPE would then depend on the OS version, which leaks the backend into the public surface harder than a `via` field would. **Rejected: a real resampler** — 1–1.5 weeks for the difference between a bounded offset with occasional ticks and a perfect one, on a path two OS versions will ever run. **Rejected: `pyobjc-framework-libdispatch`** — a core dependency for two calls ctypes already makes. **NOT VERIFIED ON HARDWARE, and the owner asked for it that way**: there is no macOS 13 or 14 Mac in this environment and no recording was made anywhere. What WAS verified is narrow and worth naming so the gap is not overstated or understated — every selector, constant, protocol and ctypes call this code depends on was probed for existence and calling convention on macOS 26, the pure mixdown half (ring accounting, both drift directions, upmix, clip, and the vDSP path agreeing with a pure-Python reference) has 14 tests that run on every platform, and 9 more cover the floors, the routing, the `respondsToSelector` guard on `captureMicrophone`, the cursor refusal and the two failure endings. What is untested is everything that needs frames to actually arrive. **`FUSED_CAPTURE_FORCE_MUX=1` forces the `AVAssetWriter` path on any Mac**, because every API it uses exists on 15 too and without it the one code path nobody developing it can run is the one just written. **Deferred**: macOS 11–12.x, which needs only a lazy `ScreenCaptureKit` import to get audio-only working (the module loads its frameworks at top, so a machine below 12.3 currently gets `Unsupported` for a verb that needs none of them); and a crossfade at ring-correction points, which would turn a faint tick into nothing at all. |

### The question card always offers "Other" (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D407 | **Every question on an AskUserQuestion card gets an "Other" row this window adds, never the model — last in the list, dashed and muted so it does not read as a fifth thing Claude suggested, opening in place into an auto-growing TEXTAREA (autofocus, one line to start, Enter answers, Shift+Enter breaks the line, Esc collapses, a blank box does nothing). Multi-select has it too: the typed answer joins the ticked labels and is appended LAST. What is typed rides a SEPARATE `custom` param (a JSON record keyed by the same question text as `answers`), latched beside the answer on the decision file, and `_answers_from` — both copies — folds it in as one extra option for that one question before running the unchanged label check. permission_server additionally appends it to that question's `options` in `updatedInput`** | Claude Code's own prompt appends "Other" to every AskUserQuestion, and ours could only echo the model's two-to-four options back — so a user who disagreed with the answer space had to pick the nearest wrong answer, and the model then acted on a choice nobody made. That is the exact failure the label validation (D146) exists to prevent, arriving through the front door. Rejected: relaxing `_answers_from` to accept any non-empty string, which is the same validator deleted — for a multi-select join it degenerates to "anything at all", and it loses the one distinction worth keeping, which is authorship (a label the model offered vs a sentence the user wrote). The second channel keeps both copies strict and keeps the parity test (`test_the_two_answer_validators_are_the_same_code`) meaningful. Growing `options` in `updatedInput` rather than only setting `answers` because the CLI checks each answer against the option list it is handed and one it cannot find there downgrades the tool_result to the weaker "follow what they actually say" (measured, same finding as the multi-select join, D146) — the appended option carries a description saying the user wrote it, so the model is never told it authored something it did not. The list is rebuilt, never mutated: every other branch hands the parked input back whole. The row is styled as a SIBLING of the model's options (same box, padding, border and hover; only the label recedes) rather than as a dashed outline — the first cut was dashed and read as a stray widget rather than as the last row of a list. A textarea and not a one-line input because the answer the model did not anticipate is the one most likely to be a sentence, and a single line scrolls it out of sight sideways — it grows on the composer's `autoGrow` pattern, with that function's flat 200px ceiling, and is re-measured on open because a display:none textarea reports a scrollHeight of 0. While a box is open the question list gives up its 46vh cap entirely (`.perm.ask .qscroll:has(.qopt.qother.typing) { max-height: none; overflow: visible }`), so there is exactly ONE scroller inside the card and it is the box; the cap comes back on Esc or on send. The list and the box are two scrollers stacked in the same axis and two of those cannot both be right — whichever one the wheel lands on, the other is the one the user meant. Rejected, because it shipped and was wrong: sizing the box to the room the list had left, clamped to [64, 200]. Measured in a real browser at 460px tall, a four-option card is 452px of list under a 212px cap, so the list was already over the cap before a character was typed — there was no room to give, the clamp bottomed out on its floor, and the result was a 64px box scrolling inside a card that was also scrolling, with the open row sitting 91px below the card's fold where the user could not see what they were typing at all. No arithmetic against the leftover room can fix that, because on a short window there is none; it only chooses which of the two scrollbars is shorter. The card growing instead is the trade: "Send answer" moves below the fold of the pane while a box is open, which Enter (the documented way to finish this card) does not need, and the transcript — the scroller every other card's controls already live in — takes the overflow. `grow` also does two things the composer's does not, both of them "the user can see what they are typing": it puts the caret back at the bottom afterwards when it was at the end of the text, because setting a height resets a textarea's scrollTop and without it every keystroke past the ceiling jumped the view back to the first line (measured: 812 characters in, caret at 812, scrollTop 0, 426px of content in a 75px box); and it asks the row for `scrollIntoView({block: "nearest"})`, which moves whichever ancestor is scrolling by the least it can and does nothing at all when the row is already visible. `scrollHeight` is padding-box while the height being set is border-box, so the border difference is added back or a one-line box reports a two-pixel overflow and paints a scrollbar. It also shipped broken once: the drawn checkbox rule was written `.perm .qopt input`, which at (0,2,1) out-specifies `.perm .qtype` (0,2,0), so the Other row's TEXT field was painted as a 14px tick — an empty grey row with a tiny white box and nowhere to type. The tick rules are type-qualified now and a stylesheet test (`test_the_drawn_tick_never_paints_the_other_rows_text_field`) holds that, because a DOM probe cannot see a bug that is only paint. |
### A fourth text runner reads GGUF, and the maintainer's wheel index is audited rather than trusted (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D411 | **`llamacpp-text` (`fused_render/ai/runners/llamacpp_text/` + `runners/llama_text.py`) loads GGUF quantizations via `llama-cpp-python`, registered as a FOURTH text-generation runner BELOW all three `transformers-text` rows so `auto` resolution never reaches it — and the pin (`==0.3.29`) is chosen by a per-platform zip-integrity AUDIT, not by "newest version resolves"** | AI-11 refused llama.cpp outright because `llama-cpp-python` "publishes an sdist and no wheels at all" — true of PyPI, and no longer the whole story: the maintainer's own index (`https://abetlen.github.io/llama-cpp-python/whl/cpu/`) publishes complete, current `py3-none` wheels, a prebuilt shared library behind ctypes with no compiler involved, checked directly rather than assumed. `explicit = true` on that index (`llamacpp_text/pyproject.toml`) is the identical confinement `transformers_text/pyproject.toml` uses for PyTorch's CPU index. **The residual risk is worse than "a second host to trust", and it is the actual finding of this decision.** A sweep of every macOS arm64 wheel this index has published for `llama-cpp-python` found the corruption is not a version-cutoff regression, it is close to a coin flip: only 4 of 16 releases sampled (0.3.18, 0.3.24, 0.3.26, 0.3.29) pass `zipfile.ZipFile(...).testzip()` on their mac wheel, the rest fail — usually on a bundled `libggml-base*.dylib`, under three DIFFERENT corruption signatures (bad CRC-32, bad uncompressed size, bad zip64 local-header offset), and every failing wheel's sha256 matched its own published GitHub digest three times over, so this is upstream publishing bad bytes, not a CDN or a fetch race. Wheel SIZE is not a proxy — PASS and FAIL sizes interleave across the version list with no pattern. Root cause is upstream issue #1650: the Release job's artifact-merge step corrupts wheels that were fine when originally built, diagnosed by a community member in Aug 2024 and still open; issue #2352's claim that "0.3.30 is unaffected" was checked directly and is FALSE (its mac wheel is also corrupt). `0.3.29` was chosen because it is the newest release where all FOUR platform tags this runner actually ships — `macosx_11_0_arm64`, `manylinux2014_x86_64.manylinux_2_17_x86_64`, `manylinux2014_aarch64.manylinux_2_17_aarch64`, `win_amd64` — pass the same test; 0.3.34 and 0.3.35, the two newest at the time, were both corrupt on macOS arm64, which is what triggered the wider sweep rather than a one-version retry. **This is why the runner is registered FOURTH and stays opt-in rather than becoming a fallback anywhere in the ordering**: a capability whose INSTALL can silently fail on the maintainer's own release pipeline must never be something `auto` reaches for, however sound the specific pinned version is once verified — the packaging risk, not a benchmark, is what keeps this below every `transformers-text` row (AI-2b's same argument, applied to a risk `_available` cannot express because it is a fact about a past release event, not about this machine). **The model-id problem is answered by copying `torch_image.py`'s `_GGUF_RECIPES` shape, not by inventing a grammar**: a GGUF repo commonly publishes two dozen quantizations of one model (`unsloth/Qwen3.5-9B-GGUF` alone is 147.81GB whole), so a model here is a `(repo, filename)` pair curated under an opaque id (the GGUF's own filename, never parsed for structure) rather than a `repo:Q4_K_M` string that would need touching every page, preference and cache tag that treats a model id as a Hub repo id verbatim. Accepted consequence, stated rather than hidden: Hub search on the Discover tab cannot populate this engine, since a bare repo id supplies no filename and only the curated ids in `_GGUF_RECIPES` ever load — the same limitation `formats.COMPONENT_REPOS`'s repos already carry. **No tokenizer/config snapshot download, which is a fact about the curated repos rather than a design choice**: checked against the Hub API, `unsloth/Qwen3.5-4B-GGUF` and `unsloth/Qwen3.5-9B-GGUF` ship no external tokenizer files at their root at all — the vocabulary and the chat template live inside the one `.gguf`'s own metadata, which `llama_cpp.Llama.metadata` reads at load time, so `download()` fetches exactly the one file `worker_base.download_file` names and nothing else. The prompt is rendered by hand from that embedded jinja2 template (`enable_thinking=False` passed unconditionally, the same default AI-11d chose for the shared Qwen3.5 catalog) and handed to `create_completion(stream=True)` rather than `create_chat_completion`, so the NDJSON streaming contract stays byte-identical to `torch_text.generate`'s. `formats.py` gains a DECISIVE branch (a root-level `.gguf` is llama.cpp's format and nothing else in this app reads one for text), and `torch_text._weights_here`'s refusal — which used to end at "a repo of GGUF files is llama.cpp's format" as a dead end — now names this engine as the way to actually load one. **`tests/test_ai_runner_deps.py`'s guard is widened rather than given a per-folder exemption**: it now asserts every non-PyPI `[[tool.uv.index]]` any runner declares carries `explicit = true`, which is AI-2a's amended rule made general (a non-PyPI index is admissible only confined to the one distribution it is pinned for) rather than a rule that happens to also allow this one folder. **Rejected: pinning the newest release (0.3.35) and treating a resolve-and-install success on this machine as sufficient** — that is exactly the check that would have shipped a broken runner to every OTHER macOS arm64 user, since the coin flip is per-platform-tag and per-release, not a property a single developer's `uv sync` can observe. **Rejected: an sdist fallback or a "build from source if the wheel fails" path** — AI-2a's wheels-only rule exists specifically to keep a compiler out of this path, and working around a corrupt wheel by compiling one is the exact failure mode the rule was written to prevent. **Rejected: dropping macOS arm64 support for this runner** — 0.3.29 passes there too; the fix was auditing the pin, not narrowing the platform list. **Post-review addendum:** the shared module was renamed `llamacpp_text.py` -> `llama_text.py` — it sat beside a same-named FOLDER (`llamacpp_text/`), which resolves correctly only because that folder carries no `__init__.py` (a namespace package loses to a same-name ordinary module in the same path entry, but only until someone adds one); every sibling multi-folder runner (`torch_text.py` beside `transformers_text/`) avoids the collision by construction instead. The runner's own platform gate was also given a real check (`_llamacpp_platform`, matching `_transformers_platform`'s shape) rather than `_always`: the wheel index publishes no macOS x86_64 tag at all, so an Intel Mac was being advertised a Load button `uv sync` could never satisfy. **Second addendum, and an amendment to `_llamacpp_platform` itself rather than only a new row (2026-08-21): a fifth text runner, `llamacpp-text-vulkan` (`llamacpp_text_vulkan/pyproject.toml`), gives GPU acceleration on NVIDIA and AMD, which the CPU-index pin above never does on Linux or Windows** — Apple Silicon already got acceleration for free (the CPU index's macOS arm64 wheel links `libggml-metal.dylib`), so the gap was every other GPU vendor. The maintainer's SEPARATE `vulkan` index (`https://abetlen.github.io/llama-cpp-python/whl/vulkan/`) is the only path that reaches both vendors from one wheel: this maintainer publishes no ROCm/HIP index at all (`rocm`, `rocm6.1` through `rocm7.1`, `hip`, `sycl` all checked and all 404), so Vulkan is not "a third accelerator variant" the way CUDA/ROCm are two separate transformers rows — it is the only accelerated option this backend has for AMD, and the only non-Apple one for NVIDIA. **Audited by the identical method D411 established, run again independently against this index's own two wheels**: `manylinux2014_x86_64.manylinux_2_17_x86_64` (181.77MB) and `win_amd64` (56.94MB) are the ONLY tags this index publishes for `0.3.29` — no macOS, no Linux aarch64 — and both pass `zipfile.ZipFile(...).testzip()`, checked 2026-08-21 against the exact GitHub release asset (`releases/download/v0.3.29-vulkan/...`) the index redirects to. `0.3.29` is also the CPU index's own audited pin, so one `llama-cpp-python` version serves both runners. **A Vulkan wheel supplies neither the loader nor the driver — those come from the GPU driver, not from `pip` — and the two absences fail differently, established by parsing the downloaded wheels rather than assumed**: `libggml-vulkan.so` declares `DT_NEEDED libvulkan.so.1` and `libggml.so`/`libllama.so` both declare `DT_NEEDED libggml-vulkan.so.0` in turn (read off the wheel's ELF `.dynamic` sections), with the identical import on `vulkan-1.dll` in the Windows DLL (read off its PE import table) — so a missing LOADER fails `import llama_cpp` outright, on either platform, regardless of whether a GPU is even present. A missing ICD (loader present, no driver registered) is the milder case: the same wheel also bundles ggml's ordinary CPU backend (`libggml-cpu.so`/`ggml-cpu.dll`, present alongside the ~74.5MB Vulkan one), so ggml's dynamic backend loading answers with a working CPU fallback rather than a crash — not a reason to pass the gate, since that machine would pay 8x the CPU-index wheel's download (182MB vs. 22.5MB on Linux) for the identical CPU-only outcome the cheaper runner already gives. `registry._vulkan` therefore refuses on a missing loader as a HARD gate (the same shape `_cuda`/`_rocm` use for a missing device) and on a missing ICD as the "advertising a claim that buys nothing" case those two gates already exist to refuse — checked via `os.path.exists` against a handful of known distro library paths on Linux (`/usr/lib/x86_64-linux-gnu`, `/usr/lib64`, `/usr/lib`, since a userspace `.so`'s location is a distro packaging convention rather than a fixed kernel path the way CUDA/ROCm's device nodes are) plus the loader's own documented ICD manifest search directories (`/etc/vulkan/icd.d`, `/usr/share/vulkan/icd.d`, verified against the Vulkan-Loader project's own `LoaderDriverInterface.md` rather than assumed), and via the `NVCUDA_DLL`-style single fixed path (`vulkan-1.dll` in `System32`) on Windows. **Fixed in the same change, found while re-deriving the tag set for the new gate: `_llamacpp_platform`'s own Windows and Linux branches checked `platform.system()` only and never `platform.machine()`**, so a Windows ARM64 machine (no `win_arm64` tag published) was being offered the same Load button the Intel-macOS fix above exists to prevent — corrected to check `machine()` per OS (`"AMD64"` on Windows, `"x86_64"`/`"aarch64"`/`"riscv64"` on Linux, spelled differently per OS on purpose rather than one shared tuple), which is also why `_vulkan`'s own gate was written architecture-first from the start rather than inheriting the same gap. **Third addendum, a correction to a claim the second addendum itself made (2026-08-21): "Apple Silicon already got acceleration for free" was FALSE at the code level, and so was every claim anywhere in this decision that a Metal-linked wheel meant a Metal-accelerated load.** `runners/llama_text.py`'s `load()` never passed `n_gpu_layers` to `Llama(...)`, and `llama-cpp-python` 0.3.29 defaults it to `0` — verified directly against the installed package's `Llama.__init__` signature — so EVERY platform this runner ever shipped, including the macOS arm64 wheel with `libggml-metal.dylib` linked in, ran 100% on the CPU regardless of what the wheel could do. The `device="cpu"` this module set was therefore accurate by accident, for the wrong reason, which is exactly why no test caught it: `device` was a hardcoded constant, never a measurement, so nothing could disagree with it. **Fixed by asking the LINKED BUILD rather than branching on which folder loaded the module**: `llama_cpp.llama_supports_gpu_offload()` — a real C API, read at the vendored commit (`src/llama.cpp`) rather than inferred from its name — asks ggml's own backend registry for a real `GPU`/`IGPU` device (`ggml_backend_dev_by_type(...) != nullptr`), which is false with no GPU backend linked at all (the CPU-only case) OR a Vulkan build with no usable driver, and true on Metal (any Apple Silicon Mac) and a Vulkan build with a working one — the identical check therefore gets Metal right for free, with no Apple-specific code, and is what the shared module now calls before ever setting `n_gpu_layers`. **Sizing is done by TRYING, not by calculating, because nothing in this binding can calculate it**: `llama-cpp-python`'s ctypes surface wraps `llama.h` only, not the lower-level `ggml-backend.h` functions (`ggml_backend_dev_memory`) that would report free VRAM, confirmed by reading the installed bindings — and llama.cpp itself does not check available memory before allocating a layer's GPU buffer, only clamping the requested count to the model's own total layer count (`n_gpu = std::min(n_gpu_layers, n_layer_all)`, read at the vendored commit). A buffer allocation that does not fit raises a catchable Python exception rather than aborting the process — `llama_model_load`'s own `try`/`catch` in `src/llama.cpp` converts it to a clean load failure — so `load()` reads the model's own layer count off its GGUF header (a new `formats.gguf_block_count`, the same header-peek technique `gguf_architecture` already uses, verified against three real downloaded headers: `qwen35.block_count` is 32 for both the 4B and 9B curated repos and 65 for the 27B) and tries a shrinking sequence of offload counts — full, roughly two-thirds, roughly one-third, then `0` — catching each failed attempt and trying fewer layers, on the reasoning that a hard OOM killing the Load button outright is a worse failure than a slower partial or full CPU load on a 4-8GB laptop GPU asked to hold a model sized for a bigger one. When the header cannot be read, the schedule falls back to the two-tier `(-1, 0)` (llama.cpp's own "all layers" sentinel, then CPU) rather than failing to size at all. `worker_base.STATE["device"]` now reports `"cpu"`, `"gpu"` (every layer offloaded) or `"gpu (partial)"` (the backoff landed on fewer than the model's own total) — a measurement of which attempt actually succeeded, never a claim about which backend served it, since nothing in the bound API reports Vulkan-versus-Metal by name. The curated catalog (`catalog.SUGGESTIONS["llamacpp-text"]`, shared by both llama.cpp runners) keeps its existing smallest-first default unchanged — the 4B Q5_K_M entry (2.9GB) already fits comfortably inside a 4-8GB laptop GPU's VRAM with headroom for context — and gained a VRAM caveat on its two largest entries (9B Q8_0 at 9.5GB, 27B UD-Q3_K_XL at 13.1GB) rather than a restructuring of the table, since both already exceed typical laptop VRAM and now degrade safely to a partial or CPU load instead of the OOM they would previously have risked once real offload was wired up. |

### Any Hub GGUF repo is loadable and searchable, not just five curated filenames (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D412 | **`formats.pick_gguf_file()` ranks an arbitrary repo's own file listing and resolves ONE loadable GGUF from it; `llama_text._resolve_model_id` falls back to it for any repo `GGUF_RECIPES` has never heard of; `hub_models.py`'s search runs the SAME picker over a result's `siblings` and drops a row it cannot resolve** | D411 shipped `llamacpp-text` able to load exactly 5 curated filenames across 3 repos and refuse everything else BY NAME — a limit this app imposed on itself, never one llama.cpp imposes: any repo with a root-level `.gguf` is loadable by `llama_cpp.Llama`. The gap mattered because `formats.GGUF_RECIPES`'s curated table was the ONLY way in, and `hub_models.py`'s Hub search offered a Download button on every GGUF repo it returned regardless — a text-generation search result this engine could never resolve, D313's own "searchable and loadable cannot come apart" contract broken the moment a second on-disk format existed behind one capability. **Deterministic, hardware-blind resolution (Option A over a hardware-budgeted Option B), and the branch's own two properties made the choice for it rather than a preference**: `catalog.py`'s `size_gb` field and `ai_runtime.py`'s downloaded/curated join both require a model id to determine the same bytes on every machine, and `llama_text._offload_schedule`'s backoff (this branch's earlier work) already turns an over-large pick into a slower load rather than a crash — so a hardware BUDGET would have bought determinism it did not need at the cost of an invariant it did, and there was nothing to budget against anyway: `llama_cpp.py`'s ctypes bindings expose no `ggml_backend_dev_memory` or other free-VRAM query, confirmed by reading the installed package. An AUTO-GENERATED recipe table (Option C) was rejected as strictly worse than the picker it would need anyway — at build time it goes stale silently, at runtime it is this picker plus a cache and a Hub-outage failure mode neither exists yet to need. **The picker's exclusion rules were measured, not assumed, and the widening mattered**: split shards (`-\d{5}-of-\d{5}\.gguf$`) and subdirectories (`BF16/`, `MTP/`) were checked against 5 real repos; the auxiliary-filename denylist (`mmproj`, `mtp`, `draft`, `projector`) started as ONE observed file (`MTP/mtp-Qwen3.8-27B-Q4_0.gguf`, from which `mmproj`/`mtp-`/`draft` were guessed) and was re-checked against ~200 real `text-generation`+`gguf`-tagged repos (2637 filenames) before being trusted — the anchored `mtp-` guess was WRONG (the common real form is `*-mtp.gguf`, the dash before "mtp" rather than after, e.g. `RVN-Q4_K_M-mtp.gguf`) and is now a bare substring, `projector` was found and added (`llama-3.2-11B-vision_f16_projector.gguf`, an mmproj-shaped file containing no "mmproj" at all), and `specul`/`speculative` was tried and rejected for a real false positive (`DeepSeek-R1-Distill-Qwen-1.5B-GRPO-SpeculativeReasoner`, a fine-tune name, not a draft model). Multimodal projectors are confirmed load-bearing for exactly the path this picker serves: every mmproj-carrying repo found in the scan is tagged `pipeline_tag: text-generation`, the base LLM and its projector sharing one repo. **Suffix ranking starts at Q4_K_M rather than the true smallest quant a repo might publish, DELIBERATELY**: `_offload_schedule`'s backoff can turn a too-large pick into a slower load, but nothing can turn an already-downloaded, needlessly-degraded quant back into a better one, so the one cost backoff cannot undo (bytes over the wire) is the one this ranking is biased against; `Q6_K`/`Q8_0` rank last among named suffixes for the same reason in reverse. unsloth's `UD-` dynamic quants (the branch's own curated `UD-Q3_K_XL` 27B entry) are eligible but ranked below every plain quant of a NAMED family (`Q4`/`Q5`/`Q6`/`Q8`), since a plain quant needs no per-layer engineering to stay usable at that width and a `UD-` one does; a PLAIN sub-4-bit quant is excluded from ranking outright (no engineering behind it) but still resolves through a single-candidate fallback when it is the only file a repo publishes — the same "no ambiguity, nothing to guess between" rule the branch's own curated-repo resolution already uses, generalized. More than one unranked candidate refuses BY NAME rather than guessing the smallest, because a `mmproj` or a draft model is also small and is not a chat model — the exact failure this whole picker exists to prevent. **The search half required one deliberate exception to `hub_models.py`'s own "search does not depend on the host" rule, and it is named as one rather than made quietly**: `Runner.hub_filter_tags` (a new optional field, empty everywhere but the two llama.cpp rows) keeps the MECHANISM host-agnostic — a runner declares its own format tag, the module stays runner-agnostic, `supported_tags()`'s "adding a runner needs no edit here" contract survives — but READING it asks `registry.for_capability`, the machine's ACTIVE runner, which is genuinely host-dependent: two machines with different text-generation engines chosen in Preferences now see different search results for an identical query. Justified on the same ground `_UNRUNNABLE_LIBRARIES` already stands on for FORMAT (as opposed to hardware availability, which the rest of that docstring's rule is actually about) — a result the active engine cannot resolve at all is not actionable here regardless of what a different engine elsewhere could do with it — and kept narrow: the difference is visible (a Preferences choice the user made), never a silent machine-to-machine variance for a reason neither could see. `expand[]=siblings`, confirmed live to return a repo's COMPLETE file list in the Hub's LIST response with no per-repo follow-up call, is what makes resolving a search result's GGUF pick free at row-construction time; `filter=gguf` is confirmed tag-based and correct, `library=gguf` is confirmed a trap (returns `Qwen/Qwen3.8-27B`, `facebook/opt-125m` and other non-GGUF `library_name` values) and is not used. **Rejected: a `repo:quant` id grammar**, for the identical reason PR #706 already rejected one — it would touch every page, preference and cache tag treating a model id as a Hub repo id verbatim; the id stays a bare repo id on both the worker and the search-result row, and `_model_row`'s new `file` field carries the pick alongside it rather than encoding it into the id. **Accepted and unclosed**: an uncurated repo's resolution checks the local hf cache first (no network for a model already fully downloaded) but re-lists the Hub on every load once the cache misses, unlike a curated recipe's own local-only check — a fully offline reload of a Piece-1 pick that somehow lost its local cache entry needs network where a curated one would not, a real but secondary gap left for a future pass rather than solved by scanning the local cache directory structure more deeply. |

### The AI request envelope is closed (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D413 | **The request envelope of a job-backed AI call is closed: an unrecognised key on `/api/ai/image` or `/api/ai/transcribe` is a 400 naming it, checked in both `runtime.js` and `ai_runtime.py`, before any other validation** | Reported as a demo where "my app edited my existing image" turned out to be `fused.ai.image({prompt, image, strength})` rendering plain text-to-image and saying nothing about the two options it dropped — a picture came back, so the failure was invisible. The repo already held the principle: `runners/engine_options.py` refuses an option an ENGINE cannot honour with a 400 rather than answering best-effort, on the argument that an *undetectable* ignored option must 400 and only a *detectable* one (`words`) may be silently best-effort. An unknown key is the most undetectable case there is — nothing in the reply mentions it — so this applies the same rule to the envelope itself rather than inventing a new one. **Both layers, because either alone is a trap for the other caller**: the bridge validates because a server-only check is dead code for every page that goes through `fused.ai.image`/`.transcribe`, and the server validates because the skill documents `curl` against these routes directly and a page can `fetch` them without the bridge at all. **The server's `_reject_unknown` and the bridge's `rejectUnknownOptions` both report every unknown key in one message, not just the first** — a page passing `image` and `strength` learns about both in one round trip — and both are checked BEFORE any field validation, so a request that is also missing `prompt` or has a bad `steps` is told about the option it does not have first; a test drives exactly that ordering. **`base` is the one deliberate asymmetry.** `aiTranscribe` injects `body.base` itself from the page's own `?path=` — never from the caller's options object — so the SERVER's accepted set for transcribe (`_TRANSCRIBE_SERVER_OPTIONS`) includes `base` while the bridge's caller-facing set (`_TRANSCRIBE_OPTIONS`/`transcribeKeys`) does not: a caller passing `base` directly is passing an option that does not exist from where it is standing, and collapsing the two sets into one would silently stop enforcing that. `onProgress`/`onSegment` are the opposite case — real, accepted options that are callbacks rather than body fields, consumed above the whitelist loop that builds the POST body — so they are explicitly exempted from the bridge's unknown-key check rather than being unknown by omission. **A drift guard, not just two implementations of one rule**: a test extracts the bridge's `imageKeys`/`transcribeKeys` array literals out of `runtime.js` by source text and asserts them equal, sorted, to the server's `_IMAGE_OPTIONS`/`_TRANSCRIBE_OPTIONS` constants — the same precedent `test_the_bridges_speaker_bound_is_the_SAME_NUMBER_python_enforces` set for `MAX_SPEAKERS`, since JS cannot import a Python module and nothing else would stop the two English-language lists from drifting apart. **The same file's `unload` route gets a consistency fix, not a new rule**: `POST /api/ai/runtime/unload` never validated `capability` — a typo reached `supervisor.unload()`, which filters workers by equality and returns `bool(targets)`, so `{"stopped": false}` answered a typo exactly as it answers a correct request against an idle machine, the same illusion as the ignored image option. `POST /api/ai/cancel`, 45 lines below in the same file, already carried `if capability is not None and capability not in registry.capabilities(): return _error(...)`; `unload` gets the identical guard, checked only when `capability` is not `None` so the `model`-only form is untouched. `skills/fused-render-ai/SKILL.md`'s claim "An unrecognised capability is a 400, not a no-op" was true of `cancel` and false of `unload`; the fix makes the sentence true rather than weakening it. **Out of scope, on purpose**: the `words` feature and the transcription abstraction, `fused.ai()`'s own text options (a typo'd `systemPrompt` is the same class of bug and is a follow-up), and actual image-to-image support — this closes the envelope around the surface that exists, it does not grow the surface. |

### A resident local model is unloaded after it sits idle (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D414 | **A resident local model auto-unloads after `ai_idle_unload_minutes` (default 10, `0` = never) of no activity, timed on `time.monotonic()` — never `time.time()` — with no exemption for a model the user explicitly loaded** | Before this the only way an 8GB model's memory came back was quitting the app or loading a different one, and a page opened once and forgotten held it forever (SPEC AI-13). **Monotonic, not wall clock**, because the whole point is a window measured in minutes of actual elapsed time: `time.time()` jumps on NTP correction and DST, and — the case that decided it — does not stop advancing while a laptop sleeps, so a machine closed at 9am and opened at 5pm would report its last-used model as eight hours idle and unload it the instant the app resumes, the opposite of "was not used". `time.monotonic()` does not advance across a suspend at all, so a woken, untouched model gets exactly one more reaper tick before it counts as idle — the sleep itself costs nothing. **Default ON at 10 minutes**, not opt-in: it is a behavior change for every existing install, accepted because the failure it fixes — gigabytes held for the life of the process — is worse than the cost it adds, one reload the next time the model is actually used, and `0` is one PUT away for anyone who would rather keep everything warm. **No exemption for an explicitly loaded model**: `loaded_at` seeds `last_activity` and the SAME rule (`reap_idle`, off `state == "ready"`) covers every resident worker regardless of how it got there — a second code path for "the user pressed Load" would be a second place for this feature to be wrong, for a distinction (explicit vs. implicit residency) the supervisor does not otherwise track. This is the choice most likely to draw a complaint, and the deliberate answer is that a per-model "keep warm" pin is a v2 feature if it does, not a day-one exemption that halves the test matrix. **A busy worker (`in_flight > 0`) is exempt from the idle window, but only up to a separate LEAK CEILING derived from the request timeout that actually bounds the call — `TRANSCRIBE_TIMEOUT_S` (4h) for transcription, `GENERATE_TIMEOUT_S` (900s) otherwise — plus a margin, and never less than the idle window itself.** The first cut collapsed this into one predicate: since `generate_text`'s stream re-stamps `last_activity` on every yielded chunk, a genuinely live call is never stale, so (the reasoning went) `in_flight` needs no exemption at all — the idle-window check alone would spare a busy worker and reap a leaked one. That reasoning is correct for `generate_text` and WRONG for the other two generation paths: `generate_image` and `generate_transcript` are single blocking `_worker_request` calls, stamped once on entry by `_in_use` and not again until the reply returns — which for a real transcription can be most of `TRANSCRIBE_TIMEOUT_S` later. Caught in review before merging: the collapsed predicate would have unloaded a 90-minute transcription at the ten-minute mark, mid-decode, with `_terminate` killing the process the request was still waiting on — precisely the failure `generate_transcript`'s own lock-ordering comment says an evicting load must not cause ("lost its transcript, failed its row with 'the transcription process did not answer'"), reintroduced by the idle reaper instead. Past the leak ceiling, `_worker_request`'s own timeout would already have raised, so a counter still reading positive there cannot be a slow answer — only a leaked `generate_text` iterator a page abandoned without closing. **Rejected: reaping inside `refresh_memory()`** — it runs only when the status endpoint is polled, so a model would be reclaimed only while somebody had the AI Models page open, which is the opposite of the promise; a reaper thread ticking on its own regardless of who is looking is the only shape that keeps it. **Rejected: a wall-clock deadline stored per worker** (`unload_at = time.time() + window`) — recomputing "idle for how long" from a stamp and `now` on every tick is one fewer piece of mutable state than maintaining a moving deadline that every re-stamp would have to update in lockstep, and it is the wall-clock problem above wearing a different hat. |

### The chat shows a turn it did not start (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D415 | **A run's `done` is per TURN and means "a `result` with nothing after it" (or a dead process) — not "a `result` has been seen"; and the page asks the server every 10s whether anything is live for this session (`liveWatchTick`), so ANY active turn lights the running chrome and streams, whoever started it — and where there is no run dir to find (a `claude` driven from a terminal), the same lap stats the ONE transcript and re-runs the reload renderer when it moves. A `<task-notification>` record renders as a one-line system chip, never as a message bubble** | Reported from use (Akshil, 2026-08-21): a background shell finished, the harness woke the agent, it worked for minutes — and the chat showed **no working line, no streaming, nothing at all**, until a few "continue"s later produced a reply. Two separate defects, and the first one is the reason the second was ever visible. **(1) `done` latched on the first `result`.** One `claude -p` process runs SEVERAL turns: a turn that started a background command is woken by the harness with a `<task-notification>` prompt when the command finishes, and everything it then says is written to the SAME `out.jsonl`, after the `result` that closed the first turn — verified on real run dirs, two `result` rows in one file, the second carrying `origin: {kind: "task-notification"}`. `_poll` set `done = True` at the first one and never took it back, so the woken turn was reported as a finished run: `pollLoop` had already torn down the working line, cleared the `run` param and released the running chrome. `done` is now `idle or not alive` — `idle` being "the last row of the file is a `result`" — which goes back to False on the wake's first row. **(2) Nothing ever asked again.** The page's only "is something running that I am not showing?" question was `adoptLiveRun`, a three-second window around OPENING a chat; after that the running state could only be armed by this composer's own `sendMessage`. So the state machine was about who sent the message rather than about what the session is doing, and every externally-initiated turn missed it — the harness's wake, the same chat open in a second pane, a spawn from the canvases workspace. The watch is now also a timer (`LIVE_WATCH_MS`, 10s) running `adoptLiveRun` for ONE lap, and the answer comes from the server (`live_run`, which reads the run dirs' pids) rather than being inferred from what this frame happens to have sent. **Rejected: `done = not alive`** — the obvious fix, built first, and it hands the COMPOSER to the background command: the process outlives the finished turn for as long as the command runs (an hour is not unusual here), so the chat would sit busy and queue everything typed into it, over a run that is saying nothing. It also meant a permanent "Thinking…" over a wait, which is the same lie as the one being fixed pointed the other way. Per-turn `done` plus a standing watch costs at most one poll interval of lag on the wake and keeps the chat idle — and composable — in between. **Rejected: deriving "running" from the session transcript's tail instead of from run dirs** — it is the only thing that would also catch a `claude --resume` the user drove from a terminal, and it means the page tailing `~/.claude/projects` and guessing turn boundaries out of records written for a different purpose; `live_run` already answers exactly this question about every run this app spawned. **That gap is now CLOSED, and the rejection above still stands — what changed is who reads the transcript.** Tested from use the same day (Akshil, 2026-08-21): an interactive `claude` in a terminal, and a `claude --resume`, on the very session the chat has open. There is no run dir, so `live_run` is blind by construction and the chat showed the conversation as it stood at page load until the reader hit reload — at which point the missing turns appeared correctly, chip and all, because **the reload path was never the broken part**. So the reload is what got automated, and deliberately nothing more. **The server stats ONE file.** `GET /api/claude-sessions/liveness` answers `(mtime, size, running)` for the single transcript the page names — one `os.stat`, no directory scan, no other session opened, and it refuses any path outside the projects tree. The PATH is the parameter rather than the session id because resolving an id to a transcript is the one thing this endpoint must not decide: with copied files the same id exists in several project buckets with divergent content, and only `_history` knows which folder the chat is open on — so `_history` now returns the path it read, as the watermark it read it at. **The page compares, on the lap it already runs.** The existing live watch (5s while visible, plus the storage poke and focus) asks only when nothing attached — run dirs first, always — and a `(mtime, size)` past the watermark re-runs `loadHistory` in a REFRESH mode: no skeleton, scroll kept unless the reader was already at the bottom, and the per-transcript records (card folds, schedule baselines) left alone, because re-folding a chip every five seconds is the redraw made visible. The pair rather than mtime alone: a coarse clock can put two appends in one tick. The stat is taken BEFORE the rows are read, so a write landing mid-read costs one redundant re-render and never a swallowed turn. Steady state is ~0.2 stats/sec per open chat tab, zero while hidden, and the transcript parsed only when the watermark actually moved. **The running line reads the last MESSAGE, not a clock** (`session_liveness.transcript_turn_open`, a second rule beside the 45s activity window and the measurement is the argument): the window is right for a badge in a LIST and was a visible lie under an open conversation — a run started outside this app writes no `turn_duration` record, so the line kept shimmering under a reply that had already landed for the balance of its 45 seconds. A user row, or an assistant row still calling tools, is a turn in flight; a plain assistant reply is one that ended; `STALE_TAIL_SEC` caps how long a turn left open by a dead process may claim to be running. The line carries **no stop button and no token count** — the process is not ours to kill and this is a file read, not a stream — and granularity is a whole turn, which is the honest ceiling on following a file somebody else is appending to. Measured end to end in the app: a turn sent from a terminal appeared with its working line ~4s after the CLI wrote the prompt row and the line cleared within one lap of the reply landing, with no reload; the run-dir path was re-checked in two tabs and still streams token by token with its stop control, and the external line never appears for a turn this app drove. **Still rejected: the PAGE tailing `~/.claude/projects`** — turn boundaries stay `_history`'s job, on the server, over rows it already knows how to read; a stat plus the existing renderer is what makes that affordable. **The watch attaches `quiet`**: the run it finds may be one whose first turn is already on screen under an older message, and `resumeRun`'s no-match fallback prints the run's message as a fresh user line — this page inventing a turn. Suppressed, the woken reply lands as what it is. **`<task-notification>` gets a chip, not a bubble.** The same wake was rendering as a screenful of raw XML in the transcript, because `_history` filtered exactly two synthetic `user` records by name (`<local-command`, `<command-name`) and every other one — the whole `_MACHINERY_DROP` list the session TITLES are already filtered by — arrived verbatim. Those records are now dropped as turns and a task-notification becomes a `notice` segment instead, one muted line from the CLI's own `summary` ("Background command … completed"), rendered as **plain text, never markdown** (the summary quotes a command line). Dropping it silently was rejected: it is the only thing on screen that accounts for a reply arriving under no message. One renderer for both shapes — `_segments_from_rows` reads the live `system`/`task_notification` row and the transcript's XML record into the same segment — so a restored conversation and a streaming one show the same chip. **Revised the same day, from a live test of this very change (two tabs on one chat): a TIMER ALONE IS A BLIND WINDOW, and a short turn fits inside it.** The second tab showed the reply ten seconds late with no chrome at all — the lap that finally looked found a run that had already FINISHED, so `resumeRun` took its done-branch. Arming mid-turn was never broken (a lap that catches a live run does light the working line and the stop square); the fix is to ask SOONER and to render what a late look finds. **Three triggers now, cheapest first.** (1) **The tab that started the turn pokes the others**: `noteChatActivity` already stamps `localStorage` as a poll loop OPENS — for the shell's tasks store — and a `storage` event fires in every other document and never in the writer, which is exactly this question's shape. So the two-tab case costs no polling and lands in ~1s, measured, with the other tab attaching mid-stream. The key is one constant both writers and this reader share. (2) **`visibilitychange` and `focus`**, so a backgrounded tab catches up on return rather than waiting out an interval. (3) **The interval, halved to 5s and SKIPPED while the document is hidden** — it is the backstop for the one starter that pokes nothing this page can hear, a run spawned by the SERVER (a scheduled message, the canvases workspace). *Accepted:* a hidden tab can miss a server-started run until the reader comes back, which is the repair it used to get anyway, and chrome nobody is looking at is worth nothing. **And `quiet` became DEDUPE rather than silence.** Suppressing the run's user line unconditionally was right for the woken run whose message is already on screen and visibly wrong for the other tab's send, whose words this transcript had never shown: the reply hung under no question. `quiet` now means "do not print a message this transcript is ALREADY showing", asked of every user bubble — where `matches` asks only the last one, because `matches` also decides whether to STRIP rows and this only decides whether to print. The done-branch gained the same test, so a turn made in another tab and finished before the first look is rendered whole (prompt plus reply) instead of dropped in silence. A message sent twice reads as already-shown and prints nothing, the safe direction and the same coincidence `matches` already refuses to resolve. |

### Withdraw the transformers-text engine family (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D416 | **Remove the `transformers-text`, `transformers-text-cuda` and `transformers-text-rocm` runners D293/D381 added — the three registry rows, the three `transformers_text*/` folders, the shared `runners/torch_text.py`, the `catalog.SUGGESTIONS["transformers-text"]` shortlist and its two `_SHARED_SUGGESTIONS` aliases — leaving `text-generation` served by `mlx-text` on Apple Silicon, `llamacpp-text` everywhere else, and `llamacpp-text-vulkan` as the one opt-in row below them. `llamacpp-text` therefore becomes what `auto` resolves to on Windows and Linux, which AI-11f was explicitly written to prevent** | **Measured, like for like, not judged.** Qwen3.5-4B, one prompt, a 128-token cap, back to back and uncontended on Linux x86_64 (Ryzen 5 9600X, 30GB RAM, Radeon RX 9060 XT / RADV GFX1200 16304MiB): llama.cpp Vulkan (Q5_K_M) 2.93GB download, 1.6s load, 0.14s first token, **53.4 tok/s**, 3.14GB peak RSS; transformers ROCm (bf16) 8.7GB, 8.3s, 1.84s, **12.6 tok/s**, 9.18GB; llama.cpp CPU 2.93GB, 1.3s, 0.71s, **6.4 tok/s**, 3.44GB; transformers CPU 8.7GB, 5.9s, 2.9s, **2.7 tok/s**, 8.37GB. llama.cpp wins BOTH hardware tiers on every axis at once — 4.2x throughput on GPU, 2.4x on CPU, ~1/3 the download, ~1/3 the memory — and all four runs produced correct fluent prose, so this is like-for-like on quality rather than a speed/quality trade. **The losing engine got a fair fight**: `transformers-text-rocm`'s availability gate was verified HONEST on RDNA4 first (torch 2.13.0+rocm7.1 reports `cuda_avail True` on gfx1200 and really did generate on the GPU), so the ROCm row was not measured while silently falling back to CPU. Owner's product rule: an engine that does not earn its place gets removed, and the user is not given several options for doing one thing. **Breadth of Hub reach is explicitly NOT a counter-argument** — models here are curated suggestions (AI-11a), so "transformers reads more arbitrary repos" argues for a catalog this app does not have. **Two things argue against removal and are recorded as accepted costs rather than answered.** (a) llama.cpp wheels come from the maintainer's GitHub Pages index rather than PyPI, and D411's own audit found 4 of 16 macOS arm64 releases fail `zipfile.testzip()`; that risk was tolerable while this row was fourth and opt-in and is now carried by the DEFAULT on two platforms. What makes it affordable: the failure is loud and at install time (`uv sync` reports it verbatim through PY-18 — a first-run error with a message, never a wrong answer later), the pinned `0.3.29` Linux and Windows wheels were verified intact and working, and macOS arm64 — where every audit failure was — still resolves to `mlx-text` ahead of this row. (b) `llamacpp-text-vulkan` becomes the only GPU text path on Linux/AMD while its `_offload_schedule` over-commit backoff is known NOT to engage there: radv satisfies an over-commit by evicting other clients instead of erroring, which took a desktop session down during testing (reported separately on PR #706). That is exactly why the default moved onto `llamacpp-text` and deliberately NOT onto the Vulkan row, which stays last and unreachable by `auto`. **The one user-visible loss, stated rather than hidden:** `formats.loaders()` now maps a directory of plain safetensors to `mlx-text` alone, so a bf16 text checkpoint is loadable on Apple Silicon and nowhere else — a Linux user with `Qwen/Qwen3-8B` already on disk gets a card naming MLX and the registry's own "needs Apple Silicon" sentence. `catalog()` offers them the GGUF of the same model at roughly a quarter of the download, and `test_a_plain_safetensors_causal_lm_is_MLXS_AND_UNAVAILABLE_off_a_mac` pins the new answer by name. **`formats.py`: kept what a surviving branch reads, deleted what nothing does.** `UNLOADABLE_QUANT` becomes a `frozenset` of method names where it was a dict of refusal SENTENCES — only `torch_text._refuse_unloadable` printed the sentences, and its keys are still read by `loaders()` to keep an AWQ/GPTQ/bitsandbytes/compressed-tensors repo away from `mlx-text` too, which cannot load one either. Its bitsandbytes comment (0.65 tok/s NF4 against 2.33 bf16 on Apple Silicon) asked in terms for an x86 re-measurement before anyone reversed that refusal; **this record IS that re-measurement**, and it settles the question by making it moot — the quantized path this app offers is GGUF, which needs no extra package and beats the unquantized transformers path it would have competed with. `TRANSFORMERS_RUNNERS` is deleted (no codes left to hold together) and `is_mlx_checkpoint` with it — unlike D406's parakeet constants, which survived because an early return depended on them, this function's ONLY caller was the fork in the safetensors branch, and with the transformers arm gone both arms answer `mlx-text`; there is no downstream claim that needs the two told apart, so keeping it would have left inert code behind a name. `TORCH_WEIGHTS` keeps its name: the extensions are torch's conventions and `diffusers-image*` and `mlx-text` still read every one of them. **A stored preference naming a removed code degrades to the ordering.** `registry.resolve()`'s existing unknown-code branch — written for a prefs.json from a NEWER build — answers it with no new mechanism: the ordering decides, the reason travels out for the page to show, and `selected` is still reported AS STORED, because a preference silently corrected on read is one the user can neither see nor undo. That last property is what created the only new frontend work in this change: a `<select value=x>` whose options contain no `x` renders EMPTY rather than falling back to its first row, so a user who had chosen Transformers would open the Engines tab to a blank picker with a warning under it. `engines.strandedSelection` detects it and `AiModelsEngines.tsx` renders the stored code as one extra DISABLED option. Pinned by `test_a_removed_engine_code_in_prefs_degrades_to_the_ordering` (all three codes, not one standing for the family) and by `engines.test.ts`. **No shipped platform is stranded**, pinned by `test_every_shipped_platform_keeps_a_local_text_engine` over an ENUMERATED platform table rather than a loop over the registry — a registry cannot know which platforms this app is distributed to, and "something serves text generation" would still pass if a removal quietly moved every non-Apple machine onto an engine the catalog and docs do not describe. Coverage on Windows and Linux is now ONE runner deep and `_llamacpp_platform`'s docstring says what that costs (Windows ARM64, and Linux outside x86_64/aarch64/riscv64, now have no local text engine and fall back to `claude-cli` — machines the transformers rows advertised a Load button to but could not have served either). **Rejected: keeping the CPU row alone as a safetensors fallback.** It is the slowest of the four measured configurations, it is the one that made this app's text catalog a 9-19GB download, and "one more option that is worse at everything" is the outcome the owner's rule names. **Rejected: leaving the pyproject.toml cross-references dangling** to avoid a `state_digest` resync of existing Diffusers and llama.cpp environments (D383's reasoning) — `projectenv.state_digest`'s own docstring records that a resync for a comment edit is a fast no-op through uv's cache, so a permanent dangling reference is the more expensive of the two. **Rejected: rewriting the SPEC items in place.** AI-11, AI-11b, AI-11c, AI-11d, AI-11f and AI-2b carry amendment notes and AI-11h states the withdrawal, following D406's precedent: the reasoning that chose torch is what makes the reversal legible, and deleting it would leave a reader unable to see that the packaging objection was overturned by a benchmark rather than by a new packaging fact. |
| D418 | **A mixture-of-experts GGUF gets an offload rung dense models cannot use — every LAYER on the GPU with only the expert tensors pinned to system RAM — inserted directly above pure CPU in `llama_text._offload_schedule`, and paid for with a monkeypatch that no version bump will retire** | D411 sized GPU offload by TRYING a shrinking sequence of `n_gpu_layers`, because nothing in the binding can query free VRAM. That sequence has one strategy — drop whole layers — and for a MoE model it is the wrong one: its expert tensors are most of the file but only a few are multiplied per token (`LFM2.5-8B-A1B` activates 4 of 32), so parking those in system RAM buys most of the VRAM back while leaving attention, which every token needs, on the GPU. llama.cpp exposes exactly this as `--n-cpu-moe`, which is not a model flag but a `tensor_buft_overrides` entry: a `(regex, buffer type)` pair the loader consults per tensor. **Position in the sequence is the decision, and it was measured rather than argued** — `LFM2.5-8B-A1B-Q4_K_M` (24 layers, 32 experts, 4 used) on a Radeon RX 9060 XT, warm, median of three 64-token generations: 24 layers dense = 4909MiB VRAM / 161.6 tok/s; 8 layers dense = 1731MiB / 55.6 tok/s; 3 layers dense = 659MiB / 41.4 tok/s; all layers with experts on CPU = 462MiB / 50.5 tok/s. So the rung goes BELOW every fractional step (full offload is ~3x any split and must be tried first; the thirds beat it whenever they fit) and ABOVE pure CPU, which it dominates on both axes at once — less VRAM and more throughput — and that double dominance over its one neighbour is the entire argument for it existing. **The gain is modest and is stated as such: +22% over the rung it displaces here**, growing with the fraction of a model's weights that are experts, so a 4-of-32 row is a mild case rather than a showcase. **`llama-cpp-python` cannot express this and upgrading will not change that** — `Llama.__init__` never touches `tensor_buft_overrides`; it fills a fresh `llama_model_default_params()` and constructs the model in the same call, leaving no object to reach between the two, and its `**kwargs` are swallowed rather than forwarded. Checked against 0.3.29 (the audited pin), 0.3.32 and 0.3.35 (latest, 2026-08-21): all three still carry the binding's own `("tensor_buft_overrides", ctypes.c_void_p),  # NOTE: unused` and no live reference in `llama.py`. So `_experts_on_cpu` patches the factory, restores it in `finally`, and keeps the override array alive across the `yield`; the comment says a pin bump is NOT a reason to go looking for its removal, because that is exactly the maintenance instinct this would otherwise invite every release. **Two facts make it far smaller than "write ctypes bindings" implies, and both were verified rather than assumed**: the struct field is already declared `c_void_p`, so the params struct needs no redeclaring — a plain `cast` fills it — and `ggml_backend_cpu_buffer_type` is reachable on `llama_cpp._lib`, the handle the binding has already opened, so there is no second `dlopen` and no `.so`/`.dylib` name to get right per platform. The patch must target the SUBMODULE (`llama_cpp.llama_cpp`), not the package: `llama.py` does `import llama_cpp.llama_cpp as llama_cpp`, so patching the package attribute binds a name nothing reads and silently does nothing — found by doing it, and pinned by a test whose fake carries the nested submodule for that reason. **Detection is a header read, not a name match**: `formats.gguf_expert_count` looks for the `<arch>.expert_count` key by SUFFIX, the same technique and the same fail-toward-None contract as `gguf_block_count`, which it now shares a scanner with (`_gguf_uint_by_suffix`) rather than duplicating; a dense model genuinely carries no such key, verified against the local cache. It is asked only when a GPU could use the answer, since on a CPU-only build every tensor is in system RAM already and the rung is a no-op. **Reported as its own device string, `"gpu (experts on cpu)"`** — "gpu (partial)" would misdescribe it (every layer IS offloaded) and "gpu" would overclaim it (most of the bytes are not), and AI-11f's rule is that this field is a measurement, so a fourth outcome needs a fourth word rather than the nearest existing one. **Failure to build the override is explicitly NOT fatal**: it costs throughput, never correctness, and the next rung is plain CPU anyway, so a binding that has moved out from under this warns and loads without it — and must then report plain `"gpu"`, which is what the degradation test pins. **Rejected: bumping the `llama-cpp-python` pin in the hope of a supported API** — checked three versions including the latest; the field is still dead upstream, and D411's audit makes a pin bump expensive (every platform wheel re-verified) for something that would not help. **Rejected: applying the override unconditionally to every MoE model** — it is a PESSIMIZATION when the model fits in VRAM (161.6 -> 50.5 tok/s here), which is the common case on a desktop card; it belongs in the backoff path and nowhere else. **Rejected: reaching for `--n-cpu-moe`'s partial form (`N` layers' experts rather than all of them)** — that is a second sizing dimension crossed with the existing one, and the probe is already "try and catch"; one all-or-nothing rung answers the case that motivated this (the model does not fit) without multiplying the attempt count. |

### Learn leaves the app for the community catalog (2026-08-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D419 | **The Learn content (`core_apps/learn/`) and every piece of its plumbing come out of the app: the `learn` row in `BUILTIN_MOUNTS`, the `learn_zip_path`/`ensure_learn_mount`/`learn_mount_ready` wrappers, `/api/config`'s `learn_mount_ready`, the `/learn` route + `LearnView` + `@apps/learn` + the sidebar's "App Basics" entry + `LearnIcon`, both packaging steps (`build_dmg.sh` 4e's learn half, `build_windows_installer.ps1`'s, `supervisor/paths.py`'s `FUSED_RENDER_LEARN_ZIP`, `dev.sh`/`stage_builtin_zips.sh`), and the frontend's whole builtin-mount readiness poll — it now ships as a community app (`learn-fused-render`), reversing D123's "the app ships this content" premise while keeping D123's mechanism** | The content is a tutorial about the app, not a feature of it, and a community app is where a tutorial can be revised without a DMG. **The MECHANISM stays, and stays exercised**: `sessions` is still a builtin zip mount, so `BUILTIN_MOUNTS`, the archive-backend attach, the force-detach-on-every-startup refresh and the readiness flag all keep their coverage — `tests/test_learn_mount.py` was repointed to sessions as `tests/test_builtin_mounts.py` rather than deleted, since it holds the only tests for the detach/force-unmount/serve-stop mechanics, and the duplicate record tests in `test_sessions_mount.py` went instead (that file is now only the shipped content's state-dir contract). **Retiring a builtin needs a migration, so `ensure_builtin_mounts()` gained one** (`_prune_retired_builtin_mounts`): every already-installed machine has a `{"builtin": "learn"}` record in its `mounts.json`, and dropping the row is not enough to reach it — `_ensure_builtin_mount`'s remove-when-the-zip-is-gone branch only ever runs for names still IN `BUILTIN_MOUNTS`, so nothing visited the record, `run_automount` re-tried attaching it every startup and failed once the upgrade took `learn.zip` out of the bundle, and `delete_mount` 400s on any `builtin` marker, leaving a broken mount row the user could not clear. Any record whose `builtin` name has left the dict is now dropped at startup (force-detached + serve stopped, planned under `_store_lock` and executed outside it, like the zip-gone path), which is D123's own "removed when the zip is absent so it can't linger as a broken mount in the UI" extended to the builtin ITSELF going away. Deliberately not gated on the old zip still being on disk: retired is retired, and an older app sharing the home re-creates its own record on its next startup exactly as it does after any other removal. Three consequences taken deliberately: (a) `StatView`'s `variant` is gone with its last non-explorer caller, so breadcrumb/header/recents are unconditional again and `Preview`'s `hideHeader` goes with it — the third variant, "app", had already gone the same way with D264; (b) `useLearnMountReady`/`useBuiltinMountReady` are deleted rather than kept generic, because a zero-key `BuiltinMountKey` union does not compile in TS and `sessions_mount_ready` has no frontend consumer to poll for — git history is the pattern's home until a surface needs it; (c) D177's three-copy library promise loses its source of truth (`check_libs.py` ran, so it was the one pinned to the bundle) and collapses to one copy, `skills/fused-render-authoring/SKILL.md`, now pinned DIRECTLY to what the macOS bundle ships. Keeping a repo copy of the content purely to keep that test's shape was rejected: two copies of a tutorial drift, and the test's job is the promise, not the file. The `"learn"` category in `frontend/src/apps/builder/app-categories.ts` stays — it orders the community catalog, which is exactly where this content now lives. |
| D420 | **The AI Models surface becomes a sub-app (`frontend/src/apps/ai_models/`) with one PATH per tab (`/ai-models/playground\|local\|engines\|usage`; `discover` left the strip at D423, see there), and the Hugging Face cache walk moves out of its router into `fused_render/ai/hub_cache.py` — with NO legacy alias for either the old `?tab=` query or the `/preferences?tab=engines` rewrite that pointed at it** | Eighteen `Ai*`/`Playground*` files sat flat in `@shell/`, interleaved with the scheduler, the queue dock and the mounts page, and `AiModels.tsx` was 1416 lines of which ~900 were the Local tab inlined into the tab dispatcher. Nothing about the cluster was ever shell: it imports only `@platform`, and the only two things outside it that reached in were shell surfaces LINKING to it (`GlobalSidebar`'s live dot, `Home`'s playground strip) — the allowed direction, so `check-boundaries.mjs` passes unchanged. **A tab became a path because a tab here is a PAGE**: five unrelated surfaces (a chat client, a disk inventory, a Hub search, an engine picker, a usage graph) sharing only a heading, and naming four of them in a query string while the fifth was the ABSENCE of the param left the page's own address ambiguous. **What did NOT change is the mount**: the five tabs are still one `AiModelsPage`, one un-keyed branch in `App.tsx`, because the cache walk is a filesystem crawl over every blob that THREE tabs read (Local's cards, Discover's already-have-it answer, the caption's total) — five independent routes would mean either three walks or three answers, and a remount per tab click would re-walk it and discard Discover's typed search. That walk is now `lib/useCacheScan.ts`, held above the strip, which is the structural statement of the same fact. The stages the user asked to see separated (`ChatStage`/`ImageStage`/`TranscribeStage`) were ALREADY separate components; what was wrong was that each imported `readParam`/`writeParams` from `AiModelsPlayground.tsx` — its own parent — so those moved to `lib/params.ts` and the 60-line composer-seed builder to `playground/appSeed.ts`. Server-side the same inversion, worse: `routers/ai_models.py` was 1707 lines of cache scanning under three endpoints, imported by `ai_runtime.py` (`cached_models`) and `hub_models.py` (`_scan_repo`) — a module three surfaces depend on reachable only through one page's router. It is `fused_render/ai/hub_cache.py` now, beside `catalog.py`/`registry.py`/`supervisor.py`; the router is 159 lines and **no `/api/*` URL changed**, since renaming endpoints is churn with no user benefit. **NO REWRITES, owner's call**: `/ai-models?tab=local` is not mapped, and the `/preferences?tab=engines` rule in `router.ts` is deleted rather than retargeted — an unknown sub-path falls back to the default tab silently (the same forgiving posture as an unknown `_mode`, PT-9), so a stale link opens the page rather than erroring, and the alternative was carrying a dead URL shape forever. **The refactor walked the page out of `test_shell_routes.py`'s blind-spot guard and that had to be repaired in the same change**: that test derives its route list by scraping `pathname === "..."` literals out of `App.tsx`, and the prefix dispatch (`isAiModelsPath`) is not a literal — so the page silently dropped out of the one test that catches "works when you click, 404s when you reload". It now also parses `AI_MODELS_TABS` out of `routes.ts`, keeping the list DERIVED rather than restated, and `shell.py` gained `/ai-models/{tab}` as a wildcard for the same reason: the tab set is the frontend's, and the server has no business holding a second copy. |

### A suggested model may come from our own distribution (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D422 | **One FILE off the mirror is a second object with a weaker claim — `models/<org>/<name>/files/<filename>/manifest.json`, one named file, no `complete` flag — and NOT a relaxed repo manifest; plus the permission translation without which the whole hook is dead code** | D421's branch lives in `download_snapshot` alone and `llama_text.download` does not go through it: it fetches ONE GGUF with `download_file`, because a GGUF repo publishes dozens of quantizations of one model (`unsloth/Qwen3.5-9B-GGUF` is 147.81GB whole for a 2.6GB file). Since D416 made llama.cpp the only local text engine on Windows and Linux, every suggested TEXT model on those platforms was off the mirror — invisibly, since a download that never asks looks perfectly normal. `download_snapshot(allow_patterns=[file])` was rejected as the fix: the repo manifest is accepted only when it asserts `complete: true`, so serving one file that way means mirroring 147.81GB, and weakening the assertion breaks AI-5k. Hence a third object with the blob at the EXISTING per-repo key (a repo published both ways stores one copy of each blob) and a SEPARATE reader — same validation vocabulary, same permission, same one-guard/`Cancelled`-only degradation, but exactly one entry whose `name` is the file asked for, and `complete` neither required nor read. **The absent assertion is safe because of what the caller does next, not because one file is small**: `download_file` writes no AI-5k fetch record and never has, so there is no record for a partial answer to self-certify — which is the entire harm that flag prevents — and the worst a wrong manifest can do is serve wrong bytes the sha256 check keeps out of the cache. Adding a record to that path would put the assertion back in scope. **The permission had to be translated**: `llamacpp-text`'s catalog ids are bare `.gguf` FILENAMES (the page keys a many-quantization repo by file) while the worker names the recipe's REPO, and `mirror.allowed` matches `org/name` — so `FUSED_MODEL_MIRROR_OK` carrying the filename is refused by the client forever, and the feature was on in every unit test and off for every real model. `catalog.mirror_id` maps a suggested id to the repo the worker will name; the privacy rule is unchanged (nothing outside `all_suggested_ids()`, curated recipe table only, one model's answer), and it belongs in the server process because `catalog`/`formats` are unreachable from a runner. The proof is parametrized over the REAL curated rows, because `test_a_suggested_model_may_use_the_mirror` asserted equality with the catalog id and passed while describing a value the client refuses. The generator gains `--model org/name --file NAME` (one file out of a partial cache, no Hub listing, name validated as the single path segment it becomes) and its default run now expands curated GGUF ids into `(repo, file)` targets — which also fixes a default invocation that could not succeed, since those ids were looked up as `models--<filename>` folders, printed SKIPPED and always exited 1. Component repos still stay on the Hub, by construction rather than by rule: the FLUX GGUF transformer's repo is not the permitted id. |
| D421 | **A SUGGESTED model may be fetched from our own CloudFront distribution instead of the Hub — two static objects (a mutable `manifest.json` and immutable `<commit>/<etag>` blobs), the branch living in `download_snapshot` alone, and any doubt at all going back to the Hub** | The point is the access log: one `manifest.json` request per download attempt, made before a single byte moves and logged even on a cache hit, is how we learn "did this user download a model" **with no telemetry in the app at all**. `HF_ENDPOINT` was rejected as the mechanism — it is a protocol switch, so the mirror would owe `/api/models/…`, the `x-linked-etag`/`x-linked-size`/`x-repo-commit` resolve headers and Xet's `xet-read-token`, none of which two static objects have. **What lands on disk is hf's cache layout byte for byte** (blob under its etag, relative symlink from `snapshots/<commit>/`, `refs/main`, plus this app's AI-5k fetch record), which is the whole design: the loaders, the Local tab's inventory, disk usage, deletion and the AI-5k fast path keep working untouched because what they read is a normal hf cache entry. The branch sits in `download_snapshot` **below every runner call site**, so no runner changed and none can forget it, and it is skipped under `**kwargs` for AI-5k's reason. **Every failure degrades to today's Hub path** — 404, 5xx, a host that does not answer, a non-JSON body, an unknown schema, a manifest whose fields do not hold up, a mid-download drop, a hash mismatch — announced on stderr; only `Cancelled` escapes (AI-5e: a ✕ must never start a download somewhere else). That promise does not rest on having enumerated a URL library's exceptions: `http.client.HTTPException` is neither `OSError` nor `ValueError`, so an `IncompleteRead` off a truncated chunked body once FAILED a download the Hub could have served — the whole branch, manifest call included, now sits inside one guard naming that family. **A 401/403 on a mirror blob is never re-resolved against the Hub**: the URL is commit-pinned and immutable, so there is nothing to refresh, and re-resolving both made a huggingface.co request in the middle of the one download that must not make one and silently dropped the sha256 check (Hub metadata carries no digest, and the etag/size/commit guard still passed because a mirror etag IS an hf blob name) — so the digest is fixed at plan time. **The manifest is a trust boundary, validated field by field**, because the failure that matters is a manifest that is plausible and wrong; rejection reads as NO MIRROR rather than as an error, a zero size is legal, and `complete: true` is required because the client writes its AI-5k record from that same file list and would otherwise self-certify a subset. **Blobs are hashed on the mirror path and not on the Hub path**, and the asymmetry is the point: here we are the origin, nobody else would notice a bad byte we shipped, and a wrong blob under a real etag is permanent. **The permission is PER-MODEL and comes from the supervisor** (`FUSED_MODEL_MIRROR_OK`, set only for ids in `catalog.all_suggested_ids()`, at both spawn sites, stripping an inherited one) — a privacy choice, since probing for an arbitrary id would tell us which models a user downloads; `mirror`/`worker_base` are stdlib-only, enforced by reading their own source. **Manifests are GENERATED from a real hf cache** (`scripts/build_model_mirror.py`) and never transcribed, with completeness proved against the Hub's listing at that commit — free on a build machine, and "the folder exists" was the original bug, since an `allow_patterns` image fetch leaves a deliberately partial cache — and any skipped model is a non-zero exit. **Windows fetches from the mirror too**, which is a correction to how this shipped: the only transport is `_segmented_fetch`, it used to refuse without `os.pwrite`, and a win32 client therefore made the manifest request, declined and took the Hub path — so Windows acquisitions were invisible to the very logs the feature exists to produce. It now degrades to a single append-only stream instead of refusing (AI-5i), which keeps the same "a counted byte is a written byte" guarantee by another route — the file's length IS the progress, and a resume is a `Range` from that length — at the cost of parallelism within one file and nothing else. The first real Windows fetch then found a LATENT bug rather than a structural one: the part file was opened in the CRT's default text mode, so every 0x0a became 0x0d 0x0a and the mirror's own sha256 refused a repo it had fetched in full — fixed with `O_BINARY` at every `os.open` (enforced by reading the module's AST, since the flag is 0 on POSIX and unobservable there) plus a part-file length check on that route, the length being an independent witness where the cursors are not (AI-5i). Out of scope: component repos stay on the Hub, and a pinned commit means a suggested model updated upstream keeps installing the pinned one until the build script reruns. **AMENDED (2026-08-22): `FUSED_MODEL_MIRROR` now ships ON, defaulting to `https://render.fused.io/mirror` (`mirror.DEFAULT_BASE`) when unset, reversing the "unset on every shipped build" line above.** The documented opt-out is setting it to `""` — `os.environ.get` already distinguishes "unset" from "set to empty", so the opt-out costs no new mechanism, and anything that is not a valid `http(s)://host` URL (`"off"`, `"0"`, `"none"`, garbage) lands on the same `""` through `_valid_base`'s existing scheme/netloc check rather than through a second keyword list. **This is safe precisely because nothing about the per-model gate moved**: `allowed()` still requires both a base URL AND `FUSED_MODEL_MIRROR_OK == model_id`, so a default base names no repo by itself, and a model outside `catalog.all_suggested_ids()` still produces no manifest request — the reversal only changes what the base defaults to, never who may be named to it. And every failure already degraded to the Hub — 404, 5xx, unreachable host, bad manifest — which is what makes shipping the default safe even though only 14 of 22 curated targets are published on the live distribution today: Windows and Linux resolve to the 8 unpublished ones (5 GGUF text models plus the two faster-whisper rows plus one FLUX component), so for them the mirror is on but not yet EFFECTIVE — every request 404s and falls through — while Apple Silicon's 13 resolved rows are all mirrored and get the real benefit now. Amending this row rather than opening a new decision: the choice being reversed is the "out of scope: unset on every shipped build" line of THIS row, not a new question, and a reader tracing D421 should see both the original call and why it changed in one place. |

### One AI Models page, and a cancelled download that admits it (2026-08-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D423 | **The Local tab becomes the whole answer — one horizontal carousel per capability holding the loaded model, then what was downloaded most recently, then the curation's recommendations with a Download button — and the Discover tab leaves the strip (its code stayed, unrouted, until D426 moved the search half of it onto this same page and deleted the directory). The carousel's arrows appear on MEASURED overflow, never on a width breakpoint** | The Local tab was an inventory and Discover was a shop, and the split cost a fresh machine the only thing it needed: Local said "Nothing cached here yet" with a button to another tab, so the answer to "what should I get" was one click away from where the answer to "what do I have" would appear. Merging them draws both in the same row, so the page fills up in place — and once it does, a tab whose entire content is a second, emptier version of that row is a strip entry that costs a reader a click to learn nothing. **Unrouted rather than deleted**: the Hub search is a real surface with real tests, and this is a judgement about the STRIP; `/ai-models/discover` falls back to the default like any stale link (D420's own forgiving posture). *That parking lot was emptied at D426* — the search became a face of this page rather than a tab of its own, and `discover/` with it — so what this row decided about the strip is now also true of the tree. **Rejected: a wrapping grid per capability**, which is what this replaced — it made every capability a block of unknown height, so the THIRD heading sat below the fold on a page whose whole job is to be swept. One row per capability keeps every heading on screen and puts the length inside the row. **Rejected: width breakpoints for the scroll arrows.** The row's width and whether it overflows are different facts, and a threshold gets both wrong at once — hiding the arrow on a narrow window holding two cards, showing it on a wide one holding twelve. `scrollWidth` vs `clientWidth`, re-measured on scroll, on resize and on every render, because a finished download replaces a card and changes the former with no resize event to hear. |
| D424 | **"Downloaded" means a materialised snapshot AND a fetch that finished, so the listing carries `partial` — read from the residue of a stopped fetch (a `.fusedpart`/`.incomplete` file in `blobs/`, or no snapshot at all) and never from the format — and a partly downloaded repo's card drops its engine tag and its Load for a "partly downloaded" tag, a Download that RESUMES, and the trash. `cached_models()` and hub search hold the same line; the frontend splits the one on-disk map in two, because "this disk HAS the model" (the ✓, a settled Download click) and "this model already has a card here" (what suppresses a recommendation) stopped being the same question** | Cancelling the first download of a recommended model left the worst card on the page: our own fetcher publishes each blob and links it into `snapshots/<commit>/` as that FILE lands, so the first small file gave the repo a revision, `revisions > 0` read as "downloaded", the recommendation with its working Download button disappeared — and what replaced it was a repo whose weights had not arrived, therefore no `loaders`, therefore `engine: null`, wearing the quiet "no engine" tag over a disabled Load. Two true sentences about the FORMAT, both lies about the DOWNLOAD, and deleting the repo by hand was the only way out (reported live on `mlx-community/whisper-tiny.en-8bit`). **Rejected: deleting what a cancelled fetch materialised.** It was the first fix attempted and it contradicts D275/AI-5i head-on — the part files and their fsynced offsets exist so that a cancel, a crash or a quit RESUMES instead of restarting, and cancel is the first of the three cases that decision names — while not even fixing the state: quitting the app mid-download leaves the identical card with no cancel involved, and cancelling a download that RESUMED a pre-existing partial (`existed == True`, where deleting must be forbidden lest a repair take the copy that worked) leaves it too. **Rejected: "no `.fused-fetch-<commit>.json` record" or "no `refs/`" as the completeness test.** Both are absences, and both are absent from repos that are perfectly complete — the record is written only by this app's fetcher (so every repo pulled by `hf`, transformers, or an older build reads as half-downloaded) and a repo pinned at a commit sha has no ref by design. **Rejected: "no engine reads it" as evidence of partiality**, which is `_engine`'s honest answer for a fully downloaded repo nobody's backend opens (a SigLIP tower, ACE-Step) and would offer to resume a download that finished months ago. Positive evidence only, therefore: a part file is left by an interrupted or in-flight fetch and by nothing else — `finish()` renames it over the blob, and the one path that abandons ours (`_clear_parts`, on the fallback to hf) deletes it first, which a cancel never takes (`except Cancelled: raise`). |
| D425 | **`catalog.py` gains a `recommended` flag — a SECOND curation axis, opt-in per entry — and the Playground sidebar becomes `recommended or downloaded or loaded` while every other picker keeps reading the whole shortlist** | One curation, two surfaces, and they want different LENGTHS of it. The AI Models page is where someone shops: its Local and Discover tabs answer "what could I have on this disk", and the shortlist's whole range — eight `mlx-text` entries from a 0.7GB LFM2.5 to a 20GB Qwen3.6 27B — is exactly the comparison that reader came for. The Playground answers a different question ("what happens if I type a sentence"), reached by someone who wants a text box, and for them five of those eight rows are a multi-gigabyte download wearing the costume of a choice. **The flag lives in the curation rather than in the tab**, because "which of these is a first thing to TRY" is an editorial judgement with the same shape as the `note` beside it — a rule computed in the component (`size_gb < 6`, or "the first three") would be a curator's opinion expressed as arithmetic over a field that means something else, and would silently re-decide itself every time a list was re-sized. **EXACTLY ONE PER LIST — one per capability AND engine, since that is what a list IS — which was the owner's call over the "handful" this shipped as first (3 of 8 `mlx-text`, 3 of 5 `llamacpp-text`, 2 of 4 and 2 of 3 whisper, 2 of 2 embeddings).** A shortlist of three is still a comparison, and the reader who came to type a sentence has no way to make it; one row is an ANSWER. It also makes the flag's own meaning checkable — "the model to try on this engine" has one correct value per list, where "a good first click" had as many as somebody felt like marking. What the marked entry has to be: strong enough that a first answer is a fair picture of what local inference does on this machine, small enough that the download is not the experience. **Where that lands is per list and is nobody's formula**: the text lists mark a middle row (the smallest entry is quick and weaker than every row above it; the largest is a 20GB download nobody should meet first), while `mlx-whisper` marks its HEAD — a 0.05GB tiny.en downloads in seconds and transcribes clear speech well enough to show what the feature does, the "download is not the experience" half of the rule winning outright. So the marked entry sometimes coincides with `default_for()` and sometimes does not, which is why the axes are described as INDEPENDENT rather than opposed and why nothing derives one from the other; the two whisper lists disagreeing (MLX marks tiny.en, CTranslate2 marks turbo) is allowed for the same reason — an engine's list is its own editorial judgement, not a copy of its neighbour's. **Explicitly NOT the `default: True` field that this file's own smallest-first rule rejected, wearing a new name.** `default_for()` is still position 0 and position 0 is still the smallest entry; the flag has no bearing on either, whether or not it happens to land on the same row — so the Playground's fallback is "the catalog default if it is drawn, else the first row that is", which is a filter reading past a row it does not offer, never the curation bent to make one rule serve two jobs. **The disk half is what makes the filter safe rather than merely shorter**: anything downloaded stays playable whether a curator marked it or not, so the tab can never hide a model somebody already spent 8GB fetching, and `loaded` sits in the predicate beside `downloaded` because the two are read from different places — a live supervisor against a memoised disk scan — and the window where they disagree is a first load, i.e. exactly when a model is answering. Vanishing mid-conversation is the one failure this list may not have. **Cached entries are `recommended: false` unconditionally** — a recommendation is a person's mark and nobody made one about a repo the user found in Hub search, the same reason their `note` is null — and it costs them nothing, since a cached entry is on the disk by definition. **Normalised to a bool in `_catalog_with_downloads` although the source writes it opt-in**, so a consumer filtering on it cannot mistake "absent" for an answer; a test pins that the literal is never written `False`, since two ways to spell "no" is how a curator comes to believe one of them means something else. **The COUNT is pinned per runner and the CHOICE deliberately is not.** Which model is marked is data a curator edits, so no test names one — an owner's correction on this axis ("reduce it to one per engine") should cost a literal and not a test file, and the earlier version of this work got that wrong twice: a positional assertion (`0 < index < len-1`) and a wire test asserting two ids by name, both of which turned a curation edit into a test edit and both of which are deleted. What survives is the mechanism: exactly one per list (none is an empty Playground group on a fresh machine, two is the comparison this was cut down to remove), the flag never written `False` in the source, the bool on both halves of the wire, and — named against no model — that the curated head is still `default` and still smallest-first. Pinned per RUNNER rather than in total, because a list is what ONE machine sees: a total would let a Windows-only list go unmarked behind a well-marked Apple one. **Rejected: special-casing a `?model=` link to an unoffered model into the sidebar for one visit.** It makes the tab's own list mean two different things on two machines to save a stale link a silent fallback (PT-9) it already had. Rejected: naming the field `playground` — the flag says what the curator thinks, and which surface reads it today is not a fact about the model. The one cost of keeping the plainer name is a vocabulary collision with the Local tab's `MergedSection.recommended` (its own name for "curated and not on this disk"), noted at both declarations. The rule and the selection both moved into `playground/pick.ts` as two pure functions, so the drawn list and the selectable list are one predicate rather than two that agree today. |

### Hub search moves onto the Local page (2026-08-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D426 | **The Hub search becomes a FACE of the Local page rather than a tab: a query box, a task filter and a sort at the top of it, an empty settled query rendering the page exactly as before, and an active one replacing every section with ONE "Search results" grid — self-naming heading, count, and a "← Back to models" control that clears query and task together. A result is the page's own card (`RecommendedCard`'s skeleton, hub variant) with the page's own download plumbing, and its disk state is read from the page's LISTING, never from the `local` field on the search reply. `discover/` is deleted and `lib/discoverView.ts` becomes `lib/hubSearchView.ts`. The task filter and the sort are the app's ONE dropdown — `@platform/ui/ContextMenu`, an icon on every row, an additive `active` flag for radio semantics — rather than two native selects, and the sort offers a fifth ordering the Hub cannot perform: Size, smallest first, ranked on the PAGE** | D423 merged the shop into the inventory and left one thing behind: **downloading a model nobody curated.** The shortlist cannot answer "the model I read about this morning", and the only surface that could was a tab nothing linked to — kept unrouted as a parking lot for code with real tests and nowhere yet to live. This is the somewhere. The search box belongs at the TOP because it is the one control that changes what the page IS, and it changes it by REPLACING: the carousels answer "what should I even get", which is the question somebody has BEFORE they type, so two grids on screen would ask the reader which of them is talking to them (D313's rule, on a bigger page). **The join is why this belongs in the app at all** (D255): huggingface.co cannot tell you that the model you are reading about is already in your cache — and the page's own walk is the only honest source for that, which is why `resultDisk` reads `diskCards` and the reply's own `local` field goes unread. That reply is frozen at the moment of the search, so a model downloaded from these very results went on offering a Download button until somebody typed again; and since D424 there are THREE verdicts to report, not two — ✓ downloaded, partly downloaded (a Download that resumes), or absent — plus "the walk has not answered", which must show neither the ✓ nor the button because both are claims. **Rejected: keeping the tab and linking to it.** It is one search box; a strip entry for it is a click to reach a control that fits above the content it filters. **Rejected: results UNDER the sections.** Two answers to two different questions stacked, with the one that was asked for below the one that was not. **Rejected: a second card component for results** — the old `HubCard` was its own copy of the head/what/foot markup, which is two places for a class name to go stale; the skeleton is written once (`ModelCard`) and composed twice, so a model looks like one kind of thing whichever grid found it. **Rejected: a login prompt on every gated card, and a standing one on every search.** A token is one fact about the MACHINE, so per-card it is four copies of one act asking to be pressed four times, and unconditional it recommends an account to somebody who never hit a wall — it appears exactly when a gated row came back and there is no token (`needsHubLogin`). **Rejected: renaming the `am-discover-*` CSS selectors** to match; the names predate the move and churning a stylesheet to rename two classes buys nothing a comment cannot. The debounce, the ✕ that clears BOTH inputs (D317), the host disclosure and the gate chrome are all lifted unchanged — the endpoints are untouched, and `tests/test_hub_models.py` passes as it stands, which is the point: this moved a surface, not a feature. **REFINED, same decision: the two controls became the app's own dropdown and the sort grew a Size.** They were `<select className="field-control">`, which is the one control on the page that renders as the PLATFORM's rather than as this app's — and, decisively for these two, a control that cannot carry an icon. The row IS a vocabulary: one task filter and five orderings are the whole of what a reader can ask of the Hub here, and a glyph per option is how that is learned at a glance rather than read twice. So the trigger is a bordered button wearing the active option's icon, its label and a caret, at `.field-control`'s own metrics so the row's height and rhythm are untouched, and the menu is `@platform/ui/ContextMenu` — with `active` added to `MenuItem` and an accent wash to match `.bar-menu-item.active`, because a menu of ALTERNATIVES has to say which one you are in and the explorer's mode dropdown already settled what that looks like. **Rejected: importing the explorer's `ModeMenu`**, which is the closest thing on screen and forbidden for good reason (`check-boundaries.mjs`: an app may not reach into another app); **rejected equally: copying it here**, which is the third hand-rolled dropdown the shared component exists to prevent. Icons are added to the shared `MenuIcons` for the same reason — `download` is REUSED for the downloads sort (an arrow into a tray is what a download count counts) while `refresh` is deliberately NOT reused for "Updated", because two circular arrows already mean "fetch this again" everywhere else and this is a fact about the repo rather than an action. One funnel for every task row rather than a glyph per pipeline tag: the tags are the server's (HS-7), so a per-task mapping would leave the next registered runner iconless or, worse, wearing an arbitrary glyph that reads as a claim about what it does. **Size is a PAGE-level sort, and that is forced rather than chosen:** the Hub's list endpoint cannot rank by size and cannot even report it — it refuses `expand[]=usedStorage` with a 400, so a size is one request per repo — and the server's `_SORTS` is an allowlist that would reject the word. So "size" must never reach the wire, and the guarantee is a TYPE: `ResultSort = HubSort | "size"` with a pure `wireSort` mapper, `searchHubModels` still taking a `HubSort`. It asks for the most-downloaded candidates (the set anybody would have got by default) and reorders them once — **ascending, smallest first**, because on a page where every card has a Download button the useful reading is "what fits". **The key is the figure the CARD SHOWS** (`hubSizeBytes`, the same precedence as `hubSizeLabel`: the weights estimate that rode in on the search reply, else the Hub's repo total): that number is the only evidence a reader has that the sort worked, so ranking by anything else produces a column of sizes that does not ascend — correct, invisible, and indistinguishable from a broken control. It mixes two measurements in one ordering, which is a cost the cell already pays (it holds either figure, says which on hover, hedges both with `≈`), and it buys the traffic back: only the repos with no estimate have to be asked about, which is exactly the set a card would have asked about on its own. **Rejected: ranking by `usedStorage` for every row** — one request per result to put the grid in an order the page cannot show its work for. **Rejected: reordering progressively as each size lands** — answers arriving in the Hub's order would walk cards out from under the reader's cursor; the grid keeps the server's order, dimmed exactly the way a refetch dims it, with "measuring sizes…" in the heading, and the order changes once. |
| D427 | **Canvases sits behind a preference now — `canvases_enabled` in `~/.fused-render/prefs.json`, default OFF, with a Canvases section on the Preferences page's Render tab as its only switch — and the flag gates the two ENTRY POINTS (the sidebar's primary row + rail icon, and the Settings menu entry), never the `/canvases` routes** | Canvases is the Workbench bridge: a Fused account, a listing of the canvases on it, and a per-canvas workspace with the live workbench embedded (D338/D339/D355). It is one of the two or three things a machine that uses it is FOR, and **nothing at all on a machine that does not** — and until now the shell had no way to say the second thing. D358's sign-in gate is a different question wearing the same shape: it answers "is there an account behind this row", which on a signed-in machine is always yes, so a user holding Fused credentials for some other reason got a permanent nav row and a permanent menu entry for a feature they never open. **The gates are therefore a conjunction, not a replacement, and the two entry points take different halves of it.** The primary row and the rail icon are `canvasesEnabled && canvasesLoggedIn` — both halves or neither, since a row that exists until you collapse the sidebar is a destination people lose (D358's rule, kept). The **menu entry is gated on the FEATURE ALONE**: it has always been deliberately ungated on sign-in because the page explains a CLI-missing / signed-out state itself, and that entry is exactly the right weight for "there is a thing here you could set up" — gating it on the account would delete the only affordance for setting Canvases up, while gating it on the pref is the whole point of having one. `prefsActive`'s double-selection exclusion follows the row's condition (`canvasesInNav`), and with the feature off the entry is not in `menuEntries` to be matched at all, so a deep link to `/canvases` lights nothing anywhere. **NOT A ROUTE GUARD, and that is the load-bearing half.** `/canvases` and `/canvases/<name>` keep answering with the flag off, exactly as they always have signed out. What a reader turning this off asked for is a sidebar and a settings menu without a feature they do not use; a bookmark that dead-ends, a canvas link pasted from a terminal that goes nowhere, and a workspace someone still has open going blank are not that — and every one of them would have been the cost of one `if` in `App.tsx`. The page is already the thing that knows how to explain its own preconditions, which is the same reason the signed-out state was never a route guard either. **Default off means a currently signed-in user loses the row until they flip it** — the owner's explicit call, and the correct polarity for an opt-in: the alternative reads "on for everyone who ever ran `fused login`", which is the state this preference exists to end. Any non-`true` stored value reads as off (`reader_enabled`'s rule), so a hand-edited `"yes"` cannot turn a feature on. **The shell reads the flag through `@apps/canvases/feature-flag` — one fetch plus a publish, not a poll, and not the app barrel.** Not the barrel for `logged-in.ts`'s reason: `index.ts` re-exports `Canvases`/`CanvasWorkspace`, so reading one boolean through it would pull the whole sub-app — workspace, lock lib, embed host — into the shell's main bundle. Not a poll, which is where it DIVERGES from `logged-in.ts`: a login happens elsewhere (another window, a `fused login` in a terminal) and so has to be watched for, whereas this pref can only change on the Preferences page of this same window — so that page hands the new value over (`publishCanvasesEnabled`) and the row appears or disappears with the checkbox instead of on the next navigation. An unanswered read renders as OFF, because assuming on would flash an entry the reader turned off on every load. And it is a SECOND store rather than a field on the first: "is this offered" and "is there an account" are different facts with different lifetimes, and only one of the three call sites wants both. **Rejected: a route guard** — it strands bookmarks, links and open workspaces to hide a nav row. **Rejected: folding the flag into `logged-in.ts`** — one store answering two questions would have to poll for the union of what either needs, and the menu entry would silently inherit the sign-in gate it has always been exempt from. **Rejected: a Canvases tab on the Preferences page** — a tab for one checkbox is a tab a reader opens once, and the Render tab already answers "which of this app's features do I want" twice (Reader, call recording). **Rejected: default on with an opt-out** — every machine that has ever signed in keeps exactly the row it has today, which changes nothing for the reader who asked for this. |

### The Playground sidebar becomes cards in collapsible capability sections (2026-08-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D428 | **The Playground sidebar's model rows become spacious CARDS carrying exactly four things — nickname, full name, size, and a Download CTA — grouped under one collapsible `<details open>` section per capability whose header wears a per-capability icon (chat bubble / picture / mic / magnifier, `playground/capabilityIcons.tsx`)** | Reverses two earlier calls by design request. The old row ("a row, not a card: LM Studio style") packed the curator's note, a fit verdict, a state dot and a status word into a 264px rail; the redesign trades that density for legibility: the note and the fit sentence still exist where the decision is actually made (the stage header and its state line), so the card drops them without losing them. **The second line never repeats the first**: `nickname` falls back to `label` for cached entries, so the full-name line renders only when it differs, else the repo id, else nothing. **The card is a div-as-button, not a `<button>`** — the Download CTA lives inside it and a button inside a button is markup browsers are free to mangle; keyboard selection is Enter/Space on the card. The CTA reuses the stage's own `downloadAiModel` + `refreshAiRuntime` path (and its ErrorBanner — a card has no room for a server sentence), disables to "Downloading…" off `runtime.downloading`, and a downloaded model shows a quiet "Downloaded" instead — the existing catalog shrink-detection refetches when the pull lands, no new plumbing. Clicking Download also SELECTS the model, deliberately: the stage is where the progress bar lives. **Sections are native `<details open>`** — free collapse state, correct semantics, no React state to persist — with the default marker replaced by a right-hand caret so the capability icon owns the left edge. The unavailable-group reason (HF-8), the empty-shortlist sentence and the "Your downloads" caption all survive inside the details body — collapse must not become the second way a ruled-out capability goes invisible, which is why the reason line lives BELOW the summary: a collapsed group still opens to its explanation. Icons are the tab's own file, not `groups.ts`, because Home imports that module eagerly and JSX would drag markup into the front-door bundle; not `MenuIcons`, because these four glyphs name TASKS, not actions, and belong to the one surface that speaks task words. Sidebar widens 264→300px to pay for the card padding. **Rejected: nesting the CTA in the row-button** (invalid HTML). **Rejected: hiding undownloaded state behind hover** — the CTA is the one action the card exists to offer. **Rejected: React-state collapse** with persistence; nobody asked for remembered collapse, and `<details>` is deletable the day someone does. |
| D429 | **Every Playground stage becomes ONE shape — a centered column on the hero card's own gutter (`min(680px, 100% - 64px)`) reading as an API surface: input, Run, the result of THAT run, with the common parameters inline under the composer and everything else behind a closed-by-default Advanced `<details>`. The text stage drops the chat (ChatStage → TextStage: one prompt, one reply, no history sent), the image stage drops the gallery strip (one image, the current run's), and the right settings rail + its `Controls` toggle are deleted from all four** | The four stages had four layouts (chat column + rail, image column + rail, transcribe column + rail, embed column alone), and the tab's actual reader is someone testing what one `fused.ai` call returns — a transcript and a thumbnail strip are app furniture answering a question nobody here asked, while the rail put temperature behind a sidebar on wide screens and a toggle on narrow ones. One column, same gutter as the hero, means the header and its stage read as one card stack on every capability. **The prompt now rides the URL** (`prompt`, the image stage's precedent): a one-off prompt is SETUP, not transcript, so the chat rule ("the URL carries the setup, never the transcript") reclassifies it rather than being broken by it. History is sent empty; `splitThink`, per-reply stats, Copy, Stop, the 409 load dance and cancel-on-unmount all survive verbatim — this moved a surface, not the wire. Common/advanced split per stage: text = temperature + max tokens up front (system prompt, top-p behind); image = aspect + speed chips (sizes, steps, guidance, seed behind — the seed-reuse caption button stays, it was the strip's only function that mattered); transcribe = task (language, VAD, word timestamps behind); embed has no knobs and just takes the column. `.pg-stage` becomes the scroller — a one-shot column has no inner log, so the hero scrolls with content. **Rejected: keeping chat behind a toggle** — two modes is two surfaces to keep honest for a tab whose exit ramp ("Build an app") already leads to anything stateful. **Rejected: a shared StageShell component** — the shape is three class names; a wrapper would abstract less code than it adds. **Rejected: keeping the rail on wide screens only** — the point is one shape everywhere, and a layout that changes at 1100px is two shapes wearing one name. |
| D430 | **The text stage's surface tightens to prompt-and-Run only: a "Try {model}" heading + load-state line stand ABOVE the composer (moved up from the empty state), ALL sampling knobs — temperature and max tokens included — move behind the fold, and the fold is renamed Advanced → "Config" and drawn as a card (bordered `<details>`, summary as header row with the sidebar's caret idiom). Run wears an inline `⏎` kbd hint; the reply card's Copy becomes an icon button pinned to its top-right (`MenuIcons.copy`, swaps to a check for 1.2s), leaving the foot to the token stats alone** | Amends D429's "common params inline" for text: with two sliders inline the surface read as a settings form with a prompt attached (see the screenshot that prompted this — sliders visually outweighed the composer). The heading survives the first run on purpose — an invitation that lives in the empty state dies the moment it works. Enter-to-send already existed; the kbd chip surfaces it instead of leaving it a tooltip secret. Copy switches from `button.textContent` swapping (breaks with an SVG child) to a `copied` state. The rename is global — `AdvancedPanel` in controls.tsx owns the summary text — so image/transcribe folds also now say "Config", but their common chips (aspect/speed, task) stay inline: chips are choices, sliders are tuning, and only tuning got demoted. **Rejected: moving image/transcribe commons behind the fold too** — not shown, not asked; one chip row does not outweigh a composer. **Rejected: a gear icon on the summary** — the caret already carries the affordance. **SUPERSEDED on that last clause:** the summary fold is gone. Config now lives behind a settings cog right-aligned on each playground stage's title row (`StageHeader` + a controlled `ConfigPanel`, controls.tsx), which freed the slot under the input for the example row — the caret carried the affordance but sat in the one place the examples needed. |
| D431 | **The image stage takes the text stage's shape whole: a one-line "Describe a picture" heading above the composer, the aspect and speed chip rows moved DOWN behind the Config fold (leading it, above the size/steps/guidance/seed stack), a Clear button beside Generate once a render is done, `⏎` on Generate, a growing two-row prompt box, and the finished picture wrapped in the shared label-plus-card block ("Result") with a Save icon pinned to its top-right corner. The empty state loses its "Make a picture with {model}" title and sub-line and is starters only** | Applies D430 by request and **reverses its "chips stay inline" clause**: that exemption was reasoning about one chip row, and the image stage has two — 10 chips, sitting between the composer and the fold, reading as required setup before a prompt that needs none of it. Shape and speed are still choices worth making, so they lead the fold rather than joining the sliders. The heading names the ACTION only: the hero card directly above already carries the model's name and load state, and the empty state was saying the name a third time — the "Quick setting" fact it also carried survives verbatim in the Steps slider's hint. Save is a plain `<a download>` on the same `rawUrl` the `<img>` already renders (the file is on disk; the server serves it), NOT clipboard-write: `ClipboardItem` takes `image/png` only and this stage does not know the render's format. It reuses `.pg-copy-btn` geometry but overrides the colours with a fixed dark scrim and white glyph — the button sits on arbitrary photo pixels, where a themed `--fg-muted` disappears against half of them. `.pg-answer-block`/`.pg-answer-label` move from the text section to shared furniture; `.pg-params` survives for TranscribeStage's Task select, which stays inline because recording and translating are two errands, not one tuned run. **Rejected: copy-image-to-clipboard**, and **rejected: both buttons** — one corner, one action. **Rejected: keeping aspect inline as "the creative choice"** — a 512² square is a fine first look at any prompt, and the chips are one click away in a fold the first render never has to open. The transcribe and embed stages then followed the same shape, so `.pg-params` (D429's inline-params row) has no users left and is deleted: Task and the embed corpus are in the fold with everything else. |

### Base-image editing in fused.ai.image, mflux-only (2026-08-21)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D432 | **Open the closed envelope (D413) for one new option, `image` — the path to a base picture to edit — accepted only when the resolved image engine is mflux; both diffusers doors refuse it. No `strength` option ships, one image only (a single string, never an array), the reply's width/height/steps/guidance default from the base image rather than from the ordinary text-to-image defaults, and no claim of seed-reproducibility appears anywhere** | D413's own closing line put image-to-image out of its scope — it closed the envelope around the surface that existed, it did not grow it. Growing it needed a library contract nobody here had run on hardware, so a first pass (the flux2-edit handoff, revision 1) named three gates and stopped short of code. **Revision 2 ran all three on an Apple M5 against mflux 0.19.0, and two came back differently than guessed and one invalidated its own question.** **Gate A — does the plain `Flux2Klein` variant take an image?** Yes, `image_path` (singular), which looked like a free win and is a dead route on three counts: it is INERT unless `image_strength` is also passed (against a deliberately unmistakable base — a magenta field, a black circle — `image_path` alone returned a clean text-to-image render with zero trace of the base, the exact D413 failure reproduced *inside the library*); even with `image_strength` set it is img2img noise strength with no useful middle ground (0.6 ignored the instruction, 0.4 destroyed the base); and `Flux2KleinEdit.__mro__` has no `Flux2Klein` in it — two independent classes over one snapshot, not an override. So `image_path`/`image_strength` are NEVER passed from this app's worker. **Gate B — does the edit class render with no reference image?** No: omitting `image_paths` crashes inside mflux's own denoiser (`mx.concatenate([latents, image_latents], …)` with `image_latents=None`), not a guarded error. So one resident model cannot serve both modes, and residency is keyed by `(model_id, mode)` inside the mflux worker: `load()` builds the plain variant as before (an untouched path — zero behaviour change for a caller who never passes `image`), and a request whose mode differs from the resident one triggers a lazy, in-process rebuild reusing the already-downloaded snapshot — no second worker to route to, no re-download. A warm swap measured ~0.6s on the M5 that ran the gate, which is NOT written into any doc as a guarantee — a cold load and a 16GB Mac are different machines, and the PR description is where a number like that belongs, not a comment or a test. **Gate C — is a generate through the edit class byte-identical to the plain one?** Unanswerable: two bare renders with an IDENTICAL seed, prompt, step count and guidance produced different PNG bytes — mflux is not byte-reproducible at a fixed seed on the version this app ships, a fact about the engine and not a regression this feature causes or fixes. The first pass at this gate used a byte-diff and read "bytes differ" as evidence the base image reached the denoiser; it had not, and only inspecting the pixels caught it — the resulting method note (verify an image option VISUALLY, never by hash) applies to every image option after this one, and **no test in this codebase may compare rendered bytes or hashes**. **`strength` is deferred for a stronger reason than "untested": the edit mechanism does not use it at all**, so withholding it costs nothing, and it is the corollary of Gate A's kill of the plain-variant route — implemented through `Flux2Klein`, `strength` would become mandatory or `image` a silent no-op, neither of which is what an "edit" option should ship as. **One image, a single string**: mflux's own argument is a list (`image_paths`), which revision 1 read as license to accept `string \| string[]` here — a type signature mistaken for a feature. A two-reference probe at 4 steps was inconclusive (the second path came back almost verbatim rather than as a blended edit), so multi-reference conditioning is UNVERIFIED and deliberately withheld: an array or non-string `image` is a 400, and the worker still passes `image_paths=[one]` because that is the library's own shape. **The envelope, both doors, one rule**: `runtime.js`'s `imageKeys` and `ai_runtime._IMAGE_OPTIONS` both gain `image`; `_IMAGE_SERVER_OPTIONS` adds the bridge-injected `base`, the identical asymmetry `_TRANSCRIBE_SERVER_OPTIONS` already carries, and the drift-guard test that compares the two languages' whitelists is what stays red until both move together. **Diffusers refuses, rather than answering best-effort or guessing**: the diffusers image pipeline's own editing SIGNATURE is known — `Flux2KleinPipeline.__call__`, the same class `torch_image.py` already loads, takes `image` as its first parameter, `list[PIL.Image.Image] | PIL.Image.Image | None = None`, with `image is None` the plain text-to-image path, read off the installed 0.39.0 source — but whether it RENDERS a correct edit is not, on any machine this app has run on, and a wrong guess there would be a broken engine that passes every test. `engine_options.UNSUPPORTED` — empty since D406 — gets its first real rows: `diffusers-image`, `diffusers-image-cuda` and `diffusers-image-rocm`, each refusing `image` with the IDENTICAL sentence, because the fact is about the pipeline class, not about which wheel is loaded. Refused twice, per that module's own rule: at the endpoint (`registry.for_capability` already knows which runner will actually serve the request, so the answer costs nothing before a job opens) and again on arrival inside `torch_image.py`, which imports the module for the first time since D406 withdrew the one engine that used to. The worker-side call cannot name its own precise runner code — the three diffusers hardware variants are five-line shells around one shared `torch_image.py`, and nothing in `worker_base`'s argv says which wheel is running — so it uses `"diffusers-image"` as a stand-in, safe only because every entry in this family reads the identical sentence. **The reply describes the render that will actually happen (AI-9), extended rather than exempted**: an edit's default width/height come from the BASE IMAGE — fit the longest side to 1024 without upscaling, snap down to a multiple of 16, floor 256, aspect preserved, the prototype's own arithmetic, confirmed as written by the gate run — read through a ~90-line stdlib PNG/JPEG/WebP header parser in `ai_runtime.py` (all three WebP sub-formats — `VP8X`, `VP8L`, `VP8 ` — not only the extended container form, since `cwebp`, Pillow and a browser's own "Save as WebP" all emit the plain ones) rather than a new Pillow dependency, since the app process has none and the route answers before any worker does. An edit's default `steps`/`guidance` are the prototype's own 4 and 1.0, not the 28/4.0 shared between both engines' plain generate path (`mflux_image/worker.py`'s own comment on why those two numbers are shared) — silently inheriting the generate defaults would be a real quality regression, and changing them for this one mode is documented here rather than landing unnoticed. **Out of scope, deliberately**: the diffusers edit/img2img path (needs `inspect.signature` on a machine with that venv — a separate PR, and Gate A's finding means checking whether whatever it offers is editing or img2img before calling the option `image` there too); multi-reference conditioning (withheld above, revivable with a proper hardware run); and export, which already fails on `fused.ai.image` before this option existed and is not made worse by it. |

### Tasks: run now by default, plan on the calendar (2026-08-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D434 | **A task is a THING TO RUN unless somebody said when. The New task card folds its when-row into More options and opens it only when the card is being used to plan (the calendar, a slot click, an edit); a one-off created with that row untouched rides the wire as `immediate`, and `taskChips` draws nothing for an immediate message — so the calendar stays a plan instead of becoming a log of everything ever typed. The primary button says what the press does: Schedule for a time still ahead (and for every repeat), Run for now or already past.** | The when-row was the field everybody scrolled past. It defaults to *now*, so the commonest thing a person did on the List was accept a time they never read, press Save, and then find their task drawn on the calendar at 3:47pm on a Tuesday beside the things they had actually planned. Two facts were tangled: `due` says WHEN a message runs, and nothing said whether anybody CHOSE it. **The flag is stored, not derived** — a due date reads "now" for both kinds, so once the minute passes the distinction cannot be recovered; `immediate` is set at creation (`schedule.create`), carried onto the message the calendar reads (`_scheduled_message`) and false for every entry written before it existed, which is the right reading since all of them came from a form that asked. **Per MESSAGE, not per task**: "do this now" and "and again on Thursday" are one task with two different messages and only the second is a plan. **Rejected: hiding immediate tasks from the calendar by comparing `due` to `created`** — a task genuinely scheduled for two minutes out is indistinguishable from one that was not scheduled at all, and the rule would have silently swallowed real plans. **Rejected: not creating a schedule entry at all and sending the message directly** — the entry is what carries the title, the permission mode, the target and the task's number, and a second creation path would have had to re-earn all four. **Rejected: `planning` deciding what gets stored** — which VIEW you came from picks where the row is drawn, `timePicked` records what you actually did, and conflating them meant opening More options on the calendar and closing it again changed the data. Never true alongside a rule: an anchor is a chosen time by construction, and the model refuses the pairing rather than trusting the form. |
| D435 | **The list row's right-hand end is folder · count · time, and the folder is a TAG: pressing it filters the page to that project.** | The row could say where the work happened and when it last moved, and could not say how much of it there was — a task with one prompt and a task with forty read identically until you opened them, which is exactly the wrong thing to have to do to choose which to open. `message_count` was already on the model and already on the wire; it only needed a slot. It goes BETWEEN the folder and the time because the time is what changes and the last thing before the edge is the one a reader lands on (the same argument that put the folder first in the first place), and it says the NOUN — "4 messages", not a speech bubble and a 4. It shipped as the glyph on the argument that the row's busiest end could not afford three more characters; it could, and a bare number between a folder and a time is a number with no unit, one the reader has to decode the icon to interpret. The count is the only thing on this row whose number could be mistaken for another kind of number (a time, an id, a count of runs), so it is the only one that needed a word. Drawn only above zero: a task whose thread has not started has nothing to say, and a column of "0" is worse than the space it fills. The chip becoming pressable is the other half — the folder was already the answer to "where did this happen", so making it a tag turns it into the way to ask "what else happened there", which is the reader's very next question. It writes the SAME `projects` filter the toolbar's popover owns (one filter, two ways in, one place to clear it) and REPLACES the selection rather than widening it, because a press that quietly added to a set would be the opposite of what it looks like. **Only on the List** — a board card is a drag target first, and a button inside one competes with the gesture that moves it. No chrome at rest, a wash on hover and focus, and negative margins matched by padding so the box costs no layout: a permanent button on every row would be a column of buttons down the edge this row has twice been trimmed for. |

### The model card says what it is, whether you have it, and how much of it (2026-08-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D436 | **The card's own SURFACE carries the disk states it had no drawing for: a complete repo gets the green wash — the SAME wash a loaded card wears, which differs only by its tinted border and 3px rail — plus the search results' own "✓ downloaded" chip in the footer, and a partly downloaded one gets a warning wash drawn as a hard-stop gradient at the FRACTION fetched (`aiModelGroups.partialFraction` — the live job's `done/total`, else bytes-on-disk over the curation's `size_gb`, clamped 2–95%, and null → a flat wash). The engine tag leaves the accent for one hue per engine FAMILY out of the calendar's categorical palette (`engines.engineHue`, `--task-c0…c7`)** | Three of this page's four states are facts about the disk and the background stated exactly one of them (loaded), so "which of these do I already have" — the question the grid is read for — was answerable only by reading each card's buttons. Both new washes are BACKGROUND ONLY: no border, no rail, no radius, no padding, because these cards sit in a horizontal carousel and a finished download that changed its card's box would shift every card to the right of it mid-scroll. **Rejected: two strengths of the same green for downloaded and loaded** — tried at 6% against 9%, and side by side the pair read as two shades of one thing rather than as two things, which asked a reader comparing cards to judge a 3% tint. One wash for "this model is on this machine", and the distinction moves to the two channels that are categorical rather than a matter of degree: the tinted border and the rail, which only a resident model wears. **Rejected: a second "Downloaded" pill of its own** — the Hub results have worn a "✓ downloaded" chip for the same fact since D313's successor, so the footer wears that one; two drawings of one fact is two vocabularies. **Rejected: 100% for a partial with no denominator** — a gradient stopped at an invented number tells the reader "2% of this is here" when the honest sentence is "some of it is, and we cannot say how much", which is what the flat wash draws. **Rejected: clamping to 0** — the 40-byte shell is 0.002% of a 4GB model, and a state drawn as nothing is not drawn. **Rejected: hashing the engine label to a hue** — stable and agreement-free, but the one clash it cannot avoid is with the accent, which is now spoken for on the same card, so the eight assignments are named and the lime one is spent deliberately. **Rejected: a ninth hand-picked palette** — `--task-c0…c7` is already this app's answer to "same thing, same colour", hand-picked, evenly spread and deepened for light mode; a second eight would be a second answer. Colour is never the only channel: the tag still says the engine's name, and the UNAVAILABLE tag keeps its dashed border and warning hue, because there the state outranks the identity. |
| D437 | **A "partly downloaded" repo that cannot be resumed offers a labelled Delete as its PRIMARY control, and a weights-only fetch removes its own cache folder on the way out when that folder holds no snapshot and no blob of any kind (`hub_cache.discard_empty_shell`)** | From the field: a cancelled download left a folder holding one 40-byte `refs/main` and nothing else. Every word the page said about it was true — partial (no snapshot IS the evidence, D424), 40 B (that is the folder), Unrecognised (no files, so no task, so no capability) — and the card still had no working control on it: the primary button was a DISABLED Download whose hover said to delete it, and the trash was an unlabelled glyph third in a row of four. The user went to Finder and deleted `~/.cache/huggingface/hub/models--…` by hand. So the card offers the act that is possible instead of the one that is not (same target, same confirm dialog — a labelled door, not a second delete), and the fetch thread stops the folder being drawn at all. **Rejected: deleting on cancel generally** — that is the call D275/AI-5i exists to refuse: part files plus fsynced segment offsets are there precisely so a cancel RESUMES, and this sweep is guarded on there being no part file and no blob, so it can never take bytes a resume would have used. **Rejected: keying the sweep on the job's outcome** — it reads the folder, so calling it after a SUCCESSFUL fetch is a no-op rather than a hazard, and a crash-during-fetch is tidied by the next attempt's own sweep. **Rejected: hiding empty shells from the listing** — the page's job includes showing what is on the disk, and a folder nobody can see is a folder nobody can delete. |
| D438 | **The tab reads "Models", not "Local"** | Accurate about the tab's first version and no longer its point: with the curation's recommendations in every capability row (D423) and a Hub search box above them (D426), most of what the tab offers is not local. "Local" also asked a reader to hold a distinction they do not have yet — local as against what? — where the page's actual subject is the noun. **The PATH stays `/ai-models/local`**: it is in bookmarks and in every `tabHref` cross-link, and a slug rename buys nothing a reader can see. |
| D440 | **The listing carries `fetchedBytes` — the bytes of a repo that actually ARRIVED, which is `size` for anything finished and much less than it mid-fetch — and every FRACTION on the page is drawn from that rather than from `size`. A partly downloaded card also gets the download manager's ✕ while its pull is running** | Our fetcher preallocates a part file to the full length of the file it is fetching, so a repo 15% into a 1.6GB download measures 1.6GB on disk: the card drew a nearly-full boundary over a fetch that had barely started, and disagreed with the job row printed on the same card ("243 MB / 1.5 GB"). `_scan_repo` is not wrong — the file really is that long, and telling you what is eating the disk is this page's other job — so `size` stays the number the page PRINTS and a second number answers "how much arrived". Read as the scan total with one correction per part file (subtract the length counted, add back the durable bytes), which is what keeps a finished repo's two numbers identical instead of re-adding the blobs and drifting from the walk. The durable figure comes from the part file's own sidecar, whose cursors `flush()` records only AFTER fsyncing the data — the same accounting a resume trusts. **No sidecar counts zero, never the file's length**: positive evidence only, the posture D437's shell reading takes, because an unaccompanied part file may be pure preallocation. hf's `.incomplete` is deliberately NOT corrected — that writer appends, so its length already is its progress. The sidecar's own bytes are subtracted too: a fraction made of bookkeeping ("9 bytes of this 1.6GB model are here") is not a fraction. **And the ✕**: while a pull ran, the primary button read "Downloading…" disabled and the trash was disabled too (deleting files a fetch holds open is what that guard is for), so a 40GB download started by mistake could not be stopped from the card that started it — only from the download manager, or by waiting. The recommended and search cards have carried this button all along; it moved to `shared/` and the repo card wears the same one, under the manager's own rule about when a job may be cancelled. |
| D439 | **A download IN FLIGHT fills its card with a GREEN boundary at the job's own progress — on all three cards that can be downloading — and the partial fraction is the MAX of the two readings available, so pressing Download can never move the boundary backwards** | Two faults in D432 as shipped. First, a model with nothing on this disk yet — a recommendation, a search result — had no wash at all while it downloaded, so the only account of a multi-GB fetch was a 3px bar inside a card the reader is watching from across a carousel; the boundary is the drawing that scales, and green is the colour of where the download is GOING. That also gives the amber a job it could not do alone: amber is a boundary that has stopped moving, green is one that has not, which is a distinction the `partly downloaded` tag cannot draw because a partial wears that tag while it is being resumed too. Second, the reported bug: a repo showing ~94% collapsed to a fraction of that the moment Download was pressed. The two readings are not commensurable — the disk knows every byte present including an earlier attempt's, the job counts what THIS run has moved — so the fill was swapping between two different questions. Both are lower bounds on how much of the model is here, so the answer is the larger of them, which makes the boundary monotonic by construction rather than by remembering what was drawn last. The job's own `total` also replaces the curated `size_gb` as the denominator wherever there is one: `size_gb` covers every repo a multi-repo model touches, which makes one repo's share read low. **Gated on `unit === "bytes"`** — a step-counted stage has a perfectly good total that is not a number of bytes, and dividing disk bytes by it drew a full card from a four-step job (caught by an existing test). |

### Benchmarking a local model on purpose (2026-08-22)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D443 | **A benchmark is an ON-DEMAND run of a FIXED workload per capability, not a passive reading of real traffic — one frozen `WORKLOADS` entry per capability constant, carried on every run record with an integer `revision` whose bump breaks comparability on purpose** | The number people actually want is "is this model faster than that one, on this laptop" and `server/ai_metrics.py` cannot answer it, however good it is at what it does: it records the generations that happened to pass through, so its tokens/second is an average over different prompts, different lengths and different sampling settings, and two models' rows differ by the work as much as by the model. A controlled workload is the only thing that makes two models on one machine, or one model across two app versions, legitimately comparable. **Rejected: recording every real generation and comparing those** — free, already half-built (AI-12 counts per model), and it produces rows that cannot be compared, which is worse than no rows because they look comparable. **Rejected: letting the caller choose the prompt / token count / image size.** A configurable benchmark is a benchmark whose two runs measured different things, and the first person to compare two models would have configured them differently. The accepted cost is stated rather than hidden: a number exists only where somebody pressed the button, so a model nobody benchmarked has no row. **`revision` is the seam that makes the freeze enforceable rather than aspirational**: a workload WILL be changed one day, and the alternative to storing the version was either never changing it or silently reporting the change as a regression in the model — so the UI refuses to draw a delta across a bump instead. **Rejected: a shared step count for image generation.** `catalog.py`'s per-model `defaults: {"steps": 4}` exists precisely because FLUX.2 klein is step-distilled; a shared count is either unfair to it or an out-of-memory on the others. So the workload fixes the prompt, the canvas and the guidance, each model contributes its own catalog step count, and comparability comes back through the primary metric being SECONDS PER STEP with the count recorded on the run. **Rejected: shipping a WAV fixture for speech to text.** Realtime factor is decode throughput and does not need intelligible speech, so a tone synthesized per run with the stdlib `wave` module does the job with no binary in the repo — accepting, and writing down, that a model with speech-dependent early-exit behaviour could look faster on a tone than on real speech. |
| D444 | **Runs are PERSISTED to `<home>/ai_benchmarks.json` — the exact opposite of `ai_metrics.py`'s deliberate in-memory design — bounded by a hard cap of 500 whole records with the OLDEST dropped on append, and a failed run is stored like any other** | `ai_metrics.py` states three deliberate non-features, and "nothing survives a restart" is the first of them: it is a counter of what this process did, it must never become a usage ledger, and forgetting it is what keeps it from becoming a file somebody has to prune. Every word of that is right about a counter and wrong about a benchmark. A benchmark is minutes of compute the user spent ON PURPOSE, and the entire ask is historical comparison — across models, and across app versions, which is why `appVersion` is on every record — so a figure that vanishes on restart has answered nothing. **Rejected: reusing ai_metrics' fixed bucket ring**, which is bounded by construction with no pruning pass to get wrong, and cannot hold this data: runs are individually meaningful and two of them cannot be merged into a bucket the way token counts can. So the bound is a count of whole records, and it drops the oldest — dropping the newest would discard the run whose button was just pressed, which is the one the user is watching. **Rejected: 404-ing or raising on a corrupt store.** It reads as no runs, like every other registry here; a hand-edited file must not be able to take the AI Models page down, and this is a disposable measurement log rather than the bookmarks. **A failed run is a RESULT and is appended too** — "this model OOMs on this laptop" is exactly what somebody benchmarks to find out, so it comes back `ok: false` with the message on a 200 (the status describes the request; `ok` describes the model) with empty `metrics` rather than a dict of nulls. **Rejected: one run per model, overwritten.** It is a third of the feature: the delta against the previous run, and the visible seam when an app version changed something, both need the pair. |
| D445 | **One PRIMARY metric per capability, declared in one place on each side, with the null-over-estimate rule absolute: a metric no runner reported is `null`, never zero and never derived — and image generation's primary metric runs the other way, so "better" is a flag rather than a sign** | Every capability reports several numbers and a chart can only plot one, so which one is the comparison. Chosen per capability rather than as "the first non-null metric", which would silently change what a chart means the day a runner started reporting one more field. Text generation splits into `tokensPerSecond` **and** `ttftMs` for the same reason in miniature: a single tokens-over-total-seconds figure makes a fast decoder with a slow prefill indistinguishable from the reverse, and those two models feel completely different to use — so the prefill is charged to TTFT and excluded from the decode rate. **`secondsPerStep` is the image metric and SMALLER is faster**, which is why the direction is a declared `higherIsBetter` flag consulted for the delta's colour instead of a "green for plus" rule: the naive version would state the opposite of the truth on one section in four. **Rejected: deriving a token count from the generated text** where a worker does not report one. It is the tempting fix — the text is right there — and it would be a different tokenizer's answer wearing this model's label. **Rejected: zero for anything unmeasured.** A zero is a believable measurement of nothing (a model that generated no tokens, a load that took no time, a machine with no RAM); `null` says "this runner does not report it", which is the truth. The rule is what forces `loadSeconds` to be null for an already-resident model, `totalMemoryBytes` to be null on Windows where the stdlib will not say, and every renderer to check `=== null` rather than falsiness — `value \|\| "—"` turns a real 0 into a dash and `value ?? 0` turns an absence into a measurement, and both look fine until the day a number matters. Memory is likewise **whatever `resident_bytes()` reports, sampled once after the run**, since that figure already reconciles RSS against a runner's own allocator and is GPU-pool aware on Apple Silicon; **rejected: continuous sampling for a true peak**, which is new cross-platform machinery plus a thread reaching into a worker mid-generation, for a second-order number — the cost, a missed transient spike, is stated in AI-14e instead. |
| D446 | **A benchmark that had to cold-load a model UNLOADS it when the run ends — success, a failed workload, a timeout, or an exception mid-measurement alike — and never unloads a model that was already resident** | A benchmark is a measurement taken on purpose, not a claim on the model's residency afterwards; leaving a cold-loaded model parked is precisely the gigabytes-for-nothing outcome AI-13's idle reaper exists to clean up EVENTUALLY, and a reader who benchmarks a 20GB model to see if it fits should not have to wait for a timer or press Unload themselves to find out the answer freed nothing. The signal already existed rather than needing new state: `_load_to_ready` (benchmark.py) returns `None` when `ready_worker` found the model already resident and a float when it started the worker itself, and that is exactly the fact "is this ours to tear down" needs — a model somebody else was already using (a Playground chat, another tab) is never ours to unload, and the capability may not even be ours any more by the time the run ends. **`try`/`finally`, wrapped around the measure-and-read phase alone, not the whole function** — the load itself can raise (a failed bring-up, a cancel, a timeout) with nothing ever having come up, and none of those paths reach the `finally` at all, which is what keeps a cancelled or failed LOAD from attempting to unload a worker that was never this run's to begin with. The unload happens AFTER memory and device are read off the live worker (`_memory_and_device`), which needs it still resident to answer at all — unloading first would record both as null. `supervisor.unload`'s own exception is swallowed and logged rather than left to propagate: a teardown that fails must not steal the traceback from a measurement that failed for a real reason, and `supervisor.py` stays untouched (byte-identical to main) because the teardown is this feature's own concern, not a change to how unloading itself works. **Rejected: unloading unconditionally.** A warm model belongs to whoever already had it resident, and evicting it out from under a live chat because someone else benchmarked the same model is a far worse cost than the memory a warm run never claimed for itself. **Rejected: a new `wasCold` flag threaded through the record.** `loadSeconds is not None` already means exactly that, and a second piece of state carrying the same fact is a second place for it to drift out of sync with the first. |
| D447 | **REVERTED 2026-08-23, pending the asset (git revert, not a rewrite — history keeps both the design and the WER-scoring code intact for re-landing). The tone workload and revision=1 are restored as they were before this decision.** Four of six speech runs in a live Run-all failed with `SpeechClipUnavailable: ... has not been published yet` — the asset the decision below depends on was never uploaded, so shipping it took the feature from "measures the wrong thing" (the tone) to "fails on every machine", which is strictly worse for a user blocked right now. Re-landing this — once a human publishes the clip per the decision's own instructions — is a `git revert` of the revert; nothing below needs to be re-derived. **What follows, unedited, is the original decision as landed**, kept for exactly that purpose: The speech-to-text benchmark's synthesized sine tone (D443) is replaced with a real 30-second speech clip fetched once from our own mirror distribution and cached on disk under a pinned sha256, plus a word-error-rate accuracy metric scored against the clip's known reference transcript — and the workload's `revision` is bumped to 2 because the two are not comparable work** | Live numbers exposed the tone for what it was measuring: `whisper-tiny.en-8bit` "decoded" 30s of tone in 0.022s (1400x realtime) against `whisper-large-v3-turbo`'s 0.65s (46x) — a plausible-looking ranking that was actually timing how fast each model gives up on silence, since a tone contains no speech for early-exit/VAD-adjacent behaviour to differ on. Fetched off `ai/runners/mirror.py`'s existing distribution rather than a new endpoint, going through `mirror.base_url()` so an operator's `FUSED_MODEL_MIRROR` override (or explicit opt-out) governs this asset too instead of a URL hardcoded past it. **Downloaded once and cached under `storage.home_dir()`** (`<home>/ai/fused-benchmark-speech-30s.flac`) alongside `ai_benchmarks.json`, re-verified against the pinned sha256 on every use rather than trusted after the first fetch — a fixed workload whose bytes can silently change on disk is not fixed. **Offline (or no mirror) with no cached copy is now a real, RECORDED failure** (`SpeechClipUnavailable`, caught by `run()`'s ordinary `except BaseException` path into an `ok:false` row) — never a silent fallback to the tone, which would have produced a run indistinguishable from a real one while measuring something else, poisoning every comparison against it. **The reference transcript ships with the workload** (`WORKLOADS[SPEECH_TO_TEXT].params["referenceText"]`), not fetched separately, because it is as much "the fixed work" as the audio is; it is the opening sentence of Lewis Carroll's *Alice's Adventures in Wonderland* (1865, public domain worldwide) — chosen because the TEXT drives the audio rather than the reverse: whoever publishes the clip must record/source a reading of exactly this passage, so the reference is guaranteed to match rather than being a guess at an already-chosen recording. **The actual audio asset does not exist on the mirror yet** — `_SPEECH_CLIP_SHA256` in `benchmark.py` ships as an explicit all-zero placeholder that `_fetch_speech_clip_bytes` recognizes and reports as "not published yet" rather than attempting a fetch that would 404 or, worse, hit a squatted path; a human must record/source a ~30s mono 16kHz FLAC (~300KB) of the pinned passage, publish it to `<mirror base>/benchmark/fused-benchmark-speech-30s.flac`, and paste its real sha256 over the placeholder. **WER is a standard stdlib word-level edit-distance DP** (substitutions + insertions + deletions over the reference word count — the three collapse into one Levenshtein distance by construction, so one DP is the whole metric), scored after normalizing both sides identically (lowercase, punctuation stripped, whitespace collapsed) so formatting differences are not counted as transcription errors. **Rejected: normalizing number words against digits** ("twenty" vs "20") — a correct version needs a real text-normalizer (currency, dates, ordinals) the stdlib does not provide, and a wrong one would be worse than none; sidestepped instead by writing the reference passage with no digits in it at all, accepting that a future passage needing a quantity would take a benign WER hit from a model that prefers digit-style output. **`null`, never `0.0`, when the worker's reply carries no readable `text`** — the module's standing null-over-zero rule applied to this metric specifically, so an unreadable transcript cannot render as a (false) perfect score. |
### Model task classification (2026-08-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D433 | **The task vocabulary becomes ONE closed table — `ai/tasks.py`, keyed by the Hub's own `pipeline_tag` and vendored from `@huggingface/tasks` (57 rows) — and classification returns THREE states (`supported` / `no-runner` + a written reason / `unknown`) instead of a capability-or-null. Every evidence path in `hub_cache.py` now emits a TAG rather than prose, the `tag.replace("-", " ")` passthrough is deleted, `registry._TASK_CAPABILITIES` + `NO_RUNNER_YET` + `_FRIENDLIER_TAGS` + `_TASK_HELP` + `hub_models._CANDIDATE_TAGS` are all absorbed into it, and the two format fallbacks (`_engine`'s DECISIVE branch, `cached_capability`'s loaders-unanimity branch) fire only for `unknown` — never to rescue a task we have ruled out** | The vocabulary was OPEN and the classification was BINARY, and each half shipped a bug. **Open:** the card path ended in hyphens-to-spaces, so any string the Hub invented became a task label nobody had classified — `SymphonyGen/SymphonyGen` (`pipeline_tag: reinforcement-learning`, four bare `.pt` checkpoints of a symbolic-music policy, no `config.json`) came out as a label in neither table, fell through the loaders fallback onto `mlx-text`, and was drawn in the Playground's TEXT section with a Load button aimed at an mlx-lm that has no config to read. `test_every_task_label_is_classified` could not catch it: it enumerated the labels the module's own tables could produce, which stopped being the set the module could produce the day the passthrough was added. **Binary:** "ruled out" and "never heard of" were the same `None`, so nothing could refuse to rescue the first — a cached diffusers VIDEO pipeline (`_class_name` → `text-to-video`, a task nothing here serves) was handed `text-to-image` by `_engine`, because the diffusers runners are DECISIVE about the FORMAT and no caller could see that the task had already been decided against. **The tag is the key, not prose**: one spelling for the card path, the architecture path, the glossary, the Discover filter and a link to the Hub — the old two-vocabulary shape is why a whisper repo read from its card said "automatic speech recognition" and the same repo read from its config said "speech recognition", with a glossary entry for only one. **Vendored rather than fetched** because classification is OFFLINE by construction (`cached_capability` reads a snapshot directory and makes no network call), and a page that needed the Hub to say what a downloaded model is would stop working on a plane; the cost is that the table is a SNAPSHOT, which is exactly what `unknown` is for. Verified against the live Hub 2026-08-23: the 57 rows are what it serves, and the tags this module used to hand-list (`text2text-generation`, `conversational`) return zero models — a stale local copy that had been offering a dead filter in the Discover menu. **`no-runner` carries a SENTENCE, per row**, which is the half the API layer asked for by name: "video generation needs far more memory and time than the image runners here are built for", "speech synthesis is a separate capability from transcription — one resident model per capability", "a policy is run by the environment it was trained for". The page printed one flat line ("the model type is not supported") for a TTS model, a video pipeline and an unrecognised tag alike; `supportReason` rides the payload and `noEngineReason` prefers it, keeping the flat note only for `unknown` — where an explanation would be a claim we have not earned — and for an older server. **A second, independent guard in `formats.loaders`**: the safetensors branch now requires `config.json`, because `mlx_lm.load` resolves a checkpoint through it, so claiming the format without one was a promise the load could not keep. Two guards for one failure, deliberately: it was silent at both layers. Same shape as the Parakeet early return this file already carries. **Rejected: generating the table from `pipelines.ts` at build time** — 57 rows change a few times a year and a hand-maintained table is reviewable in a way a generated one is not, while the invariant that matters (every row classified, an unvendored tag `unknown`) is a test either way. **Rejected: keeping `capability_for_task` as a shim** — the null it returns is the exact ambiguity this removes. **Rejected: mapping `feature-extraction`/`sentence-similarity` to the embedding capability** now that there is somewhere to say why they are not: what wears them is a text-only sentence encoder with no `get_image_features` for either embedding runner to call. |
| D441 | **The Playground sidebar shows EVERY downloaded model, and one that nothing here runs says so: a closed-by-default "Not supported" `<details>` at the foot of the rail, drawing an unselectable dashed card per repo with its size, its task and the server's own sentence about why. Fed by a new `unsupported[]` key on `/api/ai/catalog` (never a row in `capabilities[].models`), built from the `capability is None` half of `cached_models()`. The "Your downloads" caption goes** | The catalog listed only what some capability could load, so a repo the user deliberately fetched — a text-to-speech model, a depth estimator, a symbolic-music policy — was in **no** picker and on **no** list this tab draws. It did not read as "we cannot run this"; it read as a download that failed, and the reader's next question (where did it go) had no answer anywhere on the page. **The row is drawn, not the capability invented** — D433 is what makes that possible, since a null capability is now three distinguishable facts with a written reason attached to one of them, and before it the only honest thing a card could have said was the flat "model type is not supported" that fitted a TTS model and an unrecognised tag equally badly. **A separate payload key rather than a fake capability group**: every app reading this endpoint maps `capabilities[].models` and offers what it finds, so a row in there is a row something will try to load — `unsupported[]` is opt-in, and an older client ignores it. **Closed by default, alone among the sidebar's `<details>`** (D428 opens the capability groups, and that stands): this is a reference list, not a menu — nothing in it is selectable, so an open one would put dead cards between the reader and the ones they came for. **The card is a plain div** — no role, no tabIndex, no click, dashed border, no hover response — because a card that looks pressable and is not teaches the wrong thing about every card beside it. The reason line WRAPS where every other line on a card truncates: it is a sentence, and eliding the one line whose whole job is to explain leaves the reader exactly where the missing card did. A repo we could not identify at all gets a card with no task chip and no sentence, which is the honest whole of what is known — inventing a reason there would be a claim we have not earned. **The "Your downloads" caption is deleted in the same pass** (reverses that clause of D428): it separated the curated head from the uncurated tail, and the cards already say which is which — a curated entry not yet fetched wears a Download button and a fetched one does not — so the heading was a second answer to a question the cards had answered. **Rejected: a badge on the card instead of a section** — the reader scanning for something to click needs the unrunnable half out of the flow, not annotated inside it. **Rejected: hiding them behind a toggle in Preferences** — the whole complaint was invisibility. **Rejected: the sparkle fallback icon** for the group; it means "a capability this build has not learned yet", which is the opposite claim, and a warning triangle would be wrong too — nothing failed, the download worked, this app just does not run that kind of model. |
| D460 | **The explorer's listing opens a row on a single click (release, no drag) instead of a double-click; its preview pane/sidebar stops following the listing SELECTION and shows only the OPEN FOLDER's own companions (`claude`/`git`/`mcp`), never a selected row's template or a selected subfolder's app; and the file sidebar's dragged width (`lib/side-store.ts`) and the folder pane's dragged width (`listing/pane-store.ts`) become one shared session value instead of two independently-remembered ones** | These three ship together because the second is what makes the first safe. Double-click existed only because a plain click had a second job to protect (FS-15's history): the pane used to render whatever the SELECTED row was — D280's folder preview, D281/D284's per-row Claude target, D269's app retarget — so a single click that navigated away would have made that preview unreachable for exactly the rows it mattered most on. Once the pane no longer reads the selection at all, there is nothing left for a click to protect by not opening, and the double-click detour — itself a workaround for the STILL OLDER bug where the same click opened or not depending on the window's width (D282) — goes with it: `Listing`'s `onRowPointerUp` opens any `select`/`defer` press that releases without passing the drag slop, in every listing, unconditionally; `onRowDoubleClick` and every `onDoubleClick` prop are deleted outright rather than kept as a harmless second path to the same place. **The pane itself is simplified to match**: `listing/pane-side.ts`'s `paneKey`/`paneSideTarget` drop their row/selection-count parameters (a mode's identity and its `_file` target are now a function of `(side, folder)` alone), and `ListingPreviewPane` sheds the entire selected-row branch — the `statPath`/`resolveConditions` template resolution, the `self`/no-row/multi-selection placeholders, and the chat-target retargeting `listing/chat-params.ts` existed solely to guard against (deleted with its test: a chat target that only ever changes on a real navigation is already reset by that navigation's own `NAV_EVENT`/`popstate` listeners, so the module had nothing left to do). Its neither-companion fallback (`PANE_SIDE_FALLBACK`, the folder-scoped case where both `claude` and `git`/`mcp` are refused, e.g. a mount) now renders a plain folder-scoped hint instead of resolving a row's own template — there is no row to resolve one for. `Listing`'s "Open app" affordance (`paneSubjectDir`/`appEntryPath`) is likewise pinned to the open folder rather than a selected subfolder, and the settle-debounce machinery built to make a selected row's pane mount cheap to arrow past (`useSettledLead`, `listing/pane-settle.ts`) is deleted along with its only caller. **Rejected: keeping the pane's row-preview mode and gating single-click-open on some narrower condition** — every candidate condition (window width, pane-open state, embedded-vs-not) is exactly the kind of "same click means different things depending on unrelated state" rule D282/D285 already spent two decisions removing; deleting the coupling outright is the only fix that does not reintroduce it from another angle. **Width**: `lib/side-store.ts` (file sidebar, pixels) and `listing/pane-store.ts` (folder pane, a fraction of its container) already shared the same default-width arithmetic (`lib/side-width.ts`'s `companionFrac`, D283) while remembering a drag independently per surface; they now read and write the SAME module-level pixel value, converting to/from a fraction at the folder pane's container width, so a drag on either carries over to the other within the session — the no-persistence-across-refresh policy both modules' headers already stated is unchanged, since the shared value is still a bare module variable. Pixels were kept as the shared unit (not the fraction) because the file sidebar's floors (`MIN_W`/`CONTENT_MIN_W`) are legibility constraints stated in pixels and have no honest fractional form, while a fraction-of-container converts to pixels losslessly given the container width at hand. **Follow-up fixes, caught across two code review passes before this shipped:** (1) a shared PIXEL width has no bound of its own once it can arrive from a container it was never dragged on — a wide monitor's file-sidebar drag read back on a narrow folder window, or this pane's own container merely shrinking under an already-dragged width — so `paneFracFromSharedWidth` caps the derived fraction at `MAX_PANE_SHARE = 0.7` on top of the pixel floors, and answers the plain companion share unconditionally below the 280px floor sum rather than a fraction over 1. **The first cut of this fix capped only the RENDER** (`paneFracFromSharedWidth`), leaving `dragPaneFrac` — the LIVE drag's own math — uncapped at its old `(W − 60) ÷ W` ceiling; a second review pass caught that this let the divider detach from the cursor mid-drag (rendering stalled at 70% while the cursor kept moving, with no resistance band to explain the stall) and, worse, committed the raw uncapped cursor position to the shared store at release — a number the render had never actually shown, which the file sidebar or the next folder would then open at. Both `dragPaneFrac` and `paneFracFromSharedWidth` now read through one function, `clampSharedPaneWidth`, so a local drag and an imported width are bounded identically and the stored, rendered and committed numbers can never disagree — rejected the alternative of capping only the imported path, since "was this pixel number dragged here or somewhere else" is not a fact the shared store retains one render later. **That same review pass also caught the cap silently undercutting `PANE_MIN_W` between 280px and ~314px** (`PANE_MIN_W ÷ MAX_PANE_SHARE`): a share cap taken before the pixel floor could ask for fewer pixels than the floor itself (196px of a 280px container), which CSS's `min-width: 220px` would then override, disagreeing with the computed flex-basis. `clampSharedPaneWidth` applies the floor LAST, unconditionally — the same ordering `clampPaneWidth` always used for the pixel floors alone — so the pane's own floor wins outright in that narrow band, exactly as it did before the cap existed. (2) single-click-open makes a HABITUAL DOUBLE-CLICK double-open — the second press lands on whatever row the just-opened folder painted under an unmoved cursor — so `Listing` arms a ~400ms post-open suppression window (`suppressPressUntilRef`) that swallows the very next press outright; this is not the double-click timer coming back, since nothing delays the FIRST press's own open. (3) single-click-open also makes the classic mouse gesture "click A, Shift-click B" unable to START a range any more — A already opens before B can be Shift-clicked — which has no code fix that preserves the exact old gesture without reintroducing a pre-open delay; the decided (not code-changed) rule is that `Mod+click` is now the mouse-only way to anchor a range without opening, exactly as it already worked before. (4) `embedded` (the `Listing` prop for the pane's own nested `_listing` preview) had no caller left once that whole selection-driven branch was deleted, and is removed outright — along with the now fully test-only `pane-modes.ts` functions `activePaneMode`/`paneOpenTarget`/`paneOpenAction` (their tests too; `paneModeList` stays, `mode-labels.test.ts` genuinely uses it), a stale `paneSubjectDir === null` branch in `Listing.tsx` that could no longer occur, and an unclamped write path in `PreviewSidebar.tsx`'s store subscriber that a second writer (the folder pane) could now reach with a width narrower than the file sidebar's own floor. |
| D442 | **Showcase apps can declare what AI they drive in metadata.json — `ai_capabilities` (catalog capability ids the app works across), `ai_models` (exact wire ids, when the app only works with specific models) and `ai_model_param` (the app's own URL param for the model, never literally `model`) — and the Playground draws a strip of matching apps under the stage (`PlaygroundApps.tsx`, rule in `appMatch.ts`) — the hub's own `AppPreviewCard` fed a synthesized AppInfo for the clone's copy (authored preview.png still, live hover, same card as /apps and /home), with a new `href` override on the card because `hrefFor` deliberately carries no query string and this click's whole point is one: a capability match is listed, an `ai_models` hit is listed FIRST with a Recommended badge, an `ai_models`-only app whose list misses the selected model is not listed at all, and clicking opens the showcase clone's copy with the selected model id in the app's declared param** | The Playground proves a model works in one call; the showcase already holds finished apps built on that same call, and nothing connected them — a user who liked a model had to find local-chat by browsing the hub and re-pick the model inside it. Matching is metadata, not inference: `category: local-ai` is not a reliable discriminator (open-mail calls fused.ai from `productivity`) and an app that CALLS fused.ai still can't take a handoff unless it reads a model param, which is exactly what `ai_model_param` asserts. **`ai_model_param` is required for a listing and `model` is rejected** — the shell's sidebar owns the `model` query key, which is why every AI app in the repo already namespaces (chatModel/imageModel/asrModel); an app we could open but not hand the model would wear a card that silently drops its promise. **The vocabulary is the catalog's own** — capability ids verbatim ("text-generation"), model ids as the wire knows them (repo id or GGUF filename) — zero mapping to drift. **Catalog read is `action:"catalog"` only, section hidden on no-cache/error/no-match**: escalation-to-clone stays the hub Showcase tab's job (useShowcaseSync); a model-trying tab must not clone a git repo, and an empty heading advertises a feature the machine can't show. **Rejected: matching on tags or category** — curator vocabulary, not a contract; open-mic is `local-ai` yet has no model param. **Rejected: opening via the /apps hub** — one more click between "this model works" and "here it is working", and the hub's cards deliberately carry no query string. **Its own component + `.pg-apps-` CSS block** so the concurrently open #760 redesign of PlaygroundTab costs neither PR a rebase. |
| D464 | **The stage settings leave the column and become a narrow card beside it, with the PAIR centered on the stage: `.pg-work` grows a second grid column, a 240px track holding a `.pg-config-card` rail that is absolutely positioned INTO that track (`inset: 0`) with the visible card (`.pg-config-inner`: bordered, `--bg-alt`, a SETTINGS caption) sticky inside it, all gated on `@container (min-width: 1000px)` over a `container-type: inline-size` `.pg-stage`. Opening the cog never changes the input column's WIDTH — it stays 680px and slides left by half the card's footprint (`margin-left: (100cqw - 680px)/2 - (gap + card)/2`, i.e. the centered pair's own left edge, 128px). The model banner above is the fixed landmark and does not move at all: `.pg-hero` widens to `min(680px + gap + card, 100% - 32px)` — exactly the pair below at its widest, on a 16px gutter instead of 32px — rather than staying a 680px card on the column's spine. Below 1000cqw the card renders in flow under the input.** | Asked for, in two passes. The first pass put the settings behind the heading's cog but left the panel a full-width band across the column, so opening it shoved the input, the chips and the result down the page — a settings surface that costs you your place in the thing you are configuring. Beside the column, a slider move and the picture it changes are in one glance. **A container query, not a media query**: the question is how much room is left around the 680px spine, and the viewport is a poor proxy — the shell sidebar, `cc-main`'s padding and the 300px model rail eat ~630px before the stage gets any, so a viewport breakpoint would open the card at widths where it does not fit and withhold it at widths where it does. **A grid track for the width, abspos for the height**: an in-flow card would size its own grid row, so opening the cog would push the composer down by the card's height — the exact shove this row exists to remove — while a purely absolute card beside the column, with no track reserving width, would cover the composer's own Run button at laptop widths. An abspos grid child takes its grid area as its containing block (row edges falling back to the container's padding box when the row placement is auto), which is what makes `inset: 0` a full-height rail; **`grid-row: 1 / -1` does NOT work here** — negative line numbers resolve against the explicit grid and there are no explicit rows, so it silently collapses to row 1 and inflates that row by the whole card. Rail and card are two boxes because sticky cannot live on an abspos box. **The column's WIDTH is the fixed point, not its left edge** (this reverses the first pass, which pinned the left edge and let the column narrow into the 820–1200cqw band): a column that resizes as the cog toggles rewraps the prompt, the result and the sample pills every time, which reads as a bigger change than the one the user asked for; sliding a same-width column left changes nothing inside it. **The vars live on `.pg-stage` OUTSIDE the query**, because a `@container` rule can never target its own container — the query resolves against the container's ancestor, so `--pg-card` declared on `.pg-stage` inside the block would never land and every `calc()` reading it would fail. **Accepted cost: the 820–1000cqw band loses the side card** and falls back to the in-flow panel, where the first pass kept it by shrinking the column; a card taller than the column overflows its rail toward the apps strip. **The banner widens rather than riding along**: an intermediate pass shifted the hero with the column to keep their left edges in line, which made the model's name jump 128px sideways on every cog click — the one element on the stage that should stay put. Given the pair's own width it has no column edge to keep in line with, and its edges are where the pair's edges land, so opening the cog fills the space under it instead of moving it. **Rejected: a modal or popover** — the point is to watch the result change while the knob moves. **Rejected: stretching the banner to the stage's full width** — it would match the apps strip at the foot of the stage, but a header wider than anything it heads reads as page chrome, and on a wide screen the 40px icon and three facts strand in it. |
| D465 | **Every stage's example prompts become a rotating row of outlined pills: an authored sample is `{ name, icon, prompt }` (`Starter`, controls.tsx) drawn as a hug-the-content pill — icon plus a two-or-three-word name, no fill, no shadow, round rotate button at the end — four on screen out of eight per stage, rotate stepping the window modularly through the rest. **How many are on screen is measured, never truncated**: a `useLayoutEffect` compares the row's `scrollWidth` to its `clientWidth` and drops one pill at a time (to a floor of two), a `ResizeObserver` re-asks for the full four when the column grows back. Icons come from one shared 19-glyph vocabulary (`starterIcons.tsx`, capabilityIcons' grammar) rather than one drawing per sample. `StarterCards` is generic over the sample and hands the whole object back, so a stage's pick can set more than a prompt: **the embed stage's samples are whole scenarios** (query + the six lines it is searched against, each set built so the three semantic matches share no word with the query), and `run()` takes both as arguments because state set in the same click is not readable yet. The row is shown by the four stages that take a prompt (text, image, embeddings, video); the transcribe stage has none, its input being a recording. | Asked for. The one-line chip row this replaces could only hold what fits on one line, so every example was a topic ("Write a haiku about running AI on a laptop") rather than a prompt worth running — and a newcomer learns nothing from a prompt with no constraints in it. Splitting the sample in two fixes both halves at once: a short name to scan, and a two-or-three-clause prompt behind it (hover `title`, load-bearing here rather than decoration) that demonstrates asking for a length, a format and an exclusion. **Pills, not the /apps hub's filled cards**: a card carries a picture and a meta row, these carry two words, and at pill size a fill plus a shadow reads as four heavy blocks under a quiet input. **Measured count, not a breakpoint and not an ellipsis**: the pills hug their names, so what fits depends on WHICH names are up — a px threshold would clip one page and leave a gap on another, and a truncated two-word label ("Lighthouse at d…") is the one thing this row cannot afford, since the name is the only thing drawn. Equal-width grid columns were rejected for the same reason: they are as wide as the longest name and cut every other one. **Eight authored, four shown**: eight full prompts written out is a wall in front of the input, and the reader only needs one — rotate is two clean pages, modular over the authored order so clicking back around lands on the same pills (no shuffle: an example row that reorders itself under the cursor is a slot machine). **Rejected: a "TRY ONE" heading over the row** — the cog pass deleted exactly that label when the starters left the empty state, and a rotate button does not need one. **Rejected: read-aloud scripts for the transcribe stage** — built first (a picked line shown above the record button, never auto-starting the mic) and removed by request: its input is a recording, and a wall of suggested sentences beside a record button answers a question nobody with a file to drop was asking. |
| D467 | **The image stage can be handed a base image to edit, and a picked file is POINTED AT rather than copied: `/api/ai/catalog` grows a computed per-entry `acceptsImage` (the resolved engine via `engine_options`, then mflux's own `formats.mflux_edit_recipe` — the two gates `/api/ai/image` refuses with), the composer grows Add an image + Webcam pills beside Generate with the photo and the live viewfinder under the prompt, and `dirpicker` grows `pick_file` behind a new `POST /api/fs/pick-file` so the shell can learn an absolute path at all. With an image attached the stage renders at the PICTURE's shape scaled to a 640 longest side — measured in the browser off the decoded file, snapped down to a multiple of 16, never upscaled (`fitToImage`) — and the size controls collapse to that one pair of numbers, with "Set a size" as the way back.** | Asked for. AI-9f shipped the editing half a release ago and nothing in the app could reach it: `fused.ai.image({image})` was page-only, and the Playground is where a person finds out a local model can do this at all. **The flag is computed, never curated**: the two facts live in two runner-side modules (one of them a table of mflux variant classes), a picker cannot ask either, and a hardcoded repo list in the frontend would draw an attach button whose every request comes back 400 the day the Engines tab is switched to diffusers. False on every non-image capability rather than True-by-vacancy — `engine_options` is an exception list, so a text runner "refuses nothing" and a flag read off that alone would have every chat model claiming it takes a photo. **`pick_file` exists because a browser strips the path.** The first cut uploaded the picked file into `~/ai/inputs` and was rejected on sight: it copied bytes that were already on this disk — and `/api/fs/mkdir` makes ONE level, so on a machine that had never rendered it also 400'd on the missing `ai/`. The honest fix is the dialog the OS already provides, raised by the process that has filesystem authority, which is the argument `dirpicker.py`'s own docstring makes for the folder chooser; it is one shared body now (`_pick`), with `files` passed only when true so every existing caller and stub still sees the two-argument backend. **A webcam frame is the one thing that IS written** — it does not exist anywhere yet — and it goes to the app's own scratch dir, `~/.fused-render/cache/image-playground`, newly exposed as `/api/config`'s `cache_dir` (branch-aware `storage.home_dir()`, so a worktree's leftovers are its own). NOT in the user's home at all: bytes this app invented belong with the app's state, and the first cut's `~/ai/inputs` put them one folder from `ai/images`, where a capture reads as one of the renders the user browses there. Path only on the wire, like `fused_dir`, and both levels are mkdir'd because `/api/fs/mkdir` makes ONE by design. **The dialog is narrowed to the three formats AND the pick is still checked**, both off the one `ATTACH_EXTENSIONS` list. The first cut filtered nothing — the three backends express filters three incompatible ways, and a greyed-out file cannot explain itself — and it was wrong the first time somebody picked a screen recording: being told a `.mov` is not a PNG is a worse answer than never being offered it, and `pick_file` grew a `types` list of bare extensions that each backend translates itself (`of type {…}` for `choose file`, `setAllowedFileTypes_` on the NSOpenPanel, a `--file-filter` pair for zenity, a positional glob string for kdialog, one `Filter` for `GetOpenFileNameW`) rather than making five callers learn five filter dialects. All-files stays reachable in every backend that has the notion, because a filter is a courtesy and a format the renderer CAN read but was not listed must not become unpickable. The check stays because the filter is the dialog's half only: it does not reach a drag-drop, a typed filename, or the Linux tools, which can merely suggest — and `/api/fs/pick-file` normalises `types` before any of it, dots off and alphanumerics only, DROPPING an over-long one rather than truncating it (a sliced extension is a filter matching files nobody asked for) and falling back to show-everything on junk, since a malformed courtesy must not become a dialog that can pick nothing. **The size is the picture's SHAPE at this stage's own scale, not the server's 1024 and not the stage's 480x272.** The first cut left `width`/`height` off and let the route derive them (fit the longest side to 1024, AI-9f): correct for a page calling `fused.ai.image`, and minutes per edit here — 1024 wide is ~4.5x the pixels of this stage's own fresh-render default, and this stage's whole promise is a picture back quickly. So the browser measures the file it is already showing a thumbnail of (`new Image()`, no new endpoint) and the fitted pair travels explicitly, which also means the panel shows the numbers that will run; a probe that has not answered falls back to the server's derivation rather than to 480x272, since a wrong SHAPE is worse than a slow render. Sending the stage's own size unconditionally would have resized a photograph down to a thumbnail on the way through an edit — and leaving the sliders on screen showing 480 next to a render that comes back 1024x688 is the "a control that lies about what will run" failure this stage refuses everywhere (its own clamp comment states it: the route floors every side to a multiple of 16, so a control may not show a number the render will not use), hence the collapse rather than a disable. `usableBase`/`imageFields` (imageInput.ts) hold both rules once, because the stage applies each of them twice — to decide what to draw and to decide what to send. **Three of the eight examples bring their own PHOTO** (`EDIT_STARTERS`, shown in place of the first three prompt-only ones on a model that can edit): an edit prompt with nothing to edit demonstrates nothing, and finding a suitable file is the step that stops somebody trying this at all. The photos ship in `static/samples` (Unsplash, re-encoded at 896x597, credited in a CREDITS.md beside them) and are copied to the cache dir on click, because the route reads a FILE and cannot be handed a URL inside a signed .app — under a FIXED name, so clicking a pill twice cannot fill the cache with copies of one sample. Picking one attaches the photo, fills the box and RUNS, exactly as a prompt-only pill does — everything the render needs is passed to `generate` rather than read back out of state it cannot see yet, and the picture is measured off the blob so the first edit is fitted to its shape rather than falling through to the server's slower 1024. The eleven are ONE pool, shuffled once at module scope: three photo pills at the head of the row read as a separate section, and shuffling per render would be the slot machine D465 refused. The composer's thumbnail opens the picture full size in a two-element lightbox (image, ✕, backdrop and Escape both close) because 28px is a reminder of WHICH picture, not a look at it, and Clear now drops the attachment with the prompt and the result — it is part of the request, not of the setup. **Rejected: an in-page file browser** — the shell would have to grow one (the template-served `folder-picker.js` is not importable from React), and the OS dialog is the thing with the user's sidebar, favourites and search in it. **No packaging change**: the app's UI runs in the user's own browser, so the camera grant is the browser's — `NSCameraUsageDescription` and the camera entitlement would only matter to a webview this app does not have (unlike `NSMicrophoneUsageDescription`, which exists because the PYTHON process records audio). |

### Text-to-video generation via h3.c (2026-08-22)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D457 | **Video generation's engine is `antirez/h3.c`, a bundled Metal binary, and NOT a Python runner — Apple Silicon only, with no fallback anywhere else** | Every other capability here has an "everywhere" row: text on llama.cpp, images on Diffusers, speech on CTranslate2, embeddings on transformers — a torch or CPU build that runs on any platform even when it is not the default. MiniMax H3 has none. **Rejected: mlx-video** — cannot run H3's architecture at all. **Rejected: ComfyUI's diffusers port on MPS** — its int8 kernel path is CUDA-only, and an M-series Mac without Metal 4 TensorOps falls back to bf16 speed, which measures no better than h3.c and carries a much heavier dependency stack. **Rejected: h3.c-ane** (the CoreML/ANE variant) — its per-shape compile cache runs to roughly 19GB, which turns a single canvas choice into a standing multi-GB tax nobody asked for. **Rejected: Phosphene** (a packaged macOS app around the same model) — it is an app, not a library or a CLI with a stable contract, and its dependencies do not survive a bare `uv sync` the way every other runner folder's do. What is left, and what h3.c actually is, is the ONLY implementation that runs the real architecture at a real speed with a dependency footprint this app can bundle — a real Metal binary this app stages exactly like it stages rclone (D103), not a Python package. That shape is why the capability breaks the pattern every other one here follows: `registry.py`'s `_h3_available` gates on Apple Silicon AND a resolvable binary, `catalog.describe()` reports `default: null` for the first time when either half fails, and `h3_video/worker.py` is the one runner in this app that shells out to a subprocess instead of loading a model into its own interpreter. |
| D458 | **The video route's clamps (`/api/ai/video`'s width/height/frames/steps bounds) are copied from the BUILT BINARY's own validation, not assumed from documentation or by analogy with the image route** | A route that guesses a video engine's limits either refuses requests the engine would have accepted, or worse, accepts requests the engine then refuses deep inside a render nobody is watching yet. Verified by actually cloning and building `antirez/h3.c` at the pinned commit (D103-style staging) and reading both its `--help` and its C source (`h3_host.c`, `h3.c`): the canvas rule (32-pixel multiples, ≤768×1344 total) matches `H3_CANVAS_MULTIPLE`/`H3_MAX_PIXELS` exactly; the frame grid is genuinely `5 + 17n` (`h3_video_vae.c`'s `chunks * 17 + 5`), but the binary's own `h3_align_frame_count` rounds a requested count UP to the next grid point, never to the nearest one, which this route now matches instead of the nearest-rounding an unverified first draft used; the frame floor is `n=1` (22 frames) rather than `n=0` (5 frames), because h3 refuses a lone 5-frame chunk outright ("requires at least one trained 22-frame decoder chunk"); the steps floor is 2, not 1, because h3 refuses `steps=1` ("denoising steps must be in [2, 1000]"); and the canvas default (864×480) is copied from the binary's own tuned default rather than an arbitrary square guess. **Rejected: trusting the plan's first-draft numbers (nearest-rounding, a 5-frame floor, a 1-step floor, a 768×768 default) without checking them against the real engine** — every one of those four was wrong, and each would have surfaced as a render that failed (or silently under-reported) only on real hardware, which is exactly the gap between "this app's own guess" and "the engine's own rule" this decision closes. |
| D459 | **ffmpeg reaches the h3 subprocess through the `h3_video` runner's OWN venv (the `imageio-ffmpeg` wheel), never through the app's payload and never through the system PATH** | h3 needs a real ffmpeg EXECUTABLE — not a library — to mux the rendered frames and the generated audio into one mp4; PyAV ships libraries, not a binary on a PATH, and asking h3 to skip the mux (`--frames-dir`, raw PPM frames) drops the audio, which is H3's headline feature over a silent video-only engine. **Rejected: bundling a static ffmpeg into the macOS app payload**, the same way rclone and h3 itself are bundled (D103, D457) — ffmpeg is needed by exactly one runner's subprocess, not by the app process itself, so putting it in the app-wide payload would tax every install with a binary only one capability ever touches, and would need its own version-pin/staging/smoke-test ceremony for a need `imageio-ffmpeg`'s wheel already satisfies. **Rejected: relying on a system ffmpeg via PATH** — a supervisor-spawned worker's environment is not guaranteed to carry the interactive shell's PATH, and "install ffmpeg yourself" is exactly the "one prerequisite" ask D103 already eliminated for rclone. **Chosen: `imageio-ffmpeg`**, a small pure-Python wheel that already bundles a real per-platform ffmpeg executable and exposes it via `get_ffmpeg_exe()` — `h3_video/worker.py` points `H3_FFMPEG` at that path before spawning h3, so the subprocess's own PATH never matters. |

### Dropping the h3.c video engine, and the macOS 14 launch failure it caused (2026-08-24)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D468 | **The `h3-video` runner is dropped outright — MiniMax H3 on `antirez/h3.c` goes, the macOS build jobs return to `macos-14`, and `ltx-video` (LTX-2.3 on `ltx-2-mlx`) is video generation's only engine** | D457 bought this engine for its ceiling and it could never have run on the oldest macOS we support, on two independent counts: the built binary carries an implicit `minos 15.0` (h3.c's Makefile sets no `-mmacosx-version-min`, so clang defaults the deployment target to the BUILD HOST's OS, and dyld refuses a main executable whose minos exceeds the running OS), and h3.c's attention path calls MPSGraph's `scaledDotProductAttention` and `MTLCompileOptions.mathMode`, both `API_AVAILABLE(macos(15.0))` with no pre-15 fallback — so even a corrected deployment target would only have moved the failure from launch to an `unrecognized selector` mid-render. The CI smoke test never caught it because it runs the binary on the macos-15 runner that built it, the one OS where it cannot fail. **The removal is not really about H3's own reach, though — it is about what bundling it cost everything else.** h3.c needs a macOS 26 SDK, which is why D457 moved the release job to `macos-15`; `build_dmg.sh` bundles Homebrew's `python@3.12` FRAMEWORK, and Homebrew ships prebuilt PER-OS bottles, so that runner staged a `pyexpat.so` referencing `XML_SetReparseDeferralEnabled` — a symbol macOS 14's `/usr/lib/libexpat.1.dylib` does not export. `fused_render.installed` imports `plistlib` imports `pyexpat`, so `dlopen` failed before anything ran and py2app showed only its generic "Launch error": **v0.4.49-v0.4.51 could not start AT ALL on macOS 14**, in exchange for a video engine that could not run there either. D457's own workflow comment argued the move was safe because "the app's own MACOSX_DEPLOYMENT_TARGET is unchanged" — true, and exactly the wrong test: a deployment target governs code WE compile, never bottles we bundle. **Rejected: bundling libexpat and gating h3 on macOS 15+** — it is the smaller diff and it works (the manual fix on the reporting user's machine was exactly that), but it leaves the whole class alive: every Homebrew-bottled binary in the payload can carry a newer-than-the-host linkage, and the build stays on a runner whose only justification is a runner nobody can use below macOS 15. Pinning to `macos-14` fixes the cause instead of the instance. **Rejected: keeping h3 and splitting the jobs** (h3 on macos-15, the framework Python staged on macos-14) — two images to keep in step for one optional engine. **The capability is not lost**: `ltx-video` already sat FIRST in the table and was the default any bare `fused.ai.video()` resolved to, so what goes is the high-fidelity 144GB option reachable only through the Engines tab. `registry.video_traits_for`'s fallback moves from H3's row to `ltx-video`'s, which changes the shape a runner this table cannot name gets (704x480/8 steps on `1 + 8n`, not 864x480/20 on `5 + 17n`) — the fallback is the SHIPPING engine's row on purpose, so an unnamed caller gets a shape some real engine accepts. **Two traces stay deliberately.** `formats.loaders` still returns early on a root `FL2VA/` tree while claiming NO runner — the exact shape D406 left the withdrawn Parakeet runner in — because `MiniMaxAI/MiniMax-H3` carries a root `model_index.json` of h3.c's own bookkeeping, so falling through would mislabel it as diffusers-loadable and offer a Load button on a layout diffusers cannot read to anyone who already fetched its 144GB. And `/api/ai/video`'s cross-engine refusal is kept as unreachable code, generic over runners rather than about those two, since it is what a second video engine's arrival would otherwise have to remember to add back; its test now drives the reading rather than a staged snapshot, which is the only honest way left to exercise it. `tests/test_build_dmg_diagnostics.py` pins `macos-14` and the absence of `macos-15` in both workflows, so moving the image forward requires first making the build stop bundling a per-OS bottle. |

### Models cards lose a badge, task rows gain a way out (2026-08-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D448 | **On the Models tab: the footer's "✓ downloaded" chip goes, and a partly downloaded card's button reads "Continue downloading". On the Tasks list: the thread count wears a speech bubble instead of the word "messages", the folder chip marks an active filter in the ACCENT, each filter menu carries its own attached ✕ once it is narrowing anything, and the folder menu sorts by the name it PRINTS** | Six by request, four of them reversing calls this repo argued for out loud, which is worth saying plainly. **The chip** was a second answer to a question the green wash already answers, and since almost every card in a capability row is downloaded it was a badge on nearly all of them — a mark that appears everywhere marks nothing. **"Download"** on a card holding two thirds of the model reads as an offer to start over, which is the one thing that button does not do; the word now matches the act (the class does not change, because it IS the same act at a later stage). **The bubble** reverses the argument that a glyph must be learned before the row can be read — true exactly once, repaid on every row after it, on a page read by sweeping a column; what made the old bare "4" ambiguous was standing alone between a folder and a time, and a number with a bubble welded to its right is not that number. The noun survives as the element's `aria-label`, because a bare digit is exactly as unlabelled to a reader who cannot see the glyph. **The accent pill** reverses "the accent on this page belongs to what is RUNNING": the quiet fill it wore is the same wash the chip wears on hover, so at rest the difference between "the pointer is here" and "this folder is the filter" was one hairline. The ambiguity with running state is real and survives only because running is drawn as a DOT and a ring on the row's left, never as a filled pill around a folder name — and one chip at most can be on. Both channels stay subtle, and the hairline more so after a look at a real filtered list: at 55% it drew a hard ring around every folder in the column and became the loudest thing in the row, so it is 22% — the wash marks the state, the border only gives it an edge. **The ✕** exists because three controls can narrow this page and undoing them meant remembering which ones you had touched; a menu's little count says a filter is on but is not a way out. It is ATTACHED to the menu it clears — `[ ⊙ Project 1 | ✕ ]`, one pill with a divider — after a lone "Clear" on the bar was tried and failed twice over: it could only mean all three controls at once (there is no room for three Clears), so dropping a project filter also threw away a status filter and a half-typed search; and it was a fourth box in a row of three menus, a different kind of control at a different height, which is what read as misaligned. Drawn only at a non-zero count, so the control is one box at rest and two only while it is doing something. The geometry is deliberate: the trigger gives up its right corners, the ✕ carries them and has no left border, so the two boxes share one 1px line and it lands as the divider — and the ✕ takes its height by stretching in the same row rather than declaring one, which is what keeps the pair level with the menus beside it. It needs `.prefs-section` and `.deploy-body` twins for the same reason `.schedule-tv-filter-btn` does: those page skins style every button at (0,1,1) and outrank a lone class, which is exactly how the first attempt came out 28px tall against a 32px trigger. **The folder order** was a real bug, not a preference: `projectOptions` sorted the full path while the menu prints `basename(path)`, so a list alphabetical by `/Users/me/Desktop/…` then `/Users/me/Fused/…` arrived on screen in no order a reader could see. Sorted by the printed name now, with the full path as tie-break so two same-named checkouts cannot swap places between renders. |
### Claude sessions belong to the file they were opened on (2026-08-23)

| # | Decision | Choice | Rationale / rejected alternatives |
|---|---|---|---|
| D449 | **A FILE target is offered only the chats that were opened on that file; a FOLDER target keeps the whole pile. The pane the chat was opened on comes off the leading `<live-app-state>` block, read by a fourth hand-duplicated mirror (`agent._pane_file` / `tasks_store.pane_file`), and rides the session row as `pane`. `_workdir`'s file→parent collapse is NOT undone.** | Three files in one folder shared one list of chats, so selecting `file1.py` offered a chat that was entirely about `file3.py` — the answer to "what have I discussed about this" was a folder's worth of other people's business. The collapse that causes it is not the bug and cannot be removed: Claude Code keys its session store by cwd, a file has no cwd, and resume, history and spawn all have to keep using the folder — only the LIST was wrong. The block is the one durable record of the file, because the transcript's own `cwd` is always the folder; it is already what `tasks_store` reads to decide which file "open this task" lands on, so the list and the task row now answer from the same fact instead of two. **Rejected: showing pane-less folder chats under every file as a second group** — a terminal session in the folder is not about any one file, and putting it under all three is the pile again with a divider in it; the folder is one click up and is where it already belongs. **Rejected: a per-file sidecar recording which chats belong to which file** — D335 is retiring the sidecars, and a new one would be a second source that can disagree with the transcript. **Rejected: deriving the file from the first prompt's text** — a chat that never names its file is not a chat about the folder. The pane is read RAW and before the stripper, kept across records, and compared as an abspath: the block is exactly what `_strip_machinery` exists to remove, a first send can be wordless and still carry the pane, and the block's url and the caller's target can spell one file differently. Cost is unchanged — the read rides the head read that already titles each row, so no transcript is opened twice. |
| D450 | **A row for a chat opened on ANOTHER file names that file, on the right, in the same grid cell as the relative time — time at rest, filename on hover or focus, cross-faded in place. Pressing it sends the HOST to that file with the session attached (`/explorer/view/<path>?_side=claude&session_id=<id>`) instead of resuming in this pane.** | On a folder the list now mixes chats about the folder with chats about each of its files, and a row that cannot say which is a row you have to open to identify. The name goes where the eye already ends the row, and it shares ONE cell with the time (`grid-area: slot` on both) so the cell is as wide as the wider of the two and the row's geometry is identical hovered and not — swapping the text outright, or stacking absolutely-positioned spans, reflows the row under the pointer and makes a list jitter as it is scanned. Basename on the row, whole path in `title`: the folder is the one already on screen, and an ellipsised name still needs somewhere to be read in full. Shown only when the pane differs from this target, which on a file target is never — naming the same file down every row would cost each of them a timestamp for no information. Opening in the host rather than here is the honest move: resuming a chat about `file3` in a pane previewing `file1` puts a transcript beside a preview of a file it never mentions, and the URL it uses is the one the Tasks list already opens a task on. **Rejected: a permanent filename column** — every row on a file target would carry the same word, and on a folder most rows have no file at all. **Rejected: `display: none`/`:empty` swapping** — both change the cell's width and reflow the row. |
| D451 | **A scheduled run prepends its own minimal `<live-app-state>` block naming `entry["target"]`, and only when that target is a file on disk. No pane snapshot, no screenshot, no annotations, and the stored `entry["message"]` is not rewritten.** | The preamble a human's send carries is built in browser JS at send time (`template.html` `composeOutgoing`), so a scheduled run — which has no browser — produced a session with no pane at all, and D449's filter would have read a task that plainly named a file as a folder chat. The scheduler holds exactly one of the facts that block normally carries, so it writes exactly that one and says in words that this is a scheduled run: a description of a screen nobody was looking at is a lie in a transcript the model reads mid-run. A folder target is left alone because the folder is already what the transcript's `cwd` says, and a block naming it would claim a pane that never existed. **Rejected: full parity (pane shot, annotations)** — there is no renderer and no reader to take them from, and faking them is the same lie at greater cost. **Rejected: writing the block into the stored entry** — the store holds what the user typed, and every reader that shows a human what was said peels the block back off anyway. |
| D452 | **The right end of a recent-chats row is `[file icon] [file name] [time]`, and the TIME NEVER LEAVES: at rest the icon alone marks a row whose chat was about a file, and on hover the name opens out between them on `max-width`, pushing the icon left and ellipsising the title to pay for it. In the Tasks LIST, a task whose target is a file wears the same glyph after its title, with the path in the `title` the folder chip already taught.** | D450 shipped this as a cross-fade — the time faded out and the filename faded in, sharing one grid cell so the row could not change width. Holding the geometry was the right instinct aimed at the wrong cost: a row that answers "when did this last move" only while you are NOT pointing at it answers at the wrong moment, and the two facts were never alternatives to choose between. So the name gets its own space and the row is allowed to open. `max-width` is what it animates, because the alternatives each break something visible — a name laid out from `display: none` shoves the time sideways in a single frame, and one positioned out of flow overlaps the title instead of making room for itself. The ICON is what makes the rest state readable: without it a folder chat and a file chat are the same row until hovered, and with a full column of names the titles drown. It carries the path in its own `title` too — at rest it is the only part of this on screen, so pointing at it has to answer the question it raises. **The Tasks list takes the glyph and NOT the name, which is the opposite call to the message count two elements along and made for the opposite reason**: a count is a number that needs a unit to be read at all ("4" between a folder and a time is a number with no unit), where a filename is prose, and a column of prose at the busiest end of a row is the crowding that row has twice been trimmed for. `taskFile` reads "the target is not the project", because `routers/tasks.py:_place` falls back to the project folder and that fallback is the only way the two are ever equal — compared with trailing slashes off both, so a folder spelled `/p/` does not grow a file mark over a slash. The mark is `position: relative; z-index: 2` for the same reason the folder tag is (D435): `.tasks-rowlink` is an invisible `<a>` over the whole row, and an element that does not lift out of the way never receives the pointer — this one exists only to be pointed at. **Rejected: putting the filename in the row** — see the count's argument, inverted. **Rejected: replacing the folder chip with the file** — the chip is the project filter, and the more specific fact does not get to eat the control. |
| D453 | **A live turn says "running" at the END OF THE CHAT'S NAME in the top strip, shimmering — a gradient clipped to the word, driven by the same `body.running` class the composer's stop-button swap already owns. And the ⋮ menu grows a second item below "Continue in terminal": archive or unarchive THIS conversation's task, keyed by the session id, hidden outright when no task is behind the chat.** | Two gaps at the top of the pane. The page knew a turn was live and said so only at the BOTTOM, by swapping the composer's arrow for a stop square — which reads as "you can stop this", not "this is working", and is off screen for a reader who has scrolled up or who is watching the preview beside the chat. The mark hangs off the existing body class rather than a flag of its own: two sources of truth for one state is how the header comes to disagree with the composer. A SHIMMER through the word and not a spinner, because the strip is 43px tall and already carries a spark, a name, a file and an id — a rotating glyph beside all that reads as a fifth object rather than as a state of the fourth — and a gradient clipped to text adds no box at all. Reduced motion keeps the word and drops only the movement: the word is the information. **The filing verb needed no new identity**: the Tasks page keys a task by its session id (`task.key`), which is the same id this page already fetches `/api/tasks` for to learn a task's number. HIDDEN rather than disabled when the chat has no task — a disabled row asks the reader to work out what would enable it, and "this conversation has never been listed as a task" is not something they can act on from this menu. Read FRESH on every open, because the Tasks page can file the same task while this menu sits closed and a stale label offers the wrong verb, which files something the reader meant to unfile; a session swapped mid-flight is dropped for the same reason. The confirmation lands inside the item and the menu leaves on its own — the shape the terminal item already uses, since this page has no toast and inventing one here would be a second vocabulary for "it worked" — and it counts the cancelled runs, because archiving cancels the task's pending work as well as filing it and that is a fact about the schedule. **This reads `/api/tasks` and FAILS CLOSED**, unlike the task-number read beside it: that one costs a nicer label and falls back to the hash, where this one is an action, and an item offered on a listing we could not read is an item whose verb we are guessing at. |
| D454 | **The Tasks list's file mark wears the ROW'S cursor and sits against the title: no `cursor: help`, and the row's 10px flex gap pulled back to 4px on the title side.** | Both from the same misreading — that the mark is its own object in the row. It is not; it is something the title wears. The row is a link and reads as one under the pointer, so a question-mark cursor over one glyph inside it announces a different kind of thing to press and looks broken beside the row's own arrow — and a tooltip needs no cursor to advertise itself. The gap is the same error in layout: the row's `gap` applies between every pair of children, so the mark shipped with 10px on both sides plus a 5px margin of its own, floating midway between the title it describes and the folder chip it has nothing to do with. Pulled back on the title side only; the folder keeps the full gap, because that one IS a separate object. |
| D455 | **The recent-chats list marks a row whose turn is STILL GOING with the same shimmering "running" the top strip wears, replacing that row's timestamp; the filename reveal is eased with the fade trailing the width on the way in; and `.kebab-opt[hidden]` is given `display: none`.** | Three finishes on D452/D453. **The list had no live state at all**: its only clue was a timestamp reading "now", which is equally true of a chat that finished a second ago — so the one row worth opening, because something is happening in it, looked like every other row. The word and the shimmer are the top strip's, shared keyframes and all: it is the same fact seen from outside the chat rather than inside it, and a second drawing of one fact is a second thing to learn. It REPLACES the timestamp rather than joining it, because "running" already answers when. Liveness comes from `_live_sessions`, a sibling of `_live_run` that matches on the WORKDIR and answers for every row in ONE scan — the list spans a folder, and asking `_live_run` per row would re-read every run dir once per row. Both spellings of a run's session are collected for the reason `_live_run` documents (`resumed_from` and the CLI's own minted id), and the pid check stays last. **The reveal was linear and read as a snap**; it is now eased, and the fade trails the width entering so the name is never text in a box that has not finished opening, while the two run together leaving so it does not linger like a stuck tooltip. **And the hidden menu item was still taking its full height** — `.kebab-opt` sets `display: block`, which beats the UA sheet's `[hidden] { display: none }`, exactly the trap `#kebabpop[hidden]` above it already carried a fix for. On a chat with no task behind it that left a blank row under the one real option, reading as a menu still loading something. |
| D456 | **The chat page's "running" mark is sidebar.css's `.sidebar-running` PORTED — same `--status-progress` hue (the token added to both of the template's palettes), same 2.2s pace, same 300%-wide gradient travelling 100% → 0%, same clip box — and the strip's mark and the row's are ONE rule with two visibility switches.** | D453/D455 hand-rolled a shimmer instead of reusing the one the app already had, so the same word in two places was two different marks: a 1.6s accent-coloured sweep here, a 2.2s amber one in the sidebar beside a running task. Reused now, and the port carries two fixes the invented version did not have — both of them documented over there as bugs that had already been paid for once. **The travel has to stay inside 100% → 0%** across a background 300% the width of the box, because every position in that range still covers the box; the invented version ran 100% → -100% over a 200% image, which exposes bare box at the ends of the cycle, and under `background-clip: text` an unpainted spot is not a dim letter but a MISSING one. **And the clip box has to hold the glyph**: at `line-height: 1` the painted area is shorter than the type and the descender of the "g" in "running" is chipped off, so the leading and a hair of padding are load-bearing rather than cosmetic. `-webkit-text-fill-color` replaces `color: transparent` for the same reason it is used over there — the fill is what the clip paints through. The two marks share ONE declaration because they are one fact seen from outside the chat and from inside it; separate rules are how they came to drift in the first place. |
| D461 | **The AI Models card's DISK states go neutral and the LOADED card keeps its green: a downloaded repo wears a grey wash mixed toward `--fg-muted`, a model with nothing of it on this disk wears one mixed toward `--bg`, a resident one keeps the green wash it shipped with on top of its border and rail — and "Try" becomes the card's one accent-filled control.** | Three changes, one argument: the card had been spending its loudest colour on its quietest fact. D436 gave a complete repo the same green wash a LOADED one wears and set the two apart by a border and a rail, which fixed the shade-judging problem it was aimed at and left a worse one — in a capability row where almost every card is downloaded, almost every card was green, so the page's live colour marked the ordinary case and the one model actually spending memory had to be picked out of a row of near-identical plates. Green now marks ONLY that state — it came off the ordinary card, not off the loud one. **A first pass took it off both surfaces** and left the resident card wearing a 1px border and a 3px rail as its whole distinction, which inverted the very complaint D436 was built for: the loudest state on the page became the hardest to spot, since its surface was then identical to the eight cards beside it. So a loaded card sits OUTSIDE the grey axis rather than at a further step along it — "this is running" is a different KIND of fact from "how much of this is here" — and the two differ in every channel there is: surface, border, rail. **The earlier rule that downloaded and loaded may differ only in border and gutter is deliberately dropped**; it existed to stop a reader judging two shades of one green, and there is no such judgement left once one card is green and the other is grey. **What replaces it is an AXIS rather than a colour**: `--fg-muted` for have, `--bg` for not-have, the plain card between them, so the states are ordered by weight and a reader sweeping the row gets have/not-have without reading a word or naming a hue. Mixed toward TOKENS rather than lightened by hand because the direction has to survive both palettes — toward the ink is brighter on dark and darker on light, and toward the page recedes in both, where any hand-picked pair of greys would be right in one theme and inverted in the other. The faded end is a `background` and deliberately NOT `opacity`: these are the cards carrying this page's one primary action, and dimming the card would dim Download with it, which reads as a control that has been refused rather than as a model nobody has fetched. It rides the two card kinds that can be not-downloaded — a recommendation, which is by construction a model the merge found no disk answer for, and a Hub result, which is the one list where all three disk answers sit side by side. Everything stays background-only, so a card changing state still never reflows the carousel (D436). **And "Try" fills with accent**, against the house rule that accent is for focus and selection and a primary button fills with `--fg` (`.btn-primary`): the card's other three controls are quiet `.cc-iconbtn` glyphs, and the one thing a reader opened the card to do had the same weight as Explore beside it. `--on-accent` for the ink, since the accent flips from bright lime to dark olive between themes and the readable ink flips with it; a doubled selector for the hover, because `.cc-iconbtn:hover` is the same specificity and would otherwise decide the fill by import order, painting a translucent wash and `--fg` text over the plate; and a `--fg` focus ring, because `.cc-iconbtn:focus-visible` draws it in accent and an accent ring on an accent plate is the keyboard user's only indicator, gone. **Rejected: a second green for downloaded** — that is the shade judgement D436 already refused, and it keeps the collision this decision exists to remove. **Rejected: taking green off the loaded card too** — tried first, described above, and it is what this decision's second pass undid. **Rejected: filling every card control** — one filled control is a next step, two are a choice with no answer. |
| D462 | **The dock's finished-row retention drops from 30s to `FINISHED_TTL_S = 3.0` (`fused_render/jobs.py`), and the client's `pollInterval` (jobs.ts) gains a `GRACE_MS` window that holds the 1s ACTIVE poll cadence for a while after the last running job disappears, instead of dropping straight to the 5s idle cadence.** | The corner exists to answer "is my work done", not to be read as a log, and 30s of a finished row sitting there read as the opposite of "done" to a user watching it. The TTL alone cannot deliver that: cut it without touching the client and the 5s idle poll (which kicks in the instant nothing is running any more) either misses the row's brief window entirely or sweeps it after a ragged 3-6s depending on poll phase — and a `foldedJobRows` regression (Akshil, 2026-08-21) already showed what a too-fast disappearance without a lagging poll costs: a report of unattended work vanishing before anyone read it. `GRACE_MS` (6000ms) is chosen to comfortably outlive `FINISHED_TTL_S` plus a poll interval so the client is still on the 1s cadence when the server-side sweep actually takes the row; the two constants are cross-referenced in comments on both sides so a future change to either prompts a check of the other. `pollInterval` stays a pure function (elapsed time passed in, no `Date.now()` of its own) so its tests do not need a fake clock; `DownloadManager`'s `useJobs` hook owns tracking when it last saw a running job. |
| D463 | **`SupervisorState._start_resident`'s eviction branch releases `_lock` before calling the ~9s `_terminate` on the model being evicted, instead of holding the lock for the whole call.** | `_lock` is an `RLock`, so the prior code was not a deadlock — it was a latency bug: every other supervisor call (`describe`, `ready_worker`, `resident_models`, health polls, every other AI request) queued behind one eviction for as long as `_terminate` took (a 2s `/quit`, SIGTERM + 3s wait, SIGKILL + 3s wait, `proc.wait` + 1s — worst case ~9s), which is why a benchmark "Run all" appeared to freeze specifically on its first model: only the first model's load has to evict a previously-resident one, so models 2..N never hit this path. The fix keeps all the bookkeeping that decides identity (marking `stopping`, removing the old worker from `_workers`) inside the lock, and calls `_terminate` only after releasing it — `_terminate` still re-acquires `_lock` itself to discard the worker's token, which keeps working unchanged. The new worker is not published into `_workers[capability]` until the old one has already been removed from it, in the SAME critical section as the removal, so there is never a window where two resident workers exist for one capability — on 16GB of unified memory that overlap is a load becoming a swap storm, which is the reason the eviction branch exists at all. |
| D469 | **`FINISHED_TTL_S`'s retention clock starts at first READ (`Job.first_read_at`, set by `list_jobs`), not at completion — a terminal row unread by anyone is bounded instead by a new `FINISHED_UNREAD_DROP_S` (10 min) backstop, and `error` stays exempt from both exactly as before.** | D462's 30s→3s cut opened a hole: a job that starts AND finishes entirely server-side (a scheduled run, or a Python reporter POSTing straight to `/api/jobs` with no `fused.trackJob()` handle) runs no JS, so it never calls `pingJobs()` — the one thing that nudges the shell's poll out of `POLL_IDLE_MS` (5s). A flat clock from `finished_at` could then expire the row entirely between two idle polls, so anything under roughly `POLL_IDLE_MS` of work could be born and swept with NOBODY ever having seen it — exactly the guarantee BG-6's "noticed by someone who was not watching the corner" sentence exists to give, and exactly what the 30s→3s cut had quietly dropped. Gating the clock on `list_jobs` (the same call the dock's poll and `fused.watchJob` both make) instead of on completion guarantees every reader who DOES look gets a full `FINISHED_TTL_S` from the moment they could first have seen the row. **The ordering in `list_jobs` is load-bearing**: `_sweep` runs against `first_read_at` values from EARLIER calls, and only afterward does this call mark newly-terminal rows as read — so a row can never be swept in the same call that first reveals it. **The unread backstop is a different rule from `MAX_JOBS`**, which is capacity pressure and only bites at 64 rows; a single unread row well under the cap would otherwise sit in `_jobs` for the life of a headless process. **Known and accepted limitation**: the clock starts on the first read by ANY client, not per-client, so a second, slower reader can still find an already-swept row — a per-client read log would be a much larger feature for a case this rare. **Rejected: a shorter fixed TTL from `finished_at`** — shrinks the never-read window without closing it; a headless job finishing in under the new TTL is still invisible. **Rejected: dropping `POLL_IDLE_MS` to ~2s** — turns a rare server-only-job case into a permanent background request every 2s for the life of every session, to buy what a read gate gives for free. |

### Python client for fused.ai (2026-08-23)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D470 | **A stdlib-only Python client for `fused.ai` ships as `fused_render/templates/shared/fused_ai.py`, mirroring `runtime.js`'s `fused.ai` surface 1:1 (names and option names) — `text`/`stream`/`transcribe`/`image`/`embed`/`models.{list,catalog,load,download,unload}`/`cancel` — with one deliberate difference: job-backed calls (`transcribe`, `image`, `models.load`/`download`) block by default (`wait=True`), polling `GET /api/jobs` until the row is terminal before returning, because a Python caller has a thread to spend where a page has a promise. `wait=False`/`on_progress=`/`timeout=` are provided for a caller that wants the immediate reply and its own loop. `ServerNotRunning` is a distinct exception from `AiError` (which carries `type`/`message`/`status` off the `{ok, error:{type, message}}` wire shape) because "the app is not running" and "the call failed" want different caller responses.** | Every Python process wanting local inference today reinvents the model layer from scratch — a hardcoded model-id ladder, a hand-rolled resident-process manager, a scratch-dir progress protocol for a detached worker, its own cancellation plumbing — none of which is a new problem, since the server already answers all of it over HTTP. Stdlib-only (`json`/`os`/`socket`/`time`/`urllib`) and no `fused_render` import is not a style choice but SPEC PY-15's own rule restated: a `.py` data file runs in a subprocess with `PYTHONPATH` stripped, so this module imports its sibling `appenv.py` for the shell-home-dir contract instead of re-deriving it, and the two are documented as travelling together whenever either is vendored. **A `stalled` job (the reporter died, `jobs.py::is_stalled`) raises immediately rather than being polled out to the registry's own ten-minute eviction (`STALE_DROP_S`)** — a caller blocking on a dead reporter for up to ten minutes is a hang this module can detect and refuses to reproduce. **The request envelope is not re-validated client-side**: an option the server does not accept is the server's own 400 to raise (D413 stands); the module's `_IMAGE_WIRE_KEYS`/`_TRANSCRIBE_WIRE_KEYS` constants exist only so a test can pin them against the server's `_IMAGE_OPTIONS`/`_TRANSCRIBE_OPTIONS` (`routers/ai_runtime.py`), the same drift guard `runtime.js` already has against the same constants — three copies of one fact is exactly how they drift, and the test is what keeps them one. **Rejected: a `fused_render/client/` package with a pip-installable re-export.** It would drag ~150MB of fastapi/duckdb/pyarrow into a caller that wants a 300-line HTTP shim — the whole point of the stdlib-only module is that it does not need the package at all. **Rejected: relative-path support via a `base` parameter mirroring the server's own.** `base` names a calling *page's* own path on the JS side; this module has no page, and `_child.py` already chdirs a running `.py` to its own directory (RH-1), so `transcribe()`/`image()` instead resolve a relative `path`/`image` to an absolute one locally with `os.path.abspath()` — the same "beside this file" meaning with no third parameter to explain. |
| D471 | **Both execution engines seed `fused_render/templates/shared` onto a user module's own `sys.path`, APPENDED (never inserted at `[0]`) so a user's own same-named module still wins — `_child.py`'s worker (`sys.path.insert(0, module_dir)` → append the shared dir right after) and `engine.py`'s generated wrapper (`_fused_sys.path.insert(0, script_dir)` → append `_shared_templates_dir()` right after), changed in the same commit.** | `import fused_ai` (D470) has to work from a user `.py` anywhere on disk, not just from inside `templates/` — existing templates reach `templates/shared/` with a relative `sys.path.insert` that only works because they physically live inside `templates/`. `_child.py`'s own module docstring already documents the exact trap a one-sided fix would fall into: PY-6a's retired PYTHONPATH injection worked under the built-in executor and silently took its ImportError fallback under the fused engine (which strips `PYTHONPATH`/`PYTHONHOME`/`VIRTUAL_ENV` from its children for venv hermeticity), and nobody noticed until a caller happened to run under the other engine. So the fix has to land in both files in one change, and each derives the shared dir from ITS OWN file's location (`fused_render/templates/shared` as a fixed sibling of `_child.py` and of `engine.py`), never from an env var: `_child.py` is invoked as a standalone script with no guarantee of inheriting the server's environment, and `engine.py` bakes a literal path into generated source that a subprocess with a stripped environment still has to resolve correctly. Proven with a real subprocess run through `executor.run_python` (not just an exec'd string), because a fake stand-in for the worker would not have caught PY-6a's original bug either. |
| D472 | **The server publishes `<shell home dir>/server.json` (`{origin, pid, shared, version, started}`) at bind time — the same lifecycle point as `set_server_origin_env`/`export_app_env`, before it starts serving — and removes it on ordinary shutdown; the write is best-effort and swallows `OSError`, and a stale file left by a crashed server is the CLIENT's problem (a connect probe before trusting it), never a heartbeat this side maintains.** | `fused_ai.py` (D470) needs an origin to call, and `FUSED_RENDER_ORIGIN` only reaches a process the server itself spawned. A user-launched app (the desktop-supervisor case this whole file exists for) inherits nothing and cannot compute the port itself — the desktop launcher auto-picks a free one, so `_branch.branch_port()`'s baseline default is only right for a bare `fused-render` run with no override. Written into `shell.storage.home_dir()`, which is already branch-resolved, so parallel per-branch dev servers write to their own file and never collide. **Best-effort is load-bearing, not a nicety**: this runs before the socket bind, so a raised exception here would be a server that fails to start over a file it does not strictly need to serve, and the desktop supervisor reads a slow start as a failed one. **Rejected: a heartbeat / liveness re-write on an interval.** Staleness is cheap to detect from the client side with one TCP connect attempt against the recorded origin, and a background timer is one more thing to shut down cleanly (and one more thing that does not run at all on the packaged app's `os._exit` exit path, which skips FastAPI shutdown events entirely — the connect-probe design degrades gracefully there where a heartbeat-freshness check would not: an `os._exit` leaves the file's last-written timestamp permanently stale, and code trusting recency over reachability would refuse a server that is, in fact, still running). |
### Playground result canvas (2026-08-24)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D473 | **Every Playground stage draws its result box BEFORE it has a result — one `ResultSlot` (`playground/controls.tsx`), rendered in the same JSX position the filled answer block occupies and under the same heading ("Result" / "Response" / "Transcript" / "Ranked by meaning"), dashed and 200px tall on all five stages rather than aspect-locked to the render it will hold.** | The tab's own layout made the gap: `.pg-apps` is pinned to the stage's bottom with `margin-top: auto` (deliberate — a short stage keeps the app strip on screen), so a stage that had not run yet showed a prompt, four sample pills, and then ~400px of nothing above "Use it in an app". Read as a page that failed to finish loading, not as a page waiting for input, which is the single loudest unfinished signal on the tab. The slot is what turns that void into the shape the answer arrives in, and every pixel it takes is a pixel taken OUT of the void rather than added to the column — `auto` was absorbing it either way. **Same JSX slot, not a sibling elsewhere in the child order** (`{run ? block : slot}`), so idle and filled are one box in two states and the first Generate does not reshuffle the column. **One height for all five, deliberately NOT the aspect-locked frame** the image and video renders fill: locking to the live `w`/`h` would avoid the first render's height change, but a 9:16 idle frame is ~1.8 column widths of empty dashed box — trading this void for a taller one, and the shift on first Generate reads as the frame becoming the picture. **Dashed with no background**, because `.pg-answer` and `.pg-image-frame` are the boxes with something in them; a solid card at this size reads as a real surface that failed to fill. **The capability's own sidebar glyph** (`capabilityIcon`, upsized from its 16px section-header size by CSS) rather than a new mark, so the empty state names its stage in the same vocabulary the rail does. **The embed stage's slot also covers `busy`**: that stage drops its ranking while a re-search runs so a stale order is never read as the new one, which without the slot left the column empty at exactly the moment something was happening. **Rejected: a skeleton or shimmer.** Nothing is loading — the stage is waiting on the reader, and a fake row animating in an idle stage promises a result nobody asked for yet. **Rejected: shortening the gap by dropping `.pg-apps`'s `margin-top: auto`.** That pin is a documented decision about a short stage keeping the strip on screen; the gap was the missing result canvas, not the strip's position. |
| D474 | **Every caption on the Tasks page is ONE instant tooltip positioned at the POINTER (`platform/lib/hints.ts`, opted into with `data-hint`), and it captions the element whose ink it explains rather than the row: the TITLE carries the task's name, the message BODY carries the message, and each mark (scope glyph, folder chip, count, both times) carries the one fact its own ink cannot show. Hit boxes reach the row's full height via `align-self: stretch` plus the row's own `--tasks-row-pad-y` with matching negative margins; the folder chip keeps its own size and is wrapped in a `.schedule-tv-id-shield` that owns the band around it and does NOTHING with it. The folder glyph moves off the chip onto the title's trailing slot, drawn only when there is no file glyph; the Project filter's trigger wears the folder too.** | Six screenshots over one day, and the tooltip alone took three mechanisms to get right — each one correct about the thing the last was wrong about. (1) *A native `title`* is placed perfectly, because the browser draws it at the cursor: it cannot caption the wrong row and cannot be clipped. Its delay is the browser's and unreachable from CSS — measured on this list, four to five seconds on a session's FIRST hover, which is the same as not offering it. (2) *A CSS `::before` panel* is instant and unplaceable: absolutely positioned against its own element, so on the list's first row it opened over the row below ("it is displaced completely from the title") and on the last cell of a row it hung outside the scroller. It also needed four support rules to survive the row at all — a z-index lift over later siblings, a right-edge anchor, a `:has()` so a mark's caption and the row's did not draw six pixels apart, and a fast-delay list. (3) *A pointer-following panel* has neither problem, and needs none of the four: one fixed-position element on `<body>` moved to the cursor, shown with no delay. `position: fixed` puts it outside every `overflow`; the cursor is by definition where the reader is looking, so it cannot caption the wrong row; and it flips rather than clamps at a viewport edge, because clamping would slide it under the cursor and cover the ink it explains. **The resolver reads `elementsFromPoint`, not `event.target`**, and that is load-bearing: the row's navigation is an `<a>` stretched over all of it, so the pointer lands on the LINK and never on the title beneath — and lifting the title above the link would take the row's click with it. An EMPTY `data-hint` stops the walk and answers nothing, which is the opt-out a dead band needs inside a region whose parent has a caption. **The caption belongs to the ink, not the row** (the last screenshot: "the tooltip of title should only show up if I am on title text"): on the row it answered for the id chip, the status ring, the empty space and every band around every mark, because the resolver walks up to whatever the row is holding. The title and the message body are the two elements here that ELLIPSISE, so they are the two whose full text a reader can actually be missing. **The hit boxes and the shield are a separate fix with a separate history.** 11–16px of ink on a 36px row is a target a pointer misses above and below, and the row answers for the miss — with the task's name, and on the chip with a click that OPENED the task, which is the worst way to miss a filter. `align-self: stretch` plus the row's own padding token is exact where a guessed 7px left ~4px strips (the marks are three different heights). Two passes then tried to give the CHIP the same treatment by growing the button, and both failed identically: `border-radius` draws on the padding box, so the target and the hover wash grew together into a slab around an 11px word ("that is such a bad design and hover — keep the tag/button size same"). So the chip's two jobs went to two elements: the shield takes the band, sits above the stretched link, carries `data-hint=""` and the ordinary cursor, and does nothing; the button is back to `padding: 3px 5px` and is the whole live target. The band is a `::after` rather than padding on the shield, which is what keeps the pill centred — padding plus the negative margin that pays for it left the pill 3px high, because `align-items: center` was then centring it in the shield's content box rather than the row's — and `z-index: -1` keeps it behind the pill so the pill keeps its own hover and press. **The folder glyph moved rather than being deleted**: on the chip it restated the one thing a folder's name cannot be mistaken about, on the busiest end of every row; after the title it answers a question the row otherwise cannot — whole folder, or one document in it — as a ternary with the file glyph, never both. The Project filter's ring went the same way: a ring beside the word Project claims a STATUS is being filtered, which is what the ring means everywhere else on this page. |
| D475 | **On the Models tab: every engine tag is a PILL in every state (tinted border + 10% wash from the same token as its ink, dash retained as the state channel); "Try" is OUTLINED in accent rather than filled; a refused Load prints a ONE-LINE reason on the card with the Engines tab as a link; a load in flight shows an indeterminate bar and its cancel ✕ lives in the progress row, not the actions strip; and `CAPABILITY_ORDER` is ONE shared list read by all three tabs with no private copy anywhere — originally the Playground's own order (image, text, transcription, embeddings, video), reordered 2026-08-24 to text, image, transcription, embeddings, video (text generation promoted to lead, everything else keeping its prior relative order) because text generation is the capability a reader arrives at this app FOR, not the second thing after image generation — the principle this decision settled (one list, no per-tab copies) is unchanged by which entry leads it.** | Five reports on one page. *"Diffusers and transformer embeddings don't have a tag — I think that is the CSS issue."* They always had one; `.am-card-engine-off` set the ink and the dash and left `border-color` at the base class's `var(--border)`, and a dashed hairline in the border token at 11px over the card's own wash is invisible — while `-family` beside it tints its border AND carries a wash. So the loadable tags read as pills and the unavailable ones read as loose text, which is backwards: this is the state with something to say. *"Try button is bg accent, instead let's have it as outline accent."* It shipped filled the day before, the case being that "use this model now" is what a reader opened the card to do; what that missed is that Load sits immediately beside it and is the control that costs memory, so the filled plate made the cheap navigation the loudest mark and the consequential button the quiet one. Outlined, Try is still the only bordered control of the four, and the page stops excepting itself from the house rule (accent for focus/selection, `--fg` for a primary fill) — the forced `--fg` focus ring goes with the fill that needed it. *"Why can't I load some models — it is not clear why that button is disabled."* The reason existed and was good (`loadRefusal` writes four, one per problem) and lived in the `title` of a DISABLED button, which is the one control a pointer user has no reason to hover: the page held the answer and showed a greyed word. `loadRefusalShort` prints the first clause of the registry's reason, cut at its own comma or dash — the middle clause is what the dashed amber tag two lines up already says, and everything past the dash is the remedy the link now carries. The link is decided on `engine.available`, never by matching words in the prose, and the line is not drawn where the refused Load is not the button on the card (a partial repo's control is an enabled "Continue downloading"). *"The unload button has a ✕ when loading — ugly, shifts the layout; think of a better way, maybe show how much is loaded and a button to cancel."* Three faults in one position: `Unload` is the wrong verb mid-load (nothing is loaded yet), the ✕ was rendered conditionally in the actions strip so it grew and shrank the row — a 26px target that MOVES while being aimed at — and a load drew no bar at all, so the card read as stalled. Now the button says `Loading…` disabled, the track is drawn for both kinds of work with an indeterminate stripe when there is no total (a travelling `translateX`, composited, and still no fabricated percentage — the supervisor reports a STATE, never a fraction), and the ✕ trails the bar, where the row is only ever drawn while there IS work and therefore costs no layout when there is none. Stopping a load really works: `supervisor.unload()` filters by model with no state check, and the `state == "ready"` rule that reads like a bar on it is the idle REAPER's predicate — a different question, whether to unload a worker nobody asked about. *"Follow playground's list order into the models section."* The two lists disagreed, each with a defensible case (a catalogue reads differently from a set of things you can do), and the page they added up to put the same five sections in two orders. The tab a reader USES the models on wins, and the tabs that inventory them follow — so `CAPABILITY_ORDER` holds that order and `PlaygroundTab` keeps no copy. **Video is now LISTED**, which is the one substantive change rather than a re-shuffle: the Playground's copy named four capabilities and video was not among them, so it had been sorting last by accident on that tab all along. **Also: the AI Models sidebar dot joins `.sidebar-rail-dot`**, the class the Tasks row wears — it had `.account-signedin-dot`, which is 7px too and positions at `top: -2px; right: -3px`, landing outside the 28px rail button's corner instead of on the glyph, and is border-box against the other's content-box so its ring ate the fill down to 5px beside a 7px neighbour. One nav-dot class, a modifier per fact; `--success-bright` stays because "memory is held" and "work nobody has read" are two claims and the shared SHAPE is what had to match. **2026-08-24: text generation moved to lead the shared order.** The user's own instruction ("text generation goes first") is a promotion of one entry, not a re-litigation of the whole list or a reason to give any tab its own copy again — `CAPABILITY_ORDER` is still the one array all three tabs read, `PlaygroundTab` still keeps none of its own, and the reorder is visible everywhere on purpose: the Playground's rail and its default stage, the Models tab's first card section, and the Benchmark tab's first drawn capability all move together, because that is exactly what "one list" is for. **2026-08-25: image generation moved back above text**, on the owner's instruction, reversing the previous day's promotion — the two are a judgement call about the first impression rather than a fact one of them got wrong (text is what a reader arrives FOR; a picture is what SHOWS a local model doing something a text box cannot claim by looking), and the case for one list is untouched by which of them wins it. The swap is the top two only, everything below keeping its place, and it moves all three tabs plus the Playground's default stage together as before. It also reaches a FOURTH list the 2026-08-24 note did not mention: Home's strip is `PLAYGROUND_GROUPS` (playground/groups.ts), a separate array carrying the label and blurb a bare capability string cannot, whose HEAD is kept in step by hand — a front door that leads with a different capability than the tab it opens is the disagreement this decision exists to prevent. Its TAIL still deliberately differs (video before embeddings): only there does the order decide what a narrow window DROPS. |
| D476 | **The Benchmark section's Metric `<select>` loses its visible "Metric" caption (kept as `aria-label`), and the unit + direction cue moves INSIDE its own option text (`metricOptionLabel`) — "Speed (× realtime)", "Peak memory (lower is better)" — with the separate badge beside the select deleted. The Share glyph becomes the three-node share mark (one node left, two right, two edges) in place of the arrow-out-of-a-tray.** | The caption goes for the reason the page-level Capability select's already went — an option's own text names the control, so "Metric" standing in front of "Throughput" is a word saying nothing; the earlier decision to KEEP this one on the grounds that it sits beside a card's `<h3>` rather than alone in a toolbar was answering a consistency worry the reader does not actually have. **The badge then failed TWICE, on the same axis, and the second failure is the interesting one.** Trailing the select it was a third mark in a row already holding an `<h3>` and a Share button, restating what the control in front of it had just said. Moved LEFT into the slot the caption vacated — so the row would read unit-then-choice, which is the right reading order for a unit — it broke outright on transcription: `× realtime` is a multiplier SUFFIX and parses only when it trails a number ("1.4× realtime"), so alone in a bordered pill beside a control it stops being a unit and becomes a stray operator. On screen it read as the dismiss ✕ of a removable filter chip, which is a shape this app uses elsewhere for exactly that meaning (D448's attached ✕ on the Tasks filter pills) — a false affordance inviting a click that would do nothing, in the one spot where the row's real controls live. Inside the option the words have a subject again. It also retires the badge's other bad case without a special rule: `Peak memory` formats through the platform's byte formatter and carries no unit STRING at all, so the pill there drew a chip containing nothing but "lower is better". **The cue stays one-sided** (named only where a shorter bar wins) and is still said exactly once — the trend heading's old per-model-name-plus-unit pill is gone too. **Rejected: keeping the pill and only softening its chrome to plain text.** The `×` is what misreads, not the border; unbordered it is still an operator with nothing to operate on. **The glyph swap is a legibility fix, not a taste one**: the tray-plus-arrow was three near-parallel strokes and the deliberate MIRROR of `download`'s arrow-into-a-tray, which is exactly what made it unreadable in a 26px icon-only button — the two differ only in an arrowhead's direction at that size. Three round nodes stay three distinguishable shapes however small the button gets, and the mark is the one the OS share sheet this button opens already uses. `MenuIcons.share` had exactly one consumer, so the swap is the whole change. |
| D477 | **`/home`'s two async strips (Fused Apps, Claude Sessions) each render a `SkeletonCard` row in their OWN real card's shape — `.app-pcard` for Fused Apps (no head icon), `.fhb-card` for Claude Sessions — sized by `shown` (the measured fit-count, never a guess), in place of a bare "Loading…" line; a resolved-empty section gets a light padding treatment, not a card-height well; neither skeleton renders an icon placeholder, shimmering or otherwise; `LivePreview`, `FolderStack` and `AppPreviewCard`'s thumbs each shimmer-then-crossfade instead of painting a blank, half-booted, or permanently-stuck-on-error iframe/still; and `preview-start.ts`'s `NEAR_VIEWPORT_MARGIN` drops from `800px 0px` to `300px 0px`.** | The text-line loading states and the 800px margin were each sized for a different page than the one they ended up on, and the first attempt at a fix introduced its own new mismatches, caught in review and a real browser pass. A `<p>` reserved one line's height, then a row of cards landed under it — Fused Apps and Claude Sessions each did this twice (once for the fetch, again if it resolved to `[]`), so the sections below jumped down twice per load. The skeleton fixes that by reusing the REAL card's own classes rather than a hand-measured constant — an earlier version built one shared shape from `.fhb-card`'s geometry and used it for both strips, which is wrong for Fused Apps (`AppPreviewCard`/`.app-pcard`: no head icon, a full-bleed thumb, different padding) — so the row the fix most wanted to stop shifting still shifted. Reusing the real classes means the browser lays the skeleton out with the exact box model (padding, border, font metrics) the real card gets, tracking any future CSS change automatically. Neither variant renders an icon: Fused Apps cards have none, and Claude Sessions' folder glyph is a static decoration identical on every card, independent of the fetch — shimmering it (or even placing an inert placeholder) claimed something was loading that wasn't. The empty state's dashed, card-height well was an overcorrection in the other direction: fine for a transient loading state, but a `[]` result is permanent, and reserving a full card's worth of space for "Recent files" on a fresh install read as a giant broken box with one line of text adrift in the middle — worse than the shift it replaced. It reverted to a light padding treatment instead. Per-card blanks got the shimmer-crossfade treatment for the same reason the row did: `LivePreview`'s iframe used to paint its own white/booting frame the instant the scheduler admitted it, `FolderStack` showed nothing until `listDir` AND the subfolder probe both landed, and `AppPreviewCard`'s still/live thumb was `null` or a bare `<img>` until decode. The crossfades needed two more passes to actually work: every `onError` handler had to reveal the frame it was gating, not just free the scheduler slot — an errored iframe is still a PAINTED result (the app's own error page, exactly what a card peek showed before this feature), and `onError={settled}`/`onError={liveSettled}` alone left the paint flag permanently false, so the shimmer covered the error forever and (for `AppPreviewCard`) silently broke export-from-card too, since `data-capture-ready` reads that same flag. And the paint flags themselves needed a reset on remount: `loaded` (`LivePreview`) and a new `bodyPainted` (`AppPreviewCard`, split from the deliberately-one-way `bodyLive` the export capture depends on) both go back to false when their iframe is torn down and recreated by a scroll away and back, so the freshly-mounted, not-yet-loaded iframe doesn't inherit the previous mount's "painted" state and render at full opacity before it has painted anything. `AppPreviewCard` shimmers only when something is actually coming (`wantsLive`, or an authored still not yet decoded) — a card with no entry file at all keeps the plain empty thumb (D365) rather than shimmering forever — and the two-branch "has it painted" test reads `liveReady` for the still-plus-hover branch and `bodyPainted` for the plain-live-thumb branch, since testing the wrong one shimmered a still-thumbed card for the entire duration of every hover. `loading="lazy"` came off all three preview iframes (a spec error, not an implementation one): mounting is already gated by `useNearViewport`, so lazy adds nothing, and its heuristics read a 400%-wide, `scale(0.25)`-ed layout box and can defer past the point a `load` event ever fires — permanent shimmer, an invisible frame, a scheduler slot held to the 10s timeout. Two CSS specificity fights needed compound selectors rather than a bare shared class: `.home-skel-card:hover` tied `.fhb-card:hover`/`.app-pcard:hover` on specificity and lost to source order, and `.fhb-thumb.home-skel-body`'s shimmer background lost to the light-theme override of `.fhb-thumb` for the same reason — both fixed by compounding onto the real class (or the theme selector) so the win is independent of which stylesheet loads later. Last, `NEAR_VIEWPORT_MARGIN` is a lookahead in BOTH directions, and 800px was sized for a grid with about one row on screen; Home stacks four ~330px rows in one scroller, so it made nearly every card in every row "near" on first paint and queued a whole embed-shell iframe document (React boot + its own API calls) for rows the reader had not scrolled to. 300px is still roughly a row of lookahead — a preview is ready before its card crosses the edge — while a row two screens down now costs nothing until approached. |
| D479 | **The Benchmark section's "Run all N models" button (idle branch only) is re-chromed from `.cc-btn` (a small quiet filled-grey pill) to a dedicated `.am-bench-runall-btn`: 1px `--fg` border, full-opacity `--fg` text, transparent plate, and `MenuIcons.play` — the same triangle every row below already wears on its own Run button — leading the label. The gap below it also closes: `.am-bench-runall + .am-bench-rows` drops that list's own `--am-gap-head` to the 6px the rows use between each other, so the button reads as the rows' own control rather than a third instrument floating in a gap owned by neither the chart above nor the list below.** | The button used to sit, quiet and grey, in exactly the spot a reader's eye skips — the gap between the comparison chart and the leaderboard — with no visual tie to either. Outlining it in accent was the obvious fix and the wrong one: on this tab accent is already spoken for as DATA ink (the comparison chart's bars, each row's metric value, the selected row's ring), so an accent-outlined control between the chart and the selection would read as one more lime thing competing to be the current selection rather than a button. Filling it was rejected on D475's precedent, which this tab does not get to except itself from — accent for focus/selection, `--fg` for a primary fill, and a fill is reserved for the one truly consequential action that needs to out-shout a cheaper sibling beside it (Load, filled, next to Try, outlined). Run All has no cheaper sibling on this page, but it IS the single most expensive press available here — minutes of compute, N cold loads, several GB re-read from disk — so a filled plate would make the priciest action the loudest mark, which is the opposite of what D475 decided. `--fg`-outlined, transparent, full-opacity text lands where both rejections point: findable, not shouting. The glyph borrows the row's own vocabulary rather than inventing a second one — "run all" is the row's run act, N times over, so it wears the row's own triangle. **The gap fix is separate but travels with it**: `.am-bench-runall` and `.am-bench-rows` each carried their own `--am-gap-head`, so the button sat in the sum of two margins belonging to neither instrument. Only the margin BELOW the button collapses (to the rows' own 6px rhythm) — the full head-gap ABOVE it, away from the chart, is unchanged and still owed. |
| D480 | **The Share button's delivery receipt moves off the section head and onto the global toast (`pushToast`/`NotificationHost`, `platform/lib/toast.ts`): `ShareChartButton.tsx` deletes its inline note/error `<span>`s, the `note` state and its `NOTE_MS` timer, and pushes `{ msg, tone: "info" }` on success or `{ msg, tone: "error" }` on failure instead — the toast store already owns auto-dismiss. `shareOutcomeNote` (`shareCard.ts`) is reworded to name the DESTINATION rather than the format: "Chart shared", "Chart copied to the clipboard", "Chart saved to your Downloads folder" (`cancelled` still returns `""`, and that empty string is still not pushed as a toast). The now-single-child `.am-bench-share` wrapper and its `-note`/`-error` colour rules are deleted; `ShareChartButton` renders its `<button>` directly into `.am-bench-headtools`.** | Measured in the running app: the inline note lived in `.am-bench-share`, inside the right-aligned `.am-bench-headtools`, inside `.am-section-head` — so the instant a receipt appeared, that whole control group got pushed left and the Metric `<select>`'s left edge jumped from x≈1183 to x≈962, a ~220px shift that moved the button the reader had just pressed out from under their pointer. The same note also sat beside the section's own `<h3>`, where a sentence like "Copied as an image" reads as a second heading rather than a receipt for one click. Both faults are structural to rendering the receipt IN the header's layout flow, not fixable by rewording or restyling it in place. The toast renders at the app root, outside every row's flow, so nothing beside the button moves, and it is unambiguously a transient system message rather than a second title. **Rejected: keeping the inline note** — the change this decision replaces, and the one with the measured 220px shift above. **Rejected: a pointer-following hint (`platform/lib/hints.ts`, D474's mechanism).** That is a hover caption for ink already on screen — it explains a mark the reader is looking AT — not a receipt for an action that already finished and whose result is no longer on screen to hover; there is nothing left under the cursor once the OS share sheet, the clipboard, or the download has already happened. **Rejected: swapping the glyph for a checkmark on success.** Zero layout cost, but it cannot say WHERE the card went, which is the entire reason this receipt exists (`shareOutcomeNote`'s own doc comment: a bare "done" leaves a reader hunting for a file that is actually on their clipboard) — three delivery channels collapse to one indistinguishable glyph. The strings themselves follow the house toast convention already in `mountHealth.ts` (`"${e.name} disconnected"` / `"${e.name} reconnected"`): sentence case, no trailing period, a completed-action past tense. |
| D481 | **The Benchmark tab's leaderboard row is the WHOLE accordion header, not a small control inside it. Clicking anywhere on a row (model name, headline, empty space — everywhere except the Run button, which is carved out below) toggles it open or closed; the `›` chevron is a plain `aria-hidden` state INDICATOR with no handler, no `tabIndex`, and no focus stop of its own, living in its OWN LEADING grid column (fixed-width, reserved on every row whether or not it draws a glyph) so every row's indicator sits at the same x instead of drifting with the length of the headline/delta text that used to precede it — and never the trailing column, which the Run `▷` button already owns. The rotation keys off `.am-bench-row.selected` alone. The row's one real focusable control — carrying `aria-expanded`/`aria-controls` — is its model-name cell, rendered as a `<button>` only when the row is expandable (`row !== null`; a never-benchmarked row gets a plain, inert `<div>` there instead, no chevron glyph, no pointer cursor, no aria-expanded, and a click that does nothing — though its leading column still reserves its width, so its name stays aligned with every other row's). Toggling is backed by a real third `?benchModel=` state (`resolveModel`, lib/benchmark.ts): `""` means "explicitly closed" and survives a reload, `null` still means "no opinion yet, open the default." The Run button opens a row but can never close it (`onOpenModel`, plus `stopPropagation` on its own click so it cannot also reach the row's toggle). An open row and its expansion (detail text, then `trendKind`'s three branches) are drawn as ONE continuous card: the row's own bottom corners square off and its bottom border drops when `.selected`, and `.am-bench-rowexpand` supplies the matching side/bottom border, the matching bottom radius, the same accent wash, and a negative `margin-top` that cancels `.am-bench-rows`' own flex `gap` — no seam, no floating block under a header. A row's hover hint (a plain border tint) and its open state (the accent border/wash) are kept from fighting over the same property via `:not(.selected)` on the hover rule, since the two have equal-weight class selectors and the later one in source order does not automatically win on specificity ties by property.** | Five passes to land here, each fixing a way the last one still fell short. (1) A separate `<details>` beside a plain-select row — two independent expanders on one row. (2) Merged into a single chevron `<button>` with `aria-expanded` — one control, but selection was a plain assignment with no way back to closed, so an already-open row's `aria-expanded="true"` was a lie a reader could not undo. (3) Fixed closing, but left the chevron — a 16px glyph — as the ONLY thing on a 36px-tall row that actually opened it, with the row's own click doing the identical job right beside it and giving no visual hint that it did ("what's the point of a tiny target next to a big one that does the same thing"). (4) The direction was explicit — "let's make the entire row have a dropdown of sorts" — so the row itself became the header, the chevron was demoted to an indicator, and the model-name cell picked up the one focusable disclosure contract a `<button>`-in-`<button>` rule (the Run button already lives in this row) rules out putting on the row element itself. (5) That pass still left the chevron sitting AFTER the headline and delta, inside the row's flexible middle column — "the chevron being in the middle looks odd" — so its x drifted with every row's own text length, which is not what a column is for. Moving it to a fixed, always-reserved LEADING column (not trailing — Run already owns that end) fixed the last of it: one indicator, one x, for every row, drawn or not. **Rejected across all five passes: moving the trend ABOVE the rows** (same full-screen disconnect, inverted), **`scrollIntoView` on selection** (fights the reader, loses their place), **an un-collapsed `<details>`** (native semantics with no way to drive "closed" explicitly from outside), **the small chevron-button-as-sole-target design pass 3 shipped** (made a 16px glyph the only reliable way to open a 36px row), and **an `auto`-width leading column** (collapses to zero on an empty, non-expandable row and pulls that row's name left of its neighbours' — the fixed width is what keeps the column, and therefore the name column after it, aligned everywhere). |
| D482 | **The Benchmark tab's `.am-bench` panel raises its cap from 1000px to 1320px at `min-width: 1440px`, but the comparison chart (`.am-bench-compare`) keeps its OWN separate, unraised 900px cap regardless of that outer width — the freed space becomes blank trailing margin beside the chart, not extra bar length.** | The original 1000px comment ("a wide measure would put a model name and its throughput at opposite ends of the window") is still correct, but it was never really about the whole tab — it is about the ONE instrument that actually pairs a fixed name against a value that can drift: the comparison chart's bar plot, whose name column is `flex: 0 0 26ch` (fixed) while its plot area is `flex: 1 1 auto` (stretches), so every extra pixel of width lengthens every bar and drags the value label after it further from the name. The leaderboard rows (`.am-bench-row`'s `16px minmax(0, 30ch) minmax(0, 1fr) auto` grid) do NOT have this problem — the name and headline-value columns are adjacent grid tracks with a fixed 12px gap between them no matter the row's total width, so widening only grows blank space before the Run button, never separates a name from its own value. Neither do the per-model trend chart or the run archive table, both of which read MORE easily with more room (a longer time axis, less column truncation) and carry no name-vs-drifting-value pairing to break. So the fix is not "never widen" or "widen everything" but exactly the shape asked for: cap the one column that has the problem, and let a trailing spacer absorb the surplus elsewhere. The cap had to go on `.am-bench-compare` as a WHOLE, not just `.am-bench-compare-body` — `.am-bench-compare-foot`'s tick labels are positioned by percentage relative to their own element's width, a sibling block of `-body` under the same parent, and capping only `-body` would leave the foot's axis row wider than the plot above it, misaligning every gridline from the tick that names it. `1440px` is the trigger because it is roughly where real 13"-16" laptops and small external monitors stop being the majority case and an unscaled 24"+ desktop display becomes the reader — below that, 1000px was already a comfortable fit and raising it would just be page filler. `1320px` is the new ceiling: a real, noticeable +32% of extra room for the trend chart and the archive table, short of the width at which even those start reading as an uncomfortably long line, and short of `1440px` itself so the panel never touches the viewport's own edges. **Rejected: raising `.am-bench`'s cap alone, with no separate cap on the comparison chart.** That is precisely reopening the concern the original 1000px comment states — it would make the exact same rationale false at a wide viewport rather than honouring it. **Rejected: capping the leaderboard rows and the trend chart/archive too, out of caution.** Neither has the fixed-column-vs-stretching-column shape that causes the problem, so capping them would just be discarding real, harmless readability room for no reason tied to the actual rationale. |
| D483 | **The Benchmark tab says what a run actually measures — server fact, not frontend prose — behind an INFO GLYPH beside each capability's heading, not a visible line under it. `/api/ai/benchmark`'s response carries `workloads` (capability → `{name, revision, params}`, built by the identical `Workload.as_dict()` a run record's own `workload` block uses), and `workloadNote(capability, workload)` (lib/benchmark.ts, unchanged by the glyph move — still the one place the sentence is built, still what `tests/test_ai_benchmark_workload_note.py` greps) turns those params into one sentence per capability. That sentence is now the `data-hint` (`platform/lib/hints.ts`, D474) on a real `<button>` (`MenuIcons.info`) placed beside the `<h3>` inside `.am-section-head`, wrapped with it in one `.am-bench-section-heading` group so `.am-section-head`'s own `justify-content: space-between` still only ever sees two children — the heading end and the controls end — never a bare third one splitting that space three ways. `aria-label="What this benchmark measures"` names the control; the hint text carries the answer, identical to the line it replaced, workload name and revision included. Rendered only when `workloadNote` returns something — a capability with no workload (`NO_WORKLOAD_YET`, video generation today) gets no glyph pointing at nothing.** | The first version put the sentence directly under the heading as a visible `.am-group-note` line, and that placement was deliberate at the time — this data was reference material, always-present, and a disclosure was explicitly rejected because "this data has no ink to hover." The user's own follow-up reopened exactly that premise: an info glyph, added on purpose, IS ink — a control with a job, not a caption pretending to have one — so the objection that blocked a tooltip earlier no longer applies to a REAL control. What did NOT change is the two things that made a tooltip suspect in the first place, and both are still honoured. **No native `title`.** D474 measured the browser's own delay at four to five seconds on a session's first hover — reading as "no tooltip" to whoever waited on it — which is why this tab already has its own instant, pointer-anchored hint system rather than reaching for the native attribute a second time. **It must survive without a pointer.** `hints.ts`'s focus path (`onFocus`, anchored to the ELEMENT via `getBoundingClientRect` rather than a cursor that is not there) only fires for something that can actually receive focus, which is the entire reason this is a real `<button type="button">` and not a `<span data-hint>` — a `<span>` would carry the hint but not the Tab stop, reintroducing the exact "a keyboard cannot reach this" gap `hints.ts`'s own focus path exists to close. Placed on the HEADING's side of `.am-section-head`, not inside `.am-bench-headtools` with the Metric select and Share, because it explains the SECTION as a whole — what a Run press on ANY row here measures — not those two controls specifically, and that group is already the row's busy end; a third control folded in there would make the reader guess which of three things it explains. **Rejected: leaving the glyph a bare, unfocusable icon.** That is the tooltip-delay problem restated in a different shape — instant for a mouse, unreachable for a keyboard — and the whole reason D474 built a FOCUS path in the first place was so a control whose only explanation is its hint is not a mouse-only feature. |
| D484 | **The git template's failed-operation "Explain" button is deleted and replaced with "Fix with AI", which hands the error off to the Claude sidebar instead of asking this view's own `fused.ai` for advice text — `adviseOnError`/`ADVISE_SYSTEM` are deleted outright, and conflict resolution (`resolveWithAI`, GT-19's primary case) is untouched. The hand-off is a PULL, not a push: the prompt is never a URL param on the claude iframe's src, only in-memory host state (`window._fusedClaudeAsk` sets it and switches the sidebar/content pane to claude; `window._fusedClaudeAskTake`, called by the claude template at its own boot through a new ancestor hop, `pullClaudeAsk`/`_fusedTakeClaudeAsk` in static/runtime.js, reads and clears it in one step) — and the host only ever STORES a prompt once it has confirmed, from that render's own gate state, that claude is actually about to be shown (`claudeEntryReady`/`resolveClaudeAskRoute`, frontend/src/apps/explorer/lib/claude-ask.ts), routing to the file sidebar's `_side` split when one exists and to the plain content-mode switch (`setMode`) when it does not — a directory opened at `?_mode=git` as Preview's own main body has no sidebar at all, and used to have no AI help there either.** | Landed across three code-review rounds on the same PR, each round catching a real defect the last missed, which is itself the reason this is recorded rather than left to the diff: (1) the FIRST shape pushed the prompt onto the claude iframe's `src` as a `_fused_ask` param, kept "one-shot" by a shell-side cache — closing over the fact that a `src` is an ADDRESS, and "follow this part of the address only the first time" cannot be expressed by a URL no matter how the cache around it is shaped; toggling the sidebar away and back, or closing and reopening the folder pane, rebuilt the identical src and replayed a stale git error into a brand-new agent run with write access to the repository. (2) Making that cache idempotent-per-key (same key -> same cached src, so an unrelated re-render would not thrash the mounted iframe) fixed the replay-on-navigation case but not the replay-on-remount case, because idempotent-per-key means the SAME key's answer survives forever, including into a remount nothing about the ask caused. The fix that actually closes it is the pull: consumption happens in the one frame that will use the text, at the one moment (its own boot) that can matter, so there is no src string and no cache left to reconstruct a stale answer from. (3) The return value of the ancestor hop (`noteAskClaude`) used to mean "a `_fusedClaudeAsk` callback exists somewhere in the chain" — true on every framed template regardless of whether anyone could actually show claude — so a gate-denied or merely-pending target reported "delivered" and the button did nothing visible; and a directory rendered at `?_mode=git` (no sidebar, `splitCapable` false) had no route to claude at all, a real regression against the advice-only `adviseOnError` it replaced, which needed no host and worked everywhere. Both are now one honesty check, checked BEFORE anything is stored: `claudeEntryReady` reads a pending gate as "not ready" (never optimistically stores a seed nothing is about to pull) and `resolveClaudeAskRoute` answers `null` — reported all the way back through `noteAskClaude` as `false` — whenever neither the sidebar nor the content pane can show claude right now. **Rejected: reinstalling the ancestor-hook effect on every dependency that could go stale** (`split.defaultSide`, `paneSides`) — both resolve asynchronously from the companion gates and can change without the effect's own trigger (`splitCapable`/`paneEnabled`) doing so, which was its own latent bug (a click writing the wrong `_side` spelling, invisible until a reload or a bookmark). The fix is a ref updated on every render, so the installed window export is a stable wrapper that reads current state at CALL time and never needs reinstalling to stay honest.** Landed via the PR's own AI-driven code review, three rounds against the same diff. |
| D485 | **The AI Models card states what a reader gets by SWEEPING the grid and puts everything else behind an `(i)`; all three card kinds share one skeleton (name, seal, `(i)` / size + repo id / actions); the engine tag, its per-family hue table and the revision UI are deleted; and a refused Load says `Switch engines` rather than naming the configuration.** | The face carried nine facts and six were IDENTITY — engine, task, parameters, quantization, weight format, the repo owner — each read once, by somebody who has already stopped at one card, while the grid itself is swept for three things that are STATE (here / loaded / arriving) plus what it costs. So identity moved into a `position: fixed` panel that cannot resize the card: these sit in a horizontal carousel, and anything opening INSIDE one shoves every card to the right of it mid-scroll. The task label went entirely, because it repeated the section heading the card is filed under. **The engine hue went with the tag it coloured**: `engineHue` mapped each family to one of the calendar's categorical colours so a row could be swept for all the MLX LM ones (D436), and inside a panel opened deliberately about ONE model there is nothing to tell apart — so the table is deleted rather than left unused, since naming its eight assignments was the whole point of it. **What the `-off` tag was also doing survives**, because that half was never identity: it was the only thing on the card explaining a greyed Load, and it is now an amber `Switch engines` link that navigates to the Engines tab, carries the registry's full sentence as an INSTANT hint (`platform/lib/hints`, not a native `title`, whose first hover in a window costs seconds) and repeats it in the disabled button's accessible name. It says the verb because the noun taught nothing — "Set to MLX FLUX" beside a dead button drew "i don't know what it means". The capability lead-in the registry writes into that sentence is stripped STRUCTURALLY, keyed on `repo.capability`, a field the page already holds — never by matching words in prose that could be reworded. **Revisions were abstracted away, not relocated**: the chevron, its drawer, the per-revision rows and their own carefully-written delete dialog (which stated the blobs a revision shares with its siblings and therefore does NOT free) are all deleted, on the test "does the user need to know this?" — picking a git commit off a folder whose only real question is what it costs is not a question anyone brings to this page, and a repo holding two commits simply reads as a bigger number that the same Delete removes. `GET /api/ai-models/revisions` and the delete endpoint's per-revision target are untouched and still tested. **The seal marks the MODEL, not the download**, so a recommendation and a matching search result wear it too: hiding it until the weights landed was the page saying least where it was surest. **Rejected: the size beside the name in the head**, which is where the recommendation cards keep it and which shipped for an hour on exactly that argument — those cards have no second line to put it on, so matching them there bought consistency with a card that had no choice, and cost the disk card the one arrangement that reads as a caption (the cost, then the address of the thing it is the cost of). **Also rejected: a disabled `✕` on a busy modal** (the mount sign-in): a disabled button takes no pointer events, so the `title` explaining why the door is shut never appears, and the control was indistinguishable from broken — it is not rendered at all while busy, and `decideClose` still refuses Esc and the backdrop, which is what actually protects the action. | Akshil, across a day of screenshots: "why do we have inconsistency between downloaded card and the normal card", "does the user need to know this? if not, abstract it away", "the dot here is barely visible", "when mounting we have a X button, i don't think that works". |
| D486 | **A Full Disk Access nudge (shell/fda.py + FdaCard.tsx): the packaged mac app detects FDA by probing, and a notification-stack card — shown only after the session first reads under a protected folder — walks the user to the exact Settings pane, once, ever.** | macOS prompts per protected-folder CATEGORY (Desktop, Documents, Downloads, removable, network volumes), so a new install is an Allow-Allow-Allow gauntlet on every category the user wanders into — and FDA, granted once in System Settings, silences all of them permanently AND survives upgrades, because releases are Developer ID signed under a stable bundle id (D73). macOS has no API to request FDA; an app can only detect it and point. Detection probes FDA-gated paths (`~/Library/Safari`, `~/Library/Mail`, the TCC db) — FDA-class paths never raise a prompt, so the probe is silent and free; PermissionError → not granted, success → granted, all targets missing → None, and None means the config field is OMITTED, so uncertainty never nags and the shell never even polls. **WHEN the card shows is the design**: not at launch — `relevant` flips on the session's first `/api/fs/list`-or-stat under a protected root (note_touch, one bool read steady-state), which is the same moment macOS starts prompting and therefore the only moment "give Full Disk Access" answers a question the user actually has. The flag is in-memory ON PURPOSE: persisted, the card would greet the next launch from the corner again, which is exactly the out-of-nowhere nag it exists to avoid (the owner's first screenshot of the always-on version: "this should not show up like this"). The card is neutral-toned on the server-status plate (no accent, no red — an invitation, not an outage), titled **Give Full Disk Access** over three how-to steps, with Open System Settings (deep-link `Privacy_AllFiles` via `open`, Popen so a wedged LaunchServices can't hold the request thread) and Not now (persisted server-side in `fda.json` — forever, not per-tab). The grant landing is an info TOAST, not a card state: the first cut reused the reconnected card and its title inherited the base plate's error-red — a success wearing the outage's colour — and the app deliberately has no green toast, every confirmation is info. Offered only when `sys.frozen == "macosx_app"`: a dev server's TCC identity is the terminal that launched it, so a grant would land on the wrong app; `FUSED_RENDER_FDA_BANNER=1/0/demo` overrides for dev, demo also forcing the probe to not-granted because dev terminals usually have FDA themselves. Rejected: hooking the card to detected DENIALS (PermissionError on a protected read) — the touch is a superset that fires before the user has even answered the first prompt, which is when the card can still save them the other four. Verified end-to-end on a branch-suffixed DMG (`io.fused.render.fda0825` — its own TCC identity, so the test could not disturb the real install's grants). | Akshil (2026-08-25): "we can just tell user to do this once and it will work forever", then "the very first time the user is doing stuff — that is when this should show up". |
| D487 | **A "Current apps" sidebar section above Bookmarks (shell/CurrentAppsSection.tsx + current-apps-lib.ts): every workspace app (`~/Fused/local/<slug>`) that still has a task not archived, in a stable order the user can drag; a row opens the app, its hover cross archives every task under it.** The Tasks page lists work by task; the thing the user is actually working ON is an app, and the question "which of my apps have something going" had no answer short of reading every row's project path. Scope is the WORKSPACE only (owner's constraint): a task on some repo elsewhere on disk is a project, not one of the apps this shell made, and the slug under `Fused/local` is the label. "Current" = has a task in any non-archived status — upcoming, in progress, done-but-unfiled, failed — because only filing says "off my desk", and the cross is that filing for the whole app at once (`POST /api/tasks/archive` per key, `Promise.allSettled`, then `pokeTasks()`; nothing destroyed, D306). **No second poll**: the section reads the pulse store (`useTasksPulseRows`, fed by the same `/api/tasks/pulse` the Tasks dot uses, feeder contract intact) and `project` was added to `_PULSE_FIELDS` for it — a sidebar `getTasks()` would be the double-poll sidebar-tasks.test.ts forbids. A row opens the app's PAGE, `/apps/<slug>` (D488) — it first opened the entry file via a click-time `GET /api/apps/entry`, and that went with D488 the same day: the page is the one place the sidebar row leads, and the entry rule now runs inside it. Rendered nothing with zero current apps until D489 put a + on the heading; the section is permanent now, empty = heading alone. The root is `Config.fused_dir` + `/local` (not `home` + `/Fused/local`): the workspace honours `FUSED_RENDER_DIR`, and a root built from home would list nothing under it. Shipped capped at five on the owner's "render 5 max" and the owner lifted that cap the next day (2026-08-26), so the list is UNCAPPED — one line per app, no scroll box and no "show more", because a shortlist that hides the sixth app answers "which of my apps have something going" wrong in the one case it matters; last-activity ordering is the implementer's assumption, stated in the lib header; the owner's brief said `~/Fused/local` in one line and `~/Fused/app/*` in another and only `local` exists, so `local` it is. **Redesigned 2026-08-26 (owner): the desk is a STORE of its own, `~/.fused-render/current_apps.json` (current_apps.py + `GET/DELETE /api/current-apps`), no longer derived from the pulse.** A NEW task adds its app (`current_apps.observe`, run inside `tasks._task_rows` against a `seen` key set — the one place every task on the machine passes, however it was started; first run seeds from every non-archived task, the migration of what the derived list showed); NOTHING removes an app automatically — archive every task under it and the row stays; the row's cross REMOVES the app and archives every task under it as the side effect (desk → tasks is the one coupling left, the reverse is what ended). Scope widened from `local/` to EVERY app kind: any workspace folder with a declared page (nearest such ancestor of the task's project, `app_dir_for`) and linked apps via the registered-apps registry. The sidebar fetches the table and refetches when the pulse shows a task key it has not seen; the running dot still reads the pulse (a subscription it already holds). Order store re-keyed by folder path (`ORDER_KEY` bumped to `:v2`). A folder that vanished lists struck-through until removed — a table that prunes itself is the coupling this ends. | Vasu (2026-08-25): "a new section in the sidebar above bookmarks called 'current apps' … apps which have any tasks which are not in archived state … constrained to ~/Fused/local only … render 5 max … a cross icon which archives all tasks for that app … on clicking the item in sidebar it opens the app". |
| D488 | **An app PAGE at `/apps/<slug>` (shell/AppPage.tsx): one workspace app, `<fused_dir>/local/<slug>`, as a place — an Overview tab running the app itself and a Tasks tab that is the Tasks page scoped to that folder. Opened from the sidebar's Current apps rows and nowhere else.** The explorer answers "what is in this folder"; the question an app with work in flight raises is "how is it going" — the app and its tasks side by side — and no view had both. It is a NEW page, not an explorer mode (D264 retired the `app` view and this brings none of it back): no listing, no crumb, no view switcher; the folder is a caption-link for when the files are the question. Overview is a plain iframe on `/render?path=<entry>` with no `_preview` flag — the reader is USING the app, so GET /render records the open (D301) like any other — and the entry is resolved at mount through `GET /api/apps/entry` (D269: the entry rule stays the server's); a folder with no page says so and links to itself. Tasks mounts `Scheduled` with a `scope` prop: the SAME page component — same List/Board/Calendar, same modal, same poll, same feeder contract (publishTasks still hands the sidebar the whole machine; the scope narrows only what is shown) — with tasks filtered to `project === dir \|\| startsWith(dir + "/")` before the toolbar filters, the h1 dropped (the page header already names the app), and New task prefilled with the app folder. The tab was first a QUERY param (`?tab=tasks`, overview = absent) so `/apps/<a>/<b>` — the retired builder route shell.py 404ed — could never look like this page; **amended 2026-08-26 (owner): one PATH per tab as D420 chose for AI Models, `/apps/<slug>/overview` and `/apps/<slug>/tasks`, bare `/apps/<slug>` redirecting to the default (same render-time replaceState), shell.py serving two levels under /apps, a stale builder link landing on the default tab like any unknown sub-path**. The query is the tab's own (`?view=` on Tasks) and rides across a switch untouched. Same day the tabs left the header for a shadcn Tabs strip (icons) inside a 1050px gutter frame matching the Tasks page. Mounted per SLUG not per nav epoch (the CanvasWorkspace exception) and the Overview frame stays mounted behind Tasks (display:none) — a tab switch is a navigation and must not reload the running app. The slug is validated after decoding in `slugFromAppPath` (one segment, no `.`/`..`/separator); the server route is a bare shell fallback and this is the only guard. Route asserted by hand in test_shell_routes.py — the scrape only sees `pathname ===` literals. Known: the Tasks tab shares `VIEW_KEY` with `/tasks`, so picking Board here is remembered there too — one page, one memory. **Re-addressed 2026-08-26 (owner, with the D487 redesign): `/apps/<folder path>?_tab=<tab>` — the whole folder as path segments in the explorer's codec (a Windows drive rides as `C:/…`), so a showcase app or a linked app anywhere on disk has a page; shell.py serves `/apps/{path:path}`. The tab went BACK to the query (the owner's call, same day, reversing the path-per-tab amendment above): with the folder in the path a trailing tab segment is ambiguous with a folder itself named `tasks`, so `_tab` names it — underscored like the explorer's `_mode` so it cannot collide with a tab's own params (`?view=`, `?file=`), which ride across a switch untouched; absent = overview, so the default tab has one address and App.tsx needs no rewrite. `appPathFromPath` validates after decoding (no `.`/`..`/empty segment). A stale `/apps/<tag>/<name>` link now decodes as a folder and lands on the page's missing banner.** | Vasu (2026-08-25): "design a new page: /apps/<app-name> … only opened via side bar 'current apps' section and nothing else … completely new page, separate from the file explorer … 2 tabs. overview and tasks … overview: renders the app itself inside the page, user can just use the app here … tasks: displays the exact task component we have with list/board etc, but it's scoped to only the tasks for that project". |
| D489 | **A + on the "Current apps" heading opens the /apps composer in a modal (shell/CurrentAppsSection.tsx + `HeroComposer` exported from apps/builder/HomeHero.tsx): type a prompt, send, and the exact create flow runs — name, scaffold, land in the new app's chat.** The sidebar section is where the owner looks at apps in flight; making a new one meant leaving for /apps or /home first. It is the SAME component, not a copy: `HeroComposer` is what the two hero pages already render under their wordmark, so the naming call, the free-name retry, the model/effort pickers, the starter chips and the post-create navigation cannot drift between three doors. Hosted in the platform `Modal` chassis (focus trap, Esc/backdrop close, exit animation) rather than a bespoke popover — with a new `plainBody` opt-out: the chassis body's `deploy-body` form vocabulary (`.deploy-body button`, fields.css) out-specifies the composer's own classes and re-skinned every chip, picker and the send button into square form buttons; the owner's rule is "not redesigning anything", so the body drops that vocabulary (`.modal-body-plain`: same padding and scroll, no descendant skins) rather than re-asserting each composer rule under `.deploy-body` the way schedule.css does for the task form. The composer navigates itself on success and that navigation remounts the sidebar, which is what closes the modal; `onCreated` closes it for the no-chat case. Consequence for D487: the section renders ALWAYS now — an empty desk is exactly where the door to "make one" belongs, and hiding the heading hid the door. The plus reveals on heading hover, the same posture as the rows' own actions. | Vasu (2026-08-25): "a small plus icon at the side of 'current apps', this opens a modal with the composer on /apps page exactly inside of the modal. User can type in a prompt and send, does the exact flow as what happens at composer at /apps". |
| D490 | **A skill IS a directory under `skills/` with a `SKILL.md` in it — the three hardcoded name lists that used to decide which skills ship are replaced by one discovery module, `fused_render/skill_sources.py` (`REPO_SKILLS_DIR`/`PACKAGED_SKILLS_DIR`, `skill_sources()`, `skill_names()`), read by both the plugin root (D216) and the user-level sync (D185), with `scripts/hatch_build.py` carrying the same scan for the packaged copy (D106)** | Four deliveries hand the same skills to a session, and only three of them had a list: the repo root **is** a plugin root (committed `.claude-plugin/plugin.json` beside `skills/`), so `claude plugin marketplace add fusedio/fused-render` already shipped whatever was in `skills/`. That asymmetry is what made a list wrong rather than merely tedious, and it had already cost: `fused-render-ai` (added 2026-08-14, #517) went into `skill_plugin.SKILLS` and `user_skills.SKILLS` but not into the build hook's `_ALL_SKILLS`, and the packaged copy is the ONLY source either delivery has on a machine with no repo — so every wheel and DMG since shipped four of five skills, the plugin root assembled from them was four skills deep, and nothing failed anywhere: a session simply did not know `fused.ai` existed and wrote pages against an API it was guessing at, which is the exact failure D216 exists to prevent. The old pin test (`skill_plugin.SKILLS == user_skills.SKILLS`) could not see it, because the third list lived in a build hook that must not import the package it is building — so the guard is now a test asserting `hatch_build._canonical_skills(REPO_ROOT) == skill_sources.skill_names()` on the real repo, plus one asserting the discovery matches what is on disk. `SKILL.md` (rather than "is a directory") is the membership rule because the packaged tree keeps its plugin manifest as a FLAT `skills/plugin.json` — nothing in the wheel may live under a dotted path — so a bare `isdir` scan would deliver `plugin.json`'s neighbours and a stray build leftover as skills; `_copy_starter_skills` now also clears the gitignored packaged tree wholesale for the same reason. Deliberately NOT an allowlist any more: the repo's own dev-only skills live in `.claude/skills/` (a separate tree that ships nowhere), so `skills/` holds exactly the shippable set, and the marketplace plugin had already committed us to that |
| D492 | **The user-level skill COPY is replaced by the published plugin: `user_plugin.py` installs `fusedio/fused-render` into the user's own Claude config and keeps it updated, on a daemon thread at server startup — and stands completely down when `enabledPlugins` carries an explicit `false` for `fused-render@fused-render`. `user_skills.py` (D185) is deleted, and the create-app / create-template routes no longer sync anything into `~/.claude`** | D185's job stands — cover the user's OWN `claude`, in a terminal or in their app folder, which `--plugin-dir` cannot reach because that flag only exists on processes we spawn. Its MECHANISM could not satisfy something the job actually needs: a user who could not get a chat session to know about `fused.ai` went to the Claude Config page first and found nothing registered under Marketplaces, Plugins OR Skills. Marker-guarded directories under `~/.claude/skills/` are not a thing that page lists, that `claude plugin list` reports, or that a user can update, disable or remove on their own terms; a marketplace plugin is all three, which is why making it visible and making it installable turn out to be the same change. **Never enable, never re-enable**: an explicit `false` is the user having said no and ends this module's involvement entirely (no install, no marketplace add, no update, not even a `claude` spawn); an ABSENT key is not a refusal but a machine that has never had the plugin, which is what an install is for — reading absence as a no would mean never installing anywhere. Enabling a fresh install is not overriding anyone, since no preference has been expressed yet. Safe to be this loose because it is NOT load-bearing and must not be: the sessions we spawn get their skills from the locally assembled root (D216) — offline, synchronous, version-matched to the installed wheel — so a disabled or failed plugin does not cost a fused-render chat one skill. A daemon THREAD, not the inline copy it replaced: this spawns `claude` and clones over the network, and D228 is the precedent — one `claude --help` on the pre-bind path outran the desktop supervisor's 20s readiness budget and killed the server three times. Rate-limited off a stamp written per ATTEMPT (1h while an install is still pending, 24h once installed), which is the only thing standing between a restart loop and a clone per restart. Dropped from the two scaffold routes rather than moved: it is machine-wide, so a new app or template gives it nothing to repair, and a network clone has no business on a UI request. Consequence accepted: an offline machine, or one whose `claude` CLI is absent, now gets no fused-render skills in the user's own terminal sessions until a later start succeeds — the copy always landed. NOT chosen: registering our locally assembled root as a local-path marketplace (`marketplace add` does accept a path). It would be skew-free, but it puts app-internal state into the user's config, where an uninstall leaves a dangling marketplace |
| D493 | **`user_plugin.py` resolves the Claude config dir the way the `claude` it SPAWNS does — `CLAUDE_CONFIG_DIR`, then `CLAUDE_DIR`, then `~/.claude` (same ordering `claude_health.py` documents) — instead of reading `claude_config/lib`'s paths; the marketplace is added `--sparse .claude-plugin skills`; and a plugin the user has UNINSTALLED is never reinstalled, told apart from a fresh machine by a sticky `ever_installed` in the stamp. (revises D492)** | Three ways the first cut of D492 broke its own promise. `lib.CLAUDE_DIR` keys off `CLAUDE_DIR` only, but `lib.claude_cli` hands `{**os.environ}` to the child — so with `CLAUDE_CONFIG_DIR` set, the install landed in one dir while `installed()` read another: a reinstall every hour forever, and an explicit `false` in the dir that actually mattered was invisible, so `plugin install` re-enabled a plugin the user had turned off. The module's whole job is to agree with the process it spawns, so it must resolve the dir that process will use. `marketplace.json` declares `"source": "./"`, so the plugin is the entire repo: a full clone measured 152MB (then copied again into the plugin cache, then re-pulled every 24h) for 180KB of skills; `--sparse` brings it to 7.0MB with all five skills present, verified against a throwaway config dir. And `claude plugin uninstall` leaves no explicit `false`, so a removal was indistinguishable from a fresh machine and was undone within the hour — which made "remove it on your own terms" untrue for every path except `disable`. |
| D494 | **A resolvable repo `skills/` is authoritative about WHICH skills exist, not just about their content: `skill_sources()` uses the repo root wholesale and consults the packaged copy only when the repo root yields no skills at all. (revises D490)** | D106's single-source rule was written about a skill's content, and D490's first cut extended it per-skill — a union, so a skill deleted or renamed out of `skills/` kept shipping forever from a stale `fused_render/skills/` left by an old local wheel build, in the one situation where the correct answer is sitting right there. Precedence is claimed by a root that HAS skills rather than one that merely lists, because `REPO_SKILLS_DIR` is `<parent of the package>/skills` — on a wheel that is `<site-packages>/skills`, a path any other distribution may create, and reading an empty one as an authoritative "no skills here" would deliver nothing on exactly the installs whose only source is the packaged copy. |

### Background apps — a folder's own long-running daemon (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D514 | **The version digest that decides whether a background app's child gets reused or retired (`background_apps.version_for`) folds in the INTERPRETER path, not just the manifest's own bytes and the daemon file's mtime/size.** | This is the fix for the class of bug an interpreter-path OMISSION already caused elsewhere in this codebase (the OpenWhisper upgrade-rot class, named directly in the code comment): a bundled-CPython swap across an app upgrade must retire a background child rather than have it reuse a venv built against an interpreter that no longer exists (or exists but is now a different, incompatible build). The manifest bytes and the daemon file alone are the folder-author's half of "did anything change"; the interpreter is the SERVER's half, and a digest that only covers the first half is a digest that lies about the second changing. |
| D500 | **The background-apps API (`server/routers/background_apps.py`) takes only `html` — the page's own path — on every endpoint, resolving the app folder from it server-side exactly as `/api/run`/`/api/engine` already resolve `py`; and `enable` gets the identical 409-on-unbuilt-venv stance `/api/engine` already takes, rather than building the venv itself.** | Both choices are reuse, not new design: a path-typed API (accept a raw folder path from the client) is new attack surface this feature does not need to open when the existing `resolve_py` pattern already answers "which folder does this page belong to" safely, and a POST that builds a venv on demand can block for minutes — exactly the reasoning `routers/app_engine.py`'s existing 409 already states for the warm `/api/engine` worker. Consistency here is not aesthetic: a third resolution rule for a third engine_host child kind is a third thing to keep in sync with the other two, for no behavior a background app actually needs to differ on. |
| D501 | **The enabled store is its OWN JSON file (`<home_dir()>/background_apps.json`), not a field folded into an existing store (`prefs.json`, `registered_apps.json`).** | The store's read/write discipline (`enabled_paths()` degrades a missing/unreadable folder to "not in the result" without ever rewriting the file — the same posture `registered_apps.py`'s `read_entries` already documents) is specific to this feature and would either have to be bolted onto an existing store's unrelated discipline or risk drifting from it silently. A dedicated file also means disabling every background app, or inspecting what's enabled, never touches a store something else already owns — `prefs.json` is user preferences with their own read/write shape, and `registered_apps.json` is a distinct concept (external app folders opened through the explorer) that happens to share none of this feature's containment or version-digest logic. |
| D502 | **`engine_host`'s reaper eligibility (`reap_idle_app_workers`) is changed to check `Child.kind == "app"` explicitly, adding a `kind` field to `Child` rather than leaving background apps to fall out of eligibility by relying on `module` staying empty for them (as it already does for a template daemon).** | The existing gate (`c.module` truthiness) would, as it happens, already exempt a background child for free — a background child has no `module` (that field is `/api/engine`'s own warm-worker marker) — but that is a COINCIDENCE of two unrelated fields never being written together, not a stated rule, and a future change to either kind could silently break it in either direction (a background kind that later needs `module` for some reason would become reap-eligible; an app kind's idle-retire could equally break if `module` stopped being the reap signal). An explicit `kind` field, checked explicitly, makes reap eligibility a decision instead of an accident — the same reasoning `restart()` was updated to also preserve `kind` across a respawn, since a restarted child that lost its kind would silently become reap-eligible (or template-default) on its next idle sweep. |

### Background apps — code-review round 2 (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D503 | **`background_apps.interpreter_for` resolves a background app's interpreter from the app's OWN folder only (`projectenv.has_project_env(folder)` + `projectenv.interpreter_for(folder)`), never by walking up to an ancestor project the way `projectenv.project_env_for`/`project_root_for` do for a plain `.py` script.** | `/api/engine`'s warm-worker dispatch (routers/app_engine.py) was the wrong precedent to mirror literally: it resolves the environment for a SCRIPT, which has no boundary of its own, so climbing to the nearest ancestor `pyproject.toml` is correct there. A background app's FOLDER is already the project boundary — it declares itself unambiguously via `[tool.fused-render.app]` — so the walk-up was a real bug, not a design choice: the shipped reference fixture (`tests/fixtures/background_app`, nested inside the fused-render repo itself, deliberately dependency-free per the plan's own description of it as "the contract reference for the authoring skill") resolved to the REPO'S OWN venv, which isn't in the project-venv store, and the enable endpoint 409'd on every attempt — the documented reference app could not be enabled. Worse than merely broken for that one folder: ANY app folder placed anywhere inside some other repo would silently inherit that repo's unrelated venv. `has_project_env(folder)` checks ONLY the folder handed to it (a `[project]` table with at least one applicable dependency, PY-17's own bar for "worth a venv") — never an ancestor — so a manifest-only app runs on `sys.executable` unconditionally, and an app that DOES declare its own `[project]` deps (same `pyproject.toml`, alongside its `[tool.fused-render.app]` table) gets a venv built at its own folder, never an ancestor's. |
| D504 | **`version_for`'s interpreter component drops `os.path.realpath` and instead hashes the interpreter path AS GIVEN plus an `os.stat` of the file at that path (mtime, size) — the identical treatment the daemon file already gets. (revises D514)** | D514's first cut folded in `os.path.realpath(interpreter)` alone, and CI caught it failing on Linux within a day: a venv's `bin/python` is a symlink to its base CPython, so realpath COLLAPSES every venv built on the same base interpreter into one identity — the opposite of what the component exists for — and on the Linux runner `/usr/bin/python3` and `/usr/bin/python3.12` are symlinks to the identical file, so a test asserting two different interpreters produce two different digests failed there (and nowhere else, since macOS/Windows runners don't alias those two specific paths — the kind of platform-dependent silence that makes a realpath choice like this dangerous to leave unexamined). Separately, and more fundamentally, a path alone (realpath'd or not) cannot see the ACTUAL upgrade-rot case D514 was written for: confirmed against a real install, the packaged app's own interpreter is rewritten IN PLACE at the SAME path on upgrade — same path, same `--version` string, different bytes, mtime moved. Hashing the raw path (so two different venvs sharing a base CPython are never conflated) plus that path's own `os.stat` (so an in-place rewrite at an unchanged path is still caught, exactly as a `daemon.py` edit already is two lines above) closes both gaps with the same mechanism already proven for the daemon file, rather than inventing a second one. `version_for` now also raises `OSError` when the interpreter itself doesn't exist, matching the daemon's existing missing-file stance — both current callers (the enable endpoint's `_resolve`, the startup resurrection hook) already `os.path.isfile(interpreter)` before reaching this, so the added `os.stat` is TOCTOU-tolerant, not a new trust assumption. |
| D505 | **A `kind="background"` child gets its own app folder in its environment — `FUSED_RENDER_APP_DIR`, exported by `engine_host._spawn_env`, carried on a new `Child.folder` set by `ensure_background`'s callers — and `templates/shared/background_app.py` is a new stdlib-only client (`status()`/`stop()`/`disable()`/`restart()`) that uses it to let a background daemon call the background-apps API about ITSELF.** | Every `/api/apps/background/*` endpoint resolves the app folder from a page's `html` path (D500) — correct for a page, but it leaves the daemon with no way to ask the server anything about itself, because a daemon has no page and no `html`. That gap is what makes OpenWhisper's tray "Quit" reach for the raw AppKit `terminate:` selector instead of the server's own `stop`: the daemon side of "quit cleanly" had nothing to call. Threaded through exactly like `cache`/`version` already are (every `ensure_background` caller already has the folder in scope from its own `_resolve`/manifest load) rather than derived from `daemon`'s dirname, because a manifest's `daemon` may name a nested path and dirname would then disagree with the folder engine_id/version were computed against. The client mirrors `fused_ai.resolve_origin`'s ladder rather than importing it, so a caller wanting three tiny endpoints does not pull in the whole AI job-polling surface — the same reasoning that already justifies `fused_ai.py` and `appenv.py` being two files instead of one. Surfaced a second, previously undocumented fact while writing this: `stop()` pops the `Child` out of `engine_host._children` before killing it, so a proxied call afterward returns 409 rather than reviving it, but a process ended any OTHER way (external kill, crash, native `terminate:`) leaves the `Child` registered and gets healed back to life by the next proxied request (`engine_forward.py`) — making a raw kill the WEAKEST possible way to quit a background app, the opposite of what "Quit" should mean. Documented in SPEC.md §46 and the `fused-render-background-apps` skill so the next background app's tray/CLI/native control reaches for this instead of rediscovering the asymmetry the hard way. **Correction, found in review before this ever reached main:** the first cut left `restart()` rebuilding its replacement `Child` field by field (`engine_id`, `python`, `daemon`, `cache`, `version`, `module`, `kind`) without carrying over the new `folder` — invisible in every direct `ensure_background` test, since none of them go through `restart()`, but exactly the path a killed-and-healed background child takes (`engine_forward.py`'s heal-on-proxy) and the one `POST /api/apps/background/restart` takes whenever a child is still live. A background daemon respawned that way would boot with no `FUSED_RENDER_APP_DIR`, silently losing the very self-addressing this decision exists to add — the tray's Quit/Turn Off would fall back to raw-terminate for the rest of the server's life with no error anywhere. Fixed by adding `folder=existing.folder` to `restart()`'s rebuild; a regression test restarts a `kind="background"` child and asserts the replacement's own `_spawn_env` still carries the var (asserting on spawn success alone would not have caught this — the original bug spawned fine, just silently minus one env var). Checked every other `Child(...)` construction in `engine_host.py` (`ensure`, `ensure_app`, `ensure_background`, `restart` — the only four) for the same class of drop: `ensure`/`ensure_app` never set `folder` at all (correct — neither is `kind="background"`), and `ensure_background` already had it from this decision's own first cut. No production code outside `engine_host.py` constructs a `Child`. |
| D495 | **Whether the Claude/companion sidebar is SHUT is now remembered for the rest of the session, on both surfaces: a new memory-only store (`apps/explorer/lib/side-hidden-store.ts`, sibling to the width store `side-store.ts`) holds one boolean, consulted by `parseSide` (`lib/preview-side.ts`) and `parsePaneSide` (`listing/pane-side.ts`) only where the URL says nothing about `_side` — an explicit `_side` (including `off`), or a legacy `_mode` deep link, always wins over the flag for that one paint. The sidebar being OPEN is what clears the flag, full stop, whether that openness came from a click (`setSide`) or from an explicit `_side`/`_mode` deep link winning over a stale flag (`sideReopenedByUrl` / `paneReopenedByUrl`, reconciled once at mount) — a close, from either source, is the only thing that sets it. A refresh clears it for free because it is a module variable, and neither module ever writes it into the URL (see D495's own correction below).** | `navigate()` (platform/lib/router) drops the whole query string on most hops and re-adds `_side` only on a folder→folder one, so a close on a file followed by any hop that touches a file at either end (file→file, file→folder, folder→file) lost the "shut" request the moment the URL that carried it was replaced, and the sidebar popped back open — reported as "can we remember that the user has hidden their claude panel for a session (both the file view and folder view) till refresh? similar to width." Built to the width store's own documented policy rather than a new one: no storage at all, so a refresh is the deliberate escape hatch back to the default-open state, exactly as it already is for a dragged width. `hidden` is a plain parameter on `parseSide`/`parsePaneSide` (default `false`) rather than the two modules importing the store directly, so the resolution rule stays pinnable by a test with no store to reset — the two call sites (`Preview.tsx`'s mount, `Listing.tsx`'s mount) read `getSideHidden()` and pass it in. The listing's writer only touches the flag when `paneEnabled` (the addressable folder view, not a frozen snapshot or an embedded panel pane), so closing a preview inside one of those does not shut every other open sidebar for the session. **Two corrections found in review, before this ever reached main:** (1) the first cut's Preview.tsx reconcile effect (`reconcileSideSearch`) took `sideReq.open` at face value, so a flag-driven close on a file whose URL was itself silent got written back as an explicit `_side=off` — breaking the refresh escape hatch (the URL, not just the module variable, now said shut) and leaking into any link copied from that file's address bar. Fixed by tracking whether the current closed state came from the flag alone (`sideFromHiddenFlag`) and, only in that case, passing the reconcile effect `open: true` — which lands in its existing "no verdict yet, leave `_side` alone" branch and keeps the URL exactly as silent as it already was. The folder pane never had this bug (it has no reconcile effect). (2) "an explicit `_side` always wins" and "reopening clears the flag" contradicted each other for a deep link that OPENS the sidebar while the flag was still set: the link won the paint (rule one) but nothing cleared the flag for it (rule two never fired, since only `setSide` wrote the store), so the very next silent-URL hop shut the panel again despite the user just having seen it open. Resolved as stated above — an explicit open is the same observable outcome as a click, so it clears the flag identically — and implemented as a MOUNT-TIME-only reconciliation (not a live effect) so it reflects what the URL asked for when the surface opened, not a rule that fights `setSide` on every later render. |

### Card thumbnails are sealed, not policed (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D496 | **A card thumbnail is SEALED BY THE EMBEDDER, not policed by the shell: every display-only thumbnail frame carries `sandbox="allow-scripts allow-same-origin"` and `allow=""` (`THUMB_SEAL`, `platform/lib/frame-focus.ts`), and the scripted escapes that survive that are closed INSIDE the page by `runtime.js` under `_nofocus=1` — which now inherits from a same-origin ancestor, suppresses `select()` alongside `focus()`, re-implements `scrollIntoView` to walk only this document's own scrollers and stop at its own viewport, and stops a thumbnail writing any URL but its own. No shell-side machinery: the earlier version of this row's fix — the shell remembering its card scroller's offset and putting it back when a frame displaced it — is deleted.** | The reported symptom was /apps scrolling choppily past a card whose app takes focus on mount (focusing an element inside a frame scrolls THAT FRAME into view, and the scroll walks out into `.apps-page`). The first fix chased it from the shell and worked; the owner rejected the shape — "NOTHING must leak out from those iframes" — and the shape was the problem: the shell repairing damage a thumbnail had already done, one escape route at a time, forever. **The right question is what the BROWSER will enforce.** `sandbox`'s mere presence sets the sandboxed automatic-features flag, for which no re-enabling token exists — so `autofocus` never applies and media never autoplays, which is the largest single cause of the jump closed in markup with no runtime involved (and a thumbnail can no longer make noise). Everything not listed stays denied, and each is something a thumbnail could do TO the page around it: navigate the shell away, open a popup, start a download, submit a form, take pointer lock, or raise an `alert`/`confirm` that blocks the window. `allow=""` is an empty permissions policy — an ABSENT `allow` would let a same-origin frame inherit the embedder's camera/microphone/geolocation grants — so a thumbnail cannot raise a permission prompt in the reader's name. **`allow-same-origin` is deliberately kept and is the owner's call ("sealed but functional")**: `/api` (runPython, fused.ai, readFile) and the theme in localStorage are origin-bound, and an opaque origin — which would additionally have killed URL writes, shared storage and WebKit's cross-origin scroll bubbling — turns every data-driven card into its own empty state. The pair `allow-scripts allow-same-origin` is famously not a security boundary (the frame can reach through `parent` and delete its own sandbox attribute) and is not used as one: these are the reader's own local apps and the attribute is doing feature containment, not privilege separation. What the seal cannot reach is scripted, so it is answered where the script runs: `select()` joins the focus gate because it focuses natively and never touches the patched `HTMLElement.prototype.focus`; `scrollIntoView` is re-implemented rather than forwarded, because the native call scrolls every container up a chain that does not stop at the frame and there is no native way to stop it from either side (`w3c/csswg-drafts#7134` is the open request; WebKit blocks cross-origin bubbling, Blink does not, and a thumbnail is same-origin here by design) — the re-implementation reproduces start/end/center/nearest over this document's own scrollable ancestors and ends at its own viewport, so an app that scrolls its log to the bottom still shows its bottom and nothing crosses the boundary. **A second leak, found while reading rather than reported: `fused.params.set` writes the topmost same-origin ancestor's URL (D46), so a thumbnail scrubbing a param on boot was rewriting the /apps address bar, one `history.replaceState` on the shell per card.** `sandbox` cannot stop that with same-origin kept, so the climb does: `findTarget` refuses to climb out of a thumbnail (`_preview=1`, inherited), which also makes `path` reserved there exactly as it is for a standalone page, and only `replaceState` is reachable from inside a thumbnail anyway (the push path needs a gesture the pointer shield never delivers), so it cannot add joint history entries either. Rejected: `inert` on the thumb box, which reads like the answer — the part that would matter, inertness propagating into the frame's document, is being REMOVED from Blink as a cross-site leak and never existed in WebKit or Gecko (`whatwg/html#7605`), and the part that is real (hit-testing, tab order) these frames already have from the pointer shield and `tabIndex={-1}`; static image thumbnails, which would leak nothing at all but have no automatic capture path (appShot's `getDisplayMedia` needs a gesture), so every app without an authored `preview.png` would go blank. Knowingly left: a native focus this cannot intercept (`dialog.showModal()`, a browser-initiated frame focus) can still scroll the grid once — the bounce returns the keyboard but not the scroll — and `noteFsChanged` still pokes ancestor caches, which is a correctness signal rather than a leak. **Four things were DELETED once the seal carried the weight, because the count of mechanisms is the thing that made this hard to reason about in the first place:** (1) the description of a thumbnail — two URL stamps, the seal, the tab-order/scrollbar markup — collapses into one `thumbFrame(src)` (`platform/lib/thumb-frame.ts`) that every thumbnail spreads, so the five parts cannot be shipped four-fifths done the way D348 shipped `_preview` without `_nofocus`, and the pinning test is now a count of `thumbFrame` calls against iframes in the file; (2) the `autofocus` attribute strip and its MutationObserver, which never worked alone (the candidate is queued at insertion and cannot be dequeued) and whose job for a thumbnail is now the sandbox flag's — the cost is one focus flicker in a pane preview before the bounce, against an observer over every mutation of every previewed document; (3) `window.__fusedNoAutofocus`, published so a page could gate its own boot focus, and the claude template's `focusBox` gate that was its only reader in the repo — the prototype patch was already dropping the focus it gated, and one mechanism covering every template beats two that have to agree; (4) the pointer-shield span on the iframe branches, redundant with the `pointer-events: none` both stylesheets already set on a thumbnail iframe (the `<img>` branches keep theirs: an image carries the native drag gesture, which pointer-events does not stop). |

### Fit verdict over a measured footprint, and whole-download totals (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D497 | **The AI Models fit badge is computed over a model's resident FOOTPRINT on a precedence ladder — measured on this machine (`ai/footprints.py`), else a curator's optional `resident_gb`, else the download size as today — with headroom thresholds (`ram - 8GB` usable, easy at 60% of it) in place of the 25%/50%-of-total fractions, an Apple-Silicon `iogpu.wired_limit_mb` hard ceiling, a runner-supplied true peak (`serve(peak_memory=…)`, `mx.get_peak_memory` on MLX), and `fit` widened from a string to `{verdict, basis, footprintBytes}` so the page can word a MEASURED verdict as a fact. (SPEC AI-8c, AI-16, AI-16a, AI-16b, AI-16c)** | `_fit_verdict` was handed `size_gb` and asked a memory question, and the two quantities coincide only for a single-checkpoint text model. `ltx-video` runs `DistilledPipeline(low_memory=True)`, which frees the transformer and the Gemma encoder BETWEEN stages — its peak is one stage, its download is two whole repos (AI-15a) — so LTX-2.3 int4 read "Likely too big for this machine" on a machine where it renders, directly contradicting the catalog note on the same row ("a 16 GB+ Mac"). A cached uncurated repo was worse still: its `size_gb` comes from `_cached_size_gb`, bytes on DISK including every revision, so the badge drifted further from the truth the longer the cache lived. Thresholds alone do NOT fix it — 28.5GB against 34.4GB of RAM is correctly "no" under any headroom rule; what fixes it is measuring the footprint, which the app already computes on every load and then throws away. The ladder degrades to today's answer rather than replacing it, so a model nobody has run yet is judged exactly as it is now, and `basis` is what carries the difference outward instead of a hedge word applied uniformly. Thresholds moved to headroom because a fraction scales wrong in one direction only: 50% of 16GB leaves a correct 8GB for the OS and the browser, 50% of 64GB leaves 32GB unusable for no stated reason. The peak probe is a second hook beside AI-8a/AI-8b's `memory=` rather than a reinterpretation of it, because `refresh_memory` polls only when the status endpoint is READ — a sampled resident figure on a staged pipeline is not a bound on anything, and `benchmark.py` already says so about its own reading. Surya (2026-08-26): "LTX-2.3 is 'Likely too big for this machine' (M5 Max, 48 GB RAM) but I seem to be OK running it", "the model download size in gb is not related to how much memory it will take". |
| D498 | **A download's bar is priced at the WHOLE download: a runner fetching more than one repo declares every phase up front through `worker_base.download_plan(phases)`, which sums each phase's `repo_total_bytes` into one grand total and reports `done` across all their folders; the job row carries `totalScope` (`"download"` vs `"phase"`), and `shared/modelSize.ts` lets a whole live total WIN outright instead of applying its never-understate rule to it. (SPEC AI-5n)** | AI-5b established that a wrong total is worse than none, and `_capped` protects a scoped total from a folder measured wider than it — neither covers a total honest about its phase and silent about the download. `ltx-video` makes two sequential `download_snapshot` calls (weights, then `mlx-community/gemma-3-12b-it-4bit`, AI-15a), so an LTX-2.3 int4 pull reports 19.1 GB while the card beside it reads the catalog's 28.5 GB — one download, two figures 30% apart, neither wrong and no way for a reader to tell that. `torch_image.py`'s GGUF recipe has the same shape, and `modelSize.ts`'s never-understate rule exists ONLY to work around it, at the cost of pinning the display to a stale-HIGH constant forever. `totalScope` rather than a flag day: single-repo runners are already correct and migrate nothing, and each multi-phase runner improves the moment it adopts the plan. A phase whose size the Hub cannot answer costs the WHOLE total rather than contributing its known siblings — summing known with unknown rebuilds AI-5b's original defect one level up, and an indeterminate bar is honest where a partial one is not. Surya (2026-08-26): "the model download size and the size mentioned in the playground can be wildly different" (with a screenshot of the card reading 27 GB beside a 15.6 / 19.1 GB progress row). |

### `fused.app` renamed to `fused.daemon` (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D506 | **The browser-facing namespace `fused.app` (`static/runtime.js`: `status`/`enable`/`disable`/`stop`/`restart`/`call`) is renamed to `fused.daemon`. Nothing else moves: the HTTP endpoints stay `/api/apps/background/*`, the Python modules stay `fused_render/background_apps.py` and `templates/shared/background_app.py`, and `Child.kind == "background"`/`ensure_background`/`kind = "background"` in the manifest are untouched.** | "App" already names three unrelated things in this codebase: any folder carrying `<meta name="fused-app">`, the warm worker `ensure_app` spawns (a different `Child.kind` entirely), and the `/apps` hub that lists them all. `fused.app.enable()` read as "enable this app" when it actually means "install a resident daemon that outlives the page" — a three-way collision on one word. `daemon` is the noun the author already writes everywhere else: the manifest's own `daemon = "daemon.py"` key, the file itself, and `engine_host`'s vocabulary throughout. The rename is JS-only and deliberately does NOT extend to the HTTP layer or the Python modules — `background_app.py` in particular must not become `daemon.py`, since that name is already taken by the app's own daemon file and the collision would be genuinely confusing. This leaves `fused.daemon` calling `/api/apps/background/*` — an intentional, permanent split between the author-facing name (which matters, so it got fixed) and the internal-only HTTP/Python naming (which doesn't, so it didn't move) — not a half-finished rename; the next reader should not "fix" the asymmetry. No back-compat alias: `fused.app` was local-only (the hosted `fused` wheel's own `runtime.js` never defined it) and the branch introducing it (PR #852) had not merged, so nothing outside this branch depended on the old name. |

### A page must not spawn a daemon merely by being previewed (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D507 | **`fused.daemon.enable()`, `restart()`, and `call()` (`static/runtime.js`) reject with a named `Error` — no request ever leaves the page — when the calling frame is a preview thumbnail: its own URL, or any same-origin ancestor's, carries `_preview=1`. `status()`, `stop()`, and `disable()` are left ungated. Enforced client-side, not server-side.** | Verified, not assumed, that the flag is knowable at the point `enable()` runs: `AppPreviewCard`'s live thumbnail (Home's app strip, the `/apps` hub — mounted for any app with no `preview.png`, or on hover for any app, sandbox `allow-scripts allow-same-origin`) loads `thumbFrame(liveSrc)`, and `thumbFrame`/`withPreviewFlag` (`frontend/src/platform/lib/thumb-frame.ts`, `router.ts`) stamp `_preview=1` onto the exact `/render?path=...` URL that becomes the iframe's `src`. `GET /render` (`server/routers/render.py`) serves the app's HTML at that URL with no redirect, so the flag lands in the rendered page's own `location.search` — reliable, not merely inherited. `runtime.js` already computed this fact for the focus contract (`IS_THUMBNAIL`, mirroring `router.IS_PREVIEW`/`ancestorIsPreview`); the guard reuses the identical self-or-ancestor climb. Given the flag reaches the frame directly, the client is the natural enforcement point — the check sits where `enable()`/`restart()`/`call()` themselves run, before any fetch. This addresses a *careless* app (the actual bug: `Sina/OpenWhisper/index.html` called `enable()` unconditionally on load, reinstalling itself every time its card was hovered), not a *determined* one — a page willing to skip `runtime.js` and `fetch("/api/apps/background/enable", ...)` directly bypasses any function-level guard regardless of preview state, and that bypass is not specific to this feature (the sandbox already grants same-origin script execution); a server-side guard keyed on Referer (the same inference `render.py`'s `_referred_by_preview` already uses for its own recording decision) was considered and rejected as solving a problem the client-side check already solves for the verified hazard, while adding a header the client can suppress just as easily as it can skip the wrapper. `call()` is in scope though it never calls `ensure_background` directly: `engine_forward.py`'s heal-on-proxy path (`_forward`, ~lines 216-222) respawns a dead-but-enabled child on any proxied call, so an unguarded `call()` from a preview could resurrect a daemon enabled in another session. `stop()`/`disable()` are excluded on purpose: they only ever turn a daemon off, and a preview must never be able to do that to a real user's daemon either — gating them on the same flag would trade one hazard for a worse one. Rejection is a loud, named `Error` (`fused.daemon.<method>: refused — …`, pointing the author at `status()` on load and an explicit user action for `enable()`/`restart()`), never a silent no-op — a silent failure here means the author never learns their page is doing the wrong thing in every context, not only preview. |
| D508 | **`fused.daemon.stop()` and `disable()` (`static/runtime.js`) are gated on the preview-thumbnail flag exactly like `enable()`/`restart()`/`call()` — D507's "left ungated" text for these two was reversed.** | Code review (2026-08-26) caught that D507's shipped comment argued the opposite of what the hole demands: it read "gating them here would let a preview do the one thing worse than starting a daemon: kill a real user's daemon just because their card scrolled past" — but gating is what PREVENTS exactly that, and leaving them open is what PERMITS it. The hole was real, not hypothetical: `AppPreviewCard`'s live thumbnail mounts an app's own `entry_html` in a sandboxed `allow-scripts` iframe, so an app whose init path called `fused.daemon.disable()` could un-persist a running daemon on a scroll-by or hover — worse than the original `enable()` bug this whole guard exists for, because `disable()` survives a server restart while an accidental `enable()` at least stays reachable to turn back off. `status()` remains the one method left open, because it is read-only and spawns/kills nothing. |
| D509 | **`background_apps.py`'s router `_folder_for` (`server/routers/background_apps.py`) now realpath's the resolved app folder, not just abspath's it — matching `engine_id_for`'s existing realpath-based identity.** | Code review (2026-08-26) flagged that `enabled` (`status()`'s `folder in enabled_paths()` check, both abspath-only) and `running` (keyed off `engine_id_for`'s realpath hash) used two different identity functions for the same folder. Verified against the code, not assumed: a folder reached through a symlink alias produced a genuine divergence — `enable()`d via one alias, `status()`'d via the other (or the real path) reported `{"enabled": false, "running": true}`, an impossible fact for one app; `enable()` through one alias and `disable()` through another wrote/removed two separate `background_apps.json` entries for what is really one folder, leaving one alias "on" after the other was turned "off." Realpath'ing `_folder_for` — the one place every endpoint (`status`/`enable`/`disable`/`stop`/`restart`) derives folder identity from `html` — makes it agree with `engine_id_for` everywhere at once, the same normalization `background_apps.load_manifest`'s own daemon-containment check already applies. Rejected: normalizing inside `background_apps.enabled_paths()`/`set_enabled()` instead — that file's abspath choice is intentional for the store's on-disk format (a symlink resolving differently on a future run would silently rewrite stored paths), so the fix stays scoped to the router's own folder-identity function, which is where the actual divergence originated. |
| D510 | **`POST /api/apps/background/restart` recomputes the version digest fresh on every call, including when a child is already live, and passes it into `engine_host.restart`'s new optional `version` parameter; `restart()` still defaults to the existing child's own version when no override is given.** | Code review (2026-08-26): the live-child branch used to call `engine_host.restart(engine_id)` bare, which rebuilds the replacement `Child` from `existing.version` — the digest computed whenever THAT child last came up, not now. `fused.daemon.restart()` called right after editing `daemon.py` would respawn the new code but tag it with the OLD version string; the next `enable()` or server-start resurrection then computes the current digest, `_matches` fails against the mismatched version, and the just-restarted child gets torn down and respawned a SECOND time for no reason the caller can see. `engine_host.restart` grew an optional `version` override rather than always recomputing internally, because `restart`'s other caller — `engine_forward.py`'s heal-on-proxy path — wants the OPPOSITE default: a healed child should keep answering to the version its caller already has cached, not silently pick up a new one mid-heal. Routing the live-child branch through `ensure_background` instead (as the fallback branch already does when there's no live child) was considered and rejected: `ensure_background` has no replay step, so a live-child restart taking that path would silently stop replaying `_reinit` requests, a regression `restart()`'s own docstring calls out as deliberate behavior. |
| D511 | **`enable`/`disable` are removed. Run state and autostart become independently settable: `POST .../start` spawns now, `POST .../stop` kills now (unchanged), `POST .../restart` respawns, `POST .../autostart` (body `{"html", "autostart": bool}`) is the ONLY thing that persists the "bring this back at every launch" flag — none of the run-state endpoints touch it. `status()` reports `autostart` as its own field alongside `running`. Autostart defaults to off and stays off unless a caller explicitly calls `autostart`/`setAutostart(true)`; `engine_host._validate_background`'s daemon-identity check no longer walks the autostart store (it now validates the daemon against its own folder's manifest, independent of any store).** | The user-visible bug: OpenWhisper's tray "Quit" and "Turn Off" looked identical when clicked (both just made the app vanish) and differed only invisibly at the next server start — because `enable()` conflated "start it" with "keep it running forever" and `disable()` conflated "stop it" with "never bring it back," so nothing could change either fact alone. Splitting them is the point of this decision, and opt-in is the point of the split: if `start()` silently set autostart the way `enable()` used to, every "just try it once" call would install a permanent daemon, exactly the invisible-persistence bug this decision exists to remove. This is why `_validate_background` had to change in the same commit, not as an unrelated cleanup — it used to require the daemon's folder to appear in the (then autostart-only) store as a precondition for spawning at all, which is backwards once autostart is opt-in: `start()` on a folder nobody has ever configured must still succeed. Deriving the folder from `os.path.dirname(daemon)` and checking that folder's OWN manifest declares that exact daemon file keeps the same anti-tamper property (the caller cannot hand over an arbitrary file pretending to be a background app's daemon) without any dependency on which apps happen to be in the autostart list. No back-compat aliases for `enable`/`disable`: this feature was unmerged (PR #852) at the time of this decision, and the only caller (`Sina/OpenWhisper`) was updated in the same change. |
| D512 | **The autostart store (`background_apps.py`'s `autostart_paths()`/`set_autostart()`) now normalizes paths with `os.path.realpath`, not `os.path.abspath` — the half of D509's fix that decision explicitly deferred.** | D509 fixed the router's `_folder_for` to realpath (matching `engine_id_for`) but left the store layer on abspath, and gave a specific reason: "that file's abspath choice is intentional for the store's on-disk format (a symlink resolving differently on a future run would silently rewrite stored paths)." This decision overrides that stance rather than merely revisiting it — the risk D509 named (a symlink target moving between server runs, silently rewriting a stored entry) is real but narrow and self-healing (the entry just points at wherever the symlink now resolves, which is the whole point of following it), while the cost of leaving it unfixed is the identical divergence bug D509 fixed for the router recurring at the store layer: `engine_id_for` (realpath) and `autostart_paths()` (abspath) could disagree about whether a symlinked alias and its target are "the same" folder, so `set_autostart(True)` through one alias and a `status()` read through another could report `{"autostart": false}` for an app that IS opted in, or the reverse. Consistency with `engine_id_for` — the identity function every other piece of this feature already keys off — wins here for the same reason D509 originally realpath'd the router: one folder must mean one autostart fact, full stop, and every place that computes folder identity has to agree on how. |
| D513 | **`engine_host._validate_background` now takes the declaring folder as an explicit argument and `ensure_background` passes its own `folder` parameter through to it, instead of `_validate_background` re-deriving the folder as `os.path.dirname(daemon)` unconditionally. `folder` stays optional on both functions, defaulting to `""`, which falls back to `os.path.dirname(daemon)` — it is NOT made required.** | D511's rewrite of `_validate_background` (self-contained folder-manifest check, no store lookup) re-derived the folder from `daemon`'s dirname, which is wrong whenever a manifest's `daemon` names a NESTED path: `background_apps.load_manifest` only enforces containment within the folder, not flatness, so `daemon = "src/daemon.py"` is a legal manifest, and `dirname(daemon)` for such an app is `<folder>/src` — a subfolder with no `pyproject.toml` of its own. `load_manifest` there returns `None`, so `_validate_background` raised `EngineError` on every bring-up; the app could never be started. `ensure_background` already receives the correct folder from its caller (threaded onto `Child.folder` for `FUSED_RENDER_APP_DIR`) — it just wasn't forwarding it to the one place that needed it for validation. Kept `folder=""` optional rather than making it required, even though `ensure_background`'s own docstring says "every production call site does" pass one (verified true: `background_apps.py`'s `resurrect_autostart` and both call sites in `routers/background_apps.py`'s `start`/`restart` all pass it) — several existing tests deliberately call `ensure_background` without a `folder` to pin down the "no self-addressing env var" default behavior (`test_ensure_background_without_folder_defaults_to_empty_string`, the three `test_ensure_background_rejects_*` tests, `test_ensure_background_spawns_and_reuses_the_fixture_daemon`), and all of those use a flat layout where `dirname(daemon)` already equals the intended folder — so the fallback preserves their behavior exactly while fixing the nested case. Making `folder` required would only formalize what is already true in production and break nothing there, but it would force every test that doesn't care about `FUSED_RENDER_APP_DIR` to start passing a folder anyway, for no behavioral gain — the fallback costs nothing a required parameter would have bought. Also fixed `ensure_background`'s docstring, which still said the child was "validated against the enabled store" — stale since D511 removed that store lookup entirely; it now says what actually happens (validated against the daemon's own declaring folder's manifest). |
| D499 | **A task can carry images: the New task form attaches by drop, paste (either field) or picker, the bytes are stored ONCE under `~/.fused-render/task-shots`, and what rides the wire — and the scheduled prompt — is PATHS the run is pre-allowed to Read.** | The claude template has had pictures since the annotation work, and a task is the same kind of thing said later, so the gap was the form and not the model. **Paths, not base64**, and that is the whole shape: the store is a JSON file a human may read, a repeat re-sends its images on every run, and an occurrence copies the template's fields — inline bytes would put megabytes in the store, re-encode them per run and bloat every list response, while a path costs the same on run 1 and run 400. It is also exactly what the template already does with annotation crops (`SHOTS` + a pre-allowed `Read(//dir/**)` rule), so the agent side needed no new idea: `agent._start` grew `extra_read_dirs`, `claude_spawn.spawn_helper` passes them through, and `_send` names the task-shots dir **only for a run that actually has images** — a directory rule on a run with nothing to read there is standing permission for no reason. That conditional is load-bearing for a second reason found by the suite: passing the kwarg unconditionally broke every `fake_spawn(target, prompt, permission_mode, session_id="")` double in the repo, and because `_send` catches a bad spawn into `_fail`, the symptom was a **`failed` event on a task with nothing wrong with it** — 20 red tests in `test_task_sync_matrix` whose message never mentioned images. Storage is `~/.fused-render/task-shots`, deliberately NOT the template's tempdir-rooted `shots` (12-hour TTL, 200-file cap): an annotation is junk once its turn is over, but a task scheduled for Friday must still find its picture on Friday. The upload endpoint is the ONLY writer, and `schedule._images` refuses any path that is not a **realpath** resident of that dir — string-prefix membership would let a symlink dropped in there point `images` at any file on disk. **UI: minimal, not the template's pill.** The first cut ported `.annchip` verbatim (🖼 + thumb + the word "image" + ✕) and read as debris on a 460px card — three pills of chrome to say "three pictures". What the card keeps is the AFFORDANCE, not the dressing: a bare 36px thumbnail with the ✕ inside its own corner on a scrim plate, and the viewer the pill's thumbnail opened (`#shotview` ported — fitted, click to natural size with the box scrolling, Escape captured so it closes the picture and not the task). Paste is bound to BOTH text fields, because a screenshot pasted on the title is as deliberate as one pasted on the description, and only a FILE paste is intercepted so ordinary text pastes stay what they were. | Akshil, 2026-08-25/26: "in claude template we can add images, let's support image drag drop and attach in tasks as well"; then, on the first cut, "one of them looks very clean, one of them looks off place — what I would have is just the image and the x icon on it, but when you click on the image, it shows me a preview". |


### `fused.daemon.watch()` — visibility-aware polling over a push channel (2026-08-26)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D515 | **`fused.daemon.watch(callback)` (`static/runtime.js`) is `status()` wrapped in a poll loop, not a new transport: it calls back on the initial read and again whenever `{running, autostart, pid, version}` differs from the last-seen status, polls only while `document.visibilityState === "visible"` (a 5s interval), refreshes immediately on `visibilitychange`→visible and window `focus`, and returns an unsubscribe function. In a preview thumbnail it does one `status()` read and a no-op unsubscribe — no timer, no listeners, matching D507/D508's stance that `status()` is the one `fused.daemon` method left open there. No WebSocket or SSE channel was added.** | The gap: quitting OpenWhisper from its tray routes through the same `POST /api/apps/background/stop` the page's own `stop()` button does, so the server knows the daemon died — but nothing told the PAGE, and its mic icon stayed stale until a manual reload. A page that only refreshes after its own `start()`/`stop()`/`restart()` calls has a structural blind spot for any OTHER surface (a tray, another tab, the server's own startup resurrection) changing the same fact. Polling beat a push channel on the actual failure mode, not just on implementation cost: **a crashed or externally-killed daemon notifies nobody either way** — there is no server-side hook on an OS-level process death to push FROM, so a push channel would only ever cover the subset of state changes that go through the server's own API (`stop`/`start`/`restart`/`autostart`), the same subset polling already catches on its next tick or its immediate focus/visibility refresh. A push channel earns its cost when the STAYING-STALE window matters (seconds count, e.g. a live collaborative cursor) — here the two refresh triggers that matter (coming back to the tab, the tray action itself) already fire near-instantly, and the remaining gap (an already-open, already-focused tab, not polling by definition since it never went hidden) is the same few-second window either transport would leave for a background tab regardless. Visibility-gating is what makes polling *cheap enough to default on*: `document.visibilityState === "visible"` stops the timer entirely for a hidden tab (idle background tabs cost nothing, not "a slower interval"), and `status()` itself is a single in-memory dict read plus one `Popen.poll()` server-side (`GET /running`'s D-numbered sibling fix, same commit) — 5s is fast enough to read as "immediate" to someone glancing at a foregrounded tab and cheap enough to run unconditionally. Diffing on `{running, autostart, pid, version}` rather than firing on every tick keeps a page's UI from re-rendering on a no-op poll; `pid`/`version` are included (not just the two UI-facing booleans) so a same-`running` restart — a crash-and-resurrect, or a restart from elsewhere — still notifies a page holding a cached `call()` target. `engine_id` is excluded: it is a hash of the FOLDER, stable for the page's whole lifetime, and would never fire on its own. |
| D516 | **An installed plugin's row EXPANDS into what it puts in a session: a new `plugins.contents` action resolves the plugin's own `installPath` out of installed_plugins.json and reads its skills, commands, agents, hook events and MCP servers off disk — honouring the paths `.claude-plugin/plugin.json` may relocate them to — and the panel links each entry to the file that declares it, opened in FusedRender. Read ON EXPAND, one plugin per call. `_parse_frontmatter` moves from `claude_config/skills.py` to `claude_config/lib.py` as `parse_frontmatter`, now that two features read the same block.** | The Installed list could say a plugin was there, what version it was and whether it was on — and nothing whatsoever about what it DOES, which is the only question you have in front of twelve rows of it. That was not an oversight but a limit of the source: `plugins list` reads settings.json and installed_plugins.json, and neither file knows a plugin's contents, which is exactly why the row had no chevron (its own comment said so). The contents are on DISK, one `installPath` away, so the fix is a second source rather than a richer read of the first. **On expand, not with the list**: filling this in for every row would walk a dozen third-party trees (context-mode ships node_modules) on a page most visits never expand a row on — so the panel is a component that fetches at MOUNT, and `ListRow` only mounts `details` when open, which makes laziness a property of the existing layout rather than a new mechanism. **The manifest is third-party content and its path values become directories we walk**, so each resolved path must stay inside the plugin root (`../../outside` yields nothing, the same lexical boundary `lib.safe_subdir` draws) and the walk is depth- and count-bounded with node_modules and dot-dirs skipped by name. A named dir REPLACES the conventional one rather than adding to it, matching what the CLI does. **Degraded reads stay stated, never empty**: a torn hooks.json keeps its row and opens the file that needs fixing, a malformed plugin.json costs only the non-default paths (tolerantly read HERE, unlike `lib.read_json`, whose raise-on-malformed rule is about the user's OWN config), and an id recorded but whose files are gone answers "reinstall it" rather than "not installed" — a different fix. **Entries are anchors, not click handlers**: an entry IS a file, so it gets middle-click, cmd-click and a copyable target for free; a new tab because this page holds unsaved-ish state (an open filter, a half-typed marketplace form) that navigating away would lose. Keyed by plugin id rather than by a path from the browser, so the action cannot be pointed at an arbitrary directory — the same membership guard `update` applies. Surya, 2026-08-26: "can we make the plugins act as a dropdown so that we can see stuff like skills/hooks etc of the plugin?" |
| D517 | **The Claude Code setup strip APPLIES the three fixes it can apply instead of printing them: `POST /api/claude/install` runs the platform's native installer as a background job (`claude_install.py`), `POST /api/claude/doctor` runs `claude doctor` and surfaces its warning/fix pairs verbatim, and the update offer is GATED on `claude update` being expected to change anything. The snapshot gains `broken`, `platform`, `install_command`, `install_method`, `updatable`, `update_command` and `doctor`; `_CACHE_VERSION` goes to 3** | D-claude-health established what is wrong with Claude Code on this machine and established it well — five resolution strategies, a version floor, `auth status`, a fingerprinted disk cache — and then ended every finding the same way: a sentence telling the user to open a terminal. On a Dock-launched app that is the app knowing the answer and asking someone else to apply it. Three of its findings are mechanisable and are now mechanised. **(1) Not installed** → the native installer, run through a thread (not `envinstall`'s detached worker: the whole value is the re-probe at the end, which has to happen in the server process because that is where the health cache and the `FUSED_RENDER_CLAUDE_BIN` adoption live — a detached child would finish with nobody to tell), reporting into `jobs.py` so it stays visible after the user navigates away. No restart is needed afterwards and that is not luck: the installer's target (`~/.local/bin`) is already first in this module's candidate list, so a forced re-measure resolves it as `source: "candidate"` without the app's PATH ever changing. An install that exits 0 and leaves nothing runnable is reported as a FAILURE, because reporting success while the strip re-renders "can't find Claude Code" would be the app saying two contradictory things in one second. **(2) The install is broken** — resolved, runnable, and it will not report a version — was measured all along and said NOTHING, which was correct (one failed probe is not a diagnosis) and left a user with a dead app and no sentence anywhere. `claude doctor` is the way out precisely because the diagnosis is then the CLI's own: it is read-only, starts no session, and prints `Running: <method>` plus `- problem` / `Fix:` pairs, all of which `parse_doctor` takes verbatim. **(3) Too old** → `claude update`, but ONLY where that does something. Homebrew, WinGet, apt, dnf and apk own their own binary and answer `claude update` with "Claude is up to date!" while changing nothing, so `update_plan` returns `updatable: False` for them and the UI renders the owning manager's real upgrade line with no button at all; `DISABLE_UPDATES` (which blocks manual updates, unlike `DISABLE_AUTOUPDATER`, which does not) withholds it too; and `start("update")` refuses server-side with a 409 whose text names the command that would work, so the guard holds even if a caller ignores the flag. **`updatable: None` still offers the update**, which is the same rule `signed_in` has always followed: only an authoritative negative may withhold an offer, and not knowing the install method is no evidence against `claude update`. Doctor is GATED to `broken or outdated` — a ~1.2s spawn is fine to pay for a card that renders while something is wrong and is not fine on every health read of a healthy machine, the same argument the endpoint already makes for staying off `/api/config`. Two scars in the path sniffer, which is the fallback for when doctor could not be asked: `/bin/` as a SUBSTRING matched almost every path a binary sits in and classified `/opt/node22/bin/claude` (npm, updates itself fine) as a system package, withholding its update offer — the generic needles are anchored to the start of the path now — and `/usr/local/bin` is deliberately absent from every list, being a common npm prefix, an Intel-Mac Homebrew link target and a hand-install location at once, so unknown is the honest answer there. Also fixed in passing: `claudeIssues` attached the macOS/Linux `curl … | bash` line to every `missing` card regardless of platform, so a Windows user with no Claude Code was handed a command their shell cannot run; the server now states `platform` and `install_command` and the UI stops guessing. Deliberately NOT automated: the dead-`FUSED_RENDER_CLAUDE_BIN` case keeps its no-button sentence, because the value lives in a shell profile or launchd plist we cannot durably edit and `adopt()` already refuses to overwrite a setting the user made — a button that silently does nothing is worse than the sentence it replaces. Tests: `test_claude_install.py` (the argv per platform, every refusal, verbatim child output, the exit-0-but-nothing-installed case, the tail cap, the timeouts, the endpoint guards) plus `test_claude_health.py`'s new doctor/method/update-plan block, including the anchoring regression; frontend `claude-health.test.ts` pins which rows get a button and — the one that matters — which must not. |
| D518 | **A model's `config.json` is harvested from the Hub and cached with a 13-day TTL (`fused_render/ai/hub_metadata.py`, `~/.fused-render/ai_hub_metadata.json`) — architecture facts (layer/head geometry, vision-tower presence, quant method, hybrid-attention layer list) for a repo that has not been downloaded at all, in the `bench_store.py`/`footprints.py` store idiom but NOT machine-scoped, since a `config.json` describes the model rather than the machine reading it. A stale entry is served, not discarded, when a refetch fails. (SPEC AI-17)** | `fit`'s KV-cache term and `hub_cache.has_vision_tower` both need a checkpoint's architecture, and the only place that lives before a download is `config.json` on the Hub — `has_vision_tower` can only read a snapshot already on disk, which is exactly the gap a Hub SEARCH result falls into: the AI Models page has to answer "will this fit, does it take images" for a repo the user has not pulled a single byte of. A `config.json` is a few KB with no weights, cheap enough to fetch for every row a search returns. Not machine-scoped (unlike `footprints.py`/`ai_footprints.json`, whose whole file is discarded on a machine-identity mismatch): a model's architecture does not change when the home directory moves to a new laptop, so discarding the cache on that event would be throwing away good data for no reason tied to what this store actually records. Serving a stale reading on a failed refetch (rather than falling through to "no metadata") is deliberate asymmetry: the "network failure degrades silently" rule is about a repo never seen before, and regressing an ALREADY-ANSWERED repo to blank on a transient DNS hiccup would make the page worse than doing nothing on that refresh at all. |
| D519 | **Per-device VRAM, a device name, multi-GPU aggregation, and a name -> memory-bandwidth table (`fused_render/ai/hw_detect.py`, `~/.fused-render/ai_hardware.json`) — detected via a subprocess probe (`nvidia-smi`/`rocm-smi`/PowerShell WMI+registry/`sysctl`) that never runs on the verdict path: `detect_hardware()`/`refresh_hardware()` do the (50-500ms) probing and persist the cache, `cached_hardware()` is a plain `storage.read_json` and the only function `fit.py`/`benchmark.py` may call, a boundary a source-grep test pins. Covers Windows' 32-bit `AdapterRAM` cap (corrected via the display driver's own `HardwareInformation.qwMemorySize` registry value, not `Win32_PhysicalMemory`) and unified-memory APU overrides (AMD Ryzen AI Max/Strix Halo, NVIDIA Grace/DGX Spark) to full system RAM. (SPEC AI-18)** | `fit.py` judges every footprint against system RAM, which is right for CPU and Apple-Silicon unified memory and silently wrong on a discrete CUDA/ROCm box, where the pool that actually holds the weights is VRAM — a different, usually much smaller number. Detection has to be a subprocess probe (there is no cheap syscall for "how much VRAM does this card have" the way `iogpu.wired_limit_mb` is one read on Apple Silicon), and `fit._wired_limit_mb`'s own docstring already states why a probe that slow cannot sit on a route the picker polls per catalog row — so this module draws the same split at a coarser grain: the slow probe writes a cache, and the verdict path only ever reads it. The Windows correction targets the REAL defect (`AdapterRAM` is a `uint32`, so a >=4GB card wraps or caps near 4GB) rather than reaching for `Win32_PhysicalMemory`, which reads total SYSTEM ram via SMBIOS and is a different quantity entirely — conflating the two would silently misreport VRAM as whatever RAM happens to total, which is worse than leaving the capped reading alone. Unified-memory APUs report a tiny BIOS-assigned carveout as "VRAM" (~0.5-1GB) rather than the real shared pool a driver can allocate from, which is the whole of system RAM — detected by name and overridden, mirroring how Apple Silicon is already treated. |
| D520 | **`fit.py`'s `download` rung gets a flat 0.5GB runtime-overhead constant, a quantization-aware `params x bytes_per_param` weight-size table (parsed off `catalog.py`'s free-text `quantization` string), a KV-cache term (fixed 8192-token estimation context, hybrid/Mamba-aware via `layer_types`), and VRAM-vs-RAM pool selection with a `gpu`/`cpu-offload`/`cpu-only` run mode read from **AI-18**'s `hw_detect.cached_hardware()`. The old `EASY_FRACTION = 0.6` step function is replaced by a continuous Gaussian fit score (`COMFORT = 0.70`, `SIGMA = 0.20`), which the three-way verdict is now derived from and which the response now also exposes directly. (SPEC AI-19)** | `measured` and `declared` are already real, observed numbers, so all four additions land ONLY on `download`, the one rung this module has ever had to guess at — adding overhead or a computed KV term on top of an observation that already includes whatever the model actually used would double count it. The weight-size table is the fix for the bug class `catalog.py:52-58` documents (an 18.6GB actual download hand-curated as 2.6GB for eighteen months): nothing had ever multiplied a real parameter count by a real per-format byte cost, only typed a headline quant label's byte-per-8-bits guess. The KV term is fixed at 8192 tokens rather than a model's advertised maximum because the advertised figure (often six figures) wildly overestimates the context an actual session opens at — the same reasoning the comparative study this build derives from states for its own equivalent constant. `head_dim` is derived here (`hidden_size / num_attention_heads`) rather than at harvest time, per **AI-17**'s own docstring, since guessing at harvest would conflate "the Hub did not publish this" with "we computed a plausible number" in the one store meant to hold only what the Hub actually said. Pool selection preserves the one property SPEC AI-19 explicitly must not regress: Apple Silicon's pool stays system RAM unconditionally and the **AI-16b** wired-limit hard ceiling — checked first, entirely independent of pool selection — remains the machine-specific cap that governs there; the comparative study detects an Apple VRAM-alike figure and then never uses it as a ceiling, and this codebase does not repeat that. The Gaussian score removes a real discontinuity in the old rule (every ratio under 60% scored identically, every ratio from 60-100% scored identically) that a UI progress bar could not render honestly; `score == 0` is a hard floor at `footprint > pool` rather than left to the exponential's own tail, since a Gaussian never reaches exactly zero and a footprint that provably does not fit must not keep a small positive score. `frontend/src/apps/ai_models/shared/fitNote.ts`'s copy table is keyed on `basis` + `verdict`, and neither gained a new value, so the copy itself is unchanged — only its header comment and `AiFitVerdict`'s TS shape (two new optional fields) were touched. |
| D521 | **`catalog.py`'s free-text `params` field (`"1.2B"`, `"39M"`, `"8B (~1B active)"`, `"4B effective"`) is parsed by `fit.parse_params` and wired live into the production caller (`ai_runtime.describe_catalog` now passes `entry.get("params")`/`entry.get("quantization")` straight to `fit.verdict`), completing SPEC item 4's `params x bytes_per_param` weight-size estimate rather than leaving it an unreachable code path. An MoE row's `"<total>B (~<active>B active)"` form parses the LEADING (total) figure — resident weight bytes scale with total parameters, not the active-per-token count, since inactive experts are ordinary tensors on disk and in memory. A `"<N> effective"` row (Gemma's MatFormer "E4B" naming) answers `None` outright rather than parsing the one number present — verified against `mlx-community/gemma-4-e4b-it-4bit`'s own curated `size_gb` (5.2GB vs. a literal-4B estimate of 2.2GB, under half), "effective" names a compute-quality-parity figure with no raw/total count in the string to recover, unlike the MoE form which states both. (SPEC AI-19 item 4)** | Without a `params` parser, item 4's own point — deriving weight size from `params x bpp` instead of a hand-curated `size_gb` — never fired for any real catalog row: every curated entry's `params` is this exact free-text shape, and `fit.py`'s `params` kwarg only ever accepted a raw numeric count nothing in this codebase produces. That is the same failure mode as the dead `resident_gb` rung this whole overhaul exists to fix — green tests over a code path no real object reaches. The MoE-vs-"effective" split needed to be an explicit choice, not an accident of regex ordering: both strings put a number right next to a unit, and a naive "grab the first number" parser would have silently taken the WRONG one for "effective" rows (verified: literal-4B estimate is under half the curated `size_gb` for the one real "effective" row in the catalog) while happening to take the RIGHT one for MoE rows only because the total is written first. Verifying against real catalog rows (not synthetic fixtures) surfaced two genuine divergences — `prism-ml/Ternary-Bonsai-27B-mlx-2bit` (ternary quantization has no row in item 4's table, falls to a ~4-bit-sized default, ~2.57x over) and `tonera/FLUX.2-klein-4B-int8-diffusers` (`"int8 (torchao)"` matches no table pattern, AND `params` counts only the transformer component of a multi-component diffusion pipeline while `size_gb` counts the whole download) — reported and regression-locked by their own test rather than papered over with a wider tolerance band, per the standing rule that a large divergence is a finding, not something to smooth away. |
| D522 | **`fit._weight_bytes` no longer lets a derived `params x bpp` estimate override a real, curated `size_gb` when the `quantization` string was NOT recognized in `QUANT_BYTES_PER_PARAM`. The derived estimate wins only when the quant IS recognized; an unrecognized quant with a real `size_gb` uses it exactly; a defaulted `params x DEFAULT_BYTES_PER_PARAM` guess is used only when there is no `size_gb` at all.** | D521 wired `params x bpp` in live, and a code-review pass on it caught that the wiring shipped an unconditional precedence: `parse_params` succeeding was enough to override `size_gb`, regardless of whether `quant_bytes_per_param` had actually matched anything or had silently substituted its default. Both of D521's own investigated rows parse `params` fine, so both would have shipped a wrong number in production — `prism-ml/Ternary-Bonsai-27B-mlx-2bit` at 15.66GB against a catalog-confirmed-accurate 6.1GB (pushes a comfortably-fitting model's verdict toward tight/no), and `tonera/FLUX.2-klein-4B-int8-diffusers` at 2.32GB against a real 8.2GB (under-reports by 3.5x, the more dangerous direction — a model that does not fit reads as though it does). A defaulted bpp carries no evidentiary basis for what a SPECIFIC checkpoint costs; `size_gb`, when a curated row has one, is a real number — verified for both rows here by their own catalog.py comments (one states its figure is "what the completed download MEASURES on disk", the other "the whole repo" per Hub metadata). This is the same "measured beats guessed" precedence `footprint_bytes`'s `measured > declared > download` ladder is already built on, applied one level down inside the `download` rung's own arithmetic. The fix is a general property (recognized-vs-unrecognized quant), not two by-id exclusions: the earlier verification test's `_KNOWN_DIVERGENT_ROW_IDS` by-id skip list is replaced with two tests that check the RULE across every curated row — a recognized-quant row's estimate is checked against a sane band, an unrecognized-quant row is checked to equal `size_gb` exactly — plus a named regression lock on the two investigated figures so a future re-divergence on either specific row is still caught by name, not just by the general property. No ternary or int8 bytes-per-param figure was added to `QUANT_BYTES_PER_PARAM`: the fallback rule makes inventing one unnecessary, and adding an evidence-backed row for either format later is a separate change. (SPEC AI-19 item 4)** |

### AI fit overhaul, Phase 2 — provenance and measurement (2026-08-27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D523 | **`benchmark._memory_and_device`'s single post-timed-pass reading is replaced by `_PeakSampler`, a background thread sampling on a 0.25s cadence DURING the timed pass and keeping the running max. `benchmark._total_memory_bytes()` gains the `GlobalMemoryStatusEx` Windows fallback `fit.machine_ram_gb` already has. (SPEC AI-20)** | A reading taken only after the pass ends misses a decode's own KV-cache high-water mark (never freed mid-generation, so its peak is near the LAST token, and can already be superseded by whatever the process does immediately after `run()` reads memory) and a diffusion step's cross-attention scratch (freed the instant the step that allocated it returns, so a post-render reading sees none of it). The sampler always takes two samples independent of its own background cadence — one in `start()` before the pass, one in `stop()` after — so a pass shorter than one tick (every workload in this codebase's fake-clock tests) still beats the old design's single reading, and the RUNNING MAX rather than the last value is kept, so a mid-pass spike that has since subsided still wins. `stop()` is idempotent by construction (a re-join of an already-finished thread is a no-op, and one more harmless sample), which `run()`'s structure requires: the success path collects the result and the surrounding `finally` tears the thread down unconditionally, so a raise mid-pass cannot leak it. `_total_memory_bytes()` was ported rather than shared with `fit.machine_ram_gb`, which returns decimal GB cached forever via `functools.lru_cache`; this one returns raw bytes, uncached, fresh per `machine()` call — sharing one function across both units and both caching policies would be the wrong direction to unify in. |
| D524 | **`fused_render/ai/speed.py` estimates tok/s as `bandwidth_GB_s / weight_gb x 0.55`, falling back to a flat per-backend constant when `hw_detect.cached_hardware()` reports no bandwidth for its device. `weight_gb` is a new public `fit.weight_bytes` wrapper over `fit._weight_bytes` — the same figure the `download` rung's own arithmetic already computes — and every estimate records `method`/`backend`/`bandwidthGbS`/`contextTokens` as its basis, not a bare number. Backend inference (`backend_bucket`) is machine-level (cached hardware name + platform architecture), not runner-aware — `sycl` and `metal-other` stay in the constant table but are unreachable by this module's own inference (no Intel probe exists; this codebase resolves no non-MLX Metal runner). Wired into `ai_runtime.describe_catalog` as `entry["speedEstimate"]`, `text-generation` entries only. (SPEC AI-21)** | The KV-cache footprint work (AI-19) already answers "will this fit"; nothing in this codebase answered "how fast, once it does" with any stated basis. The formula matches the comparative study's own equivalent — a bandwidth-bound decode loop reads roughly one weight-sized pass per token, so tok/s scales with how many such reads the memory bus completes per second. `weight_gb`, not `fit.footprint_bytes`'s fuller figure, because the KV term and flat runtime overhead are real memory the model occupies but not bandwidth the decode loop repeatedly reads through — including them would understate the true bandwidth-bound rate. Runner-aware backend selection (reading which ACTUAL runner will load a given model, e.g. via `registry`) was considered and deferred rather than built speculatively: no caller in this codebase threads a runner code through to any of `fit.py`/`speed.py` today, and a documented, honestly-labelled machine-level guess (`method`/`backend` on every response) is a better answer than inventing that wiring unasked. Text-generation only in the router, because the OTHER three capabilities report `secondsPerStep`/`realtimeFactor`/`textsPerSecond` — a bare tok/s number under an image or speech row would be actively misleading, not merely unavailable, so those entries get `speedEstimate: null` by construction rather than a number under the wrong unit. |
| D525 | **A local calibration factor — `median(measured_tok_s / uncalibrated_estimate_tok_s)` over `bench_store.py`'s own `text-generation` runs, anchored on `params_b >= 1.0 and not is_moe`, clamped to `[0.05, 3.0]`, persisted at `~/.fused-render/ai_speed_calibration.json` — is applied to every `estimate_tok_s` call once one exists. Idempotent BY CONSTRUCTION: `recalibrate` computes every ratio against the raw, uncalibrated formula (`_uncalibrated`), never against `estimate_tok_s`'s own possibly-already-calibrated output. (SPEC item 11)** | SPEC item 11's own required property — repeated application must stop moving, not compound — has two possible designs: an explicit "divide the previously applied factor back out of the denominator before computing a new one" step, or simply never letting the old factor into the computation in the first place. This module takes the second: `recalibrate`'s ratios are always `measured / _uncalibrated(...)`, and `_uncalibrated` by construction never reads `stored_calibration_factor()` — so there is nothing to divide out, because there was never anything to divide out OF. Verified directly (`test_recalibrate_is_idempotent_across_repeated_calls`): three consecutive `recalibrate()` calls over identical `runs`/cached-hardware evidence return the identical factor each time, not `0.6`, `0.36`, `0.216`. The billion-parameter/non-MoE anchor rule (llmfit's own) excludes exactly the regime the bandwidth formula does not model — a small model's tok/s is dominated by fixed per-call overhead the formula ignores entirely, so including it would calibrate against the wrong thing. A recalibration with no anchors leaves the STORED value untouched rather than resetting it (`test_recalibrate_with_no_anchors_leaves_the_stored_value_untouched`) — an empty batch (a Benchmark tab session with nothing new, or every run filtered out) must not silently erase a real prior calibration. |
| D526 | **`resident_gb` — plumbed since AI-16, set by nothing — is populated for exactly two curated rows, `dgrauet/ltx-2.3-mlx-q4` (20.5) and `dgrauet/ltx-2.3-mlx-q8` (29.8), computed as `max(weights bytes, gemma bytes) / 1e9` off the exact Hub-measured figures `catalog.py` already states above them, and deliberately left absent on every other row. (SPEC AI-22)** | `ltx-video` is the row `fit.py`'s own module docstring names as the motivating case for the `declared` rung: `DistilledPipeline(low_memory=True)` frees the transformer and Gemma-3 text encoder between stages, so the true resident peak is the larger single stage, never the `size_gb` sum both downloads fetch. Every other curated row is a pipeline whose components stay resident together for the whole run, so `size_gb` there already equals the true resident footprint — a `resident_gb` on any of them would not be better evidence, only a second, driftable copy of the same number. Deriving `resident_gb` from the `download` rung's own `params x bpp + KV + overhead` arithmetic (AI-19) for every row was considered and rejected on the same evidentiary-quality standard: writing that computed result into `catalog.py` as a static field would not add information the `download` rung does not already compute live, would go stale the next time `fit.py`'s own constants change, and would wear the `declared` label's implicit "a curator confirmed this" claim without having earned it. A regression test (`test_no_single_stage_pipeline_entry_declares_a_resident_gb`) pins the absence on every other row, so a future addition has to argue the case in words rather than add a plausible number by habit. |
| D527 | **`fused_render/ai/gguf_sources.py` probes `QUANTIZER_NAMESPACES` (`unsloth`, `bartowski`, `ggml-org`, `TheBloke`, `mradermacher`, most-active-first) for `{provider}/{basename}-GGUF`, verifies a hit against the candidate's own `cardData.base_model` tag, and falls back to +/-30% parameter-count similarity ONLY when no tag is present — an explicit tag naming a different repo disqualifies the candidate outright rather than being rescued by a coincidental param match. Cached 7 days at `~/.fused-render/ai_gguf_sources.json`, `hub_metadata.py`'s store idiom, not machine-scoped. `catalog.COUNTERPART_IDS` is left untouched. (SPEC AI-23)** | The build spec named `COUNTERPART_IDS` as this item's migration target, but auditing it (both its live content and its git history) found it has only ever mapped a curated id to the SAME checkpoint in a different EXPORT FORMAT (ONNX) for the two ids the torch-runner removal orphaned — a different question from "which GGUF quantization exists", and one its own docstring explicitly disclaims generalizing ("two lines of data cannot invent ids for every other repo"). Migrating it into a GGUF-namespace resolver would have repurposed a table for a claim its own comments reject; building `gguf_sources` as new, additive capability keeps both tables honest about what each one actually asserts. The base-model-tag-is-decisive rule (never falling through to param similarity once a tag names something else) exists because the ONE failure this module has to avoid is pairing two same-named, unrelated checkpoints — an existence check alone would do exactly that, and a tag that names the WRONG source is stronger negative evidence than a coincidental size match is positive evidence. |
| D528 | **`formats.select_gguf_recipe(files, budget_bytes, params=None)` ranks a repo's own listing against `GGUF_QUALITY_ORDER` (`Q8_0` down to `IQ1_M`, most-to-least-fidelity) and returns the best-quality candidate whose SUMMED size (shard sets collapsed to one candidate first) fits `budget_bytes`, or the single best-quality candidate when `budget_bytes` is None. `pick_gguf_file` (D412) is left untouched. A file with no known size is estimated as `params x fit.quant_bytes_per_param(token)`, `fit`'s bpp table imported lazily. (SPEC AI-24)** | Item 14 asks for budget-aware selection "sharing, not duplicating" item 4's bpp table, but `formats.py` is loaded as a bare stdlib-only module by a worker's interpreter (no `fused_render` package on `sys.path` there), so a module-level import of `fit` would break every worker that imports `formats` at all — the import is deferred inside the function body, which no worker code path calls. `pick_gguf_file`'s own module note gives a considered, still-valid reason to keep it hardware-blind (a model id has to mean the same bytes on every machine, and `llama_text._offload_schedule`'s backoff makes an over-large default pick affordable) — reopening that decision was out of scope here, so `select_gguf_recipe` is additive: a new entry point for a caller that already has a budget (a curated recipe wanting to size itself against `hw_detect`, or a future Hub-search flow), not a change to what a bare repo id resolves to by default. Shard collapsing happens BEFORE ranking rather than after, because judging an individual shard against the budget is precisely the bug item 14 warns about: a 3-shard model's first part alone can look like it fits a budget the whole download would blow through. |
| D529 | **`fused_render/ai/catalog_overlay.py` reads `~/.fused-render/models.json` fresh on every `catalog.for_runner` call and merges it into that runner's curated list -- an entry whose `id` matches a built-in row replaces it at the same position, a new `id` appends -- following `server/templates.py`'s registry idiom (read-per-call, missing-file no-op, malformed-file silent degradation) rather than inventing a second one. (SPEC AI-25)** | `server/templates.py` already answered every question a second user-extensible overlay would otherwise re-derive from scratch, and answered them for the identical reason (a curated list a user can extend without a restart, that must never let a typo take the page down) -- reusing its idiom means this module inherits a rule set already exercised in production rather than a fresh one with its own untested edge cases. Wired at exactly one call site, `catalog.for_runner`, because every other `SUGGESTIONS` reader (`for_capability`, `all_suggested_ids`, `runners_offering`) already routes through it -- a second wiring point would be a second place for the two to drift apart, the same failure `worker_base.py`'s own module docstring names as the reason IT is one shared file rather than copied per runner. |
| D530 | **`worker_base._ensure_disk_space(total_bytes, folder)`, called from `download_snapshot`/`download_file` right after each already computes a real listing total, raises `InsufficientDiskSpace` when the target volume's free space is already less than that total -- before either function opens a connection. Both an unknown total and a `shutil.disk_usage` probe failure skip the check silently. (SPEC AI-26)** | The grep-confirmed gap ("no disk-space precheck anywhere") meant every insufficient-space failure surfaced as a mid-transfer `OSError: [Errno 28]`, after however many gigabytes a fetch had already written, with a message naming a syscall rather than a shortfall. The check sits at the ONE point both download functions already have a real total in hand (their own listing, priced for the progress bar) and have not yet opened a connection -- no new network call, no new failure mode beyond the one it prevents. Skipping silently on `total_bytes=None` or a `disk_usage` failure mirrors the existing `_fallback` philosophy this file already applies to a failed Hub listing: a figure this function cannot verify is not evidence of a shortfall, and refusing a download over an unknown quantity would be a worse failure mode than the mid-transfer `ENOSPC` it replaces. |
| D531 | **`hub_cache.has_cached_snapshot(repo_id)` -- True only when a real snapshot directory exists -- gates `ai_runtime._accepts_image`'s new fallback from the on-disk `has_vision_tower` reading to `hub_metadata.get(...)["hasVisionTower"]`: the fallback fires only when nothing is cached at all. (SPEC AI-17 item 17)** | `has_vision_tower` answers False for two situations a caller cannot otherwise tell apart -- a real checkpoint with no tower, and nothing on disk to read at all -- and only the second is safe to override with a pre-download Hub reading. Wiring the fallback straight into `has_vision_tower`'s own False would let a stale or wrong `hub_metadata` entry override an actual measurement of a cached repo, which is exactly backwards from AI-17's own "measured beats guessed" precedence every other rung in this build already follows. Wired at `ai_runtime._accepts_image` (the one function `describe_catalog` already calls per row for both curated and cached halves) rather than inside `has_vision_tower` itself, so `hub_cache.py` -- read by callers with no reason to make a network-backed call -- keeps its existing "read straight off disk, no model load, no network" contract unchanged. |
| D532 | **`registry.TOOL_USE_FAMILIES` -- a known-family allowlist (qwen3, qwen2.5, command-r, hermes, llama-3+instruct, mistral+instruct, gemma-3/4+-it) -- backs `supports_tool_use`/`capability_tags`, added on top of the five existing capability constants without touching them. Both functions are dependency-light: they take repo id plus optional `model_type`/`architecture` as arguments rather than reading `hub_metadata`/`hub_cache` themselves. (SPEC AI-18/item 18)** | The spec is explicit that inferring tool-use from a regex over the repo id is dishonest -- a checkpoint's chat template is what actually determines tool-calling support, and no string pattern can verify that; a curated allowlist of families this codebase (via the comparative study it derives from) has reason to trust is the honest alternative, at the cost of missing families nobody has added yet. The instruct/`-it` qualifier on llama-3/mistral/gemma-3/gemma-4 exists because each family's BASE checkpoint is not trained on a tool-calling format the way its instruction-tuned sibling is -- tagging the base model would offer a control that then fails inside the chat loop. `registry.py` reading no filesystem or network of its own is an existing property this addition had to preserve (`RUNNERS_DIR`/`glob` aside, every OTHER function here answers from arguments or process-environment facts, never a Hub or cache read) -- `ai_runtime.py` already holds the `hub_metadata`/`hub_cache` results these functions need for other reasons (AI-17 item 17's vision fallback), so passing them through costs nothing and keeps the capability registry itself testable with zero I/O. |
| D533 | **Path-hardening audit (SPEC AI-29): `_safe_repo_relative_name`/`_safe_blob_name` now gate every Hub-or-mirror-reported filename and etag in `worker_base._segmented_fetch`'s per-file loop before a `_FileFetch` is constructed; `.part`/sidecar file opens gain `O_NOFOLLOW` (not `O_EXCL`); `mirror.py` was already safe and untouched; no shell-out exists to apply `--` to.** | The Hub metadata path had zero defense against a malicious `rfilename`/etag joined straight into a snapshot symlink target or a blob path -- proven, not assumed, by a test that failed before the fix. `mirror.py`'s manifest reader already had the equivalent checks (`_safe_name`/`_safe_filename`/`_safe_etag`) because a CDN manifest was always treated as untrusted-origin; the Hub metadata path was not held to the same standard even though a malicious repo owner controls `rfilename` just as freely as a compromised mirror would control a manifest. `O_EXCL` was the spec's literal ask but is incompatible with this codebase's actual download design: a `.part` file is deliberately REOPENED across process restarts to resume (AI-5i's own sidecar-based resume), so refusing to open an existing one would break every resume, not just a planted symlink. `O_NOFOLLOW` defends the identical attack (a symlink pre-planted at the exact `.part`/sidecar path) without that cost, because nothing in this codebase ever creates a symlink at either path itself -- a real resume target is always a regular file. |
| D534 | **`hub_metadata.cached(repo_id)` -- a plain disk read, never touching `_fetch_raw` -- is now the only function `ai_runtime._accepts_image`/`_capability_tags` may call; `supervisor.start_hub_metadata_refresh()` (mirroring `start_hardware_refresh`'s shape exactly) sweeps `catalog.all_suggested_ids()` through `hub_metadata.get()` on a 20-minute background tick and is the only caller of `get()` outside this module's own tests. (code review finding 1)** | `hub_metadata.get()` is a synchronous `urllib` GET with an 8-second timeout, called from `describe_catalog` -- a route the AI Models picker polls -- for every text-generation entry with no cached snapshot; on a Linux/Windows box the `llamacpp-text` list is several curated GGUF repos, none of which publishes a `config.json`, so one catalog request could reissue several timeout-bound fetches, and offline or behind a captive portal could stall a page load tens of seconds. `hw_detect.py` already drew the identical split for the identical reason (`detect_hardware`/`refresh_hardware` on a background thread, `cached_hardware()` the only function the verdict path may call) -- `hub_metadata.py`'s own module docstring even names that split without the code following it, which is what made this the wrong side of a boundary this codebase had already established rather than a new design question. A 20-minute tick (much shorter than `_HARDWARE_REFRESH_INTERVAL_S`'s 6 hours) costs nothing extra for an already-fresh entry, since `hub_metadata.get`'s own TTLs (13 days positive, 1 hour negative) are what actually bound the network cost of a sweep -- the tick interval only controls how promptly a newly-stale negative entry gets re-tried. |
| D535 | **`worker_base._ensure_disk_space` now subtracts `bytes_on_disk(folder)` from `total_bytes` before comparing against free space -- a resume is judged against the REMAINING bytes, not the repo's full size again. (code review finding 2, on SPEC AI-26/D530)** | A 30GB model interrupted at 28GB and retried with 5GB free was refused ("needs 30.0 GB, only 5.0 GB is free") even though only 2GB remained to fetch -- the precheck was comparing the wrong quantity against free space and defeated the module's own `.part`/sidecar resume machinery on every large-model retry. `bytes_on_disk(folder)` is reused rather than re-derived: it is the SAME figure the progress bar already trusts for "how much of this repo is durably on disk right now," including a `.fusedpart`'s allocated-blocks progress rather than its sparse `ftruncate`d length, so subtracting it here cannot disagree with what the bar itself reports. It is not scoped to the current fetch's `allow_patterns`, which can make it a slight OVERESTIMATE of what is present for THIS scope when the folder holds bytes from a different revision or a differently-scoped prior fetch -- accepted deliberately, since a precheck erring toward "let a resume through that comes up a little short" is the safe direction (the fetch itself still fails loudly on a genuine shortfall), while erring the other way (refusing a resume that would actually have fit) is the exact regression this fix exists to remove. |
| D536 | **`catalog.for_runner` now looks up the `models.json` overlay under the SAME resolved key `builtin` reads (`_SHARED_SUGGESTIONS.get(code, code)`), not the raw `code` argument. (code review finding 3, on SPEC AI-25/D529)** | `for_runner("llamacpp-text-vulkan")` reads the built-in `llamacpp-text` list via the alias, but the overlay was still keyed by the raw `"llamacpp-text-vulkan"` -- so an overlay entry filed under `"llamacpp-text"` (the only name a user could plausibly have copied out of the built-in curation) silently did nothing on a Vulkan machine, and the same gap applied to every `diffusers-image-cuda/-rocm` and `onnx-embed-directml/-cuda/-rocm` variant. Every hardware variant of a runner already shares one built-in list by construction (`_SHARED_SUGGESTIONS`'s whole reason to exist); the overlay has to share one namespace with it for the same reason, not a per-variant one nobody asked for and no docstring anywhere claimed to support. |
| D537 | **`catalog_overlay.apply` now SHALLOW-MERGES a matching overlay row over its built-in counterpart (`{**builtin_row, **entry}`), field by field, instead of replacing it wholesale. Field removal is deliberately not supported — no `null`-means-delete convention, since `null`/`None` is already a real value some fields legitimately hold. (code review finding 4, on SPEC AI-25/D529)** | The module's own docstring gives "override a row's `size_gb`" as the motivating example, and a wholesale replace broke exactly that: `{"id": ..., "size_gb": 5.0}` dropped `label`/`note`/`params`/`quantization`/`recommended` entirely, and losing `params`/`quantization` specifically degrades `fit.verdict`/`speed.estimate_tok_s` back to their no-params/no-quant fallback branches -- a user overriding a download size for accuracy got a WORSE fit/speed estimate for their trouble, the opposite of the feature's intent. A delete convention was considered and deliberately left out (asked for explicitly, not skipped by oversight): `null` is not a safe sentinel for it since a field can legitimately be `None`/absent already, nothing in this build's scope needs a builtin field erased outright rather than overridden, and a dedicated marker is a bigger feature than this fix calls for. |
| D538 | **`formats.GGUF_QUALITY_ORDER` is reordered to interleave the `IQ*` rows at their real bit-width tier (`IQ4_XS` into the 4-bit tier, `IQ3_M` into 3-bit, `IQ2_M` into 2-bit; `IQ1_M` stays the bottom rung), instead of listing every named quant first and every `IQ` row after regardless of width. (code review finding 5, on SPEC AI-24/D528)** | SPEC item 14's list wrote the four `IQ*` rows last, and taking that literally put a 4-bit `IQ4_XS` after a 2-bit `Q2_K` -- so a repo offering both, on a budget both fit, returned `Q2_K`, the LOWER-quality file, directly contradicting `select_gguf_recipe`'s own "best quality that still fits" docstring. Within one bit-width tier a plain quant still outranks its `IQ` sibling, restating the identical precedence `_gguf_rank`'s dynamic-quant tiers already use for `pick_gguf_file` (a plain quant needs no per-layer engineering to stay usable at that width, so it is trusted first) -- this is the same rule applied a second time in a second table, not a new one invented for this fix. |
| D539 | **`gguf_sources.sources_for`'s cache entries now carry `paramsKnown` (was `params` available on the probe that produced them). A cached NEGATIVE result written WITHOUT `params` is no longer reused by a later call that DOES supply `params`; a POSITIVE result, and a negative written WITH `params`, are trusted regardless. (code review finding 6, on SPEC AI-23/D527)** | Without `params`, only the `base_model`-tag verification tier can confirm a candidate -- a genuine match the param-similarity fallback WOULD have found stays unconfirmed and the probe answers `()`. Caching that `()` under `repo_id` alone shadowed a later, better-informed call for the whole 7-day TTL: the later call supplies `params`, would find the same repo via similarity, but the cache answered from the strictly weaker prior probe instead. A positive result needs no such gate -- a confirmed `base_model` tag is real evidence regardless of whether `params` was ever supplied, and cannot be invalidated by having MORE information later. |
| D540 | **`frontend/src/platform/lib/api.ts`'s `AiCatalogModel` gains `tags?: string[]`, matching the wire field `ai_runtime.py:905` already writes. (code review finding 7, on SPEC AI-28/D532)** | The route wrote `entry["tags"]` for every catalog entry but no TS interface declared it, so any consumer had to reach it through a cast -- the exact gap the PR's own `score`/`runMode`/`speedEstimate` additions were careful to avoid. Marked optional to match the established convention every other recently-added field on this interface already follows (a stale client, a test literal, or an older cached response shape does not have to carry it), rather than a required field that would also have broken every existing `AiCatalogModel` test fixture in the frontend suite. |
| D541 | **`fit.verdict` gains a `hardware: hw_detect.HardwareInfo \| None` keyword, threaded straight through to `_select_pool` — the same `_NOT_GIVEN`-sentinel shape `footprint_store` already has. Left unpassed, `verdict()` does its own single `hw_detect.cached_hardware()` read; passed explicitly (the shape `ai_runtime.describe_catalog` now uses — one `hardware = hw_detect.cached_hardware()` read per request, threaded through every entry's `fit.verdict` call), zero further reads happen. A process-wide `functools.lru_cache` was considered and rejected. (code review finding, on SPEC AI-19/D520)** | `_select_pool` called `hw_detect.cached_hardware()` itself on every `fit.verdict` invocation — a fresh `storage.read_json` open and JSON parse per catalog ROW, on `GET /api/ai/catalog`, a route the model picker polls, for dozens of entries per request. The exact cost `machine_ram_gb` is `lru_cache`d to avoid, and the exact problem `footprints.load_store()`/`footprint_store` already solved for the measured-footprint read on this same code path — this read got neither treatment when it landed. A permanent `lru_cache` (the `machine_ram_gb` shape) was the wrong fix here specifically: `supervisor.start_hardware_refresh()` rewrites `ai_hardware.json` on a 6-hour tick (AI-19's own hardware-detection background loop, wired up after `fit.py` was first built), so a memoized-forever reading would never see an eGPU plugged in mid-session — the precise scenario that background refresh exists to catch. Threading a per-request reading through sidesteps the staleness question entirely: each request is at most as fresh as its own read, the same guarantee `footprint_store` already gives the measured-footprint rung, and the boundary `tests/test_ai_hw_detect.py::test_fit_module_only_reads_the_cache_never_the_probe` pins (`fit.py` only ever reaches `hw_detect` via `cached_hardware`) is unchanged — the call moved from `_select_pool` to `verdict()`, not to a different function. Pinned by a test that counts actual `hw_detect.cached_hardware()` calls across a multi-entry catalog request and asserts exactly one, not by re-checking the verdict came out right (which would pass equally well against the bug). |
| D542 | **`formats.select_gguf_recipe` now estimates a PARTIALLY-known shard set (some parts sized, some `None`) from the known parts own average, instead of summing only the known ones and silently dropping the rest. (Bugbot review, on SPEC AI-24/D528)** | Dropping an unsized shard from the sum always UNDERCOUNTS the real download -- a 3-part quant with 2 known 4GB parts and 1 unknown part reported 8GB when the honest figure is closer to 12GB, which is exactly backwards for a function whose whole job is never recommending a quant that does not actually fit a budget. Estimating the missing shard from its known siblings average uses REAL, MEASURED evidence (shards of one quantization are near-equal size by construction -- llama.cpp splits a GGUF by byte offset, not tensor boundary) rather than falling back to the `params x bpp` table, which stays reserved for the case where NOTHING in the group has a known size at all. |
| D543 | **`fit.available_budget_bytes(hardware=...)` -- the largest footprint this machine could plausibly hold, reusing RESERVE_BYTES/the Apple wired-limit ceiling/`_select_pool` exactly as `verdict()` does, rather than a second budget computation. For a discrete GPU it asks `_select_pool` for the COMBINED VRAM+RAM offload ceiling (`math.inf` as the footprint, forcing its cpu-offload branch), not the VRAM-alone figure a specific footprint might land inside. (SPEC AI-24 item 14 wiring)** | Item 14 needs a budget BEFORE a specific footprint exists -- a GGUF file picker deciding which file to fetch, not `verdict()`'s job of judging one already-chosen footprint -- and the coordinator was explicit that two independent "how much memory do I have" answers computed differently for the same machine would be worse than the hard-coded table item 14 replaces. The combined-ceiling choice (not VRAM-alone) matches `llama_text._offload_schedule`'s own existing backoff: a file that does not fit VRAM alone still loads, slower, via CPU offload, so the true budget question a download-time picker should ask is whether the bytes fit ANYWHERE on the machine, not whether they fit the fastest pool alone. |
| D544 | **`supervisor._child_env` now injects `FUSED_AI_MEMORY_BUDGET_BYTES` (from `fit.available_budget_bytes()`) into every worker spawn, popped when the computation answers `None` -- the identical shape `FUSED_MODEL_MIRROR_OK` already establishes for a per-spawn, server-computed fact a worker needs but cannot derive itself. (SPEC AI-24 item 14 wiring)** | A worker's bare-module interpreter (`llama_text.py`, imported via `sys.path.insert` with no `fused_render` package reachable) cannot import `fit`/`hw_detect` to compute its own budget, so the number has to cross the process boundary somehow -- this reuses the exact env-var mechanism `FUSED_MODEL_MIRROR_OK` already proved out for the identical class of problem (a per-model fact only the server process can compute). Computed FRESH on every spawn rather than once and cached, since `hw_detect`'s own background refresh (`start_hardware_refresh`) means the answer can genuinely change between one worker bring-up and the next -- an eGPU plugged in mid-session should reach the NEXT download, not be frozen at whatever the budget was when the app started. |
| D545 | **`llama_text._resolve_curated_recipe` wires item 14 into the actual curated-download path: the curated `GGUF_RECIPES` file is the floor/default, replaced ONLY when its own real size (from a listing of its repo) exceeds `FUSED_AI_MEMORY_BUDGET_BYTES`, in which case `formats.select_gguf_recipe` picks the best-quality alternative from the SAME repo that fits. `gguf_sources` stays unwired -- a genuinely different question with no honest caller yet. (code review 2026-08-27, on SPEC AI-24/AI-30/D528)** | The first landing of item 14 built and tested `select_gguf_recipe` but called it from nothing -- `ai_runtime.py`'s curated-download path still read `formats.GGUF_RECIPES` verbatim. The floor/default design (never substituting when the curated file already fits) was chosen over unconditionally picking the theoretically best file for three concrete reasons found by reading the download path end to end: (1) `catalog.py`'s own text treats a curated `size_gb` as a promise a reader plans a download around, and silently fetching different bytes under an unchanged displayed size breaks that promise for the common case; (2) `hub_cache.is_downloaded`, the AI Models page's checkmark, and `catalog.mirror_id` all address a curated model by the recipe's OWN filename -- an unconditional substitution would make a freshly-downloaded, correctly-fitting alternative look perpetually undownloaded to every one of those readers, a REGRESSION worse than the hard-coded table it replaces; (3) `llama_text._offload_schedule` already makes an over-large curated pick AFFORDABLE (a slower load via CPU offload, never a failure), so there is no correctness argument for downgrading a model that already fits, only a resource-usage one the floor/default preserves by construction. `gguf_sources`'s own question (a cross-namespace conversion of a model curated for a DIFFERENT engine) is orthogonal to picking a file WITHIN one already-curated repo's listing, and wiring it here to satisfy a "make it non-inert" instinct would not be an honest caller -- reported plainly instead, per the coordinator's own explicit instruction not to wire it just to avoid it sitting unused. |
| D546 | **`hw_detect._linux_cpu_name()` reads `/proc/cpuinfo`'s `model name` field on Linux instead of `platform.processor()`, which the stdlib itself documents as frequently empty there; `detect_hardware` also falls back to `_bandwidth_for(cpu_name)` when a device is unified but its OWN name (an ordinary-looking iGPU string) matched nothing. (Bugbot review, on SPEC AI-18/D519)** | An empty `platform.processor()` on Linux meant `_apply_unified_override`'s AMD Strix Halo detection (keyed off the CPU name, since the iGPU's own device name never contains "ryzen ai max"/"strix halo") never fired there, so those machines kept a tiny BIOS-reported VRAM carveout instead of the real shared pool -- and the bandwidth table's matching rows, keyed on the identical CPU-name signal, were equally unreachable. `/proc/cpuinfo` is the standard mechanism `lscpu` itself reads, chosen over guessing at an alternative because the coordinator asked for the REAL Linux signal, verified rather than assumed. **Reasoned, not observed on real Strix Halo hardware** -- no such machine was available to check the exact `model name` string a real BIOS reports; the fix targets the documented field format rather than a guessed one. |
| D547 | **`speed._catalog_entry` now scans `catalog.for_runner(code)` for every runner code, instead of a raw `catalog.SUGGESTIONS` dict scan -- routing `recalibrate`'s default catalog lookup through the same `~/.fused-render/models.json` overlay (`catalog_overlay.apply`, SPEC AI-25) `estimate_tok_s`'s own live estimate already goes through. (Bugbot review, on SPEC AI-21/D524/D525)** | A user-corrected `size_gb`/`params`/`quantization` in `models.json` was honoured by the ESTIMATE (`ai_runtime.describe_catalog` reads `catalog.for_runner`) but silently ignored when COMPUTING the calibration factor meant to correct that same estimate -- the stored factor was derived against a different weight size than the one every future estimate would actually use, quietly wrong in a way nothing would surface. No circular import or signature mismatch: `catalog.for_runner(code)` only needs a runner CODE, and `catalog.SUGGESTIONS`'s own keys already are exactly those codes, so the fix is a straight substitution with no new plumbing. |

### An app's own `.fused/` folder (2026-08-27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D548 | **Every app folder gets a `.fused/` at its root — `data/` for state the app cannot rebuild, `cache/` for bytes it can, and `meta.json` recording the absolute path the app was set up at — created by the SERVER on app open (`app_fused_dir.ensure`, called from `record_app_open`), gitignored, and given no `window.fused` or shared-helper API of its own.** | An app had nowhere to put what it accumulates while running, so pages each invented somewhere: a JSON file beside `index.html` (authored content — it lands in the app's git history and in its `.fused` export), a scratch dir under the workspace (invisible to the app that owns it), `~/.myapp` (survives the app's deletion, collides with the next app of that name). One hidden folder at the app root fixes all three at once, and hidden is load-bearing rather than cosmetic: `app_listing`'s workspace walk and `appfile._iter_app_files` both already skip dotted names, so the convention costs those two zero new rules. The `data`/`cache` split is a PROMISE — everything under `cache/` must be reconstructible from `data/` plus the outside world — which is what makes "delete the cache" a safe operation for the user, for us, and for a disk cleaner; anything that fails the test is data, and the authoring skill says so in those words. **Creation is the server's, on open rather than on create**, because scaffolding-only would have left the convention true of new apps and false of every app that already exists, and an app that must `makedirs` before its first write is an app whose authors will skip the folder half the time; GET /render serving a marker-carrying page IS the open (D301), so the hook already existed and needed no cooperation from any client. The call is gated on `app_listing.app_entry` rather than on reaching `record_app_open`, which changes nothing on the render path (the marker is already established there) and exists for the legacy `POST /api/apps/recents/open` the shell no longer uses: its `path` is an arbitrary caller-supplied directory that neither branch of the workspace/registered fork validates before the fork, and creating `.fused/` in a folder that is not an app is a write nobody asked for. `POST /api/apps/new` calls it a second time, deliberately BEFORE `app_git.init_repo`, so the freshly written `.gitignore` is already excluding the folder when the boilerplate `add -A` runs — the packaged starter kit could not carry it instead, since git stores no empty directories and a copytree of the kit would produce nothing. **`meta.json` is a witness, not a lookup key**: every path resolves from the live folder, so the recorded `app_dir` exists only to be compared against it, and `ensure` never rewrites it even when it disagrees (it logs at INFO). The disagreement IS the value — it is the only way an app learns it was moved or copied, which is exactly when a path-keyed cache entry or an absolute path stored in `data/` has gone stale — and rewriting on sight would erase the evidence in the same moment it mattered; what a move MEANS is the app's call, so there is no automatic repair. A `meta.json` that exists but does not parse is left alone for the same reason it is written with `open(..., "x")` rather than check-then-write: it is a user-writable file, two concurrent renders of one app are ordinary, and losing that race is a normal outcome (the winner wrote the same three fields). Mount-backed folders are refused outright — a remote mount is not an app's private disk, `makedirs` on one is a network round trip on the render path, and a directory-creating access pattern against a wedged mount is a shape that has killed mounts here before; the guard is a string prefix test, so the local case pays nothing. **No API surface, deliberately**: no `fused.dataDir`, no `fused.cacheDir`, no stdlib-only helper in `templates/shared/` — the paths are three string joins off the app root, and each helper would be one more thing to keep in step with a convention whose whole value is that it is written down once. `app_fused_dir.py` exists for the server; apps build the paths themselves, which the authoring skill (a new "Where an app keeps state" section, plus a caching section with an atomic `os.replace` memoiser and five new pitfalls) and the starter `CLAUDE.md` both spell out — the same documents that answer the second half of the ask, "cache more", which with no runtime surface to add is guidance and nothing else. Machine-local is enforced in four places, not one: `.fused/` joins `app_git._GITIGNORE` (new repos), the `.git/info/exclude` sweeps in `app_git._ensure_excludes` AND `templates/claude/agent.py` (repos predating this, and the `add -A` of every Claude turn — the second was already flagged in-file as "keep in step with app_git._GITIGNORE" and would otherwise have committed whole caches), and `index/ignore.DEFAULT_IGNORE_NAMES` — `~/Fused` is indexed and an app cache has no size bound, so one page caching tiles could spend the 200k-row corpus on rows nobody searches for. The name collides with the `.fused` app-file extension (§43) only in spelling: the index records `ext` for files alone and `os.path.splitext(".fused")` yields no extension either way, so no directory can surface as a phantom exported-app card, and the gitignore pattern's trailing slash keeps an exported `<name>.fused` FILE tracked. Tests: `tests/test_app_fused_dir.py` (layout, idempotence, the stale-`app_dir` non-rewrite, the unparseable-meta non-overwrite, the mount refusal, non-directory and unwritable inputs) plus, in `tests/test_apps_api.py`, the creation path, the open path for a pre-existing app, the non-app gate, and a `.fused` FILE sitting in the way proving a failed creation never fails the open; `tests/test_app_git.py` pins the `.gitignore` pattern including its trailing slash. |

### Claude config: the git-init race and the Windows MCP `.cmd` question (2026-08-27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D549 | **The MCP registration entry's `command` stays the bare resolved `fused` path on every platform, including the bare `fused.cmd` `inspect_app._fused_cli()` reports on Windows — NOT routed through a `cmd.exe /c` hop (`command: "cmd.exe", args: ["/c", fused, ...]`) added by this codebase. (SPEC MC-12)** | Investigated after base commit f44bb53a fixed `_fused_cli()` to look for `fused.cmd` instead of `fused.exe`: a `.cmd` is a batch file `CreateProcess` cannot run directly, and routing it through `cmd.exe` reads like the obvious next fix. Rejected on the strength of cross-spawn's own source (`lib/parse.js`), which is what the MCP TypeScript SDK's stdio transport spawns through (`shell: false`): cross-spawn decides whether to build its OWN safe `cmd.exe /c` hop by reading the **command's file extension** — only `.com`/`.exe` skip the hop. Naming `cmd.exe` ourselves makes that check see an `.exe` and skip the hop cross-spawn would otherwise have applied, which converts a case cross-spawn already handles correctly (cross-spawn's own README cites nodejs/node#7367 — a space in both the command and an argument — as one of the bugs its hop fixes, via `windowsVerbatimArguments`, which a JSON `args` array gives us no way to request) into a definite regression. The alternative theory (a bare, uncorrected host `spawn`) is not rescued by a hand-rolled hop either: `cmd /c`'s own quote-stripping rule only has a safe fast path for exactly one quoted segment, and both the wrapper path and the served app path can independently need one (a spaced Windows username hits the wrapper path too). Neither host behaviour has been verified against a real Windows Claude Code install — this is inference from the MCP SDK's published source, not an observed spawn. **The check that would settle it:** register the entry on a real Windows machine, then run `claude mcp list` and see whether the server reports **connected** (the un-hopped `.cmd` resolves fine) rather than merely **added but unreachable** (this reasoning was wrong and the hop is needed after all — reopen this decision, don't silently add the hop back). |
### Repo update notifications (2026-08-26/27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D550 | **The community showcase clone stops being managed: `community._refresh` clones `~/Fused/showcase` once, if missing, and never fetches or fast-forwards it again — the origin-URL check that used to gate a foreign-repo refusal moves into `_cache_ready` instead, so a git repo tracking some other remote is still refused, not silently adopted, without a fetch step left to gate it. (SPEC §33)** | The startup sync (`refresh_in_background`, now `ensure_showcase_in_background`) fetched and `merge --ff-only`ed the clone on EVERY server start, silently — a repo the user is actively editing getting background-synced on its owner's behalf is exactly the kind of surprise D550-552 as a whole exist to replace with an opt-in row (D552/D553). Once GT-20 gives every git repo a passive notice and an opt-in Update, the showcase clone does not need a SECOND, bespoke sync path of its own — it becomes an ordinary repo the same mechanism already covers the moment an app in it is opened. **Rejected: keeping the startup sync alongside the new opt-in check** — two things fetching the same repo on two different schedules is the "which one owns this" question D553's key-decisions explicitly avoid creating for RepoUpdatesSlot; one mechanism, used everywhere, is the smaller surface. |
| D551 | **The community marketplace no longer produces a second, installed copy of an app (`~/Fused/local/<slug>`, `install`/`update`/`uninstall`). Opening a showcase app IS opening your copy, edited in place under `~/Fused/showcase/<slug>`; the explorer preview's Clone button for a showcase app is deleted (the `.fused`-app-file Clone button is unrelated and untouched).** | Two copies of the same app (the showcase clone's, and whatever the user installed) meant two places edits could live, an `update` that had to decide whether the installed copy was "dirty" (edited) before it dared overwrite it, and a force-update path whose whole job was recovering from that decision going wrong (the snapshot-commit-can-fail case tests/test_community_marketplace_fixes.py already pinned). None of that is needed once there is only one copy: the showcase clone is the user's tree from the moment it lands (community.py's own header: "cloning an app copies its CURRENT state, edits included"), so "clone to install, edit the install, update the install" collapses to "edit the thing you already have". `installs.json` and `~/Fused/local/<slug>` are left on disk untouched for anyone upgrading from before this change — nothing reads or writes either path any more, including the workspace-move migration, which no longer rewrites installs.json paths. |
| D552 | **The upstream check for GT-20 (§33) is a NEW server-side module, `fused_render/git_upstream.py`, that MIRRORS `templates/git/ops.py`'s git plumbing and `deeplink.py`'s `origin/HEAD` symref resolution rather than importing either — and `ops.py` gains the matching `rebase` op in lockstep, the same two-implementations-of-one-operation shape `git_show.py`/`git_repos.py` already use for the identical reason. Triggered from `GET /render`'s D301 block (beside `record_app_open`, inside the same `_preview` guard), never from `/api/fs/list`.** | `ops.py` is reached only as `fused.runPython("./ops.py")` from inside the git companion's own iframe — the activity card (§36) that shows the check's result has no route to it, and `ops.py` may not import `fused_render` at all (SPEC PY-15, exec'd standalone). Mirroring is the established answer to that constraint (`git_show.py:144-155`'s own header states the reason for its copy of `_locate`), not a new pattern invented for this feature — the cost is that a rebase added to one module and not the other would leave the git panel unable to do what its own divergence refusal (GT-15) now tells the user is available, so the two are changed together, every time. `GET /render` is chosen as the trigger over `/api/fs/list` because it is ALREADY the one definition of "this app is being opened" (D301) — no new signal, no client cooperation — while `/api/fs/list` fires once per directory LISTING and is the hook `index.freshness.note_folder_opened` uses, gated on the unrelated file-indexing preference; borrowing it would put a git notification behind an unrelated switch and make the throttle carry all the load a wrong trigger creates. **Rejected: checking on a timer or at server start** — the check is about a repo the user is ACTIVELY working in right now, and a background poll of every repo on the machine is the wedge-prone "walk everything" pattern the mount-refusal rules throughout §33 exist to prevent one instance of. **AMENDED 2026-08-27 (user call, D555 amendment):** the "changed together, every time" lockstep this decision states no longer includes a `rebase` op -- `ops.py` never keeps one in the end: the secondary Rebase button it backed was removed as too dangerous to offer (D555 amendment), so `ops.py` and `git_upstream.py` stay in lockstep on the READ/detection plumbing this decision is really about, with no mutation left to keep in step. |
| D553 | **Repo update rows land in the activity card as a second named `RepoUpdatesSlot` (SPEC BG-15), not a new surface: primary Update (`--ff-only` pull) on the default branch, secondary Rebase (onto the tracked default branch only) off it, decided from branch shape alone — never from the behind count. A refusal that is not a conflict offers "Fix with Claude", which stages `{path, prompt}` in a new cross-navigation store (`apps/explorer/lib/pending-claude-ask.ts`) and navigates, rather than calling `window._fusedClaudeAsk` — which only exists once a Claude-capable surface for that exact path is already mounted, the whole reason a separate store is needed.** | The action is branch-shaped rather than count-shaped because a feature branch one commit behind is exactly as much "not the default branch" as one fifty behind — the count changes the SENTENCE (GT-20's status text), never which button is primary. A rebase conflict is left mid-operation for the existing conflict reader/`resolve` op to pick up (GT-19) rather than aborted, because aborting would silently discard a decision the button just took on the user's behalf a second time — the same posture `_stash_apply`'s conflicting-apply case already takes. **Rejected: a third top-level component instantiating a second `<DownloadManager>`** — `QueueDock.tsx` is the one place a `<DownloadManager>` is built in the shell, so `RepoUpdatesDock.tsx` exports a hook (`useRepoUpdatesSlot`) `QueueDock` calls itself, rather than a sibling component whose rows would have to reach that same instance through a prop-forwarding hop `NotificationHost`/`App.tsx` do not otherwise need. **Rejected: modeling a row on the job registry** — see BG-15. **AMENDED 2026-08-27 (user call: "the rebase button is scary, let's just remove it"):** the secondary Rebase action this row describes was removed outright -- too dangerous to offer from a passive notice -- rather than left reachable. See D555's own amendment for the replacement shape (Update/Switch, one action per row, no secondary slot). |
| D554 | **The git companion (`templates/git/template.html`) toolbar now has a Rebase button of its own, shown next to "Get latest" whenever the branch is genuinely diverged (`ahead && behind`, not merely behind) — `confirmable("rebase", ...)`, gated exactly like the other destructive actions, driving `pendingConfirm`'s pre-existing `op === "rebase"` branch and `ops.py`'s `_rebase`. The confirm bar it opens now actually renders too, passed into `toolbar()` and appended under the bar it belongs to.** | Code review (2026-08-27) caught that D552's own "lockstep" claim — "a rebase added to one module and not the other would leave the git panel unable to do what its own divergence refusal ... tells the user is available" — was false the moment it was written: `ops.py` gained `_rebase` and `template.html` gained the `DESTRUCTIVE` entry and the `pendingConfirm` branch for it, but no `confirmable("rebase", ...)` call site ever set `?ask=rebase`, and no `askIn("branch")` ever read the confirm bar `pendingConfirm` built — both halves of the trigger were missing, so `_pull`'s own divergence message ("...or use Rebase to replay your commits onto the tracked default branch") pointed at a button that did not exist anywhere reachable from where that message appears. Wiring the trigger, not deleting the dead branch, is what the "lockstep" property actually requires — the alternative (stop advertising Rebase in `_pull`'s message) would have left the git panel permanently unable to resolve a divergence it can plainly detect, for no reason. Distinct from, not a duplicate of, the activity card's own Rebase (D553): that one is a passive, cross-repo notice surfaced outside any particular repo's own view; this one is reached from INSIDE the repo the divergence is in, exactly where the failed Pull would otherwise leave a dead end. **AMENDED 2026-08-27 (user call, D555 amendment):** the toolbar Rebase button this decision wires up was removed outright along with `ops.py`'s `_rebase`/`DESTRUCTIVE_OPS` entry and `log.py`'s `rebase_target`/`rebase_ahead` fields it depended on -- too dangerous to offer, not a bug to keep fixing. `_pull`'s divergence refusal points at a terminal again, not this button. |

### Repo update notifications — the activity card refit (2026-08-27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D555 | **Repo updates get their OWN notification card (`shell/RepoUpdatesDock.tsx`), a sibling of the jobs/queue activity card in `NotificationHost`'s column, not rows pinned inside it (supersedes D553's `RepoUpdatesSlot`). Off the default branch the row's PRIMARY action becomes `switch` — a new server-side mutation, `git_upstream.switch_repo`, a plain `git checkout <default_branch>` — with Rebase demoted to secondary on the same row; on the default branch Update stays primary.** | The user reported four defects live in the running app, all traced to one root cause: rows for a completely different KIND of thing (a standing branch condition, not a job) wedged inside a card whose header, fold and Clear were built only ever to reason about jobs and the queue. With zero jobs and zero queue but one repo row, `jobsSummary` fell through to a job-count sentence and read **"0 finished"**; the collapse toggle did nothing (repo rows were exempt from the fold, and there were no job rows left to fold); Clear disappeared (its count was jobs only); and there was no way to dismiss a repo row at all. Patching each symptom in place would have left the same wedge for the next kind of row; giving repo updates their own card removes the wedge instead — `DownloadManager.tsx`'s empty-card gate, header and `clearable` go back to only ever meaning jobs and queue, and "0 finished" becomes structurally impossible there again. Switch is preferred over Rebase as the off-default primary because it can never rewrite or conflict with the user's own commits — the whole point of this feature is nudging a repo toward its default branch without ever overriding a user's work — and after a successful switch the repo is very likely still behind, so the row reappearing with Update once the check re-runs is intended. **Rejected: keeping the rows in the jobs card and fixing each symptom individually** (a repo-aware header sentence, a repo-aware fold, a repo-aware Clear count) — three special cases bolted onto a card that was never designed to hold two kinds of row, for a feature whose whole card no longer needs to share the jobs card's one plate now that `NotificationHost` already composes an ordered column of siblings. **AMENDED 2026-08-27 (user call: "the rebase button is scary, let's just remove it"):** the Rebase secondary action this row describes is gone -- removed outright, too dangerous to offer, rather than left as the card's only work-losing button. Every row now offers exactly one action (Update on the default branch, Switch off it); `RepoRow.secondaryAction` and the `"rebase"` `RepoAction` member were deleted along with it, as were `fused_render/git_upstream.py`'s `rebase_repo` and the server router's `"rebase"` POST action. Placement moved again too: this card is no longer a sibling in `NotificationHost`'s column, per D563 -- it is `StatusBar`'s (`platform/ui/StatusBar.tsx`), inside `#main`, for the reason D563 itself gives. |
| D556 | **Dismissing a repo row (a new per-row ✕, `RepoUpdatesDock.tsx`) hides it only until the server's OWN throttled recheck (`git_upstream.CHECK_TTL_S`, 300s) produces a newer `checked_at` for that repo — `dismissed[root] >= repo.checked_at` — rather than permanently or via a server-side dismissal endpoint. The dismissed map is held at MODULE level, not component state.** | The payload already carries `checked_at` (the throttle's own timestamp), so nothing server-side needs to change to express "hide until the next real check" — a client-only map keyed on a value the server already reports is the whole mechanism. Module-level state (not `useState` alone) is what survives a remount: switching panes or panels tears `RepoUpdatesDock` down and rebuilds it, and a dismissal a user just made must not silently resurrect on the next pane switch. A full PAGE reload resurrecting a dismissed row within the throttle window is accepted as a deliberate, documented trade rather than reaching for `localStorage`: the state is exactly as ephemeral as "which of these have I already looked at and decided to ignore for now", and persisting that across a full app restart is not a promise this feature makes. **Rejected: a server-side dismiss endpoint with its own persisted state** — `git_upstream.py`'s whole design is a throttled in-memory check with no durable state of its own (module docstring), and a dismissal endpoint would be a second kind of state next to it for a UI-only concern the `checked_at` field already lets the client answer unassisted. |
| D557 | **The card's own collapse folds EVERY row — rendering NO rows at all when collapsed, not a CSS `.dl-rows.is-folded` height cap (that cap hides nothing for the 1-4 rows this card typically holds, which was shipped as a live bug and caught in code review, 2026-08-27) — unlike the jobs card's PARTIAL fold, which deliberately pins the queue's rows and a live-run stand-in outside the collapse (BG-10).** | That asymmetry does not carry over: a repo row has no in-flight message a fold could strand mid-turn the way a live queued send would be, the header already states how many updates are waiting whether the list is open or not (`repoUpdatesSummary`), and every row is now individually dismissible (D556) — so collapsing the whole list costs a user nothing they cannot immediately get back by re-expanding it or by dismissing the row outright. The jobs card's own partial fold exists specifically because SOME of its rows (the queue's, a live turn) have a cancel that is otherwise unreachable once folded; no repo row is ever in that position, so there is no equivalent carve-out to make here, and folding everything is the more honest, simpler rule for a card whose only job is "how many, and do you want to look right now". |
| D558 | **`fused_render/jobs.py::clear_finished` (the bulk "Clear" button) now takes TERMINAL records only (`state != RUNNING`), dropping the `or is_stalled(...)` term it used to OR in. The per-row `dismiss` (a single ✕) is unchanged and still takes a stalled row. `DownloadManager.tsx`'s `clearable` count and its post-clear local patch are updated to match (new pure helpers `clearableCount`/`jobsAfterClear` in `platform/lib/jobs.ts`).** | `is_stalled` means only "no report in `STALE_AFTER_S`" (30s) — a long model load, a slow generation between report ticks, or a throttled background tab all satisfy it while the work is still genuinely running. Sweeping those rows in bulk did not stop the work (`ai/supervisor._cancel_state` returns None for a missing row, read as "not cancelled" by `_is_cancelled`), but it did remove the only channel reporting on it: the row disappears, and `fused.watchJob` gives up after a few consecutive misses and tells the page the work stopped — so a user who pressed Clear reads it as having cancelled their AI job, while the real process keeps running with nothing on screen saying so. That is the exact failure this whole feature (§36) exists to prevent, reached through its own Clear button. The per-row ✕ keeps the old, more permissive rule ON PURPOSE (BG-7's own reasoning stands): closing one specific stalled row is a user who usually knows what it was, because they are looking at that one row; a bulk sweep cannot know that about any row it takes, so it is held to the stricter rule. |
| D559 | **(Superseded by D562, 2026-08-27 — the user rejected the whole concept of a "non-foldable card"; `rowsShown`/`foldedJobRows` and this static-span mitigation are DELETED, not merely bypassed.)** The jobs card's collapse toggle (`DownloadManagerView`) now renders as a plain, non-interactive `<span className="dl-toggle is-static">` — no chevron, no button, no `onClick` — instead of a `<button>` whenever nothing in the current job list would actually be hidden by folding (`jobs.length > foldedJobRows(jobs).length`). Investigated, not guessed at: reading `rowsShown`/`foldedJobRows` found no bug in the fold logic itself.** | The user reported the toggle "does nothing". It genuinely does nothing in two real, already-documented states: `rowsShown`'s own rule keeps the queue's rows on screen regardless of `collapsed` (a queue row's ✕ is its only cancel, so folding it away was the very bug this rule prevents), and `foldedJobRows` deliberately keeps a live scheduled run's stand-in row through the fold for the identical reason. A card whose only rows are queue-shaped, or that job list, therefore has nothing left for a fold to hide — the button was real and wired correctly, but pressing it changed nothing on screen, which reads exactly like a broken control. The fix is honesty about what the control can do, not forcing a fold that would re-break the queue/live-run exemption `rowsShown`/`foldedJobRows` exist to protect: when nothing is foldable, no pressable affordance is offered at all. `RepoUpdatesDock.tsx`'s own card (D555) does not need the equivalent fix — it returns `null` with no visible rows and folds EVERY row once it renders, so whenever its toggle is on screen at least one row exists for it to fold; there is no state there where the button would be inert. **Follow-up (task 11, code review 2026-08-27):** a `collapsed` persisted `true` from an earlier session, combined with a job list that is entirely fold-exempt in THIS render, used to strand the card folded with no control left to un-collapse — the header renders the static span exactly because nothing is foldable, yet `.dl-rows` still got its folded height cap and the collapsed-only progress bar still drew. A new `effectiveCollapsed = collapsed && foldable` now gates both, falling back to expanded whenever nothing is foldable without touching the STORED preference — a later job that IS foldable still starts folded. |
| D560 | **Repo-update rows track WHICH action is running (`busyAction: RepoAction \| null`, `RepoRowView`), not just whether one is — each button reads "Working…" only when it is the one `busyAction` names, and both stay disabled while either is running. `RepoUpdatesDock.test.tsx` stopped `mock.module`-ing `@platform/lib/router`/`@platform/lib/api` entirely, in favor of a minimal `location`/`window`/`history` global stub (mirroring `hook-harness.ts`'s `Clock`) and a per-test `globalThis.fetch` stub.** | Task 12 (code review, 2026-08-27): a single shared `busy: boolean` covered both the primary and secondary button, and only the primary's label checked it — pressing Rebase (secondary) made Switch (primary, an action the user never touched) read "Working…", while Rebase's own label never changed at all. Writing the test surfaced a SECOND, more consequential defect: this file's own `mock.module` of router.ts (added in an earlier task, `{navigate: () => {}}`) broke TWO unrelated files — `useWalkSearch.render.test.ts` and `FilesHome.render.test.tsx` — the moment all three ran in one `bun test` invocation, which is exactly how the coordinator's own end-of-work full-suite run works. `mock.module` replaces a specifier for the WHOLE process, first-registration wins, and — the sharper half of the lesson — even a COMPLETE stub (every export name) still broke `FilesHome.render.test.tsx`, because its assertions depend on `navigate`'s REAL behavior (`history.pushState` + `window.dispatchEvent`), not merely its presence. `FilesHome.render.test.tsx`'s own header comment already states this exact principle for `@platform/lib/api` ("a real ES module namespace export is frozen... this stubs `globalThis.fetch` instead"); this is the same principle applied to router.ts. Verified in both file orders (this file first and last) that the combined suite passes with zero contamination either way. **AMENDED 2026-08-27 (user call, D555 amendment):** the second button this decision fixes a shared-state bug between (Rebase, secondary) was removed outright, so a row offers only one button today -- `busyAction`'s `RepoAction | None` shape is kept anyway, still correct and still the cheaper property to reason about for one button, and general enough to survive a second action returning. |
| D561 | **`switch_repo`'s "already checked out in another worktree" refusal (`git_upstream.py::_worktree_holding_branch`) now runs the parsed worktree path through `os.path.normpath` before it reaches the message, and the message's em-dash is a plain hyphen.** | Windows CI on D560 (`test-python-windows`, the only red lane) caught both live: git reports a worktree's path with FORWARD slashes in its own text REGARDLESS OF PLATFORM (`"already used by worktree at 'C:/Users/...'"` on a Windows runner), so the refusal built from it verbatim showed a Windows user a path form their own shell and Explorer never write — `os.path.normpath` is a no-op for slash direction on POSIX and turns the forward-slash form into native backslashes on Windows, the same tool this codebase already reaches for everywhere else a path needs the host's own separators (`background_apps.py`, `zip_import.py`, `server/common.py`, et al.). Separately, the CI log rendered the message's em-dash as a mojibake replacement character; encoding the message as ASCII locally reproduces a real `UnicodeEncodeError`, confirming it is not only a log-viewer artifact. `_worktree_holding_branch` is the ONLY place in this module a path parsed out of git's own output reaches an app-authored, user-facing sentence — `_brief`'s raw-quote refusals are git's own words, not built here, so they needed no equivalent fix. The test (`test_switch_names_the_worktree_already_holding_the_default_branch`) now asserts `os.path.normpath(os.path.realpath(local))`, which is genuinely cross-platform with no `os.name` branch: identity on POSIX, native backslashes on Windows, matching whatever `switch_repo` itself produces on that platform. **Not fixed as part of this decision:** the module's other two app-authored refusal strings (`_mutation_preflight`'s "in-progress" and "dirty" messages) carry the same em-dash and are latent instances of the identical risk — left as-is, scoped to the one message code review flagged. |
| D562 | **Every fold exemption is REVERSED: "everything is foldable, even for the job cards" (user, 2026-08-27). `jobs.ts`'s `rowsShown`/`RowsShown` and `foldedJobRows` are deleted outright; `DownloadManagerView`'s collapse toggle is ALWAYS a real `<button>` (the D559 "static span when nothing is foldable" mitigation is gone with the exemptions it existed to work around); collapsing now renders NO `.dl-rows` at all — not the queue's rows, not a live scheduled run's job-row stand-in, no partial-height `.dl-rows.is-folded` CSS cap. Reachability while collapsed moves from the exemptions into the header: `QueueSlot.cancelAll` becomes a function of `collapsed` (`(collapsed: boolean) => ReactNode`, built by `QueueDock.tsx`) rather than a pre-decided node, and `queue-dock-lib.ts::showCancelAll` drops its threshold from 2+ withdrawable rows to 1+ once the card is collapsed.** | This directly reverses D558/D559 (superseded below) and the original partial-fold design BG-10 documented from the start: the queue's rows and a live-run stand-in were exempt from the collapse because folding either away took the only cancel a queued message or a live turn has with it — a card collapsed weeks ago left scheduled work arriving with nothing on screen to stop it. That reasoning does not disappear; it MOVES. A collapsed card still cannot silently swallow the one thing a user could stop: it just does so through the header's `Cancel all` (now reachable at a single row, since collapsing removes that row's own ✕) rather than through a per-row-kind carve-out in the fold logic itself. `jobsSummary` (unchanged) keeps naming what is hidden, and the collapsed-only overall progress bar (unchanged) keeps saying something is still running — "the fold hides the detail, never the fact that something is happening" holds exactly as before, satisfied by different mechanisms. `RepoUpdatesDock.tsx`'s own card (D557) already worked this way and needed no change — the jobs card is the one that changed to match it, not the other way around. **Rejected: keeping the exemptions behind a flag or a config switch** — the user's own words rejected the CONCEPT of a non-foldable card outright, not merely its current shipped shape; a flag would keep the rejected idea alive as an option. **Rejected: lifting `collapsed` state out of `DownloadManagerView` into `QueueDock`/`App.tsx`** so `showCancelAll`'s threshold could be computed once, in one place — the smaller, more local change is handing `QueueDock` a `cancelAll` CALLBACK instead, so the collapse preference stays exactly where it always lived (`DownloadManagerView`'s own `useState(loadCollapsed)`) and only the ONE decision that needs to know about it (whether to show the button) crosses the boundary, as a function rather than as lifted state. **AMENDED 2026-08-27 (D563, status bar redesign):** the `cancelAll`-as-a-function-of-`collapsed` shape this decision built, and the threshold drop to 1+ it exists to describe, are both gone -- collapsed is a chip with no controls now, not a short card with a header still on screen, so there is nothing folded-but-visible left for a lowered threshold to reach. `queue.cancelAll` is a plain node again; see D563 for the replacement. |
| D563 | **The two long-lived notification cards (the activity card and the repo-updates card) move out of `NotificationHost`'s fixed, floating bottom-right column into a new `StatusBar` (`platform/ui/StatusBar.tsx`), mounted as the LAST child inside `#main` (not spanning the sidebar) rather than as `#app`'s sibling. Collapsed, a card renders only its existing `.dl-toggle` chip — the chevron, `jobsSummary`/`repoUpdatesSummary`, and (jobs only) the aggregate percentage, nothing else; expanded, a `.dl-panel` (the old card's plate, moved) opens floating above the bar, anchored to that chip. `.status-bar:empty { display: none }` collapses the strip to nothing when both cards return null. Each card keeps its own persisted `collapsed` key and its own `useAutoExpandOnNew` wiring, unlifted. `queue-dock-lib.ts::showCancelAll` drops the `collapsed` parameter D562 gave it, back to a flat 2+-withdrawable-rows threshold, and `QueueSlot.cancelAll`/`RepoUpdatesCardView`'s Clear go back to being pre-decided nodes rather than functions of `collapsed`.** | User complaint: "the collapsed notification is also taking too much space. lets have a bottom status bar where the notifications can be collapsed to. it is impossible to use the claude template with it." `.notif-host` is `position: fixed`, so even COLLAPSED its header sat on top of whatever page was under it — collapsing a floating card only ever shrinks its own footprint, it never gives the page back the space, which is exactly what made a page like the Claude template unusable underneath it. `#main` is already `flex: 1 1 auto; display: flex; flex-direction: column` (explorer.css), so the bar as its last child reserves height for free with no new layout machinery; every page's content shortens by exactly the bar's height, and nothing may overlap while collapsed. Inside `#main` rather than `#app`-level so the sidebar keeps its own full height and its own bottom (GlobalSidebar's Preferences menu) — the bar only ever answers to the main column's own content, the same scope `#main` already has for everything else. D562's own "reachability while collapsed" promise (`queue?.cancelAll`/`queue?.note` rendering outside `.dl-rows` so they survive a collapse, and `showCancelAll`'s threshold dropping to one row once folded) does NOT carry forward: the chip the user picked explicitly is three things — summary, percentage, chevron — with no room budgeted for a button, so a control placed on it would compete with the one line the chip has. That reachability requirement is retired on purpose here, not left silently broken: `queue?.cancelAll`/`queue?.note`/Clear/every row now live ONLY in the expanded panel, and the collapsed-threshold branch `showCancelAll` grew for D562 is deleted along with the state it was answering (a card that stayed on screen, folded) rather than left as unreachable dead code. Toasts, `FdaCard` and `ServerStatusBanner` stay in `NotificationHost`'s column: a toast is seconds-long (the wrong lifetime for a permanently reserved strip), and the other two are exceptional/near-permanent enough that overlaying the page is still the right call while they are showing. **Rejected: anchoring every expanded panel to the bar's own right edge instead of to its own chip** — with both cards' chips packed left-to-right in the bar's flow (not right-aligned, unlike the old floating column), a single shared anchor point would stack two open panels directly on top of each other if both cards were expanded at once; anchoring each panel to its own trigger keeps them side by side for free and needs no z-order tiebreak between the two cards. **AMENDED 2026-08-27 (D565, user verdict on the shipped bar: "different categories of status bar should be always present"):** the `.status-bar:empty { display: none }` rule this decision built, and the "empty means gone" reasoning behind it, are SUPERSEDED -- the three sections are a fixed, always-present readout now, each drawing its own idle state instead of vanishing. See D565 for the replacement and its own reasoning. |
| D564 | **The queue half's header button, "Cancel all" (`QueueDock.tsx`), is relabelled "Cancel queued". No behaviour, threshold (`showCancelAll`) or tooltip change — the tooltip ("Cancel every queued message") already said the true scope.** | User feedback: the card is mostly job rows, and "Cancel all" sitting in its one shared header read as "cancel everything on this card" — precisely what the button does not do. It withdraws queued SCHEDULED MESSAGES only; it cannot touch a job row at all (a job's own stop is its row's ✕, unaffected by this button either before or after the rename). "Clear" (`DownloadManagerView`, dismisses terminal job rows) is untouched — it is already the conventional word for sweeping ended rows and its own tooltip ("Dismiss finished") already names its set correctly, so nothing about it was mislabelled. |
| D565 | **The status bar's three sections (Models, Activity, Updates) are ALWAYS PRESENT now — `.status-bar:empty { display: none }` and D563's "empty means gone" reasoning are SUPERSEDED, not extended (`#main` is permanently ~34px shorter on every page as the accepted cost). Each section draws its own IDLE state instead of vanishing (`No models loaded` / `Idle` / `Up to date`) — plain, muted (`--fg-muted`) text with no chevron, since there is no panel behind an idle section worth opening. Ordered left to right by LIFETIME: Models (persistent, always true the instant anything is resident) first, Activity and Updates (transient, appear and resolve) after — the same principle `NotificationHost.tsx` documents for its own toast/server-card column, restated here for what is always true versus what is currently happening rather than for how long each lives. `.dl-summary`'s font-weight drops 600 -> 500, and `.status-bar` gains `font-variant-numeric: tabular-nums` so every count and percentage in the bar inherits it.** | User verdict on the shipped round-1 bar, from a screenshot: "this is very ugly. different categories of status bar should be always present and look better." A bar that vanishes when nothing is happening reads as a notification tray, not a status bar — the three categories the user asked for are a fixed readout of the machine's state (what's loaded, what's running, what's behind), true or false at every instant, not events that come and go. The chip's 600-weight summary "read like a heading" in the same screenshot; 500 keeps enough weight to mark it as the bar's own subject without competing with an actual heading anywhere else on the page. Colour is reserved for STATE, not decoration, everywhere in the bar now (idle `--fg-muted`, active `--fg`, failure `--error` — see D568's `is-failure` fix) — the one exception is `.dl-new-dot` (D567), a deliberate "look here" cue rather than ordinary chip text. **Rejected: keeping `:empty` and only widening what counts as "not empty"** — the user's own words reject the CONCEPT of a bar that can be gone, not merely round 1's threshold for it, the same shape D561's own "Rejected: keeping the exemptions behind a flag" argument took for the fold. |
| D566 | **A third status-bar section, Models (`shell/ModelsDock.tsx`), reads `GET /api/ai/runtime` (via the existing `apps/ai_models/lib/aiRuntime.ts` `useAiRuntime` poll — no second transport) and offers `POST /api/ai/runtime/unload` per row. Idle: "No models loaded". Active: plain text, count and cost — `2 models · 18 GB` — with NO label prefix once there is a value ("Models: " lives only in the idle string). The panel is a QUICK-INFO POPOVER, not a management console: one row per loaded model, its name (`repoName()`-trimmed, full id on hover), its own resident bytes, and an Unload button — no gauge, no proportional fill, no RAM-fraction indicator of any kind. Unload has no confirmation step; a row shows "Unloading…" and disables itself mid-flight, and a failed unload says so inline (`.dl-status`) rather than doing nothing visible. `unloadAiModel`'s response — a fresh runtime snapshot — is published back through `aiRuntime.ts`'s shared store on success, updating every reader (this panel, the sidebar's own resident-model dot) without waiting out the next poll tick.** | First draft of this section was a gauge — a proportional memory-pressure indicator — and the user's own words cut it: "we don't need a gauge if too complicated. just a quick info upon clicking which we have a list of loaded models we can unload." That is the whole feature: a quick look at what's resident, and a way to free it, nothing a dashboard would add on top. No new backend surface either — `GET /api/ai/runtime`'s own docstring already sanctions polling it ("so the sidebar can poll it"), `unloadAiModel` (`platform/lib/api.ts`) already exists and already carries the D3 `X-Fused` guard, and `useAiRuntime` is the SAME shared poll `GlobalSidebar`'s resident-model dot already reads, so this section costs the app no second request. No confirmation dialog on Unload despite it being a real, immediate, work-destroying action (it can cost minutes to reload) because the action is recoverable by loading again and the panel is small — the same posture the row-level ✕ takes elsewhere in this corner (round 1: a request is confirmed by its own visible in-flight state, not by a modal, when the undo path is "do the thing again"). Lives in `shell/`, not `platform/`, for the same reason `QueueDock.tsx`/`RepoUpdatesDock.tsx` do: `aiRuntime.ts` is `apps/ai_models` territory and platform may not import apps (`check-boundaries.mjs`); shell may import anything, so the shell composes this section too and hands it to `StatusBar` as a third slot. |
| D567 | **`lib/autoExpand.ts`'s `useAutoExpandOnNew` no longer forces a folded section open. It used to call `persist(false); setCollapsed(false)` the instant an id it had not seen before arrived; it now only returns a boolean — an unacknowledged arrival while collapsed — which each section draws as a quiet `.dl-new-dot` on its own chip, cleared the moment the section is actually opened (by the user, for any reason), and never written to `localStorage` on its own.** | Code review finding #4, and the sharpest one: this recreated, one level down, the EXACT complaint the whole status-bar redesign exists to fix. A user collapses a section to reclaim space, opens a page like the Claude template, and the first background job that page spawns used to pop a floating `.dl-panel` over that same page uninvited — `StatusBar.tsx`'s own justification for letting a panel overlay content ("it is user-initiated") was never true for this path — and PERSISTED the expansion, so the panel came back open on the next reload even with nothing left unread. The fix keeps the useful half of the old behaviour (noticing a genuinely new id, via the same `jobs.ts` `trackSeenIds` all three sections share) and drops the harmful half (acting on that notice by force). Applies to all three sections uniformly — Models (D566) inherited it for free, being built on the same hook. |
| D568 | **Six geometry fixes to the chip/panel split (round-1's own D563), found in one code review pass and landed together: (1) `.dl-panel`'s `max-width: 100%` — deleted; it resolved against `.dl-host`, content-sized to the chip alone post-split, clamping the panel's real `min(340px, ...)` width down to roughly the chip's own ~130px, which is the crushed panel, two-letter-ellipsised repo name and wrapped `Clear` button the user's screenshot showed. (2) `.dl-panel`'s anchor flips `right: 0` -> `left: 0`; chips pack LEFT in this bar (`.status-bar` sets no `justify-content`), unlike the old right-anchored floating column this replaced, so a right-anchored 340px panel grew off the left edge of the viewport the moment fix (1) let it actually reach 340px. **AMENDED 2026-08-27 (D569, user: "the items must be right aligned"):** `.status-bar` now sets `justify-content: flex-end`, so the premise this fix's own reasoning stated — "chips pack LEFT in this bar" — is no longer true; chips pack RIGHT now. The anchor flipped BACK, `left: 0` -> `right: 0`, for the mirror-image reason: a left-anchored panel grows off `#main`'s right edge the instant a chip near that edge opens. See D569 for the full round-3 change this rides in on. (3) `DownloadManagerView`'s panel `.dl-head` now renders only when it has something to offer (`queue?.cancelAll || clearable > 0`) instead of unconditionally — before the chip/panel split the head always held the toggle too, so it could never have been empty; after the split, one job running with nothing queued and nothing terminal left it a blank ~30px padded band. (4) `.dl-host` becomes `flex: 0 1 auto; min-width: 0` (was `flex: 0 0 auto`, no `min-width`) so a chip can actually shrink under pressure and `.dl-summary`'s existing `text-overflow: ellipsis` has a narrower box to engage in, instead of a second chip in a narrow window being pushed straight past `#main`'s right edge. (5) `.dl-panel`'s `z-index` rises 2000 -> 2001, one above `.notif-host`'s own 2000; `#main` (the panel's ancestor) precedes `<NotificationHost />` in `App.tsx`, so on an EQUAL z-index the later sibling won regardless of which the user actually opened, and a toast, `FdaCard` or the server banner painted over an open panel. (6) `.dl-chevron`'s glyph flips `⌄` (down) -> `⌃` (up), and `.is-collapsed`'s rotation flips `-90deg` -> `90deg` to keep landing on the same "closed, pointing right" read — the panel opens UPWARD (`bottom: 100%`) now, not downward, so the old down-pointing "expanded" glyph pointed away from where expanding actually revealed content.** | All six are geometry the test suite structurally cannot see (a DOM has no viewport in `react-test-renderer`, and `bun test` never lays anything out) — caught by a code review pass reasoning about the containing block and the flex context, not by a red test, and verified against a running dev server rather than by a green suite. (1)+(2) are the two HIGH findings and, together, the direct cause of the round-1 screenshot; (2) was LATENT behind (1) — fixing the width alone, without also fixing the anchor, would have made the panel the right size in the wrong place. |
| D569 | **Four round-3 fixes to the verified-in-browser round-2 bar, from the user's own reaction plus one measured bug, landed together: (1) IDLE LABELS name their own category — `Idle` -> `No activity` (`DownloadManagerView`), `Up to date` -> `No updates` (`RepoUpdatesCardView`) — `No models loaded` was already correct and untouched. (2) `.status-bar` gains `justify-content: flex-end`, right-aligning the three chips; `.dl-panel`'s anchor flips `left: 0` -> `right: 0` in lockstep (D568 fix (2) amended in place, above) so a 340px panel opens against its own chip's edge instead of growing off `#main`'s right edge. (3) `.dl-chevron`'s literal text glyph `⌃` is replaced by an inline SVG (`platform/ui/DlChevron.tsx`, a lucide "chevron-up" `<polyline>` on the app's usual 24-unit/strokeWidth-2 grid), following the SAME rotate-one-glyph-with-CSS convention `shell/NewJobModal.tsx`'s `ICON_CHEVRON_DOWN` and `platform/ui/MenuIcons.tsx`'s `chevron` already use elsewhere in the app, rather than inventing a fourth spelling; one shared component replaces three hand-drawn copies (`DownloadManager.tsx`/`ModelsDock.tsx`/`RepoUpdatesDock.tsx` each drew `⌃` separately) for the same anti-drift reason `platform/ui/Chevron.tsx`'s own header gives. Class names, rotation and `currentColor` are unchanged from D568 finding #6. (4) A running, cancellable job row's protected trailing controls (`.dl-amount`, `.dl-pct`, `.dl-row-cancel`, `.dl-x` — all still `flex: 0 0 auto`, untouched) could still be pushed 33px past `.dl-panel`'s right edge and clipped by its `overflow: hidden`, because `.dl-model` (the dimmed model-name suffix beside a job's title) was ALSO `flex: 0 0 auto` on the theory that `.dl-title` alone would always give up enough room first — true for a long PROMPT, false for a long MODEL NAME, which is its own unrelated text. `.dl-model` becomes `flex: 0 1 auto; min-width: 0` plus the same `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` `.dl-title` already wore, making it the row's second shrinkable element rather than an unshrinkable one. Separately, `.dl-rows`'s `overflow-y: auto` was silently computing `overflow-x: auto` too (CSS overflow's "visible becomes auto on one axis when the other is not visible" rule) — closed explicitly with `overflow-x: hidden`, `overflow-y: auto` unchanged.** | (1): user, looking at the bar: "what is idle? what is up to date?" — D566's "no label prefix once there is a value" was about the ACTIVE state and still holds there; the two bare adjectives named no subject at all. (2): user, verbatim: "the items must be right aligned." (3): user, verbatim: "the collapsing arrow is very ugly" — a typeset caret renders at whatever weight and baseline the font stack gives it, which is not a weight anyone chose. (4): measured in a real browser against the verified round-2 build — a running `download` job with a realistic long title (`Downloading Qwen2.5-Coder-32B-Instruct-4bit`) reproduced it every time; the user separately reported a second symptom of the SAME cause with a screenshot ("the notification cards should not be scrollable") — the overflowing row had turned `.dl-rows` into a horizontal scroll container, so the panel scrolled sideways to reach the half-clipped Cancel button rather than simply failing to show it. `.dl-rows`'s intentional VERTICAL scroll (a long job list must not push its own header off-screen) is unchanged; only the horizontal axis, which nobody asked for, is closed. All four fixes are geometry or plain-language judgment calls the test suite cannot fully see — `react-test-renderer` has no viewport, the same limitation D568's own reasoning names — so every CSS-side assertion added for this round is a STYLESHEET-LEVEL source pin (proving the declarations exist and agree with each other) rather than a measured layout, and the real geometry was verified against a running dev server, same as D568. Chevron correctness (3) is the one part of this round genuinely checkable end-to-end: `platform/ui/DlChevron.test.tsx` renders the real component and asserts it draws an `<svg><polyline>`, not text. **Rejected: leaving `.dl-model` at `flex: 0 0 auto` and instead giving `.dl-title` a hard floor via a fixed `min-width`** — a floor on the ALREADY-correct shrinkable element does nothing to stop the ACTUALLY-unshrinkable one (`.dl-model`) from overflowing; the floor would only guarantee the title keeps some pixels while `.dl-model` still pushes Cancel out, treating the symptom on the wrong element. |
| D570 | **`.dl-chevron`'s REST-state colour drops `--fg` -> `--fg-muted`, brightening to `--fg` only on `.dl-toggle:hover`/`:focus-visible` (a new rule, `.dl-toggle:hover .dl-chevron, .dl-toggle:focus-visible .dl-chevron`); `DlChevron.tsx`'s `strokeWidth` drops `2` -> `1.5`. `.is-failure`'s `--error` tint (D568) is untouched and still wins over hover — it sits later in the cascade at equal specificity.** | User, on the round-3 SVG chevron (D569): "the up arrow is glaring." The exact over-correction shape D568 finding #7 hit in the opposite direction (an 11px `--fg-muted` text glyph that was too QUIET, fixed by going to 15px full `--fg`) — a crisp SVG stroke at full `--fg` next to `.dl-summary`'s 500-weight text became the loudest mark in the bar, wrong for a disclosure affordance whose job is to be found, not to shout. Moving the brightness to hover/focus rather than just muting it flat is not a pure downgrade: it gives the chip real hover feedback it should have carried since round 1 and never did (`.dl-toggle`'s own background wash was the only affordance), while resting quieter than `.dl-summary`. 1.5 stays well clear of a hairline — this is not a reversion to the 11px text caret round 2 replaced, only a lighter drawn stroke. |
| D571 | **`.dl-model`'s `flex-shrink` rises `1` -> `999` (its `flex-grow` stays `0`); `.dl-title` is unchanged at `flex: 1 1 auto`.** | Coordinator, from the same browser pass that verified D569's fix (4): with both elements at an equal `flex-shrink: 1`, they shrank in LOCKSTEP under pressure — `Downloadin…` / `Qwen2.…` — cutting the row's two identifying fields to uselessness together while `.dl-amount`/`.dl-pct` kept full width, exactly the outcome D569 was supposed to prevent, just moved one level up. `.dl-model` is a dimmed SECONDARY suffix, and per `jobs.ts`'s own comment on it, a redundant restatement whenever the title already carries the model name — true in exactly the repro that surfaced this. A 999:1 shrink-factor split exploits how CSS Flexbox actually resolves shrinkage: it distributes negative space in passes, and an item clamped at its `min-width` freezes and drops out, with remaining negative space then redistributed among the flexible items still standing. At that ratio the first pass assigns the model suffix effectively all of the negative space; once it bottoms out at its own `min-width: 0` and freezes, `.dl-title` is the only item left standing and absorbs whatever deficit remains. The practical result is an ordering — the model collapses to nothing FIRST, the title only starts giving up characters once there is nothing left of the model to take — built entirely out of a mechanism designed for proportions, with no JS measurement, no `ResizeObserver`, and no second breakpoint. |
| D572 | **`JobRow`'s `cancel`/`dismiss` handlers (`DownloadManager.tsx`) set a new `failure` state on a rejected request instead of an empty `catch`, and render it in the row's existing `.dl-status` line (`{failure ?? status}`, so a local failure takes priority over the job's own ordinary status sentence until the next attempt or a fresh success clears it). `JobRow` gains two optional, defaulted props — `cancelFn = cancelJob`, `dismissFn = dismissJob` — as a pure test seam so the failure-path tests can inject a rejecting function without a process-wide `mock.module` on `@platform/lib/jobs`.** | User: "the cancel button also doesn't seem to be doing anything?" Investigated: the report's own repro (a stubbed `/api/jobs` response) meant Cancel's POST 404'd against a job id the server had never heard of — not a bug by itself — but it exposed a real, `git log -S`-confirmed-original-to-this-branch (`0a1cec2d`) defect: the `catch` block discarded a rejected `cancelJob`/`dismissJob` call with a bare comment ("nothing applied locally — the refresh below is the source of truth"), which is sound reasoning for the SUCCESS path and for an ordinary race (the server is authoritative about whether work actually stopped) and unsound for a request that never landed at all — a 404, a 500, or an offline server all left the button silently inert with no toast, no console entry, no state change and no label move, confirmed in a live browser pass (click, no console entry, no error, no flip to "Cancelling…"). Fixed to the standard D566 already set for this exact class of problem in the Models panel's own Unload ("must not fail silently", surfaced inline via `.dl-status` rather than a toast, since the row IS the thing the user is looking at) — applied uniformly to BOTH controls the round-1 split created (Cancel and Dismiss), since they share the identical empty-catch shape. The optimistic `onPatch` and the `onChanged()` refresh are unchanged; only a REJECTED request's silence is fixed. **This is the coverage gap itself, not a geometry contract** — unlike D569-D571, this failure path is fully checkable with `react-test-renderer` (no viewport needed to observe a rejected promise updating a status line), and `JobRow.test.tsx` now covers it directly: a rejected `cancelFn`/`dismissFn` surfaces the row's own failure sentence and leaves the row/list otherwise untouched, and a successful call still shows no failure line. A green suite over an empty `catch` is exactly how this shipped originally — the gap was in test coverage, not in the test tooling's ability to see the bug. |
| D573 | **The status bar becomes a VS Code/Cursor status bar: no chevron anywhere (`platform/ui/DlChevron.tsx` + its test DELETED, `.dl-chevron`'s CSS removed), each section's chip is ALWAYS a real `<button>` showing a category NAME plus a count (`Models 2`, `Activity 3`, `Updates 1`) with the idle sentence moved off the bar into the panel (`.dl-panel-empty`: "No models loaded" / "No activity" / "No updates"), the separate unpressable `.dl-idle` span retired in favour of `.dl-toggle.is-idle` (muted text, same clickable chip), `.status-bar`'s `min-height` 34px -> 24px with `gap: 0`/`padding: 0 2px` and `.dl-toggle` at 11px, the inter-section vertical rule (`.dl-host + .dl-host`'s `border-left`) dropped, an OPEN chip holding the hover wash via `.dl-toggle[aria-expanded="true"]`, `.dl-toggle`'s horizontal negative margin dropped to stop adjacent chips overlapping, and `.q-title`'s `flex-grow` dropped `1` -> `0`.** | User: "the shevron doesn't belong to the status bar, lets follow vscode/cursor for inspiration" and "lets have simpler stuff like models (x count) \| notifications \| downloads etc and the no xyz part in the popover thing that opens" — rejecting the D565-D572 design outright rather than adjusting it. A VS Code status-bar item is a label you click; hover is the entire affordance, so a disclosure triangle (D567-D570's whole subject) is the wrong idiom and every rule tuning it went with the component. Two consequences worth naming: an IDLE section now opens a panel (D565's "no panel behind it worth opening" is reversed — the idle sentence has to live somewhere and the chip no longer has room), and `jobsSummary`/`repoUpdatesSummary` stay pure, tested functions that no longer render anywhere in the bar. `.q-title`'s grow factor went for a separate complaint in the same pass — the trailing ✕ sat after a wide gap of dead space, because a stretched title shoved the row's action and ✕ to the panel edge away from the name they belong to; with no grow the controls follow the text. Density, the hover/open wash and the ✕'s new position are geometry contracts the suite structurally cannot see (the D569-D571 lesson) — verified in a browser, not by a test. |
| D574 | **`useAutoExpandOnNew` (`platform/lib/autoExpand.ts`) OPENS the arriving section's own panel again — REVERSING D567 by explicit user request — returning `{ hasNew, autoOpen, acknowledge }` instead of a bare boolean, where `autoOpen` is transient React state the caller ORs over its persisted `collapsed` flag and NEVER writes to localStorage. Gains a third `ready` parameter (default `true`) so the seen-set seeds from the first render that HAS data. New `platform/lib/dismissOnOutside.ts` (`useDismissOnOutside(hostRef, open, close)`) closes an open panel on an outside pointer-down or Escape, scoped to the whole `.dl-host`. `.dl-toggle[aria-expanded="true"]`'s background moves `--row-bg-hover` -> `--row-bg-active`.** | User: "when we have something new, always show the notification. don't keep no activity displayed" — reported with the Activity panel open reading `No activity` while the new-item dot sat over on Models, so a quiet dot alone makes the user hunt for which of three sections actually received the thing while the one they have open is free to be truthfully, uselessly empty. D567's guard is kept where it belongs: the real defect it fixed was never the opening, it was `persist(false)` writing a FORCED expansion back to localStorage as though the user had chosen it, so the panel returned open on the next reload with nothing left to show — `autoOpen` is therefore transient only, and the chip's first click drops it without touching the saved preference (`autoOpen` implies `collapsed`, so falling through to the setter would persist exactly that). Paired with dismissal, and the pairing is the point: an auto-opened panel is only tolerable because "clicking anywhere else should background the notification" (user) now works — pointerdown-not-click and `capture: true` follow the three existing hand-rolled outside-dismiss sites (`BarMenu.tsx`, `PaneModeMenu.tsx`, `ContextMenu.tsx`), none of which exposed a reusable seam, and scoping "inside" to the whole `.dl-host` rather than the panel is what stops the outside handler double-firing with the chip's own onClick and what keeps Unload/Cancel/Clear/✕ from dismissing the panel under themselves. `ready` fixes a bug the auto-open promoted from cosmetic to loud: the seeding effect has no dependency array (it must run every render to diff the list), so on the first render `ids` was the caller's initial `[]`, the seen set seeded EMPTY, and every pre-existing item read as an arrival the moment data landed — a dot and now an auto-opened panel on a plain page load, the exact opposite of the hook's stated intent. `ids.length > 0` would not do, since an idle machine must still announce its FIRST real arrival, so each call site supplies a live "has my fetch answered once" flag (`useJobs`'s and `useRepoUpdates`'s new `settled`, `aiRuntime.ts`'s new module-level `aiRuntimeSettled()`). The open-chip colour changed because D573 had left `:hover` and `[aria-expanded="true"]` both resolving to `--row-bg-hover`, making an open chip and a merely-hovered one pixel-identical — unanswerable already, and worse once a panel can open with the user having touched nothing; `--row-bg-active` is the app's existing engaged/selected token (sidebar.css uses it for a selected row) so no new token was needed. |
| D575 | **The Models chip drops its count: `Models · 628 MB`, not `Models 1 · 628 MB`. The label, the `·` and the resident total stay; idle keeps the bare `Models` label and its existing `.is-idle` treatment.** | User: "lets just have [model · memory size] and remove the count". With the count gone the chip reads identically for one resident model and for five, which is the point — the size is the number being watched, and it is already the TOTAL across all of them. Activity and Updates keep their counts: the request named Models only. `models.length` still decides `idle` and still drives the panel's rows, so nothing but the chip's own string changed. |
| D576 | **The status bar matches VS Code's own metrics rather than approximating them: `.status-bar` gets a fixed `height: 22px` (was `min-height: 24px`) and `padding: 0 8px` (was `0 2px`) with `gap: 0` kept; `.dl-host` and `.dl-toggle` both go `height: 100%` so an item is a FULL-HEIGHT SQUARE rectangle — `padding: 0 5px`, `border-radius: 0`, and round 4's `-3px` vertical margin deleted along with the inset it compensated for; `.dl-toggle`'s `font-size` goes 11px -> 12px and `.dl-summary`'s `font-weight` 500 -> 400; and two new tokens, `--status-item-hover` / `--status-item-active`, replace `--row-bg-hover` / `--row-bg-active` on the item's hover and open states.** | User: "lets follow exactly what vscode is doing for the status bar" — every previous round guessed at the idiom (34px then 24px tall, 12px then 11px text, a 6px radius throughout), so this one takes the real values instead. The radius and the inset padding were the substance of it: a 6px-radius pill inset 3px from the bar's edges reads as a chip or a badge FLOATING IN the bar, where a VS Code status-bar item is a square rectangle spanning the bar's full height whose hover fill runs edge to edge vertically. The two new tokens are `statusBarItem.hoverBackground` and `statusBarItem.activeBackground` — ALPHA OVERLAYS (white 0.12 / 0.18 dark, black 0.10 / 0.16 light, since a white overlay on a light bar is invisible) rather than opaque mixes like the `--row-bg-*` pair they replace, because an overlay reads correctly whatever the bar is painted, which is why VS Code specifies them that way; the 0.12-vs-0.18 gap is also what finally makes an open item tellable from a hovered neighbour, the ambiguity D574 flagged and only partly fixed. NOT copied: VS Code surfaces detail in hover TOOLTIPS, and these sections host real interactive content (Unload, Cancel, a row's ✕) that a tooltip cannot carry, so the click-to-open popovers stay. |
| D577 | **Three row/chip honesty fixes. (a) `.dl-amount` moves OUT of the protected `flex: 0 0 auto` group to `flex: 0 9999 auto` with `min-width: 0` and ellipsis, making it the FIRST thing to shrink, ahead of `.dl-model`'s 999 and `.dl-title`'s 1; `.dl-title`'s `min-width` goes `0` -> `15ch`; `.dl-pct`, `.dl-row-cancel` and `.dl-x` stay protected. (b) `useAutoExpandOnNew` returns `hasNew && !autoOpen` — the dot is suppressed while this section's panel is showing the arrival. (c) `DownloadManagerView` feeds the chip a percentage ONLY when exactly one job is running (`runningJobs === 1 ? overallFraction(jobs) : null`); `overallFraction` itself is unchanged. Plus `jobFraction`'s `=== null` -> `== null`.** | (a) User: "the UI is not readable" — the row rendered `Downloa…   2100000000 / 4600000000   46%   [Cancel]`. D571's goal (the title gives way so `Cancel` is never pushed out of the panel) was right, but with a `0` floor and every other element refusing to shrink, "gives way" meant the title surrendered EVERYTHING before anything else surrendered anything. The amount is the most redundant thing in the row — the progress bar and `.dl-pct` each already say how far along the job is — so it is the right thing to lose, and the title needs a floor at a length that still carries information (`Downloa…` does not). Ellipsized rather than `display: none` under a width threshold: `.dl-panel` is `min(340px, 100vw - 32px)`, i.e. fixed at 340px above a 372px viewport, so such a threshold would essentially never fire — the variable is CONTENT length, not panel width. (b) Browser pass found `Activity 1  46% ●` with the panel already open: the dot exists to announce news the user cannot see, and an open panel listing the arrival IS them seeing it. Suppressed at the return rather than never set, so `acknowledge` (which clears both) cannot leave a dot behind for something the user just closed. (c) User: "if there are multiple activities, what does the percentages mean?" — nothing useful. The unweighted mean averages incommensurable work (a 4.6GB download at 10% beside a 2MB task at 90% reads as "halfway" when nearly all the work remains), and one indeterminate job blanks the aggregate for everything beside it. Byte-weighting was rejected: it fixes only download-vs-download, still cannot compare bytes against a task with no byte count, and leaves the null-poisoning. Gated at the CALL SITE rather than inside `overallFraction` so the pure function and its existing tests stay valid and no export dies. `== null` also covers `undefined`, so a payload missing `done`/`total` yields null instead of `NaN%` in the bar — not user-reachable (jobs.py serializes explicit nulls) but free to close. |
| D578 | **A separator returns between adjacent bar sections — `.status-bar > .dl-host:not(:last-child)::after`, a 1px x 11px `var(--fg-muted)` block at `opacity: 0.5` with `margin: 0 3px` — partially reversing D573's "no divider" call.** | User: "can we keep the | between them?", the second time they have asked (their original framing was `models (x count) \| notifications \| downloads`), so the request outranks the reference: VS Code genuinely draws no dividers between status-bar items and this is a deliberate deviation from it. On the HOST, never inside `.dl-toggle`, and that is what makes it viable now — the button is a full-height rectangle taking a hover and an open background wash (D576), so a separator inside it would be washed with the label and read as part of the item instead of as chrome between two items. A 1px hairline rather than the literal `|` glyph, for the reason this project already established when the round-2 `⌃` went to SVG: a typeset glyph renders at whatever weight and baseline the font stack supplies, and at 12px a `|` sits off-centre and inherits the label's weight. Short (11px against the bar's 22px) on purpose — a full-height rule reads as a structural division of the bar rather than a light separation of items. A `::after` with `content: ""` is already outside the accessibility tree and unclickable, so it needs no `aria-hidden` or `pointer-events`. The `3px` margins sit inside the 10px of dead space between two neighbours' `padding: 0 5px`, crowding neither label; because this ADDS space rather than removing it, it cannot reintroduce the 12px hover-wash overlap the old negative margin caused — the washes now stop 3px short of the separator on each side. |
| D579 | **Two of the three bar sections are renamed, user-facing copy only: `Updates` -> `Notifications` (empty state `No updates` -> `No notifications`, title `Show/Hide updates` -> `Show/Hide notifications`) and `Activity` -> `Jobs` (empty state `No activity` -> `No jobs`, title `Hide details`/`Show details` -> `Hide jobs`/`Show jobs`). Counts unchanged; the bar now reads `Models · 628 MB \| Jobs 2 \| Notifications 1`. NO structural rename: `RepoUpdatesDock.tsx`/`DownloadManager.tsx` keep their filenames, every `.dl-*`/`.q-*` class stays, and `repoRows`/`visibleRepoRows`/`jobRows` are untouched.** | User: `Activity` and `Updates` "sound too alike", then "git updates does not make sense out of an app. it belongs to 'notifications'", then "what about jobs?". The pair collided because NEITHER label named its subject — both read as "stuff that changed" and neither said whose. `Notifications` fixes more than the collision: a repo being behind upstream is ONE KIND of notification, not a top-level peer of Models, so `Updates` had framed a git-specific concept as a category of its own; `Notifications` is the extensible category instead, and a future alert that is not a repo update gets a home without a fourth section. It also matches the user's original framing for this bar ("models (x count) \| notifications \| downloads etc"). `Jobs` is this codebase's own word for exactly what that section holds (`fused_render/jobs.py`, `/api/jobs`, the `Job` dataclass, `KINDS`), so the label and the store it reads from finally agree, and it avoids the subset trap that ruled out `Downloads` and `Tasks` — downloads, background tasks AND the scheduled queue are all jobs, so it neither overclaims nor underclaims. `Activity` was also the vaguest of the three labels and half the reason for the collision. The `title` attributes were made consistent in the same pass: the jobs chip said `Hide details`, which told a screen-reader user nothing about WHICH section, so all three now follow `Show <thing>` / `Hide <thing>`. `tests/test_queue_dock.py` pins the empty-state markup as a literal source line (twice); that one string was updated, nothing else in the file. |
| D580 | **`useAutoExpandOnNew` can now steer the panel in BOTH directions: its two transient booleans (`autoOpen`, and the new `autoClose`) are one internal `Override = null \| "open" \| "closed"`, so they are mutually exclusive by construction. A non-empty -> empty transition in the row list sets `"closed"` when a panel is actually on screen; callers read `open = autoClose ? false : !collapsed \|\| autoOpen`. All three docks collapse their two-branch toggle into ONE that acts on the VISIBLE state (`wantOpen = !open`, `acknowledge()`, then write the preference only if `collapsed === wantOpen`). The `[collapsed]` effect now clears the override in both directions rather than only on expand. Still NO write to localStorage from either override.** | User: "after a job finishes, ensure we close the jobs popover if no jobs left" — the mirror of D574, and the same objection underneath it: auto-opening on arrival is pointless if the panel then sits on screen showing `No jobs` once the work is done, which is the empty-panel state that started this whole round. Implemented in the shared hook, so Models (unloading the last resident model) and Notifications (dismissing the last repo update) get it too — for Models it is the better feel of the three: the user clicks Unload on the last row and the panel gets out of the way. Closes a panel opened by ANY route, per the coordinator's call on the user's unconditional wording; if it reads as abrupt, scoping it to auto-opened panels is a one-line change to the `panelOpen` test. Three things make it safe rather than twitchy: (1) an ARRIVAL WINS over a same-tick drain by construction, not by luck — `arrived` implies `ids` is non-empty, so the `ids.length === 0` test cannot also fire, and an early return makes that explicit so there is no close-then-open flash; (2) the transition fires EXACTLY ONCE, because `trackSeenIds` builds its set only from `currentIds` and so shrinks with the list, leaving `prev.size === 0` on the following tick — a section that was already empty is never touched, which is what stops this fighting a user who deliberately opened an idle section; (3) the override is only set when a panel is actually open, so an already-closed section is not left holding a `"closed"` override that the next chip click would have to spend itself clearing. The unified toggle is what keeps D574's no-persist rule intact WITHOUT a special case for it: dismissing an auto-opened panel, or reopening an auto-closed one, finds the saved flag already agreeing with the outcome, so clearing the override is the whole of the work and nothing is written; a click on a chip whose state came from the preference itself still flips and saves it. Verified by inspection across all four states (preference-closed, preference-open, auto-opened, auto-closed). |
| D581 | **No layout shift when a count appears: the Jobs and Notifications chips render an ALWAYS-PRESENT `.dl-count` slot (`display: inline-block; min-width: 2ch; padding-left: 0.4em`) nested INSIDE `.dl-summary`, empty when the count is zero. The aggregate percentage is REMOVED from the Jobs chip entirely, retiring D577's single-job gate and `jobs.ts`'s `overallFraction` (deleted — the chip was its only caller; three tests in `jobs.test.ts` that pinned it went with it). Models is deliberately left FLUID.** | User: "can we do something on jobs and notifications so that there is no content layout shift when new jobs or notifications show up?" `2ch` is exact rather than a guess because `.status-bar` sets `font-variant-numeric: tabular-nums` for the whole bar, so a digit is one `ch` and two digits fit precisely; three digits grow the chip once, at 100 jobs, which is graceful overflow and not a case worth designing for. Empty rather than a literal `Jobs 0` — an explicit zero is noise, and this whole round has been trimming exactly that. The slot is nested inside `.dl-summary` rather than being a sibling flex item because `.dl-toggle` sets `gap: 6px`, which applies to an empty slot too and would compound with the slot's own reserved width into visible dead space; as `padding-left` it is part of the slot's own box. WHY ONLY TWO SECTIONS, so nobody "fixes" the third: the bar is `justify-content: flex-end`, so a chip's width change moves itself and everything to its LEFT and nothing to its right. Notifications is rightmost, so its count shifts Jobs and Models both; Jobs' count shifts Models; Models is leftmost, so its `· 628 MB` grows into empty bar space and shifts nothing — a slot there would buy no stability and cost permanent dead space. The percentage had to go rather than get a slot of its own: it appeared and disappeared in that same spot and so shifted the bar by itself, and reserving ~4ch for it permanently would leave obvious dead space in a 22px bar. It was already the third telling of the same fact — the panel draws a percentage AND a progress bar on every row, which is where per-job progress belongs — so D577's gate, which existed only to stop it lying across multiple jobs, is moot. |
| D582 | **One panel open at a time. New `platform/lib/exclusiveSection.ts` — a module-level store with `SECTION_ORDER = ["models", "jobs", "notifications"]` and `useExclusiveSection(key, wantOpen, forceClose)` — closes every loser whenever more than one section wants to be open. `useAutoExpandOnNew` gains `forceClose` (sets the `"closed"` override). Ties are broken by a microtask-scoped TICK: same commit means same `seq`, so a genuine clash falls to `SECTION_ORDER` and Models wins; a later commit outranks, so an ordinary click always beats whatever was open.** | User: "we also need to ensure only one of the three tabs are active at one time" — two panels overlapped, and two chips both holding D576's active wash made that signal meaningless. IN PLATFORM, and that is load-bearing rather than incidental: `check-boundaries.mjs` forbids platform importing shell and the participants straddle the line (`DownloadManager.tsx` is platform, `ModelsDock.tsx`/`RepoUpdatesDock.tsx` are shell), so the shared state has to sit on the side both can reach. NOT lifted into `StatusBar.tsx`: the cards arrive as opaque `ReactNode` props precisely so the bar never learns what its children are, and threading open-state through it would spend that seam. A module store rather than a context provider for the reason `aiRuntime.ts` is one — there is one status bar per document, the participants mount independently, and a provider would have to be installed above all three, i.e. exactly the edit to `StatusBar`/`App` just ruled out. The tick is what makes the two awkward cases deterministic instead of dependent on effect-execution order: two sections auto-opening on one poll response, and a RELOAD where two sections' persisted preferences both say open (the sharp edge — per-section flags could always disagree, and rather than a migration the READ is simply made deterministic, Models winning and the losers force-closed transiently so nothing is rewritten). A tick is stamped only on the false->true edge, so a section that is merely still open cannot keep out-bidding a sibling the user just clicked. The arbiter ONLY ever closes — nothing here opens a panel — so D580's auto-close cannot hand the screen to a previously-open sibling: a drained section leaves nothing open. And it closes via `forceClose`, never the persisting path, because closing B because the user opened A is the app arbitrating, not the user choosing (the standing D567 guard). |
| D583 | **The Jobs and Notifications counts are ALWAYS rendered, zero included (`Jobs 0`, `Notifications 0`), and D581's reserved slot is gone — `.dl-count` keeps only `padding-left: 0.4em`, its `min-width: 2ch` and `display: inline-block` deleted. Models still has NO count (D575 stands): `Models · 628 MB`, bare label when idle.** | User on D581's version: "this is ugly" — the bar read `Models \| Jobs   \| Notifications`. D581 met the no-shift goal and paid for it in visible dead space: an empty 2ch slot left `Jobs` sitting well back from its separator while `Models` sat tight against its own, so the separators read as unevenly spaced. An always-present number buys the SAME stability with nothing reserved-but-empty — it never appears or disappears, so the only remaining width change is 9 -> 10, which is rare and was never the complaint. It is also what the reference actually does: VS Code's own errors/warnings item reads `0` rather than going blank, and the user asked to "follow exactly what vscode is doing". `.dl-toggle.is-idle`'s muting is what keeps `Jobs 0` reading as quiet rather than as an alert, and `.status-bar`'s inherited `font-variant-numeric: tabular-nums` still stops 1 -> 2 shuffling glyph widths. The separator margin needed no change: with the dead space gone, every label now sits 8px from its neighbouring hairline (a chip's own `padding: 0 5px` plus the rule's `margin: 0 3px`) on both sides, so `margin: 0 3px` is left exactly as D578 set it. |
| D584 | **Zero renders as an OUTLINED DOT, not the digit `0`: a `.dl-zero` span, `width/height: 1ch`, transparent with a `1px solid var(--fg-muted)` border and `border-radius: 50%`. D583's always-render rule is unchanged — something always occupies the slot — this only changes WHAT occupies it at zero.** | User: "job 0 and notification 0 is ugly. we can have an outlined dot". Exactly one digit wide, which is the whole point and is exact rather than approximate: `* { box-sizing: border-box }` (base.css) puts the border inside the box, so `width: 1ch` IS the element's advance, and the bar's inherited `font-variant-numeric: tabular-nums` makes one `ch` precisely one digit's advance — swapping the dot for a numeral moves nothing. OUTLINED, never filled, because filled is already taken: `.dl-new-dot` is the filled `--accent` dot meaning "something new arrived", and a second filled dot meaning the opposite ("nothing here") would be a real ambiguity in a 22px strip; hollow + `--fg-muted` reads as absence. A styled element rather than the `○` character, for the reason this project has now learned twice (the round-2 chevron, D578's separator): a glyph's size, weight and baseline vary with the font stack and a border-drawn circle varies with nothing. At 12px, 1ch is ~6.7px, within a pixel of `.dl-new-dot`'s 6px, so the two read as one family of mark. Models is unaffected — it has no count (D575). |
| D585 | **Eight code-review fixes. (1) `useAutoExpandOnNew`'s arrival branch now tests EFFECTIVE visibility (a hoisted `panelOpen`, shared with the drain branch) instead of the persisted `collapsed`. (2) All three docks' `close` — the outside-click/Escape path — is now `forceClose`, writing nothing. (3) Repo-row dismissal keys on a new `repoDismissSignature` (`branch@behind`) instead of `checked_at`; `dismissed` becomes `Record<string, string>`. (4) `update_repo` refuses `not-default` unless HEAD is actually on `default_branch`. (5) Both mutations hold the process-wide check slot via a new `_mutation_slot` (bounded wait, then a `busy` refusal). (6) `_refresh_after_mutation` pops `_checked[root]` alongside `_state[root]`. (7) `useRepoUpdates`' poll gains a generation counter so only the newest invocation schedules. (8) `repoUpdatesSummary` and its now-unused `plural` helper deleted; `community.py`'s `_cache_lock` docstring no longer names deleted callers.** | (1) HIGH, and it defeated D574 outright: on a default install there is no stored key, so `collapsed === false`, and a section that had auto-closed on drain held `override === "closed"` while `collapsed` stayed false — the next arrival matched neither, and the standing override was never cleared, so the section went permanently deaf (no panel AND no dot) for the rest of the session. The same applied to any section D582's arbiter had force-closed. VERIFIED by reverting the fix: the new test fails, then passes. (2) `useDismissOnOutside` fires on any pointer-down outside THIS host, and a sibling chip is outside it — so opening Models wrote `jobs-collapsed = "1"` and `repo-updates-collapsed = "1"`. All three keys converged on "1" and the preference became write-only: the D567 guard breached through the dismiss path rather than through `forceClose`. (3) `check_repo` stamps a fresh `checked_at` on every throttled re-check whether or not anything moved, so a dismissed row returned every five minutes — and having left `visible` it had also left the seen set, so the return read as an arrival and popped the panel open over the user's work. `RepoStatus` carries no HEAD sha, so `behind` (plus `branch`) is the closest honest proxy for "upstream moved" and needs no server change. (4) THE MOST SERIOUS: the endpoint accepts `update` for any known root while the card only OFFERS it when `on_default` was true at CHECK time. On a stale row, `git pull --ff-only origin main` fast-forwarded the branch the user was actually on — and with no local commits it SUCCEEDED silently, moving a feature branch to main's tip and erasing it as a distinct point, while the message said "Updated to origin/main" and named no branch. Verified by disabling the guard: `ok: True` with HEAD on `feature`. Left out of the shared preflight because `switch_repo` legitimately runs off-default. (5) `note_app_opened` starts a fetch that can hold the slot for `TIMEOUT_S`; a mutation inside that window died on `FETCH_HEAD.lock` as an opaque `git-failed`. A bounded wait then an honest `busy` refusal — never an unbounded block (which would hang the request behind someone else's fetch) and never a non-blocking acquire (which would refuse a click that only needed to wait). (6) The stamp outliving the state left `is_known_repo` False while every POST was refused `unknown-repo` for a repo the card had just shown, for a full `CHECK_TTL_S`. (7) `clearTimeout` cancels a pending timer but the fork happens across the `await`: two invocations both in flight each assigned `timer` on the way out, leaking one unclearable chain — the doubling the comment already claimed to have fixed. Superseded responses are now dropped too, so an older read cannot repaint stale rows. (8) Documentation only. |
| D586 | **Failed jobs move from Jobs to Notifications. `jobs.ts` gains `isFailure`/`inFlightJobs`/`failedJobs`; `DownloadManagerView` draws `inFlightJobs(...)` so no `error` row (or its count, or its Clear, or the `.is-failure` tint) appears there any more; the tint moves to the Notifications chip. `QueueDock` forwards the error rows out of the snapshot it already receives (`queue.onJobs`) to `App`, which holds them and hands them to `RepoUpdatesDock` as `failed`, with `setFailedJobs` doubling as `JobRow`'s `onPatch`. Notifications draws repo rows first, then failures as reused `JobRow`s; its count is `visible.length + failed.length`. `useAutoExpandOnNew` gains a 4th `announceOnly` parameter — dot only, never touching visibility — and the failures hook runs in that mode. NO new store, endpoint, or persistence layer.** | User: "maybe we can have a flow like running activities are shown in jobs and after done, a completed message goes to notifications?", scoped to the cheap version: successes still vanish, only failures move. The semantic problem was real — `Jobs` claims work IN PROGRESS, and a `state: "error"` row is not in progress yet sat there indefinitely while being the one thing that actually wanted attention. It needs no subsystem because `fused_render/jobs.py`'s `_sweep` ALREADY keeps `error` rows until explicitly dismissed (where `done`/`cancelled` age out via `first_read_at` + `FINISHED_TTL_S`), so the lifetime, the `dismissJob` endpoint and its `X-Fused` guard all already exist: this is a client-side re-route of rows that are already there. `cancelled` and `done` deliberately do NOT move — a cancel is user-initiated and ages out, a success has its artefact on disk. The filter is applied UPSTREAM of every derived number in `DownloadManagerView` (count, `clearableCount`, the idle predicate, the fold) because a count that still included failures was the likeliest bug in the change. `JobRow` is reused verbatim rather than a new row shape invented: it already draws the title, the failure sentence and the ✕, and it already carries D572's rejected-request surfacing, which is exactly what a dismissal here most needs to keep — and `App`'s own state IS the list `JobRow`'s `onPatch(js => js.filter(...))` wants, so a dismissed failure disappears on the server's confirmation rather than on the next poll. ANNOUNCE-ONLY IS THE ONE PLACE D574 IS WRONG: a background pyramid build failing deserves a mark on the chip, not a panel thrown over the page the user is working in, so failures set the dot and never open (verified by flipping the flag: the test fails, then passes); repo arrivals keep their auto-open. `announceOnly` disables the drain-close too, or an error list emptying would shut a panel the repo rows were still filling. THE TWO DISMISSAL MODELS STAY SEPARATE, deliberately: a repo dismissal is client-side and expires when the repo actually moves (D585 finding 3), a failure's is server-side and permanent, so Clear takes repo rows ONLY and a failure is dismissed by its own row's ✕ — one button cannot honestly promise both. Wired through `App` as plain props because it is the only place both sections are in scope, and `StatusBar` takes them as opaque `ReactNode`s precisely so it never learns what its children are; `QueueDock` calls up only when the error-id SET changes, so the ~1/s poll does not re-render the shell. |
| D587 | **Models never auto-opens. `useAutoExpandOnNew`'s 4th parameter becomes an `AutoExpandOptions` object with two independent opt-outs — `neverOpen` (no auto-open; the dot still fires) and `neverClose` (no auto-close on drain) — replacing D586's single `announceOnly` boolean. `ModelsDock` passes `{ neverOpen: true }`; the failures hook in Notifications passes `{ neverOpen: true, neverClose: true }`. New `autoExpand.test.tsx` covers all three modes at the hook.** | User: "the models popover should never auto open. that is user only". A model becoming resident is a CONSEQUENCE of something the user already did, or of an app quietly loading one — a state readout, not an event worth covering the page for. STRUCTURAL, not merely unlikely: with `neverOpen` set, this section has no code path to `setOverride("open")` at all, so no future arrival can slip through (verified by deleting the guard — three tests fail, then pass). What Models KEEPS is deliberate and is why one boolean was not enough: the quiet dot (costs the user nothing, and "never auto open" is about the panel, not the indicator), auto-close on drain (D580's Unload-the-last-row behaviour was explicitly good — closing is not opening), and its persisted preference reopening it on reload (that IS the user's own choice). Two flags rather than one because the two callers want DIFFERENT halves suppressed, and collapsing them would let a future caller pick the wrong behaviour by accident. KNOCK-ON 1 — D582's tie-break: `SECTION_ORDER` is UNCHANGED and still correct, but which ties can occur has narrowed, so the comment and the tests now separate the two cases rather than asserting one rule that reads as covering both. A reload where two persisted preferences say open can still resolve to Models, legitimately (a saved preference is the user's choice). A same-tick AUTO-OPEN tie is now only ever Jobs vs Notifications, so Jobs wins as the first ELIGIBLE section rather than the first section outright — pinned as its own test. KNOCK-ON 2 — D585 finding 1's fix must not hand Models an auto-open path: verified directly rather than reasoned about, with a test that drains a `neverOpen` section and then delivers an arrival, asserting it goes from closed to closed-with-a-dot and never to open. |
| D588 | **One indicator per chip, and the counts are gone. New `.dl-dot` — a 7px circle, OUTLINED when the section holds nothing and FILLED (`.is-on`) when it holds something — is a DIRECT FLEX CHILD of `.dl-toggle`, replacing `.dl-count`, `.dl-zero` and `.dl-new-dot`, all three deleted along with `hasNew` from `useAutoExpandOnNew` and the `hasNew` prop from all three card views. Models carries no indicator at all this round and keeps label + optional size, with the size now derived from READY models only. `.dl-summary` holds only the label.** | User: "jobs and notifications can have a empty/filled circle - no need for count", then "lets just remove the circle from models item" after "I see many different states for the model item: `Models` / `Models · x GB ●` / `Models ●` ... this is confusing". THE RESERVATION MACHINERY IS RETIRED, NOT LEFT DORMANT: D581 held an empty `2ch` slot so a count appearing could not reflow the bar, D583 replaced it with an always-rendered digit, D584 made the zero an outlined dot — all three were answers to "a count changes width", and one element at one size in two visual states answers it by construction, so `min-width`/`padding-left`/the tabular reasoning are deleted (tabular-nums stays on `.status-bar`; Models' size is the last number in the bar). THE CENTRING FIX FALLS OUT OF THE SAME RESTRUCTURE (user: "check if the circle can be properly centered instead of being bottom aligned vertically"): the old circle sat three deep (`.dl-summary > .dl-count > .dl-zero`) and leaned on `vertical-align: middle`, which aligns to the baseline plus half the parent's x-height — an approximation that sits low and drifts with font metrics; as a flex item of an `align-items: center` parent it is centred on the chip's box with no `vertical-align` at all, and both states being ONE element means they cannot drift apart. 7px explicit rather than `1ch`: `1ch` existed only to match a digit's advance while the circle could swap with one, and that constraint is void — `gap: 6px` kept, which reads correctly at this size. `currentColor` for border AND fill is the whole colour story in one line: the circle inherits the chip's own state, so `--fg-muted` under `.is-idle`, `--fg` ordinarily, `--error` under `.is-failure` on Notifications — a failure tints it and a plain repo update does not, with no second rule to keep in sync. `.dl-new-dot` went because a filled "something new" mark beside a filled "there is something here" circle is the exact ambiguity the outlined zero was chosen to avoid; D586's rule that an error-sourced notification signals WITHOUT opening survives intact, with the filled circle as that quiet signal — and it is now structural rather than configured, since failures are simply absent from the ids the auto-open hook is given, so `hasNew` had no consumer left anywhere and was deleted rather than left dormant. MODELS' LOADING GAP, the third state the user was reading: a worker in venv/starting/downloading/loading carries `residentBytes: null` (and supervisor.py sums the same field, so its `totalResidentBytes` is null too), which made a mid-bring-up chip fall back to the bare label while NOT being muted. The chip now counts ready models only and is keyed on the BYTES — so a ready worker whose runner reported no size cannot produce that state either — while the PANEL still lists a bring-up with its state where the size goes, because bring-up progress properly belongs to Jobs, where `supervisor._report` already gives it a percentage and a cancel. |
| D589 | **The aggregate memory figure leaves the Models chip: the summary is a plain `Models`, the `residentBytes` reduce is deleted, and `idle` keys off `models.length` instead of a byte sum. The PER-ROW `.dl-amount` keeps its figure, deliberately untouched.** | User: "the memory gb next to the models isn't even accurate." They are right, and it was not fixable by arithmetic: `residentBytes` is — per `api.ts`'s own comment on the field — "RSS of the worker process. Not the model's size", so summing it under-reports MLX's allocator pool (which the process may hold without it showing as resident) and over-reports pages shared between workers. Presenting that sum as the models' memory was dishonest, so it is deleted rather than corrected. Keying `idle` off the row list also DISSOLVES D588's ready-vs-loading problem instead of solving it a second way: with no size to fall back from, a bring-up and a fully resident model both simply mean "there is something here", and only genuinely-nothing is muted — which is why D588's two "reads as idle" tests are replaced rather than repaired. The per-row figure stays because the same caveat is much weaker per worker: it is a real, comparable number there, it is the panel's only cost signal, and the panel is where a user goes to decide what to unload. |
| D590 | **One shared indicator for the whole bar: new `platform/ui/StatusDot.tsx` (`<StatusDot on label />`) renders the `.dl-dot` circle, and all chips use it — Models INCLUDED, reversing D588's removal of the circle from that chip alone. The circle carries a worded `role="img"` + `aria-label` ("no jobs" / "jobs running") instead of `aria-hidden`. No chip renders a digit anywhere in its collapsed summary.** | User: "lets just stick to a circle for all items", then "and remember, no count. just a circle outlined or filled". D588's Models-only removal answered a real complaint ("I see many different states for the model item ... this is confusing"), but the user's own remedy is UNIFORMITY across the bar rather than a per-chip judgment — and once D589 took the size off, the states Models can show are exactly the two every other chip has, so there was nothing left for the removal to fix. A COMPONENT RATHER THAN FOUR COPIES because four chips wanting identical geometry is precisely where copies drift: D588's centring bug existed because the circle's wrapper differed from what the CSS assumed, and a hand-written `<span>` in four files is four chances to reintroduce it. It lives in platform since `check-boundaries.mjs` forbids platform importing shell and the chips straddle that line (`DownloadManager.tsx` is platform; the other docks are shell), and its own header states the invariant that keeps the centring: it must remain a DIRECT flex child of `.dl-toggle`, never nested back inside `.dl-summary`. WORDED, NOT HIDDEN, NOT NUMBERED: the circle is the only state a sighted user gets, so `aria-hidden` would make the chip's state sight-only — but the accessible name must not smuggle a count back in either, so the caller passes words and `role="img"` makes it announce as one thing. Audited rather than assumed: every chip's summary is a bare label plus `StatusDot`, and a test pins that no reachable chip state renders a digit at all. The D581/D583 reservation machinery (`.dl-count`, `.dl-zero`, `min-width: 2ch`, the slot padding) is deleted outright, not left dormant — it existed only to stop counts shifting the layout, and a fixed-width slot around a circle is what produced the ragged dead space the user called ugly. |
| D591 | **A fourth status-bar section, Engines, with a Stop per engine. Server: `engine_host.running_engines()` (snapshot under `_lock`, `_alive()` outside it, modelled on `background_running_folders`) plus `GET /api/engines/running` (unguarded read) and `POST /api/engines/{engine_id}/stop` (`X-Fused` guarded) on `routers/engines.py`. Frontend: `shell/EnginesDock.tsx` — a poll, one row per live child, a Stop button — placed BETWEEN Models and Jobs, and `SECTION_ORDER` becomes `["models", "engines", "jobs", "notifications"]`. Tests in `tests/test_engines_api.py`.** | User: "add an item for the background tasks (engine/daemon) thing over here too. I want the user to be able to stop the background engines from here". Everything needed was already in memory — `_children`, `Child.kind/folder/module`, `_alive`, and a `stop()` that already does the whole teardown — so this is a read plus a control, not new machinery. THE HELPER LIVES IN `engine_host`, not the router: the router must not touch that module's lock or its private dict, which is the boundary `background_running_folders` exists to keep, and it is also what keeps the `Popen.poll()` per child outside the lock. NOT ROUTED THROUGH `POST /api/apps/background/stop`, which was the obvious-looking shortcut and is wrong twice: it takes an `html` PAGE path and derives the folder with a `dirname()`, so handing it a folder resolves to the folder's PARENT, and it covers `kind="background"` only — missing template engines and warm app workers entirely. The endpoint's docstring states why Stop is NOT destructive (a template engine respawns on the next `ensure`, a warm app worker on its next call and is idle-reaped on a timer anyway, and a background daemon going down IS the documented "quit this app right now" action) so nobody later mistakes it for one and hides it behind a confirm. Rows are labelled by `basename(folder)` / `module` / `engine_id` in that order, with the KIND shown beside the name so three similarly-named rows stay distinguishable and an unexpected row explains itself. `neverOpen` on its hook, the same call the user made for Models ("the models popover should never auto open. that is user only") — an engine coming up is a consequence of opening a page, not news; drain-close is kept. Engines sits beside Models because both report what is RUNNING RIGHT NOW, where Jobs and Notifications are transient work that appears and resolves. Its test file is `test_engines_api.py`, NOT `test_engines_router.py`: the map template already owns that basename, and with no `__init__.py` in these test dirs pytest's module naming collides — the identical "import file mismatch" that `test_reader.py` already suffers from in this repo, invisible in a standalone run and only surfacing in the full suite. |
| D592 | **`.dl-panel-empty` gains `text-align: center`.** | User: "also make the 'no jobs' etc text center aligned in the popover". One rule covers all four sections because they already share the class — `No models loaded` / `No engines running` / `No jobs` / `No notifications` — which is the payoff of D573 having moved those sentences into one place instead of leaving each chip its own. A single short sentence left-aligned in a 340px panel reads as a stranded fragment; centred it reads as the panel's own state. |
| D593 | **`.dl-row-cancel` becomes a prominent control: a filled `--row-bg-hover` resting background, `--fg` text at 11px (was muted 10px), `padding: 3px 10px` (was `1px 6px`), a `--fg-muted` border, and a hover that moves the BACKGROUND to `--row-bg-active` rather than changing opacity. No `--error`, no red.** | User: "can we make the unload button on the model row more prominent. it is easy to miss right now." It was `.dl-clear`'s quiet outline shrunk down, so at the end of a row full of muted metadata it read as more metadata rather than as a control. DELIBERATELY NOT DESTRUCTIVE STYLING: unloading a model reloads on next use, and every Engines kind respawns (`routers/engines.py`'s own docstring makes that case), so a danger treatment would overstate what these do — the target is "clearly a control", not "warning". Hover moves the background because opacity is not a state change a user can read as engagement, and `--row-bg-active` is the app's existing engaged token (the accent wash a selected sidebar row wears), so no new token was needed. ONE CLASS COVERS ALL THREE ROW ACTIONS — Unload (Models), Stop (Engines) and Cancel (a running job) — so Cancel gets the same lift: making Unload prominent while its neighbours stayed faint would imply a hierarchy between them that does not exist, and would be worse than the status quo the user complained about. |
| D594 | **The model row's memory becomes two figures: the model's real COST as the primary, colour-coded against this machine's ceiling, with live worker RSS in parentheses beside it — `9.2 GB (8.4 GB)`. Server: `supervisor.describe()` adds `footprintBytes`/`footprintBasis` per loaded row (straight from `fit.footprint_bytes`) and `memoryCeilingBytes` once at the top level (new `_memory_ceiling_bytes`: `fit._wired_limit_bytes` first, `machine_ram_gb` second, None third). Frontend: new pure `memoryBand()` + `MemoryCell`, three `.is-mem-{easy,tight,no}` classes over the existing `--success`/`--warning`/`--error` tokens, and `.dl-mem-live` for the parenthetical. The CHIP is untouched — no aggregate comes back.** | User: "lets also color code the memory usage of the model in relation to the user's total memory. if possible lets also add the real resident memory in a parenthesis next to it". NOTHING NEW WAS COMPUTED: `fit.footprint_bytes` already owns the whole precedence ladder (measured > declared > download) and reports which rung answered, `fit._wired_limit_bytes`/`machine_ram_gb` already own the ceiling, and `AiFitVerdict` already established the `basis` vocabulary the AI Models page speaks to the user — so this reuses all three rather than inventing a parallel ladder, new thresholds, or a second word for the same concept. THE ORDERING IS THE SUBSTANCE: primary = cost, parenthetical = live. Reversed, `residentBytes` — worker-process RSS, "not the model's size" by its own field comment, and the exact figure D589 deleted from the chip for being inaccurate — would sit in the position of authority. THE WIRED LIMIT IS THE DENOMINATOR where it exists, not raw total RAM: on Apple Silicon it is the real ceiling a model must fit under, and dividing by total RAM would call a model comfortable while it is about to swap. Carried ONCE per payload since it is a per-machine constant. `no` is the only band with the error colour — a large model that FITS is not an error, which is the distinction `tight` exists to draw. NULL IS NOT ZERO throughout: an unmeasured, undeclared model has no primary figure and falls back to RSS alone, uncoloured; zero would be a lie the page would happily colour green. RECONCILES WITH D589 rather than reversing it: the chip still carries only its circle, per "no count. just a circle outlined or filled" — this makes the per-row numbers accurate, which is the honest version of what the deleted aggregate was reaching for, and is why the temptation to re-sum these new figures onto the chip was declined. One `footprints.load_store()` serves the whole response, which is the cost `footprint_bytes`' own `footprint_store` parameter exists to avoid paying per row. |
| D595 | **Collapsed is the default for all four sections. `loadCollapsed` in `DownloadManager.tsx`, `ModelsDock.tsx`, `RepoUpdatesDock.tsx` and `EnginesDock.tsx` reads `!== "0"` instead of `=== "1"`, and each `catch` returns `true`. Stored values keep their meanings — `"1"` collapsed, `"0"` expanded — so only the ABSENT case moves and no migration is needed. `DownloadManagerView` and `RepoUpdatesDockView` gain an `initialCollapsed?: boolean` TEST SEAM.** | Coordinator's browser pass on a fresh profile: all four panels opened over the page at once, and Engines (newest, so no stored key) sat open and empty over the page content. Two things made the old default worse than when it was written: the D582 arbiter now has to pick ONE winner among four sections that all want to be open, decided by registration order rather than by anything meaningful; and the cost is paid once per section, so a user clicks three or four panels shut — each writing its own key — to reach a quiet bar. "Expanded is the honest default" was true when a chip carried its count and the panel was the only way to see detail; since D588-D590 the chip's circle already says whether there is anything inside, so an auto-opened EMPTY panel communicates nothing and covers the page to do it. THE SENTINEL IS NOT FLIPPED, deliberately: `!== "0"` leaves `"1"` and `"0"` meaning exactly what they meant, so someone who had deliberately expanded a section stays expanded and nobody's stored choice is silently reinterpreted. THE `catch` GOES COLLAPSED TOO — a private-mode profile takes that branch on EVERY load, so it is the one case that never gets to express a preference, and it is also the branch a test environment takes, which is what makes the new default coverable at all. The test seam exists because ~21 tests had been depending on the old default rather than declaring what they needed; they now pass `initialCollapsed={false}` explicitly, and two new tests render WITHOUT it to pin the real fallback. Injectable rather than stubbed via `globalThis.localStorage` for the reason both files already document at length for `mock.module`: a process-wide replacement has contaminated unrelated suites here before. Untouched: the auto-open-on-arrival path, which is transient override state, and every `saveCollapsed` write. |
| D596 | **The job row is relaid out instead of retuned: `.dl-model` moves OUT of `.dl-row-head` onto its own `<div>` between the head and the progress bar (losing `flex: 0 999 auto` and its `min-width` entirely), `.dl-title` wraps to two clamped lines (`-webkit-line-clamp: 2`, `overflow-wrap: anywhere`, no `white-space: nowrap`) while keeping D577's `min-width: 15ch`, and `.dl-amount` rejoins the protected group at `flex: 0 0 auto`. `.dl-pct`/`.dl-row-cancel`/`.dl-x` unchanged. D571's shrink ladder is DELETED, not adjusted.** | User: "we have a ton of free space in the jobs card. why are we truncating stuff instead of placing things elsewhere?" — a running FLUX row rendered `update picture to be ghibli st…` then a lone `F…` then `75%` then `Cancel`, in a card with obvious empty space. The ladder was doing exactly what it was told; the instrument was wrong. It made the row a single LINE and then decided which text loses, when the card was never short of space — the row was short of lines. `F…` is that approach's pathological end: a field reduced to one character plus an ellipsis, conveying nothing while still costing width, and implying recoverable content behind it. THE MODEL IS THE FIELD THAT SHOULD BE RELEGATED, not minced: `jobs.ts`'s own comment already calls it a redundant restatement whenever the title names the model, which was true in exactly this repro. Off the head line it has the panel's full width and needs no shrink factor, no floor and no place in any ladder. THE TITLE WRAPS rather than getting its own line, because the head is only ever as tall as its content — a short title draws no second line and no model line, so there is no ragged hole for the common case (pinned by its own test). `.dl-amount` returns to protected because with the head no longer over-subscribed there is nothing a shrinking amount would buy. `align-items: baseline` on the head is unchanged: CSS aligns a block container by its FIRST baseline, so the controls stay level with the title's first line — stated here because it is the one part of this change that wants a browser's confirmation rather than a test's. The four CSS tests that pinned the ladder's shrink factors are replaced (not retuned) by tests for the new contract, including a MARKUP assertion that `.dl-model` really is outside `.dl-row-head` — the CSS is only correct if the element actually moved, and a stylesheet assertion cannot see that. |
| D597 | **The live memory figure gets a true OS footprint. New `worker_base.os_footprint_bytes()` reads macOS `TASK_VM_INFO`'s `phys_footprint` via stdlib `ctypes` (offset 144, verified empirically), falling back to RSS off darwin and to None where neither reads; reported on `/health` as `os_footprint_bytes`, carried as `Worker.os_footprint_bytes`, exposed by `describe()` as `osFootprintBytes`, and rendered as the row's parenthetical `(24 GB now)`. ADDITIVE: `resident_bytes()`, `peak_resident_bytes()` and `footprints.py` are untouched. `resident_bytes()`'s docstring is corrected.** | User: "i see a 12gb (1.8gb) for flux, but my system monitor says 23gb?" — measured on the live worker: `phys_footprint` 24 GB (23 GB of it dirty `IOAccelerator` regions across 1870 mappings), our cost figure 12.0 GB, our live figure 1.79 GB, `ps` RSS 172 MB. TWO COMPOUNDING ERRORS. First, `psutil` RSS does not see Metal buffers AT ALL, and `resident_bytes()`'s docstring asserted the opposite — "on Apple Silicon the GPU pool IS system memory, so it is the honest single number". The premise (unified memory) is true and the conclusion is false: the pool is system memory and is still charged to `phys_footprint`, not `resident_size`. That comment was load-bearing for the whole design, so it is corrected here even where behaviour is not. Second, `get_active_memory()` is the wrong probe for a "right now" column — after a render MLX returns buffers to its own pool but not to the OS, so active collapsed to 1.8 GB while the process held 24 GB. That exclusion is deliberate and CORRECT for the cost column (`mflux_image/worker.py`'s comment; D310 measured a ~23.6 GB pool against ~14.1 GB active) because torch reports allocated rather than the driver's reservation and runners must stay comparable — it simply does not apply to a live reading. STRICTLY ADDITIVE, and that is the important constraint: `resident_bytes`/`peak_resident_bytes` feed `footprints.py` -> `fit.py`'s "measured" rung and every fit badge, so turning a 12 GB measured footprint into a 24 GB one would silently re-verdict every model the user has ever run (easy -> tight, tight -> no). Whether a measured footprint should include the allocator pool is a real design question and not #877's. `ctypes` rather than psutil because psutil's `memory_full_info()` does not expose this counter on darwin; the offset was verified by measurement, not by counting header fields — `resident_size` at offset 16 matches `ps -o rss` exactly, and dirtying 500 MiB moved offset 144 by 524.6 MB while offset 152 moved by nothing. The two figures are LABELLED (`12 GB (1.8 GB now)`, with a `title` spelling out cost-and-basis vs held-right-now) because two bare byte counts side by side are indistinguishable and they answer different questions. Tests pin the probe's plausibility and that it TRACKS a real allocation (a constant would pass a bare sanity check and still be useless), its clean fallback, and that the field rides through `describe` without coercing null to zero — no test pins a byte count, which varies per machine. |
| D598 | **`.dl-amount` leaves the job row's head line and joins `.dl-status`, dot-joined: `Denoising · 0 / 4`. The status line's guard widens from `(failure ?? status) &&` to a composed `failure ?? [status, amount].filter(Boolean).join(" · ")`, so it renders whenever ANY part exists. `.dl-pct` STAYS in the head. `.dl-amount`'s own rule is untouched — Models and Engines rows still use it.** | User, on D596: "why isn't the step count next to denoising?" — the head read `make it anime styled  0 / 4  0%  Cancel` while the status line read a bare `Denoising`. Same instinct as D596: `0 / 4` is a PROGRESS fact, so it belongs with the other progress facts, and the line already spoke this idiom (it rendered `Denoising · ~4s left` before this round). Split apart, the head carried a number with no context and the status line carried context with no number. THE PERCENTAGE DOES NOT MOVE and is not duplicated into the sentence: it is the one field a user scans down a column of rows to compare, which is what its `tabular-nums` was for. THE GUARD WAS THE TRAP: `amount` and `status` are INDEPENDENTLY present or absent — a download row can carry `4.2 GB / 10 GB` with no phase text and a task row a phase with no amount — so leaving the old gate in place would have silently dropped a download row's byte counts entirely, a straight regression and worse than the problem being fixed. Hence render-if-any and join-only-what-exists, with `filter(Boolean)` so a missing part produces no leading separator. FAILURE PRECEDENCE IS UNCHANGED: a local action's failure takes the whole line and gets NO amount appended, because the sentence is about the button just pressed and " · 0 / 4" after it would read as progress on the failure. Two JobRow tests had asserted "no `.dl-status` element" as their proxy for "no failure"; that premise is gone now that a row with byte counts and no phase draws one, so they assert the absence of the failure TEXT instead — checking for the element would have made them fail for the correct behaviour. |
| D599 | **`refresh_memory` reads `os_footprint_bytes` off `/health` on every poll, beside the two fields it already read. Assigned whenever the worker ANSWERED — including as `None` — unlike the `isinstance`-gated fields around it; a poll that failed outright (`health` falsy) still leaves the previous value standing. NOT fed to `footprints.record`.** | The field was assigned only in the LOAD loop, which exits the moment a worker reaches `ready`, so the value froze at whatever the last load-time poll saw — before MLX had faulted in its Metal buffers. Live: the row read `12 GB (436 MB now)` for a FLUX worker whose real `phys_footprint` was 24 GB, and 436 MB was a genuine reading of a worker that had not yet allocated its GPU pool. `refresh_memory` is the only function that keeps polling a ready worker, so a figure that MOVES after load has to be read there or it is never read again. THE OFFSET WAS EXONERATED FIRST, by measurement rather than argument: a process dirtying 700 MiB reported 749,863,872 B from the probe against `footprint`'s `phys_footprint: 715 MB` — the same counter to the byte. (The earlier 500 MiB experiment could not have separated offset 144 from `internal` or `resident_size_peak`, as the coordinator noted; XNU's layout puts `phys_footprint` after `compressed_lifetime` at 136, and `internal` at 48, so 144 was right for a reason the experiment did not show.) THE `None` RULE differs from its neighbours deliberately: `resident_bytes`/`peak_resident_bytes` feed `footprints.record` -> `fit.py`'s durable measured rung, where holding the last known number through a failed poll is correct; this field is display-only and claims to describe RIGHT NOW, so a worker that answers "no such counter" (the non-Darwin fallback) must clear the cell rather than leave a stale number beside a live one — prefer showing nothing over showing something old. A dropped request is still the transient case and keeps the old value. VERIFIED END TO END on a live FLUX worker after one generation: `/api/ai/runtime` reported 25,676,453,144 B (23.91 GiB) against `footprint -p`'s 24.00 GiB — a 0.36% delta, with RSS at 1.72 GiB confirming it misses the pool entirely. Tests drive `supervisor._health` directly, which is the seam the omission hid behind: the load path populated the field, so every load-path test passed while nothing asserted it survived past ready. One superseded test was deleted rather than repaired — it set the attribute and then called `describe()`, a premise this fix invalidates because `describe()` now re-reads from `/health`. |
| D600 | **The model row becomes `1.8 GB now (24 GB held)`: `residentBytes` leads, `osFootprintBytes` is parenthetical, and the measured cost (`footprintBytes`) leaves the row for the hover `title` alongside its basis and — newly named — the machine ceiling. The colour band is computed from AND painted on the PARENTHETICAL (`.dl-mem-live.is-mem-*`), not the primary; `.dl-amount.is-mem-*` is deleted. `footprintBytes` stays in the payload untouched.** | User's own pick from three options with the trade-offs spelled out, reversing the D594/D597 ordering. Pairing a durable cost with a live reading put two numbers on DIFFERENT TIME AXES side by side, and on MLX they disagreed by ~11 GB with nothing on screen to explain it (13 GB cost against 24 GB held — see D601). RSS and `phys_footprint` are both "right now" and RSS is a strict SUBSET of the footprint, so the pair now reads as one fact ("this much resident, this much held in total") instead of as a contradiction. Labelled in words because two byte figures side by side are otherwise indistinguishable. THE BAND MOVED TO THE PARENTHETICAL — the coordinator's one deliberate deviation from the option as the user read it, flagged to them so they can overrule it: banding the primary would colour a 1.8 GB RSS green while the machine sat at 24 GB of 24 GB, a signal that is actively false at the one moment it matters, a model pinning the machine while glowing "comfortable". Against the ceiling, `osFootprintBytes` is the only figure on this row a colour can honestly answer "how close is this to the limit" for, so the class goes on the figure it DESCRIBES rather than on its neighbour. NO FIGURE, NO COLOUR, and specifically no fallback to banding the primary instead: `osFootprintBytes` is null off Darwin and on any worker without a counter, an uncoloured row is the correct rendering of "we do not know the pressure", and a fallback would look identical while silently changing what the colour MEANS. Three explicit render shapes rather than one clever expression, because the degenerate cases are where a memory cell tells its worst lies — a lone `0`, or a stranded `(… held)` with nothing in front of it; a not-ready worker still shows its STATE, unchanged. Nothing else depended on `footprintBytes` driving a band (checked: `memoryBand` had exactly one caller). |
| D601 | **RECORDED DEBT, deliberately not fixed here: the measured cost figure under-reports by ~11 GB on MLX. `peak_resident_bytes` — which `refresh_memory` writes into `footprints.py` and which `fit.py` reads back as its "measured" rung — is built from RSS and the runner's own probe, and NEITHER sees the Metal pool. Measured on a live FLUX worker: cost 13.16 GiB against a real `phys_footprint` of 24.00 GiB.** | The contradiction is now VISIBLE rather than latent, which is why it is worth writing down: before D597 both displayed figures were wrong in the same direction, so nothing on screen disagreed. The cost figure still drives EVERY fit badge (the AI Models page's easy/tight/no verdicts, the Discover cards, the download-time "will this fit" answer), so correcting it is not a display change — it re-verdicts every model the user has ever run, in the direction of "worse than you were told". Doing it properly needs its own deliberate piece of work: the footprint store migrated or cleared (its entries were recorded under the old definition and cannot be compared with new ones), a decision about whether a "measured footprint" should include an allocator's reservation at all (`mflux_image/worker.py` argues it should NOT, so that runners stay comparable — torch reports what it allocated, not the driver's reservation, and D310 measured a ~23.6 GB pool against ~14.1 GB active), and a pass over every surface that renders a verdict. Kept out of #877 across three separate requests to change it. The user-visible symptom is contained for now: the cost figure is off the row (D600) and lives only in a tooltip, so the two numbers a user compares at a glance are both live and consistent. |
| D602 | **`.dl-head` becomes a FOOTER: it renders after `.dl-rows` in both panels (`DownloadManagerView`, `RepoUpdatesCardView`), its padding tightens `8px 10px` -> `6px 10px`, and it takes over the hairline that `.dl-rows` used to carry as `border-top`. Both existing omission guards are unchanged. The class keeps its name.** | User: "notification UI is messed up" — a full-width padded band holding one small right-aligned `Clear` button, as the FIRST thing in the panel, read as a blank row that had failed to render. It was a header while it also carried a count and a title on its left; D588/D590 removed both and left a header with nothing to head. Under the list the same button reads as acting on what is above it, which is what it does. THE HAIRLINE WAS THE ACTUAL SOURCE of the line that made the band look like a row, and it was on `.dl-rows`, not on `.dl-head` — found rather than counteracted with a new rule. Left in place it would now draw a stray line across the panel's top edge, doubled against `.dl-panel`'s own border, so it moved to `.dl-head` as a `border-top` and does the same job (divide actions from content) from the correct side. `6px` rather than `8px` because `.dl-row` is `8px 10px`: staying under the rows' own padding is what stops a footer reading as one more row. Both panels moved in lockstep and the class was NOT forked — the Jobs footer legitimately holds two buttons (`queue?.cancelAll` and Clear) and nothing here assumes a single child. Placed after `queue?.note` in the Jobs panel so the footer is genuinely last. Models and Engines have no `.dl-head` at all and are untouched; per-row actions stay in their rows. |
| D603 | **The fold is not persisted at all. All four `COLLAPSED_KEY`s (`models-`, `jobs-`, `repo-updates-`, `engines-collapsed`), all four `loadCollapsed`/`saveCollapsed` pairs and every write are DELETED; each section initialises `useState(initialCollapsed ?? true)`. The `initialCollapsed` test seam is KEPT — it is now the only way to mount a section pre-opened. Existing keys on real machines are inert; no migration.** | User: "on page reload the models popover auto opens for some reason". It was NOT the auto-open path — D587's `neverOpen` was intact and D595's absent-key default was collapsed — it was a stored `"0"` from having clicked Models open earlier to read the memory figures, faithfully restored on every load since. Correct per the design and indistinguishable from a bug from where the user sits, which is the third objection to a panel appearing unbidden. THE REASONING IS THAT THESE ARE OVERLAYS, NOT DOCKED PANELS: a `.dl-panel` floats above page content and is dismissed by an outside pointer-down or Escape, which is popover behaviour, and a popover that restores itself across reloads covers the page on every navigation. "Open" is a statement about this moment, not a preference worth remembering. It also makes D582's arbiter trivial instead of arbitrary (nothing wants to be open at mount) and finally makes "never auto open" hold on EVERY path rather than all but one. DELETED, NOT MERELY UNREAD: a key that is written and never read is worse than no key, because the next reader assumes it means something. The D595 comments explaining at length why an absent key meant collapsed are gone with the distinction they described. The seam survives because ~20 tests per file are about what an OPEN panel contains rather than about the default, and with no storage read there is no other way to say so; its doc was rewritten to stop claiming a persisted fallback. The pytest guard got STRONGER rather than disappearing — it used to police WHO may write the fold (a count of `saveCollapsed` call sites); it now asserts no source file in this feature touches `localStorage` for the fold at all, across all four sections, checked on code with comments stripped so the files' own explanations of the removal cannot trip it. THE TRADE-OFF, accepted on the user's behalf: anyone who liked leaving Jobs open to watch a long download now reopens it after a reload. Given the panel covers page content and this is the third objection to unbidden panels, that is the right side to err on, and it is one commit to reverse. Untouched: the transient `autoOpen`/`autoClose` overrides, and `close = forceClose` (D585 finding 2). |
| D604 | **The bulk-action footer requires a PLURALITY, not a presence: `RepoUpdatesCardView`'s guard goes `visible.length > 0` -> `> 1`, and `DownloadManagerView`'s Clear goes `clearable > 0` -> `> 1` (both the band's own guard and the button's). `queue?.cancelAll`'s threshold is UNCHANGED — `queue-dock-lib.showCancelAll` already required `>= 2`. `.dl-head` is not forked; the band still renders if EITHER Jobs guard is true. PART 2 OF THE REQUEST — content-sizing `.dl-panel` — WAS ABANDONED; see below.** | User, with a screenshot of a one-row Notifications panel: "the notification card size is still not done". Measured: `.dl-panel` 340x88 with `.dl-head` at 338x32 — 36% of the card's height for one small button, ~70% of that band empty to its left, and a `border-top` making the emptiness read as deliberate. `Clear` is dismiss-ALL, so at exactly one row it is redundant: that row's own ✕ does the identical thing in one click, adjacent to the thing it affects. THE RULE WAS ALREADY IN THIS CODEBASE — `showCancelAll` has required two withdrawable rows since D562, with the same reasoning written out ("for a single one the row's own ✕ — right there on screen — is the same action with a better name on it"), so this aligns the sibling controls with a decision already taken rather than inventing a threshold. `cancelAll` therefore needed no change, which answers the question asked: the per-row control does cover the single case, and the shell had already concluded so. One repo row plus one failure does NOT earn the footer either — Clear only ever acts on repo rows (D586's two dismissal models), so only those may be counted, and that boundary is now pinned. PART 2 ABANDONED, on evidence rather than taste: `.dl-panel` is `position: absolute`, so its containing block is `.dl-host`, which is content-sized to the CHIP alone (~100-180px). `width: fit-content` resolves as `min(max-content, max(min-content, available))`, and `available` for an abspos box is that containing block — so `fit-content` would clamp the panel toward the chip's width, which is EXACTLY the HIGH bug code review finding #1 already fixed here (its comment names the symptom: "the crushed ~130px panel, ellipsised repo name and wrapped Clear button the screenshot showed"). A `max-width` cannot rescue a value that is already too small, and the proposed `min-width: 240px` would simply pin every panel at 240px — a narrower FIXED width that truncates long titles harder than today and silently undoes D596's wrapping fix. This rule's own comment had also already rejected content-sizing on design grounds: "a card left to its content width jumps every time a row's title or byte count changes length". Part 1 is what recovers the space the user was complaining about. |
| D605 | **The auto-close drain gate is driven by EVERYTHING THE PANEL DRAWS, not by one of its two row sources. `useAutoExpandOnNew` gains `alsoDrawn` — a second id list that counts for OCCUPANCY but never announces — and the seen set is tracked over the union of the two while `arrived` is tested against `ids` alone. `DownloadManagerView` passes `queue.drawn` as `alsoDrawn`, `RepoUpdatesDockView` passes its `failed` job ids, and every identity is PREFIXED per source (`job:` / `queue:` / `repo:`). `neverClose` is DELETED, not kept alongside it.** | Code review 2026-08-28, finding 1 — a real bug in both panels. The gate reads `prev.size > 0 && ids.length === 0 && panelOpen`, which has always MEANT "the panel is genuinely empty, so there is nothing left to hold it open" — but each caller passed only one of its panel's two row lists. Jobs draws the scheduled queue's rows above its job rows, so one live scheduled message plus one download meant the download finishing took `ids` to `[]` and force-closed the panel over the queue rows still rendered inside it, INCLUDING that live turn's only ✕ (the one control wanted at exactly that moment). Notifications draws failures under its repo rows, so pressing Update on the last repo row closed the panel over failure rows the user was reading. NOT FIXED BY REVIVING `neverClose`: that flag suppressed the close unconditionally, which trades a panel that closes too eagerly for one that never closes at all — and D588 had already deleted its only caller. TWO LISTS RATHER THAN ONE UNION, because the announce half must stay narrow: a union fed straight to `trackSeenIds` would make a background failure an "arrival" and auto-OPEN Notifications, which is exactly what D586 forbids ("a background build failing must set the signal, not throw a panel over the page the user is working in"). Splitting occupancy from announcement keeps that promise STRUCTURAL — failures are absent from `ids`, so there is no code path from a failure to `setOverride("open")` — and it is also why `neverClose`'s caller was really asking a question about what the panel DRAWS rather than for a switch. THE SEEN SET IS TRACKED OVER THE UNION, not over `ids`: tracking `ids` alone would shrink `prev` to empty on the tick the first source drained, and the genuine "both sources are now empty" transition a tick later would then be missed entirely, so D580's close would simply stop happening for these panels. PREFIXES rather than trusting two id namespaces not to overlap: a scheduled run's job id is literally `sys:schedule:<entry id>`, embedding the very entry id the queue rows are keyed by, so a collision would put an unseen row into `prev` and silently swallow a real arrival. AUTO-OPEN BEHAVIOUR IS UNCHANGED in every section — verified by test, since widening the occupancy set is precisely the change that could have leaked into the announce set. |
| D606 | **The model row's "held" figure becomes `max(residentBytes, osFootprintBytes)`, and the colour band is computed from that same value. `worker_base.os_footprint_bytes()` applies the same max on the measuring side, reading `resident_size` (offset 16) out of the SAME `task_vm_info` call as `phys_footprint` (offset 144), and is redocumented as a LOWER BOUND rather than as "what the process is holding". The parenthetical is OMITTED when the max makes the two figures equal. `fit.py`, `footprints.py`, `resident_bytes()`, `peak_resident_bytes()` and the AI Models fit badge are untouched.** | Code review 2026-08-28, finding 3. D600's row and `os_footprint_bytes`'s docstring both asserted that RSS is a strict SUBSET of `phys_footprint`, which is FALSE: `phys_footprint` deliberately excludes clean file-backed pages, and those are counted in `resident_size`. Measured on this machine in a plain interpreter with no framework loaded: `resident_size` 19.2 MB against `phys_footprint` 9.3 MB. So for any runner that maps its weights read-only — GGUF/llama.cpp, torch with `mmap=True` — the footprint is the SMALLER of the two by roughly the size of the model file, and the row rendered `8.2 GB now (1.1 GB held)`: a pair that reads as a contradiction, with `memoryBand` then painting "easy" off the smaller number while the machine held the larger. That is precisely the false-comfort signal D600's own comment says the band exists to avoid, arriving through the other door. `max` RATHER THAN EITHER COUNTER ALONE because neither is the total — RSS misses the Metal pool (172 MB of RSS against 24 GB of footprint on a live MLX FLUX worker), the footprint misses clean file pages — so their max is a strictly better lower bound than either and it restores the invariant the pair depends on: "held" is never less than "now". A true total would need a region-by-region walk to add the disjoint parts, which is not worth a per-poll probe; hence the docstring says LOWER BOUND rather than claiming the truth. APPLIED IN BOTH PLACES, deliberately: the worker side is where the two counters can be read from one `task_vm_info` at one instant (so the max cannot straddle two moments, and no psutil is needed on darwin), but `residentBytes` is `resident_bytes()`, which can exceed the kernel's RSS when a runner supplies its own framework probe — so only the display side can guarantee the invariant against the two numbers actually on screen. THE EQUAL CASE OMITS THE PARENTHETICAL: `1.2 GB now (1.2 GB held)` carries no information and sends the reader hunting for a difference that is not there, and a machine with no footprint counter has always rendered the bare figure, so the two degenerate cases now agree. Compared as BYTES, not as formatted strings, since `formatSize` rounds and two genuinely different figures can print alike. NOTHING MOVES IN `fit.py`'s LADDER: `os_footprint_bytes` feeds only `/health` -> `describe()` -> the status-bar row, never `peak_resident_bytes` -> `footprints.py`, so no model's "measured" verdict is re-decided (the D601 under-report stays separately scoped). |
| D607 | **`memoryBand`'s doc stops claiming the status bar and the AI Models fit badge cannot disagree, and its parameter is renamed `footprintBytes` -> `bytes`.** | Code review 2026-08-28, finding 12. The claim was true when written and false since D600: the band's input is now the row's live HELD figure while the badge's is `footprintBytes` off `fit.py`'s ladder, and on MLX those differ by ~11 GB by this branch's own measurement (D601). The THRESHOLDS still match; only the inputs diverge. The comment now says why that divergence is intended rather than a bug to reconcile — the badge answers "can I run this here", a durable claim that must stay stable across a run for someone comparing models on a catalogue page; the bar answers "what is this costing me right now", which must move as the worker allocates or a live colour means nothing. Forcing them onto one input would make one of the two lie. The parameter name was the same staleness in miniature: no caller has passed a footprint since D600, and the one that exists passes a `max` of two live readings. The badge itself is deliberately unchanged. |
| D608 | **The status-bar popover panel fits its content: `.dl-panel` goes `width: min(340px, calc(100vw - 32px))` -> `width: max-content` + `min-width: min(240px, calc(100vw - 32px))` + `max-width: min(340px, calc(100vw - 32px))`.** | User, 2026-08-28, asking a second time — with a screenshot of the Notifications panel holding one repo row (`sandbox [Update] ✕` / `Newer changes available`) sitting in a 340px card with a visible void on its right. D604 had already fixed the matching HEIGHT complaint by making the bulk-action footer require two or more rows; the width was left as a fixed number. The rule's own comment argued FOR the fixed width ("a card left to its content width jumps every time a row's title or byte count changes length") — that reasoning is kept, annotated, and overruled rather than deleted, because the cost is real: a panel's width now varies with its rows. What bounds it is the pair of limits, not the removal of one: a 240px floor and the same 340px ceiling the fixed width used to be, so a panel moves inside a ~100px band. **`max-content`, NOT `fit-content` — a previous attempt used `fit-content` and was correctly abandoned.** `fit-content` resolves as `min(max-content, max(min-content, available))`, and for an absolutely positioned box `available` IS the containing block — here `.dl-host`, content-sized to the chip alone at roughly 100–180px (D568 finding #6), so `fit-content` clamps the panel back toward the chip: the crushed ~130px panel, ellipsised repo name and wrapped `Clear` button of D568 finding #1, reintroduced by a different route. `max-content` is defined as the preferred width given UNLIMITED available space and never consults the containing block, which is why it is the one keyword that works in this position. The `min-width` is load-bearing rather than cosmetic: under `max-content` a block-level child with no intrinsic content contributes nothing to the parent's preferred width, and a job row's `.dl-bar` track is exactly such a child. It carries the same `calc(100vw - 32px)` guard the cap does, because a `min-width` beats a `max-width` per CSS and a bare `240px` floor would push the panel past the viewport edge below 272px. Measured in the running app: every empty panel (Models / Engines / Jobs / Notifications) and the one-row Notifications panel land on the 240px floor (was 340px); a job row with a long model name takes the panel to exactly 340px and wraps its title to the two-line clamp; `.dl-bar` measures 218px in a 240px panel and 318px in a 340px one — it stretches, it does not collapse; `.dl-rows` keeps `overflow-x: hidden` and no panel scrolls sideways. |
| D609 | **Every row's trailing ✕ is pinned to the row's right edge: `margin-left: auto` on `.dl-x`, and on `.q-x`/`.q-spin` as a separate rule.** | User, 2026-08-28, with a screenshot of the card D608 had just narrowed: "the x icon should always be at the very right of the card". In the repo row the ✕ sat immediately after the `Update` button with ~80px of dead card between it and the edge (measured: ✕ right edge 744px inside a panel whose content ends at 824px). The margin goes on the LAST item rather than a `justify-content` on the head, because only the last item may absorb the slack — moving the whole group right is what D573 already rejected when the void was on the other side. It has to be all three trailing controls, not just `.dl-x`: the Jobs panel draws ONE list holding both row kinds, so pinning `.dl-x` alone would leave a queue `.q-row` and a job `.dl-row` disagreeing about where the ✕ column is, which is the exact stepping `.dl-x`'s own 16px-slot comment exists to prevent; and `.q-spin` shares the pin because it stands in the ✕'s slot on a claimed row precisely so the trailing control does not jump as an entry moves queued -> sending. `.q-open` is deliberately EXCLUDED (hence the separate rule rather than joining the `.q-open, .q-x` block): it is not last in the head, and an auto margin there would swallow the slack before the ✕ saw it. One auto margin per head in every case — `.q-x` and `.q-spin` are a ternary, never both. **Costs D608's sizing nothing**, which was the thing to check: an `auto` margin makes no contribution to a box's max-content width, so the panel's intrinsic width is unchanged — the one-row Notifications panel measured 240 x 56 both before and after, with the ✕ moving from 744px to 824px, flush against the row's content edge, while the `Update` button did not move at all (663.97-720.22 both times). The degenerate case agrees rather than jumping: where the row's own content sets the panel width there is no free space, the margin resolves to zero, and the ✕ lands flush anyway (measured on a stalled job row at the 340px cap — ✕ right edge 729.33px against a content edge of 729.33px). Hit area unchanged at 16 x 11, sitting inside the row's 10px padding, so neither `.dl-panel`'s `overflow: hidden` nor `.dl-rows`' `overflow-x: hidden` clips it or its 2px-offset focus ring. Note the download row was ALREADY flush by accident — `.dl-title` is `flex: 1 1 auto` and eats the slack itself — so the rule is inert there today and load-bearing only where the title does not grow (`.q-title` is `flex: 0 1 auto`, D573). Stating it on the ✕ is what keeps that an invariant rather than a coincidence of the title's flex. |
| D610 | **D608's `min-width` comes OFF `.dl-panel` and lands on the two things that actually need it: `.dl-panel-empty` and `.dl-row`, both `min(238px, calc(100vw - 34px))`. A panel whose rows ask for no width now hugs them.** | User, 2026-08-28, on the shipped D608+D609 card: "the update button position looks odd over there", with a screenshot of `sandbox [Update]` at the left and the ✕ far off at the right. **This is D573's complaint arriving from the other side of the row, and that is the trap this entry exists to record.** D573 removed a `flex: 1 1 auto` from `.q-title` because the trailing ✕ "sits after a wide gap of dead space"; D609's edge pin re-opened the identical void, just relocated from title-side to action-side. Putting the title's grow back would group the action and the ✕ at the right edge and re-create precisely the layout D573 rejected in the user's own words — **both sides have now been tried and rejected, so the third fix is not to move the void again but to delete its source.** The source was never the ✕: it was D608's 240px floor holding a 160px card open, and the ✕ merely made the surplus visible by sitting at the far edge of it. With the floor gone the repo panel measures 160.22 x 56 against row content of 138.22px, the gap between `Update` and the ✕ is 8px — the head's own `gap`, i.e. no void at all — and the ✕ is still flush right at 824px against a content edge of 824px, satisfying "always at the very right" and "the gap looks odd" simultaneously, because a zero gap is at the right edge trivially. **What the floor was really protecting turned out to be the EMPTY STATES, not the progress bar** (measured with it removed: "No jobs" collapsed to a 65px card, narrower than the chip that opens it; "No models loaded" 124px; "No engines running" 132px) — so `.dl-panel-empty` keeps a floor. `.dl-row` keeps one too, and that is what floors the bar: `.dl-bar` is a stretching block with no intrinsic width, so it takes the row's content box exactly (measured 218px in a 240px panel, 318px in a 340px one), which means flooring the row floors the bar without a second number on `.dl-bar` itself. `238px`, not 240: `box-sizing` is border-box globally, and 238 plus the panel's 1px borders is the same 240px card D608 drew — Jobs, Models and Engines panels are pixel-unchanged, since model and engine rows are `.dl-row` too. `.q-row` deliberately gets NO floor; it is the only row kind that hugs, which is the whole of the change. D609's pin stays and stays load-bearing: a Jobs panel holding a queue `.q-row` above a job `.dl-row` is floored by the `.dl-row`, and without the pin the queue row's ✕ would float mid-row while the job row's sat at the edge. |


### Claude chat: the screenshot button joins the strip, and the pixels come off the screen (2026-08-27)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D611 | **The chat's screenshot button is ONE seat in the `#anncta` strip, between Comment and Record, and every pane capture tries the native screen shot (`POST /api/capture/shot-region`) before the tab share or the DOM clone.** The camera pill left both composers (`#viewshot` beside Send, `#hviewshot` on the home card) for the strip; `shotPane` gained `shotNativePane` as its first answer, with our overlay hidden for the shot and every refusal falling through to the paths that were there. | *Placement (owner call):* the button acts on the PREVIEW, like Comment and Record, not on the draft — beside Send it read as an attach control for the message; and the strip is the one row the home card and the chat both keep, so one copy serves both composers where two had to be kept in step (`shotBtns`, `applyPaneNoun`). It hides while a mode is armed, like the mic — a mode takes the strip down to a single control — which is the one thing given up: a pane shot cannot be attached mid-comment-mode (leave the mode first). Narrow layout unchanged: hidden in the Chat view (`.viewshot` class kept as that hook), shown in Preview. *Capture:* the same mechanism the shell's "Set Current View as Preview" and export use (`appShot.ts`, AF-11), for the same reasons it replaced tab capture there — no share prompt (so a walkthrough's tenth crop raises no picker and nothing hinges on transient activation), any browser, and WebGL maps are live pixels instead of a blank region with a caveat. The frame's CONTENT box is shot (border trimmed) and drawn at CSS size, so every pin, crop rect and badge stays in the space `annStageRect` already answers in. Rejected: replacing the older paths outright — a hidden pane (narrow Chat view), a window straddling displays, a platform without a still or an ungranted TCC all still need a picture, and the fallbacks are the error handling; hi-res (device-pixel) canvases — would have moved every downstream coordinate for a gain `SHOT_VIEW_EDGE` caps anyway. |
| D612 | **The chat's paste/drop attachments accept ANY file type, not just images: `shotIsImage` stops being a GATE and becomes the switch for three things only — which byte cap applies (`SHOT_ATTACH_MAX_BYTES` 4 MB for a picture, `SHOT_ATTACH_MAX_FILE_BYTES` 25 MB for anything else), whether a thumbnail is made, and which word the `<pane-shot>` block uses. A non-picture is `kind: "file"` and rides the identical pipeline. A drop carrying `application/x-fused-path` is preferred over `files` and attaches the file's REAL path with no upload, the page reporting each such directory in a new `read_dirs` param that `agent.py` validates into one `extra_read_dirs` Read rule per spawn. NO drag source in fused-render produces that payload yet, deliberately. (SPEC PT-16)** | The complaint was exactly one sentence wide: a chat that could be handed a screenshot OF a CSV but not the CSV. The gate was the only thing stopping it — the shots directory, the chip, the viewer, the wire block, the receipt and the pruner were already type-agnostic paths on disk — so removing it cost less than keeping it, and the four caps/glyph/thumbnail/wording branches it now drives are all things that genuinely differ by whether the bytes are pixels. **The cap SPLIT rather than one raised number:** a picture's limit is about the agent's read of the PIXELS (8 MB of PNG shows nothing its 4 MB self does not, and 4 MB is where a phone photo lands), where a log's or a dump's extra bytes ARE the content and the only honest ceiling is the blob-plus-one-upload round trip — one number would have been wrong for whichever half it was not chosen for. **The real path over a copy** for an internal drag, even though copying works: the file is one the user already has at a path they can name, the agent is about to Read it and likely to EDIT it next, and an edit landing in a temp copy the pruner deletes in 12 hours is a silent loss of the user's work — so the path travels and the grant travels with it. `read_dirs` is sent per TURN and not per session because the SPAWN is per turn (every send is a fresh `claude --resume`, `_start`), which is what makes the honest option also the simple one: the grant matches the message it arrived with and expires with it, rather than accumulating over a conversation. `_attach_dirs` re-validates on the python side (absolute, existing, non-root, deduplicated, `_ATTACH_DIRS_MAX` 4) because the param crosses the bridge as a plain string and that is the last stop before it becomes a permission rule; a filesystem root is refused by name, since `Read(///**)` is the whole disk — the blanket rule D146's own test asserts is never emitted. Unparseable is an empty list, never an error: a refused grant costs one permission card, a refused send costs the message. **NOT DONE, and this is the reason rather than an omission:** the explorer half — dragging a listing row into the chat — cannot be built the way it was specified. Those rows carry no `dataTransfer` because they are not an HTML5 drag at all: `listing/row-drag.ts` owns the gesture with pointer events precisely BECAUSE `draggable` is a flag the browser reads when movement begins rather than at pointerdown, which made every press on an unselected row arm a move-drag the marquee sweep could never win (a reported bug). Re-adding `draggable` to get a `dragstart` would reintroduce it, and the two mechanisms are mutually exclusive even in principle — a native drag takes the pointer, so the internal move-drag it would have to coexist with stops receiving `pointermove` — while pointer capture on `documentElement` also means the preview-pane iframe sees no events during the gesture, so a chat framed there could not observe the drag even if a payload existed. Reaching the chat needs a cross-frame channel (a `postMessage` protocol from `row-drag.ts` to the framed template), which is a separate design and was not in scope. The chat side ships anyway rather than waiting: it is the half with the contract in it (`SHOT_PATH_TYPE`, `shotDropPaths`, `shotAttachPaths`, `shotReadDirs`, `read_dirs`, `extra_read_dirs`), it is tested end to end, and any future source — that channel, or another native-drag surface — is then one `setData` call away. Tests: `tests/test_claude_shots.py` (the gate's removal and per-kind extensions, the cap split naming the limit it actually hit, the `"file"` wire sentence and the opener's kind-aware noun, `MARKER_FILE` in `MARKERS`, the real-path drop attaching without an upload or a size, `shotReadDirs` excluding the shots dir and deduplicating, the spawn line's per-directory rule plus `_attach_dirs`'s refusals, the doc-glyph chip and picture-less viewer, and the restored file receipt's "file pruned"). |
| D613 | **An oversize PICTURE is downscaled rather than refused, a refusal says its cause ON THE ROW, and a thumbnail replaces the 🖼 glyph instead of joining it.** `shotAttachFile` probes every image's decodability (`shotPixels`: `createImageBitmap`, then an `<img>` decode), and an image over `SHOT_ATTACH_MAX_BYTES` whose pixels are readable is re-encoded through `shotEncode`'s OWN ladder at `SHOT_VIEW_EDGE` (`shotShrink`; `shotEncode` gained an `out` param so it can report the size the winning attempt used) and uploaded as that copy, keeping the original `name`, taking its extension from `shotExt` of the re-encoded blob, and carrying a `viewNote` that says what was done. An image this engine CANNOT decode falls to the 25 MB file cap (`shotCapFor(file, decodable)`), travels as `kind: "image"` with a `size` and NO thumbnail, and is refused only above that. Every refusal gains a `why` — one short clause, on the wire — printed inline by the new `shotFailLabel` shared by `shotChip` and `shotReceipt`. Both surfaces now draw a picture OR a glyph, never both. (SPEC PT-16) | Two halves of one screenshot the owner sent: a dropped PNG that became "🖼 no image" with nothing on screen saying why, and an image receipt wearing the glyph AND the thumbnail. **The cap was the wrong instrument, not the wrong number:** D612 argues a picture's 4 MB limit is about the AGENT's read of the pixels — which is precisely the argument that a 9 MB photo should be RESIZED, since 1600px of it reads identically and the app's own whole-pane captures are already capped at exactly that edge. Refusing was solving a problem we had a solution to. Reusing `shotEncode` rather than writing a second ladder is what keeps the webp-silently-PNG guard (D-shotExt), the quality-before-resolution order and the three-attempt give-up in ONE place; the `out` param exists because the chip has to name the size that actually won, which `shotFit` alone cannot predict past a halving. **Decodability is probed for EVERY image, not just oversize ones,** because the answer is load-bearing twice and the second use is the glyph bug's root: `URL.createObjectURL` on a .tiff renders an empty box in Chrome — no error, no broken-image glyph, nothing — so a row with both a 🖼 and a hole was the worst of the three possible outputs, and deciding it needs to know whether pixels exist. A thumbnail and a `size` are therefore written as ALTERNATIVES (never both), which is what lets the restore path recognise an undecodable image off the wire alone and give it the file's HEAD-probe treatment instead of an `<img>` that would 404 and blame the pruner for a format. **`why` rides the wire** rather than being parsed back out of `viewNote`: a restored turn rebuilds its receipt from that JSON alone, so without it a refusal that explained itself while the page was open silently reverts to "no image" on reopen — and a regex over a prose sentence breaks on the next sentence anyone writes. **Rejected: keeping the glyph and hiding the thumbnail** — the picture is the more informative icon of the two, and it is the one the user is looking for when scrolling back. **Rejected: a placeholder where the glyph was** — `.annchip` and `.annsum-row` are flex rows aligned to one edge, so a row without a glyph slides its words against that same edge and nothing moves. Tests: `tests/test_claude_shots.py` (the downscale's exact fit, encoder call and `.webp` path against a `.jpg` original; the undecodable image's file cap, absent thumbnail and untouched bytes; `shotFailLabel`'s seven outputs; the picture-XOR-glyph branch in both builders plus the `flex-end` rule it relies on; `why` on the wire; the restore's `bare` test).
| D614 | **A picture NEITHER the browser nor the agent can decode is TRANSCODED SERVER-SIDE, not explained away.** New `agent.py` action `image_to_png`: `path` must resolve inside SHOTS (`_in_shots`, the four-step containment `_in_canvases_root` documents), Pillow decodes it (first frame of a multi-frame TIFF, mode coerced to RGB/RGBA), `/usr/bin/sips -s format png` is the macOS fallback for what Pillow refuses, the longest edge is capped at `SHOT_PNG_EDGE` (1600 — the page's own `SHOT_VIEW_EDGE`), and a sibling `.png` is written beside the original (a JPEG ladder 90→60 `.jpg` where the PNG misses 4 MB). Returns `{path, width, height, bytes, source_w, source_h}` or `{error}`; never raises, deletes nothing. `shotAttachFile` calls it (`shotServerPng`) in the undecodable branch AFTER the upload: on success `view` is the CONVERTED path, `thumb` is `fused.rawUrl(that)`, there is no `size`, and the note says "converted from HEIC to PNG on the server (4032×3024 → 1600×1200) because neither the browser nor the agent can read that format". (SPEC PT-16) **The same ladder also lives in `fused_render/server/image_convert.py`, for the New task modal's upload endpoint, and it is DUPLICATED ON PURPOSE rather than pending de-duplication:** SPEC PY-15/D166 forbid a template importing `fused_render` (the claude agent runs as a subprocess, is deliberately absent from `executor.INPROCESS_HELPERS`, and PY-6a retired the `sys.path` bootstrap that once made such an import resolve), so an `import` here would work in a dev checkout and fail — or silently take a fallback — under the fused engine or a user's own interpreter, which is the exact silent-divergence class D166 exists to end. Written twice and tested once, the same shape `_pane_file`, `_ann_notes` and `_app_dir_for` already have in this file: `tests/test_image_convert_parity.py` pins the two to ONE answer — the four constants (edge, byte cap, JPEG ladder, sips timeout) and byte-identical output for a small PNG, an oversize TIFF and a picture that trips the JPEG ladder. Moving the ladder to `templates/shared/` instead is not available: that layer is stdlib-only (pinned) and this needs Pillow. | Reported with a screenshot of `RGB.byte.tif` and `IMG_4031.HEIC`: both attached, both wearing the 🖼 glyph, both **useless**. D613 wrote the note for that branch on the premise that "the AGENT may well know a format this browser does not" — and for the two formats users actually drop that is simply FALSE: `Read` opens neither, so the attachment was a path to bytes nobody in the conversation could look at, and the sentence explaining the missing thumbnail was soothing the user with a hope the turn would not honour. The page already KNEW (`shotPixels` returning null); it just had nowhere to send the bytes. **Server side because that is where the decoders are** — Pillow is in `[bundled]`, and macOS ships ImageIO behind `sips`, which is the only thing here that opens HEIC (the `pillow-heif` wheel is a compiled dependency we do not ship, and HEIC is the DEFAULT iPhone camera format, so the fallback is the common path and not the exotic one). **A sibling, and the original kept:** the original is what the user actually attached, and the pruner (30d/1000) already owns everything in that directory — a second deletion policy for a second file in the same directory is the only thing here that could lose a picture early. **PNG first, JPEG only on a miss:** scans, screenshots and diagrams are the common TIFFs and are exactly what PNG is good at and what JPEG's ringing ruins, while a 12 MP photo is the common HEIC and is where a lossless 1600px re-encode blows past 4 MB — so the format is MEASURED rather than guessed from the extension. **NO `size` on a converted shot, against the first draft of this:** D613 made `thumb` and `size` alternatives, and the restore path reads exactly that (`bare` = an image carrying a `size` has no pixels anyone can draw) — a converted picture IS drawable, so writing both would have made every reopened turn render it as a probe instead of an image. The thumbnail is the raw-URL of the converted file rather than a `blob:` for the same reason: it is the one of the two that survives a reload. **Failure costs the NOTE, never the attachment** (`shotServerPng` swallows a rejection, an `{error}` and a missing bridge alike): the only thing riding on the call is how good the attachment is, and a picture that vanished because the converter was unhappy would be a worse bug than the one this fixes — what changes on that path is that the note finally says the true thing ("the agent probably cannot read this format — say so rather than guessing at the pixels"). **Rejected: converting on the way IN, before the upload** — the bytes have to be on disk for python to read them, and uploading a converted copy only would have thrown away the original the user attached. **Rejected: a new directory for conversions** — same grant, same pruner, same reader; a second of each for a file that sits next to its source. **NOT DONE:** no conversion on Windows or Linux for formats Pillow refuses (there is no `sips` there and no bundled HEIF decoder), so a HEIC dropped there still gets the honest bytes-and-a-glyph note; and the RESTORE path is unchanged by design — a converted shot restores as an ordinary picture because that is exactly what it now is. Tests: `tests/test_claude_shots.py` (the transcode's chip shape, wire fields and exact note; `shotFormatLabel`'s spellings; a thrown bridge costing only the note; a decodable picture never asking; and on the python side a real TIFF → PNG beside it, the 1600 cap, a multi-frame first frame, CMYK/L/I;16 conversion, alpha kept then flattened by the JPEG ladder, `shots-evil`/relative/outside refusals, junk bytes → `{error}`, and a sips-made HEIC end to end on darwin).
| D615 | **THE 25 MB NON-IMAGE CAP IS GONE. `SHOT_ATTACH_MAX_FILE_BYTES` and `shotCapFor` are deleted, the `blob.size > cap` refusal with them, and no attachment is turned away for its SIZE any more — a non-picture, and a picture no engine here can decode, travel whole at any number of bytes. `SHOT_ATTACH_MAX_BYTES` (4 MB) survives as strictly what D613 made it: the DOWNSCALE TRIGGER for a picture whose pixels are readable, never a refusal. The only refusal left in `shotAttachFile` is an upload that actually failed; `SHOT_ATTACH_MAX` (4 attachments per message) is untouched, being about how much a turn can be READ rather than about bytes. (SPEC PT-16)** | Owner call, 2026-08-27. **The cap protected nothing.** Everything it looked like it was defending was already defended by something else: the shots directory prunes itself (12h / 200 files), so a 40 MB dump is not a lasting cost; the agent's `Read` TRUNCATES a long file at its own end, so the bytes past the useful ones are never read by anyone; and `/api/fs/upload` enforces no size limit of its own (verified — `_fs_upload` does an `await file.read()` with no length check, and the uvicorn config passes no body-size kwarg), so there was no server contract the number was keeping either. What it DID do was refuse the one gesture the whole of D612 exists for: dropping the 30 MB log you want looked at. **The 4 MB number is kept and re-scoped rather than raised**, because D613's argument for it was never about the disk — it is about the agent's read of the PIXELS, which is precisely why an oversize picture is RESIZED to meet it. A limit and a trigger that happen to share a number are two different things, and the branch that read it as a limit is what came out. **Deleted rather than raised:** a cap at 100 MB or 1 GB would be the same bug one drop bigger, and it would still need a chip, a `why`, a test and a sentence in the spec to explain a number nobody could justify. **NOT changed:** the per-MESSAGE cap, which answers a different question (four attachments is about a turn a human and a model can both follow, and a slip of the mouse over a folder of 200 files); and every OTHER refusal path (`shotFailLabel`, `why`, the upload catch), which is the shape a refused attachment wears and still has a live cause. Tests: `tests/test_claude_shots.py` (the absence asserted as an absence — no `const SHOT_ATTACH_MAX_FILE_BYTES`, no `shotCapFor` reader, no "the limit is" and no `blob.size >` left in `shotAttachFile`; a 50 MB undecodable TIFF and a 40 MB `.sql` attached whole and byte for byte; the 4 MB number's two surviving readers being `file.size >` and `shotShrink`; and the upload-failure chip as the one refusal left, with its `why`). |
| D616 | **A file attachment is an ICON everywhere it is small and its OWN fused-render TEMPLATE when it is opened.** The chip and the receipt keep the 📄 glyph, always; the glyph is the door (a real `<button>` on the receipt now too, not only on the pending chip), and `shotViewOpen` frames the file in the template the explorer would give it. The decision is the LEFT PANE's, taken by the left pane's own two functions about a different file: `GET /api/fs/stat` on the attachment, `paneOfferable` (drop `conditional` entries, drop the chat mode, first one wins), then `paneSrcFor` — which grew `file` and `remote` parameters defaulting to the chat's own target, so there is one URL builder and not two. `/render?path=<template>&_file=<attachment>` (+`_remote=1`), plus the shell's two display-only stamps `_preview=1` and `_nofocus=1` and its `THUMB_SEAL` sandbox (`allow-scripts allow-same-origin`, `allow=""`, `tabindex=-1`, `aria-hidden`) mirrored into the vanilla-JS markup. A "loading preview…" line stands until the frame's `load`; every close runs `shotViewUnframe`, which writes `src` back to `about:blank`. (SPEC PT-16)** | Owner call, 2026-08-27: the glyph by default, the preview on the click. A name and a size answer "which file is this" and do not answer **"is this the right file"** — which is the question the viewer exists for, and the one D612's picture-less dialog left standing for every non-image. fused-render already OWNS the answer: there is a template for a .csv, a .md, a .parquet, a .pdf, and the chat is already framing one of them for its target. **/render and NOT /embed**, against the brief's first suggestion: `/embed` serves the React shell, which nests the target one iframe deeper — an extra document, an extra boot and a chrome bar around a preview that has a header of its own here — and this template's own pane comment already rejected it for exactly that reason. Verified live rather than reasoned about: the URL was fetched against the running server for a real `.csv` in the shots directory and rendered the duckdb data grid, columns and row count and all. **Reusing `paneOfferable`/`paneSrcFor` rather than a per-extension table**: a table here would drift from the registry on the next rebinding and ignore a user's own override (§16), and a preview that works in the pane must not be subtly different in the viewer — which is also why `paneSrcFor` took parameters instead of being copied. **The unmount is the load-bearing half, not a tidy-up:** a template is a RUNNING document — a warm python worker behind it, a poll, possibly a map redrawing — so a frame merely `hidden` goes on costing all of that behind a modal the user shut, and Escape is the exit most likely to be used and least likely to be tested. `about:blank` rather than removing the element, so the layout and the `load` listener stay wired. **NO iframe in a chip or a receipt (owner call):** a composer holding four attachments would boot four templates — four workers, four first renders — to draw four icon-sized pictures nobody can read at that size, which is the cost of a thumbnail without any of a thumbnail's value. **An IMAGE keeps the picture viewer:** a template is a worse view of pixels than the pixels. A refusal (`view` null) has nothing to frame, and a file the pruner has deleted stats as an error, so both land on exactly today's name/path/size dialog — every "no preview" answer is a `null` and never an error panel. Tests: `tests/test_claude_shots.py` (the stat-based decision under node, including the chat mode and a `conditional` entry skipped, the `_render` sentinel, the `_remote` hint and five distinct paths to `null`; the frame URL's exact shape and both stamps; the sandbox read OUT of `frame-focus.ts` so the mirror cannot drift; the unmount on every exit and before every re-open; the loading line and its `load` handler; the receipt glyph's door and its button reset; and `<iframe` appearing exactly twice in the file). |
| D617 | **EVERY REMAINING ATTACHMENT RESTRICTION IS GONE. `SHOT_ATTACH_MAX` (4 attachments per message) and `_ATTACH_DIRS_MAX` (4 Read grants per spawn) are deleted, and with them the `room`/`slice` rationing and the "N more pictures" overflow chip in both `shotAttachFiles` and `shotAttachPaths`. Every dropped or pasted file attaches, sequentially, each with its own chip; every real-path attachment's directory becomes its own `Read` rule. `_attach_dirs` still VALIDATES (absolute, existing, non-root, not the shots dir, deduplicated) — it just no longer counts. `SHOT_ATTACH_MAX_BYTES` (4 MB) remains what D613/D615 made it and nothing more: the downscale trigger for a readable picture, never a refusal. (SPEC PT-16) | Owner call, 2026-08-27: "this is all local, remove all restrictions". **The count cap refused the one gesture it was written for.** D615 kept it on the argument that four attachments is about how much a TURN can be read — but the turn is not the page's to ration: dropping a folder of screenshots is a deliberate act by someone sitting at the machine that holds both the files and the agent, and answering it with 4 uploads and a chip saying "26 more pictures" made the composer the obstacle. Nothing was being protected — the shots directory prunes itself (12h/200), `Read` truncates at the agent's end, and `/api/fs/upload` enforces no limit — so, exactly as with D615's 25 MB, the number came out rather than up: a cap at 8 or at 40 is the same bug one drop bigger, and it would still need a chip, a `why`, a test and a spec sentence to justify a number nobody can. **`_ATTACH_DIRS_MAX` goes for the same reason and one more:** the bound it was defending was never the cap's doing — the dirs DEDUPE, so thirty rows out of one folder was always one rule, and the pathological case (thirty rows, thirty folders) is a long argv string on a local tool the user drove by hand, not an unsafe one. Refusing the grant only ever cost them a permission card per attachment. **What deliberately does NOT change:** the pane shot's single seat (D285 — "the pane, now" has one answer, and that is a fact about the subject, not a cap); the 4 MB downscale trigger; and every refusal path that still has a live cause (an upload that failed, `shotFailLabel`, `why`). Tests: `tests/test_claude_shots.py` (the absence asserted as an absence — no `SHOT_ATTACH_MAX =`, no `-attachment cap` string, no `room`/`slice`/overflow branch left in either attach function; thirty dropped files becoming thirty chips, thirty uploads and zero `why`s; thirty dragged paths becoming thirty attachments in one repaint; and on the python side no `_ATTACH_DIRS_MAX` attribute plus twelve real folders yielding twelve rules). |

### New task modal: any file, no caps (2026-08-28)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D618 | **The New task card takes ANY FILE at ANY SIZE, through paste and drop only, and previews a non-picture in its own template.** Deleted: `IMAGES_MAX` (4) on both sides — the constant, the client's pre-upload race check and `schedule._images`' count check — the endpoint's `_SHOT_MAX_BYTES` (4 MB) and its image-only MIME gate, and the ＋ picker (`nt-img-add`, its hidden `<input type=file>` and its CSS). `POST /api/schedule/shot` is MULTIPART (`UploadFile`) behind the same `X-Fused` guard, still server-minted name + a sanitised client extension (MIME map first, filename second), and answers `{path, kind: "image"｜"file", width?, height?}`. The transcode/downscale ladder moved into `fused_render/server/image_convert.py` (`ext_for`, `is_image`, `browser_blind`, `sips_to_png`, `transcode`, `dimensions`) and runs SERVER-SIDE for both cases: a picture no browser draws (`.tif`/`.heic`/`.psd`/raw…) and any image over 4 MB come back as a `-view.png` (JPEG ladder past the budget), 1600px longest edge, original kept. Chip: thumbnail for a picture, 📄 + a 12ch-clipped filename for a file, same footprint, same ✕, thumbnail XOR glyph. Viewer: a picture as before; a file gets `/api/fs/stat` → the first non-chat, non-`conditional` template → a sealed `/render?path=…&_file=…&_preview=1&_nofocus=1` iframe (`THUMB_SEAL` + `thumbUrl`, both IMPORTED), "loading preview…" until `load`, and the conditional render IS the unmount on Close/Escape/scrim. `_attachments_block` now says "Attached files (read them with the Read tool):". (SPEC SCH-17) | Owner call, 2026-08-28, and the parity half of D612–D617: the chat could be handed a 40 MB log and the task card — the surface whose whole job is "do this to these files later" — refused a fifth screenshot and any `.csv` at all. Every argument D615/D617 made against the chat's caps holds here verbatim (the shots directory prunes itself, `Read` truncates at the agent's end, the upload endpoint enforces nothing), so the numbers came out rather than up. **MULTIPART because the reason for the data URL expired:** the old comment justified base64 by "the form already holds the image as a data URL for its thumbnail" — it does not any more, since the chip's thumbnail is a `blob:` URL and only a drawable picture gets one, so what was left was a 33% tax paid twice on the gesture the change exists for. `UploadFile` is optional in the signature on purpose: a required `File(...)` makes a JSON body a 422 raised BEFORE our code, which would have answered a cross-origin POST without the D3 guard. **SERVER-SIDE downscale for BOTH cases, against the brief's offer of a canvas in the modal:** the two branches are one call from python and two code paths in the browser — and one of those paths cannot exist, since a canvas cannot resize bytes it cannot decode. Doing it where the bytes already are keeps the modal at one `FormData` and puts the 1600px edge in ONE place for the whole app. **The converted copy is `-view.png`, not a same-name sibling** as in `agent.py`: there the original was always a format PNG could not collide with, here a 6 MB `.png` would be asked to overwrite itself. **`kind` is the server's and the client only guesses:** the chip appears before the upload answers, so a `.tif` wears the glyph for a moment and becomes a thumbnail when a drawable path comes back — the alternative (waiting for the POST to draw anything) is a card that looks broken on a slow write. **The ＋ went, not the row:** paste and drop are the gestures every user of this card makes, and a dashed square standing permanently in a chip row was chrome for the one who does neither. **The `images` key STAYS on the wire and in the store** though it now holds anything: every entry written before today spells it that way, and renaming it would buy a migration for a word. **NOT DONE:** `agent.py` still carries its own copy of the ladder (it was being edited concurrently) — it should import `image_convert` and keep only `_in_shots`, since authorising the path is the caller's job and the only half that must not move. Tests: `frontend/src/shell/new-task-images.test.ts` (31 — the caps' and the picker's absence asserted as absences, `attachmentKindOf`'s TIFF-is-a-file rule, `taskPreviewSrcFor`'s exact URL, the skipped chat/`conditional` entries, the `_render` sentinel, the `_remote` hint and six paths to `null`, the multipart body, the server's `kind` overriding the guess, thumbnail-XOR-glyph, exactly one `<iframe>`, and the seal imported rather than mirrored) and `tests/test_schedule_images.py` (21 — the guard before the 400 on a non-multipart body, any type and any size stored byte for byte, the sanitised extension, a Pillow-made TIFF transcoded to `-view.png` at the 1600 edge with the original beside it, the over-budget PNG's `-view.jpg`, junk bytes costing the conversion and never the file, seven attachments scheduled, and the block's new wording). |
| D619 | **A SCHEDULED TASK'S ATTACHMENTS ARE HANDED TO THE RUN AS THE CHAT'S OWN `<pane-shot>` BLOCK, not as a plain-text tail — and the entry carries the filename and the kind, not only the path.** `_attachments_block` now writes `<pane-shot>`: one sentence (the chat's own `"image"`/`"file"` wording, minus the `"pane"`/`"overview"` kinds a headless run cannot have) then the JSON array `[{kind, view, name, viewNote: ""}]`, and `_composed` puts it BETWEEN the `<live-app-state>` block and the user's words — `composeOutgoing`'s order. New `attachments` field on create (`[{path, name, kind}]`, same realpath containment as `_images` via one shared `_shot_path`, `kind` in {image,file}, `name` basenamed and length-capped), stored on the entry, copied onto every occurrence, sent by the card beside `images` and read back by an Edit (`restoredAttachments`). Either field alone works: `images` is derived from `attachments`, and `attachments` from `images` with a basename and an extension guess (`_derived_attachment`, mirroring the card's `DRAWABLE_EXTS`). | **Reported with a screenshot:** opening a fired task's chat showed the user's turn ending in `Attached files (read them with the Read tool): /Users/…/task-shots/…pdf /…png` — raw temp paths, in the bubble, as if the user had typed them — while the SAME chat renders its own attachments as receipt rows (📄 name, a thumbnail, click → viewer). Same files, two presentations, and the unreadable one was the one the user never chose. **Speak the chat's wire rather than invent a shape:** `template.html` already reads this block back on restore (`paneShotIn` → `shotRestoreReceipt`, which gives an image `fused.rawUrl(view)` and a file a HEAD probe — `/api/fs/raw` serves the task-shots dir, verified by curl), and `pane-shot` has been in every machinery strip list all along (`tasks_store._MACHINERY_STRIP`, `agent.py`'s copy, `sessionTitle`'s `BLOCK_OPENERS`), so nothing had to learn anything — the scheduler just had to stop writing a shape only it knew. **The name and the kind travel because a path cannot carry them:** the stored name is a minted timestamp and a transcoded `.tif` is a picture whose extension says otherwise, both facts known in the browser at attach time and nowhere else; a receipt row built from the path alone would show `20260828-102517-94794d2f.pdf` and put an `<img>` on a `.csv`. **`images` KEPT, not renamed:** every entry on disk spells it that way, `_send`'s Read grant reads it, and a migration bought for a word is a migration nobody asked for — so the new field rides beside it and each is derived from the other. **A stored entry is never re-validated** (`_stored_attachments`): `_attachments` refuses a file that has gone, which is right for a request and fatal for a tick — a task can fire days later with its files moved, and one deleted attachment must not raise inside `_materialize` and take every other task down with it. **The tag is duplicated, not imported** (PY-15/D166, the fourth copy in the system) with the parity test D146 asks for: it reads `PANE_SHOT_TAG` out of `template.html`, and a second test pins the extension list against the card's `DRAWABLE_EXTS`. **Rejected:** teaching the chat to render the old tail — a second wire format for the same fact, read by one writer, when the first already renders; putting the block after the message — every leading-block strip in the system would read it as the user's own words; carrying pixels instead of paths — the reason a repeat can re-read its images every run with nothing in the store. **NOT DONE:** the `Read` grant on task-shots is granted for the SPAWN, so a follow-up turn typed into the resumed chat carries only its own `read_dirs` and a later re-read of a task attachment cards like any other outside Read. Left alone deliberately — the receipt row's own viewer opens the file without the agent, and a standing grant minted from a transcript is a bigger decision than this bug. Tests: `tests/test_schedule_images.py` (block shape, payload always an array, the three-way noun, no pane/overview claim, block order, the legacy-path fallback including `.tif`→file, a stored entry never re-validated, junk tolerated, both wire fields and either alone, containment + symlink, kind and name validation, the occurrence copy, the two parity tests), `tests/test_claude_shots.py::test_the_scheduler_writes_a_block_this_page_can_read` (python writes, node reads: `paneShotIn` round trip, `stripBlocks` leaving only the words, `sessionTitle` naming the row with a marker), and `frontend/src/shell/new-task-images.test.ts` (the wire pair, the Edit restore). |

### Markdown template redesign: no mode, outline as a left rail (2026-08-28)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D620 | **The markdown template drops its read/write mode entirely — REVERSES D186. `writable` (SPEC MD-15) becomes the only editability gate; `edit`/`#toggle-edit`/`editWanted()`/`editing()`/`applyEditMode` are deleted outright. The outline moves OUT of the one right sidebar and becomes a permanent left rail (`#outline-rail`), open by default via a new `outline` param (absent = open, `"0"` closes, mirroring `graph`/`depth`), 15rem wide and resizable 12–30rem, with an accent 2px active-heading marker driven off the editor's own scroll/update events (no timer). The right sidebar's graph section is drawn only when the note has a resolved outbound link or a backlink — otherwise absent, not an empty canvas. Four features added: an image lightbox (click to expand, ←/→/Esc/backdrop, navigating the SOURCE in doc order rather than the DOM so it is correct past the CodeMirror viewport); real per-language fenced-code highlighting (`codeLanguages` wired into `CM.markdown`, plus a light-mode `HighlightStyle` built from this view's own tokens, since `oneDark` only covers dark) with a block border/corner treatment, a language chip and a hover-reveal copy button; a selection format bubble positioned by hand from `coordsAtPos` (bold/italic/strike/code/link, all calling the existing `toggleWrap`/`insertLink`, plus "Turn into"); and a `/`-menu block-insert as a fourth `autocompletion` source, sharing one `convertLines(kind)` with "Turn into". The vendored CodeMirror bundle gained three exports it did not have — `HighlightStyle`, `syntaxHighlighting`, and (found necessary once building an actual style: there is no way to define one without it) `tags` from `@lezer/highlight`. Separately, folded into this build on the owner's word after the palette read "warm brown on a cool blue page": the template's dark-mode `--fg`/`--fg-muted`/`--border`/`--bg`/`--bg-alt`/`--selection` (and light's matching set) move from an Obsidian-scale approximation to the app shell's own tokens (`frontend/src/styles/tokens.css`) — verified `--ghost` still reads fainter than `--fg-muted` against the new, darker `--bg` (contrast 3.21:1 vs 6.98:1, actually improved from 2.90:1 against the old `--bg`), so it was left unchanged. FOLDED IN, three more owner-flagged rounds: (1) the sidebar toggle moves OUT of a floating corner cluster (now deleted, `#corner`/`.corner-btn`) into each panel's own head row, mirrored from the outline rail rather than duplicated — one shared set of classes (`.panel-head`, `.panel-collapse-btn`, `.panel-reopen-btn`), identical DOM order in both, with `.mirror` (`flex-direction: row-reverse`) the only difference between the rail's button landing at its right (inner) edge and the right panel's at its left (inner) edge; a floating button belonged to neither the panel nor the document, and visibly overlapped both. (2) The outline head's heading COUNT is removed entirely ("Outline 21" read as an ordinal — "what is outline 1?" — not a quantity, and the outline is never capped, so the count told the eye nothing it did not already see); the right panel's BACKLINKS count stays, because that number answers something the visible list cannot (it can be capped or scrolled), but it is now a separate `.panel-count` pill rather than bare text after the label, so it cannot be misread the same way. (3) The selection bubble's bold/italic/strikethrough stop being traced SVG letterforms (a hollow-outline `B` reads as a damaged shape at 14–16px, not as bold) and become real characters carrying the actual formatting (`font-weight: 700`/`font-style: italic`/`text-decoration: line-through`, sized by eye against the link icon rather than at nominal size) — the control demonstrates its own effect, which no icon can; inline code becomes a small `.lp-code`-idiom chip around the backtick pair that produces it, replacing an abstract rounded-rectangle outline that communicated nothing. FURTHER FOLDED IN, two more rounds: (4) `.mirror`'s `flex-direction: row-reverse` (note (1) above) turned out to reverse the LABEL too, not just the button — "BACKLINKS" landed at the panel's outer edge instead of reading from the left, disagreeing with every left-aligned line below it. Fixed with `order: -1` on the button alone (`.panel-head.mirror .panel-collapse-btn`), which moves only the chevron; the label/count/spacer stay in normal reading order. A real specificity bug was found the same round: `#fmt-turn-btn` (0,1,0,0) lost silently to the base `#fmt-bubble button` rule (0,1,0,1) — the exact same shape as the `#graph-sec[hidden]` bug two rounds earlier, an id-scoped rule outranking a later, less specific override with no error — fixed by qualifying the ancestor id (`#fmt-bubble #fmt-turn-btn`) rather than `!important`, and the same fix applied to one more instance found on a sweep (`#fmt-turn-menu button`(`:hover`), which was only winning by source-order luck against the same base rule). (5) The default widths come down again — 21rem/15rem to 18rem/12rem for the right panel and the rail (owner: "the 2 sidebars are taking a lot of space") — and the joint clamp (D620 (folded in already) `layoutPanels`) gains a SECOND, gentler layer ahead of its existing 20rem hard floor: a 35rem document COMFORT target that shrinks either panel toward its own MIN, never closing one, spent before the hard floor and only when the panels' DEFAULTS would eat into it — free above the document's 46rem cap, so it costs nothing on a wide window and only narrows the panels in the middle range where a note is read beside an open chat panel, which is what both owner reports were actually about. FURTHER FOLDED IN again: links stopped activating on click across this whole redesign, traced to D620 itself rather than to any edit of the link code (`git diff main...HEAD` shows the link decorators, `markdownLinkWidget`, the `.lp-link` CSS and the delegated click handler all untouched) — a note used to open read-only by default, and a native `<a>` activates on click only in a NON-editable CM view; now that a writable note is always editable, the surface is `contenteditable`, where a click on an anchor places the caret instead of following it, standard browser behaviour rather than a CodeMirror quirk. Local vault links kept working throughout because `markdownLinkWidget` already hands them `data-path` and the existing delegated click handler opens them in JS regardless — external/absolute/mailto/bare-`#anchor` links (`markdownLinkWidget`'s early-return branch) and bare autolinks (`pushUrl`, MD-24) had no such JS path and went silently dead. Fixed by extending the SAME delegated handler (SPEC MD-31) rather than reinstating any native-click reliance. Two PRE-EXISTING bugs surfaced and fixed alongside it, unrelated to D620 but newly visible because the owner was looking at links: a nested image-in-link (`[![alt](img)](url)`, GitHub's badge/shield pattern) never rendered — the plain-link regex's label group cannot see a `]` inside its own label, so the construct fell through and stayed as raw text — fixed by asking the TREE for an outer Link whose `getChild("Image")` shape means "the label is one Image," not by growing the regex into a bracket-balancer (SPEC MD-31a); and a link destination containing a balanced paren (`Foo_(bar)`, the Wikipedia case) was silently truncated by a `()`-excluding URL-group character class in three places, now `\S*` in all three (SPEC MD-31b). THREE MORE FOLDED IN, from Cursor Bugbot review of PR #909, all confirmed genuine by reading the source before fixing: (1) the image lightbox never moved DOM focus off the editor, so typing while it was open still edited the ALWAYS-EDITABLE document underneath (MD-1a) and the 2s idle autosave could commit it, and arrow-key paging raced CodeMirror's own keymap because the overlay's `keydown` listener was bubble-phase on `window` — strictly AFTER CM's own contentDOM handlers, too late to stop anything. Fixed by actually moving focus into the overlay on open and back to the editor on close, plus a capture-phase listener that calls `stopPropagation()` on every key while open as defense in depth, plus Tab-cycling among the overlay's own controls (SPEC MD-27). (2) The panel-width grip's drag and arrow-key handlers read `getBoundingClientRect()` — the CURRENT RENDERED, possibly comfort-clamped or hard-floored box — as the base for computing and then PERSISTING a new width, so nudging a grip while a panel sat narrower than its own preference silently overwrote that preference with the clamped number. THE THIRD INSTANCE of one pattern on this branch (after `graph=0` written by a presentational close, and `#side-reopen` shipping `hidden` backwards) — fixed structurally rather than as a fourth spot-fix: both grips now compute off `desiredRailPx`/`desiredSidePx` (the stored INTENT, untouched by clamping) instead of the rendered box, and the invariant this enforces is now stated once in the code as a comment rather than left implicit (SPEC MD-19d). (3) The fence chip's copy button closed over the BODY STRING it was built with; CM reuses a widget's DOM (and its stale closure) whenever two builds collide on the reuse key (language + body length + first 40 characters), which is not rare — any edit past character 40 that leaves the length unchanged collides — so the button could silently copy stale code, the worst failure mode for a copy button since nothing looks wrong. Fixed by having the click handler read the CURRENT document at click time via the widget's own live position (`enclosingFence`+`fenceLangAndBody`, reusing the exact `posAtDOM` idiom `taskWidget` already established) rather than tightening the key further, which stays a hash and can always collide again. Behaviour-tested headlessly (`tests/test_markdown_live_preview.py`): two fences built to deliberately COLLIDE on the reuse key, each still reporting its own correct current body. (1) and (2) need real interaction and are left for the owner to verify.** | Owner's call, 2026-08-28: no mode, a writable note is editable by default, in exchange for accepting the risk D186 was written to avoid — the 2s-idle autosave can now rewrite a note the instant it is opened, with no lock to catch a stray keystroke on a file you meant only to read. D186 itself is not wrong about that risk; it is superseded by a deliberate trade the owner made with the risk named, not discovered as a side effect. The outline's move off the right sidebar is a REVERSAL of MD-19a/MD-19b's placement rule ("a section of the one right panel, not a second panel and not a second toggle") for a reason MD-19a itself gave the right panel: a permanently visible affordance for a subject that always exists (a note's structure) does not need to be gated behind a toggle at all — which is also why it defaults open rather than merely being reachable in one more click than before. The graph's new condition follows the design note's own rule directly ("chrome appears only when its subject exists") and keeps MD-11a's distinction intact: an ABSENT graph section is silent, but the backlinks notice above it still explains an unscanned root in graph.py's own words, so "nobody looked" is never conflated with "there is nothing" by omitting the graph's reason too. The bundle's third export (`tags`) is a deviation from the plan's literal text, which named only `HighlightStyle` and `syntaxHighlighting` as the gap — but a `HighlightStyle` is a set of `{tag, ...}` rules, and there is no tag namespace inside the template without importing one; the alternative (hand-rolling CSS class selectors keyed to CodeMirror's internal highlight class names) is more fragile, not less, so `tags` was added and is reported here rather than silently smuggled in as if the plan had already said so. |

### `fused.ai.video` takes a reference image (2026-08-28)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D621 | **`fused.ai.video` gains a SINGLE reference image, `image`: one string, conditioning at frame 0 with strength 1.0, no per-image frame index or strength, no `images` list. The default canvas DERIVES from that image's own aspect ratio, snapped to a multiple of 64 (the engine's own two-stage grid) rather than the app's ordinary 32-multiple video rail — and `/api/ai/video` accepts `base` for the first time, since a page-relative `image` needs it.** | User-scoped request: the engine already supports full I2V conditioning (`DistilledPipeline.generate_and_save`'s `image` kwarg reaches real per-stage conditioning at `distilled.py:129,188-201,328-334`, verified against the pinned `ltx-2-mlx` sha), and the app was not passing it through. Multi-anchor conditioning — several reference images, each with its own frame index and strength — is real capability upstream but UNVERIFIED on this app's own hardware in this cut, and a control surface for it would be exactly D413's own failure mode: an option accepted and either ignored or wrong in a way nobody watching the render would notice. Scoped down to the one case AI-9f already shipped for `/api/ai/image`'s edit `image` — a single path, resolved once, no array to reason about — rather than build the general surface first and discover the narrow gate later. Path resolution (page-relative-to-`base`, existence, is-a-file) is NOT a third hand-rolled copy: `_resolve_reference_image` in `ai_runtime.py` is shared by both routes, since a second copy of `/api/ai/transcribe`'s own `path` rule was already one too many. The 64-multiple snap is not cosmetic: LTX's two-stage pipeline re-derives its own output size via `snap_output_dimensions(..., two_stage=True)`, which is COARSER than the 32-multiple grid `_video_side`/`_clamp_video_canvas` already enforce for every video request — a derived default that stopped at 32 would still get silently re-snapped by the engine, and the reply would then be echoing a canvas nobody actually rendered. `registry.VideoTraits.supports_image` (an ENGINE fact, `ltx-video`'s row `True`) travels through `catalog.py`'s `videoTraits` payload so the Playground's reference-image control cannot appear for a resolved engine that would refuse it — the same "a control cannot show a value the render won't use" rule AI-15 already states for the frame/canvas/step sliders. `imageio-ffmpeg`'s PATH plumbing (`worker.py:271-312`) already exists for exactly this CRF round-trip and has never been exercised end to end; this PR does not change it, and a live conditioned render is the check that finally does. |

### Explorer companion pane: `_noopen` replaces the overloaded `_preview` flag (2026-08-28)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D622 | **`_preview=1` on the explorer's companion pane (claude/git/mcp, `ListingPreviewPane.tsx`) is replaced by a new reserved param, `_noopen=1`. `render.py`'s recording guard now honours either flag, and its Referer-inheritance check does too; `runtime.js` and `IS_THUMBNAIL` are untouched — the companion simply stops sending the flag they read.** `_preview=1` was doing two unrelated jobs at once: telling `GET /render` not to `record_app_open` (D301's `_preview != "1"` guard, and `_referred_by_preview`'s Referer-inheritance for a page it iframes directly), and telling `runtime.js` "this document is a picture" (`selfOrAncestorHasFlag` → `IS_THUMBNAIL`, D496/D507's display-only contract). The companion pane only ever wanted the first job — the second is actively wrong for it: it is fully interactive (you type in it), and `IS_THUMBNAIL` gates `fused.daemon.start/stop/restart/setAutostart/call` and degrades `fused.daemon.watch` to one one-shot read. Because `IS_THUMBNAIL` is read off any same-origin ANCESTOR, not just `self`, the damage reached two levels down: the claude template's own `#leftframe` — an ordinary app, its own `src` carrying no flag at all — inherited `_preview=1` from the companion document above it, so every `fused.daemon` call inside it was silently rejected and `watch()` never polled. The identical page worked when opened as a real, unwrapped page, because there nothing upstream ever set the flag. **The fix is a second flag, not a smarter first one**, matching the existing `_nofocus` convention (reserved, `_`-prefixed, one job, inherited the same way): `render.py`'s guard is now `_preview != "1" and _noopen != "1" and not _referred_by_suppressed_open(referer)` (the renamed `_referred_by_preview`, now checking `_preview=1` OR `_noopen=1` in the referring URL's query — an app the companion iframes directly must still not record an open, which is the one behavior this flag exists to protect and must not regress). `runtime.js` gets no new branch: `_noopen` is invisible to it by design, so `IS_THUMBNAIL` goes false for the companion and everything nested in it, restoring `fused.daemon.*` — the entire point. **The params-climb consequence, investigated and found to need no containment.** With `IS_THUMBNAIL` false, `runtime.js`'s `findTarget()` (D46) now climbs from the companion instead of refusing outright. The climb stops one step below the nearest `_fusedParamBoundary`-flagged ancestor (set by `Panel.tsx`/`Tabs.tsx`, TM-3/D72) — so in split/tab layout the target is the Listing pane's own `/embed` window, and in a plain, unsplit explorer view (no boundary above Listing at all) the target is Listing's own top-level window. **Both are exactly the target the sibling main preview iframe (the non-companion `_render` template next to it) already writes to today, flag or no flag** — the companion joining that is not a new leak, it is the companion finally behaving like every other iframe Listing hosts, which is the documented, accepted D46/D72 trade-off ("pages wanting pane-private state namespace their own keys — documented, not enforced"). Two things bound the actual risk further: `isReserved()` in `runtime.js` refuses to `set()` any `_`-prefixed key or (when standalone) `path`, so the folder-listing's own reserved state (`_side`, `_file`, `path`, `_layout`) can never be clobbered by a companion-framed app's `fused.params.set`; only a NON-reserved, custom key could land on Listing's visible URL, and that was already true for the main preview pane. Deliberately **not** addressed by adding a boundary on Listing's own window: `_fusedParamBoundary` is a property of the whole containing window, not of one child iframe, so flagging Listing to wall off the companion would also wall off the main preview iframe's own climb — turning an already-correct, already-shipped behavior into a regression to fix a leak that does not exist. **`_referred_by_preview` is renamed `_referred_by_suppressed_open`** (its job is "was this open already suppressed", not "was this a preview") and its docstring updated to name both flags. Tests: `tests/test_apps_api.py::test_a_noopen_render_records_nothing` / `test_a_render_referred_by_noopen_records_nothing` (parallel to the existing `_preview` pair, both pairs still pass — neither flag's behavior regressed); `tests/test_sessions_inbox_open_dir.py::test_the_companion_iframe_is_marked_noopen_not_preview` (new — pins the literal `&_noopen=1` in the pane's src and the absence of `&_preview=1`) and the pre-existing literal-source assertion in that file updated for the new flag name (SPEC FS-10/FS-11, PT-16). | The two-job flag was never wrong on its own terms (D301, D496, D507 each solved a real problem with it) — it just accreted a second meaning nobody re-examined once a THIRD caller (an interactive pane, not a card and not a thumbnail-viewer) started sending it for the first of the two jobs only. Splitting the flag rather than teaching `runtime.js` to special-case the companion (e.g. "trust `_preview` for recording but not for `IS_THUMBNAIL` when inside `#leftframe`") keeps `runtime.js`'s contract exactly what D496/D507 already documented and testable with one grep instead of a conditional on frame identity. Rejected: reusing `_preview` and adding a THIRD flag to opt back into live daemon calls (e.g. `_interactive=1`) — that inverts the polarity for no reason, since the display-only behavior is the one that needs an opt-IN and the companion is the only caller that would ever set it; two positive, independent flags (`_noopen`, `_preview`) compose more simply than a base flag plus a countermand. Rejected containing the params-climb with a boundary on the companion's OWN document from the Listing side — investigated and found unnecessary (see above), and the existing reserved-key gate (`isReserved`) already does the only containment this change could plausibly need. |
| D623 | **Publish (SPEC §48, `docs/PUBLISH.md`): an app goes to the author's own hosting, starting with Cloudflare Pages. The seam is a `PublishAdapter` Protocol plus a registry whose whole offer rule is set membership over a two-axis capability grid — one *runtime* cell (`runtime:js` / `runtime:pyodide` / `runtime:cpython`) and one *state* cell (`state:none` / `state:client-local` / `state:server-backed` / `state:shared`), a target covering a region of it. Eligibility (`publish/eligibility.py`) runs the REAL exporter first, so `ExportPlan.errors` (EX-3/EX-4) are the blockers — the gate §18 has defined since Deploy was removed and nothing consumed until now. Whether the app's imports exist in Pyodide is answered by `publish/pyodide_imports.json`, a checked-in projection of the pinned `pyodide-lock.json` regenerated by `scripts/refresh_pyodide_imports.py`, not by a curated list. `publish/site.py` turns a bundle into a static site that carries its own interpreter: the payload verbatim at the root (EX-1), `_fused/` for `runtime.js` / `boot.py` / a VERBATIM copy of `_binding.py` (D167) / `site.json` / icon / web manifest / the Pyodide core and every wheel in the dependency closure, and `fused-sw.js` at the site root. The harness is written to `/fused-runtime` in the browser FS rather than `/app`, with `sys.path` taking it first. State is **seed-then-overlay**: seed the Pyodide FS from the published files every boot, keep only the DIFFERENCES in `localStorage` (with `null` tombstones), so an app persisting with plain `open()`/`os.replace` publishes UNCHANGED and a re-publish lands new content on top of the reader's existing progress. Origin stability is enforced three times (a record in the app's own `.fused/data/publish.json` per §47, an adapter that refuses to deploy when the recorded project is gone, and a run that refuses to rename a live app's project); **Forget** is the escape hatch and deletes nothing at the provider. Auth is `wrangler login` — browser OAuth, run by the author — and there is deliberately NO API-token field; `auth()` is read-only and may never open a browser, `login`/`deploy`/`forget` carry `X-Fused`. A publish is a background RUN (`publish/runs.py`), one per (app, target), polled by phase at `/api/publish/run`, kept 15 minutes after finishing. The Publish tab is a seventh entry in `APP_PAGE_TABS`; every reason, note and phase label it shows comes down from the server unmodified. A QR of the canonical URL is drawn by a ~300-line in-repo encoder (`frontend/src/shell/qr.ts`, byte mode, level M, versions 1–6) rather than a dependency.** | Owner's direction, 2026-09-06 (issue #2, parent #1): start with Cloudflare, one PR covering all four layers, Pyodide bundled into the deployment rather than pulled from a CDN, a generated lettermark when the app ships no `icon.svg`, and the owner — not this session — runs `wrangler login` and the first real publish. The grid is two axes rather than a flat list of tiers because runtime and state fail INDEPENDENTLY: an app can need Python and no state, or plain JS and a shared document, and a tier list forces a false ordering between them. Set membership as the entire rule is what makes "a target is disabled because its adapter is not written yet, never because the provider is incapable" (parent #1) structural rather than a claim in a doc — a provider with no adapter is simply absent from the registry, and the missing CELL is what the disabled row names. Seed-then-overlay was chosen over the two obvious alternatives after both were rejected for the same reason: snapshotting the whole Pyodide FS into `localStorage` makes a re-publish either clobber the reader's progress or be invisible to them (the snapshot shadows the new files), and asking authors to rewrite persistence against a `fused.*` storage API would mean the app that motivated this — a spaced-repetition deck whose `store.py` uses `open()` and `os.replace` — could not be published without being changed, which is the opposite of what a publish seam is for. Vendoring Pyodide rather than pointing at jsDelivr: a published app that stops working when a CDN moves a path is not something the author can fix, and the whole point of a static site with its own interpreter is that it has no dependencies at run time. `loadPackage` by NAME with wheels beside `pyodide-lock.json`, not by wheel URL — the URL form silently reaches for the network the moment a package has a dependency, which is a failure that only appears on someone else's phone. The run store is process memory and NOT §36's Activity dock: the subprocess dies with the server anyway, so persisting a run status would only preserve a claim about work nobody is doing, and the one thing that must survive (where the app was published) is the §47 record. Rejected an API-token field outright, per the issue: it is a worse experience, a worse security posture, and it would make fused-render the custodian of a long-lived credential for an account it has no other relationship with — the `mounts`/rclone posture (drive a tool the user installed and signed into) is the precedent, and §1's non-goal is untouched. Rejected a QR npm dependency for ~300 lines of finite-field arithmetic that will never change again; it is verified by a test that READS the symbols back (unmask, walk the placement, check the Reed-Solomon parity, decode the header), which is a check against the spec rather than against the encoder. |

### Publish: the ICP asset canister, and funding as its own seam (2026-09-06)

| # | Decision | Rationale / rejected alternatives |
|---|---|---|
| D624 | **Publish rung 1, second target (SPEC §48 PB-17..PB-24, `docs/PUBLISH.md`): an ICP asset canister, plus `FundedTarget` — the seam for a target that costs money rather than asking for an account.** Built ALONGSIDE Cloudflare rather than after it: an adapter interface validated against exactly one backend is not a seam, it is that backend's assumptions with indirection in front, and the second implementation is what proves the first one's design. ICP is the only target with no provider account at all — no signup, no card, no company that can close the author's account or delete their app. `publish/icp.py` (`icp-canister`) covers the same four cells as Cloudflare and drives the author's own icp-cli exactly as `cloudflare.py` drives wrangler: `FUSED_RENDER_ICP`, then `icp` on `PATH`, then `npx --yes @icp-sdk/icp-cli@1`, then `unavailable` with the install guide. Canonical URL `https://<canister-id>.icp0.io`; mainnet only (`-e ic`), never a local replica. `icp deploy` wants a project manifest and `site.py` produces a bare directory, so a minimal `icp.yaml` is synthesized per publish in a temp dir and deleted — which makes icp-cli's own `.icp/data/mappings/ic.ids.json` OURS to keep, seeded back from the record before every re-publish (without it each one looks like a first). **Funding is a second Protocol, not a fourth `AuthState`**: `auth()` answers `ready`/`unavailable` only, and `FundedTarget` (`funding()` / `create_identity()` / `cycles(record)`) carries the rest, with the Fund cycles panel keyed on `isinstance(adapter, FundedTarget)` so a target with nothing to fund cannot grow an empty one. One identity Fused-wide, created on the FIRST Fund cycles press, key in the OS keyring. Seed phrase shown ONCE: read via `--output-seed` into a `0600` file unlinked before the call returns, crossing the API in exactly one response, held in component state and dropped on an explicit *I've saved this*. `PublishError.salvage` + a `runs.py` write on failure, so a canister minted before a failed upload is recorded rather than abandoned. Insufficient cycles recognised specifically and answered with the principal and the transfer command; a pre-flight balance check runs before anything is spent. Cycles readout as a RUNWAY (balance ÷ `idle_cycles_burned_per_day`), §47 **cache** in `<app>/.fused/cache/` (`publish/cycles.py`), refreshed at most daily and shown stale-with-a-timestamp rather than hidden. | Rejected a fourth `AuthState` for funding: it is not authentication, there is nothing to sign into, and a disabled row saying "not signed in" gives the author nothing to act on — it is a publish precondition with its own affordance, which is also where a future Buy cycles onramp lands (explicitly out of scope). Rejected per-app identities: funding a fresh principal for every app is the friction that stops an author after the first; one principal, one balance, topped up once. **Reversed an earlier draft of the issue that had Fused capture the mnemonic into its own keyring and reveal it from Preferences** — that would make us the custodian of a portable, unrevokable credential able to move real money, breaking the seam's founding constraint on the one target where user-ownership IS the pitch; "never a seed phrase in a text field" is about INPUT, and printing one once at creation is output. Rejected reading the phrase off stdout with `-q`: every adapter here hands the whole captured blob to a failure formatter, so a phrase on stdout is one bad exit away from an error message, a log, and a pasted issue report. Rejected password-encrypted and plaintext key storage (a prompt on every publish; a money-moving key in a readable file) — a keyring we cannot reach is an `unavailable` with a reason, never a silent fallback. Rejected bundling `icp` in the payload the way D103 staged rclone: D459 already refused that for ffmpeg, one adapter's Rust binary must not tax every install, and the npm fallback is the scoped version of D103's "remove the prerequisite" — a lazy pinned download into the tools dir on first use is the next step if a machine with neither `icp` nor Node turns out to be common. Rejected treating an unreadable balance as zero: refusing a publish the author has already paid for is worse than letting the deploy report its own failure. Rejected putting the cycles reading in §47 `data/`: it is rebuildable by asking again, and the canister id — which is not — already lives in the record. Rejected a modal for the seed phrase: one that dismisses on a stray click is the wrong container for something that cannot be shown again. Warning kept serious but not apocalyptic, because losing the phrase is not losing the funds (`icp identity export` emits a PEM from the keyring at any later time) and an apocalyptic warning about a recoverable situation is how you teach people to ignore warnings. **Open, and flagged rather than assumed**: whether an npx-launched `icp` reaches the same keyring item as an installed one (the failure mode is an author who appears to have lost their principal after tidying up their machine), and whether icp-cli's `--json` covers the commands we parse — `_principal()` asks two flag shapes and every parser reads JSON first and falls back to the table, so neither answer can silently lose an identity. |
